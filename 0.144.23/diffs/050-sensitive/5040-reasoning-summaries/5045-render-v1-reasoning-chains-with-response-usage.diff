diff --git a/codex-rs/tui/src/app/agent_status_feed_tests.rs b/codex-rs/tui/src/app/agent_status_feed_tests.rs
index d8ca1b1f7d..00298a1d15 100644
--- a/codex-rs/tui/src/app/agent_status_feed_tests.rs
+++ b/codex-rs/tui/src/app/agent_status_feed_tests.rs
@@ -71,6 +71,9 @@ fn agent_status_uses_reasoning_summaries_only() {
                 id: "reasoning-with-summary".to_string(),
                 summary: vec!["safe summary".to_string()],
                 content: vec!["hidden raw reasoning".to_string()],
+                response_id: None,
+                reasoning_output_tokens: None,
+                continued: false,
             },
             thread_id: "thread-child".to_string(),
             turn_id: "turn-1".to_string(),
@@ -83,6 +86,9 @@ fn agent_status_uses_reasoning_summaries_only() {
                 id: "reasoning-without-summary".to_string(),
                 summary: Vec::new(),
                 content: vec!["raw-only reasoning".to_string()],
+                response_id: None,
+                reasoning_output_tokens: None,
+                continued: false,
             },
             thread_id: "thread-child".to_string(),
             turn_id: "turn-1".to_string(),
diff --git a/codex-rs/tui/src/chatwidget.rs b/codex-rs/tui/src/chatwidget.rs
index 078c0e66c7..ce97b02fcb 100644
--- a/codex-rs/tui/src/chatwidget.rs
+++ b/codex-rs/tui/src/chatwidget.rs
@@ -667,6 +667,9 @@ pub(crate) struct ChatWidget {
     reasoning_buffer: String,
     // Preserves reasoning-summary part boundaries for transcript-only recording.
     reasoning_summary_parts: Vec<String>,
+    // Exact response-level reasoning usage accumulated across one V1 continued chain.
+    pending_reasoning_output_tokens: Option<i64>,
+    pending_reasoning_item_ids: Vec<String>,
     // Associates streamed assistant deltas with their commentary/final presentation channel.
     agent_message_phases: HashMap<String, MessagePhase>,
     status_state: StatusState,
diff --git a/codex-rs/tui/src/chatwidget/constructor.rs b/codex-rs/tui/src/chatwidget/constructor.rs
index 78c2dfd4e6..f99c15cf2f 100644
--- a/codex-rs/tui/src/chatwidget/constructor.rs
+++ b/codex-rs/tui/src/chatwidget/constructor.rs
@@ -180,6 +180,8 @@ impl ChatWidget {
             interrupts: InterruptManager::new(),
             reasoning_buffer: String::new(),
             reasoning_summary_parts: Vec::new(),
+            pending_reasoning_output_tokens: None,
+            pending_reasoning_item_ids: Vec::new(),
             agent_message_phases: HashMap::new(),
             status_state: StatusState::default(),
             review: ReviewState::default(),
diff --git a/codex-rs/tui/src/chatwidget/input_flow.rs b/codex-rs/tui/src/chatwidget/input_flow.rs
index 07c8b2bbc1..f7ea1b707d 100644
--- a/codex-rs/tui/src/chatwidget/input_flow.rs
+++ b/codex-rs/tui/src/chatwidget/input_flow.rs
@@ -44,6 +44,8 @@ impl ChatWidget {
                     // Reset any reasoning header only when we are actually submitting a turn.
                     self.reasoning_buffer.clear();
                     self.reasoning_summary_parts.clear();
+                    self.pending_reasoning_output_tokens = None;
+                    self.pending_reasoning_item_ids.clear();
                     self.set_status_header(String::from("Working"));
                     self.submit_user_message_with_user_prompt_submit_hook(
                         user_message,
diff --git a/codex-rs/tui/src/chatwidget/replay.rs b/codex-rs/tui/src/chatwidget/replay.rs
index 03795b42b9..c991672989 100644
--- a/codex-rs/tui/src/chatwidget/replay.rs
+++ b/codex-rs/tui/src/chatwidget/replay.rs
@@ -179,7 +179,13 @@ impl ChatWidget {
             }
             ThreadItem::Plan { text, .. } => self.on_plan_item_completed(text),
             ThreadItem::Reasoning {
-                summary, content, ..
+                id,
+                summary,
+                content,
+                response_id,
+                continued,
+                reasoning_output_tokens,
+                ..
             } => {
                 if from_replay {
                     let reasoning_parts = summary.into_iter().chain(
@@ -196,7 +202,12 @@ impl ChatWidget {
                         self.on_agent_reasoning_delta(delta);
                     }
                 }
-                self.on_agent_reasoning_final();
+                let is_v1_reasoning = response_id.is_some();
+                self.on_agent_reasoning_final(
+                    is_v1_reasoning.then_some(id),
+                    is_v1_reasoning && continued,
+                    is_v1_reasoning.then_some(reasoning_output_tokens).flatten(),
+                );
             }
             item @ ThreadItem::CommandExecution {
                 status: codex_app_server_protocol::CommandExecutionStatus::InProgress,
diff --git a/codex-rs/tui/src/chatwidget/slash_dispatch.rs b/codex-rs/tui/src/chatwidget/slash_dispatch.rs
index f9ead0caa5..246304b311 100644
--- a/codex-rs/tui/src/chatwidget/slash_dispatch.rs
+++ b/codex-rs/tui/src/chatwidget/slash_dispatch.rs
@@ -734,6 +734,8 @@ impl ChatWidget {
                 if self.is_session_configured() {
                     self.reasoning_buffer.clear();
                     self.reasoning_summary_parts.clear();
+                    self.pending_reasoning_output_tokens = None;
+                    self.pending_reasoning_item_ids.clear();
                     self.set_status_header(String::from("Working"));
                     self.submit_user_message(user_message);
                 } else {
diff --git a/codex-rs/tui/src/chatwidget/streaming.rs b/codex-rs/tui/src/chatwidget/streaming.rs
index 6bde30c6b1..4bc04cab5f 100644
--- a/codex-rs/tui/src/chatwidget/streaming.rs
+++ b/codex-rs/tui/src/chatwidget/streaming.rs
@@ -258,19 +258,49 @@ impl ChatWidget {
         self.request_redraw();
     }
 
-    pub(super) fn on_agent_reasoning_final(&mut self) {
+    pub(super) fn on_agent_reasoning_final(
+        &mut self,
+        reasoning_item_id: Option<String>,
+        continued: bool,
+        reasoning_output_tokens: Option<i64>,
+    ) {
         // At the end of a reasoning block, record transcript-only content.
         if !self.reasoning_buffer.is_empty() {
             self.reasoning_summary_parts
                 .push(std::mem::take(&mut self.reasoning_buffer));
         }
-        if !self.reasoning_summary_parts.is_empty() {
-            let reasoning_parts = std::mem::take(&mut self.reasoning_summary_parts);
-            let cell = history_cell::new_reasoning_summary_block(reasoning_parts, &self.config.cwd);
+        if let Some(tokens) = reasoning_output_tokens {
+            self.pending_reasoning_output_tokens = Some(
+                self.pending_reasoning_output_tokens
+                    .unwrap_or_default()
+                    .saturating_add(tokens),
+            );
+        }
+        if let Some(item_id) = reasoning_item_id {
+            self.pending_reasoning_item_ids.push(item_id);
+        }
+        if !continued
+            && (!self.reasoning_summary_parts.is_empty()
+                || !self.pending_reasoning_item_ids.is_empty())
+        {
+            let mut reasoning_parts = std::mem::take(&mut self.reasoning_summary_parts);
+            if reasoning_parts.is_empty() {
+                reasoning_parts.push("**[No reasoning summary]**".to_string());
+            }
+            let cell = history_cell::new_reasoning_summary_block_with_usage(
+                reasoning_parts,
+                &self.config.cwd,
+                std::mem::take(&mut self.pending_reasoning_item_ids),
+                self.pending_reasoning_output_tokens.take(),
+            );
             self.add_boxed_history(cell);
         }
         self.reasoning_buffer.clear();
-        self.reasoning_summary_parts.clear();
+        if !continued {
+            self.reasoning_summary_parts.clear();
+            self.pending_reasoning_output_tokens = None;
+            self.pending_reasoning_item_ids.clear();
+        }
         self.request_redraw();
     }
 
@@ -299,6 +329,8 @@ impl ChatWidget {
         }
         self.reasoning_buffer.clear();
         self.reasoning_summary_parts.clear();
+        self.pending_reasoning_output_tokens = None;
+        self.pending_reasoning_item_ids.clear();
         self.status_state.remember_retry_status_header();
         self.bottom_pane.ensure_status_indicator();
         self.status_state.terminal_title_status_kind = TerminalTitleStatusKind::Thinking;
diff --git a/codex-rs/tui/src/chatwidget/tests/helpers.rs b/codex-rs/tui/src/chatwidget/tests/helpers.rs
index b0d1a9e9e7..187c8472e7 100644
--- a/codex-rs/tui/src/chatwidget/tests/helpers.rs
+++ b/codex-rs/tui/src/chatwidget/tests/helpers.rs
@@ -633,6 +633,9 @@ pub(super) fn handle_agent_reasoning_final(chat: &mut ChatWidget) {
                 id: "reasoning-1".to_string(),
                 summary: Vec::new(),
                 content: Vec::new(),
+                response_id: None,
+                reasoning_output_tokens: None,
+                continued: false,
             },
         }),
         /*replay_kind*/ None,
diff --git a/codex-rs/tui/src/chatwidget/tests/history_replay.rs b/codex-rs/tui/src/chatwidget/tests/history_replay.rs
index d5510ac0d2..c4a5503847 100644
--- a/codex-rs/tui/src/chatwidget/tests/history_replay.rs
+++ b/codex-rs/tui/src/chatwidget/tests/history_replay.rs
@@ -1126,6 +1126,9 @@ async fn replayed_reasoning_item_preserves_summary_parts_and_hides_raw_reasoning
                 "**Checking tests**\n\n<!-- -->".to_string(),
             ],
             content: vec!["Raw reasoning".to_string()],
+            response_id: None,
+            reasoning_output_tokens: None,
+            continued: false,
         },
         "turn-1".to_string(),
         ReplayKind::ThreadSnapshot,
@@ -1174,6 +1177,9 @@ async fn replayed_reasoning_item_shows_raw_reasoning_when_enabled() {
             id: "reasoning-1".to_string(),
             summary: vec!["Summary only".to_string()],
             content: vec!["Raw reasoning".to_string()],
+            response_id: None,
+            reasoning_output_tokens: None,
+            continued: false,
         },
         "turn-1".to_string(),
         ReplayKind::ThreadSnapshot,
@@ -1188,6 +1194,120 @@ async fn replayed_reasoning_item_shows_raw_reasoning_when_enabled() {
     assert!(rendered.contains("Raw reasoning"));
 }
 
+#[tokio::test]
+async fn replayed_v1_reasoning_chain_renders_once_with_response_usage() {
+    let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+    let _ = drain_insert_history(&mut rx);
+
+    chat.replay_thread_item(
+        AppServerThreadItem::Reasoning {
+            id: "rs_opaque-a".to_string(),
+            summary: vec!["**First**\n\none".to_string()],
+            content: Vec::new(),
+            response_id: Some("resp-1".to_string()),
+            reasoning_output_tokens: None,
+            continued: true,
+        },
+        "turn-1".to_string(),
+        ReplayKind::ThreadSnapshot,
+    );
+    assert!(rx.try_recv().is_err());
+
+    chat.replay_thread_item(
+        AppServerThreadItem::Reasoning {
+            id: "rs_opaque-b".to_string(),
+            summary: vec!["**Second**\n\ntwo".to_string()],
+            content: Vec::new(),
+            response_id: Some("resp-1".to_string()),
+            reasoning_output_tokens: Some(37),
+            continued: false,
+        },
+        "turn-1".to_string(),
+        ReplayKind::ThreadSnapshot,
+    );
+
+    let rendered = match rx.try_recv() {
+        Ok(AppEvent::InsertHistoryCell(cell)) => {
+            lines_to_single_string(&cell.transcript_lines(/*width*/ 80))
+        }
+        other => panic!("expected InsertHistoryCell, got {other:?}"),
+    };
+    assert!(rendered.contains("one"));
+    assert!(rendered.contains("two"));
+    assert_eq!(rendered.matches("opaque-a").count(), 1);
+    assert_eq!(rendered.matches("opaque-b").count(), 1);
+    assert!(!rendered.contains("rs_opaque"));
+    assert_eq!(rendered.matches("37 tokens").count(), 1);
+}
+
+#[tokio::test]
+async fn reasoning_usage_distinguishes_missing_from_zero() {
+    let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+    let _ = drain_insert_history(&mut rx);
+
+    for (id, tokens) in [("missing", None), ("zero", Some(0))] {
+        chat.replay_thread_item(
+            AppServerThreadItem::Reasoning {
+                id: id.to_string(),
+                summary: vec![format!("**{id}**\n\n{id}")],
+                content: Vec::new(),
+                response_id: Some(format!("resp-{id}")),
+                reasoning_output_tokens: tokens,
+                continued: false,
+            },
+            "turn-1".to_string(),
+            ReplayKind::ThreadSnapshot,
+        );
+    }
+
+    let missing = match rx.try_recv() {
+        Ok(AppEvent::InsertHistoryCell(cell)) => {
+            lines_to_single_string(&cell.transcript_lines(/*width*/ 80))
+        }
+        other => panic!("expected missing-usage history cell, got {other:?}"),
+    };
+    let zero = match rx.try_recv() {
+        Ok(AppEvent::InsertHistoryCell(cell)) => {
+            lines_to_single_string(&cell.transcript_lines(/*width*/ 80))
+        }
+        other => panic!("expected zero-usage history cell, got {other:?}"),
+    };
+    assert!(!missing.contains("tokens"));
+    assert!(zero.contains("0 tokens"));
+}
+
+#[tokio::test]
+async fn empty_v1_reasoning_chain_renders_one_placeholder() {
+    let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+    let _ = drain_insert_history(&mut rx);
+
+    for (id, continued, tokens) in [("rs_empty-a", true, None), ("rs_empty-b", false, Some(23))] {
+        chat.replay_thread_item(
+            AppServerThreadItem::Reasoning {
+                id: id.to_string(),
+                summary: Vec::new(),
+                content: Vec::new(),
+                response_id: Some("resp-empty".to_string()),
+                reasoning_output_tokens: tokens,
+                continued,
+            },
+            "turn-1".to_string(),
+            ReplayKind::ThreadSnapshot,
+        );
+    }
+
+    let rendered = match rx.try_recv() {
+        Ok(AppEvent::InsertHistoryCell(cell)) => {
+            lines_to_single_string(&cell.transcript_lines(/*width*/ 80))
+        }
+        other => panic!("expected placeholder history cell, got {other:?}"),
+    };
+    assert_eq!(rendered.matches("[No reasoning summary]").count(), 1);
+    assert_eq!(rendered.matches("empty-a").count(), 1);
+    assert_eq!(rendered.matches("empty-b").count(), 1);
+    assert_eq!(rendered.matches("23 tokens").count(), 1);
+}
+
 #[tokio::test]
 async fn replayed_in_progress_mcp_tool_call_stays_active() {
     let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
@@ -1260,6 +1380,9 @@ async fn live_reasoning_summary_is_not_rendered_twice_when_item_completes() {
                 id: "reasoning-1".to_string(),
                 summary: vec!["Summary only".to_string()],
                 content: Vec::new(),
+                response_id: None,
+                reasoning_output_tokens: None,
+                continued: false,
             },
         }),
         /*replay_kind*/ None,
@@ -1336,6 +1459,9 @@ async fn live_reasoning_summary_drops_empty_parts_without_losing_content() {
                     "**Checking tests**\n\n<!-- -->".to_string(),
                 ],
                 content: Vec::new(),
+                response_id: None,
+                reasoning_output_tokens: None,
+                continued: false,
             },
         }),
         /*replay_kind*/ None,
diff --git a/codex-rs/tui/src/chatwidget/turn_runtime.rs b/codex-rs/tui/src/chatwidget/turn_runtime.rs
index f1ac0d7685..f7d68038cf 100644
--- a/codex-rs/tui/src/chatwidget/turn_runtime.rs
+++ b/codex-rs/tui/src/chatwidget/turn_runtime.rs
@@ -89,6 +89,8 @@ impl ChatWidget {
         }
         self.reasoning_summary_parts.clear();
         self.reasoning_buffer.clear();
+        self.pending_reasoning_output_tokens = None;
+        self.pending_reasoning_item_ids.clear();
         self.set_ambient_pet_notification(
             crate::pets::PetNotificationKind::Running,
             /*body*/ None,
@@ -102,6 +104,12 @@ impl ChatWidget {
         duration_ms: Option<i64>,
         from_replay: bool,
     ) {
+        if !self.reasoning_buffer.is_empty() || !self.reasoning_summary_parts.is_empty() {
+            self.on_agent_reasoning_final(
+                /*reasoning_item_id*/ None, /*continued*/ false,
+                /*reasoning_output_tokens*/ None,
+            );
+        }
         self.input_queue.submit_pending_steers_after_interrupt = false;
         // Use `last_agent_message` from the turn-complete notification as the copy
         // source only when no earlier item-level event (AgentMessageItem, plan
diff --git a/codex-rs/tui/src/history_cell/messages.rs b/codex-rs/tui/src/history_cell/messages.rs
index 1133c69bae..f401cb7955 100644
--- a/codex-rs/tui/src/history_cell/messages.rs
+++ b/codex-rs/tui/src/history_cell/messages.rs
@@ -300,6 +300,8 @@ pub(crate) struct ReasoningSummaryCell {
     /// Session cwd used to render local file links inside the reasoning body.
     cwd: PathBuf,
     transcript_only: bool,
+    reasoning_item_ids: Vec<String>,
+    reasoning_output_tokens: Option<i64>,
 }
 
 impl ReasoningSummaryCell {
@@ -311,6 +313,8 @@ impl ReasoningSummaryCell {
             content,
             cwd: cwd.to_path_buf(),
             transcript_only,
+            reasoning_item_ids: Vec::new(),
+            reasoning_output_tokens: None,
         }
     }
 
@@ -323,7 +327,7 @@ impl ReasoningSummaryCell {
             &mut lines,
         );
         let summary_style = Style::default().dim().italic();
-        let summary_lines = lines
+        let mut summary_lines = lines
             .into_iter()
             .map(|mut line| {
                 line.spans = line
@@ -335,6 +339,24 @@ impl ReasoningSummaryCell {
             })
             .collect::<Vec<_>>();
 
+        if let Some(first_line) = summary_lines.first_mut() {
+            if !self.reasoning_item_ids.is_empty() {
+                first_line.spans.push(Span::styled(
+                    format!(" ▪ {}", self.reasoning_item_ids.join(", ")),
+                    Style::default().fg(Color::DarkGray),
+                ));
+            }
+            if let Some(tokens) = self.reasoning_output_tokens {
+                first_line.spans.push(Span::styled(
+                    format!(
+                        " ▪ {} tokens",
+                        codex_protocol::num_format::format_with_separators(tokens)
+                    ),
+                    Style::default().fg(Color::DarkGray),
+                ));
+            }
+        }
+
         adaptive_wrap_lines(
             &summary_lines,
             RtOptions::new(width as usize)
@@ -666,18 +688,37 @@ pub(crate) fn new_user_prompt_with_identity(
 /// did while the turn was live, even if rendering happens after other app state has advanced. Part
 /// boundaries are preserved so standalone empty placeholders can be removed without changing
 /// literal HTML comments or bold-only summary content.
+#[cfg(test)]
 pub(crate) fn new_reasoning_summary_block(
     reasoning_parts: Vec<String>,
     cwd: &Path,
+) -> Box<dyn HistoryCell> {
+    new_reasoning_summary_block_with_usage(reasoning_parts, cwd, Vec::new(), None)
+}
+
+pub(crate) fn new_reasoning_summary_block_with_usage(
+    reasoning_parts: Vec<String>,
+    cwd: &Path,
+    reasoning_item_ids: Vec<String>,
+    reasoning_output_tokens: Option<i64>,
 ) -> Box<dyn HistoryCell> {
     let (header, content) = split_reasoning_summary_parts(&reasoning_parts);
     let transcript_only = header.is_empty();
-    Box::new(ReasoningSummaryCell::new(
-        header,
-        content,
-        cwd,
-        transcript_only,
-    ))
+    let mut cell = ReasoningSummaryCell::new(header, content, cwd, transcript_only);
+    cell.reasoning_item_ids = reasoning_item_ids
+        .into_iter()
+        .filter_map(display_reasoning_item_id)
+        .collect();
+    cell.reasoning_output_tokens = reasoning_output_tokens;
+    Box::new(cell)
+}
+
+fn display_reasoning_item_id(item_id: String) -> Option<String> {
+    let item_id = item_id.strip_prefix("rs_").unwrap_or(&item_id).trim();
+    if item_id.starts_with("item-") {
+        return None;
+    }
+    (!item_id.is_empty()).then(|| item_id.to_string())
 }
 
 /// Split structured reasoning-summary parts into the status header and renderable content.
diff --git a/codex-rs/tui/src/resume_picker.rs b/codex-rs/tui/src/resume_picker.rs
index ea6a3757f1..e71aa9d670 100644
--- a/codex-rs/tui/src/resume_picker.rs
+++ b/codex-rs/tui/src/resume_picker.rs
@@ -6315,6 +6315,9 @@ session_picker_view = "dense"
                     id: String::from("reasoning-1"),
                     summary: Vec::new(),
                     content: vec![String::from("private raw chain of thought")],
+                    response_id: None,
+                    reasoning_output_tokens: None,
+                    continued: false,
                 }],
                 status: codex_app_server_protocol::TurnStatus::Completed,
                 error: None,
@@ -6377,6 +6380,9 @@ session_picker_view = "dense"
                     id: String::from("reasoning-1"),
                     summary: vec![String::from("public summary")],
                     content: vec![String::from("raw reasoning content")],
+                    response_id: None,
+                    reasoning_output_tokens: None,
+                    continued: false,
                 }],
                 status: codex_app_server_protocol::TurnStatus::Completed,
                 error: None,
