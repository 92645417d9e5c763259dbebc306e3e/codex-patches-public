diff --git a/codex-rs/tui/src/status_indicator_widget.rs b/codex-rs/tui/src/status_indicator_widget.rs
index d1ea483d1d23eaa079a964639da0388a19c1c454..44c5be6109b37d509d38d414d2a24978338c11a7 100644
--- a/codex-rs/tui/src/status_indicator_widget.rs
+++ b/codex-rs/tui/src/status_indicator_widget.rs
@@ -10,6 +10,7 @@ use std::time::Instant;
 use crossterm::event::KeyCode;
 use ratatui::buffer::Buffer;
 use ratatui::layout::Rect;
+use ratatui::style::Color;
 use ratatui::style::Stylize;
 use ratatui::text::Line;
 use ratatui::text::Span;
@@ -25,7 +26,6 @@ use crate::line_truncation::truncate_line_with_ellipsis_if_overflow;
 use crate::motion::MotionMode;
 use crate::motion::ReducedMotionIndicator;
 use crate::motion::activity_indicator;
-use crate::motion::shimmer_text;
 use crate::render::renderable::Renderable;
 use crate::text_formatting::capitalize_first;
 use crate::tui::FrameRequester;
@@ -34,6 +34,21 @@ use crate::wrapping::word_wrap_lines;
 
 pub(crate) const STATUS_DETAILS_DEFAULT_MAX_LINES: usize = 3;
 const DETAILS_PREFIX: &str = "  └ ";
+const HEADER_COLOR_CYCLE: [Color; 6] = [
+    Color::Red,
+    Color::Yellow,
+    Color::Green,
+    Color::Cyan,
+    Color::Blue,
+    Color::Magenta,
+];
+
+fn animated_header_span(text: &str, elapsed: Duration) -> Span<'static> {
+    let index = (elapsed.as_millis() / 350) as usize % HEADER_COLOR_CYCLE.len();
+    Span::from(text.to_string())
+        .fg(HEADER_COLOR_CYCLE[index])
+        .bold()
+}
 
 #[derive(Debug, Clone, Copy, PartialEq, Eq)]
 pub(crate) enum StatusDetailsCapitalization {
@@ -260,7 +275,12 @@ impl Renderable for StatusIndicatorWidget {
             spans.push(indicator);
             spans.push(" ".into());
         }
-        spans.extend(shimmer_text(&self.header, motion_mode));
+        match motion_mode {
+            MotionMode::Animated | MotionMode::Reduced if !self.header.is_empty() => {
+                spans.push(animated_header_span(&self.header, elapsed_duration));
+            }
+            _ => {}
+        }
         if !spans.is_empty() {
             spans.push(" ".into());
         }
@@ -343,6 +363,16 @@ mod tests {
         insta::assert_snapshot!(terminal.backend());
     }
 
+    #[test]
+    fn animated_header_colors_whole_label() {
+        let first = animated_header_span("Working", Duration::from_millis(0));
+        let second = animated_header_span("Working", Duration::from_millis(350));
+
+        assert_eq!(first.content.as_ref(), "Working");
+        assert_eq!(second.content.as_ref(), "Working");
+        assert_ne!(first.style.fg, second.style.fg);
+    }
+
     #[test]
     fn renders_truncated() {
         let (tx_raw, _rx) = unbounded_channel::<AppEvent>();
