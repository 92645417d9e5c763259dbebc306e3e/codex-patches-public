diff --git a/codex-rs/tui/src/chatwidget/model_popups.rs b/codex-rs/tui/src/chatwidget/model_popups.rs
index fec2657a0d24946c604a84c57b3370f5fa999e74..618e70ed16a74fb843fa97bcf7c3c98b6684d000 100644
--- a/codex-rs/tui/src/chatwidget/model_popups.rs
+++ b/codex-rs/tui/src/chatwidget/model_popups.rs
@@ -391,6 +391,10 @@ impl ChatWidget {
         self.notify(Notification::PlanModePrompt {
             title: PLAN_MODE_REASONING_SCOPE_TITLE.to_string(),
         });
+        self.notify_helper_action_required(
+            "Plan mode needs input",
+            "Codex is waiting for your plan mode reasoning scope choice.",
+        );
     }
 
     /// Open a popup to choose the standard reasoning effort for the given model.
diff --git a/codex-rs/tui/src/chatwidget/notifications.rs b/codex-rs/tui/src/chatwidget/notifications.rs
index 2084956b2c844d60227c22409bbcba278b30236c..f40e0399759514c20ecedf2ad889611ddd43fc9a 100644
--- a/codex-rs/tui/src/chatwidget/notifications.rs
+++ b/codex-rs/tui/src/chatwidget/notifications.rs
@@ -21,6 +21,20 @@ impl ChatWidget {
             tui.notify(notif.display());
         }
     }
+
+    pub(super) fn notify_helper_action_required(&self, title: &str, body: &str) {
+        let Some(thread_id) = self.thread_id else {
+            return;
+        };
+        let session_id = thread_id.to_string();
+        crate::codex_socket::notify_action_required(
+            session_id.as_str(),
+            self.turn_lifecycle.last_turn_id.as_deref(),
+            self.config.cwd.as_path(),
+            title,
+            body,
+        );
+    }
 }
 
 #[derive(Debug)]
diff --git a/codex-rs/tui/src/chatwidget/tool_requests.rs b/codex-rs/tui/src/chatwidget/tool_requests.rs
index 2afe8265f1a2bf7d966d3fdbc45d13ef0228eb3d..31c8e1f43a431cbf98d4de4096615aeaed5fd1e1 100644
--- a/codex-rs/tui/src/chatwidget/tool_requests.rs
+++ b/codex-rs/tui/src/chatwidget/tool_requests.rs
@@ -285,6 +285,10 @@ impl ChatWidget {
         let command = shlex::try_join(ev.command.iter().map(String::as_str))
             .unwrap_or_else(|_| ev.command.join(" "));
         self.notify(Notification::ExecApprovalRequested { command });
+        self.notify_helper_action_required(
+            "Approval requested",
+            "Codex needs command approval to continue.",
+        );
 
         let available_decisions = ev.effective_available_decisions();
         let request = ApprovalRequest::Exec(ExecApprovalRequest {
@@ -329,6 +333,10 @@ impl ChatWidget {
             cwd: self.config.cwd.to_path_buf(),
             changes: ev.changes.keys().cloned().collect(),
         });
+        self.notify_helper_action_required(
+            "Approval requested",
+            "Codex needs edit approval to continue.",
+        );
     }
 
     pub(crate) fn handle_elicitation_request_now(
@@ -341,6 +349,10 @@ impl ChatWidget {
         self.notify(Notification::ElicitationRequested {
             server_name: params.server_name.clone(),
         });
+        self.notify_helper_action_required(
+            "Approval requested",
+            "Codex needs an external approval response to continue.",
+        );
 
         let thread_id = ThreadId::from_string(&params.thread_id)
             .unwrap_or_else(|_| self.thread_id.unwrap_or_default());
@@ -424,6 +436,10 @@ impl ChatWidget {
             (count, _) => format!("{count} questions requested"),
         };
         self.notify(Notification::PlanModePrompt { title });
+        self.notify_helper_action_required(
+            "Plan mode needs input",
+            "Codex is waiting for your plan mode response.",
+        );
         self.bottom_pane.push_user_input_request(ev);
         self.set_ambient_pet_notification(
             crate::pets::PetNotificationKind::Waiting,
@@ -444,6 +460,10 @@ impl ChatWidget {
         });
         self.bottom_pane
             .push_approval_request(request, &self.config.features);
+        self.notify_helper_action_required(
+            "Approval requested",
+            "Codex needs permissions approval to continue.",
+        );
         self.set_ambient_pet_notification(
             crate::pets::PetNotificationKind::Waiting,
             /*body*/ None,
diff --git a/codex-rs/tui/src/codex_socket.rs b/codex-rs/tui/src/codex_socket.rs
index 75be72a98c68e35d473af1029f549a4dfc4fb7f7..e44d41fd1bd4c32d9fa25276d6c146c3915eb598 100644
--- a/codex-rs/tui/src/codex_socket.rs
+++ b/codex-rs/tui/src/codex_socket.rs
@@ -78,6 +78,30 @@ pub(crate) fn notify_turn_completed(notification: &TurnCompletedNotification, wo
     });
 }
 
+pub(crate) fn notify_action_required(
+    session_id: &str,
+    turn_id: Option<&str>,
+    working_dir: &Path,
+    title: &str,
+    body: &str,
+) {
+    let Some(payload) = action_required_payload(
+        session_id,
+        turn_id,
+        working_dir,
+        title,
+        body,
+        chrono::Utc::now(),
+    ) else {
+        return;
+    };
+    tokio::task::spawn_blocking(move || {
+        if let Err(err) = send_payload_to_path(&payload, &codex_socket_path()) {
+            tracing::debug!("codex helper action-required socket notification skipped: {err}");
+        }
+    });
+}
+
 pub(crate) async fn edit_with_helper(
     seed: &str,
     session_id: Option<&str>,
@@ -153,6 +177,29 @@ fn completion_payload(
     })
 }
 
+fn action_required_payload(
+    session_id: &str,
+    turn_id: Option<&str>,
+    working_dir: &Path,
+    title: &str,
+    body: &str,
+    created_at: chrono::DateTime<chrono::Utc>,
+) -> Option<CompletionPayload> {
+    let session_id = Uuid::parse_str(session_id).ok()?.to_string();
+    Some(CompletionPayload {
+        kind: "Completion",
+        source: "codex",
+        session_id,
+        turn_id: turn_id.map(positive_turn_id).unwrap_or(1),
+        status: "NeedsInput",
+        duration_ms: 0,
+        working_dir: working_dir.display().to_string(),
+        title: title.to_string(),
+        body: body.to_string(),
+        created_at: created_at.to_rfc3339_opts(chrono::SecondsFormat::Secs, true),
+    })
+}
+
 fn positive_turn_id(turn_id: &str) -> u64 {
     if let Ok(parsed) = turn_id.parse::<u64>()
         && parsed > 0
@@ -307,6 +354,34 @@ mod tests {
         assert_eq!(json["CreatedAt"], "2026-06-12T10:11:12Z");
     }
 
+    #[test]
+    fn action_required_payload_reuses_completion_schema() {
+        let created_at = chrono::DateTime::parse_from_rfc3339("2026-06-12T10:11:12Z")
+            .unwrap()
+            .with_timezone(&chrono::Utc);
+        let payload = action_required_payload(
+            "123e4567-e89b-12d3-a456-426614174000",
+            Some("turn-9"),
+            Path::new("/repo"),
+            "Approval requested",
+            "Codex needs approval to continue.",
+            created_at,
+        )
+        .expect("payload");
+
+        let json = serde_json::to_value(payload).expect("serialize");
+        assert_eq!(json["Type"], "Completion");
+        assert_eq!(json["Source"], "codex");
+        assert_eq!(json["SessionID"], "123e4567-e89b-12d3-a456-426614174000");
+        assert_eq!(json["TurnID"], 9);
+        assert_eq!(json["Status"], "NeedsInput");
+        assert_eq!(json["DurationMS"], 0);
+        assert_eq!(json["WorkingDir"], "/repo");
+        assert_eq!(json["Title"], "Approval requested");
+        assert_eq!(json["Body"], "Codex needs approval to continue.");
+        assert_eq!(json["CreatedAt"], "2026-06-12T10:11:12Z");
+    }
+
     #[test]
     fn completion_payload_skips_non_completed_or_invalid_session() {
         assert_eq!(
