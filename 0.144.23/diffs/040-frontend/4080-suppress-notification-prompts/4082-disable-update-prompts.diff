diff --git a/codex-rs/tui/src/lib.rs b/codex-rs/tui/src/lib.rs
index 45816a49fe5e10cf7751f1a938afa54d90f72fc9..ff3687f8552216a02cfdd6640830ff391cffa436 100644
--- a/codex-rs/tui/src/lib.rs
+++ b/codex-rs/tui/src/lib.rs
@@ -200,6 +200,7 @@ mod update_versions;
 mod updates;
 #[cfg(any(not(debug_assertions), test))]
 mod updates_cache;
+mod updates_policy;
 mod version;
 mod width;
 #[cfg(any(target_os = "windows", test))]
diff --git a/codex-rs/tui/src/updates.rs b/codex-rs/tui/src/updates.rs
index 64e7f76344f9046591cb2343a1b015c4fca3093c..528f3cdbd4982ea77334792c4bf4669534c32a56 100644
--- a/codex-rs/tui/src/updates.rs
+++ b/codex-rs/tui/src/updates.rs
@@ -11,6 +11,7 @@ use crate::update_versions::is_source_build_version;
 use crate::updates_cache::VersionInfo;
 use crate::updates_cache::read_version_info;
 use crate::updates_cache::version_filepath;
+use crate::updates_policy::update_prompts_disabled;
 use chrono::Duration;
 use chrono::Utc;
 use codex_login::default_client::create_client;
@@ -22,7 +23,10 @@ use crate::version::CODEX_CLI_VERSION;
 pub(crate) use crate::updates_cache::dismiss_version;
 
 pub fn get_upgrade_version(config: &Config) -> Option<String> {
-    if !config.check_for_update_on_startup || is_source_build_version(CODEX_CLI_VERSION) {
+    if update_prompts_disabled()
+        || !config.check_for_update_on_startup
+        || is_source_build_version(CODEX_CLI_VERSION)
+    {
         return None;
     }
 
@@ -130,7 +134,10 @@ async fn fetch_latest_github_release_version() -> anyhow::Result<String> {
 /// Returns the latest version to show in a popup, if it should be shown.
 /// This respects the user's dismissal choice for the current latest version.
 pub fn get_upgrade_version_for_popup(config: &Config) -> Option<String> {
-    if !config.check_for_update_on_startup || is_source_build_version(CODEX_CLI_VERSION) {
+    if update_prompts_disabled()
+        || !config.check_for_update_on_startup
+        || is_source_build_version(CODEX_CLI_VERSION)
+    {
         return None;
     }
 
diff --git a/codex-rs/tui/src/updates_policy.rs b/codex-rs/tui/src/updates_policy.rs
new file mode 100644
index 0000000000000000000000000000000000000000..40a7f4b228e521b2624d9ebebddc59cf87286c52
--- /dev/null
+++ b/codex-rs/tui/src/updates_policy.rs
@@ -0,0 +1,13 @@
+pub(crate) const fn update_prompts_disabled() -> bool {
+    true
+}
+
+#[cfg(test)]
+mod tests {
+    use super::*;
+
+    #[test]
+    fn update_prompts_are_disabled() {
+        assert!(update_prompts_disabled());
+    }
+}
