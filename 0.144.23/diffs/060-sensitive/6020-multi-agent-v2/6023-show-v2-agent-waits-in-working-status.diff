diff --git a/codex-rs/tui/src/app/tests.rs b/codex-rs/tui/src/app/tests.rs
index e6eb8e8246..45a8a1ee53 100644
--- a/codex-rs/tui/src/app/tests.rs
+++ b/codex-rs/tui/src/app/tests.rs
@@ -1447,6 +1447,7 @@ async fn collab_receiver_notification_caches_thread_without_app_server_read() {
                 status: codex_app_server_protocol::CollabAgentToolCallStatus::InProgress,
                 sender_thread_id: ThreadId::new().to_string(),
                 receiver_thread_ids: vec![receiver_thread_id.to_string()],
+                multi_agent_version: None,
                 prompt: None,
                 model: None,
                 reasoning_effort: None,
@@ -1484,6 +1485,7 @@ async fn collab_receiver_notification_renders_metadata_without_app_server_read()
                 status: codex_app_server_protocol::CollabAgentToolCallStatus::InProgress,
                 sender_thread_id: ThreadId::new().to_string(),
                 receiver_thread_ids: vec![receiver_thread_id.to_string()],
+                multi_agent_version: None,
                 prompt: None,
                 model: None,
                 reasoning_effort: None,
@@ -1544,6 +1546,7 @@ async fn v1_collab_receiver_metadata_is_cached_before_active_drain() -> Result<(
                 status: codex_app_server_protocol::CollabAgentToolCallStatus::Completed,
                 sender_thread_id: primary_thread_id.to_string(),
                 receiver_thread_ids: vec![receiver_thread_id.to_string()],
+                multi_agent_version: None,
                 prompt: Some("start scout".to_string()),
                 model: None,
                 reasoning_effort: None,
@@ -1618,6 +1621,7 @@ async fn collab_receiver_notification_does_not_cache_not_found_thread() {
                 status: codex_app_server_protocol::CollabAgentToolCallStatus::Failed,
                 sender_thread_id: ThreadId::new().to_string(),
                 receiver_thread_ids: vec![receiver_thread_id.to_string()],
+                multi_agent_version: None,
                 prompt: Some("hello".to_string()),
                 model: None,
                 reasoning_effort: None,
@@ -6661,6 +6665,7 @@ async fn replace_chat_widget_reseeds_collab_agent_metadata_for_replay() {
                                 codex_app_server_protocol::CollabAgentToolCallStatus::InProgress,
                             sender_thread_id: ThreadId::new().to_string(),
                             receiver_thread_ids: vec![receiver_thread_id.to_string()],
+                            multi_agent_version: None,
                             prompt: None,
                             model: None,
                             reasoning_effort: None,
@@ -6720,6 +6725,7 @@ async fn running_thread_replay_uses_item_nickname_without_populating_agent_navig
                     status: codex_app_server_protocol::CollabAgentToolCallStatus::Completed,
                     sender_thread_id: sender_thread_id.to_string(),
                     receiver_thread_ids: vec![receiver_id.clone()],
+                    multi_agent_version: None,
                     prompt: None,
                     model: None,
                     reasoning_effort: None,
@@ -6790,6 +6796,7 @@ async fn primary_resume_turns_keep_spawn_and_close_item_nickname() -> Result<()>
         status: codex_app_server_protocol::CollabAgentToolCallStatus::Completed,
         sender_thread_id: primary_thread_id.to_string(),
         receiver_thread_ids: vec![receiver_id.clone()],
+        multi_agent_version: None,
         prompt: None,
         model: None,
         reasoning_effort: None,
diff --git a/codex-rs/tui/src/chatwidget.rs b/codex-rs/tui/src/chatwidget.rs
index de81efd906..0ae552c3c8 100644
--- a/codex-rs/tui/src/chatwidget.rs
+++ b/codex-rs/tui/src/chatwidget.rs
@@ -156,6 +156,7 @@ use codex_protocol::items::AgentMessageItem;
 use codex_protocol::models::MessagePhase;
 use codex_protocol::plan_tool::PlanItemArg as UpdatePlanItemArg;
 use codex_protocol::plan_tool::StepStatus as UpdatePlanItemStatus;
+use codex_protocol::protocol::MultiAgentVersion;
 use codex_protocol::request_permissions::RequestPermissionsEvent;
 use codex_protocol::user_input::ByteRange;
 use codex_protocol::user_input::TextElement;
diff --git a/codex-rs/tui/src/chatwidget/protocol.rs b/codex-rs/tui/src/chatwidget/protocol.rs
index 254e51be71..b8d713f64c 100644
--- a/codex-rs/tui/src/chatwidget/protocol.rs
+++ b/codex-rs/tui/src/chatwidget/protocol.rs
@@ -347,23 +347,28 @@ impl ChatWidget {
                 id,
                 tool,
                 status,
+                multi_agent_version,
                 sender_thread_id,
                 receiver_thread_ids,
                 prompt,
                 model,
                 reasoning_effort,
                 agents_states,
-            } => self.on_collab_agent_tool_call(ThreadItem::CollabAgentToolCall {
-                id,
-                tool,
-                status,
-                sender_thread_id,
-                receiver_thread_ids,
-                prompt,
-                model,
-                reasoning_effort,
-                agents_states,
-            }),
+            } => self.on_collab_agent_tool_call(
+                ThreadItem::CollabAgentToolCall {
+                    id,
+                    tool,
+                    status,
+                    multi_agent_version,
+                    sender_thread_id,
+                    receiver_thread_ids,
+                    prompt,
+                    model,
+                    reasoning_effort,
+                    agents_states,
+                },
+                from_replay,
+            ),
             item @ ThreadItem::SubAgentActivity { .. } => self.on_sub_agent_activity(item),
             ThreadItem::EnteredReviewMode { review, .. } if !from_replay => {
                 self.enter_review_mode_with_hint(review, /*from_replay*/ false);
diff --git a/codex-rs/tui/src/chatwidget/replay.rs b/codex-rs/tui/src/chatwidget/replay.rs
index 7de49addb6..03795b42b9 100644
--- a/codex-rs/tui/src/chatwidget/replay.rs
+++ b/codex-rs/tui/src/chatwidget/replay.rs
@@ -249,23 +249,28 @@ impl ChatWidget {
                 id,
                 tool,
                 status,
+                multi_agent_version,
                 sender_thread_id,
                 receiver_thread_ids,
                 prompt,
                 model,
                 reasoning_effort,
                 agents_states,
-            } => self.on_collab_agent_tool_call(ThreadItem::CollabAgentToolCall {
-                id,
-                tool,
-                status,
-                sender_thread_id,
-                receiver_thread_ids,
-                prompt,
-                model,
-                reasoning_effort,
-                agents_states,
-            }),
+            } => self.on_collab_agent_tool_call(
+                ThreadItem::CollabAgentToolCall {
+                    id,
+                    tool,
+                    status,
+                    multi_agent_version,
+                    sender_thread_id,
+                    receiver_thread_ids,
+                    prompt,
+                    model,
+                    reasoning_effort,
+                    agents_states,
+                },
+                from_replay,
+            ),
             item @ ThreadItem::SubAgentActivity { .. } => self.on_sub_agent_activity(item),
             ThreadItem::DynamicToolCall { .. } => {}
             ThreadItem::Sleep(_) => {}
diff --git a/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__v2_collab_wait_status.snap b/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__v2_collab_wait_status.snap
new file mode 100644
index 0000000000..274789b2ea
--- /dev/null
+++ b/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__v2_collab_wait_status.snap
@@ -0,0 +1,10 @@
+---
+source: tui/src/chatwidget/tests/app_server.rs
+expression: "render_bottom_popup(&chat, 80)"
+---
+⠋ Waiting for agents (0s ▪ esc to interrupt)
+
+────────────────────────────────────────────────────────────────────────────────
+ Ask Codex to do anything
+────────────────────────────────────────────────────────────────────────────────
+  gpt-5.6-sol default ▪ /tmp/project
diff --git a/codex-rs/tui/src/chatwidget/tests/app_server.rs b/codex-rs/tui/src/chatwidget/tests/app_server.rs
index dd909016d4..a1562299d1 100644
--- a/codex-rs/tui/src/chatwidget/tests/app_server.rs
+++ b/codex-rs/tui/src/chatwidget/tests/app_server.rs
@@ -1,4 +1,5 @@
 use super::*;
+use codex_protocol::protocol::MultiAgentVersion;
 use pretty_assertions::assert_eq;
 
 const SAFETY_BUFFERING_HEADER_TEXT: &str =
@@ -413,6 +414,7 @@ async fn collab_spawn_end_shows_requested_model_and_effort() {
                 status: AppServerCollabAgentToolCallStatus::InProgress,
                 sender_thread_id: sender_thread_id.to_string(),
                 receiver_thread_ids: Vec::new(),
+                multi_agent_version: None,
                 prompt: Some("Explore the repo".to_string()),
                 model: Some("gpt-5".to_string()),
                 reasoning_effort: Some(ReasoningEffortConfig::High),
@@ -432,6 +434,7 @@ async fn collab_spawn_end_shows_requested_model_and_effort() {
                 status: AppServerCollabAgentToolCallStatus::Completed,
                 sender_thread_id: sender_thread_id.to_string(),
                 receiver_thread_ids: vec![spawned_thread_id.to_string()],
+                multi_agent_version: None,
                 prompt: Some("Explore the repo".to_string()),
                 model: None,
                 reasoning_effort: None,
@@ -488,6 +491,7 @@ async fn v1_collab_spawn_uses_item_role_description_without_cached_metadata() {
                 status: AppServerCollabAgentToolCallStatus::Completed,
                 sender_thread_id: sender_thread_id.to_string(),
                 receiver_thread_ids: vec![spawned_thread_id.to_string()],
+                multi_agent_version: None,
                 prompt: None,
                 model: None,
                 reasoning_effort: None,
@@ -866,8 +870,10 @@ async fn live_app_server_command_execution_strips_shell_wrapper() {
 }
 
 #[tokio::test]
-async fn live_app_server_collab_wait_items_render_history() {
+async fn live_v1_app_server_collab_wait_items_render_history() {
     let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+    let _ = chat.config.features.enable(Feature::Collab);
+    let _ = chat.config.features.enable(Feature::MultiAgentV2);
     let sender_thread_id =
         ThreadId::from_string("019cff70-2599-75e2-af72-b90000000001").expect("valid thread id");
     let receiver_thread_id =
@@ -899,6 +905,7 @@ async fn live_app_server_collab_wait_items_render_history() {
                     receiver_thread_id.to_string(),
                     other_receiver_thread_id.to_string(),
                 ],
+                multi_agent_version: Some(MultiAgentVersion::V1),
                 prompt: None,
                 model: None,
                 reasoning_effort: None,
@@ -922,6 +929,7 @@ async fn live_app_server_collab_wait_items_render_history() {
                     receiver_thread_id.to_string(),
                     other_receiver_thread_id.to_string(),
                 ],
+                multi_agent_version: Some(MultiAgentVersion::V1),
                 prompt: None,
                 model: None,
                 reasoning_effort: None,
@@ -958,6 +966,152 @@ async fn live_app_server_collab_wait_items_render_history() {
     assert_chatwidget_snapshot!("app_server_collab_wait_items_render_history", combined);
 }
 
+#[tokio::test]
+async fn unknown_multi_agent_version_collab_wait_keeps_history() {
+    let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(Some("gpt-5.4")).await;
+    let _ = chat.config.features.enable(Feature::MultiAgentV2);
+    let sender_thread_id = ThreadId::new();
+
+    chat.handle_server_notification(
+        ServerNotification::ItemCompleted(ItemCompletedNotification {
+            thread_id: sender_thread_id.to_string(),
+            turn_id: "turn-1".to_string(),
+            completed_at_ms: 0,
+            item: AppServerThreadItem::CollabAgentToolCall {
+                id: "wait-unknown".to_string(),
+                tool: AppServerCollabAgentTool::Wait,
+                status: AppServerCollabAgentToolCallStatus::Completed,
+                sender_thread_id: sender_thread_id.to_string(),
+                receiver_thread_ids: Vec::new(),
+                multi_agent_version: None,
+                prompt: None,
+                model: None,
+                reasoning_effort: None,
+                agents_states: HashMap::new(),
+            },
+        }),
+        /*replay_kind*/ None,
+    );
+
+    let history = drain_insert_history(&mut rx)
+        .into_iter()
+        .map(|lines| lines_to_single_string(&lines))
+        .collect::<Vec<_>>()
+        .join("\n");
+    assert!(history.contains("Finished waiting"));
+    assert!(history.contains("No agents completed yet"));
+}
+
+#[tokio::test]
+async fn live_and_replayed_v2_app_server_collab_wait_uses_status_without_history() {
+    let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+    let _ = chat.config.features.disable(Feature::MultiAgentV2);
+    chat.bottom_pane.set_task_running(/*running*/ true);
+    let sender_thread_id = ThreadId::new();
+    let wait_item =
+        |status, receiver_thread_ids: &[ThreadId]| AppServerThreadItem::CollabAgentToolCall {
+            id: "wait-v2".to_string(),
+            tool: AppServerCollabAgentTool::Wait,
+            status,
+            sender_thread_id: sender_thread_id.to_string(),
+            receiver_thread_ids: receiver_thread_ids
+                .iter()
+                .map(ToString::to_string)
+                .collect(),
+            multi_agent_version: Some(MultiAgentVersion::V2),
+            prompt: None,
+            model: None,
+            reasoning_effort: None,
+            agents_states: HashMap::new(),
+        };
+
+    chat.handle_server_notification(
+        ServerNotification::ItemStarted(ItemStartedNotification {
+            thread_id: "thread-1".to_string(),
+            turn_id: "turn-1".to_string(),
+            started_at_ms: 0,
+            item: wait_item(AppServerCollabAgentToolCallStatus::InProgress, &[]),
+        }),
+        /*replay_kind*/ None,
+    );
+
+    assert_eq!(
+        chat.status_state.current_status.header,
+        "Waiting for agents"
+    );
+    assert_chatwidget_snapshot!(
+        "v2_collab_wait_status",
+        render_bottom_popup(&chat, /*width*/ 80)
+    );
+    assert!(drain_insert_history(&mut rx).is_empty());
+
+    chat.handle_server_notification(
+        ServerNotification::ItemCompleted(ItemCompletedNotification {
+            thread_id: "thread-1".to_string(),
+            turn_id: "turn-1".to_string(),
+            completed_at_ms: 0,
+            item: wait_item(AppServerCollabAgentToolCallStatus::Completed, &[]),
+        }),
+        /*replay_kind*/ None,
+    );
+
+    assert_eq!(chat.status_state.current_status.header, "Working");
+    assert!(drain_insert_history(&mut rx).is_empty());
+
+    let one_receiver = [ThreadId::new()];
+    let three_receivers = [ThreadId::new(), ThreadId::new(), ThreadId::new()];
+    for (receiver_thread_ids, expected) in [
+        (one_receiver.as_slice(), "Waiting for 1 agent"),
+        (three_receivers.as_slice(), "Waiting for 3 agents"),
+    ] {
+        chat.handle_server_notification(
+            ServerNotification::ItemStarted(ItemStartedNotification {
+                thread_id: "thread-1".to_string(),
+                turn_id: "turn-1".to_string(),
+                started_at_ms: 0,
+                item: wait_item(
+                    AppServerCollabAgentToolCallStatus::InProgress,
+                    receiver_thread_ids,
+                ),
+            }),
+            /*replay_kind*/ None,
+        );
+        assert_eq!(chat.status_state.current_status.header, expected);
+        chat.set_status_header(String::from("Thinking"));
+        chat.handle_server_notification(
+            ServerNotification::ItemCompleted(ItemCompletedNotification {
+                thread_id: "thread-1".to_string(),
+                turn_id: "turn-1".to_string(),
+                completed_at_ms: 0,
+                item: wait_item(
+                    AppServerCollabAgentToolCallStatus::Completed,
+                    receiver_thread_ids,
+                ),
+            }),
+            /*replay_kind*/ None,
+        );
+        assert_eq!(chat.status_state.current_status.header, "Thinking");
+    }
+    assert!(drain_insert_history(&mut rx).is_empty());
+
+    chat.set_status_header(String::from("Thinking"));
+    chat.handle_server_notification(
+        ServerNotification::ItemStarted(ItemStartedNotification {
+            thread_id: "thread-1".to_string(),
+            turn_id: "turn-1".to_string(),
+            started_at_ms: 0,
+            item: wait_item(
+                AppServerCollabAgentToolCallStatus::InProgress,
+                &three_receivers,
+            ),
+        }),
+        Some(ReplayKind::ThreadSnapshot),
+    );
+
+    assert_eq!(chat.status_state.current_status.header, "Thinking");
+    assert!(drain_insert_history(&mut rx).is_empty());
+}
+
 #[tokio::test]
 async fn live_app_server_collab_spawn_completed_renders_requested_model_and_effort() {
     let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
@@ -977,6 +1131,7 @@ async fn live_app_server_collab_spawn_completed_renders_requested_model_and_effo
                 status: AppServerCollabAgentToolCallStatus::InProgress,
                 sender_thread_id: sender_thread_id.to_string(),
                 receiver_thread_ids: Vec::new(),
+                multi_agent_version: None,
                 prompt: Some("Explore the repo".to_string()),
                 model: Some("gpt-5".to_string()),
                 reasoning_effort: Some(ReasoningEffortConfig::High),
@@ -997,6 +1152,7 @@ async fn live_app_server_collab_spawn_completed_renders_requested_model_and_effo
                 status: AppServerCollabAgentToolCallStatus::Completed,
                 sender_thread_id: sender_thread_id.to_string(),
                 receiver_thread_ids: vec![spawned_thread_id.to_string()],
+                multi_agent_version: None,
                 prompt: Some("Explore the repo".to_string()),
                 model: Some("gpt-5".to_string()),
                 reasoning_effort: Some(ReasoningEffortConfig::High),
diff --git a/codex-rs/tui/src/chatwidget/tool_lifecycle.rs b/codex-rs/tui/src/chatwidget/tool_lifecycle.rs
index cf6de68417..610fdec327 100644
--- a/codex-rs/tui/src/chatwidget/tool_lifecycle.rs
+++ b/codex-rs/tui/src/chatwidget/tool_lifecycle.rs
@@ -114,13 +114,36 @@ impl ChatWidget {
         self.request_redraw();
     }
 
-    pub(super) fn on_collab_agent_tool_call(&mut self, item: ThreadItem) {
+    pub(super) fn on_collab_agent_tool_call(&mut self, item: ThreadItem, from_replay: bool) {
         let ThreadItem::CollabAgentToolCall {
-            id, tool, status, ..
+            id,
+            tool,
+            status,
+            receiver_thread_ids,
+            multi_agent_version,
+            ..
         } = &item
         else {
             return;
         };
+        if *multi_agent_version == Some(MultiAgentVersion::V2)
+            && matches!(tool, CollabAgentTool::Wait)
+        {
+            if !from_replay {
+                let waiting_header = match receiver_thread_ids.len() {
+                    0 => String::from("Waiting for agents"),
+                    1 => String::from("Waiting for 1 agent"),
+                    count => format!("Waiting for {count} agents"),
+                };
+                if matches!(status, CollabAgentToolCallStatus::InProgress) {
+                    self.set_status_header(waiting_header);
+                } else if self.status_state.current_status.header == waiting_header {
+                    self.set_status_header(String::from("Working"));
+                }
+            }
+            return;
+        }
+
         if matches!(tool, CollabAgentTool::SpawnAgent)
             && let Some(spawn_request) = multi_agents::spawn_request_summary(&item)
         {
diff --git a/codex-rs/tui/src/multi_agents.rs b/codex-rs/tui/src/multi_agents.rs
index d25f94bddd..e36cd925cb 100644
--- a/codex-rs/tui/src/multi_agents.rs
+++ b/codex-rs/tui/src/multi_agents.rs
@@ -780,6 +780,7 @@ mod tests {
                 status: CollabAgentToolCallStatus::Completed,
                 sender_thread_id: sender_thread_id.to_string(),
                 receiver_thread_ids: vec![robie_id.to_string()],
+                multi_agent_version: None,
                 prompt: Some("Compute 11! and reply with just the integer result.".to_string()),
                 model: Some("gpt-5".to_string()),
                 reasoning_effort: Some(ReasoningEffortConfig::High),
@@ -800,6 +801,7 @@ mod tests {
                 status: CollabAgentToolCallStatus::Completed,
                 sender_thread_id: sender_thread_id.to_string(),
                 receiver_thread_ids: vec![robie_id.to_string()],
+                multi_agent_version: None,
                 prompt: Some("Please continue and return the answer only.".to_string()),
                 model: None,
                 reasoning_effort: None,
@@ -820,6 +822,7 @@ mod tests {
                 status: CollabAgentToolCallStatus::InProgress,
                 sender_thread_id: sender_thread_id.to_string(),
                 receiver_thread_ids: vec![robie_id.to_string()],
+                multi_agent_version: None,
                 prompt: None,
                 model: None,
                 reasoning_effort: None,
@@ -837,6 +840,7 @@ mod tests {
                 status: CollabAgentToolCallStatus::Completed,
                 sender_thread_id: sender_thread_id.to_string(),
                 receiver_thread_ids: vec![robie_id.to_string(), bob_id.to_string()],
+                multi_agent_version: None,
                 prompt: None,
                 model: None,
                 reasoning_effort: None,
@@ -863,6 +867,7 @@ mod tests {
                 status: CollabAgentToolCallStatus::Completed,
                 sender_thread_id: sender_thread_id.to_string(),
                 receiver_thread_ids: vec![robie_id.to_string()],
+                multi_agent_version: None,
                 prompt: None,
                 model: None,
                 reasoning_effort: None,
@@ -883,6 +888,7 @@ mod tests {
                 status: CollabAgentToolCallStatus::Completed,
                 sender_thread_id: sender_thread_id.to_string(),
                 receiver_thread_ids: vec![robie_id.to_string()],
+                multi_agent_version: None,
                 prompt: None,
                 model: None,
                 reasoning_effort: None,
@@ -967,6 +973,7 @@ mod tests {
                 status: CollabAgentToolCallStatus::Completed,
                 sender_thread_id: sender_thread_id.to_string(),
                 receiver_thread_ids: vec![robie_id.to_string()],
+                multi_agent_version: None,
                 prompt: Some(String::new()),
                 model: Some("gpt-5".to_string()),
                 reasoning_effort: Some(ReasoningEffortConfig::High),
@@ -1029,6 +1036,7 @@ mod tests {
                 status: CollabAgentToolCallStatus::Completed,
                 sender_thread_id: sender_thread_id.to_string(),
                 receiver_thread_ids: vec![robie_id.to_string()],
+                multi_agent_version: None,
                 prompt: None,
                 model: None,
                 reasoning_effort: None,
@@ -1063,6 +1071,7 @@ mod tests {
                 status: CollabAgentToolCallStatus::Completed,
                 sender_thread_id: sender_thread_id.to_string(),
                 receiver_thread_ids: vec![agent_id.to_string()],
+                multi_agent_version: None,
                 prompt: Some("Continue".to_string()),
                 model: None,
                 reasoning_effort: None,
@@ -1098,6 +1107,7 @@ mod tests {
                 status: CollabAgentToolCallStatus::Completed,
                 sender_thread_id: sender_thread_id.to_string(),
                 receiver_thread_ids: vec![robie_id.to_string()],
+                multi_agent_version: None,
                 prompt: None,
                 model: None,
                 reasoning_effort: None,
