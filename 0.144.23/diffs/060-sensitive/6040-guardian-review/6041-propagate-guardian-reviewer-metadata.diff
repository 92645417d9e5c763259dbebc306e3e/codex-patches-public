diff --git a/codex-rs/analytics/src/analytics_client_tests.rs b/codex-rs/analytics/src/analytics_client_tests.rs
index 709016fef145fc9168b46690aec8a6605aeb30b6..a2f94b618f3c4c14b253bbb510b121793e1afefe 100644
--- a/codex-rs/analytics/src/analytics_client_tests.rs
+++ b/codex-rs/analytics/src/analytics_client_tests.rs
@@ -977,6 +977,7 @@ fn sample_guardian_review_completed(
                 risk_level: None,
                 user_authorization: None,
                 rationale: None,
+                reviewer: None,
             },
             action: GuardianApprovalReviewAction::Command {
                 source: AppServerGuardianCommandSource::Shell,
diff --git a/codex-rs/app-server-protocol/schema/json/ServerNotification.json b/codex-rs/app-server-protocol/schema/json/ServerNotification.json
index 8cd8549f30c841a95af6ce3b2ec8540aae222fd3..2f494a3f68b0a6b87439c967dbc180b3f861d420 100644
--- a/codex-rs/app-server-protocol/schema/json/ServerNotification.json
+++ b/codex-rs/app-server-protocol/schema/json/ServerNotification.json
@@ -1800,6 +1800,16 @@
             "null"
           ]
         },
+        "reviewer": {
+          "anyOf": [
+            {
+              "$ref": "#/definitions/GuardianReviewer"
+            },
+            {
+              "type": "null"
+            }
+          ]
+        },
         "riskLevel": {
           "anyOf": [
             {
@@ -2043,6 +2053,27 @@
       ],
       "type": "string"
     },
+    "GuardianReviewer": {
+      "properties": {
+        "model": {
+          "type": "string"
+        },
+        "reasoningEffort": {
+          "anyOf": [
+            {
+              "$ref": "#/definitions/ReasoningEffort"
+            },
+            {
+              "type": "null"
+            }
+          ]
+        }
+      },
+      "required": [
+        "model"
+      ],
+      "type": "object"
+    },
     "GuardianRiskLevel": {
       "description": "[UNSTABLE] Risk level assigned by approval auto-review.",
       "enum": [
diff --git a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json
index d7e504074620bf77950acca823d8a492629bc310..149e90f470b152d5e530ad3a4fbc618bb4a4cbc5 100644
--- a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json
+++ b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json
@@ -11122,6 +11122,16 @@
               "null"
             ]
           },
+          "reviewer": {
+            "anyOf": [
+              {
+                "$ref": "#/definitions/v2/GuardianReviewer"
+              },
+              {
+                "type": "null"
+              }
+            ]
+          },
           "riskLevel": {
             "anyOf": [
               {
@@ -11365,6 +11375,27 @@
         ],
         "type": "string"
       },
+      "GuardianReviewer": {
+        "properties": {
+          "model": {
+            "type": "string"
+          },
+          "reasoningEffort": {
+            "anyOf": [
+              {
+                "$ref": "#/definitions/v2/ReasoningEffort"
+              },
+              {
+                "type": "null"
+              }
+            ]
+          }
+        },
+        "required": [
+          "model"
+        ],
+        "type": "object"
+      },
       "GuardianRiskLevel": {
         "description": "[UNSTABLE] Risk level assigned by approval auto-review.",
         "enum": [
diff --git a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json
index 093746106cd4c7c1bbeedc6a60c3a722f6359fca..4fbb821e662244e34173abb2524fdd6c7504cc49 100644
--- a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json
+++ b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json
@@ -7349,6 +7349,16 @@
             "null"
           ]
         },
+        "reviewer": {
+          "anyOf": [
+            {
+              "$ref": "#/definitions/GuardianReviewer"
+            },
+            {
+              "type": "null"
+            }
+          ]
+        },
         "riskLevel": {
           "anyOf": [
             {
@@ -7592,6 +7602,27 @@
       ],
       "type": "string"
     },
+    "GuardianReviewer": {
+      "properties": {
+        "model": {
+          "type": "string"
+        },
+        "reasoningEffort": {
+          "anyOf": [
+            {
+              "$ref": "#/definitions/ReasoningEffort"
+            },
+            {
+              "type": "null"
+            }
+          ]
+        }
+      },
+      "required": [
+        "model"
+      ],
+      "type": "object"
+    },
     "GuardianRiskLevel": {
       "description": "[UNSTABLE] Risk level assigned by approval auto-review.",
       "enum": [
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ItemGuardianApprovalReviewCompletedNotification.json b/codex-rs/app-server-protocol/schema/json/v2/ItemGuardianApprovalReviewCompletedNotification.json
index 4e486066113af6efbfee58137328cb36288ce036..dd182f87474b6c013c034b112cc20c3f6c5867e5 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ItemGuardianApprovalReviewCompletedNotification.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ItemGuardianApprovalReviewCompletedNotification.json
@@ -278,6 +278,16 @@
             "null"
           ]
         },
+        "reviewer": {
+          "anyOf": [
+            {
+              "$ref": "#/definitions/GuardianReviewer"
+            },
+            {
+              "type": "null"
+            }
+          ]
+        },
         "riskLevel": {
           "anyOf": [
             {
@@ -521,6 +531,27 @@
       ],
       "type": "string"
     },
+    "GuardianReviewer": {
+      "properties": {
+        "model": {
+          "type": "string"
+        },
+        "reasoningEffort": {
+          "anyOf": [
+            {
+              "$ref": "#/definitions/ReasoningEffort"
+            },
+            {
+              "type": "null"
+            }
+          ]
+        }
+      },
+      "required": [
+        "model"
+      ],
+      "type": "object"
+    },
     "GuardianRiskLevel": {
       "description": "[UNSTABLE] Risk level assigned by approval auto-review.",
       "enum": [
@@ -553,6 +584,11 @@
       ],
       "type": "string"
     },
+    "ReasoningEffort": {
+      "description": "A non-empty reasoning effort value advertised by the model.",
+      "minLength": 1,
+      "type": "string"
+    },
     "RequestPermissionProfile": {
       "additionalProperties": false,
       "properties": {
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ItemGuardianApprovalReviewStartedNotification.json b/codex-rs/app-server-protocol/schema/json/v2/ItemGuardianApprovalReviewStartedNotification.json
index 7d64012fdaef329ed5b1ce423f651aafac03aec4..84087fe4ecaf94f74722cb7966d96ed6a2a9de9f 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ItemGuardianApprovalReviewStartedNotification.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ItemGuardianApprovalReviewStartedNotification.json
@@ -271,6 +271,16 @@
             "null"
           ]
         },
+        "reviewer": {
+          "anyOf": [
+            {
+              "$ref": "#/definitions/GuardianReviewer"
+            },
+            {
+              "type": "null"
+            }
+          ]
+        },
         "riskLevel": {
           "anyOf": [
             {
@@ -514,6 +524,27 @@
       ],
       "type": "string"
     },
+    "GuardianReviewer": {
+      "properties": {
+        "model": {
+          "type": "string"
+        },
+        "reasoningEffort": {
+          "anyOf": [
+            {
+              "$ref": "#/definitions/ReasoningEffort"
+            },
+            {
+              "type": "null"
+            }
+          ]
+        }
+      },
+      "required": [
+        "model"
+      ],
+      "type": "object"
+    },
     "GuardianRiskLevel": {
       "description": "[UNSTABLE] Risk level assigned by approval auto-review.",
       "enum": [
@@ -546,6 +577,11 @@
       ],
       "type": "string"
     },
+    "ReasoningEffort": {
+      "description": "A non-empty reasoning effort value advertised by the model.",
+      "minLength": 1,
+      "type": "string"
+    },
     "RequestPermissionProfile": {
       "additionalProperties": false,
       "properties": {
diff --git a/codex-rs/app-server-protocol/schema/typescript/v2/GuardianApprovalReview.ts b/codex-rs/app-server-protocol/schema/typescript/v2/GuardianApprovalReview.ts
index 11d797eb194e254b3da735c357ddef46d7676ea8..bbb4711b13068dad5d65dd7985593de0ea0691e5 100644
--- a/codex-rs/app-server-protocol/schema/typescript/v2/GuardianApprovalReview.ts
+++ b/codex-rs/app-server-protocol/schema/typescript/v2/GuardianApprovalReview.ts
@@ -2,6 +2,7 @@
 
 // This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
 import type { GuardianApprovalReviewStatus } from "./GuardianApprovalReviewStatus";
+import type { GuardianReviewer } from "./GuardianReviewer";
 import type { GuardianRiskLevel } from "./GuardianRiskLevel";
 import type { GuardianUserAuthorization } from "./GuardianUserAuthorization";
 
@@ -10,4 +11,4 @@ import type { GuardianUserAuthorization } from "./GuardianUserAuthorization";
  * `item/autoApprovalReview/*` notifications. This shape is expected to change
  * soon.
  */
-export type GuardianApprovalReview = { status: GuardianApprovalReviewStatus, riskLevel: GuardianRiskLevel | null, userAuthorization: GuardianUserAuthorization | null, rationale: string | null, };
+export type GuardianApprovalReview = { status: GuardianApprovalReviewStatus, riskLevel: GuardianRiskLevel | null, userAuthorization: GuardianUserAuthorization | null, rationale: string | null, reviewer: GuardianReviewer | null, };
diff --git a/codex-rs/app-server-protocol/schema/typescript/v2/GuardianReviewer.ts b/codex-rs/app-server-protocol/schema/typescript/v2/GuardianReviewer.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f5bc1b7d6fafcc6f585f1b896a4306d4d695e572
--- /dev/null
+++ b/codex-rs/app-server-protocol/schema/typescript/v2/GuardianReviewer.ts
@@ -0,0 +1,6 @@
+// GENERATED CODE! DO NOT MODIFY BY HAND!
+
+// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
+import type { ReasoningEffort } from "../ReasoningEffort";
+
+export type GuardianReviewer = { model: string, reasoningEffort: ReasoningEffort | null, };
diff --git a/codex-rs/app-server-protocol/schema/typescript/v2/index.ts b/codex-rs/app-server-protocol/schema/typescript/v2/index.ts
index acea222fc79c57dcb47cf4026e4c38b1e15a53bc..ece67df404e4f7f2bd4d5add8263a60970c4ced4 100644
--- a/codex-rs/app-server-protocol/schema/typescript/v2/index.ts
+++ b/codex-rs/app-server-protocol/schema/typescript/v2/index.ts
@@ -174,6 +174,7 @@ export type { GuardianApprovalReview } from "./GuardianApprovalReview";
 export type { GuardianApprovalReviewAction } from "./GuardianApprovalReviewAction";
 export type { GuardianApprovalReviewStatus } from "./GuardianApprovalReviewStatus";
 export type { GuardianCommandSource } from "./GuardianCommandSource";
+export type { GuardianReviewer } from "./GuardianReviewer";
 export type { GuardianRiskLevel } from "./GuardianRiskLevel";
 export type { GuardianUserAuthorization } from "./GuardianUserAuthorization";
 export type { GuardianWarningNotification } from "./GuardianWarningNotification";
diff --git a/codex-rs/app-server-protocol/src/protocol/item_builders.rs b/codex-rs/app-server-protocol/src/protocol/item_builders.rs
index 751bf4f67b71c03e468e0ecdce4de9b4095f81b0..df9e117054b226a9db74213a51a5c597879f2054 100644
--- a/codex-rs/app-server-protocol/src/protocol/item_builders.rs
+++ b/codex-rs/app-server-protocol/src/protocol/item_builders.rs
@@ -283,6 +283,7 @@ pub fn guardian_auto_approval_review_notification(
         risk_level: assessment.risk_level.map(Into::into),
         user_authorization: assessment.user_authorization.map(Into::into),
         rationale: assessment.rationale.clone(),
+        reviewer: assessment.reviewer.clone().map(Into::into),
     };
     let action = assessment.action.clone().into();
     match assessment.status {
diff --git a/codex-rs/app-server-protocol/src/protocol/thread_history.rs b/codex-rs/app-server-protocol/src/protocol/thread_history.rs
index 3d17275439b600df892b185730d6956f17c5784a..7c677be9d4d115bc176e172b53e4fa46fe7c0fce 100644
--- a/codex-rs/app-server-protocol/src/protocol/thread_history.rs
+++ b/codex-rs/app-server-protocol/src/protocol/thread_history.rs
@@ -3637,6 +3637,7 @@ mod tests {
                 user_authorization: None,
                 rationale: None,
                 decision_source: None,
+                reviewer: None,
                 action: serde_json::from_value(serde_json::json!({
                     "type": "command",
                     "source": "shell",
@@ -3658,6 +3659,7 @@ mod tests {
                 decision_source: Some(
                     codex_protocol::protocol::GuardianAssessmentDecisionSource::Agent,
                 ),
+                reviewer: None,
                 action: serde_json::from_value(serde_json::json!({
                     "type": "command",
                     "source": "shell",
@@ -3723,6 +3725,7 @@ mod tests {
                 user_authorization: None,
                 rationale: None,
                 decision_source: None,
+                reviewer: None,
                 action: serde_json::from_value(serde_json::json!({
                     "type": "execve",
                     "source": "shell",
diff --git a/codex-rs/app-server-protocol/src/protocol/v2/item.rs b/codex-rs/app-server-protocol/src/protocol/v2/item.rs
index 0850258051f369e89e7ff9bd732000bdc96820e7..401462cbefc52b814f210cb4723bf4394e93a374 100644
--- a/codex-rs/app-server-protocol/src/protocol/v2/item.rs
+++ b/codex-rs/app-server-protocol/src/protocol/v2/item.rs
@@ -20,6 +20,7 @@ pub use codex_extension_items::web_search::WebSearchItem;
 use codex_protocol::approvals::GuardianAssessmentAction as CoreGuardianAssessmentAction;
 use codex_protocol::approvals::GuardianAssessmentDecisionSource as CoreGuardianAssessmentDecisionSource;
 use codex_protocol::approvals::GuardianCommandSource as CoreGuardianCommandSource;
+use codex_protocol::approvals::GuardianReviewer as CoreGuardianReviewer;
 use codex_protocol::items::AgentMessageContent as CoreAgentMessageContent;
 use codex_protocol::items::CollabAgentTool as CoreCollabAgentTool;
 use codex_protocol::items::CollabAgentToolCallStatus as CoreCollabAgentToolCallStatus;
@@ -522,6 +523,24 @@ pub struct GuardianApprovalReview {
     pub risk_level: Option<GuardianRiskLevel>,
     pub user_authorization: Option<GuardianUserAuthorization>,
     pub rationale: Option<String>,
+    pub reviewer: Option<GuardianReviewer>,
+}
+
+#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, JsonSchema, TS)]
+#[serde(rename_all = "camelCase")]
+#[ts(export_to = "v2/")]
+pub struct GuardianReviewer {
+    pub model: String,
+    pub reasoning_effort: Option<ReasoningEffort>,
+}
+
+impl From<CoreGuardianReviewer> for GuardianReviewer {
+    fn from(value: CoreGuardianReviewer) -> Self {
+        Self {
+            model: value.model,
+            reasoning_effort: value.reasoning_effort,
+        }
+    }
 }
 
 #[derive(Serialize, Deserialize, Debug, Clone, Copy, PartialEq, Eq, JsonSchema, TS)]
diff --git a/codex-rs/app-server-protocol/src/protocol/v2/tests.rs b/codex-rs/app-server-protocol/src/protocol/v2/tests.rs
index 92dbd118679852dee4796e019f9d95531d31a61b..870c487f75197eb08a8a328f41b9642b7fbcca1d 100644
--- a/codex-rs/app-server-protocol/src/protocol/v2/tests.rs
+++ b/codex-rs/app-server-protocol/src/protocol/v2/tests.rs
@@ -2414,6 +2414,7 @@ fn automatic_approval_review_deserializes_aborted_status() {
             risk_level: None,
             user_authorization: None,
             rationale: None,
+            reviewer: None,
         }
     );
 }
diff --git a/codex-rs/app-server/src/bespoke_event_handling.rs b/codex-rs/app-server/src/bespoke_event_handling.rs
index 218cc1287154cc6689ffb89dbbd2bd5419f8e264..7a6796d843b9f5d1badb915d159c9bd890af3bc8 100644
--- a/codex-rs/app-server/src/bespoke_event_handling.rs
+++ b/codex-rs/app-server/src/bespoke_event_handling.rs
@@ -2364,6 +2364,7 @@ mod tests {
             } else {
                 Some(codex_protocol::protocol::GuardianAssessmentDecisionSource::Agent)
             },
+            reviewer: None,
             action: serde_json::from_value(json!({
                 "type": "command",
                 "source": "shell",
@@ -2426,6 +2427,7 @@ mod tests {
                 user_authorization: None,
                 rationale: None,
                 decision_source: None,
+                reviewer: None,
                 action: action.clone(),
             },
         );
@@ -2474,6 +2476,10 @@ mod tests {
                 decision_source: Some(
                     codex_protocol::protocol::GuardianAssessmentDecisionSource::Agent,
                 ),
+                reviewer: Some(codex_protocol::approvals::GuardianReviewer {
+                    model: "gpt-5.6-sol".to_string(),
+                    reasoning_effort: Some(codex_protocol::openai_models::ReasoningEffort::High),
+                }),
                 action: action.clone(),
             },
         );
@@ -2497,6 +2503,15 @@ mod tests {
                     Some(codex_app_server_protocol::GuardianUserAuthorization::Low)
                 );
                 assert_eq!(payload.review.rationale.as_deref(), Some("too risky"));
+                assert_eq!(
+                    payload.review.reviewer,
+                    Some(codex_app_server_protocol::GuardianReviewer {
+                        model: "gpt-5.6-sol".to_string(),
+                        reasoning_effort: Some(
+                            codex_protocol::openai_models::ReasoningEffort::High,
+                        ),
+                    })
+                );
                 assert_eq!(payload.action, action.into());
             }
             other => panic!("unexpected notification: {other:?}"),
@@ -2528,6 +2543,7 @@ mod tests {
                 decision_source: Some(
                     codex_protocol::protocol::GuardianAssessmentDecisionSource::Agent,
                 ),
+                reviewer: None,
                 action: action.clone(),
             },
         );
diff --git a/codex-rs/core/src/guardian/review.rs b/codex-rs/core/src/guardian/review.rs
index c3b1f62cf78cf355dc269a0f68262d8850e61325..0aee36ca88cf68cba316c71d848b47c7168bbc90 100644
--- a/codex-rs/core/src/guardian/review.rs
+++ b/codex-rs/core/src/guardian/review.rs
@@ -5,6 +5,7 @@ use codex_analytics::GuardianReviewFailureReason;
 use codex_analytics::GuardianReviewTerminalStatus;
 use codex_analytics::GuardianReviewTrackContext;
 use codex_analytics::GuardianReviewedAction;
+use codex_protocol::approvals::GuardianReviewer;
 use codex_protocol::config_types::ApprovalsReviewer;
 use codex_protocol::protocol::AskForApproval;
 use codex_protocol::protocol::CodexErrorInfo;
@@ -290,6 +291,11 @@ async fn run_guardian_review(
     let assessment_turn_id = guardian_request_turn_id(&request, &turn.sub_id).to_string();
     let action_summary = guardian_assessment_action(&request);
     let reviewed_action = guardian_reviewed_action(&request);
+    let session_config = guardian_review_session_config(session.as_ref(), turn.as_ref()).await;
+    let reviewer = session_config
+        .as_ref()
+        .ok()
+        .map(GuardianReviewSessionConfig::reviewer);
     let review_tracking = GuardianReviewTrackContext::new(
         session.thread_id.to_string(),
         assessment_turn_id.clone(),
@@ -314,6 +320,7 @@ async fn run_guardian_review(
                 user_authorization: None,
                 rationale: None,
                 decision_source: None,
+                reviewer: reviewer.clone(),
                 action: action_summary.clone(),
             }),
         )
@@ -351,6 +358,7 @@ async fn run_guardian_review(
                     user_authorization: None,
                     rationale: None,
                     decision_source: Some(GuardianAssessmentDecisionSource::Agent),
+                    reviewer: reviewer.clone(),
                     action: action_summary,
                 }),
             )
@@ -361,16 +369,25 @@ async fn run_guardian_review(
 
     let schema = guardian_output_schema();
     let terminal_action = action_summary.clone();
-    let (outcome, analytics_result) = Box::pin(run_guardian_review_session_with_retry(
-        session.clone(),
-        turn.clone(),
-        request,
-        retry_reason.clone(),
-        schema,
-        external_cancel,
-        GUARDIAN_REVIEW_MAX_ATTEMPTS,
-    ))
-    .await;
+    let (outcome, analytics_result) = match session_config {
+        Ok(session_config) => {
+            Box::pin(run_guardian_review_session_with_retry_config(
+                session.clone(),
+                turn.clone(),
+                session_config,
+                request,
+                retry_reason.clone(),
+                schema,
+                external_cancel,
+                GUARDIAN_REVIEW_MAX_ATTEMPTS,
+            ))
+            .await
+        }
+        Err(err) => (
+            GuardianReviewOutcome::Error(GuardianReviewError::prompt_build(err)),
+            GuardianReviewAnalyticsResult::without_session(),
+        ),
+    };
 
     let completed_at_ms = now_unix_timestamp_ms();
     let (assessment, count_denial_for_circuit_breaker) = match outcome {
@@ -444,6 +461,7 @@ async fn run_guardian_review(
                             user_authorization: None,
                             rationale: Some(rationale),
                             decision_source: Some(GuardianAssessmentDecisionSource::Agent),
+                            reviewer: reviewer.clone(),
                             action: terminal_action,
                         }),
                     )
@@ -479,6 +497,7 @@ async fn run_guardian_review(
                             user_authorization: None,
                             rationale: None,
                             decision_source: Some(GuardianAssessmentDecisionSource::Agent),
+                            reviewer: reviewer.clone(),
                             action: action_summary,
                         }),
                     )
@@ -577,6 +596,7 @@ async fn run_guardian_review(
                 user_authorization: Some(assessment.user_authorization),
                 rationale: Some(assessment.rationale.clone()),
                 decision_source: Some(GuardianAssessmentDecisionSource::Agent),
+                reviewer,
                 action: terminal_action,
             }),
         )
@@ -670,6 +690,7 @@ pub(crate) fn spawn_approval_request_review(
     rx
 }
 
+#[derive(Clone)]
 pub(super) struct GuardianReviewSessionConfig {
     pub(super) spawn_config: crate::config::Config,
     model: String,
@@ -680,6 +701,15 @@ pub(super) struct GuardianReviewSessionConfig {
     model_override: Option<String>,
 }
 
+impl GuardianReviewSessionConfig {
+    pub(super) fn reviewer(&self) -> GuardianReviewer {
+        GuardianReviewer {
+            model: self.model.clone(),
+            reasoning_effort: self.reasoning_effort.clone(),
+        }
+    }
+}
+
 pub(super) async fn guardian_review_session_config(
     session: &Session,
     turn: &TurnContext,
@@ -785,22 +815,13 @@ pub(super) async fn guardian_review_session_config(
 async fn run_guardian_review_session_before_deadline(
     session: Arc<Session>,
     turn: Arc<TurnContext>,
+    session_config: GuardianReviewSessionConfig,
     request: GuardianApprovalRequest,
     retry_reason: Option<String>,
     schema: serde_json::Value,
     external_cancel: Option<CancellationToken>,
     deadline: Instant,
 ) -> (GuardianReviewOutcome, GuardianReviewAnalyticsResult) {
-    let session_config = match guardian_review_session_config(session.as_ref(), turn.as_ref()).await
-    {
-        Ok(session_config) => session_config,
-        Err(err) => {
-            return (
-                GuardianReviewOutcome::Error(GuardianReviewError::prompt_build(err)),
-                GuardianReviewAnalyticsResult::without_session(),
-            );
-        }
-    };
     let (session_outcome, session_analytics_result) = Box::pin(
         session
             .guardian_review_session
@@ -876,9 +897,10 @@ async fn run_guardian_review_session_before_deadline(
     }
 }
 
-pub(super) async fn run_guardian_review_session_with_retry(
+async fn run_guardian_review_session_with_retry_config(
     session: Arc<Session>,
     turn: Arc<TurnContext>,
+    session_config: GuardianReviewSessionConfig,
     request: GuardianApprovalRequest,
     retry_reason: Option<String>,
     schema: serde_json::Value,
@@ -892,6 +914,7 @@ pub(super) async fn run_guardian_review_session_with_retry(
         let (outcome, mut analytics_result) = run_guardian_review_session_before_deadline(
             Arc::clone(&session),
             Arc::clone(&turn),
+            session_config.clone(),
             request.clone(),
             retry_reason.clone(),
             schema.clone(),
@@ -912,6 +935,39 @@ pub(super) async fn run_guardian_review_session_with_retry(
     }
 }
 
+#[cfg(test)]
+pub(super) async fn run_guardian_review_session_with_retry(
+    session: Arc<Session>,
+    turn: Arc<TurnContext>,
+    request: GuardianApprovalRequest,
+    retry_reason: Option<String>,
+    schema: serde_json::Value,
+    external_cancel: Option<CancellationToken>,
+    max_attempts: i64,
+) -> (GuardianReviewOutcome, GuardianReviewAnalyticsResult) {
+    let session_config = match guardian_review_session_config(session.as_ref(), turn.as_ref()).await
+    {
+        Ok(session_config) => session_config,
+        Err(err) => {
+            return (
+                GuardianReviewOutcome::Error(GuardianReviewError::prompt_build(err)),
+                GuardianReviewAnalyticsResult::without_session(),
+            );
+        }
+    };
+    run_guardian_review_session_with_retry_config(
+        session,
+        turn,
+        session_config,
+        request,
+        retry_reason,
+        schema,
+        external_cancel,
+        max_attempts,
+    )
+    .await
+}
+
 async fn wait_before_guardian_retry(
     attempt_count: i64,
     deadline: Instant,
diff --git a/codex-rs/core/src/guardian/tests.rs b/codex-rs/core/src/guardian/tests.rs
index c59733c512ad4b5e47fe9e9ae79d366204249074..5f7e6bdffbd1956cdc6067241f1b186e69ee140a 100644
--- a/codex-rs/core/src/guardian/tests.rs
+++ b/codex-rs/core/src/guardian/tests.rs
@@ -1429,6 +1429,7 @@ async fn guardian_request_model_for_auto_review(
     String,
     String,
     String,
+    codex_protocol::approvals::GuardianReviewer,
     codex_analytics::GuardianReviewAnalyticsResult,
 )> {
     let server = start_mock_server().await;
@@ -1470,6 +1471,9 @@ async fn guardian_request_model_for_auto_review(
         .auto_review_model_override = auto_review_model_override;
     let parent_model = turn.model_info.slug.clone();
     let preferred_model = turn.provider.approval_review_preferred_model().to_string();
+    let reviewer = crate::guardian::review::guardian_review_session_config(&session, &turn)
+        .await?
+        .reviewer();
     seed_guardian_parent_history(&session, &turn).await;
 
     let (outcome, analytics_result) = run_guardian_review_session_for_test(
@@ -1505,6 +1509,7 @@ async fn guardian_request_model_for_auto_review(
         request_model,
         parent_model,
         preferred_model,
+        reviewer,
         analytics_result,
     ))
 }
@@ -1515,7 +1520,7 @@ async fn guardian_review_uses_model_catalog_override_when_preferred_review_model
     skip_if_no_network!(Ok(()));
 
     let override_model = "guardian-review-model-override".to_string();
-    let (request_model, parent_model, preferred_model, analytics_result) =
+    let (request_model, parent_model, preferred_model, reviewer, analytics_result) =
         guardian_request_model_for_auto_review(
             Some(override_model.clone()),
             GuardianTestCatalog::Bundled,
@@ -1523,6 +1528,7 @@ async fn guardian_review_uses_model_catalog_override_when_preferred_review_model
         .await?;
 
     assert_eq!(request_model, override_model);
+    assert_eq!(request_model, reviewer.model);
     assert_ne!(request_model, parent_model);
     assert_ne!(request_model, preferred_model);
     assert_eq!(
@@ -1554,7 +1560,7 @@ async fn guardian_review_uses_preferred_review_model_without_model_catalog_overr
 -> anyhow::Result<()> {
     skip_if_no_network!(Ok(()));
 
-    let (request_model, parent_model, preferred_model, analytics_result) =
+    let (request_model, parent_model, preferred_model, reviewer, analytics_result) =
         guardian_request_model_for_auto_review(
             /*auto_review_model_override*/ None,
             GuardianTestCatalog::Bundled,
@@ -1562,6 +1568,7 @@ async fn guardian_review_uses_preferred_review_model_without_model_catalog_overr
         .await?;
 
     assert_eq!(request_model, preferred_model);
+    assert_eq!(request_model, reviewer.model);
     assert_ne!(request_model, parent_model);
     assert_eq!(
         analytics_result.guardian_catalog_contains_auto_review,
@@ -1592,7 +1599,7 @@ async fn guardian_review_records_missing_auto_review_model_in_analytics_metadata
 -> anyhow::Result<()> {
     skip_if_no_network!(Ok(()));
 
-    let (request_model, parent_model, preferred_model, analytics_result) =
+    let (request_model, parent_model, preferred_model, reviewer, analytics_result) =
         guardian_request_model_for_auto_review(
             /*auto_review_model_override*/ None,
             GuardianTestCatalog::ParentOnly,
@@ -1600,6 +1607,7 @@ async fn guardian_review_records_missing_auto_review_model_in_analytics_metadata
         .await?;
 
     assert_eq!(request_model, parent_model);
+    assert_eq!(request_model, reviewer.model);
     assert_ne!(request_model, preferred_model);
     assert_eq!(
         analytics_result.guardian_catalog_contains_auto_review,
diff --git a/codex-rs/protocol/src/approvals.rs b/codex-rs/protocol/src/approvals.rs
index 5cf4f27f183cb5317224aeec29a9d1d03f7d4ec0..a7c4a85cbf73df493277161ae1d0e95f9c530fd5 100644
--- a/codex-rs/protocol/src/approvals.rs
+++ b/codex-rs/protocol/src/approvals.rs
@@ -1,6 +1,7 @@
 use crate::mcp::RequestId;
 use crate::models::AdditionalPermissionProfile;
 use crate::models::PermissionProfile;
+use crate::openai_models::ReasoningEffort;
 use crate::parse_command::ParsedCommand;
 use crate::protocol::FileChange;
 use crate::protocol::ReviewDecision;
@@ -175,6 +176,15 @@ pub struct NetworkPolicyAmendment {
     pub action: NetworkPolicyRuleAction,
 }
 
+/// Resolved model identity used for one Guardian assessment lifecycle.
+#[derive(Debug, Clone, Deserialize, Serialize, PartialEq, Eq, JsonSchema, TS)]
+pub struct GuardianReviewer {
+    pub model: String,
+    #[serde(default, skip_serializing_if = "Option::is_none")]
+    #[ts(optional)]
+    pub reasoning_effort: Option<ReasoningEffort>,
+}
+
 #[derive(Debug, Clone, Deserialize, Serialize, PartialEq, JsonSchema, TS)]
 pub struct GuardianAssessmentEvent {
     /// Stable identifier for this guardian review lifecycle.
@@ -210,6 +220,10 @@ pub struct GuardianAssessmentEvent {
     #[serde(default, skip_serializing_if = "Option::is_none")]
     #[ts(optional)]
     pub decision_source: Option<GuardianAssessmentDecisionSource>,
+    /// Model and reasoning effort that produced this assessment.
+    #[serde(default, skip_serializing_if = "Option::is_none")]
+    #[ts(optional)]
+    pub reviewer: Option<GuardianReviewer>,
     /// Canonical action payload that was reviewed.
     pub action: GuardianAssessmentAction,
 }
