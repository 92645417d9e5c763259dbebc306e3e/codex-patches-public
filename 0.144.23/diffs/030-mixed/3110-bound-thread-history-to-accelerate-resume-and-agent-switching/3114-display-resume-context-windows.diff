diff --git a/codex-rs/tui/src/app.rs b/codex-rs/tui/src/app.rs
index 40e3079dcf70cc48ec3ccf85642afeab95ec5393..6123ca3e2715cf7202913ad95c2e2ddfe28385e5 100644
--- a/codex-rs/tui/src/app.rs
+++ b/codex-rs/tui/src/app.rs
@@ -1076,11 +1076,17 @@ See the Codex keymap documentation for supported actions and examples."
         }
         let initial_session_started_at = Instant::now();
         if let Some(started) = initial_started_thread {
-            let thread_id = started.session.thread_id;
-            if started.blocks_direct_input {
+            let AppServerStartedThread {
+                session,
+                turns,
+                blocks_direct_input,
+                history_window_metadata,
+            } = started;
+            let thread_id = session.thread_id;
+            if blocks_direct_input {
                 app.mark_primary_thread_parent_owned(thread_id);
             }
-            app.enqueue_primary_thread_session(started.session, started.turns)
+            app.enqueue_primary_thread_session(session, turns, history_window_metadata)
                 .await?;
             if should_prompt_for_paused_goal_after_startup_resume {
                 app.maybe_prompt_resume_paused_goal_after_resume(&mut app_server, thread_id)
diff --git a/codex-rs/tui/src/app/session_lifecycle.rs b/codex-rs/tui/src/app/session_lifecycle.rs
index 29506ffc931a6d797e839be30eb7640d2c9b70e0..9df698614aab10fb946b9ff9e36eec37c6a053a6 100644
--- a/codex-rs/tui/src/app/session_lifecycle.rs
+++ b/codex-rs/tui/src/app/session_lifecycle.rs
@@ -546,10 +546,16 @@ impl App {
             .set_queue_submissions_until_session_configured(/*queue*/ false);
         match result {
             Ok(started) => {
-                if started.blocks_direct_input {
-                    self.mark_primary_thread_parent_owned(started.session.thread_id);
+                let AppServerStartedThread {
+                    session,
+                    turns,
+                    blocks_direct_input,
+                    history_window_metadata,
+                } = started;
+                if blocks_direct_input {
+                    self.mark_primary_thread_parent_owned(session.thread_id);
                 }
-                self.enqueue_primary_thread_session(started.session, started.turns)
+                self.enqueue_primary_thread_session(session, turns, history_window_metadata)
                     .await?;
                 self.chat_widget.maybe_send_next_queued_input();
             }
@@ -655,13 +661,20 @@ impl App {
             initial_user_message,
         );
         self.replace_chat_widget(ChatWidget::new_with_app_event(init));
-        if started.blocks_direct_input {
-            self.mark_primary_thread_parent_owned(started.session.thread_id);
+        let AppServerStartedThread {
+            session,
+            turns,
+            blocks_direct_input,
+            history_window_metadata,
+        } = started;
+        if blocks_direct_input {
+            self.mark_primary_thread_parent_owned(session.thread_id);
         }
         self.enqueue_primary_thread_session_with_presentation(
-            started.session,
-            started.turns,
+            session,
+            turns,
             presentation,
+            history_window_metadata,
         )
         .await?;
         self.backfill_loaded_subagent_threads(app_server).await;
diff --git a/codex-rs/tui/src/app/tests.rs b/codex-rs/tui/src/app/tests.rs
index 9c5b49f6124c480b4ef6438ef0b1fd18c538e52a..86633a667048b601137139b5aecb9522e095ee01 100644
--- a/codex-rs/tui/src/app/tests.rs
+++ b/codex-rs/tui/src/app/tests.rs
@@ -337,6 +337,7 @@ async fn enqueue_primary_thread_session_replays_buffered_approval_after_attach()
     app.enqueue_primary_thread_session(
         test_thread_session(thread_id, test_path_buf("/tmp/project")),
         Vec::new(),
+        Default::default(),
     )
     .await?;
 
@@ -385,6 +386,7 @@ async fn resolved_buffered_approval_does_not_become_actionable_after_drain() ->
     app.enqueue_primary_thread_session(
         test_thread_session(thread_id, test_path_buf("/tmp/project")),
         Vec::new(),
+        Default::default(),
     )
     .await?;
     while app_event_rx.try_recv().is_ok() {}
@@ -482,6 +484,7 @@ async fn enqueue_primary_thread_session_replays_turns_before_initial_prompt_subm
                 }],
             }],
         )],
+        Default::default(),
     )
     .await?;
 
@@ -1736,8 +1739,12 @@ fn selected_and_resumed_threads_use_server_capability_for_v1_and_v2_children() -
             .start_thread(app.chat_widget.config_ref())
             .await?;
         let root_thread_id = root.session.thread_id;
-        app.enqueue_primary_thread_session(root.session, root.turns)
-            .await?;
+        app.enqueue_primary_thread_session(
+            root.session,
+            root.turns,
+            root.history_window_metadata,
+        )
+        .await?;
 
         let rollout_dir = app
             .config
@@ -3925,6 +3932,7 @@ async fn primary_thread_ignores_child_mcp_startup_notifications() {
             session: test_thread_session(child_thread_id, test_path_buf("/tmp/child")),
             turns: Vec::new(),
             blocks_direct_input: false,
+            history_window_metadata: Default::default(),
         },
         &mut child_snapshot,
     )
@@ -4580,8 +4588,12 @@ async fn set_thread_goal_draft_materializes_long_objective_and_confirms_before_p
         .start_thread(app.chat_widget.config_ref())
         .await?;
     let thread_id = started.session.thread_id;
-    app.enqueue_primary_thread_session(started.session, started.turns)
-        .await?;
+    app.enqueue_primary_thread_session(
+        started.session,
+        started.turns,
+        started.history_window_metadata,
+    )
+    .await?;
     let objective = "x".repeat(MAX_THREAD_GOAL_OBJECTIVE_CHARS + 1);
 
     app.set_thread_goal_draft(
@@ -5752,8 +5764,12 @@ async fn prompt_edit_forks_before_selected_prompt_and_preserves_source() -> Resu
             crate::app_server_session::ResumeModelSettings::OverrideFromCurrentConfig,
         )
         .await?;
-    app.enqueue_primary_thread_session(started.session, started.turns)
-        .await?;
+    app.enqueue_primary_thread_session(
+        started.session,
+        started.turns,
+        started.history_window_metadata,
+    )
+    .await?;
     while app_event_rx.try_recv().is_ok() {}
     let source_before = std::fs::read_to_string(&source_path)?;
     let mut tui = crate::tui::test_support::make_test_tui()?;
@@ -5861,8 +5877,12 @@ async fn prompt_edit_before_first_prompt_starts_fresh_thread() -> Result<()> {
             crate::app_server_session::ResumeModelSettings::OverrideFromCurrentConfig,
         )
         .await?;
-    app.enqueue_primary_thread_session(started.session, started.turns)
-        .await?;
+    app.enqueue_primary_thread_session(
+        started.session,
+        started.turns,
+        started.history_window_metadata,
+    )
+    .await?;
     while app_event_rx.try_recv().is_ok() {}
     let mut tui = crate::tui::test_support::make_test_tui()?;
 
@@ -6116,6 +6136,7 @@ async fn refreshed_snapshot_session_persists_resumed_turns() {
             session: resumed_session.clone(),
             turns: resumed_turns.clone(),
             blocks_direct_input: true,
+            history_window_metadata: Default::default(),
         },
         &mut snapshot,
     )
@@ -6421,9 +6442,13 @@ async fn interrupt_without_active_turn_is_treated_as_handled() {
             .await
             .expect("thread/start should succeed");
         let thread_id = started.session.thread_id;
-        app.enqueue_primary_thread_session(started.session, started.turns)
-            .await
-            .expect("primary thread should be registered");
+        app.enqueue_primary_thread_session(
+            started.session,
+            started.turns,
+            started.history_window_metadata,
+        )
+        .await
+        .expect("primary thread should be registered");
         let op = AppCommand::interrupt();
 
         let handled = Box::pin(app.try_submit_active_thread_op_via_app_server(
@@ -6454,9 +6479,13 @@ async fn override_turn_context_sends_thread_settings_update() {
         let thread_id = started.session.thread_id;
         let initial_model = started.session.model.clone();
         let initial_effort = started.session.reasoning_effort.clone();
-        app.enqueue_primary_thread_session(started.session, started.turns)
-            .await
-            .expect("primary thread should be registered");
+        app.enqueue_primary_thread_session(
+            started.session,
+            started.turns,
+            started.history_window_metadata,
+        )
+        .await
+        .expect("primary thread should be registered");
         let service_tier = ServiceTier::Fast.request_value().to_string();
         let collaboration_mode = CollaborationMode {
             mode: ModeKind::Plan,
diff --git a/codex-rs/tui/src/app/tests/startup.rs b/codex-rs/tui/src/app/tests/startup.rs
index 193ea5aa12890d66aed35dd964b15727295ced09..01b1663094ef1892e40bd539699d664a4d8f20ac 100644
--- a/codex-rs/tui/src/app/tests/startup.rs
+++ b/codex-rs/tui/src/app/tests/startup.rs
@@ -163,6 +163,7 @@ async fn startup_thread_started_submits_queued_startup_input() {
             session: test_thread_session(thread_id, test_path_buf("/tmp/project")),
             turns: Vec::new(),
             blocks_direct_input: false,
+            history_window_metadata: Default::default(),
         }),
     )
     .await
@@ -241,6 +242,7 @@ fn stale_startup_thread_started_removes_local_routing_state() -> Result<()> {
                     session: test_thread_session(stale_thread_id, test_path_buf("/tmp/project")),
                     turns: Vec::new(),
                     blocks_direct_input: false,
+                    history_window_metadata: Default::default(),
                 }),
             )
             .await?;
diff --git a/codex-rs/tui/src/app/thread_routing.rs b/codex-rs/tui/src/app/thread_routing.rs
index bf57cdc56deb98104c8ca9ea1ec95316a0f58a44..7a1e318660fbbee4d4a97e20ec524e5846b6819b 100644
--- a/codex-rs/tui/src/app/thread_routing.rs
+++ b/codex-rs/tui/src/app/thread_routing.rs
@@ -14,17 +14,21 @@ struct ResumeHistoryLoadSummary {
     messages: u64,
     turns: u64,
     app_server_turns: u64,
-    context_windows: u64,
+    loaded_context_windows: u64,
+    omitted_context_windows: u64,
 }
 
-fn resume_history_load_summary(turns: &[Turn]) -> Option<ResumeHistoryLoadSummary> {
+fn resume_history_load_summary(
+    turns: &[Turn],
+    metadata: codex_app_server_protocol::ThreadResumeHistoryWindowMetadata,
+) -> Option<ResumeHistoryLoadSummary> {
     if turns.is_empty() {
         return None;
     }
 
     let mut messages = 0;
     let mut visible_turns = 0;
-    let mut context_windows = 0;
+    let mut visible_context_windows = 0;
     for turn in turns {
         let mut has_visible_conversation = false;
         for item in &turn.items {
@@ -34,7 +38,7 @@ fn resume_history_load_summary(turns: &[Turn]) -> Option<ResumeHistoryLoadSummar
                     has_visible_conversation = true;
                 }
                 ThreadItem::ContextCompaction { .. } => {
-                    context_windows += 1;
+                    visible_context_windows += 1;
                 }
                 _ => {}
             }
@@ -48,7 +52,12 @@ fn resume_history_load_summary(turns: &[Turn]) -> Option<ResumeHistoryLoadSummar
         messages,
         turns: visible_turns,
         app_server_turns: turns.len() as u64,
-        context_windows,
+        loaded_context_windows: if metadata.loaded_context_windows == 0 {
+            visible_context_windows
+        } else {
+            u64::from(metadata.loaded_context_windows)
+        },
+        omitted_context_windows: u64::from(metadata.omitted_context_windows),
     })
 }
 impl App {
@@ -1201,11 +1210,13 @@ impl App {
         &mut self,
         session: ThreadSessionState,
         turns: Vec<Turn>,
+        history_window_metadata: codex_app_server_protocol::ThreadResumeHistoryWindowMetadata,
     ) -> Result<()> {
         self.enqueue_primary_thread_session_with_presentation(
             session,
             turns,
             ThreadAttachPresentation::SessionLineage,
+            history_window_metadata,
         )
         .await
     }
@@ -1215,6 +1226,7 @@ impl App {
         session: ThreadSessionState,
         turns: Vec<Turn>,
         presentation: ThreadAttachPresentation,
+        history_window_metadata: codex_app_server_protocol::ThreadResumeHistoryWindowMetadata,
     ) -> Result<()> {
         let thread_id = session.thread_id;
         self.primary_thread_id = Some(thread_id);
@@ -1239,7 +1251,7 @@ impl App {
                 self.chat_widget.handle_prompt_edit_thread_session(session);
             }
         }
-        let resume_history_summary = resume_history_load_summary(&turns);
+        let resume_history_summary = resume_history_load_summary(&turns, history_window_metadata);
         let should_buffer_initial_replay = !turns.is_empty();
         if should_buffer_initial_replay {
             self.app_event_tx
@@ -1253,7 +1265,8 @@ impl App {
                     summary.messages,
                     summary.turns,
                     summary.app_server_turns,
-                    summary.context_windows,
+                    summary.loaded_context_windows,
+                    summary.omitted_context_windows,
                 ),
             )));
         }
@@ -1696,47 +1709,80 @@ mod tests {
 
     #[test]
     fn resume_history_summary_counts_visible_turns() {
-        let summary = resume_history_load_summary(&[
-            test_turn(
-                "turn-1",
-                vec![
-                    ThreadItem::UserMessage {
-                        id: "user-1".to_string(),
+        let summary = resume_history_load_summary(
+            &[
+                test_turn(
+                    "turn-1",
+                    vec![
+                        ThreadItem::UserMessage {
+                            id: "user-1".to_string(),
+                            client_id: None,
+                            content: Vec::new(),
+                        },
+                        ThreadItem::AgentMessage {
+                            id: "agent-1".to_string(),
+                            text: "reply".to_string(),
+                            phase: None,
+                            memory_citation: None,
+                        },
+                    ],
+                ),
+                test_turn(
+                    "compact-only",
+                    vec![ThreadItem::ContextCompaction {
+                        id: "compact-1".to_string(),
+                    }],
+                ),
+                test_turn(
+                    "turn-2",
+                    vec![ThreadItem::UserMessage {
+                        id: "user-2".to_string(),
                         client_id: None,
                         content: Vec::new(),
-                    },
-                    ThreadItem::AgentMessage {
-                        id: "agent-1".to_string(),
-                        text: "reply".to_string(),
-                        phase: None,
-                        memory_citation: None,
-                    },
-                ],
-            ),
-            test_turn(
+                    }],
+                ),
+            ],
+            Default::default(),
+        )
+        .expect("summary");
+
+        assert_eq!(
+            summary,
+            ResumeHistoryLoadSummary {
+                messages: 3,
+                turns: 2,
+                app_server_turns: 3,
+                loaded_context_windows: 1,
+                omitted_context_windows: 0,
+            }
+        );
+    }
+
+    #[test]
+    fn resume_history_summary_uses_metadata_window_counts_when_present() {
+        let summary = resume_history_load_summary(
+            &[test_turn(
                 "compact-only",
                 vec![ThreadItem::ContextCompaction {
                     id: "compact-1".to_string(),
                 }],
-            ),
-            test_turn(
-                "turn-2",
-                vec![ThreadItem::UserMessage {
-                    id: "user-2".to_string(),
-                    client_id: None,
-                    content: Vec::new(),
-                }],
-            ),
-        ])
+            )],
+            codex_app_server_protocol::ThreadResumeHistoryWindowMetadata {
+                loaded_context_windows: 1,
+                omitted_context_windows: 63,
+                ..Default::default()
+            },
+        )
         .expect("summary");
 
         assert_eq!(
             summary,
             ResumeHistoryLoadSummary {
-                messages: 3,
-                turns: 2,
-                app_server_turns: 3,
-                context_windows: 1,
+                messages: 0,
+                turns: 0,
+                app_server_turns: 1,
+                loaded_context_windows: 1,
+                omitted_context_windows: 63,
             }
         );
     }
diff --git a/codex-rs/tui/src/app_server_session.rs b/codex-rs/tui/src/app_server_session.rs
index 49c7eff2d6c6d8f45f7f2dbc40ba2a5b6f88528c..27c9dab421483f23edcdef143c6e4740602b515b 100644
--- a/codex-rs/tui/src/app_server_session.rs
+++ b/codex-rs/tui/src/app_server_session.rs
@@ -217,6 +217,8 @@ pub(crate) struct AppServerStartedThread {
     pub(crate) session: ThreadSessionState,
     pub(crate) turns: Vec<Turn>,
     pub(crate) blocks_direct_input: bool,
+    pub(crate) history_window_metadata:
+        codex_app_server_protocol::ThreadResumeHistoryWindowMetadata,
 }
 
 pub(crate) fn source_agent_path(source: &SessionSource) -> Option<String> {
@@ -1659,6 +1661,7 @@ async fn started_thread_from_start_response(
         session,
         turns: response.thread.turns,
         blocks_direct_input,
+        history_window_metadata: Default::default(),
     })
 }
 
@@ -1676,6 +1679,7 @@ async fn started_thread_from_resume_response(
         session,
         turns: response.thread.turns,
         blocks_direct_input,
+        history_window_metadata: response.history_window_metadata,
     })
 }
 
@@ -1693,6 +1697,7 @@ async fn started_thread_from_fork_response(
         session,
         turns: response.thread.turns,
         blocks_direct_input,
+        history_window_metadata: Default::default(),
     })
 }
 
diff --git a/codex-rs/tui/src/history_cell/separators.rs b/codex-rs/tui/src/history_cell/separators.rs
index 0c24f5d674914cc7e1caef114372537936df2a64..6347ae723537beda0a14161992d5b98e4628c161 100644
--- a/codex-rs/tui/src/history_cell/separators.rs
+++ b/codex-rs/tui/src/history_cell/separators.rs
@@ -29,7 +29,8 @@ pub struct ResumeHistoryLoadedSeparator {
     messages: u64,
     turns: u64,
     app_server_turns: u64,
-    context_windows: u64,
+    loaded_context_windows: u64,
+    omitted_context_windows: u64,
 }
 
 impl ResumeHistoryLoadedSeparator {
@@ -37,13 +38,15 @@ impl ResumeHistoryLoadedSeparator {
         messages: u64,
         turns: u64,
         app_server_turns: u64,
-        context_windows: u64,
+        loaded_context_windows: u64,
+        omitted_context_windows: u64,
     ) -> Self {
         Self {
             messages,
             turns,
             app_server_turns,
-            context_windows,
+            loaded_context_windows,
+            omitted_context_windows,
         }
     }
 
@@ -52,10 +55,21 @@ impl ResumeHistoryLoadedSeparator {
         let turns = pluralize(self.turns, "turn", "turns");
         let app_server_turns =
             pluralize(self.app_server_turns, "app-server turn", "app-server turns");
-        let context_windows = pluralize(self.context_windows, "context", "contexts");
+        let loaded_context_windows = pluralize(self.loaded_context_windows, "context", "contexts");
+        let context_windows = if self.omitted_context_windows == 0 {
+            format!(
+                "{} {loaded_context_windows} loaded",
+                self.loaded_context_windows
+            )
+        } else {
+            format!(
+                "{} {loaded_context_windows} loaded, {} omitted",
+                self.loaded_context_windows, self.omitted_context_windows
+            )
+        };
         format!(
-            "Loaded {} {messages}, {} {turns}, {} {app_server_turns}, {} {context_windows}",
-            self.messages, self.turns, self.app_server_turns, self.context_windows
+            "Loaded {} {messages}, {} {turns}, {} {app_server_turns}, {context_windows}",
+            self.messages, self.turns, self.app_server_turns
         )
     }
 }
diff --git a/codex-rs/tui/src/history_cell/tests.rs b/codex-rs/tui/src/history_cell/tests.rs
index 55e098e16b60cfa991dfdb51e35c6aa95832920b..3e63c9e5be9e9a85ae4cfc4151b3537091dcf8a5 100644
--- a/codex-rs/tui/src/history_cell/tests.rs
+++ b/codex-rs/tui/src/history_cell/tests.rs
@@ -607,14 +607,18 @@ fn final_message_separator_includes_worked_label_after_one_minute() {
 
 #[test]
 fn resume_history_loaded_separator_includes_resume_counts() {
-    let cell = ResumeHistoryLoadedSeparator::new(3, 2, 4, 1);
+    let cell = ResumeHistoryLoadedSeparator::new(3, 2, 4, 1, 63);
     let rendered = render_lines(&cell.display_lines(/*width*/ 120));
 
     assert_eq!(rendered.len(), 1);
-    assert!(rendered[0].contains("Loaded 3 messages, 2 turns, 4 app-server turns, 1 context"));
+    assert!(
+        rendered[0].contains(
+            "Loaded 3 messages, 2 turns, 4 app-server turns, 1 context loaded, 63 omitted"
+        )
+    );
     assert_eq!(
         render_lines(&cell.raw_lines()),
-        vec!["Loaded 3 messages, 2 turns, 4 app-server turns, 1 context"]
+        vec!["Loaded 3 messages, 2 turns, 4 app-server turns, 1 context loaded, 63 omitted"]
     );
 }
 
