diff --git a/codex-rs/tui/src/app.rs b/codex-rs/tui/src/app.rs
index 6123ca3e27..bf5303b15c 100644
--- a/codex-rs/tui/src/app.rs
+++ b/codex-rs/tui/src/app.rs
@@ -502,6 +502,16 @@ struct InitialHistoryReplayBuffer {
     render_from_transcript_tail: bool,
 }
 
+struct CachedThreadPresentation {
+    chat_widget: ChatWidget,
+    transcript_cells: Vec<Arc<dyn HistoryCell>>,
+    deferred_history_lines: Vec<crate::terminal_hyperlinks::HyperlinkLine>,
+    has_emitted_history_lines: bool,
+    transcript_reflow: TranscriptReflowState,
+    initial_history_replay_buffer: Option<InitialHistoryReplayBuffer>,
+    event_sequence: u64,
+}
+
 pub(crate) struct App {
     model_catalog: Arc<ModelCatalog>,
     pub(crate) session_telemetry: SessionTelemetry,
@@ -568,6 +578,7 @@ pub(crate) struct App {
     windows_sandbox: WindowsSandboxState,
 
     thread_event_channels: HashMap<ThreadId, ThreadEventChannel>,
+    thread_presentations: HashMap<ThreadId, Box<CachedThreadPresentation>>,
     thread_event_listener_tasks: HashMap<ThreadId, JoinHandle<()>>,
     agent_navigation: AgentNavigationState,
     side_threads: HashMap<ThreadId, SideThreadState>,
@@ -1057,6 +1068,7 @@ See the Codex keymap documentation for supported actions and examples."
             pending_shutdown_exit_thread_id: None,
             windows_sandbox: WindowsSandboxState::default(),
             thread_event_channels: HashMap::new(),
+            thread_presentations: HashMap::new(),
             thread_event_listener_tasks: HashMap::new(),
             agent_navigation: AgentNavigationState::default(),
             side_threads: HashMap::new(),
diff --git a/codex-rs/tui/src/app/background_requests.rs b/codex-rs/tui/src/app/background_requests.rs
index 2e1ab5e6cb..ac8c1c1425 100644
--- a/codex-rs/tui/src/app/background_requests.rs
+++ b/codex-rs/tui/src/app/background_requests.rs
@@ -578,17 +578,7 @@ impl App {
 
         let should_send = {
             let mut guard = store.lock().await;
-            guard
-                .buffer
-                .push_back(ThreadBufferedEvent::FeedbackSubmission(event.clone()));
-            if guard.buffer.len() > guard.capacity
-                && let Some(removed) = guard.buffer.pop_front()
-                && let ThreadBufferedEvent::Request(request) = &removed
-            {
-                guard
-                    .pending_interactive_replay
-                    .note_evicted_server_request(request);
-            }
+            guard.push_buffered_event(ThreadBufferedEvent::FeedbackSubmission(event.clone()));
             guard.active
         };
 
diff --git a/codex-rs/tui/src/app/session_lifecycle.rs b/codex-rs/tui/src/app/session_lifecycle.rs
index 9df698614a..66265c4b3d 100644
--- a/codex-rs/tui/src/app/session_lifecycle.rs
+++ b/codex-rs/tui/src/app/session_lifecycle.rs
@@ -351,10 +351,15 @@ impl App {
     /// Replaces the chat widget and re-seeds the new widget's collab metadata from the navigation
     /// cache.
     ///
-    /// Thread switches reconstruct the `ChatWidget`, which loses the `collab_agent_metadata` map.
-    /// This helper copies every known nickname/role from `AgentNavigationState` into the
-    /// replacement widget so that replayed collab items render agent names immediately.
-    pub(super) fn replace_chat_widget(&mut self, mut chat_widget: ChatWidget) {
+    /// Cold thread attachment reconstructs the `ChatWidget`, which loses the
+    /// `collab_agent_metadata` map. This helper copies every known nickname/role from
+    /// `AgentNavigationState` into the replacement widget so replayed collab items render agent
+    /// names immediately. Hot thread navigation swaps the previously rendered widget back in.
+    pub(super) fn replace_chat_widget(&mut self, chat_widget: ChatWidget) {
+        drop(self.swap_chat_widget(chat_widget));
+    }
+
+    fn swap_chat_widget(&mut self, mut chat_widget: ChatWidget) -> ChatWidget {
         chat_widget.set_app_server_supports_detach(self.app_server_target.supports_detach());
         // Transfer the last-written terminal title to the replacement widget
         // so it knows what OSC title is currently displayed. Without this, the
@@ -372,8 +377,61 @@ impl App {
                 entry.agent_role.clone(),
             );
         }
-        self.chat_widget = chat_widget;
+        let previous = std::mem::replace(&mut self.chat_widget, chat_widget);
         self.sync_active_agent_label();
+        previous
+    }
+
+    fn install_thread_presentation(
+        &mut self,
+        presentation: Box<CachedThreadPresentation>,
+        previous_event_sequence: u64,
+    ) -> Box<CachedThreadPresentation> {
+        let CachedThreadPresentation {
+            chat_widget,
+            transcript_cells,
+            deferred_history_lines,
+            has_emitted_history_lines,
+            transcript_reflow,
+            initial_history_replay_buffer,
+            event_sequence: _,
+        } = *presentation;
+        let previous_chat_widget = self.swap_chat_widget(chat_widget);
+        let previous_transcript_cells =
+            std::mem::replace(&mut self.transcript_cells, transcript_cells);
+        let previous_deferred_history_lines =
+            std::mem::replace(&mut self.deferred_history_lines, deferred_history_lines);
+        let previous_has_emitted_history_lines = std::mem::replace(
+            &mut self.has_emitted_history_lines,
+            has_emitted_history_lines,
+        );
+        let previous_transcript_reflow =
+            std::mem::replace(&mut self.transcript_reflow, transcript_reflow);
+        let previous_initial_history_replay_buffer = std::mem::replace(
+            &mut self.initial_history_replay_buffer,
+            initial_history_replay_buffer,
+        );
+        Box::new(CachedThreadPresentation {
+            chat_widget: previous_chat_widget,
+            transcript_cells: previous_transcript_cells,
+            deferred_history_lines: previous_deferred_history_lines,
+            has_emitted_history_lines: previous_has_emitted_history_lines,
+            transcript_reflow: previous_transcript_reflow,
+            initial_history_replay_buffer: previous_initial_history_replay_buffer,
+            event_sequence: previous_event_sequence,
+        })
+    }
+
+    fn empty_thread_presentation(chat_widget: ChatWidget) -> Box<CachedThreadPresentation> {
+        Box::new(CachedThreadPresentation {
+            chat_widget,
+            transcript_cells: Vec::new(),
+            deferred_history_lines: Vec::new(),
+            has_emitted_history_lines: false,
+            transcript_reflow: TranscriptReflowState::default(),
+            initial_history_replay_buffer: None,
+            event_sequence: 0,
+        })
     }
 
     pub(super) async fn select_agent_thread(
@@ -386,9 +444,10 @@ impl App {
             return Ok(());
         }
 
-        if !self
-            .refresh_agent_picker_thread_liveness(app_server, thread_id)
-            .await
+        if !self.thread_event_channels.contains_key(&thread_id)
+            && !self
+                .refresh_agent_picker_thread_liveness(app_server, thread_id)
+                .await
         {
             self.chat_widget
                 .add_error_message(format!("Agent thread {thread_id} is no longer available."));
@@ -423,41 +482,77 @@ impl App {
             return Ok(());
         }
         let previous_thread_id = self.active_thread_id;
+        self.drain_active_thread_events(tui).await?;
         self.store_active_thread_receiver().await;
+        let previous_event_sequence = match previous_thread_id {
+            Some(previous_thread_id) => self
+                .thread_event_sequence(previous_thread_id)
+                .await
+                .unwrap_or_default(),
+            None => 0,
+        };
         self.active_thread_id = None;
-        let Some((receiver, mut snapshot)) = self.activate_thread_for_replay(thread_id).await
+        let cached_presentation = self.thread_presentations.remove(&thread_id);
+        let cached_event_sequence = cached_presentation
+            .as_ref()
+            .map(|presentation| presentation.event_sequence);
+        let Some((receiver, mut snapshot, reused_cached_presentation)) = self
+            .activate_thread_for_replay(thread_id, cached_event_sequence)
+            .await
         else {
             self.chat_widget
                 .add_error_message(format!("Agent thread {thread_id} is already active."));
             if let Some(previous_thread_id) = previous_thread_id {
                 self.activate_thread_channel(previous_thread_id).await;
             }
+            if let Some(cached_presentation) = cached_presentation {
+                self.thread_presentations
+                    .insert(thread_id, cached_presentation);
+            }
             return Ok(());
         };
 
-        self.refresh_snapshot_session_if_needed(
-            app_server,
-            thread_id,
-            is_replay_only,
-            &mut snapshot,
-        )
-        .await;
+        if !reused_cached_presentation {
+            self.refresh_snapshot_session_if_needed(
+                app_server,
+                thread_id,
+                is_replay_only,
+                &mut snapshot,
+            )
+            .await;
+        }
         let blocks_direct_input = self.agent_navigation.is_parent_owned(thread_id);
 
         self.active_thread_id = Some(thread_id);
         self.active_thread_rx = Some(receiver);
 
-        let init = self.chatwidget_init_for_forked_or_resumed_thread(
-            tui,
-            self.config.clone(),
-            /*initial_user_message*/ None,
-        );
-        self.replace_chat_widget(ChatWidget::new_with_app_event(init));
+        let presentation = match (cached_presentation, reused_cached_presentation) {
+            (Some(presentation), true) => presentation,
+            (_, _) => {
+                let init = self.chatwidget_init_for_forked_or_resumed_thread(
+                    tui,
+                    self.config.clone(),
+                    /*initial_user_message*/ None,
+                );
+                Self::empty_thread_presentation(ChatWidget::new_with_app_event(init))
+            }
+        };
+        let previous_presentation =
+            self.install_thread_presentation(presentation, previous_event_sequence);
+        if let Some(previous_thread_id) = previous_thread_id {
+            self.thread_presentations
+                .insert(previous_thread_id, previous_presentation);
+        }
         if blocks_direct_input {
             self.chat_widget.set_parent_owned_thread();
         }
 
-        self.reset_for_thread_switch(tui)?;
+        if reused_cached_presentation {
+            tui.clear_pending_history_lines();
+            Self::clear_terminal_for_thread_switch(&mut tui.terminal)?;
+        } else {
+            self.reset_for_thread_switch(tui)?;
+        }
         self.replay_thread_snapshot(snapshot, !is_replay_only);
         if is_replay_only {
             let message = if attached_replay_only {
@@ -508,6 +603,7 @@ impl App {
     pub(super) fn reset_thread_event_state(&mut self) {
         self.abort_all_thread_event_listeners();
         self.thread_event_channels.clear();
+        self.thread_presentations.clear();
         self.agent_navigation.clear();
         self.side_threads.clear();
         self.active_thread_id = None;
diff --git a/codex-rs/tui/src/app/side.rs b/codex-rs/tui/src/app/side.rs
index 3bba923ae5..ef61dd804c 100644
--- a/codex-rs/tui/src/app/side.rs
+++ b/codex-rs/tui/src/app/side.rs
@@ -389,6 +389,7 @@ impl App {
     pub(super) async fn discard_thread_local_state(&mut self, thread_id: ThreadId) {
         self.abort_thread_event_listener(thread_id);
         self.thread_event_channels.remove(&thread_id);
+        self.thread_presentations.remove(&thread_id);
         self.side_threads.remove(&thread_id);
         self.agent_navigation.remove(thread_id);
         if self.active_thread_id == Some(thread_id) {
diff --git a/codex-rs/tui/src/app/test_support.rs b/codex-rs/tui/src/app/test_support.rs
index e0eafaa23b..e30c658f75 100644
--- a/codex-rs/tui/src/app/test_support.rs
+++ b/codex-rs/tui/src/app/test_support.rs
@@ -53,6 +53,7 @@ pub(super) async fn make_test_app() -> App {
         pending_shutdown_exit_thread_id: None,
         windows_sandbox: WindowsSandboxState::default(),
         thread_event_channels: HashMap::new(),
+        thread_presentations: HashMap::new(),
         thread_event_listener_tasks: HashMap::new(),
         agent_navigation: AgentNavigationState::default(),
         side_threads: HashMap::new(),
diff --git a/codex-rs/tui/src/app/tests.rs b/codex-rs/tui/src/app/tests.rs
index fea22e244e..af6531d169 100644
--- a/codex-rs/tui/src/app/tests.rs
+++ b/codex-rs/tui/src/app/tests.rs
@@ -1817,6 +1817,14 @@ fn selected_and_resumed_threads_use_server_capability_for_v1_and_v2_children() -
             std::iter::from_fn(|| app_event_rx.try_recv().ok())
                 .any(|event| matches!(event, AppEvent::CodexOp(Op::UserTurn { .. })))
         );
+        let cached_v1_cell = Arc::new(UserHistoryCell {
+            message: "cached v1 presentation".to_string(),
+            text_elements: Vec::new(),
+            local_image_paths: Vec::new(),
+            remote_image_urls: Vec::new(),
+            prompt_identity: None,
+        }) as Arc<dyn HistoryCell>;
+        app.transcript_cells.push(Arc::clone(&cached_v1_cell));
 
         app.select_agent_thread(&mut tui, &mut app_server, child_thread_ids[1])
             .await?;
@@ -1833,6 +1841,15 @@ fn selected_and_resumed_threads_use_server_capability_for_v1_and_v2_children() -
                 .any(|event| matches!(event, AppEvent::CodexOp(Op::UserTurn { .. })))
         );
 
+        app.select_agent_thread(&mut tui, &mut app_server, child_thread_ids[0])
+            .await?;
+        assert!(
+            app.transcript_cells
+                .iter()
+                .any(|cell| Arc::ptr_eq(cell, &cached_v1_cell)),
+            "hot agent navigation should reuse the rendered presentation instead of rebuilding it"
+        );
+
         let resumed = app_server
             .resume_thread(
                 app.config.clone(),
@@ -4495,6 +4512,7 @@ async fn make_test_app() -> App {
         pending_shutdown_exit_thread_id: None,
         windows_sandbox: WindowsSandboxState::default(),
         thread_event_channels: HashMap::new(),
+        thread_presentations: HashMap::new(),
         thread_event_listener_tasks: HashMap::new(),
         agent_navigation: AgentNavigationState::default(),
         side_threads: HashMap::new(),
@@ -4560,6 +4578,7 @@ async fn make_test_app_with_channels() -> (
             pending_shutdown_exit_thread_id: None,
             windows_sandbox: WindowsSandboxState::default(),
             thread_event_channels: HashMap::new(),
+            thread_presentations: HashMap::new(),
             thread_event_listener_tasks: HashMap::new(),
             agent_navigation: AgentNavigationState::default(),
             side_threads: HashMap::new(),
diff --git a/codex-rs/tui/src/app/thread_events.rs b/codex-rs/tui/src/app/thread_events.rs
index 45f1c0321d..1cec426856 100644
--- a/codex-rs/tui/src/app/thread_events.rs
+++ b/codex-rs/tui/src/app/thread_events.rs
@@ -42,6 +42,8 @@ pub(super) struct ThreadEventStore {
     pub(super) session: Option<ThreadSessionState>,
     pub(super) turns: Vec<Turn>,
     pub(super) buffer: VecDeque<ThreadBufferedEvent>,
+    buffer_sequences: VecDeque<u64>,
+    next_sequence: u64,
     pub(super) pending_interactive_replay: PendingInteractiveReplayState,
     pub(super) active_turn_id: Option<String>,
     pub(super) input_state: Option<ThreadInputState>,
@@ -66,6 +68,8 @@ impl ThreadEventStore {
             session: None,
             turns: Vec::new(),
             buffer: VecDeque::new(),
+            buffer_sequences: VecDeque::new(),
+            next_sequence: 0,
             pending_interactive_replay: PendingInteractiveReplayState::default(),
             active_turn_id: None,
             input_state: None,
@@ -92,7 +96,16 @@ impl ThreadEventStore {
     }
 
     pub(super) fn rebase_buffer_after_session_refresh(&mut self) {
-        self.buffer.retain(Self::event_survives_session_refresh);
+        let mut retained_events = VecDeque::new();
+        let mut retained_sequences = VecDeque::new();
+        for (sequence, event) in self.buffer_sequences.drain(..).zip(self.buffer.drain(..)) {
+            if Self::event_survives_session_refresh(&event) {
+                retained_sequences.push_back(sequence);
+                retained_events.push_back(event);
+            }
+        }
+        self.buffer = retained_events;
+        self.buffer_sequences = retained_sequences;
     }
 
     pub(super) fn set_turns(&mut self, turns: Vec<Turn>) {
@@ -104,7 +117,7 @@ impl ThreadEventStore {
         self.turns = turns;
     }
 
-    pub(super) fn push_notification(&mut self, notification: ServerNotification) {
+    pub(super) fn push_notification(&mut self, notification: ServerNotification) -> u64 {
         self.pending_interactive_replay
             .note_server_notification(&notification);
         match &notification {
@@ -121,28 +134,42 @@ impl ThreadEventStore {
             }
             _ => {}
         }
-        self.buffer
-            .push_back(ThreadBufferedEvent::Notification(notification));
-        if self.buffer.len() > self.capacity
-            && let Some(removed) = self.buffer.pop_front()
-            && let ThreadBufferedEvent::Request(request) = &removed
-        {
-            self.pending_interactive_replay
-                .note_evicted_server_request(request);
-        }
+        self.push_buffered_event(ThreadBufferedEvent::Notification(notification))
     }
 
-    pub(super) fn push_request(&mut self, request: ServerRequest) {
+    pub(super) fn push_request(&mut self, request: ServerRequest) -> u64 {
         self.pending_interactive_replay
             .note_server_request(&request);
-        self.buffer.push_back(ThreadBufferedEvent::Request(request));
-        if self.buffer.len() > self.capacity
-            && let Some(removed) = self.buffer.pop_front()
-            && let ThreadBufferedEvent::Request(request) = &removed
-        {
-            self.pending_interactive_replay
-                .note_evicted_server_request(request);
+        self.push_buffered_event(ThreadBufferedEvent::Request(request))
+    }
+
+    pub(super) fn push_buffered_event(&mut self, event: ThreadBufferedEvent) -> u64 {
+        let sequence = self.record_event_sequence();
+        self.buffer.push_back(event);
+        self.buffer_sequences.push_back(sequence);
+        if self.buffer.len() > self.capacity {
+            self.buffer_sequences.pop_front();
+            if let Some(removed) = self.buffer.pop_front()
+                && let ThreadBufferedEvent::Request(request) = &removed
+            {
+                self.pending_interactive_replay
+                    .note_evicted_server_request(request);
+            }
         }
+        sequence
+    }
+
+    pub(super) fn record_transient_event(&mut self) -> u64 {
+        self.record_event_sequence()
+    }
+
+    fn record_event_sequence(&mut self) -> u64 {
+        self.next_sequence = self.next_sequence.saturating_add(1);
+        self.next_sequence
+    }
+
+    pub(super) fn event_sequence(&self) -> u64 {
+        self.next_sequence
     }
 
     pub(super) fn pending_replay_requests(&self) -> Vec<ServerRequest> {
@@ -199,26 +226,62 @@ impl ThreadEventStore {
     }
 
     pub(super) fn snapshot(&self) -> ThreadEventSnapshot {
-        ThreadEventSnapshot {
-            session: self.session.clone(),
-            turns: self.turns.clone(),
+        self.snapshot_after(/*event_sequence*/ None)
+            .expect("a full snapshot cannot be stale")
+    }
+
+    pub(super) fn snapshot_after(
+        &self,
+        event_sequence: Option<u64>,
+    ) -> Option<ThreadEventSnapshot> {
+        let requested_delta = event_sequence.is_some();
+        let event_sequence = event_sequence.unwrap_or_default();
+        if event_sequence > self.next_sequence {
+            return None;
+        }
+        if requested_delta {
+            match self.buffer_sequences.front().copied() {
+                Some(first_retained) if event_sequence.saturating_add(1) < first_retained => {
+                    return None;
+                }
+                None if event_sequence < self.next_sequence => return None,
+                Some(_) | None => {}
+            }
+        }
+        Some(ThreadEventSnapshot {
+            session: if requested_delta {
+                None
+            } else {
+                self.session.clone()
+            },
+            turns: if requested_delta {
+                Vec::new()
+            } else {
+                self.turns.clone()
+            },
             // Thread switches replay buffered events into a rebuilt ChatWidget. Only replay
             // interactive prompts that are still pending, or answered approvals/input will reappear.
             events: self
-                .buffer
+                .buffer_sequences
                 .iter()
-                .filter(|event| match event {
+                .zip(&self.buffer)
+                .filter(|(sequence, _)| **sequence > event_sequence)
+                .filter_map(|(_, event)| match event {
                     ThreadBufferedEvent::Request(request) => self
                         .pending_interactive_replay
-                        .should_replay_snapshot_request(request),
+                        .should_replay_snapshot_request(request)
+                        .then(|| event.clone()),
                     ThreadBufferedEvent::Notification(_)
                     | ThreadBufferedEvent::HistoryEntryResponse(_)
-                    | ThreadBufferedEvent::FeedbackSubmission(_) => true,
+                    | ThreadBufferedEvent::FeedbackSubmission(_) => Some(event.clone()),
                 })
-                .cloned()
                 .collect(),
-            input_state: self.input_state.clone(),
-        }
+            input_state: if requested_delta {
+                None
+            } else {
+                self.input_state.clone()
+            },
+        })
     }
 
     pub(super) fn note_outbound_op<T>(&mut self, op: T)
@@ -630,4 +693,46 @@ mod tests {
             serde_json::to_value(notification).expect("MCP notification should serialize"),
         );
     }
+
+    #[test]
+    fn cached_presentation_snapshot_replays_only_later_events() {
+        let thread_id = ThreadId::new();
+        let session = test_thread_session(thread_id, test_path_buf("/tmp/project"));
+        let turns = vec![test_turn("turn-0", TurnStatus::Completed, Vec::new())];
+        let mut store = ThreadEventStore::new_with_session(/*capacity*/ 8, session, turns);
+        store.push_notification(turn_started_notification(thread_id, "turn-1"));
+        let cached_sequence = store.event_sequence();
+        let expected = turn_completed_notification(thread_id, "turn-1", TurnStatus::Completed);
+        store.push_notification(expected.clone());
+
+        let snapshot = store
+            .snapshot_after(Some(cached_sequence))
+            .expect("cached event sequence should still be retained");
+
+        assert_eq!(snapshot.session, None);
+        assert!(snapshot.turns.is_empty());
+        assert!(snapshot.input_state.is_none());
+        let [ThreadBufferedEvent::Notification(actual)] = snapshot.events.as_slice() else {
+            panic!("expected one delta notification");
+        };
+        assert_eq!(
+            serde_json::to_value(actual).expect("notification should serialize"),
+            serde_json::to_value(expected).expect("notification should serialize")
+        );
+    }
+
+    #[test]
+    fn cached_presentation_snapshot_rejects_an_evicted_delta() {
+        let thread_id = ThreadId::new();
+        let mut store = ThreadEventStore::new(/*capacity*/ 1);
+        let cached_sequence = store.event_sequence();
+        store.push_notification(turn_started_notification(thread_id, "turn-1"));
+        store.push_notification(turn_completed_notification(
+            thread_id,
+            "turn-1",
+            TurnStatus::Completed,
+        ));
+
+        assert!(store.snapshot_after(Some(cached_sequence)).is_none());
+    }
 }
diff --git a/codex-rs/tui/src/app/thread_routing.rs b/codex-rs/tui/src/app/thread_routing.rs
index dedfb88284..f9009ac994 100644
--- a/codex-rs/tui/src/app/thread_routing.rs
+++ b/codex-rs/tui/src/app/thread_routing.rs
@@ -130,16 +130,30 @@ impl App {
         }
     }
 
+    pub(super) async fn thread_event_sequence(&self, thread_id: ThreadId) -> Option<u64> {
+        let channel = self.thread_event_channels.get(&thread_id)?;
+        Some(channel.store.lock().await.event_sequence())
+    }
+
     pub(super) async fn activate_thread_for_replay(
         &mut self,
         thread_id: ThreadId,
-    ) -> Option<(mpsc::Receiver<ThreadBufferedEvent>, ThreadEventSnapshot)> {
+        cached_event_sequence: Option<u64>,
+    ) -> Option<(
+        mpsc::Receiver<ThreadBufferedEvent>,
+        ThreadEventSnapshot,
+        bool,
+    )> {
         let channel = self.thread_event_channels.get_mut(&thread_id)?;
         let receiver = channel.receiver.take()?;
         let mut store = channel.store.lock().await;
         store.active = true;
-        let snapshot = store.snapshot();
-        Some((receiver, snapshot))
+        let (snapshot, reused_cached_presentation) =
+            match cached_event_sequence.and_then(|sequence| store.snapshot_after(Some(sequence))) {
+                Some(snapshot) => (snapshot, true),
+                None => (store.snapshot(), false),
+            };
+        Some((receiver, snapshot, reused_cached_presentation))
     }
 
     pub(super) async fn clear_active_thread(&mut self) {
@@ -1180,17 +1194,9 @@ impl App {
             // retaining another deep copy for thread replay only accumulates already-delivered
             // history data. Inactive responses still need the buffer because they are not sent.
             if !should_send || !matches!(&event, HistoryLookupResponse::Batch { .. }) {
-                guard
-                    .buffer
-                    .push_back(ThreadBufferedEvent::HistoryEntryResponse(event.clone()));
-                if guard.buffer.len() > guard.capacity
-                    && let Some(removed) = guard.buffer.pop_front()
-                    && let ThreadBufferedEvent::Request(request) = &removed
-                {
-                    guard
-                        .pending_interactive_replay
-                        .note_evicted_server_request(request);
-                }
+                guard.push_buffered_event(ThreadBufferedEvent::HistoryEntryResponse(event.clone()));
+            } else {
+                guard.record_transient_event();
             }
             should_send
         };
