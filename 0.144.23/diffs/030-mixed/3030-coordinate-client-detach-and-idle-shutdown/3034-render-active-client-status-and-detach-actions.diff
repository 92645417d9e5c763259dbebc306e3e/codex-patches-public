diff --git a/codex-rs/tui/src/app.rs b/codex-rs/tui/src/app.rs
index 3f17c011a5..95f13a70a8 100644
--- a/codex-rs/tui/src/app.rs
+++ b/codex-rs/tui/src/app.rs
@@ -1010,6 +1010,7 @@ impl App {
             }
         };
         chat_widget.remote_connection = remote_connection;
+        chat_widget.set_app_server_supports_detach(app_server_target.supports_detach());
         let thread_and_widget_ms = thread_and_widget_started_at.elapsed().as_millis();
         chat_widget
             .maybe_prompt_windows_sandbox_enable(should_prompt_windows_sandbox_nux_at_startup);
diff --git a/codex-rs/tui/src/app/app_server_event_targets.rs b/codex-rs/tui/src/app/app_server_event_targets.rs
index 5935e46d68..f4db24b22f 100644
--- a/codex-rs/tui/src/app/app_server_event_targets.rs
+++ b/codex-rs/tui/src/app/app_server_event_targets.rs
@@ -172,6 +172,7 @@ pub(super) fn server_notification_thread_target(
         | ServerNotification::EnvironmentConnected(_)
         | ServerNotification::EnvironmentDisconnected(_)
         | ServerNotification::RemoteControlStatusChanged(_)
+        | ServerNotification::ActiveClientCountChanged(_)
         | ServerNotification::ExternalAgentConfigImportProgress(_)
         | ServerNotification::ExternalAgentConfigImportCompleted(_)
         | ServerNotification::DeprecationNotice(_)
diff --git a/codex-rs/tui/src/app/event_dispatch.rs b/codex-rs/tui/src/app/event_dispatch.rs
index d06c8c53fd..3b50c48453 100644
--- a/codex-rs/tui/src/app/event_dispatch.rs
+++ b/codex-rs/tui/src/app/event_dispatch.rs
@@ -2455,6 +2455,11 @@ impl App {
     ) -> AppRunControl {
         match mode {
             ExitMode::ShutdownFirst => {
+                if self.app_server_target.supports_detach()
+                    && let Err(err) = app_server.shutdown_when_no_active_clients().await
+                {
+                    tracing::warn!(error = %err, "failed to request app-server idle shutdown");
+                }
                 // Mark the thread we are explicitly shutting down for exit so
                 // its shutdown completion does not trigger agent failover.
                 self.pending_shutdown_exit_thread_id =
diff --git a/codex-rs/tui/src/app/session_lifecycle.rs b/codex-rs/tui/src/app/session_lifecycle.rs
index f115b2cd95..d7c8ffd98d 100644
--- a/codex-rs/tui/src/app/session_lifecycle.rs
+++ b/codex-rs/tui/src/app/session_lifecycle.rs
@@ -350,6 +350,7 @@ impl App {
     /// This helper copies every known nickname/role from `AgentNavigationState` into the
     /// replacement widget so that replayed collab items render agent names immediately.
     pub(super) fn replace_chat_widget(&mut self, mut chat_widget: ChatWidget) {
+        chat_widget.set_app_server_supports_detach(self.app_server_target.supports_detach());
         // Transfer the last-written terminal title to the replacement widget
         // so it knows what OSC title is currently displayed. Without this, the
         // new widget would redundantly clear and rewrite the same title, causing
diff --git a/codex-rs/tui/src/app_server_session.rs b/codex-rs/tui/src/app_server_session.rs
index 47554f4ea3..be4f7cae74 100644
--- a/codex-rs/tui/src/app_server_session.rs
+++ b/codex-rs/tui/src/app_server_session.rs
@@ -22,6 +22,7 @@ use codex_app_server_client::TypedRequestError;
 use codex_app_server_protocol::Account;
 use codex_app_server_protocol::AskForApproval;
 use codex_app_server_protocol::AuthMode;
+use codex_app_server_protocol::ClientNotification;
 use codex_app_server_protocol::ClientRequest;
 use codex_app_server_protocol::ConfigBatchWriteParams;
 use codex_app_server_protocol::ConfigRequirementsReadResponse;
@@ -485,6 +486,13 @@ impl AppServerSession {
         self.client.next_event().await
     }
 
+    pub(crate) async fn shutdown_when_no_active_clients(&mut self) -> Result<()> {
+        self.client
+            .notify(ClientNotification::ShutdownWhenNoActiveClients)
+            .await
+            .wrap_err("app-server idle shutdown request failed")
+    }
+
     #[cfg(test)]
     pub(crate) async fn start_thread(&mut self, config: &Config) -> Result<AppServerStartedThread> {
         self.start_thread_with_session_start_source(config, /*session_start_source*/ None)
diff --git a/codex-rs/tui/src/bottom_pane/chat_composer.rs b/codex-rs/tui/src/bottom_pane/chat_composer.rs
index d665be09f7..76c9d7345d 100644
--- a/codex-rs/tui/src/bottom_pane/chat_composer.rs
+++ b/codex-rs/tui/src/bottom_pane/chat_composer.rs
@@ -466,6 +466,7 @@ pub(crate) struct ChatComposer {
     personality_command_enabled: bool,
     windows_degraded_sandbox_active: bool,
     side_conversation_active: bool,
+    app_server_supports_detach: bool,
     history_search: Option<HistorySearchSession>,
     submit_keys: Vec<KeyBinding>,
     queue_keys: Vec<KeyBinding>,
@@ -540,6 +541,7 @@ impl ChatComposer {
             personality_command_enabled: self.personality_command_enabled,
             allow_elevate_sandbox: self.windows_degraded_sandbox_active,
             side_conversation_active: self.side_conversation_active,
+            app_server_supports_detach: self.app_server_supports_detach,
         }
     }
 
@@ -638,6 +640,7 @@ impl ChatComposer {
             personality_command_enabled: false,
             windows_degraded_sandbox_active: false,
             side_conversation_active: false,
+            app_server_supports_detach: false,
             history_search: None,
             submit_keys: vec![key_hint::plain(KeyCode::Enter)],
             queue_keys: vec![key_hint::plain(KeyCode::Tab)],
@@ -787,6 +790,10 @@ impl ChatComposer {
         self.side_conversation_active = active;
     }
 
+    pub fn set_app_server_supports_detach(&mut self, supports_detach: bool) {
+        self.app_server_supports_detach = supports_detach;
+    }
+
     /// Compatibility shim for tests that still toggle the removed steer mode flag.
     #[cfg(test)]
     pub fn set_steer_enabled(&mut self, _enabled: bool) {}
diff --git a/codex-rs/tui/src/bottom_pane/chat_composer/slash_input.rs b/codex-rs/tui/src/bottom_pane/chat_composer/slash_input.rs
index d8a6201946..a868ade615 100644
--- a/codex-rs/tui/src/bottom_pane/chat_composer/slash_input.rs
+++ b/codex-rs/tui/src/bottom_pane/chat_composer/slash_input.rs
@@ -180,6 +180,7 @@ impl<'a> SlashInput<'a> {
                 personality_command_enabled: self.command_flags.personality_command_enabled,
                 windows_degraded_sandbox_active: self.command_flags.allow_elevate_sandbox,
                 side_conversation_active: self.command_flags.side_conversation_active,
+                app_server_supports_detach: self.command_flags.app_server_supports_detach,
             },
             self.service_tier_commands.to_vec(),
         );
diff --git a/codex-rs/tui/src/bottom_pane/command_popup.rs b/codex-rs/tui/src/bottom_pane/command_popup.rs
index f613e5ee3f..268f69dcf2 100644
--- a/codex-rs/tui/src/bottom_pane/command_popup.rs
+++ b/codex-rs/tui/src/bottom_pane/command_popup.rs
@@ -37,6 +37,7 @@ pub(crate) struct CommandPopup {
     command_filter: String,
     commands: Vec<CommandItem>,
     state: ScrollState,
+    app_server_supports_detach: bool,
 }
 
 #[derive(Clone, Copy, Debug, Default)]
@@ -50,6 +51,7 @@ pub(crate) struct CommandPopupFlags {
     pub(crate) personality_command_enabled: bool,
     pub(crate) windows_degraded_sandbox_active: bool,
     pub(crate) side_conversation_active: bool,
+    pub(crate) app_server_supports_detach: bool,
 }
 
 impl From<CommandPopupFlags> for BuiltinCommandFlags {
@@ -64,6 +66,7 @@ impl From<CommandPopupFlags> for BuiltinCommandFlags {
             personality_command_enabled: value.personality_command_enabled,
             allow_elevate_sandbox: value.windows_degraded_sandbox_active,
             side_conversation_active: value.side_conversation_active,
+            app_server_supports_detach: value.app_server_supports_detach,
         }
     }
 }
@@ -87,6 +90,7 @@ impl CommandPopup {
             command_filter: String::new(),
             commands,
             state: ScrollState::new(),
+            app_server_supports_detach: flags.app_server_supports_detach,
         }
     }
 
@@ -205,7 +209,9 @@ impl CommandPopup {
             .into_iter()
             .map(|(item, indices)| {
                 let name = format!("/{}", item.command());
-                let description = item.description().to_string();
+                let description = item
+                    .description(self.app_server_supports_detach)
+                    .to_string();
                 GenericDisplayRow {
                     name,
                     name_prefix_spans: Vec::new(),
@@ -253,9 +259,9 @@ impl CommandItem {
         }
     }
 
-    fn description(&self) -> &str {
+    fn description(&self, app_server_supports_detach: bool) -> &str {
         match self {
-            Self::Builtin(cmd) => cmd.description(),
+            Self::Builtin(cmd) => cmd.description_for_app_server(app_server_supports_detach),
             Self::ServiceTier(command) => &command.description,
         }
     }
@@ -421,7 +427,7 @@ mod tests {
             .into_iter()
             .map(|item| {
                 let command = item.command();
-                let description = item.description();
+                let description = item.description(popup.app_server_supports_detach);
                 format!("/{command} - {description}")
             })
             .collect::<Vec<_>>()
@@ -493,6 +499,35 @@ mod tests {
         assert!(items.contains(&CommandItem::Builtin(SlashCommand::Quit)));
     }
 
+    #[test]
+    fn detached_app_server_exit_and_quit_descriptions_request_idle_shutdown() {
+        let mut popup = CommandPopup::new(
+            CommandPopupFlags {
+                app_server_supports_detach: true,
+                ..CommandPopupFlags::default()
+            },
+            Vec::new(),
+        );
+
+        popup.on_composer_text_change("/exit".to_string());
+        assert_eq!(
+            popup
+                .selected_item()
+                .as_ref()
+                .map(|item| item.description(popup.app_server_supports_detach)),
+            Some("exit Codex; app-server stops when no active clients remain")
+        );
+
+        popup.on_composer_text_change("/quit".to_string());
+        assert_eq!(
+            popup
+                .selected_item()
+                .as_ref()
+                .map(|item| item.description(popup.app_server_supports_detach)),
+            Some("exit Codex; app-server stops when no active clients remain")
+        );
+    }
+
     #[test]
     fn btw_hidden_in_empty_filter_but_shown_for_prefix() {
         let mut popup = CommandPopup::new(CommandPopupFlags::default(), Vec::new());
@@ -537,6 +572,7 @@ mod tests {
                 personality_command_enabled: true,
                 windows_degraded_sandbox_active: false,
                 side_conversation_active: false,
+                app_server_supports_detach: false,
             },
             Vec::new(),
         );
@@ -564,6 +600,7 @@ mod tests {
                 personality_command_enabled: false,
                 windows_degraded_sandbox_active: false,
                 side_conversation_active: false,
+                app_server_supports_detach: false,
             },
             Vec::new(),
         );
@@ -596,6 +633,7 @@ mod tests {
                 personality_command_enabled: true,
                 windows_degraded_sandbox_active: false,
                 side_conversation_active: false,
+                app_server_supports_detach: false,
             },
             Vec::new(),
         );
diff --git a/codex-rs/tui/src/bottom_pane/mod.rs b/codex-rs/tui/src/bottom_pane/mod.rs
index 1edbc55014..80c02e34c9 100644
--- a/codex-rs/tui/src/bottom_pane/mod.rs
+++ b/codex-rs/tui/src/bottom_pane/mod.rs
@@ -444,6 +444,12 @@ impl BottomPane {
         self.request_redraw();
     }
 
+    pub(crate) fn set_app_server_supports_detach(&mut self, supports_detach: bool) {
+        self.composer
+            .set_app_server_supports_detach(supports_detach);
+        self.request_redraw();
+    }
+
     pub(crate) fn set_placeholder_text(&mut self, placeholder: String) {
         self.composer.set_placeholder_text(placeholder);
         self.request_redraw();
diff --git a/codex-rs/tui/src/bottom_pane/slash_commands.rs b/codex-rs/tui/src/bottom_pane/slash_commands.rs
index 6ca1ba93d1..175f3ed81e 100644
--- a/codex-rs/tui/src/bottom_pane/slash_commands.rs
+++ b/codex-rs/tui/src/bottom_pane/slash_commands.rs
@@ -64,6 +64,7 @@ pub(crate) struct BuiltinCommandFlags {
     pub(crate) personality_command_enabled: bool,
     pub(crate) allow_elevate_sandbox: bool,
     pub(crate) side_conversation_active: bool,
+    pub(crate) app_server_supports_detach: bool,
 }
 
 /// Return the built-ins that should be visible/usable for the current input.
@@ -118,6 +119,7 @@ pub(crate) fn find_builtin_command(name: &str, flags: BuiltinCommandFlags) -> Op
     builtins_for_input(BuiltinCommandFlags {
         token_activity_command_enabled: true,
         side_conversation_active: false,
+        app_server_supports_detach: flags.app_server_supports_detach,
         ..flags
     })
     .into_iter()
@@ -173,6 +175,7 @@ mod tests {
             personality_command_enabled: true,
             allow_elevate_sandbox: true,
             side_conversation_active: false,
+            app_server_supports_detach: false,
         }
     }
 
diff --git a/codex-rs/tui/src/chatwidget.rs b/codex-rs/tui/src/chatwidget.rs
index 91a192923f..9930f4a152 100644
--- a/codex-rs/tui/src/chatwidget.rs
+++ b/codex-rs/tui/src/chatwidget.rs
@@ -548,6 +548,7 @@ pub(crate) struct ChatWidget {
     active_collaboration_mask: Option<CollaborationModeMask>,
     has_chatgpt_account: bool,
     has_codex_backend_auth: bool,
+    app_server_supports_detach: bool,
     model_catalog: Arc<ModelCatalog>,
     session_telemetry: SessionTelemetry,
     session_header: SessionHeader,
diff --git a/codex-rs/tui/src/chatwidget/constructor.rs b/codex-rs/tui/src/chatwidget/constructor.rs
index ef248e94cd..4fb7004f40 100644
--- a/codex-rs/tui/src/chatwidget/constructor.rs
+++ b/codex-rs/tui/src/chatwidget/constructor.rs
@@ -115,6 +115,7 @@ impl ChatWidget {
             active_collaboration_mask,
             has_chatgpt_account,
             has_codex_backend_auth,
+            app_server_supports_detach: false,
             model_catalog,
             session_telemetry,
             session_header: SessionHeader::new(header_model),
diff --git a/codex-rs/tui/src/chatwidget/protocol.rs b/codex-rs/tui/src/chatwidget/protocol.rs
index 9586f5fd7d..5737f07a4c 100644
--- a/codex-rs/tui/src/chatwidget/protocol.rs
+++ b/codex-rs/tui/src/chatwidget/protocol.rs
@@ -194,6 +194,7 @@ impl ChatWidget {
             ServerNotification::ServerRequestResolved(_)
             | ServerNotification::AccountUpdated(_)
             | ServerNotification::AccountRateLimitsUpdated(_)
+            | ServerNotification::ActiveClientCountChanged(_)
             | ServerNotification::ThreadStarted(_)
             | ServerNotification::ThreadStatusChanged(_)
             | ServerNotification::ThreadArchived(_)
diff --git a/codex-rs/tui/src/chatwidget/settings.rs b/codex-rs/tui/src/chatwidget/settings.rs
index 73dbdb33c8..341bd3b493 100644
--- a/codex-rs/tui/src/chatwidget/settings.rs
+++ b/codex-rs/tui/src/chatwidget/settings.rs
@@ -207,6 +207,12 @@ impl ChatWidget {
         self.has_codex_backend_auth
     }
 
+    pub(crate) fn set_app_server_supports_detach(&mut self, supports_detach: bool) {
+        self.app_server_supports_detach = supports_detach;
+        self.bottom_pane
+            .set_app_server_supports_detach(supports_detach);
+    }
+
     pub(crate) fn update_account_state(
         &mut self,
         status_account_display: Option<StatusAccountDisplay>,
diff --git a/codex-rs/tui/src/chatwidget/slash_dispatch.rs b/codex-rs/tui/src/chatwidget/slash_dispatch.rs
index 8153891eb5..d41769a816 100644
--- a/codex-rs/tui/src/chatwidget/slash_dispatch.rs
+++ b/codex-rs/tui/src/chatwidget/slash_dispatch.rs
@@ -1034,6 +1034,7 @@ impl ChatWidget {
             personality_command_enabled: self.config.features.enabled(Feature::Personality),
             allow_elevate_sandbox,
             side_conversation_active: self.active_side_conversation,
+            app_server_supports_detach: self.app_server_supports_detach,
         }
     }
 
diff --git a/codex-rs/tui/src/slash_command.rs b/codex-rs/tui/src/slash_command.rs
index eddeb2b630..50291065c0 100644
--- a/codex-rs/tui/src/slash_command.rs
+++ b/codex-rs/tui/src/slash_command.rs
@@ -82,6 +82,10 @@ pub enum SlashCommand {
 impl SlashCommand {
     /// User-visible description shown in the popup.
     pub fn description(self) -> &'static str {
+        self.description_for_app_server(/*supports_detach*/ false)
+    }
+
+    pub fn description_for_app_server(self, supports_detach: bool) -> &'static str {
         match self {
             SlashCommand::Feedback => "send logs to maintainers",
             SlashCommand::New => "start a new chat during a conversation",
@@ -96,7 +100,14 @@ impl SlashCommand {
             SlashCommand::Clear => "clear the terminal and start a new chat",
             SlashCommand::Fork => "fork the current chat",
             SlashCommand::App => "continue this session in the Desktop app",
-            SlashCommand::Quit | SlashCommand::Exit => "exit Codex",
+            SlashCommand::Quit if supports_detach => {
+                "exit Codex; app-server stops when no active clients remain"
+            }
+            SlashCommand::Quit => "quit this TUI",
+            SlashCommand::Exit if supports_detach => {
+                "exit Codex; app-server stops when no active clients remain"
+            }
+            SlashCommand::Exit => "exit Codex",
             SlashCommand::Copy => "copy last response as markdown",
             SlashCommand::Raw => "toggle raw scrollback mode for copy-friendly terminal selection",
             SlashCommand::Diff => "show git diff (including untracked files)",
