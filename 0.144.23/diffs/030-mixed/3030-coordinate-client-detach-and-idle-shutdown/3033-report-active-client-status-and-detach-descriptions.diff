diff --git a/codex-rs/app-server-protocol/src/protocol/common.rs b/codex-rs/app-server-protocol/src/protocol/common.rs
index 40c53e9edb..139a37c42c 100644
--- a/codex-rs/app-server-protocol/src/protocol/common.rs
+++ b/codex-rs/app-server-protocol/src/protocol/common.rs
@@ -1697,6 +1697,7 @@ server_notification_definitions! {
     AccountRateLimitsUpdated => "account/rateLimits/updated" (v2::AccountRateLimitsUpdatedNotification),
     AppListUpdated => "app/list/updated" (v2::AppListUpdatedNotification),
     RemoteControlStatusChanged => "remoteControl/status/changed" (v2::RemoteControlStatusChangedNotification),
+    ActiveClientCountChanged => "appServer/activeClientCount/changed" (v2::ActiveClientCountChangedNotification),
     ExternalAgentConfigImportProgress => "externalAgentConfig/import/progress" (v2::ExternalAgentConfigImportProgressNotification),
     ExternalAgentConfigImportCompleted => "externalAgentConfig/import/completed" (v2::ExternalAgentConfigImportCompletedNotification),
     FsChanged => "fs/changed" (v2::FsChangedNotification),
@@ -1765,6 +1766,7 @@ pub struct ServerNotificationEnvelope {
 
 client_notification_definitions! {
     Initialized,
+    ShutdownWhenNoActiveClients,
 }
 
 #[cfg(test)]
diff --git a/codex-rs/app-server-protocol/src/protocol/v2/notification.rs b/codex-rs/app-server-protocol/src/protocol/v2/notification.rs
index 8289cf5683..5d9cd2ffef 100644
--- a/codex-rs/app-server-protocol/src/protocol/v2/notification.rs
+++ b/codex-rs/app-server-protocol/src/protocol/v2/notification.rs
@@ -25,6 +25,13 @@ pub struct WarningNotification {
     pub message: String,
 }
 
+#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, JsonSchema, TS)]
+#[serde(rename_all = "camelCase")]
+#[ts(export_to = "v2/")]
+pub struct ActiveClientCountChangedNotification {
+    pub active_client_count: usize,
+}
+
 #[derive(Serialize, Deserialize, Debug, Clone, PartialEq, JsonSchema, TS)]
 #[serde(rename_all = "camelCase")]
 #[ts(export_to = "v2/")]
diff --git a/codex-rs/app-server/src/in_process.rs b/codex-rs/app-server/src/in_process.rs
index 4c4f3906b3..fe4f03e624 100644
--- a/codex-rs/app-server/src/in_process.rs
+++ b/codex-rs/app-server/src/in_process.rs
@@ -436,6 +436,9 @@ async fn start_uninitialized(args: InProcessStartArgs) -> IoResult<InProcessClie
             let processor = Arc::new(MessageProcessor::new(MessageProcessorArgs {
                 outgoing: Arc::clone(&processor_outgoing),
                 active_client_count: Arc::new(std::sync::atomic::AtomicUsize::new(0)),
+                shutdown_when_no_active_clients_requested: Arc::new(
+                    std::sync::atomic::AtomicBool::new(false),
+                ),
                 analytics_events_client,
                 arg0_paths: args.arg0_paths,
                 config: args.config,
diff --git a/codex-rs/app-server/src/lib.rs b/codex-rs/app-server/src/lib.rs
index 8bfcb56d39..bbc64fe7e8 100644
--- a/codex-rs/app-server/src/lib.rs
+++ b/codex-rs/app-server/src/lib.rs
@@ -48,6 +48,7 @@ use crate::transport::start_remote_control;
 use crate::transport::start_stdio_connection;
 use crate::transport::start_websocket_acceptor;
 use codex_analytics::AppServerRpcTransport;
+use codex_app_server_protocol::ActiveClientCountChangedNotification;
 use codex_app_server_protocol::ConfigWarningNotification;
 use codex_app_server_protocol::JSONRPCMessage;
 use codex_app_server_protocol::ServerNotification;
@@ -293,6 +294,24 @@ fn should_shutdown_when_no_connections(
         && running_turn_count == 0
 }
 
+fn should_shutdown_when_no_active_clients(
+    requested: bool,
+    had_connection: bool,
+    active_client_count: usize,
+    running_turn_count: usize,
+) -> bool {
+    requested && had_connection && active_client_count == 0 && running_turn_count == 0
+}
+
+fn active_client_count_from_connections(
+    connections: &HashMap<ConnectionId, ConnectionState>,
+) -> usize {
+    connections
+        .values()
+        .filter(|connection_state| connection_state.session.counts_as_active_client())
+        .count()
+}
+
 fn config_warning_from_error(
     summary: impl Into<String>,
     err: &std::io::Error,
@@ -868,9 +887,13 @@ pub async fn run_main_with_transport_options(
         let initialize_notification_sender = outgoing_message_sender.clone();
         let outbound_control_tx = outbound_control_tx;
         let active_client_count = Arc::new(AtomicUsize::new(0));
+        let shutdown_when_no_active_clients_requested = Arc::new(AtomicBool::new(false));
         let processor = Arc::new(MessageProcessor::new(MessageProcessorArgs {
             outgoing: outgoing_message_sender,
             active_client_count,
+            shutdown_when_no_active_clients_requested: Arc::clone(
+                &shutdown_when_no_active_clients_requested,
+            ),
             analytics_events_client,
             arg0_paths,
             config: Arc::new(config),
@@ -913,6 +936,15 @@ pub async fn run_main_with_transport_options(
                 ) {
                     break "last_connection_closed";
                 }
+                if should_shutdown_when_no_active_clients(
+                    shutdown_when_no_active_clients_requested
+                        .load(std::sync::atomic::Ordering::Acquire),
+                    had_connection,
+                    active_client_count_from_connections(&connections),
+                    running_turn_count,
+                ) {
+                    break "no_active_clients";
+                }
                 if matches!(
                     shutdown_state.update(running_turn_count, connections.len()),
                     ShutdownAction::Finish
@@ -1005,6 +1037,8 @@ pub async fn run_main_with_transport_options(
                                 let Some(connection_state) = connections.remove(&connection_id) else {
                                     continue;
                                 };
+                                let was_active_client =
+                                    connection_state.session.counts_as_active_client();
                                 connection_state.session.rpc_gate.close().await;
                                 let outbound_closed = outbound_control_tx
                                     .send(OutboundControlEvent::Closed { connection_id })
@@ -1019,72 +1053,105 @@ pub async fn run_main_with_transport_options(
                                 if !outbound_closed {
                                     break "outbound_router_closed";
                                 }
+                                if was_active_client {
+                                    initialize_notification_sender
+                                        .send_server_notification(
+                                            ServerNotification::ActiveClientCountChanged(
+                                                ActiveClientCountChangedNotification {
+                                                    active_client_count:
+                                                        active_client_count_from_connections(
+                                                            &connections,
+                                                        ),
+                                                },
+                                            ),
+                                        )
+                                        .await;
+                                }
                             }
                             TransportEvent::IncomingMessage { connection_id, message } => {
                                 match message {
                                     JSONRPCMessage::Request(request) => {
-                                        let Some(connection_state) = connections.get_mut(&connection_id) else {
-                                            warn!("dropping request from unknown connection: {connection_id:?}");
-                                            continue;
-                                        };
-                                        let was_initialized =
-                                            connection_state.session.initialized();
-                                        processor
-                                            .process_request(
-                                                connection_id,
-                                                request,
-                                                &transport,
-                                                Arc::clone(&connection_state.session),
-                                            )
-                                            .await;
-                                        let opted_out_notification_methods_snapshot = connection_state
-                                            .session
-                                            .opted_out_notification_methods();
-                                        let experimental_api_enabled =
-                                            connection_state.session.experimental_api_enabled();
-                                        let is_initialized = connection_state.session.initialized();
-                                        if let Ok(mut opted_out_notification_methods) = connection_state
-                                            .outbound_opted_out_notification_methods
-                                            .write()
-                                        {
-                                            *opted_out_notification_methods =
-                                                opted_out_notification_methods_snapshot;
-                                        } else {
-                                            warn!(
-                                                "failed to update outbound opted-out notifications"
-                                            );
-                                        }
-                                        connection_state
-                                            .outbound_experimental_api_enabled
-                                            .store(
-                                                experimental_api_enabled,
-                                                std::sync::atomic::Ordering::Release,
-                                            );
-                                        if !was_initialized && is_initialized {
+                                        let became_initialized = {
+                                            let Some(connection_state) = connections.get_mut(&connection_id) else {
+                                                warn!("dropping request from unknown connection: {connection_id:?}");
+                                                continue;
+                                            };
+                                            let was_initialized =
+                                                connection_state.session.initialized();
                                             processor
-                                                .send_initialize_notifications_to_connection(
+                                                .process_request(
                                                     connection_id,
+                                                    request,
+                                                    &transport,
+                                                    Arc::clone(&connection_state.session),
                                                 )
                                                 .await;
+                                            let opted_out_notification_methods_snapshot = connection_state
+                                                .session
+                                                .opted_out_notification_methods();
+                                            let experimental_api_enabled =
+                                                connection_state.session.experimental_api_enabled();
+                                            let is_initialized = connection_state.session.initialized();
+                                            if let Ok(mut opted_out_notification_methods) = connection_state
+                                                .outbound_opted_out_notification_methods
+                                                .write()
+                                            {
+                                                *opted_out_notification_methods =
+                                                    opted_out_notification_methods_snapshot;
+                                            } else {
+                                                warn!(
+                                                    "failed to update outbound opted-out notifications"
+                                                );
+                                            }
+                                            connection_state
+                                                .outbound_experimental_api_enabled
+                                                .store(
+                                                    experimental_api_enabled,
+                                                    std::sync::atomic::Ordering::Release,
+                                                );
+                                            if !was_initialized && is_initialized {
+                                                processor
+                                                    .send_initialize_notifications_to_connection(
+                                                        connection_id,
+                                                    )
+                                                    .await;
+                                                initialize_notification_sender
+                                                    .send_server_notification_to_connections(
+                                                        &[connection_id],
+                                                        ServerNotification::RemoteControlStatusChanged(
+                                                            remote_control_status.clone(),
+                                                        ),
+                                                    )
+                                                    .await;
+                                                processor
+                                                    .connection_initialized(
+                                                        connection_id,
+                                                        connection_state
+                                                            .session
+                                                            .request_attestation(),
+                                                    )
+                                                    .await;
+                                                connection_state
+                                                    .outbound_initialized
+                                                    .store(true, std::sync::atomic::Ordering::Release);
+                                                true
+                                            } else {
+                                                false
+                                            }
+                                        };
+                                        if became_initialized {
                                             initialize_notification_sender
-                                                .send_server_notification_to_connections(
-                                                    &[connection_id],
-                                                    ServerNotification::RemoteControlStatusChanged(
-                                                        remote_control_status.clone(),
+                                                .send_server_notification(
+                                                    ServerNotification::ActiveClientCountChanged(
+                                                        ActiveClientCountChangedNotification {
+                                                            active_client_count:
+                                                                active_client_count_from_connections(
+                                                                    &connections,
+                                                                ),
+                                                        },
                                                     ),
                                                 )
                                                 .await;
-                                            processor
-                                                .connection_initialized(
-                                                    connection_id,
-                                                    connection_state
-                                                        .session
-                                                        .request_attestation(),
-                                                )
-                                                .await;
-                                            connection_state
-                                                .outbound_initialized
-                                                .store(true, std::sync::atomic::Ordering::Release);
                                         }
                                     }
                                     JSONRPCMessage::Response(response) => {
@@ -1373,6 +1440,7 @@ mod tests {
     #[cfg(debug_assertions)]
     use super::loader_overrides_with_test_user_config_file;
     use super::should_drain_when_no_connections;
+    use super::should_shutdown_when_no_active_clients;
     use super::should_shutdown_when_no_connections;
     #[cfg(debug_assertions)]
     use codex_config::LoaderOverrides;
@@ -1406,6 +1474,22 @@ mod tests {
         ));
     }
 
+    #[test]
+    fn idle_shutdown_waits_for_no_active_clients_and_no_running_turns() {
+        assert!(!should_shutdown_when_no_active_clients(
+            /*requested*/ true, /*had_connection*/ true, /*active_client_count*/ 1,
+            /*running_turn_count*/ 0,
+        ));
+        assert!(!should_shutdown_when_no_active_clients(
+            /*requested*/ true, /*had_connection*/ true, /*active_client_count*/ 0,
+            /*running_turn_count*/ 1,
+        ));
+        assert!(should_shutdown_when_no_active_clients(
+            /*requested*/ true, /*had_connection*/ true, /*active_client_count*/ 0,
+            /*running_turn_count*/ 0,
+        ));
+    }
+
     #[test]
     fn no_connection_shutdown_runs_only_after_clients_and_turns_are_gone() {
         assert!(!should_shutdown_when_no_connections(
diff --git a/codex-rs/app-server/src/message_processor.rs b/codex-rs/app-server/src/message_processor.rs
index 619c65d904..e39d5818ba 100644
--- a/codex-rs/app-server/src/message_processor.rs
+++ b/codex-rs/app-server/src/message_processor.rs
@@ -135,6 +135,7 @@ pub(crate) struct MessageProcessor {
     windows_sandbox_processor: WindowsSandboxRequestProcessor,
     request_serialization_queues: RequestSerializationQueues,
     active_client_count: Arc<AtomicUsize>,
+    shutdown_when_no_active_clients_requested: Arc<AtomicBool>,
 }
 
 #[derive(Debug)]
@@ -223,6 +224,7 @@ impl ConnectionSessionState {
 pub(crate) struct MessageProcessorArgs {
     pub(crate) outgoing: Arc<OutgoingMessageSender>,
     pub(crate) active_client_count: Arc<AtomicUsize>,
+    pub(crate) shutdown_when_no_active_clients_requested: Arc<AtomicBool>,
     pub(crate) analytics_events_client: AnalyticsEventsClient,
     pub(crate) arg0_paths: Arg0DispatchPaths,
     pub(crate) config: Arc<Config>,
@@ -247,6 +249,7 @@ impl MessageProcessor {
         let MessageProcessorArgs {
             outgoing,
             active_client_count,
+            shutdown_when_no_active_clients_requested,
             analytics_events_client,
             arg0_paths,
             config,
@@ -527,6 +530,7 @@ impl MessageProcessor {
             windows_sandbox_processor,
             request_serialization_queues,
             active_client_count,
+            shutdown_when_no_active_clients_requested,
         }
     }
 
@@ -650,16 +654,25 @@ impl MessageProcessor {
     }
 
     pub(crate) async fn process_notification(&self, notification: JSONRPCNotification) {
-        // Currently, we do not expect to receive any notifications from the
-        // client, so we just log them.
-        tracing::info!("<- notification: {:?}", notification);
+        let notification = serde_json::to_value(notification)
+            .and_then(serde_json::from_value::<ClientNotification>);
+        match notification {
+            Ok(notification) => self.process_client_notification(notification).await,
+            Err(err) => tracing::info!("ignored untyped client notification: {err}"),
+        }
     }
 
     /// Handles typed notifications from in-process clients.
     pub(crate) async fn process_client_notification(&self, notification: ClientNotification) {
-        // Currently, we do not expect to receive any typed notifications from
-        // in-process clients, so we just log them.
-        tracing::info!("<- typed notification: {:?}", notification);
+        match notification {
+            ClientNotification::Initialized => {
+                tracing::info!("<- typed notification: initialized");
+            }
+            ClientNotification::ShutdownWhenNoActiveClients => {
+                self.shutdown_when_no_active_clients_requested
+                    .store(true, Ordering::Release);
+            }
+        }
     }
 
     async fn run_request_with_context<F>(
diff --git a/codex-rs/app-server/src/message_processor_tracing_tests.rs b/codex-rs/app-server/src/message_processor_tracing_tests.rs
index bd3a5d7bf6..8897911df6 100644
--- a/codex-rs/app-server/src/message_processor_tracing_tests.rs
+++ b/codex-rs/app-server/src/message_processor_tracing_tests.rs
@@ -253,6 +253,9 @@ async fn build_test_processor(
     let processor = Arc::new(MessageProcessor::new(MessageProcessorArgs {
         outgoing,
         active_client_count: Arc::new(std::sync::atomic::AtomicUsize::new(0)),
+        shutdown_when_no_active_clients_requested: Arc::new(std::sync::atomic::AtomicBool::new(
+            false,
+        )),
         analytics_events_client,
         arg0_paths: Arg0DispatchPaths::default(),
         config,
