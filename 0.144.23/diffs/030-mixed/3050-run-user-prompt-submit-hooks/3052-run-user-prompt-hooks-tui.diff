diff --git a/codex-rs/tui/src/app/event_dispatch.rs b/codex-rs/tui/src/app/event_dispatch.rs
index 4b9e8c4200..f113840c0c 100644
--- a/codex-rs/tui/src/app/event_dispatch.rs
+++ b/codex-rs/tui/src/app/event_dispatch.rs
@@ -2125,9 +2125,13 @@ impl App {
             AppEvent::SubmitUserMessageWithMode {
                 text,
                 collaboration_mode,
+                run_user_prompt_submit_hook,
             } => {
-                self.chat_widget
-                    .submit_user_message_with_mode(text, collaboration_mode);
+                self.chat_widget.submit_user_message_with_mode(
+                    text,
+                    collaboration_mode,
+                    run_user_prompt_submit_hook,
+                );
             }
             AppEvent::ManageSkillsClosed => {
                 self.chat_widget.handle_manage_skills_closed();
diff --git a/codex-rs/tui/src/app/tests.rs b/codex-rs/tui/src/app/tests.rs
index 06710a4e6c..19e6fd1c49 100644
--- a/codex-rs/tui/src/app/tests.rs
+++ b/codex-rs/tui/src/app/tests.rs
@@ -608,6 +608,7 @@ async fn replay_thread_snapshot_restores_draft_and_queued_input() {
             reasoning_effort: None,
             developer_instructions: None,
         },
+        /*run_user_prompt_submit_hook*/ false,
     );
     let expected_input_state = app
         .chat_widget
@@ -665,7 +666,11 @@ async fn replay_thread_snapshot_restores_the_matching_safety_buffer_prompt() {
         developer_instructions: None,
     };
     app.chat_widget
-        .submit_user_message_with_mode("buffered prompt A".to_string(), default_mode.clone());
+        .submit_user_message_with_mode(
+            "buffered prompt A".to_string(),
+            default_mode.clone(),
+            /*run_user_prompt_submit_hook*/ false,
+        );
     let expected_input_state = app
         .chat_widget
         .capture_thread_input_state()
@@ -687,7 +692,11 @@ async fn replay_thread_snapshot_restores_the_matching_safety_buffer_prompt() {
         ThreadId::new(),
         test_path_buf("/tmp/other-project"),
     ));
-    chat_widget.submit_user_message_with_mode("buffered prompt B".to_string(), default_mode);
+    chat_widget.submit_user_message_with_mode(
+        "buffered prompt B".to_string(),
+        default_mode,
+        /*run_user_prompt_submit_hook*/ false,
+    );
     app.chat_widget = chat_widget;
     app.replay_thread_snapshot(snapshot, /*resume_restored_queue*/ false);
 
diff --git a/codex-rs/tui/src/app/thread_routing.rs b/codex-rs/tui/src/app/thread_routing.rs
index 6737aee87f..d4cea8b044 100644
--- a/codex-rs/tui/src/app/thread_routing.rs
+++ b/codex-rs/tui/src/app/thread_routing.rs
@@ -617,6 +617,7 @@ impl App {
                 final_output_json_schema,
                 collaboration_mode,
                 personality,
+                run_user_prompt_submit_hook,
             } => {
                 let mut should_start_turn = true;
                 if let Some(turn_id) = self.active_turn_id_for_thread(thread_id).await {
@@ -710,6 +711,7 @@ impl App {
                             collaboration_mode.clone(),
                             *personality,
                             final_output_json_schema.clone(),
+                            *run_user_prompt_submit_hook,
                         )
                         .await?;
                     if self.active_thread_id == Some(thread_id)
diff --git a/codex-rs/tui/src/app_command.rs b/codex-rs/tui/src/app_command.rs
index 346e4503c5..c49c620d30 100644
--- a/codex-rs/tui/src/app_command.rs
+++ b/codex-rs/tui/src/app_command.rs
@@ -43,6 +43,7 @@ pub(crate) enum AppCommand {
         final_output_json_schema: Option<Value>,
         collaboration_mode: Option<CollaborationMode>,
         personality: Option<Personality>,
+        run_user_prompt_submit_hook: bool,
     },
     OverrideTurnContext {
         cwd: Option<PathBuf>,
@@ -130,6 +131,7 @@ impl AppCommand {
         final_output_json_schema: Option<Value>,
         collaboration_mode: Option<CollaborationMode>,
         personality: Option<Personality>,
+        run_user_prompt_submit_hook: bool,
     ) -> Self {
         Self::UserTurn {
             items,
@@ -144,6 +146,7 @@ impl AppCommand {
             final_output_json_schema,
             collaboration_mode,
             personality,
+            run_user_prompt_submit_hook,
         }
     }
 
diff --git a/codex-rs/tui/src/app_event.rs b/codex-rs/tui/src/app_event.rs
index 0a53cad7be..1ab1783f6d 100644
--- a/codex-rs/tui/src/app_event.rs
+++ b/codex-rs/tui/src/app_event.rs
@@ -1015,6 +1015,7 @@ pub(crate) enum AppEvent {
     SubmitUserMessageWithMode {
         text: String,
         collaboration_mode: CollaborationModeMask,
+        run_user_prompt_submit_hook: bool,
     },
 
     /// Open the approval popup.
diff --git a/codex-rs/tui/src/app_server_session.rs b/codex-rs/tui/src/app_server_session.rs
index efb2c65a38..0647ebc0d3 100644
--- a/codex-rs/tui/src/app_server_session.rs
+++ b/codex-rs/tui/src/app_server_session.rs
@@ -20,6 +20,8 @@ use codex_app_server_client::AppServerPath;
 use codex_app_server_client::AppServerRequestHandle;
 use codex_app_server_client::TypedRequestError;
 use codex_app_server_protocol::Account;
+use codex_app_server_protocol::AdditionalContextEntry;
+use codex_app_server_protocol::AdditionalContextKind;
 use codex_app_server_protocol::AskForApproval;
 use codex_app_server_protocol::AuthMode;
 use codex_app_server_protocol::ClientNotification;
@@ -143,6 +145,7 @@ const JSONRPC_INVALID_REQUEST: i64 = -32600;
 const JSONRPC_METHOD_NOT_FOUND: i64 = -32601;
 pub(crate) const EXTERNAL_AGENT_CONFIG_IMPORT_IN_PROGRESS_MESSAGE: &str = "A previous external agent import is still running. Wait for it to finish before importing again.";
 const THREAD_SETTINGS_UPDATE_METHOD: &str = "thread/settings/update";
+const TUI_USER_PROMPT_SUBMIT_HOOK_MARKER: &str = "codex.tui.run_user_prompt_submit_hook";
 
 #[derive(Clone, Copy, Debug, Eq, PartialEq)]
 pub(crate) enum ForkGoalContinuation {
@@ -861,10 +864,20 @@ impl AppServerSession {
         collaboration_mode: Option<codex_protocol::config_types::CollaborationMode>,
         personality: Option<codex_protocol::config_types::Personality>,
         output_schema: Option<serde_json::Value>,
+        run_user_prompt_submit_hook: bool,
     ) -> Result<TurnStartResponse> {
         let request_id = self.next_request_id();
         let (sandbox_policy, permissions) =
             turn_permissions_overrides(permissions_override, cwd.as_path());
+        let additional_context = run_user_prompt_submit_hook.then(|| {
+            HashMap::from([(
+                TUI_USER_PROMPT_SUBMIT_HOOK_MARKER.to_string(),
+                AdditionalContextEntry {
+                    value: "true".to_string(),
+                    kind: AdditionalContextKind::Application,
+                },
+            )])
+        });
         self.client
             .request_typed(ClientRequest::TurnStart {
                 request_id,
@@ -873,7 +886,7 @@ impl AppServerSession {
                     client_user_message_id: None,
                     input: items,
                     responsesapi_client_metadata: None,
-                    additional_context: None,
+                    additional_context,
                     environments: None,
                     cwd: Some(cwd),
                     runtime_workspace_roots: Some(workspace_roots.to_vec()),
diff --git a/codex-rs/tui/src/bottom_pane/chat_composer.rs b/codex-rs/tui/src/bottom_pane/chat_composer.rs
index 76c9d7345d..d573b1689d 100644
--- a/codex-rs/tui/src/bottom_pane/chat_composer.rs
+++ b/codex-rs/tui/src/bottom_pane/chat_composer.rs
@@ -309,6 +309,7 @@ pub enum InputResult {
     Submitted {
         text: String,
         text_elements: Vec<TextElement>,
+        run_user_prompt_submit_hook: bool,
     },
     Queued {
         text: String,
@@ -605,6 +606,7 @@ impl ChatComposer {
                 active_agent_label: None,
                 external_editor_key: Some(key_hint::ctrl(KeyCode::Char('g'))),
                 show_transcript_key: Some(key_hint::ctrl(KeyCode::Char('t'))),
+                submit_key: Some(key_hint::plain(KeyCode::Enter)),
                 insert_newline_key: footer_insert_newline_key(
                     &default_keymap.editor.insert_newline,
                     use_shift_enter_hint,
@@ -756,6 +758,7 @@ impl ChatComposer {
         self.draft.textarea.set_keymap_bindings(keymap);
         self.footer.external_editor_key = primary_binding(&keymap.app.open_external_editor);
         self.footer.show_transcript_key = primary_binding(&keymap.app.open_transcript);
+        self.footer.submit_key = primary_binding(&keymap.composer.submit);
         self.footer.insert_newline_key = footer_insert_newline_key(
             &keymap.editor.insert_newline,
             self.footer.use_shift_enter_hint,
@@ -2865,8 +2868,16 @@ impl ChatComposer {
 
     /// Common logic for handling message submission/queuing.
     /// Returns the appropriate InputResult based on `should_queue`.
-    fn handle_submission(&mut self, should_queue: bool) -> (InputResult, bool) {
-        let result = self.handle_submission_with_time(should_queue, Instant::now());
+    fn handle_submission(
+        &mut self,
+        should_queue: bool,
+        run_user_prompt_submit_hook: bool,
+    ) -> (InputResult, bool) {
+        let result = self.handle_submission_with_time(
+            should_queue,
+            run_user_prompt_submit_hook,
+            Instant::now(),
+        );
         self.reset_vim_mode_after_successful_dispatch(&result.0);
         result
     }
@@ -2887,6 +2898,7 @@ impl ChatComposer {
     fn handle_submission_with_time(
         &mut self,
         should_queue: bool,
+        run_user_prompt_submit_hook: bool,
         now: Instant,
     ) -> (InputResult, bool) {
         // Preserve newlines that are part of a paste before applying parent-owned submission
@@ -3008,6 +3020,7 @@ impl ChatComposer {
                     InputResult::Submitted {
                         text,
                         text_elements,
+                        run_user_prompt_submit_hook,
                     },
                     true,
                 )
@@ -3259,11 +3272,19 @@ impl ChatComposer {
         if self.queue_keys.is_pressed(key_event)
             && (self.is_task_running || self.queue_submissions || !self.is_bang_shell_command())
         {
-            return self.handle_submission(self.is_task_running || self.queue_submissions);
+            return self.handle_submission(
+                self.is_task_running || self.queue_submissions,
+                /*run_user_prompt_submit_hook*/ false,
+            );
         }
 
         if self.submit_keys.is_pressed(key_event) {
-            return self.handle_submission(self.queue_submissions);
+            return self.handle_submission(
+                self.queue_submissions,
+                key_event.code == KeyCode::Enter
+                    && !self.is_task_running
+                    && !self.queue_submissions,
+            );
         }
 
         if let KeyEvent {
@@ -3591,6 +3612,7 @@ impl ChatComposer {
             status_line_value: self.footer.status_line_value.clone(),
             status_line_enabled: self.footer.status_line_enabled,
             key_hints: FooterKeyHints {
+                submit: self.footer.submit_key,
                 toggle_shortcuts: self.footer.toggle_shortcuts_key,
                 queue: self.footer.queue_key,
                 insert_newline: self.footer.insert_newline_key,
@@ -4376,6 +4398,14 @@ impl ChatComposer {
                     | FooterMode::ShortcutOverlay
                     | FooterMode::EscHint => false,
                 };
+                let show_submit_hook_hint = match footer_props.mode {
+                    FooterMode::ComposerHasDraft => !footer_props.is_task_running,
+                    FooterMode::HistorySearch
+                    | FooterMode::QuitShortcutReminder
+                    | FooterMode::ComposerEmpty
+                    | FooterMode::ShortcutOverlay
+                    | FooterMode::EscHint => false,
+                };
                 let custom_height = self.custom_footer_height();
                 let footer_hint_height =
                     custom_height.unwrap_or_else(|| footer_height(&footer_props));
@@ -4445,6 +4475,7 @@ impl ChatComposer {
                             show_cycle_hint,
                             show_shortcuts_hint,
                             show_queue_hint,
+                            show_submit_hook_hint,
                         )
                     };
                     let right_line =
@@ -4495,6 +4526,7 @@ impl ChatComposer {
                                     show_cycle_hint,
                                     show_shortcuts_hint,
                                     show_queue_hint,
+                                    show_submit_hook_hint,
                                     footer_props.key_hints,
                                 ))
                             }
@@ -4534,6 +4566,7 @@ impl ChatComposer {
                                             show_cycle_hint,
                                             show_shortcuts_hint,
                                             show_queue_hint,
+                                            show_submit_hook_hint,
                                         );
                                     }
                                 } else {
@@ -4545,6 +4578,7 @@ impl ChatComposer {
                                         show_cycle_hint,
                                         show_shortcuts_hint,
                                         show_queue_hint,
+                                        show_submit_hook_hint,
                                     );
                                 }
                             }
@@ -4572,6 +4606,7 @@ impl ChatComposer {
                             show_cycle_hint,
                             show_shortcuts_hint,
                             show_queue_hint,
+                            show_submit_hook_hint,
                         );
                     }
                     if show_right && let Some(line) = &right_line {
@@ -4714,7 +4749,12 @@ mod tests {
             composer.set_text_content(command.to_string(), Vec::new(), Vec::new());
 
             assert_eq!(
-                composer.handle_submission(/*should_queue*/ false).0,
+                composer
+                    .handle_submission(
+                        /*should_queue*/ false,
+                        /*run_user_prompt_submit_hook*/ false,
+                    )
+                    .0,
                 InputResult::Command(expected)
             );
         }
@@ -5940,6 +5980,7 @@ mod tests {
             InputResult::Submitted {
                 text,
                 text_elements,
+                ..
             } => {
                 assert_eq!(text, "x".repeat(LARGE_PASTE_CHAR_THRESHOLD + 5));
                 assert!(text_elements.is_empty());
@@ -6039,7 +6080,9 @@ mod tests {
 
         composer.handle_key_event(KeyEvent::new(KeyCode::Char('i'), KeyModifiers::NONE));
         composer.set_text_content("queued".to_string(), Vec::new(), Vec::new());
-        let (result, _) = composer.handle_submission(/*should_queue*/ true);
+        let (result, _) = composer.handle_submission(
+            /*should_queue*/ true, /*run_user_prompt_submit_hook*/ false,
+        );
 
         assert_eq!(
             composer.vim_mode_indicator_span(),
@@ -8200,8 +8243,11 @@ mod tests {
             );
             now += step;
 
-            let (result, _) =
-                composer.handle_submission_with_time(/*should_queue*/ false, now);
+            let (result, _) = composer.handle_submission_with_time(
+                /*should_queue*/ false,
+                /*run_user_prompt_submit_hook*/ false,
+                now,
+            );
             assert!(
                 matches!(result, InputResult::None),
                 "Enter during a burst should insert newline, not submit"
@@ -8252,7 +8298,9 @@ mod tests {
         }
         assert!(composer.is_in_paste_burst());
 
-        let (result, _) = composer.handle_submission_with_time(/*should_queue*/ true, now);
+        let (result, _) = composer.handle_submission_with_time(
+            /*should_queue*/ true, /*run_user_prompt_submit_hook*/ false, now,
+        );
 
         assert_eq!(
             result,
@@ -9298,6 +9346,70 @@ mod tests {
         );
     }
 
+    #[test]
+    fn idle_enter_opts_into_user_prompt_submit_hook() {
+        let (tx, _rx) = unbounded_channel::<AppEvent>();
+        let sender = AppEventSender::new(tx);
+        let mut composer = ChatComposer::new(
+            /*has_input_focus*/ true,
+            sender,
+            /*enhanced_keys_supported*/ false,
+            "Ask Codex to do anything".to_string(),
+            /*disable_paste_burst*/ false,
+        );
+        composer
+            .draft
+            .textarea
+            .set_text_clearing_elements("run hook");
+
+        let (result, _needs_redraw) =
+            composer.handle_key_event(KeyEvent::new(KeyCode::Enter, KeyModifiers::NONE));
+
+        match result {
+            InputResult::Submitted {
+                text,
+                run_user_prompt_submit_hook,
+                ..
+            } => {
+                assert_eq!(text, "run hook");
+                assert!(run_user_prompt_submit_hook);
+            }
+            other => panic!("expected submitted input, got {other:?}"),
+        }
+    }
+
+    #[test]
+    fn idle_tab_submits_without_user_prompt_submit_hook() {
+        let (tx, _rx) = unbounded_channel::<AppEvent>();
+        let sender = AppEventSender::new(tx);
+        let mut composer = ChatComposer::new(
+            /*has_input_focus*/ true,
+            sender,
+            /*enhanced_keys_supported*/ false,
+            "Ask Codex to do anything".to_string(),
+            /*disable_paste_burst*/ false,
+        );
+        composer
+            .draft
+            .textarea
+            .set_text_clearing_elements("skip hook");
+
+        let (result, _needs_redraw) =
+            composer.handle_key_event(KeyEvent::new(KeyCode::Tab, KeyModifiers::NONE));
+
+        match result {
+            InputResult::Submitted {
+                text,
+                run_user_prompt_submit_hook,
+                ..
+            } => {
+                assert_eq!(text, "skip hook");
+                assert!(!run_user_prompt_submit_hook);
+            }
+            other => panic!("expected submitted input, got {other:?}"),
+        }
+    }
+
     #[test]
     fn tab_queues_slash_led_prompts_while_task_running_without_validation() {
         use crossterm::event::KeyCode;
@@ -9945,6 +10057,7 @@ mod tests {
             InputResult::Submitted {
                 text,
                 text_elements,
+                ..
             } => {
                 assert_eq!(text, format!("{large} src/main.rs"));
                 assert!(text_elements.is_empty());
@@ -10512,6 +10625,7 @@ mod tests {
             InputResult::Submitted {
                 text,
                 text_elements,
+                ..
             } => {
                 assert_eq!(text, "[Image #1] hi");
                 assert_eq!(text_elements.len(), 1);
@@ -11007,6 +11121,7 @@ mod tests {
             InputResult::Submitted {
                 text,
                 text_elements,
+                ..
             } => {
                 let expected = format!("{large_content} [Image #1]");
                 assert_eq!(text, expected);
@@ -11050,6 +11165,7 @@ mod tests {
             InputResult::Submitted {
                 text,
                 text_elements,
+                ..
             } => {
                 let trimmed = large_content.trim().to_string();
                 assert_eq!(text, format!("{trimmed} [Image #1]"));
@@ -11093,6 +11209,7 @@ mod tests {
             InputResult::Submitted {
                 text,
                 text_elements,
+                ..
             } => {
                 assert_eq!(text, "line1\nline2\n [Image #1]");
                 assert!(!text.contains('\r'));
@@ -11156,6 +11273,7 @@ mod tests {
             InputResult::Submitted {
                 text,
                 text_elements,
+                ..
             } => {
                 assert_eq!(text, format!("/unknown {large_content}"));
                 assert!(text_elements.is_empty());
@@ -11184,6 +11302,7 @@ mod tests {
             InputResult::Submitted {
                 text,
                 text_elements,
+                ..
             } => {
                 assert_eq!(text, "[Image #1]");
                 assert_eq!(text_elements.len(), 1);
diff --git a/codex-rs/tui/src/bottom_pane/chat_composer/footer_state.rs b/codex-rs/tui/src/bottom_pane/chat_composer/footer_state.rs
index 113385ff72..eea6f22b93 100644
--- a/codex-rs/tui/src/bottom_pane/chat_composer/footer_state.rs
+++ b/codex-rs/tui/src/bottom_pane/chat_composer/footer_state.rs
@@ -32,6 +32,7 @@ pub(super) struct FooterState {
     pub(super) active_agent_label: Option<String>,
     pub(super) external_editor_key: Option<KeyBinding>,
     pub(super) show_transcript_key: Option<KeyBinding>,
+    pub(super) submit_key: Option<KeyBinding>,
     pub(super) insert_newline_key: Option<KeyBinding>,
     pub(super) queue_key: Option<KeyBinding>,
     pub(super) toggle_shortcuts_key: Option<KeyBinding>,
diff --git a/codex-rs/tui/src/bottom_pane/chat_composer/slash_input.rs b/codex-rs/tui/src/bottom_pane/chat_composer/slash_input.rs
index a868ade615..9adad0c99a 100644
--- a/codex-rs/tui/src/bottom_pane/chat_composer/slash_input.rs
+++ b/codex-rs/tui/src/bottom_pane/chat_composer/slash_input.rs
@@ -296,7 +296,9 @@ impl ChatComposer {
                     }
                 }
                 if self.is_task_running {
-                    return self.handle_submission(/*should_queue*/ true);
+                    return self.handle_submission(
+                        /*should_queue*/ true, /*run_user_prompt_submit_hook*/ false,
+                    );
                 }
                 (InputResult::None, true)
             }
diff --git a/codex-rs/tui/src/bottom_pane/footer.rs b/codex-rs/tui/src/bottom_pane/footer.rs
index b51c8901d9..3bc5c62b5b 100644
--- a/codex-rs/tui/src/bottom_pane/footer.rs
+++ b/codex-rs/tui/src/bottom_pane/footer.rs
@@ -106,6 +106,7 @@ const FOOTER_CONTEXT_GAP_COLS: u16 = 1;
 
 #[derive(Clone, Copy, Debug, Eq, PartialEq)]
 pub(crate) struct FooterKeyHints {
+    pub(crate) submit: Option<KeyBinding>,
     pub(crate) toggle_shortcuts: Option<KeyBinding>,
     pub(crate) queue: Option<KeyBinding>,
     pub(crate) insert_newline: Option<KeyBinding>,
@@ -121,6 +122,7 @@ impl FooterKeyHints {
     #[cfg(test)]
     pub(crate) fn default_bindings() -> Self {
         Self {
+            submit: Some(key_hint::plain(KeyCode::Enter)),
             toggle_shortcuts: Some(key_hint::plain(KeyCode::Char('?'))),
             queue: Some(key_hint::plain(KeyCode::Tab)),
             insert_newline: Some(key_hint::ctrl(KeyCode::Char('j'))),
@@ -234,12 +236,21 @@ pub(crate) fn footer_height(props: &FooterProps) -> u16 {
         | FooterMode::ShortcutOverlay
         | FooterMode::EscHint => false,
     };
+    let show_submit_hook_hint = match props.mode {
+        FooterMode::ComposerHasDraft => !props.is_task_running,
+        FooterMode::QuitShortcutReminder
+        | FooterMode::HistorySearch
+        | FooterMode::ComposerEmpty
+        | FooterMode::ShortcutOverlay
+        | FooterMode::EscHint => false,
+    };
     footer_from_props_lines(
         props,
         /*collaboration_mode_indicator*/ None,
         /*show_cycle_hint*/ false,
         show_shortcuts_hint,
         show_queue_hint,
+        show_submit_hook_hint,
     )
     .len() as u16
 }
@@ -269,6 +280,7 @@ pub(crate) fn render_footer_from_props(
     show_cycle_hint: bool,
     show_shortcuts_hint: bool,
     show_queue_hint: bool,
+    show_submit_hook_hint: bool,
 ) {
     Paragraph::new(prefix_lines(
         footer_from_props_lines(
@@ -277,6 +289,7 @@ pub(crate) fn render_footer_from_props(
             show_cycle_hint,
             show_shortcuts_hint,
             show_queue_hint,
+            show_submit_hook_hint,
         ),
         " ".repeat(FOOTER_INDENT_COLS).into(),
         " ".repeat(FOOTER_INDENT_COLS).into(),
@@ -292,6 +305,7 @@ pub(crate) fn left_fits(area: Rect, left_width: u16) -> bool {
 #[derive(Clone, Copy, Debug, PartialEq, Eq)]
 enum SummaryHintKind {
     None,
+    SubmitHook,
     Shortcuts,
     QueueMessage,
     QueueShort,
@@ -311,6 +325,19 @@ fn left_side_line(
     let mut line = Line::from("");
     match state.hint {
         SummaryHintKind::None => {}
+        SummaryHintKind::SubmitHook => {
+            if let Some(key) = key_hints.submit {
+                line.push_span(key);
+                line.push_span(" to submit with UserPromptSubmit".dim());
+            }
+            if let Some(key) = key_hints.queue {
+                if !line.spans.is_empty() {
+                    line.push_span(" ▪ ".dim());
+                }
+                line.push_span(key);
+                line.push_span(" to submit".dim());
+            }
+        }
         SummaryHintKind::Shortcuts => {
             if let Some(key) = key_hints.toggle_shortcuts {
                 line.push_span(key);
@@ -356,10 +383,13 @@ pub(crate) fn single_line_footer_layout(
     show_cycle_hint: bool,
     show_shortcuts_hint: bool,
     show_queue_hint: bool,
+    show_submit_hook_hint: bool,
     key_hints: FooterKeyHints,
 ) -> (SummaryLeft, bool) {
     let hint_kind = if show_queue_hint {
         SummaryHintKind::QueueMessage
+    } else if show_submit_hook_hint {
+        SummaryHintKind::SubmitHook
     } else if show_shortcuts_hint {
         SummaryHintKind::Shortcuts
     } else {
@@ -702,6 +732,7 @@ fn footer_from_props_lines(
     show_cycle_hint: bool,
     show_shortcuts_hint: bool,
     show_queue_hint: bool,
+    show_submit_hook_hint: bool,
 ) -> Vec<Line<'static>> {
     let key_hints = props.key_hints;
     // Passive footer context can come from the configurable status line, the
@@ -746,6 +777,8 @@ fn footer_from_props_lines(
             let state = LeftSideState {
                 hint: if show_queue_hint {
                     SummaryHintKind::QueueMessage
+                } else if show_submit_hook_hint {
+                    SummaryHintKind::SubmitHook
                 } else if show_shortcuts_hint {
                     SummaryHintKind::Shortcuts
                 } else {
@@ -792,12 +825,12 @@ pub(crate) fn passive_footer_status_line(props: &FooterProps) -> Option<Line<'st
 
 /// Whether the current footer mode allows contextual information to replace instructional hints.
 ///
-/// In practice this means the composer is idle, or it has a draft but is not currently running a
-/// task, so the footer can spend the row on ambient context instead of "what to do next" text.
+/// In practice this means the composer is empty, so the footer can spend the row on ambient
+/// context instead of "what to do next" text.
 pub(crate) fn shows_passive_footer_line(props: &FooterProps) -> bool {
     match props.mode {
         FooterMode::ComposerEmpty => true,
-        FooterMode::ComposerHasDraft => !props.is_task_running,
+        FooterMode::ComposerHasDraft => false,
         FooterMode::HistorySearch
         | FooterMode::QuitShortcutReminder
         | FooterMode::ShortcutOverlay
@@ -820,6 +853,7 @@ pub(crate) fn footer_line_width(
     show_cycle_hint: bool,
     show_shortcuts_hint: bool,
     show_queue_hint: bool,
+    show_submit_hook_hint: bool,
 ) -> u16 {
     footer_from_props_lines(
         props,
@@ -827,6 +861,7 @@ pub(crate) fn footer_line_width(
         show_cycle_hint,
         show_shortcuts_hint,
         show_queue_hint,
+        show_submit_hook_hint,
     )
     .last()
     .map(|line| line.width() as u16)
@@ -1325,6 +1360,14 @@ mod tests {
                     | FooterMode::ShortcutOverlay
                     | FooterMode::EscHint => false,
                 };
+                let show_submit_hook_hint = match props.mode {
+                    FooterMode::ComposerHasDraft => !props.is_task_running,
+                    FooterMode::HistorySearch
+                    | FooterMode::QuitShortcutReminder
+                    | FooterMode::ComposerEmpty
+                    | FooterMode::ShortcutOverlay
+                    | FooterMode::EscHint => false,
+                };
                 let status_line_active = uses_passive_footer_status_layout(props);
                 let passive_status_line = if status_line_active {
                     passive_footer_status_line(props)
@@ -1360,6 +1403,7 @@ mod tests {
                         show_cycle_hint,
                         show_shortcuts_hint,
                         show_queue_hint,
+                        show_submit_hook_hint,
                     )
                 };
                 let right_line = if status_line_active {
@@ -1419,6 +1463,7 @@ mod tests {
                             show_cycle_hint,
                             show_shortcuts_hint,
                             show_queue_hint,
+                            show_submit_hook_hint,
                             props.key_hints,
                         );
                         match summary_left {
@@ -1431,6 +1476,7 @@ mod tests {
                                     show_cycle_hint,
                                     show_shortcuts_hint,
                                     show_queue_hint,
+                                    show_submit_hook_hint,
                                 );
                             }
                             SummaryLeft::Custom(line) => {
@@ -1451,6 +1497,7 @@ mod tests {
                         show_cycle_hint,
                         show_shortcuts_hint,
                         show_queue_hint,
+                        show_submit_hook_hint,
                     );
                     let show_context = can_show_left_and_context
                         && !matches!(
@@ -1749,6 +1796,24 @@ mod tests {
             },
         );
 
+        snapshot_footer(
+            "footer_composer_has_draft_submit_hook_hint",
+            FooterProps {
+                mode: FooterMode::ComposerHasDraft,
+                esc_backtrack_hint: false,
+                use_shift_enter_hint: false,
+                is_task_running: false,
+                queue_submissions: false,
+                collaboration_modes_enabled: false,
+                is_wsl: false,
+                quit_shortcut_key: key_hint::ctrl(KeyCode::Char('c')),
+                status_line_value: None,
+                status_line_enabled: false,
+                key_hints: FooterKeyHints::default_bindings(),
+                active_agent_label: None,
+            },
+        );
+
         let props = FooterProps {
             mode: FooterMode::ComposerEmpty,
             esc_backtrack_hint: false,
diff --git a/codex-rs/tui/src/bottom_pane/mcp_server_elicitation.rs b/codex-rs/tui/src/bottom_pane/mcp_server_elicitation.rs
index 447ab8e6c3..019c29dcec 100644
--- a/codex-rs/tui/src/bottom_pane/mcp_server_elicitation.rs
+++ b/codex-rs/tui/src/bottom_pane/mcp_server_elicitation.rs
@@ -1267,6 +1267,7 @@ impl McpServerElicitationOverlay {
             InputResult::Submitted {
                 text,
                 text_elements,
+                ..
             }
             | InputResult::Queued {
                 text,
diff --git a/codex-rs/tui/src/bottom_pane/request_user_input/mod.rs b/codex-rs/tui/src/bottom_pane/request_user_input/mod.rs
index bf1fa5fa8a..467003993e 100644
--- a/codex-rs/tui/src/bottom_pane/request_user_input/mod.rs
+++ b/codex-rs/tui/src/bottom_pane/request_user_input/mod.rs
@@ -1100,6 +1100,7 @@ impl RequestUserInputOverlay {
             InputResult::Submitted {
                 text,
                 text_elements,
+                ..
             }
             | InputResult::Queued {
                 text,
diff --git a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__adjacent_plugin_completion_inserts_separator.snap b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__adjacent_plugin_completion_inserts_separator.snap
index c962377f5b..2885439b9b 100644
--- a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__adjacent_plugin_completion_inserts_separator.snap
+++ b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__adjacent_plugin_completion_inserts_separator.snap
@@ -10,4 +10,4 @@ expression: terminal.backend()
 "                                                                                                    "
 "                                                                                                    "
 "                                                                                                    "
-"                                                                                 100% context left  "
+"  tab to submit message ▪ enter to submit message with UserPromptSubmit hook     100% context left  "
diff --git a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__backspace_after_pastes.snap b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__backspace_after_pastes.snap
index 0b88e19a22..0b41a69525 100644
--- a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__backspace_after_pastes.snap
+++ b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__backspace_after_pastes.snap
@@ -11,4 +11,4 @@ expression: terminal.backend()
 "                                                                                                    "
 "                                                                                                    "
 "                                                                                                    "
-"                                                                                 100% context left  "
+"  enter to submit with UserPromptSubmit ▪ tab to submit                          100% context left  "
diff --git a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__footer_mode_hidden_while_typing.snap b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__footer_mode_hidden_while_typing.snap
index 5faacfa64f..b47ae78258 100644
--- a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__footer_mode_hidden_while_typing.snap
+++ b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__footer_mode_hidden_while_typing.snap
@@ -10,4 +10,4 @@ expression: terminal.backend()
 "                                                                                                    "
 "                                                                                                    "
 "                                                                                                    "
-"                                                                                 100% context left  "
+"  enter to submit with UserPromptSubmit ▪ tab to submit                          100% context left  "
diff --git a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__footer_mode_shell_command_absorbs_bang.snap b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__footer_mode_shell_command_absorbs_bang.snap
index da51ff78bb..acc74b27a2 100644
--- a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__footer_mode_shell_command_absorbs_bang.snap
+++ b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__footer_mode_shell_command_absorbs_bang.snap
@@ -10,4 +10,4 @@ expression: terminal.backend()
 "                                                                                                    "
 "                                                                                                    "
 "                                                                                                    "
-"  gpt-5.4 high fast ▪ ~/code/codex-1 ▪ Context 0% used                                  Shell mode  "
+"  enter to submit with UserPromptSubmit ▪ tab to submit                                 Shell mode  "
diff --git a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__image_placeholder_multiple.snap b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__image_placeholder_multiple.snap
index 49eca416c2..5526570e0a 100644
--- a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__image_placeholder_multiple.snap
+++ b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__image_placeholder_multiple.snap
@@ -10,4 +10,4 @@ expression: terminal.backend()
 "                                                                                                    "
 "                                                                                                    "
 "                                                                                                    "
-"                                                                                 100% context left  "
+"  enter to submit with UserPromptSubmit ▪ tab to submit                          100% context left  "
diff --git a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__image_placeholder_single.snap b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__image_placeholder_single.snap
index 3a5dd7a758..276e7805aa 100644
--- a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__image_placeholder_single.snap
+++ b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__image_placeholder_single.snap
@@ -10,4 +10,4 @@ expression: terminal.backend()
 "                                                                                                    "
 "                                                                                                    "
 "                                                                                                    "
-"                                                                                 100% context left  "
+"  enter to submit with UserPromptSubmit ▪ tab to submit                          100% context left  "
diff --git a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__large.snap b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__large.snap
index d2f77dbec3..32999483f0 100644
--- a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__large.snap
+++ b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__large.snap
@@ -11,4 +11,4 @@ expression: terminal.backend()
 "                                                                                                    "
 "                                                                                                    "
 "                                                                                                    "
-"                                                                                 100% context left  "
+"  enter to submit with UserPromptSubmit ▪ tab to submit                          100% context left  "
diff --git a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__multiple_pastes.snap b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__multiple_pastes.snap
index 0d16cec0b4..17544cafb2 100644
--- a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__multiple_pastes.snap
+++ b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__multiple_pastes.snap
@@ -11,4 +11,4 @@ expression: terminal.backend()
 "                                                                                                    "
 "                                                                                                    "
 "                                                                                                    "
-"                                                                                 100% context left  "
+"  enter to submit with UserPromptSubmit ▪ tab to submit                          100% context left  "
diff --git a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__remote_image_rows.snap b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__remote_image_rows.snap
index cb00c404ba..44e9de23a2 100644
--- a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__remote_image_rows.snap
+++ b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__remote_image_rows.snap
@@ -10,4 +10,4 @@ expression: terminal.backend()
 "                                                                                                    "
 "                                                                                                    "
 "                                                                                                    "
-"                                                                                 100% context left  "
+"  enter to submit with UserPromptSubmit ▪ tab to submit                          100% context left  "
diff --git a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__remote_image_rows_after_delete_first.snap b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__remote_image_rows_after_delete_first.snap
index fd3cf1f6cf..dda8779055 100644
--- a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__remote_image_rows_after_delete_first.snap
+++ b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__remote_image_rows_after_delete_first.snap
@@ -10,4 +10,4 @@ expression: terminal.backend()
 "                                                                                                    "
 "                                                                                                    "
 "                                                                                                    "
-"                                                                                 100% context left  "
+"  enter to submit with UserPromptSubmit ▪ tab to submit                          100% context left  "
diff --git a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__remote_image_rows_selected.snap b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__remote_image_rows_selected.snap
index cb00c404ba..44e9de23a2 100644
--- a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__remote_image_rows_selected.snap
+++ b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__remote_image_rows_selected.snap
@@ -10,4 +10,4 @@ expression: terminal.backend()
 "                                                                                                    "
 "                                                                                                    "
 "                                                                                                    "
-"                                                                                 100% context left  "
+"  enter to submit with UserPromptSubmit ▪ tab to submit                          100% context left  "
diff --git a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__skill_popup_closes_after_trailing_space.snap b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__skill_popup_closes_after_trailing_space.snap
index f2cc29cf1c..546efdf5c3 100644
--- a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__skill_popup_closes_after_trailing_space.snap
+++ b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__skill_popup_closes_after_trailing_space.snap
@@ -10,4 +10,4 @@ expression: terminal.backend()
 "                                                                                                    "
 "                                                                                                    "
 "                                                                                                    "
-"                                                                                 100% context left  "
+"  tab to submit message ▪ enter to submit message with UserPromptSubmit hook     100% context left  "
diff --git a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__skill_popup_closes_between_spaces_before_plain_text.snap b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__skill_popup_closes_between_spaces_before_plain_text.snap
index e27fb9b1f4..6cb27d70e6 100644
--- a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__skill_popup_closes_between_spaces_before_plain_text.snap
+++ b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__skill_popup_closes_between_spaces_before_plain_text.snap
@@ -10,4 +10,4 @@ expression: terminal.backend()
 "                                                                                                    "
 "                                                                                                    "
 "                                                                                                    "
-"                                                                                 100% context left  "
+"  tab to submit message ▪ enter to submit message with UserPromptSubmit hook     100% context left  "
diff --git a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__small.snap b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__small.snap
index 8d3f8216db..668687dc87 100644
--- a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__small.snap
+++ b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__chat_composer__tests__small.snap
@@ -11,4 +11,4 @@ expression: terminal.backend()
 "                                                                                                    "
 "                                                                                                    "
 "                                                                                                    "
-"                                                                                 100% context left  "
+"  enter to submit with UserPromptSubmit ▪ tab to submit                          100% context left  "
diff --git a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__footer__tests__footer_composer_has_draft_submit_hook_hint.snap b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__footer__tests__footer_composer_has_draft_submit_hook_hint.snap
new file mode 100644
index 0000000000..a9c6f48daa
--- /dev/null
+++ b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__footer__tests__footer_composer_has_draft_submit_hook_hint.snap
@@ -0,0 +1,6 @@
+---
+source: tui/src/bottom_pane/footer.rs
+assertion_line: 1560
+expression: terminal.backend()
+---
+"  enter to submit with UserPromptSubmit ▪ tab to submit      100% context left  "
diff --git a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__footer__tests__footer_status_line_overrides_draft_idle.snap b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__footer__tests__footer_status_line_overrides_draft_idle.snap
index bb0e2d33b9..a9c6f48daa 100644
--- a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__footer__tests__footer_status_line_overrides_draft_idle.snap
+++ b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__footer__tests__footer_status_line_overrides_draft_idle.snap
@@ -1,5 +1,6 @@
 ---
 source: tui/src/bottom_pane/footer.rs
+assertion_line: 1560
 expression: terminal.backend()
 ---
-"  Status line content                                                           "
+"  enter to submit with UserPromptSubmit ▪ tab to submit      100% context left  "
diff --git a/codex-rs/tui/src/chatwidget/input_flow.rs b/codex-rs/tui/src/chatwidget/input_flow.rs
index 77ad911346..ebaaf6b26e 100644
--- a/codex-rs/tui/src/chatwidget/input_flow.rs
+++ b/codex-rs/tui/src/chatwidget/input_flow.rs
@@ -21,6 +21,7 @@ impl ChatWidget {
             InputResult::Submitted {
                 text,
                 text_elements,
+                run_user_prompt_submit_hook,
             } => {
                 let user_message = self.user_message_from_submission(text, text_elements);
                 if user_message.text.is_empty()
@@ -44,7 +45,10 @@ impl ChatWidget {
                     self.reasoning_buffer.clear();
                     self.reasoning_summary_parts.clear();
                     self.set_status_header(String::from("Working"));
-                    self.submit_user_message(user_message);
+                    self.submit_user_message_with_user_prompt_submit_hook(
+                        user_message,
+                        run_user_prompt_submit_hook,
+                    );
                 } else {
                     self.queue_user_message(user_message);
                 }
@@ -199,6 +203,7 @@ impl ChatWidget {
         &mut self,
         text: String,
         mut collaboration_mode: CollaborationModeMask,
+        run_user_prompt_submit_hook: bool,
     ) {
         if self.blocks_direct_input {
             self.add_error_message(PARENT_OWNED_INPUT_MESSAGE.to_string());
@@ -229,7 +234,10 @@ impl ChatWidget {
         if should_queue {
             self.queue_user_message(user_message);
         } else {
-            self.submit_user_message(user_message);
+            self.submit_user_message_with_user_prompt_submit_hook(
+                user_message,
+                run_user_prompt_submit_hook,
+            );
         }
     }
 
diff --git a/codex-rs/tui/src/chatwidget/input_submission.rs b/codex-rs/tui/src/chatwidget/input_submission.rs
index ec55f34f81..10f9f52cf4 100644
--- a/codex-rs/tui/src/chatwidget/input_submission.rs
+++ b/codex-rs/tui/src/chatwidget/input_submission.rs
@@ -63,9 +63,21 @@ impl ChatWidget {
     }
 
     pub(super) fn submit_user_message(&mut self, user_message: UserMessage) {
-        let _accepted = self.submit_user_message_with_history_record(
+        self.submit_user_message_with_user_prompt_submit_hook(
+            user_message,
+            /*run_user_prompt_submit_hook*/ false,
+        );
+    }
+
+    pub(super) fn submit_user_message_with_user_prompt_submit_hook(
+        &mut self,
+        user_message: UserMessage,
+        run_user_prompt_submit_hook: bool,
+    ) {
+        let _accepted = self.submit_user_message_with_history_record_and_hook(
             user_message,
             UserMessageHistoryRecord::UserMessageText,
+            run_user_prompt_submit_hook,
         );
     }
 
@@ -73,11 +85,25 @@ impl ChatWidget {
         &mut self,
         user_message: UserMessage,
         history_record: UserMessageHistoryRecord,
+    ) -> bool {
+        self.submit_user_message_with_history_record_and_hook(
+            user_message,
+            history_record,
+            /*run_user_prompt_submit_hook*/ false,
+        )
+    }
+
+    fn submit_user_message_with_history_record_and_hook(
+        &mut self,
+        user_message: UserMessage,
+        history_record: UserMessageHistoryRecord,
+        run_user_prompt_submit_hook: bool,
     ) -> bool {
         self.submit_user_message_with_history_and_shell_escape_policy(
             user_message,
             history_record,
             ShellEscapePolicy::Allow,
+            run_user_prompt_submit_hook,
         )
         .0
     }
@@ -91,6 +117,7 @@ impl ChatWidget {
             user_message,
             UserMessageHistoryRecord::UserMessageText,
             shell_escape_policy,
+            /*run_user_prompt_submit_hook*/ false,
         )
         .1
     }
@@ -100,6 +127,7 @@ impl ChatWidget {
         user_message: UserMessage,
         history_record: UserMessageHistoryRecord,
         shell_escape_policy: ShellEscapePolicy,
+        run_user_prompt_submit_hook: bool,
     ) -> (bool, Option<AppCommand>) {
         if !self.is_session_configured() {
             tracing::warn!("cannot submit user message before session is configured; queueing");
@@ -349,6 +377,7 @@ impl ChatWidget {
             /*final_output_json_schema*/ None,
             collaboration_mode,
             personality,
+            run_user_prompt_submit_hook,
         );
         let submitted_message = UserMessage {
             text,
diff --git a/codex-rs/tui/src/chatwidget/plan_implementation.rs b/codex-rs/tui/src/chatwidget/plan_implementation.rs
index dd810dc493..af870f10a3 100644
--- a/codex-rs/tui/src/chatwidget/plan_implementation.rs
+++ b/codex-rs/tui/src/chatwidget/plan_implementation.rs
@@ -37,6 +37,7 @@ pub(super) fn selection_view_params(
                 tx.send(AppEvent::SubmitUserMessageWithMode {
                     text: user_text.clone(),
                     collaboration_mode: mask.clone(),
+                    run_user_prompt_submit_hook: true,
                 });
             })];
             (actions, None)
diff --git a/codex-rs/tui/src/chatwidget/tests/composer_submission.rs b/codex-rs/tui/src/chatwidget/tests/composer_submission.rs
index 1690c48a12..23ebc2cbe6 100644
--- a/codex-rs/tui/src/chatwidget/tests/composer_submission.rs
+++ b/codex-rs/tui/src/chatwidget/tests/composer_submission.rs
@@ -37,7 +37,11 @@ async fn parent_owned_thread_blocks_all_direct_input_entry_points() {
     let collaboration_mode_before = chat.active_collaboration_mask.clone();
     let plan_mode = collaboration_modes::mask_for_kind(chat.model_catalog.as_ref(), ModeKind::Plan)
         .expect("expected plan collaboration mode");
-    chat.submit_user_message_with_mode("Implement the plan.".to_string(), plan_mode);
+    chat.submit_user_message_with_mode(
+        "Implement the plan.".to_string(),
+        plan_mode,
+        /*run_user_prompt_submit_hook*/ false,
+    );
     assert_eq!(chat.active_collaboration_mask, collaboration_mode_before);
     assert_no_submit_op(&mut op_rx);
 
diff --git a/codex-rs/tui/src/chatwidget/tests/plan_mode.rs b/codex-rs/tui/src/chatwidget/tests/plan_mode.rs
index b37801ec37..be50bcc095 100644
--- a/codex-rs/tui/src/chatwidget/tests/plan_mode.rs
+++ b/codex-rs/tui/src/chatwidget/tests/plan_mode.rs
@@ -174,6 +174,7 @@ async fn plan_implementation_popup_yes_emits_submit_message_event() {
     let AppEvent::SubmitUserMessageWithMode {
         text,
         collaboration_mode,
+        run_user_prompt_submit_hook,
     } = event
     else {
         panic!("expected SubmitUserMessageWithMode, got {event:?}");
@@ -183,6 +184,7 @@ async fn plan_implementation_popup_yes_emits_submit_message_event() {
         plan_implementation::PLAN_IMPLEMENTATION_CODING_MESSAGE
     );
     assert_eq!(collaboration_mode.mode, Some(ModeKind::Default));
+    assert!(run_user_prompt_submit_hook);
 }
 
 #[tokio::test]
@@ -277,7 +279,11 @@ async fn submit_user_message_with_mode_sets_coding_collaboration_mode() {
 
     let default_mode = collaboration_modes::default_mode_mask(chat.model_catalog.as_ref())
         .expect("expected default collaboration mode");
-    chat.submit_user_message_with_mode("Implement the plan.".to_string(), default_mode);
+    chat.submit_user_message_with_mode(
+        "Implement the plan.".to_string(),
+        default_mode,
+        /*run_user_prompt_submit_hook*/ false,
+    );
 
     match next_submit_op(&mut op_rx) {
         Op::UserTurn {
@@ -752,7 +758,11 @@ async fn submit_user_message_with_mode_errors_when_mode_changes_during_running_t
 
     let default_mode = collaboration_modes::default_mask(chat.model_catalog.as_ref())
         .expect("expected default collaboration mode");
-    chat.submit_user_message_with_mode("Implement the plan.".to_string(), default_mode);
+    chat.submit_user_message_with_mode(
+        "Implement the plan.".to_string(),
+        default_mode,
+        /*run_user_prompt_submit_hook*/ true,
+    );
 
     assert_eq!(chat.active_collaboration_mode_kind(), ModeKind::Plan);
     assert!(chat.input_queue.queued_user_messages.is_empty());
@@ -800,7 +810,11 @@ async fn submit_user_message_with_mode_allows_same_mode_during_running_turn() {
     chat.set_collaboration_mask(plan_mask.clone());
     chat.on_task_started();
 
-    chat.submit_user_message_with_mode("Continue planning.".to_string(), plan_mask);
+    chat.submit_user_message_with_mode(
+        "Continue planning.".to_string(),
+        plan_mask,
+        /*run_user_prompt_submit_hook*/ false,
+    );
 
     assert_eq!(chat.active_collaboration_mode_kind(), ModeKind::Plan);
     assert!(chat.input_queue.queued_user_messages.is_empty());
@@ -834,7 +848,11 @@ async fn submit_user_message_with_mode_submits_when_plan_stream_is_not_active()
     let expected_mode = default_mode
         .mode
         .expect("expected default collaboration mode kind");
-    chat.submit_user_message_with_mode("Implement the plan.".to_string(), default_mode);
+    chat.submit_user_message_with_mode(
+        "Implement the plan.".to_string(),
+        default_mode,
+        /*run_user_prompt_submit_hook*/ true,
+    );
 
     assert_eq!(chat.active_collaboration_mode_kind(), expected_mode);
     assert!(chat.input_queue.queued_user_messages.is_empty());
@@ -842,8 +860,12 @@ async fn submit_user_message_with_mode_submits_when_plan_stream_is_not_active()
         Op::UserTurn {
             collaboration_mode: Some(CollaborationMode { mode, .. }),
             personality: None,
+            run_user_prompt_submit_hook,
             ..
-        } => assert_eq!(mode, expected_mode),
+        } => {
+            assert_eq!(mode, expected_mode);
+            assert!(run_user_prompt_submit_hook);
+        }
         other => {
             panic!("expected Op::UserTurn with default collab mode, got {other:?}")
         }
diff --git a/codex-rs/tui/src/chatwidget/tests/review_mode.rs b/codex-rs/tui/src/chatwidget/tests/review_mode.rs
index 71c80e31b1..a79e1e999a 100644
--- a/codex-rs/tui/src/chatwidget/tests/review_mode.rs
+++ b/codex-rs/tui/src/chatwidget/tests/review_mode.rs
@@ -807,13 +807,20 @@ async fn esc_interrupt_sends_all_pending_steers_immediately_and_keeps_existing_d
         .set_composer_text("first pending steer".to_string(), Vec::new(), Vec::new());
     chat.handle_key_event(KeyEvent::new(KeyCode::Enter, KeyModifiers::NONE));
     match next_submit_op(&mut op_rx) {
-        Op::UserTurn { items, .. } => assert_eq!(
+        Op::UserTurn {
             items,
-            vec![UserInput::Text {
-                text: "first pending steer".to_string(),
-                text_elements: Vec::new(),
-            }]
-        ),
+            run_user_prompt_submit_hook,
+            ..
+        } => {
+            assert!(!run_user_prompt_submit_hook);
+            assert_eq!(
+                items,
+                vec![UserInput::Text {
+                    text: "first pending steer".to_string(),
+                    text_elements: Vec::new(),
+                }]
+            );
+        }
         other => panic!("expected Op::UserTurn, got {other:?}"),
     }
 
@@ -822,13 +829,20 @@ async fn esc_interrupt_sends_all_pending_steers_immediately_and_keeps_existing_d
     chat.handle_key_event(KeyEvent::new(KeyCode::Enter, KeyModifiers::NONE));
 
     match next_submit_op(&mut op_rx) {
-        Op::UserTurn { items, .. } => assert_eq!(
+        Op::UserTurn {
             items,
-            vec![UserInput::Text {
-                text: "second pending steer".to_string(),
-                text_elements: Vec::new(),
-            }]
-        ),
+            run_user_prompt_submit_hook,
+            ..
+        } => {
+            assert!(!run_user_prompt_submit_hook);
+            assert_eq!(
+                items,
+                vec![UserInput::Text {
+                    text: "second pending steer".to_string(),
+                    text_elements: Vec::new(),
+                }]
+            );
+        }
         other => panic!("expected Op::UserTurn, got {other:?}"),
     }
 
@@ -845,13 +859,20 @@ async fn esc_interrupt_sends_all_pending_steers_immediately_and_keeps_existing_d
     chat.on_interrupted_turn(TurnAbortReason::Interrupted);
 
     match next_submit_op(&mut op_rx) {
-        Op::UserTurn { items, .. } => assert_eq!(
+        Op::UserTurn {
             items,
-            vec![UserInput::Text {
-                text: "first pending steer\nsecond pending steer".to_string(),
-                text_elements: Vec::new(),
-            }]
-        ),
+            run_user_prompt_submit_hook,
+            ..
+        } => {
+            assert!(!run_user_prompt_submit_hook);
+            assert_eq!(
+                items,
+                vec![UserInput::Text {
+                    text: "first pending steer\nsecond pending steer".to_string(),
+                    text_elements: Vec::new(),
+                }]
+            );
+        }
         other => panic!("expected merged pending steers to submit, got {other:?}"),
     }
 
