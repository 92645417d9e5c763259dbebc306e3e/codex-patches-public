diff --git a/codex-rs/app-server/tests/suite/v2/model_list.rs b/codex-rs/app-server/tests/suite/v2/model_list.rs
index 5e0983448b..8c5fb40baf 100644
--- a/codex-rs/app-server/tests/suite/v2/model_list.rs
+++ b/codex-rs/app-server/tests/suite/v2/model_list.rs
@@ -95,6 +95,22 @@ fn expected_visible_models() -> Vec<Model> {
         .collect()
 }
 
+fn expected_chat_completions_models() -> Vec<Model> {
+    ["mimo-v2.5", "deepseek-v4"]
+        .into_iter()
+        .map(|slug| {
+            codex_models_manager::model_info::chat_completions_model_info(slug)
+                .unwrap_or_else(|| panic!("expected Chat Completions model metadata for {slug}"))
+        })
+        .map(ModelPreset::from)
+        .map(|preset| {
+            let mut model = model_from_preset(&preset);
+            model.supports_personality = preset.supports_personality;
+            model
+        })
+        .collect()
+}
+
 #[tokio::test]
 async fn list_models_returns_all_models_with_large_limit() -> Result<()> {
     let codex_home = TempDir::new()?;
@@ -122,7 +138,8 @@ async fn list_models_returns_all_models_with_large_limit() -> Result<()> {
         next_cursor,
     } = to_response::<ModelListResponse>(response)?;
 
-    let expected_models = expected_visible_models();
+    let mut expected_models = expected_visible_models();
+    expected_models.extend(expected_chat_completions_models());
 
     assert_eq!(items, expected_models);
     assert!(next_cursor.is_none());
@@ -372,6 +389,7 @@ openai_base_url = "{server_uri}/v1"
         },
     ];
 
+    expected_items.extend(expected_chat_completions_models());
     assert_eq!(items, expected_items);
     assert!(next_cursor.is_none());
     assert_eq!(
diff --git a/codex-rs/models-manager/src/manager_tests.rs b/codex-rs/models-manager/src/manager_tests.rs
index fbc77f4414..b2857211df 100644
--- a/codex-rs/models-manager/src/manager_tests.rs
+++ b/codex-rs/models-manager/src/manager_tests.rs
@@ -1162,12 +1162,20 @@ fn build_available_models_picks_default_after_hiding_hidden_models() {
     let expected_mimo = ModelPreset::from(
         crate::model_info::chat_completions_model_info("mimo-v2.5").expect("mimo model"),
     );
+    let expected_deepseek = ModelPreset::from(
+        crate::model_info::chat_completions_model_info("deepseek-v4").expect("deepseek model"),
+    );
 
     let available = manager.build_available_models(vec![hidden_model, visible_model]);
 
     assert_eq!(
         available,
-        vec![expected_hidden, expected_visible, expected_mimo]
+        vec![
+            expected_hidden,
+            expected_visible,
+            expected_mimo,
+            expected_deepseek
+        ]
     );
 }
 
@@ -1194,7 +1202,7 @@ async fn static_manager_reads_latest_auth_mode() {
             .iter()
             .map(|model| model.model.as_str())
             .collect::<Vec<_>>(),
-        vec!["chatgpt-only", "api-model", "mimo-v2.5"]
+        vec!["chatgpt-only", "api-model", "mimo-v2.5", "deepseek-v4"]
     );
 
     auth_manager.set_external_auth(Arc::new(TestExternalApiKeyAuth));
@@ -1205,7 +1213,7 @@ async fn static_manager_reads_latest_auth_mode() {
             .iter()
             .map(|model| model.model.as_str())
             .collect::<Vec<_>>(),
-        vec!["api-model", "mimo-v2.5"]
+        vec!["api-model", "mimo-v2.5", "deepseek-v4"]
     );
 }
 
diff --git a/codex-rs/models-manager/src/model_info.rs b/codex-rs/models-manager/src/model_info.rs
index 1c8f615c40..626672c4a4 100644
--- a/codex-rs/models-manager/src/model_info.rs
+++ b/codex-rs/models-manager/src/model_info.rs
@@ -25,7 +25,10 @@ const LOCAL_PRAGMATIC_TEMPLATE: &str = "You are a deeply pragmatic, effective so
 const PERSONALITY_PLACEHOLDER: &str = "{{ personality }}";
 const CHAT_COMPLETIONS_CONTEXT_WINDOW: i64 = 384_000;
 const MIMO_V2_5: &str = "mimo-v2.5";
+const DEEPSEEK_V4: &str = "deepseek-v4";
 const OBSOLETE_MIMO_V2_5_PRO: &str = "mimo-v2.5-pro";
+const OBSOLETE_DEEPSEEK_V4_PRO: &str = "deepseek-v4-pro";
+const OBSOLETE_DEEPSEEK_V4_FLASH: &str = "deepseek-v4-flash";
 
 pub fn with_config_overrides(mut model: ModelInfo, config: &ModelsManagerConfig) -> ModelInfo {
     if let Some(supports_reasoning_summaries) = config.model_supports_reasoning_summaries
@@ -90,17 +93,28 @@ pub fn chat_completions_model_info(slug: &str) -> Option<ModelInfo> {
         .find(|model| model.slug == slug)
 }
 
-fn chat_completions_models() -> [ModelInfo; 1] {
-    [chat_completions_model(
-        MIMO_V2_5,
-        "MiMo-V2.5",
-        "MiMo OpenAI-compatible Chat Completions model.",
-        96,
-    )]
+fn chat_completions_models() -> [ModelInfo; 2] {
+    [
+        chat_completions_model(
+            MIMO_V2_5,
+            "MiMo-V2.5",
+            "MiMo OpenAI-compatible Chat Completions model.",
+            96,
+        ),
+        chat_completions_model(
+            DEEPSEEK_V4,
+            "DeepSeek-V4",
+            "DeepSeek OpenAI-compatible Chat Completions model.",
+            97,
+        ),
+    ]
 }
 
 fn is_obsolete_chat_completions_model(slug: &str) -> bool {
-    matches!(slug, OBSOLETE_MIMO_V2_5_PRO)
+    matches!(
+        slug,
+        OBSOLETE_MIMO_V2_5_PRO | OBSOLETE_DEEPSEEK_V4_PRO | OBSOLETE_DEEPSEEK_V4_FLASH
+    )
 }
 
 fn chat_completions_model(
@@ -220,16 +234,18 @@ pub fn model_info_from_slug(slug: &str) -> ModelInfo {
 
 fn local_personality_messages_for_slug(slug: &str) -> Option<ModelMessages> {
     match slug {
-        "gpt-5.2-codex" | "exp-codex-personality" | MIMO_V2_5 => Some(ModelMessages {
-            instructions_template: Some(format!(
-                "{DEFAULT_PERSONALITY_HEADER}\n\n{PERSONALITY_PLACEHOLDER}\n\n{BASE_INSTRUCTIONS}"
-            )),
-            instructions_variables: Some(ModelInstructionsVariables {
-                personality_default: Some(String::new()),
-                personality_friendly: Some(LOCAL_FRIENDLY_TEMPLATE.to_string()),
-                personality_pragmatic: Some(LOCAL_PRAGMATIC_TEMPLATE.to_string()),
-            }),
-        }),
+        "gpt-5.2-codex" | "exp-codex-personality" | MIMO_V2_5 | DEEPSEEK_V4 => {
+            Some(ModelMessages {
+                instructions_template: Some(format!(
+                    "{DEFAULT_PERSONALITY_HEADER}\n\n{PERSONALITY_PLACEHOLDER}\n\n{BASE_INSTRUCTIONS}"
+                )),
+                instructions_variables: Some(ModelInstructionsVariables {
+                    personality_default: Some(String::new()),
+                    personality_friendly: Some(LOCAL_FRIENDLY_TEMPLATE.to_string()),
+                    personality_pragmatic: Some(LOCAL_PRAGMATIC_TEMPLATE.to_string()),
+                }),
+            })
+        }
         _ => None,
     }
 }
diff --git a/codex-rs/models-manager/src/model_info_tests.rs b/codex-rs/models-manager/src/model_info_tests.rs
index e87ee7c08d..3b1ee35a40 100644
--- a/codex-rs/models-manager/src/model_info_tests.rs
+++ b/codex-rs/models-manager/src/model_info_tests.rs
@@ -75,27 +75,29 @@ fn model_context_window_uses_model_value_without_override() {
 
 #[test]
 fn chat_completions_models_use_shared_backend_limits() {
-    let model =
-        chat_completions_model_info("mimo-v2.5").expect("Chat Completions model should exist");
-
-    assert_eq!(model.context_window, Some(384_000));
-    assert_eq!(model.max_context_window, Some(384_000));
-    assert_eq!(
-        model.auto_review_model_override.as_deref(),
-        Some("mimo-v2.5")
-    );
-    assert_eq!(model.input_modalities, vec![InputModality::Text]);
-    assert!(!model.supports_search_tool);
-    assert!(!model.supports_image_detail_original);
-    assert!(!model.supports_parallel_tool_calls);
+    for slug in ["mimo-v2.5", "deepseek-v4"] {
+        let model = chat_completions_model_info(slug).expect("Chat Completions model should exist");
+
+        assert_eq!(model.context_window, Some(384_000));
+        assert_eq!(model.max_context_window, Some(384_000));
+        assert_eq!(model.auto_review_model_override.as_deref(), Some(slug));
+        assert_eq!(model.input_modalities, vec![InputModality::Text]);
+        assert!(!model.supports_search_tool);
+        assert!(!model.supports_image_detail_original);
+        assert!(!model.supports_parallel_tool_calls);
+    }
 }
 
 #[test]
 fn chat_completions_models_replace_obsolete_api_model_entries() {
     let mut obsolete = model_info_from_slug("mimo-v2.5-pro");
     obsolete.slug = "mimo-v2.5-pro".to_string();
-    let models = with_chat_completions_models(vec![obsolete]);
+    let mut deepseek_obsolete = model_info_from_slug("deepseek-v4-pro");
+    deepseek_obsolete.slug = "deepseek-v4-pro".to_string();
+    let models = with_chat_completions_models(vec![obsolete, deepseek_obsolete]);
 
     assert!(models.iter().any(|model| model.slug == "mimo-v2.5"));
+    assert!(models.iter().any(|model| model.slug == "deepseek-v4"));
     assert!(!models.iter().any(|model| model.slug == "mimo-v2.5-pro"));
+    assert!(!models.iter().any(|model| model.slug == "deepseek-v4-pro"));
 }
