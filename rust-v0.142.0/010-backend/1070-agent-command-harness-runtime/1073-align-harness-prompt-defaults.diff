diff --git a/codex-rs/app-server/src/request_processors/account_processor.rs b/codex-rs/app-server/src/request_processors/account_processor.rs
index 4d1bf237cd..ab5b58e6d5 100644
--- a/codex-rs/app-server/src/request_processors/account_processor.rs
+++ b/codex-rs/app-server/src/request_processors/account_processor.rs
@@ -966,11 +966,9 @@ impl AccountRequestProcessor {
             .rate_limits
             .iter()
             .cloned()
-            .map(|snapshot| {
-                let limit_id = snapshot
-                    .limit_id
-                    .clone()
-                    .unwrap_or_else(|| "codex".to_string());
+            .enumerate()
+            .map(|(index, snapshot)| {
+                let limit_id = account_rate_limit_key(&snapshot, index);
                 (limit_id, snapshot)
             })
             .collect();
@@ -1289,6 +1287,29 @@ fn workspace_messages_feature_disabled(err: &BackendRequestError) -> bool {
     err.status().is_some_and(|status| status.as_u16() == 404)
 }
 
+fn account_rate_limit_key(snapshot: &CoreRateLimitSnapshot, index: usize) -> String {
+    if let Some(limit_id) = snapshot
+        .limit_id
+        .as_deref()
+        .map(str::trim)
+        .filter(|limit_id| !limit_id.is_empty())
+    {
+        return limit_id.to_string();
+    }
+
+    if let Some(limit_name) = snapshot
+        .limit_name
+        .as_deref()
+        .map(str::trim)
+        .filter(|limit_name| !limit_name.is_empty())
+        .filter(|limit_name| !limit_name.eq_ignore_ascii_case("codex"))
+    {
+        return limit_name.to_ascii_lowercase().replace(['-', ' '], "_");
+    }
+
+    format!("unknown_{index}")
+}
+
 #[cfg(test)]
 mod tests {
     use super::*;
@@ -1380,4 +1401,20 @@ mod tests {
             assert_eq!(workspace_messages_feature_disabled(&err), expected);
         }
     }
+
+    #[test]
+    fn account_rate_limit_key_does_not_collapse_missing_id_to_codex() {
+        let snapshot = CoreRateLimitSnapshot {
+            limit_id: None,
+            limit_name: Some("GPT-5.3-Codex-Spark".to_string()),
+            primary: None,
+            secondary: None,
+            credits: None,
+            individual_limit: None,
+            plan_type: None,
+            rate_limit_reached_type: None,
+        };
+
+        assert_eq!(account_rate_limit_key(&snapshot, 1), "gpt_5.3_codex_spark");
+    }
 }
diff --git a/codex-rs/app-server/src/request_processors/thread_processor_tests.rs b/codex-rs/app-server/src/request_processors/thread_processor_tests.rs
index f54ddf52ba..8d0b9ab950 100644
--- a/codex-rs/app-server/src/request_processors/thread_processor_tests.rs
+++ b/codex-rs/app-server/src/request_processors/thread_processor_tests.rs
@@ -1526,11 +1526,17 @@ mod thread_processor_behavior_tests {
         let before = RolloutItem::Compacted(codex_protocol::protocol::CompactedItem {
             message: "legacy".to_string(),
             replacement_history: None,
+            window_number: None,
+            first_window_id: None,
+            previous_window_id: None,
             window_id: None,
         });
         let compacted = RolloutItem::Compacted(codex_protocol::protocol::CompactedItem {
             message: String::new(),
             replacement_history: Some(Vec::new()),
+            window_number: None,
+            first_window_id: None,
+            previous_window_id: None,
             window_id: None,
         });
         let context_compacted =
diff --git a/codex-rs/code-mode-protocol/src/description.rs b/codex-rs/code-mode-protocol/src/description.rs
index 6ae31ff209..9df1ea5e74 100644
--- a/codex-rs/code-mode-protocol/src/description.rs
+++ b/codex-rs/code-mode-protocol/src/description.rs
@@ -16,8 +16,8 @@ const EXEC_DESCRIPTION_TEMPLATE: &str = r#"Run JavaScript code to orchestrate/co
 - Nested tools return either an object or a string, based on the description.
 - Runs raw JavaScript -- no Node, no file system, no network access, no console.
 - Accepts raw JavaScript source text, not JSON, quoted strings, or markdown code fences.
-- You may optionally start the tool input with a first-line pragma like `// @exec: {"yield_time_ms": 10000, "max_output_tokens": 1000}`.
-- `yield_time_ms` asks `exec` to yield early if the script is still running. Defaults to 10000 ms.
+- You may optionally start the tool input with a first-line pragma like `// @exec: {"yield_time_ms": 300000, "max_output_tokens": 1000}`.
+- `yield_time_ms` asks `exec` to yield early if the script is still running. Defaults to 300000 ms.
 - `max_output_tokens` sets the token budget for direct `exec` results. Defaults to 10000 tokens.
 - When the JS code is fully evaluated, the isolate's lifetime ends and unawaited promises are silently discarded.
 
@@ -35,7 +35,7 @@ const EXEC_DESCRIPTION_TEMPLATE: &str = r#"Run JavaScript code to orchestrate/co
 - `yield_control()`: yields the accumulated output to the model immediately while the script keeps running."#;
 const WAIT_DESCRIPTION_TEMPLATE: &str = r#"- Use `wait` only after `exec` returns `Script running with cell ID ...`.
 - `cell_id` identifies the running `exec` cell to resume.
-- `yield_time_ms` controls how long to wait for more output before yielding again. Defaults to 10000 ms.
+- `yield_time_ms` controls how long to wait for more output before yielding again. Defaults to 300000 ms.
 - `max_tokens` limits how much new output this wait call returns. Defaults to 10000 tokens.
 - `terminate: true` stops the running cell; false or omitted waits for output.
 - `wait` returns only the new output since the last yield, or the final completion or termination result for that cell.
@@ -163,7 +163,7 @@ pub struct ParsedExecSource {
 pub fn parse_exec_source(input: &str) -> Result<ParsedExecSource, String> {
     if input.trim().is_empty() {
         return Err(
-            "exec expects raw JavaScript source text (non-empty). Provide JS only, optionally with first-line `// @exec: {\"yield_time_ms\": 10000, \"max_output_tokens\": 1000}`.".to_string(),
+            "exec expects raw JavaScript source text (non-empty). Provide JS only, optionally with first-line `// @exec: {\"yield_time_ms\": 300000, \"max_output_tokens\": 1000}`.".to_string(),
         );
     }
 
diff --git a/codex-rs/codex-api/src/rate_limits.rs b/codex-rs/codex-api/src/rate_limits.rs
index 91f96fc3b8..00a376cff4 100644
--- a/codex-rs/codex-api/src/rate_limits.rs
+++ b/codex-rs/codex-api/src/rate_limits.rs
@@ -153,7 +153,7 @@ pub fn parse_rate_limit_event(payload: &str) -> Option<RateLimitSnapshot> {
         .or(event.limit_name)
         .map(normalize_limit_id);
     Some(RateLimitSnapshot {
-        limit_id: Some(limit_id.unwrap_or_else(|| "codex".to_string())),
+        limit_id,
         limit_name: None,
         primary,
         secondary,
@@ -375,4 +375,18 @@ mod tests {
         assert_eq!(updates[0].secondary, None);
         assert_eq!(updates[0].credits, None);
     }
+
+    #[test]
+    fn parse_rate_limit_event_preserves_missing_limit_id() {
+        let snapshot = parse_rate_limit_event(
+            r#"{"type":"codex.rate_limits","rate_limits":{"primary":{"used_percent":42}}}"#,
+        )
+        .expect("snapshot");
+
+        assert_eq!(snapshot.limit_id, None);
+        assert_eq!(
+            snapshot.primary.map(|window| window.used_percent),
+            Some(42.0)
+        );
+    }
 }
diff --git a/codex-rs/core/src/context/turn_aborted.rs b/codex-rs/core/src/context/turn_aborted.rs
index 4f44b4cc4e..8a0b207738 100644
--- a/codex-rs/core/src/context/turn_aborted.rs
+++ b/codex-rs/core/src/context/turn_aborted.rs
@@ -50,4 +50,14 @@ mod tests {
             assert!(guidance.contains("latest instruction"));
         }
     }
+
+    #[test]
+    fn interrupted_guidance_routes_later_steers_to_latest_instruction() {
+        let marker = TurnAborted::new(TurnAborted::INTERRUPTED_DEVELOPER_GUIDANCE);
+        let rendered_marker = marker.body();
+
+        assert!(rendered_marker.contains("This is context, not a user request"));
+        assert!(rendered_marker.contains("If a later user message is present"));
+        assert!(!rendered_marker.contains("ask the user what to do"));
+    }
 }
diff --git a/codex-rs/core/src/state/session.rs b/codex-rs/core/src/state/session.rs
index b549aaa322..1eda106d94 100644
--- a/codex-rs/core/src/state/session.rs
+++ b/codex-rs/core/src/state/session.rs
@@ -336,20 +336,20 @@ impl SessionState {
 
 // Sometimes new snapshots don't include credits or plan information.
 // Preserve those from the previous snapshot when missing. For `limit_id`, keep
-// non-codex limit names distinct when the server omits the bucket id.
+// non-codex limit names distinct when the server omits the bucket id, but do not
+// collapse an unknown bucket into `codex`.
 fn merge_rate_limit_fields(
     previous: Option<&RateLimitSnapshot>,
     mut snapshot: RateLimitSnapshot,
 ) -> RateLimitSnapshot {
     if snapshot.limit_id.is_none() {
-        snapshot.limit_id = Some(
-            snapshot
-                .limit_name
-                .as_deref()
-                .filter(|limit_name| !limit_name.eq_ignore_ascii_case("codex"))
-                .unwrap_or("codex")
-                .to_string(),
-        );
+        snapshot.limit_id = snapshot
+            .limit_name
+            .as_deref()
+            .map(str::trim)
+            .filter(|limit_name| !limit_name.is_empty())
+            .filter(|limit_name| !limit_name.eq_ignore_ascii_case("codex"))
+            .map(|limit_name| limit_name.to_ascii_lowercase().replace(['-', ' '], "_"));
     }
     if snapshot.credits.is_none() {
         snapshot.credits = previous.and_then(|prior| prior.credits.clone());
diff --git a/codex-rs/core/src/state/session_tests.rs b/codex-rs/core/src/state/session_tests.rs
index d11e3ed6df..d906f4a1f5 100644
--- a/codex-rs/core/src/state/session_tests.rs
+++ b/codex-rs/core/src/state/session_tests.rs
@@ -36,7 +36,7 @@ async fn clear_connector_selection_removes_entries() {
 }
 
 #[tokio::test]
-async fn set_rate_limits_defaults_limit_id_to_codex_when_missing() {
+async fn set_rate_limits_preserves_missing_limit_id_without_name() {
     let session_configuration = make_session_configuration_for_tests().await;
     let mut state = SessionState::new(session_configuration);
 
@@ -60,7 +60,7 @@ async fn set_rate_limits_defaults_limit_id_to_codex_when_missing() {
             .latest_rate_limits
             .as_ref()
             .and_then(|v| v.limit_id.clone()),
-        Some("codex".to_string())
+        None
     );
 }
 
@@ -89,7 +89,7 @@ async fn set_rate_limits_uses_non_codex_limit_name_when_id_is_missing() {
             .latest_rate_limits
             .as_ref()
             .and_then(|v| v.limit_id.clone()),
-        Some("GPT-5.3-Codex-Spark".to_string())
+        Some("gpt_5.3_codex_spark".to_string())
     );
 }
 
@@ -110,7 +110,7 @@ async fn replace_history_clears_auto_compact_window_prefill() {
 }
 
 #[tokio::test]
-async fn set_rate_limits_defaults_to_codex_when_limit_id_missing_after_other_bucket() {
+async fn set_rate_limits_does_not_reuse_previous_bucket_when_limit_id_missing() {
     let session_configuration = make_session_configuration_for_tests().await;
     let mut state = SessionState::new(session_configuration);
 
@@ -148,7 +148,7 @@ async fn set_rate_limits_defaults_to_codex_when_limit_id_missing_after_other_buc
             .latest_rate_limits
             .as_ref()
             .and_then(|v| v.limit_id.clone()),
-        Some("codex".to_string())
+        None
     );
 }
 
diff --git a/codex-rs/core/src/tools/code_mode/wait_spec.rs b/codex-rs/core/src/tools/code_mode/wait_spec.rs
index 72bb09084c..f0d2deea58 100644
--- a/codex-rs/core/src/tools/code_mode/wait_spec.rs
+++ b/codex-rs/core/src/tools/code_mode/wait_spec.rs
@@ -12,7 +12,7 @@ pub(crate) fn create_wait_tool() -> ToolSpec {
         (
             "yield_time_ms".to_string(),
             JsonSchema::number(Some(
-                "Wait before yielding more output. Defaults to 10000 ms.".to_string(),
+                "Wait before yielding more output. Defaults to 300000 ms.".to_string(),
             )),
         ),
         (
@@ -90,7 +90,7 @@ mod tests {
                         (
                             "yield_time_ms".to_string(),
                             JsonSchema::number(Some(
-                                "Wait before yielding more output. Defaults to 10000 ms."
+                                "Wait before yielding more output. Defaults to 300000 ms."
                                     .to_string(),
                             )),
                         ),
diff --git a/codex-rs/core/src/tools/handlers/multi_agents_spec.rs b/codex-rs/core/src/tools/handlers/multi_agents_spec.rs
index 89b2bde7e2..c32b0a2033 100644
--- a/codex-rs/core/src/tools/handlers/multi_agents_spec.rs
+++ b/codex-rs/core/src/tools/handlers/multi_agents_spec.rs
@@ -663,7 +663,7 @@ fn spawn_agent_tool_description(
     }
     let agent_role_usage_hint = available_models_description
         .map(|_| {
-            "Agent-role guidance below only helps choose which agent to use after spawning is already authorized; it never authorizes spawning by itself."
+            "Agent-role guidance below only helps choose which agent to use after you decide to spawn one; it never requires spawning by itself."
         })
         .unwrap_or_default();
     format!(
@@ -671,7 +671,6 @@ fn spawn_agent_tool_description(
         {tool_description}
 This spawn_agent tool provides you access to sub-agents that inherit your current model by default. Do not set the `model` field unless the user explicitly asks for a different model or there is a clear task-specific reason. You should follow the rules and guidelines below to use this tool.
 
-Do not spawn sub-agents unless the user explicitly asks for sub-agents, delegation, or parallel agent work.
 {agent_role_usage_hint}
 
 ### Designing delegated subtasks
diff --git a/codex-rs/core/src/tools/handlers/shell_spec.rs b/codex-rs/core/src/tools/handlers/shell_spec.rs
index 3fb563b8d9..846088685b 100644
--- a/codex-rs/core/src/tools/handlers/shell_spec.rs
+++ b/codex-rs/core/src/tools/handlers/shell_spec.rs
@@ -45,7 +45,7 @@ pub(crate) fn create_exec_command_tool_with_environment_id(
         (
             "yield_time_ms".to_string(),
             JsonSchema::number(Some(
-                "Wait before yielding output. Defaults to 10000 ms; effective range is 250-30000 ms.".to_string(),
+                "Wait before yielding output. Defaults to 300000 ms; effective range is 250-300000 ms.".to_string(),
             )),
         ),
         (
@@ -168,7 +168,7 @@ pub fn create_shell_command_tool(options: CommandToolOptions) -> ToolSpec {
         (
             "timeout_ms".to_string(),
             JsonSchema::number(Some(
-                "Maximum command runtime. Defaults to 10000 ms.".to_string(),
+                "Maximum command runtime. Defaults to 300000 ms.".to_string(),
             )),
         ),
     ]);
diff --git a/codex-rs/core/src/tools/handlers/shell_spec_tests.rs b/codex-rs/core/src/tools/handlers/shell_spec_tests.rs
index b7bbfa54a1..5d13e0d787 100644
--- a/codex-rs/core/src/tools/handlers/shell_spec_tests.rs
+++ b/codex-rs/core/src/tools/handlers/shell_spec_tests.rs
@@ -58,7 +58,7 @@ fn exec_command_tool_matches_expected_spec() {
         (
             "yield_time_ms".to_string(),
             JsonSchema::number(Some(
-                    "Wait before yielding output. Defaults to 10000 ms; effective range is 250-30000 ms.".to_string(),
+                    "Wait before yielding output. Defaults to 300000 ms; effective range is 250-300000 ms.".to_string(),
                 )),
         ),
         (
@@ -241,7 +241,7 @@ Examples of valid command strings:
         (
             "timeout_ms".to_string(),
             JsonSchema::number(Some(
-                "Maximum command runtime. Defaults to 10000 ms.".to_string(),
+                "Maximum command runtime. Defaults to 300000 ms.".to_string(),
             )),
         ),
         (
diff --git a/codex-rs/core/templates/agents/orchestrator.md b/codex-rs/core/templates/agents/orchestrator.md
index c4d4424f7d..07290c1e19 100644
--- a/codex-rs/core/templates/agents/orchestrator.md
+++ b/codex-rs/core/templates/agents/orchestrator.md
@@ -40,3 +40,9 @@ When the user asks for a review, you default to a code-review mindset. Your resp
   - If the user asks a question, answer it first, then continue coordinating sub-agents.
 - When you ask sub-agent to do the work for you, your only role becomes to coordinate them. Do not perform the actual work while they are working.
 - When you have plan with multiple step, process them in parallel by spawning one agent per step when this is possible.
+
+## Command Output
+- Protect the TUI from accidental output floods.
+- Prefer command-native quiet flags for large outputs.
+- For read-only inspection, cap output before it reaches the tool with patterns like `sed -n '1,200p' file`, `cmd | head -n 200`, or `cmd | head -c 12000`.
+- For side-effecting commands that may produce large logs, write to a log file and summarize the relevant lines instead of piping raw output through a truncation command.
diff --git a/codex-rs/core/tests/suite/search_tool.rs b/codex-rs/core/tests/suite/search_tool.rs
index 7228547725..abeb30572d 100644
--- a/codex-rs/core/tests/suite/search_tool.rs
+++ b/codex-rs/core/tests/suite/search_tool.rs
@@ -855,7 +855,7 @@ async fn tool_search_returns_deferred_v1_multi_agent_tools() -> Result<()> {
         .get("description")
         .and_then(Value::as_str)
         .expect("spawn_agent description should be present");
-    assert!(description.contains(
+    assert!(!description.contains(
         "Do not spawn sub-agents unless the user explicitly asks for sub-agents, delegation, or parallel agent work."
     ));
     assert!(description.contains("### Designing delegated subtasks"));
diff --git a/codex-rs/core/tests/suite/spawn_agent_description.rs b/codex-rs/core/tests/suite/spawn_agent_description.rs
index fb493c1b14..224028ad15 100644
--- a/codex-rs/core/tests/suite/spawn_agent_description.rs
+++ b/codex-rs/core/tests/suite/spawn_agent_description.rs
@@ -212,10 +212,10 @@ async fn spawn_agent_description_lists_visible_models_and_reasoning_efforts() ->
         "hidden picker model should be omitted from spawn_agent description: {description:?}"
     );
     assert!(
-        description.contains(
+        !description.contains(
             "Do not spawn sub-agents unless the user explicitly asks for sub-agents, delegation, or parallel agent work."
         ),
-        "expected explicit authorization rule in spawn_agent description: {description:?}"
+        "spawn_agent description should not add a generic explicit-request-only rule: {description:?}"
     );
     assert!(
         !description.contains("### When to delegate vs. do the subtask yourself"),
@@ -223,7 +223,7 @@ async fn spawn_agent_description_lists_visible_models_and_reasoning_efforts() ->
     );
     assert!(
         description.contains(
-            "Agent-role guidance below only helps choose which agent to use after spawning is already authorized; it never authorizes spawning by itself."
+            "Agent-role guidance below only helps choose which agent to use after you decide to spawn one; it never requires spawning by itself."
         ),
         "expected agent-role clarification in spawn_agent description: {description:?}"
     );
diff --git a/codex-rs/hooks/src/events/common.rs b/codex-rs/hooks/src/events/common.rs
index 85a81087e1..32c6320ebe 100644
--- a/codex-rs/hooks/src/events/common.rs
+++ b/codex-rs/hooks/src/events/common.rs
@@ -9,8 +9,6 @@ use crate::engine::ConfiguredHandler;
 use crate::engine::dispatcher;
 
 const MAX_ADDITIONAL_CONTEXTS_FOR_MODEL: usize = 8;
-const MAX_ADDITIONAL_CONTEXT_CHARS: usize = 10_000;
-const ADDITIONAL_CONTEXT_TRUNCATED_MARKER: &str = "\n\n[additional context truncated]";
 
 /// Identifies a thread-spawned subagent when a normal hook runs inside it.
 #[derive(Debug, Clone, PartialEq, Eq)]
@@ -43,7 +41,7 @@ pub(crate) fn append_additional_context(
 ) {
     entries.push(HookOutputEntry {
         kind: HookOutputEntryKind::Context,
-        text: bounded_additional_context(additional_context.clone()),
+        text: additional_context.clone(),
     });
     additional_contexts_for_model.push(additional_context);
 }
@@ -58,19 +56,6 @@ pub(crate) fn flatten_additional_contexts<'a>(
         .collect()
 }
 
-fn bounded_additional_context(additional_context: String) -> String {
-    if additional_context.chars().count() <= MAX_ADDITIONAL_CONTEXT_CHARS {
-        return additional_context;
-    }
-
-    let mut truncated = additional_context
-        .chars()
-        .take(MAX_ADDITIONAL_CONTEXT_CHARS)
-        .collect::<String>();
-    truncated.push_str(ADDITIONAL_CONTEXT_TRUNCATED_MARKER);
-    truncated
-}
-
 pub(crate) fn serialization_failure_hook_events(
     handlers: Vec<ConfiguredHandler>,
     turn_id: Option<String>,
@@ -187,8 +172,6 @@ mod tests {
     use codex_protocol::protocol::HookEventName;
     use pretty_assertions::assert_eq;
 
-    use super::ADDITIONAL_CONTEXT_TRUNCATED_MARKER;
-    use super::MAX_ADDITIONAL_CONTEXT_CHARS;
     use super::MAX_ADDITIONAL_CONTEXTS_FOR_MODEL;
     use super::append_additional_context;
     use super::flatten_additional_contexts;
@@ -312,25 +295,19 @@ mod tests {
     }
 
     #[test]
-    fn additional_contexts_for_model_are_bounded() {
+    fn additional_contexts_for_model_keep_full_text() {
         let mut entries = Vec::new();
         let mut additional_contexts_for_model = Vec::new();
+        let full_context = "x".repeat(20_000);
         append_additional_context(
             &mut entries,
             &mut additional_contexts_for_model,
-            "x".repeat(MAX_ADDITIONAL_CONTEXT_CHARS + 1),
+            full_context.clone(),
         );
 
         assert_eq!(entries.len(), 1);
-        assert!(
-            entries[0]
-                .text
-                .ends_with(ADDITIONAL_CONTEXT_TRUNCATED_MARKER)
-        );
-        assert_eq!(
-            additional_contexts_for_model[0],
-            "x".repeat(MAX_ADDITIONAL_CONTEXT_CHARS + 1)
-        );
+        assert_eq!(entries[0].text, full_context);
+        assert_eq!(additional_contexts_for_model[0], full_context);
 
         let chunks = [
             vec!["a".to_string(); MAX_ADDITIONAL_CONTEXTS_FOR_MODEL],
diff --git a/codex-rs/hooks/src/output_spill.rs b/codex-rs/hooks/src/output_spill.rs
index b1828c0825..616b03f9cd 100644
--- a/codex-rs/hooks/src/output_spill.rs
+++ b/codex-rs/hooks/src/output_spill.rs
@@ -1,63 +1,17 @@
 use codex_protocol::ThreadId;
 use codex_protocol::items::HookPromptFragment;
-use codex_utils_absolute_path::AbsolutePathBuf;
-use codex_utils_output_truncation::TruncationPolicy;
-use codex_utils_output_truncation::approx_token_count;
-use codex_utils_output_truncation::formatted_truncate_text;
-use tokio::fs;
-use tracing::warn;
-use uuid::Uuid;
-
-const HOOK_OUTPUTS_DIR: &str = "hook_outputs";
-const HOOK_OUTPUT_TOKEN_LIMIT: usize = 2_500;
 
 #[derive(Clone)]
-pub(crate) struct HookOutputSpiller {
-    output_dir: AbsolutePathBuf,
-}
+pub(crate) struct HookOutputSpiller;
 
 impl HookOutputSpiller {
     pub(crate) fn new() -> Self {
-        Self {
-            output_dir: AbsolutePathBuf::resolve_path_against_base(std::env::temp_dir(), "/")
-                .join(HOOK_OUTPUTS_DIR),
-        }
+        Self
     }
 
-    /// Keeps hook text within the model-visible hook-output budget.
-    ///
-    /// Oversized text is written in full under the OS temp directory at
-    /// `<temp_dir>/hook_outputs/<thread_id>/`
-    /// and replaced with the same head/tail preview style used for other truncated
-    /// output, plus a path back to the preserved full text.
-    pub(crate) async fn maybe_spill_text(&self, thread_id: ThreadId, text: String) -> String {
-        if approx_token_count(&text) <= HOOK_OUTPUT_TOKEN_LIMIT {
-            return text;
-        }
-
-        let path = hook_output_path(&self.output_dir, thread_id);
-        if let Some(parent) = path.parent()
-            && let Err(err) = fs::create_dir_all(parent.as_ref()).await
-        {
-            warn!(
-                "failed to create hook output directory {}: {err}",
-                parent.display()
-            );
-            return formatted_truncate_text(
-                &text,
-                TruncationPolicy::Tokens(HOOK_OUTPUT_TOKEN_LIMIT),
-            );
-        }
-
-        if let Err(err) = fs::write(path.as_ref(), &text).await {
-            warn!("failed to write hook output {}: {err}", path.display());
-            return formatted_truncate_text(
-                &text,
-                TruncationPolicy::Tokens(HOOK_OUTPUT_TOKEN_LIMIT),
-            );
-        }
-
-        spilled_hook_output_preview(&text, &path)
+    /// Preserve hook text intact for model-visible additional context.
+    pub(crate) async fn maybe_spill_text(&self, _thread_id: ThreadId, text: String) -> String {
+        text
     }
 
     pub(crate) async fn maybe_spill_texts(
@@ -88,24 +42,6 @@ impl HookOutputSpiller {
     }
 }
 
-fn hook_output_path(output_dir: &AbsolutePathBuf, thread_id: ThreadId) -> AbsolutePathBuf {
-    output_dir
-        .join(thread_id.to_string())
-        .join(format!("{}.txt", Uuid::new_v4()))
-}
-
-/// Builds the model-visible replacement for a spilled hook output.
-///
-/// The path footer is budgeted before truncation so adding the recovery path
-/// does not let the preview grow past the hook-output limit.
-fn spilled_hook_output_preview(text: &str, path: &AbsolutePathBuf) -> String {
-    let footer = format!("\n\nFull hook output saved to: {}", path.display());
-    let preview_policy = TruncationPolicy::Tokens(
-        HOOK_OUTPUT_TOKEN_LIMIT.saturating_sub(approx_token_count(&footer)),
-    );
-    format!("{}{footer}", formatted_truncate_text(text, preview_policy))
-}
-
 #[cfg(test)]
 #[path = "output_spill_tests.rs"]
 mod tests;
diff --git a/codex-rs/hooks/src/output_spill_tests.rs b/codex-rs/hooks/src/output_spill_tests.rs
index 6c5f9b5848..30699776e6 100644
--- a/codex-rs/hooks/src/output_spill_tests.rs
+++ b/codex-rs/hooks/src/output_spill_tests.rs
@@ -1,42 +1,33 @@
 use super::*;
-use anyhow::Context;
 use anyhow::Result;
 use tempfile::tempdir;
 
 #[tokio::test]
 async fn small_hook_output_remains_inline() -> Result<()> {
     let dir = tempdir()?;
-    let output_dir = AbsolutePathBuf::from_absolute_path(dir.path())?.join(HOOK_OUTPUTS_DIR);
     let thread_id = ThreadId::new();
-    let spiller = HookOutputSpiller {
-        output_dir: output_dir.clone(),
-    };
+    let spiller = HookOutputSpiller::new();
 
     let output = spiller
         .maybe_spill_text(thread_id, "short".to_string())
         .await;
 
     assert_eq!(output, "short");
-    assert!(!output_dir.exists());
+    assert!(dir.path().read_dir()?.next().is_none());
     Ok(())
 }
 
 #[tokio::test]
-async fn large_hook_output_spills_to_file() -> Result<()> {
+async fn large_hook_output_remains_inline() -> Result<()> {
     let dir = tempdir()?;
     let text = "hook output ".repeat(1_000);
-    let output_dir = AbsolutePathBuf::from_absolute_path(dir.path())?.join(HOOK_OUTPUTS_DIR);
-    let spiller = HookOutputSpiller { output_dir };
+    let spiller = HookOutputSpiller::new();
 
     let output = spiller
         .maybe_spill_text(ThreadId::new(), text.clone())
         .await;
 
-    assert!(output.contains("tokens truncated"));
-    let path = output
-        .lines()
-        .find_map(|line| line.strip_prefix("Full hook output saved to: "))
-        .context("spill path")?;
-    assert_eq!(fs::read_to_string(path).await?, text);
+    assert_eq!(output, text);
+    assert!(dir.path().read_dir()?.next().is_none());
     Ok(())
 }
diff --git a/codex-rs/protocol/src/config_types.rs b/codex-rs/protocol/src/config_types.rs
index 83bcbdc0ae..102385b0f2 100644
--- a/codex-rs/protocol/src/config_types.rs
+++ b/codex-rs/protocol/src/config_types.rs
@@ -307,8 +307,8 @@ pub enum Personality {
 #[ts(rename_all = "camelCase")]
 #[strum(serialize_all = "camelCase")]
 pub enum MultiAgentMode {
-    None,
     #[default]
+    None,
     ExplicitRequestOnly,
     Proactive,
 }
@@ -731,6 +731,11 @@ mod tests {
     use super::*;
     use pretty_assertions::assert_eq;
 
+    #[test]
+    fn multi_agent_mode_default_injects_no_mode_instructions() {
+        assert_eq!(MultiAgentMode::None, MultiAgentMode::default());
+    }
+
     #[test]
     fn apply_mask_can_clear_optional_fields() {
         let mode = CollaborationMode {
