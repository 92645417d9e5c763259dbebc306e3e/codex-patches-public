diff --git a/codex-rs/app-server-protocol/schema/json/ServerNotification.json b/codex-rs/app-server-protocol/schema/json/ServerNotification.json
index b4e185914b..2df6aa301c 100644
--- a/codex-rs/app-server-protocol/schema/json/ServerNotification.json
+++ b/codex-rs/app-server-protocol/schema/json/ServerNotification.json
@@ -2807,6 +2807,14 @@
         }
       ]
     },
+    "MultiAgentVersion": {
+      "enum": [
+        "disabled",
+        "v1",
+        "v2"
+      ],
+      "type": "string"
+    },
     "NetworkAccess": {
       "enum": [
         "restricted",
@@ -3838,6 +3846,17 @@
           "description": "Model provider used for this thread (for example, 'openai').",
           "type": "string"
         },
+        "multiAgentVersion": {
+          "anyOf": [
+            {
+              "$ref": "#/definitions/MultiAgentVersion"
+            },
+            {
+              "type": "null"
+            }
+          ],
+          "description": "Multi-agent runtime selected for this loaded thread. Omitted when the runtime is unavailable, such as for an unloaded stored thread."
+        },
         "name": {
           "description": "Optional user-facing thread title.",
           "type": [
diff --git a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json
index 693ccad478..e1640716ec 100644
--- a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json
+++ b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json
@@ -13711,6 +13711,14 @@
           }
         ]
       },
+      "MultiAgentVersion": {
+        "enum": [
+          "disabled",
+          "v1",
+          "v2"
+        ],
+        "type": "string"
+      },
       "NetworkAccess": {
         "enum": [
           "restricted",
@@ -18230,6 +18238,17 @@
             "description": "Model provider used for this thread (for example, 'openai').",
             "type": "string"
           },
+          "multiAgentVersion": {
+            "anyOf": [
+              {
+                "$ref": "#/definitions/v2/MultiAgentVersion"
+              },
+              {
+                "type": "null"
+              }
+            ],
+            "description": "Multi-agent runtime selected for this loaded thread. Omitted when the runtime is unavailable, such as for an unloaded stored thread."
+          },
           "name": {
             "description": "Optional user-facing thread title.",
             "type": [
diff --git a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json
index 58dad82b22..a0607ac944 100644
--- a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json
+++ b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json
@@ -9997,6 +9997,14 @@
         }
       ]
     },
+    "MultiAgentVersion": {
+      "enum": [
+        "disabled",
+        "v1",
+        "v2"
+      ],
+      "type": "string"
+    },
     "NetworkAccess": {
       "enum": [
         "restricted",
@@ -15978,6 +15986,17 @@
           "description": "Model provider used for this thread (for example, 'openai').",
           "type": "string"
         },
+        "multiAgentVersion": {
+          "anyOf": [
+            {
+              "$ref": "#/definitions/MultiAgentVersion"
+            },
+            {
+              "type": "null"
+            }
+          ],
+          "description": "Multi-agent runtime selected for this loaded thread. Omitted when the runtime is unavailable, such as for an unloaded stored thread."
+        },
         "name": {
           "description": "Optional user-facing thread title.",
           "type": [
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadForkResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadForkResponse.json
index e1d0d8d2e8..61876d911b 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadForkResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadForkResponse.json
@@ -719,6 +719,14 @@
         }
       ]
     },
+    "MultiAgentVersion": {
+      "enum": [
+        "disabled",
+        "v1",
+        "v2"
+      ],
+      "type": "string"
+    },
     "NetworkAccess": {
       "enum": [
         "restricted",
@@ -1119,6 +1127,17 @@
           "description": "Model provider used for this thread (for example, 'openai').",
           "type": "string"
         },
+        "multiAgentVersion": {
+          "anyOf": [
+            {
+              "$ref": "#/definitions/MultiAgentVersion"
+            },
+            {
+              "type": "null"
+            }
+          ],
+          "description": "Multi-agent runtime selected for this loaded thread. Omitted when the runtime is unavailable, such as for an unloaded stored thread."
+        },
         "name": {
           "description": "Optional user-facing thread title.",
           "type": [
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadListResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadListResponse.json
index 09fee6b708..f21987ad09 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadListResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadListResponse.json
@@ -616,6 +616,14 @@
         }
       ]
     },
+    "MultiAgentVersion": {
+      "enum": [
+        "disabled",
+        "v1",
+        "v2"
+      ],
+      "type": "string"
+    },
     "NonSteerableTurnKind": {
       "enum": [
         "review",
@@ -910,6 +918,17 @@
           "description": "Model provider used for this thread (for example, 'openai').",
           "type": "string"
         },
+        "multiAgentVersion": {
+          "anyOf": [
+            {
+              "$ref": "#/definitions/MultiAgentVersion"
+            },
+            {
+              "type": "null"
+            }
+          ],
+          "description": "Multi-agent runtime selected for this loaded thread. Omitted when the runtime is unavailable, such as for an unloaded stored thread."
+        },
         "name": {
           "description": "Optional user-facing thread title.",
           "type": [
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadMetadataUpdateResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadMetadataUpdateResponse.json
index 172273875e..37dc31ce9b 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadMetadataUpdateResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadMetadataUpdateResponse.json
@@ -616,6 +616,14 @@
         }
       ]
     },
+    "MultiAgentVersion": {
+      "enum": [
+        "disabled",
+        "v1",
+        "v2"
+      ],
+      "type": "string"
+    },
     "NonSteerableTurnKind": {
       "enum": [
         "review",
@@ -910,6 +918,17 @@
           "description": "Model provider used for this thread (for example, 'openai').",
           "type": "string"
         },
+        "multiAgentVersion": {
+          "anyOf": [
+            {
+              "$ref": "#/definitions/MultiAgentVersion"
+            },
+            {
+              "type": "null"
+            }
+          ],
+          "description": "Multi-agent runtime selected for this loaded thread. Omitted when the runtime is unavailable, such as for an unloaded stored thread."
+        },
         "name": {
           "description": "Optional user-facing thread title.",
           "type": [
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadReadResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadReadResponse.json
index 67683a0284..153d986e47 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadReadResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadReadResponse.json
@@ -616,6 +616,14 @@
         }
       ]
     },
+    "MultiAgentVersion": {
+      "enum": [
+        "disabled",
+        "v1",
+        "v2"
+      ],
+      "type": "string"
+    },
     "NonSteerableTurnKind": {
       "enum": [
         "review",
@@ -910,6 +918,17 @@
           "description": "Model provider used for this thread (for example, 'openai').",
           "type": "string"
         },
+        "multiAgentVersion": {
+          "anyOf": [
+            {
+              "$ref": "#/definitions/MultiAgentVersion"
+            },
+            {
+              "type": "null"
+            }
+          ],
+          "description": "Multi-agent runtime selected for this loaded thread. Omitted when the runtime is unavailable, such as for an unloaded stored thread."
+        },
         "name": {
           "description": "Optional user-facing thread title.",
           "type": [
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadResumeResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadResumeResponse.json
index 41802f0f3c..6f6b7c3b69 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadResumeResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadResumeResponse.json
@@ -719,6 +719,14 @@
         }
       ]
     },
+    "MultiAgentVersion": {
+      "enum": [
+        "disabled",
+        "v1",
+        "v2"
+      ],
+      "type": "string"
+    },
     "NetworkAccess": {
       "enum": [
         "restricted",
@@ -1119,6 +1127,17 @@
           "description": "Model provider used for this thread (for example, 'openai').",
           "type": "string"
         },
+        "multiAgentVersion": {
+          "anyOf": [
+            {
+              "$ref": "#/definitions/MultiAgentVersion"
+            },
+            {
+              "type": "null"
+            }
+          ],
+          "description": "Multi-agent runtime selected for this loaded thread. Omitted when the runtime is unavailable, such as for an unloaded stored thread."
+        },
         "name": {
           "description": "Optional user-facing thread title.",
           "type": [
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadRollbackResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadRollbackResponse.json
index 2452b0867e..bdee08f104 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadRollbackResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadRollbackResponse.json
@@ -616,6 +616,14 @@
         }
       ]
     },
+    "MultiAgentVersion": {
+      "enum": [
+        "disabled",
+        "v1",
+        "v2"
+      ],
+      "type": "string"
+    },
     "NonSteerableTurnKind": {
       "enum": [
         "review",
@@ -910,6 +918,17 @@
           "description": "Model provider used for this thread (for example, 'openai').",
           "type": "string"
         },
+        "multiAgentVersion": {
+          "anyOf": [
+            {
+              "$ref": "#/definitions/MultiAgentVersion"
+            },
+            {
+              "type": "null"
+            }
+          ],
+          "description": "Multi-agent runtime selected for this loaded thread. Omitted when the runtime is unavailable, such as for an unloaded stored thread."
+        },
         "name": {
           "description": "Optional user-facing thread title.",
           "type": [
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadStartResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadStartResponse.json
index c0912787fe..da22508e57 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadStartResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadStartResponse.json
@@ -719,6 +719,14 @@
         }
       ]
     },
+    "MultiAgentVersion": {
+      "enum": [
+        "disabled",
+        "v1",
+        "v2"
+      ],
+      "type": "string"
+    },
     "NetworkAccess": {
       "enum": [
         "restricted",
@@ -1119,6 +1127,17 @@
           "description": "Model provider used for this thread (for example, 'openai').",
           "type": "string"
         },
+        "multiAgentVersion": {
+          "anyOf": [
+            {
+              "$ref": "#/definitions/MultiAgentVersion"
+            },
+            {
+              "type": "null"
+            }
+          ],
+          "description": "Multi-agent runtime selected for this loaded thread. Omitted when the runtime is unavailable, such as for an unloaded stored thread."
+        },
         "name": {
           "description": "Optional user-facing thread title.",
           "type": [
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadStartedNotification.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadStartedNotification.json
index 421d8011dc..83d74aaa8f 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadStartedNotification.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadStartedNotification.json
@@ -616,6 +616,14 @@
         }
       ]
     },
+    "MultiAgentVersion": {
+      "enum": [
+        "disabled",
+        "v1",
+        "v2"
+      ],
+      "type": "string"
+    },
     "NonSteerableTurnKind": {
       "enum": [
         "review",
@@ -910,6 +918,17 @@
           "description": "Model provider used for this thread (for example, 'openai').",
           "type": "string"
         },
+        "multiAgentVersion": {
+          "anyOf": [
+            {
+              "$ref": "#/definitions/MultiAgentVersion"
+            },
+            {
+              "type": "null"
+            }
+          ],
+          "description": "Multi-agent runtime selected for this loaded thread. Omitted when the runtime is unavailable, such as for an unloaded stored thread."
+        },
         "name": {
           "description": "Optional user-facing thread title.",
           "type": [
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadUnarchiveResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadUnarchiveResponse.json
index a95e2ff725..05e762c569 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadUnarchiveResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadUnarchiveResponse.json
@@ -616,6 +616,14 @@
         }
       ]
     },
+    "MultiAgentVersion": {
+      "enum": [
+        "disabled",
+        "v1",
+        "v2"
+      ],
+      "type": "string"
+    },
     "NonSteerableTurnKind": {
       "enum": [
         "review",
@@ -910,6 +918,17 @@
           "description": "Model provider used for this thread (for example, 'openai').",
           "type": "string"
         },
+        "multiAgentVersion": {
+          "anyOf": [
+            {
+              "$ref": "#/definitions/MultiAgentVersion"
+            },
+            {
+              "type": "null"
+            }
+          ],
+          "description": "Multi-agent runtime selected for this loaded thread. Omitted when the runtime is unavailable, such as for an unloaded stored thread."
+        },
         "name": {
           "description": "Optional user-facing thread title.",
           "type": [
diff --git a/codex-rs/app-server-protocol/schema/typescript/MultiAgentVersion.ts b/codex-rs/app-server-protocol/schema/typescript/MultiAgentVersion.ts
new file mode 100644
index 0000000000..c15d47ee2c
--- /dev/null
+++ b/codex-rs/app-server-protocol/schema/typescript/MultiAgentVersion.ts
@@ -0,0 +1,5 @@
+// GENERATED CODE! DO NOT MODIFY BY HAND!
+
+// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
+
+export type MultiAgentVersion = "disabled" | "v1" | "v2";
diff --git a/codex-rs/app-server-protocol/schema/typescript/index.ts b/codex-rs/app-server-protocol/schema/typescript/index.ts
index 8080f942ad..a3b59c322c 100644
--- a/codex-rs/app-server-protocol/schema/typescript/index.ts
+++ b/codex-rs/app-server-protocol/schema/typescript/index.ts
@@ -52,6 +52,7 @@ export type { McpServerInfo } from "./McpServerInfo";
 export type { MessagePhase } from "./MessagePhase";
 export type { ModeKind } from "./ModeKind";
 export type { MultiAgentMode } from "./MultiAgentMode";
+export type { MultiAgentVersion } from "./MultiAgentVersion";
 export type { NetworkPolicyAmendment } from "./NetworkPolicyAmendment";
 export type { NetworkPolicyRuleAction } from "./NetworkPolicyRuleAction";
 export type { ParsedCommand } from "./ParsedCommand";
diff --git a/codex-rs/app-server-protocol/schema/typescript/v2/Thread.ts b/codex-rs/app-server-protocol/schema/typescript/v2/Thread.ts
index 1944cbd67b..edec8b28d6 100644
--- a/codex-rs/app-server-protocol/schema/typescript/v2/Thread.ts
+++ b/codex-rs/app-server-protocol/schema/typescript/v2/Thread.ts
@@ -2,6 +2,7 @@
 
 // This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
 import type { AbsolutePathBuf } from "../AbsolutePathBuf";
+import type { MultiAgentVersion } from "../MultiAgentVersion";
 import type { GitInfo } from "./GitInfo";
 import type { SessionSource } from "./SessionSource";
 import type { ThreadSource } from "./ThreadSource";
@@ -57,6 +58,10 @@ cliVersion: string, /**
  * Origin of the thread (CLI, VSCode, codex exec, codex app-server, etc.).
  */
 source: SessionSource, /**
+ * Multi-agent runtime selected for this loaded thread.
+ * Omitted when the runtime is unavailable, such as for an unloaded stored thread.
+ */
+multiAgentVersion?: MultiAgentVersion | null, /**
  * Optional analytics source classification for this thread.
  */
 threadSource: ThreadSource | null, /**
diff --git a/codex-rs/app-server-protocol/src/protocol/common.rs b/codex-rs/app-server-protocol/src/protocol/common.rs
index 3085a5bbc5..16b922256f 100644
--- a/codex-rs/app-server-protocol/src/protocol/common.rs
+++ b/codex-rs/app-server-protocol/src/protocol/common.rs
@@ -2767,6 +2767,7 @@ mod tests {
                     cwd: cwd.clone(),
                     cli_version: "0.0.0".to_string(),
                     source: v2::SessionSource::Exec,
+                    multi_agent_version: None,
                     can_accept_direct_input: None,
                     thread_source: None,
                     agent_nickname: None,
diff --git a/codex-rs/app-server-protocol/src/protocol/v2/tests.rs b/codex-rs/app-server-protocol/src/protocol/v2/tests.rs
index 871ca5f7ca..fb40c18762 100644
--- a/codex-rs/app-server-protocol/src/protocol/v2/tests.rs
+++ b/codex-rs/app-server-protocol/src/protocol/v2/tests.rs
@@ -273,6 +273,7 @@ fn thread_resume_response_round_trips_initial_turns_page() {
             cwd: absolute_path("tmp"),
             cli_version: "0.0.0".to_string(),
             source: SessionSource::Exec,
+            multi_agent_version: None,
             can_accept_direct_input: None,
             thread_source: None,
             agent_nickname: None,
@@ -317,7 +318,7 @@ fn thread_resume_response_round_trips_initial_turns_page() {
     let legacy_thread =
         serde_json::from_value::<Thread>(legacy_thread).expect("deserialize legacy thread");
     assert!(!legacy_thread.is_pinned);
-
+    assert_eq!(value["thread"].get("multiAgentVersion"), None);
     assert_eq!(
         value.get("initialTurnsPage"),
         Some(&json!({
@@ -344,6 +345,17 @@ fn thread_resume_response_round_trips_initial_turns_page() {
     let decoded = serde_json::from_value::<ThreadResumeResponse>(value)
         .expect("deserialize thread resume response");
     assert_eq!(decoded, response);
+
+    let mut response_with_multi_agent_version = response.clone();
+    response_with_multi_agent_version.thread.multi_agent_version = Some(MultiAgentVersion::V2);
+    let value = serde_json::to_value(&response_with_multi_agent_version)
+        .expect("serialize thread resume response with multi-agent version");
+    assert_eq!(value["thread"]["multiAgentVersion"], json!("v2"));
+    assert_eq!(
+        serde_json::from_value::<ThreadResumeResponse>(value)
+            .expect("deserialize thread resume response with multi-agent version"),
+        response_with_multi_agent_version
+    );
 }
 
 #[test]
diff --git a/codex-rs/app-server-protocol/src/protocol/v2/thread_data.rs b/codex-rs/app-server-protocol/src/protocol/v2/thread_data.rs
index 4e681271d1..ef9b942a02 100644
--- a/codex-rs/app-server-protocol/src/protocol/v2/thread_data.rs
+++ b/codex-rs/app-server-protocol/src/protocol/v2/thread_data.rs
@@ -3,6 +3,7 @@ use super::ThreadItem;
 use super::ThreadStatus;
 use super::TurnStatus;
 use codex_experimental_api_macros::ExperimentalApi;
+pub use codex_protocol::protocol::MultiAgentVersion;
 use codex_protocol::protocol::SessionSource as CoreSessionSource;
 use codex_protocol::protocol::SubAgentSource as CoreSubAgentSource;
 use codex_protocol::protocol::ThreadHistoryMode as CoreThreadHistoryMode;
@@ -211,6 +212,10 @@ pub struct Thread {
     pub cli_version: String,
     /// Origin of the thread (CLI, VSCode, codex exec, codex app-server, etc.).
     pub source: SessionSource,
+    /// Multi-agent runtime selected for this loaded thread.
+    /// Omitted when the runtime is unavailable, such as for an unloaded stored thread.
+    #[serde(default, skip_serializing_if = "Option::is_none")]
+    pub multi_agent_version: Option<MultiAgentVersion>,
     /// Whether the app server accepts direct turn input for this loaded thread.
     /// `None` means the capability is unavailable, such as for an unloaded stored thread.
     #[experimental("thread.canAcceptDirectInput")]
diff --git a/codex-rs/app-server/src/request_processors/thread_processor.rs b/codex-rs/app-server/src/request_processors/thread_processor.rs
index 518568c6da..e164e33288 100644
--- a/codex-rs/app-server/src/request_processors/thread_processor.rs
+++ b/codex-rs/app-server/src/request_processors/thread_processor.rs
@@ -2099,11 +2099,15 @@ impl ThreadRequestProcessor {
         let fallback_provider = self.config.model_provider_id.clone();
 
         for stored_thread in stored_threads {
-            let (thread, _) = thread_from_stored_thread(
+            let thread_id = stored_thread.thread_id;
+            let (mut thread, _) = thread_from_stored_thread(
                 stored_thread,
                 fallback_provider.as_str(),
                 &self.config.cwd,
             );
+            if let Ok(loaded_thread) = self.thread_manager.get_thread(thread_id).await {
+                thread.multi_agent_version = loaded_thread.multi_agent_version();
+            }
             status_ids.push(thread.id.clone());
             threads.push(thread);
         }
@@ -2529,6 +2533,7 @@ impl ThreadRequestProcessor {
             }
             thread.session_id.clone_from(&fallback_thread.session_id);
             thread.ephemeral = fallback_thread.ephemeral;
+            thread.multi_agent_version = fallback_thread.multi_agent_version;
             thread.can_accept_direct_input = fallback_thread.can_accept_direct_input;
             thread
         } else {
@@ -3742,6 +3747,7 @@ impl ThreadRequestProcessor {
             );
             thread_summary.session_id = existing_thread.session_configured().session_id.to_string();
             thread_summary.thread_source = config_snapshot.thread_source.clone().map(Into::into);
+            thread_summary.multi_agent_version = existing_thread.multi_agent_version();
             thread_summary.can_accept_direct_input = Some(can_accept_direct_input(
                 existing_thread.multi_agent_version(),
                 &config_snapshot.session_source,
@@ -4003,10 +4009,9 @@ impl ThreadRequestProcessor {
     ) -> std::result::Result<Thread, String> {
         let config_snapshot = thread.config_snapshot().await;
         let session_id = thread.session_configured().session_id.to_string();
-        let can_accept_direct_input = can_accept_direct_input(
-            thread.multi_agent_version(),
-            &config_snapshot.session_source,
-        );
+        let multi_agent_version = thread.multi_agent_version();
+        let can_accept_direct_input =
+            can_accept_direct_input(multi_agent_version, &config_snapshot.session_source);
         let thread = match thread_history {
             InitialHistory::Resumed(resumed) => {
                 let fallback_provider = config_snapshot.model_provider_id.as_str();
@@ -4081,6 +4086,7 @@ impl ThreadRequestProcessor {
             )),
         };
         let mut thread = thread?;
+        thread.multi_agent_version = multi_agent_version;
         thread.can_accept_direct_input = Some(can_accept_direct_input);
         thread.id = thread_id.to_string();
         thread.session_id = session_id;
@@ -4429,6 +4435,7 @@ impl ThreadRequestProcessor {
             forked_thread.multi_agent_version(),
             &config_snapshot.session_source,
         ));
+        thread.multi_agent_version = forked_thread.multi_agent_version();
         thread.session_id = session_configured.session_id.to_string();
         thread.thread_source = config_snapshot.thread_source.clone().map(Into::into);
 
@@ -5293,6 +5300,7 @@ pub(crate) fn thread_from_stored_thread(
         agent_nickname: source.get_nickname(),
         agent_role: source.get_agent_role(),
         source: source.into(),
+        multi_agent_version: None,
         can_accept_direct_input: None,
         thread_source: thread.thread_source.map(Into::into),
         git_info,
@@ -5501,6 +5509,7 @@ fn build_thread_from_snapshot(
         agent_nickname: config_snapshot.session_source.get_nickname(),
         agent_role: config_snapshot.session_source.get_agent_role(),
         source: config_snapshot.session_source.clone().into(),
+        multi_agent_version,
         can_accept_direct_input: Some(can_accept_direct_input(
             multi_agent_version,
             &config_snapshot.session_source,
diff --git a/codex-rs/app-server/src/request_processors/thread_resume_redaction.rs b/codex-rs/app-server/src/request_processors/thread_resume_redaction.rs
index fa03d34832..563c59373b 100644
--- a/codex-rs/app-server/src/request_processors/thread_resume_redaction.rs
+++ b/codex-rs/app-server/src/request_processors/thread_resume_redaction.rs
@@ -205,6 +205,7 @@ mod tests {
             cwd: test_path_buf("/tmp").abs(),
             cli_version: "0.0.0".to_string(),
             source: SessionSource::Cli,
+            multi_agent_version: None,
             can_accept_direct_input: None,
             thread_source: None,
             agent_nickname: None,
diff --git a/codex-rs/app-server/src/request_processors/thread_summary.rs b/codex-rs/app-server/src/request_processors/thread_summary.rs
index 8376866ace..1a2c05bfff 100644
--- a/codex-rs/app-server/src/request_processors/thread_summary.rs
+++ b/codex-rs/app-server/src/request_processors/thread_summary.rs
@@ -320,6 +320,7 @@ pub(crate) fn summary_to_thread(
         agent_nickname: source.get_nickname(),
         agent_role: source.get_agent_role(),
         source: source.into(),
+        multi_agent_version: None,
         can_accept_direct_input: None,
         thread_source: None,
         git_info,
diff --git a/codex-rs/app-server/tests/suite/v2/thread_fork.rs b/codex-rs/app-server/tests/suite/v2/thread_fork.rs
index 61fea2727e..d408601a26 100644
--- a/codex-rs/app-server/tests/suite/v2/thread_fork.rs
+++ b/codex-rs/app-server/tests/suite/v2/thread_fork.rs
@@ -275,6 +275,11 @@ async fn thread_fork_creates_new_thread_and_emits_started() -> Result<()> {
     assert_eq!(thread.source, SessionSource::VsCode);
     assert_eq!(thread.thread_source, Some(ThreadSource::User));
     assert_eq!(thread.name, None);
+    assert_eq!(
+        thread.multi_agent_version,
+        Some(MultiAgentVersion::V1),
+        "forking a persisted V1 thread must preserve its runtime version"
+    );
 
     assert_eq!(
         thread.turns.len(),
diff --git a/codex-rs/app-server/tests/suite/v2/thread_start.rs b/codex-rs/app-server/tests/suite/v2/thread_start.rs
index 9f58dbf789..993bc94764 100644
--- a/codex-rs/app-server/tests/suite/v2/thread_start.rs
+++ b/codex-rs/app-server/tests/suite/v2/thread_start.rs
@@ -14,6 +14,7 @@ use codex_app_server_protocol::JSONRPCNotification;
 use codex_app_server_protocol::JSONRPCResponse;
 use codex_app_server_protocol::McpServerStartupState;
 use codex_app_server_protocol::McpServerStatusUpdatedNotification;
+use codex_app_server_protocol::MultiAgentVersion;
 use codex_app_server_protocol::RequestId;
 use codex_app_server_protocol::SandboxMode;
 #[cfg(not(windows))]
@@ -431,6 +432,38 @@ async fn thread_start_creates_thread_and_emits_started() -> Result<()> {
     Ok(())
 }
 
+#[tokio::test]
+async fn thread_start_exposes_effective_multi_agent_version() -> Result<()> {
+    let server = create_mock_responses_server_repeating_assistant("Done").await;
+    let codex_home = TempDir::new()?;
+    create_config_toml_without_approval_policy(codex_home.path(), &server.uri())?;
+    let config_path = codex_home.path().join("config.toml");
+    let config = std::fs::read_to_string(&config_path)?;
+    std::fs::write(
+        config_path,
+        format!("{config}\n[features]\nmulti_agent_v2 = true\n"),
+    )?;
+
+    let mut mcp = TestAppServer::builder()
+        .with_codex_home(codex_home.path())
+        .build()
+        .await?;
+    timeout(DEFAULT_READ_TIMEOUT, mcp.initialize()).await??;
+
+    let response = start_thread_with_model(
+        &mut mcp,
+        "mock-model",
+        /*allow_provider_model_fallback*/ false,
+    )
+    .await?;
+
+    assert_eq!(
+        response.thread.multi_agent_version,
+        Some(MultiAgentVersion::V2)
+    );
+    Ok(())
+}
+
 #[tokio::test]
 async fn thread_start_history_mode_accepts_legacy_and_paginated() -> Result<()> {
     let server = create_mock_responses_server_repeating_assistant("Done").await;
