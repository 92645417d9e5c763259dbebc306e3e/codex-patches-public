diff --git a/codex-rs/app-server-protocol/schema/json/ServerNotification.json b/codex-rs/app-server-protocol/schema/json/ServerNotification.json
index 0eae8288cc..1084cde406 100644
--- a/codex-rs/app-server-protocol/schema/json/ServerNotification.json
+++ b/codex-rs/app-server-protocol/schema/json/ServerNotification.json
@@ -3388,6 +3388,20 @@
         "threadId": {
           "type": "string"
         },
+        "tps": {
+          "format": "double",
+          "type": [
+            "number",
+            "null"
+          ]
+        },
+        "ttfb": {
+          "format": "double",
+          "type": [
+            "number",
+            "null"
+          ]
+        },
         "turnId": {
           "type": "string"
         }
diff --git a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json
index 771eb67443..3e6daa799a 100644
--- a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json
+++ b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json
@@ -16932,6 +16932,20 @@
           "threadId": {
             "type": "string"
           },
+          "tps": {
+            "format": "double",
+            "type": [
+              "number",
+              "null"
+            ]
+          },
+          "ttfb": {
+            "format": "double",
+            "type": [
+              "number",
+              "null"
+            ]
+          },
           "turnId": {
             "type": "string"
           }
diff --git a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json
index 9f83d63757..7f0a473501 100644
--- a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json
+++ b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json
@@ -13197,6 +13197,20 @@
         "threadId": {
           "type": "string"
         },
+        "tps": {
+          "format": "double",
+          "type": [
+            "number",
+            "null"
+          ]
+        },
+        "ttfb": {
+          "format": "double",
+          "type": [
+            "number",
+            "null"
+          ]
+        },
         "turnId": {
           "type": "string"
         }
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ResponseOutputBytesDeltaNotification.json b/codex-rs/app-server-protocol/schema/json/v2/ResponseOutputBytesDeltaNotification.json
index 119800ea39..01d08b86a2 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ResponseOutputBytesDeltaNotification.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ResponseOutputBytesDeltaNotification.json
@@ -15,6 +15,20 @@
     "threadId": {
       "type": "string"
     },
+    "tps": {
+      "format": "double",
+      "type": [
+        "number",
+        "null"
+      ]
+    },
+    "ttfb": {
+      "format": "double",
+      "type": [
+        "number",
+        "null"
+      ]
+    },
     "turnId": {
       "type": "string"
     }
diff --git a/codex-rs/app-server-protocol/schema/typescript/v2/ResponseOutputBytesDeltaNotification.ts b/codex-rs/app-server-protocol/schema/typescript/v2/ResponseOutputBytesDeltaNotification.ts
index 5f283b5556..15b413c62b 100644
--- a/codex-rs/app-server-protocol/schema/typescript/v2/ResponseOutputBytesDeltaNotification.ts
+++ b/codex-rs/app-server-protocol/schema/typescript/v2/ResponseOutputBytesDeltaNotification.ts
@@ -2,4 +2,4 @@
 
 // This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
 
-export type ResponseOutputBytesDeltaNotification = { threadId: string, turnId: string, requestId: string, byteCount: bigint, reset: boolean, };
+export type ResponseOutputBytesDeltaNotification = { threadId: string, turnId: string, requestId: string, byteCount: bigint, reset: boolean, ttfb: number | null, tps: number | null, };
diff --git a/codex-rs/app-server-protocol/src/protocol/event_mapping.rs b/codex-rs/app-server-protocol/src/protocol/event_mapping.rs
index da3c339949..8bb2ee787a 100644
--- a/codex-rs/app-server-protocol/src/protocol/event_mapping.rs
+++ b/codex-rs/app-server-protocol/src/protocol/event_mapping.rs
@@ -406,6 +406,8 @@ pub fn item_event_to_server_notification(
                 request_id: event.request_id,
                 byte_count: event.byte_count,
                 reset: event.reset,
+                ttfb: event.ttfb,
+                tps: event.tps,
             })
         }
         EventMsg::AgentReasoningSectionBreak(event) => {
diff --git a/codex-rs/app-server-protocol/src/protocol/v2/thread.rs b/codex-rs/app-server-protocol/src/protocol/v2/thread.rs
index be42d523cd..6b10ab1868 100644
--- a/codex-rs/app-server-protocol/src/protocol/v2/thread.rs
+++ b/codex-rs/app-server-protocol/src/protocol/v2/thread.rs
@@ -1574,6 +1574,8 @@ pub struct ResponseOutputBytesDeltaNotification {
     pub request_id: String,
     pub byte_count: u64,
     pub reset: bool,
+    pub ttfb: Option<f64>,
+    pub tps: Option<f64>,
 }
 
 #[derive(Serialize, Deserialize, Debug, Clone, PartialEq, JsonSchema, TS)]
diff --git a/codex-rs/codex-api/src/common.rs b/codex-rs/codex-api/src/common.rs
index 44044bf35e..c15f0b81d0 100644
--- a/codex-rs/codex-api/src/common.rs
+++ b/codex-rs/codex-api/src/common.rs
@@ -82,7 +82,13 @@ pub enum ResponseEvent {
     /// Transport implementations publish these observations on a side channel;
     /// the core stream mapper merges them into its internal event stream without
     /// coupling byte accounting to semantic response decoding.
-    ResponseBytesDelta(usize),
+    ResponseBytesDelta {
+        byte_count: usize,
+        ttfb: Option<f64>,
+    },
+    RequestPerformance {
+        tps: Option<f64>,
+    },
     SafetyBuffering(SafetyBuffering),
     OutputItemDone(ResponseItem),
     OutputItemAdded(ResponseItem),
diff --git a/codex-rs/core/src/client.rs b/codex-rs/core/src/client.rs
index 3b8027665f..ac37ffadf7 100644
--- a/codex-rs/core/src/client.rs
+++ b/codex-rs/core/src/client.rs
@@ -2112,6 +2112,7 @@ where
         let mut tx_last_response = Some(tx_last_response);
         let mut items_added: Vec<ResponseItem> = Vec::new();
         let mut performance_recorded = false;
+        let mut ttfb_published = false;
         let mut api_stream = api_stream;
         let mut response_bytes_rx = response_bytes_rx;
         let upstream_request_id = upstream_request_id.as_deref();
@@ -2146,7 +2147,18 @@ where
                     }
                 } => {
                     match byte_count {
-                        Some(byte_count) => Some(Ok(ResponseEvent::ResponseBytesDelta(byte_count))),
+                        Some(byte_count) => {
+                            let ttfb = if !ttfb_published {
+                                let value = performance.as_ref().and_then(|(timing, _)| {
+                                    timing.request_started_at().zip(timing.first_response_byte_at())
+                                }).map(|(started, first)| first.saturating_duration_since(started).as_secs_f64());
+                                ttfb_published = value.is_some();
+                                value
+                            } else {
+                                None
+                            };
+                            Some(Ok(ResponseEvent::ResponseBytesDelta { byte_count, ttfb }))
+                        }
                         None => {
                             response_bytes_rx = None;
                             continue;
@@ -2196,6 +2208,21 @@ where
                     if let Some((timing, transport)) = performance.as_ref()
                         && !performance_recorded
                     {
+                        let (_, generation_duration) =
+                            response_performance_durations(timing, Instant::now());
+                        let tps = token_usage.as_ref().and_then(|usage| {
+                            billed_output_tokens_per_second(
+                                usage.output_tokens,
+                                generation_duration?,
+                            )
+                        });
+                        if tx_event
+                            .send(Ok(ResponseEvent::RequestPerformance { tps }))
+                            .await
+                            .is_err()
+                        {
+                            return;
+                        }
                         record_completed_model_request_performance(
                             &session_telemetry,
                             timing,
@@ -2355,7 +2382,7 @@ fn record_completed_model_request_performance(
 ) {
     let (ttfb, generation_duration) = response_performance_durations(timing, Instant::now());
     let output_token_count = usage.map(|usage| usage.output_tokens);
-    let output_tokens_per_second = output_token_count.and_then(|output_token_count| {
+    let tps = output_token_count.and_then(|output_token_count| {
         billed_output_tokens_per_second(output_token_count, generation_duration?)
     });
     let outcome = if usage.is_some() {
@@ -2369,7 +2396,7 @@ fn record_completed_model_request_performance(
         ttfb,
         generation_duration,
         output_token_count,
-        output_tokens_per_second,
+        tps,
     );
 }
 
diff --git a/codex-rs/core/src/client_tests.rs b/codex-rs/core/src/client_tests.rs
index 09f49df385..afdd5753c4 100644
--- a/codex-rs/core/src/client_tests.rs
+++ b/codex-rs/core/src/client_tests.rs
@@ -715,8 +715,14 @@ async fn response_stream_merges_raw_transport_bytes_before_completion() {
         events.push(event.expect("mapped response event"));
     }
 
-    assert!(matches!(events[0], ResponseEvent::ResponseBytesDelta(7)));
-    assert!(matches!(events[1], ResponseEvent::ResponseBytesDelta(11)));
+    assert!(matches!(
+        events[0],
+        ResponseEvent::ResponseBytesDelta { byte_count: 7, .. }
+    ));
+    assert!(matches!(
+        events[1],
+        ResponseEvent::ResponseBytesDelta { byte_count: 11, .. }
+    ));
     assert!(matches!(events[2], ResponseEvent::Created));
     assert!(matches!(events[3], ResponseEvent::Completed { .. }));
 }
diff --git a/codex-rs/core/src/session/turn.rs b/codex-rs/core/src/session/turn.rs
index 98cc4ec36d..782f014a90 100644
--- a/codex-rs/core/src/session/turn.rs
+++ b/codex-rs/core/src/session/turn.rs
@@ -1754,8 +1754,10 @@ async fn emit_response_output_bytes(
     request_id: &str,
     byte_count: usize,
     reset: bool,
+    ttfb: Option<f64>,
+    tps: Option<f64>,
 ) {
-    if byte_count == 0 && !reset {
+    if byte_count == 0 && !reset && ttfb.is_none() && tps.is_none() {
         return;
     }
     sess.send_event(
@@ -1766,6 +1768,8 @@ async fn emit_response_output_bytes(
             request_id: request_id.to_string(),
             byte_count: u64::try_from(byte_count).unwrap_or(u64::MAX),
             reset,
+            ttfb,
+            tps,
         }),
     )
     .await;
@@ -2143,7 +2147,16 @@ async fn try_run_sampling_request(
         .or_cancel(&cancellation_token)
         .await??;
     let response_output_request_id = Uuid::new_v4().to_string();
-    emit_response_output_bytes(&sess, &turn_context, &response_output_request_id, 0, true).await;
+    emit_response_output_bytes(
+        &sess,
+        &turn_context,
+        &response_output_request_id,
+        0,
+        true,
+        None,
+        None,
+    )
+    .await;
     let mut in_flight: FuturesOrdered<BoxFuture<'static, CodexResult<ResponseInputItem>>> =
         FuturesOrdered::new();
     let mut needs_follow_up = false;
@@ -2200,16 +2213,34 @@ async fn try_run_sampling_request(
             }
         };
 
-        if let ResponseEvent::ResponseBytesDelta(byte_count) = event {
-            emit_response_output_bytes(
-                &sess,
-                &turn_context,
-                &response_output_request_id,
-                byte_count,
-                false,
-            )
-            .await;
-            continue;
+        match event {
+            ResponseEvent::ResponseBytesDelta { byte_count, ttfb } => {
+                emit_response_output_bytes(
+                    &sess,
+                    &turn_context,
+                    &response_output_request_id,
+                    byte_count,
+                    false,
+                    ttfb,
+                    None,
+                )
+                .await;
+                continue;
+            }
+            ResponseEvent::RequestPerformance { tps } => {
+                emit_response_output_bytes(
+                    &sess,
+                    &turn_context,
+                    &response_output_request_id,
+                    0,
+                    false,
+                    None,
+                    tps,
+                )
+                .await;
+                continue;
+            }
+            _ => {}
         }
 
         sess.services
@@ -2630,7 +2661,7 @@ async fn try_run_sampling_request(
                     error_or_panic("ReasoningRawContentDelta without active item".to_string());
                 }
             }
-            ResponseEvent::ResponseBytesDelta(_) => {
+            ResponseEvent::ResponseBytesDelta { .. } | ResponseEvent::RequestPerformance { .. } => {
                 unreachable!("raw response bytes are handled before semantic response events")
             }
         }
diff --git a/codex-rs/core/src/turn_timing.rs b/codex-rs/core/src/turn_timing.rs
index 7c64e78cb9..d01e14ebe0 100644
--- a/codex-rs/core/src/turn_timing.rs
+++ b/codex-rs/core/src/turn_timing.rs
@@ -369,7 +369,8 @@ fn response_event_records_turn_ttft(event: &ResponseEvent) -> bool {
         | ResponseEvent::ReasoningSummaryDone { .. }
         | ResponseEvent::ReasoningContentDelta { .. } => true,
         ResponseEvent::Created
-        | ResponseEvent::ResponseBytesDelta(_)
+        | ResponseEvent::ResponseBytesDelta { .. }
+        | ResponseEvent::RequestPerformance { .. }
         | ResponseEvent::ServerModel(_)
         | ResponseEvent::ModelVerifications(_)
         | ResponseEvent::TurnModerationMetadata(_)
diff --git a/codex-rs/core/tests/suite/otel.rs b/codex-rs/core/tests/suite/otel.rs
index a8dc168883..81b194ee38 100644
--- a/codex-rs/core/tests/suite/otel.rs
+++ b/codex-rs/core/tests/suite/otel.rs
@@ -634,7 +634,7 @@ async fn process_sse_emits_completed_telemetry() {
                     && line.contains("output_token_count=5")
                     && extract_log_field(line, "ttfb_ms").is_some()
                     && extract_log_field(line, "generation_duration_ms").is_some()
-                    && extract_log_field(line, "output_tokens_per_second").is_some()
+                    && extract_log_field(line, "tps").is_some()
             })
             .map(|_| Ok(()))
             .unwrap_or(Err(
diff --git a/codex-rs/otel/src/events/session_telemetry.rs b/codex-rs/otel/src/events/session_telemetry.rs
index 49d7277453..539caef6c0 100644
--- a/codex-rs/otel/src/events/session_telemetry.rs
+++ b/codex-rs/otel/src/events/session_telemetry.rs
@@ -7,7 +7,7 @@ use crate::metrics::API_CALL_COUNT_METRIC;
 use crate::metrics::API_CALL_DURATION_METRIC;
 use crate::metrics::MODEL_REQUEST_GENERATION_DURATION_METRIC;
 use crate::metrics::MODEL_REQUEST_OUTPUT_TOKEN_COUNT_METRIC;
-use crate::metrics::MODEL_REQUEST_OUTPUT_TOKENS_PER_SECOND_METRIC;
+use crate::metrics::MODEL_REQUEST_TPS_METRIC;
 use crate::metrics::MODEL_REQUEST_TTFB_METRIC;
 use crate::metrics::MetricsClient;
 use crate::metrics::MetricsConfig;
@@ -968,7 +968,7 @@ impl SessionTelemetry {
         ttfb: Option<Duration>,
         generation_duration: Option<Duration>,
         output_token_count: Option<i64>,
-        output_tokens_per_second: Option<f64>,
+        tps: Option<f64>,
     ) {
         let tags = [("transport", transport), ("outcome", outcome)];
         if let Some(ttfb) = ttfb {
@@ -988,12 +988,8 @@ impl SessionTelemetry {
                 &tags,
             );
         }
-        if let Some(output_tokens_per_second) = output_tokens_per_second {
-            self.histogram_f64(
-                MODEL_REQUEST_OUTPUT_TOKENS_PER_SECOND_METRIC,
-                output_tokens_per_second,
-                &tags,
-            );
+        if let Some(tps) = tps {
+            self.histogram_f64(MODEL_REQUEST_TPS_METRIC, tps, &tags);
         }
 
         let ttfb = ttfb.map(|duration| duration.as_secs_f64());
@@ -1008,7 +1004,7 @@ impl SessionTelemetry {
                 ttfb = ttfb,
                 generation_duration_ms = generation_duration_ms,
                 output_token_count = output_token_count,
-                output_tokens_per_second = output_tokens_per_second,
+                tps = tps,
                 service_tier = self.metadata.service_tier.as_deref(),
                 model_reasoning_effort = self.metadata.model_reasoning_effort.as_deref(),
             },
@@ -1278,7 +1274,8 @@ impl SessionTelemetry {
     fn responses_type(event: &ResponseEvent) -> String {
         match event {
             ResponseEvent::Created => "created".into(),
-            ResponseEvent::ResponseBytesDelta(_) => "response_bytes_delta".into(),
+            ResponseEvent::ResponseBytesDelta { .. } => "response_bytes_delta".into(),
+            ResponseEvent::RequestPerformance { .. } => "request_performance".into(),
             ResponseEvent::OutputItemDone(item) | ResponseEvent::OutputItemAdded(item) => {
                 SessionTelemetry::responses_item_type(item)
             }
diff --git a/codex-rs/otel/src/metrics/names.rs b/codex-rs/otel/src/metrics/names.rs
index c4bd6991a8..d82a9b0846 100644
--- a/codex-rs/otel/src/metrics/names.rs
+++ b/codex-rs/otel/src/metrics/names.rs
@@ -28,8 +28,7 @@ pub const MODEL_REQUEST_TTFB_METRIC: &str = "codex.model_request.ttfb";
 pub const MODEL_REQUEST_GENERATION_DURATION_METRIC: &str =
     "codex.model_request.generation_duration_ms";
 pub const MODEL_REQUEST_OUTPUT_TOKEN_COUNT_METRIC: &str = "codex.model_request.output_token_count";
-pub const MODEL_REQUEST_OUTPUT_TOKENS_PER_SECOND_METRIC: &str =
-    "codex.model_request.output_tokens_per_second";
+pub const MODEL_REQUEST_TPS_METRIC: &str = "codex.model_request.tps";
 pub const TURN_NETWORK_PROXY_METRIC: &str = "codex.turn.network_proxy";
 pub const TURN_MEMORY_METRIC: &str = "codex.turn.memory";
 pub const TURN_TOOL_CALL_METRIC: &str = "codex.turn.tool.call";
diff --git a/codex-rs/protocol/src/protocol.rs b/codex-rs/protocol/src/protocol.rs
index 976b689345..fde5cb542c 100644
--- a/codex-rs/protocol/src/protocol.rs
+++ b/codex-rs/protocol/src/protocol.rs
@@ -78,6 +78,19 @@ where
         .ok_or_else(|| D::Error::custom("number is not representable as f64"))
 }
 
+fn deserialize_optional_json_f64<'de, D>(deserializer: D) -> Result<Option<f64>, D::Error>
+where
+    D: Deserializer<'de>,
+{
+    Option::<serde_json::Number>::deserialize(deserializer)?
+        .map(|number| {
+            number
+                .as_f64()
+                .ok_or_else(|| D::Error::custom("number is not representable as f64"))
+        })
+        .transpose()
+}
+
 use ts_rs::TS;
 
 pub use crate::approvals::ApplyPatchApprovalRequestEvent;
@@ -1933,6 +1946,10 @@ pub struct ResponseOutputBytesDeltaEvent {
     pub request_id: String,
     pub byte_count: u64,
     pub reset: bool,
+    #[serde(deserialize_with = "deserialize_optional_json_f64")]
+    pub ttfb: Option<f64>,
+    #[serde(deserialize_with = "deserialize_optional_json_f64")]
+    pub tps: Option<f64>,
 }
 
 #[derive(Debug, Clone, Deserialize, Serialize, JsonSchema, TS)]
@@ -4639,6 +4656,29 @@ mod tests {
         Ok(())
     }
 
+    #[test]
+    fn response_performance_floats_deserialize_inside_tagged_event() -> Result<()> {
+        let value = json!({
+            "type": "response_output_bytes_delta",
+            "thread_id": "thread",
+            "turn_id": "turn",
+            "request_id": "request",
+            "byte_count": 1234,
+            "reset": false,
+            "ttfb": 0.125,
+            "tps": 42.5
+        });
+
+        let EventMsg::ResponseOutputBytesDelta(event) =
+            serde_json::from_value::<EventMsg>(value)?
+        else {
+            anyhow::bail!("expected response_output_bytes_delta event");
+        };
+        assert_eq!(event.ttfb, Some(0.125));
+        assert_eq!(event.tps, Some(42.5));
+        Ok(())
+    }
+
     #[test]
     fn review_decision_denied_round_trip() -> Result<()> {
         let decision = ReviewDecision::Denied {
