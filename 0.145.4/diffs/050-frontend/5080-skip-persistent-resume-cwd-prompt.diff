diff --git a/codex-rs/tui/src/app/tests.rs b/codex-rs/tui/src/app/tests.rs
index 1605a68844..8802203462 100644
--- a/codex-rs/tui/src/app/tests.rs
+++ b/codex-rs/tui/src/app/tests.rs
@@ -5833,6 +5833,7 @@ fn session_start_error_surfaces_archived_guidance_without_rollout_path() {
             "/Users/me/.codex/archived_sessions/rollout.jsonl",
         )),
         thread_id,
+        status: None,
     };
     let expected = format!(
         "session {thread_id} is archived. Run `codex unarchive {thread_id}` to unarchive it first."
@@ -6175,6 +6176,7 @@ async fn remote_resume_current_cwd_rejection_snapshot() -> Result<()> {
             crate::resume_picker::SessionTarget {
                 path: None,
                 thread_id: ThreadId::new(),
+                status: None,
             },
         )
         .await?;
@@ -6217,6 +6219,7 @@ async fn remote_exec_resume_current_cwd_is_rejected() -> Result<()> {
             crate::resume_picker::SessionTarget {
                 path: None,
                 thread_id: ThreadId::new(),
+                status: None,
             },
         )
         .await?;
@@ -6254,6 +6257,7 @@ async fn in_app_resume_session_cwd_without_metadata_is_non_fatal() -> Result<()>
             crate::resume_picker::SessionTarget {
                 path: None,
                 thread_id: ThreadId::new(),
+                status: None,
             },
         )
         .await?;
@@ -6316,6 +6320,7 @@ async fn remote_resume_keeps_server_only_cwd_out_of_local_config() -> Result<()>
             crate::resume_picker::SessionTarget {
                 path: Some(rollout_path),
                 thread_id: ThreadId::from_string(&thread_id)?,
+                status: None,
             },
         )
         .await?;
@@ -6434,6 +6439,7 @@ async fn in_app_resume_uses_configured_or_explicit_cwd() -> Result<()> {
                 crate::resume_picker::SessionTarget {
                     path: Some(rollout_path),
                     thread_id,
+                    status: None,
                 },
             )
             .await?;
@@ -6543,6 +6549,7 @@ async fn remembered_current_cwd_stays_at_launch_across_in_app_resumes() -> Resul
         targets.push(crate::resume_picker::SessionTarget {
             path: Some(rollout_path),
             thread_id: ThreadId::from_string(&thread_id)?,
+            status: None,
         });
     }
     let state_db =
diff --git a/codex-rs/tui/src/app/tests/session_lifecycle_requests.rs b/codex-rs/tui/src/app/tests/session_lifecycle_requests.rs
index 6c490ec566..13c59f3c34 100644
--- a/codex-rs/tui/src/app/tests/session_lifecycle_requests.rs
+++ b/codex-rs/tui/src/app/tests/session_lifecycle_requests.rs
@@ -324,6 +324,7 @@ fn session_lifecycle_avoids_redundant_subagent_metadata_reads() -> Result<()> {
                         crate::resume_picker::SessionTarget {
                             path: Some(root_rollout_path),
                             thread_id: root_thread_id,
+                            status: None,
                         },
                     )
                     .await?;
diff --git a/codex-rs/tui/src/app/tests/startup.rs b/codex-rs/tui/src/app/tests/startup.rs
index 20f31349cf..16a6af61dc 100644
--- a/codex-rs/tui/src/app/tests/startup.rs
+++ b/codex-rs/tui/src/app/tests/startup.rs
@@ -19,6 +19,7 @@ fn startup_waiting_gate_is_only_for_fresh_or_exit_session_selection() {
             crate::resume_picker::SessionTarget {
                 path: Some(PathBuf::from("/tmp/restore")),
                 thread_id: ThreadId::new(),
+                status: None,
             }
         )),
         false
@@ -28,6 +29,7 @@ fn startup_waiting_gate_is_only_for_fresh_or_exit_session_selection() {
             crate::resume_picker::SessionTarget {
                 path: Some(PathBuf::from("/tmp/fork")),
                 thread_id: ThreadId::new(),
+                status: None,
             }
         )),
         false
@@ -39,10 +41,12 @@ fn startup_paused_goal_prompt_gate_is_only_for_quiet_resume() {
     let resume = SessionSelection::Resume(crate::resume_picker::SessionTarget {
         path: Some(PathBuf::from("/tmp/restore")),
         thread_id: ThreadId::new(),
+        status: None,
     });
     let fork = SessionSelection::Fork(crate::resume_picker::SessionTarget {
         path: Some(PathBuf::from("/tmp/fork")),
         thread_id: ThreadId::new(),
+        status: None,
     });
     let no_images: Vec<PathBuf> = Vec::new();
     let initial_images = vec![PathBuf::from("/tmp/image.png")];
@@ -111,6 +115,7 @@ fn startup_waiting_gate_not_applied_for_resume_or_fork_session_selection() {
         crate::resume_picker::SessionTarget {
             path: Some(PathBuf::from("/tmp/restore")),
             thread_id: ThreadId::new(),
+            status: None,
         },
     ));
     assert_eq!(
@@ -124,6 +129,7 @@ fn startup_waiting_gate_not_applied_for_resume_or_fork_session_selection() {
         crate::resume_picker::SessionTarget {
             path: Some(PathBuf::from("/tmp/fork")),
             thread_id: ThreadId::new(),
+            status: None,
         },
     ));
     assert_eq!(
@@ -272,6 +278,7 @@ async fn ignore_same_thread_resume_reports_noop_for_current_thread() {
     let ignored = app.ignore_same_thread_resume(&crate::resume_picker::SessionTarget {
         path: Some(test_path_buf("/tmp/project")),
         thread_id,
+        status: None,
     });
 
     assert!(ignored);
@@ -296,6 +303,7 @@ async fn ignore_same_thread_resume_allows_reattaching_displayed_inactive_thread(
     let ignored = app.ignore_same_thread_resume(&crate::resume_picker::SessionTarget {
         path: Some(test_path_buf("/tmp/project")),
         thread_id,
+        status: None,
     });
 
     assert!(!ignored);
diff --git a/codex-rs/tui/src/lib.rs b/codex-rs/tui/src/lib.rs
index 5e6cdd4a63..599e2582d1 100644
--- a/codex-rs/tui/src/lib.rs
+++ b/codex-rs/tui/src/lib.rs
@@ -39,6 +39,7 @@ use codex_app_server_protocol::ThreadListCwdFilter;
 use codex_app_server_protocol::ThreadListParams;
 use codex_app_server_protocol::ThreadSortKey as AppServerThreadSortKey;
 use codex_app_server_protocol::ThreadSourceKind;
+use codex_app_server_protocol::ThreadStatus;
 use codex_cloud_config::cloud_config_bundle_loader_for_storage;
 use codex_config::CloudConfigBundleLoader;
 use codex_config::ConfigLoadError;
@@ -579,6 +580,7 @@ fn session_target_from_app_server_thread(
         Ok(thread_id) => Some(resume_picker::SessionTarget {
             path: thread.path,
             thread_id,
+            status: Some(thread.status),
         }),
         Err(err) => {
             warn!(
@@ -780,6 +782,7 @@ async fn resolve_startup_resume_or_fork_cwd(
     cwd_override: Option<&Path>,
     uses_remote_workspace: bool,
     uses_remote_workspace_or_environment: bool,
+    skip_active_persistent_prompt: bool,
 ) -> color_eyre::Result<ResolveCwdOutcome> {
     let Some((action, target_session)) = (match session_selection {
         resume_picker::SessionSelection::Resume(target_session) => {
@@ -804,6 +807,9 @@ async fn resolve_startup_resume_or_fork_cwd(
     if uses_remote_workspace {
         return Ok(ResolveCwdOutcome::Continue(Some(config.cwd.to_path_buf())));
     }
+    if skip_active_persistent_prompt && resume_cwd_mode.is_none() {
+        return Ok(ResolveCwdOutcome::Continue(Some(config.cwd.to_path_buf())));
+    }
 
     resolve_cwd_for_resume_or_fork(
         tui,
@@ -821,6 +827,20 @@ async fn resolve_startup_resume_or_fork_cwd(
     .await
 }
 
+fn is_active_local_daemon_resume(
+    app_server_target: &AppServerTarget,
+    session_selection: &resume_picker::SessionSelection,
+) -> bool {
+    matches!(app_server_target, AppServerTarget::LocalDaemon { .. })
+        && matches!(
+            session_selection,
+            resume_picker::SessionSelection::Resume(resume_picker::SessionTarget {
+                status: Some(ThreadStatus::Active { .. }),
+                ..
+            })
+        )
+}
+
 fn should_load_configured_environments(
     loader_overrides: &LoaderOverrides,
     app_server_target: &AppServerTarget,
@@ -1662,6 +1682,7 @@ async fn run_ratatui_app(
         cli.cwd.as_deref(),
         uses_remote_workspace,
         uses_remote_workspace_or_environment(&app_server_target, &environment_manager),
+        is_active_local_daemon_resume(&app_server_target, &session_selection),
     )
     .await
     {
@@ -2235,6 +2256,7 @@ mod tests {
             let target_session = resume_picker::SessionTarget {
                 path: Some(rollout_path),
                 thread_id,
+                status: None,
             };
             let session_selection = match action {
                 CwdPromptAction::Resume => resume_picker::SessionSelection::Resume(target_session),
@@ -2250,6 +2272,7 @@ mod tests {
                 cwd_override,
                 /*uses_remote_workspace*/ false,
                 /*uses_remote_workspace_or_environment*/ false,
+                /*skip_active_persistent_prompt*/ matches!(action, CwdPromptAction::Resume),
             )
             .await?
             {
@@ -2322,10 +2345,12 @@ mod tests {
             &resume_picker::SessionSelection::Resume(resume_picker::SessionTarget {
                 path: None,
                 thread_id: ThreadId::new(),
+                status: None,
             }),
             /*cwd_override*/ None,
             /*uses_remote_workspace*/ false,
             /*uses_remote_workspace_or_environment*/ true,
+            /*skip_active_persistent_prompt*/ false,
         )
         .await
         .expect_err("remote current cwd should require an explicit override");
@@ -2358,10 +2383,12 @@ mod tests {
             &resume_picker::SessionSelection::Resume(resume_picker::SessionTarget {
                 path: None,
                 thread_id: ThreadId::new(),
+                status: None,
             }),
             /*cwd_override*/ None,
             /*uses_remote_workspace*/ false,
             /*uses_remote_workspace_or_environment*/ false,
+            /*skip_active_persistent_prompt*/ false,
         )
         .await
         .expect_err("session cwd should require saved metadata");
@@ -2373,6 +2400,42 @@ mod tests {
         Ok(())
     }
 
+    #[tokio::test]
+    async fn startup_active_local_daemon_resume_skips_unconfigured_cwd_prompt()
+    -> color_eyre::Result<()> {
+        let temp_dir = TempDir::new()?;
+        let current_cwd = temp_dir.path().join("current");
+        std::fs::create_dir_all(&current_cwd)?;
+        let config = ConfigBuilder::default()
+            .codex_home(temp_dir.path().to_path_buf())
+            .loader_overrides(LoaderOverrides::without_managed_config_for_tests())
+            .harness_overrides(ConfigOverrides {
+                cwd: Some(current_cwd.clone()),
+                ..Default::default()
+            })
+            .build()
+            .await?;
+        let mut tui = tui::test_support::make_test_tui()?;
+        let selection = resume_picker::SessionSelection::Resume(resume_picker::SessionTarget {
+            path: None,
+            thread_id: ThreadId::new(),
+            status: Some(ThreadStatus::Active {
+                active_flags: Vec::new(),
+            }),
+        });
+
+        let outcome = resolve_startup_resume_or_fork_cwd(
+            &mut tui, &config, /*state_db*/ None, &selection, /*cwd_override*/ None,
+            /*uses_remote_workspace*/ false,
+            /*uses_remote_workspace_or_environment*/ false,
+            /*skip_active_persistent_prompt*/ true,
+        )
+        .await?;
+
+        assert_eq!(outcome, ResolveCwdOutcome::Continue(Some(current_cwd)));
+        Ok(())
+    }
+
     #[test]
     fn alternate_screen_auto_uses_alt_screen() {
         assert!(determine_alt_screen_mode(
@@ -2399,6 +2462,7 @@ mod tests {
         let target = crate::resume_picker::SessionTarget {
             path: None,
             thread_id,
+            status: None,
         };
 
         assert_eq!(target.display_label(), format!("thread {thread_id}"));
@@ -2504,6 +2568,50 @@ mod tests {
         Ok(())
     }
 
+    #[test]
+    fn active_local_daemon_resume_is_the_only_cwd_prompt_skip_target() -> color_eyre::Result<()> {
+        let socket_path = AbsolutePathBuf::relative_to_current_dir("codex.sock")?;
+        let endpoint = RemoteAppServerEndpoint::UnixSocket { socket_path };
+        let local_daemon = AppServerTarget::LocalDaemon {
+            endpoint: endpoint.clone(),
+        };
+        let remote = AppServerTarget::Remote { endpoint };
+        let active = resume_picker::SessionTarget {
+            path: None,
+            thread_id: ThreadId::new(),
+            status: Some(ThreadStatus::Active {
+                active_flags: Vec::new(),
+            }),
+        };
+        let idle = resume_picker::SessionTarget {
+            path: None,
+            thread_id: ThreadId::new(),
+            status: Some(ThreadStatus::Idle),
+        };
+
+        assert!(is_active_local_daemon_resume(
+            &local_daemon,
+            &resume_picker::SessionSelection::Resume(active.clone())
+        ));
+        assert!(!is_active_local_daemon_resume(
+            &local_daemon,
+            &resume_picker::SessionSelection::Resume(idle)
+        ));
+        assert!(!is_active_local_daemon_resume(
+            &local_daemon,
+            &resume_picker::SessionSelection::Fork(active.clone())
+        ));
+        assert!(!is_active_local_daemon_resume(
+            &AppServerTarget::Embedded,
+            &resume_picker::SessionSelection::Resume(active.clone())
+        ));
+        assert!(!is_active_local_daemon_resume(
+            &remote,
+            &resume_picker::SessionSelection::Resume(active)
+        ));
+        Ok(())
+    }
+
     #[test]
     fn implicit_daemon_preparation_error_uses_embedded() -> color_eyre::Result<()> {
         let socket_path = AbsolutePathBuf::relative_to_current_dir("codex.sock")?;
diff --git a/codex-rs/tui/src/resume_picker.rs b/codex-rs/tui/src/resume_picker.rs
index e07e83467f..1c0d6f3166 100644
--- a/codex-rs/tui/src/resume_picker.rs
+++ b/codex-rs/tui/src/resume_picker.rs
@@ -84,6 +84,7 @@ const PICKER_LIST_HORIZONTAL_INSET: u16 = 4;
 pub struct SessionTarget {
     pub path: Option<PathBuf>,
     pub thread_id: ThreadId,
+    pub status: Option<codex_app_server_protocol::ThreadStatus>,
 }
 
 impl SessionTarget {
@@ -130,8 +131,17 @@ impl SessionPickerAction {
         }
     }
 
-    fn selection(self, path: Option<PathBuf>, thread_id: ThreadId) -> SessionSelection {
-        let target_session = SessionTarget { path, thread_id };
+    fn selection(
+        self,
+        path: Option<PathBuf>,
+        thread_id: ThreadId,
+        status: Option<codex_app_server_protocol::ThreadStatus>,
+    ) -> SessionSelection {
+        let target_session = SessionTarget {
+            path,
+            thread_id,
+            status,
+        };
         match self {
             SessionPickerAction::Resume => SessionSelection::Resume(target_session),
             SessionPickerAction::Fork => SessionSelection::Fork(target_session),
@@ -979,6 +989,7 @@ struct Row {
     path: Option<PathBuf>,
     preview: String,
     thread_id: Option<ThreadId>,
+    status: Option<codex_app_server_protocol::ThreadStatus>,
     thread_name: Option<String>,
     created_at: Option<DateTime<Utc>>,
     updated_at: Option<DateTime<Utc>>,
@@ -1267,7 +1278,11 @@ impl PickerState {
                         },
                     };
                     if let Some(thread_id) = thread_id {
-                        return Ok(Some(self.action.selection(path, thread_id)));
+                        return Ok(Some(self.action.selection(
+                            path,
+                            thread_id,
+                            row.status.clone(),
+                        )));
                     }
                     self.inline_error = Some(match path {
                         Some(path) => {
@@ -1987,6 +2002,7 @@ fn row_from_app_server_thread(thread: Thread) -> Option<Row> {
             preview.to_string()
         },
         thread_id: Some(thread_id),
+        status: Some(thread.status),
         thread_name: thread.name,
         created_at: chrono::DateTime::from_timestamp(thread.created_at, 0)
             .map(|dt| dt.with_timezone(&Utc)),
@@ -3491,6 +3507,7 @@ mod tests {
             path: Some(PathBuf::from(path)),
             preview: preview.to_string(),
             thread_id: None,
+            status: None,
             thread_name: None,
             created_at: Some(timestamp),
             updated_at: Some(timestamp),
@@ -3567,6 +3584,7 @@ mod tests {
             path: Some(PathBuf::from("/tmp/a.jsonl")),
             preview: String::from("first message"),
             thread_id: None,
+            status: None,
             thread_name: Some(String::from("My session")),
             created_at: None,
             updated_at: None,
@@ -3784,6 +3802,7 @@ mod tests {
             path: Some(PathBuf::from("/tmp/a.jsonl")),
             preview: String::from("first message"),
             thread_id: Some(thread_id),
+            status: None,
             thread_name: Some(String::from("My session")),
             created_at: None,
             updated_at: None,
@@ -3844,6 +3863,7 @@ mod tests {
             path: Some(PathBuf::from("/tmp/a.jsonl")),
             preview: String::from("first message"),
             thread_id: Some(thread_id),
+            status: None,
             thread_name: Some(String::from("feat(tui): add raw scrollback mode")),
             created_at: parse_timestamp_str("2026-05-02T14:31:08Z"),
             updated_at: parse_timestamp_str("2026-05-02T14:48:19Z"),
@@ -4054,6 +4074,7 @@ mod tests {
             path: None,
             preview: String::from("remote session"),
             thread_id: Some(ThreadId::new()),
+            status: None,
             thread_name: None,
             created_at: None,
             updated_at: None,
@@ -4079,6 +4100,7 @@ mod tests {
             path: None,
             preview: String::from("remote session"),
             thread_id: Some(ThreadId::new()),
+            status: None,
             thread_name: None,
             created_at: None,
             updated_at: None,
@@ -4110,6 +4132,7 @@ mod tests {
                 path: Some(PathBuf::from("/tmp/a.jsonl")),
                 preview: String::from("Fix resume picker timestamps"),
                 thread_id: None,
+                status: None,
                 thread_name: None,
                 created_at: Some(now - Duration::minutes(16)),
                 updated_at: Some(now - Duration::seconds(42)),
@@ -4120,6 +4143,7 @@ mod tests {
                 path: Some(PathBuf::from("/tmp/b.jsonl")),
                 preview: String::from("Investigate lazy pagination cap"),
                 thread_id: None,
+                status: None,
                 thread_name: None,
                 created_at: Some(now - Duration::hours(1)),
                 updated_at: Some(now - Duration::minutes(35)),
@@ -4130,6 +4154,7 @@ mod tests {
                 path: Some(PathBuf::from("/tmp/c.jsonl")),
                 preview: String::from("Explain the codebase"),
                 thread_id: None,
+                status: None,
                 thread_name: None,
                 created_at: Some(now - Duration::hours(2)),
                 updated_at: Some(now - Duration::hours(2)),
@@ -4558,6 +4583,7 @@ mod tests {
             path: None,
             preview: String::from("preview"),
             thread_id: Some(thread_id),
+            status: None,
             thread_name: None,
             created_at: None,
             updated_at: None,
@@ -4596,6 +4622,7 @@ mod tests {
                 path: None,
                 preview: String::from("one"),
                 thread_id: Some(ThreadId::new()),
+                status: None,
                 thread_name: None,
                 created_at: None,
                 updated_at: None,
@@ -4606,6 +4633,7 @@ mod tests {
                 path: None,
                 preview: String::from("two"),
                 thread_id: Some(ThreadId::new()),
+                status: None,
                 thread_name: None,
                 created_at: None,
                 updated_at: None,
@@ -4675,6 +4703,7 @@ mod tests {
                 path: None,
                 preview: String::from("Find pending threads and emails"),
                 thread_id: Some(thread_id),
+                status: None,
                 thread_name: None,
                 created_at: None,
                 updated_at: None,
@@ -4685,6 +4714,7 @@ mod tests {
                 path: None,
                 preview: String::from("Plan raw scrollback mode"),
                 thread_id: Some(ThreadId::new()),
+                status: None,
                 thread_name: None,
                 created_at: None,
                 updated_at: None,
@@ -4740,6 +4770,7 @@ mod tests {
             path: None,
             preview: String::from("preview"),
             thread_id: Some(thread_id),
+            status: None,
             thread_name: None,
             created_at: None,
             updated_at: None,
@@ -4770,6 +4801,7 @@ mod tests {
             path: Some(PathBuf::from("/tmp/a.jsonl")),
             preview: String::from("preview"),
             thread_id: None,
+            status: None,
             thread_name: None,
             created_at: None,
             updated_at: None,
@@ -4846,6 +4878,7 @@ mod tests {
             path: None,
             preview: String::from("preview"),
             thread_id: Some(thread_id),
+            status: None,
             thread_name: None,
             created_at: None,
             updated_at: None,
@@ -5010,6 +5043,7 @@ session_picker_view = "dense"
             path: None,
             preview: String::from("preview"),
             thread_id: Some(thread_id),
+            status: None,
             thread_name: None,
             created_at: None,
             updated_at: None,
@@ -5049,6 +5083,7 @@ session_picker_view = "dense"
             path: None,
             preview: String::from("preview"),
             thread_id: Some(thread_id),
+            status: None,
             thread_name: None,
             created_at: None,
             updated_at: None,
@@ -5125,6 +5160,7 @@ session_picker_view = "dense"
             thread_id: Some(
                 ThreadId::from_string("019dabc1-0ef5-7431-b81c-03037f51f62c").expect("thread id"),
             ),
+            status: None,
             thread_name: None,
             created_at: parse_timestamp_str("2026-04-28T16:30:00Z"),
             updated_at: parse_timestamp_str("2026-04-28T17:45:00Z"),
@@ -5381,6 +5417,7 @@ session_picker_view = "dense"
             path: Some(PathBuf::from("/tmp/a.jsonl")),
             preview: String::from("Investigate picker expansion"),
             thread_id: Some(thread_id),
+            status: None,
             thread_name: None,
             created_at: parse_timestamp_str("2026-04-28T16:30:00Z"),
             updated_at: parse_timestamp_str("2026-04-28T17:45:00Z"),
@@ -5450,6 +5487,7 @@ session_picker_view = "dense"
             thread_id: Some(
                 ThreadId::from_string("019dabc1-0ef5-7431-b81c-03037f51f62c").expect("thread id"),
             ),
+            status: None,
             thread_name: None,
             created_at: parse_timestamp_str("2026-04-28T16:30:00Z"),
             updated_at: parse_timestamp_str("2026-04-28T17:45:00Z"),
@@ -5508,6 +5546,7 @@ session_picker_view = "dense"
                 path: Some(PathBuf::from(format!("/tmp/{idx}.jsonl"))),
                 preview: format!("item-{idx}"),
                 thread_id: None,
+                status: None,
                 thread_name: None,
                 created_at: Some(now - Duration::hours(idx)),
                 updated_at: Some(now - Duration::minutes(idx * 5)),
@@ -5560,6 +5599,7 @@ session_picker_view = "dense"
                 path: Some(PathBuf::from(format!("/tmp/{idx}.jsonl"))),
                 preview: format!("item-{idx}"),
                 thread_id: None,
+                status: None,
                 thread_name: None,
                 created_at: Some(now - Duration::hours(idx)),
                 updated_at: Some(now - Duration::minutes(idx * 5)),
@@ -6141,6 +6181,7 @@ session_picker_view = "dense"
             path: Some(PathBuf::from("/tmp/missing.jsonl")),
             preview: String::from("missing metadata"),
             thread_id: None,
+            status: None,
             thread_name: None,
             created_at: None,
             updated_at: None,
@@ -6180,6 +6221,9 @@ session_picker_view = "dense"
             path: None,
             preview: String::from("pathless thread"),
             thread_id: Some(thread_id),
+            status: Some(codex_app_server_protocol::ThreadStatus::Active {
+                active_flags: Vec::new(),
+            }),
             thread_name: None,
             created_at: None,
             updated_at: None,
@@ -6198,6 +6242,7 @@ session_picker_view = "dense"
             Some(SessionSelection::Resume(SessionTarget {
                 path: None,
                 thread_id: selected_thread_id,
+                status: Some(codex_app_server_protocol::ThreadStatus::Active { .. }),
             })) => assert_eq!(selected_thread_id, thread_id),
             other => panic!("unexpected selection: {other:?}"),
         }
@@ -6220,7 +6265,9 @@ session_picker_view = "dense"
             created_at: 1,
             updated_at: 2,
             recency_at: Some(2),
-            status: codex_app_server_protocol::ThreadStatus::Idle,
+            status: codex_app_server_protocol::ThreadStatus::Active {
+                active_flags: Vec::new(),
+            },
             path: None,
             cwd: test_path_buf("/tmp").abs(),
             cli_version: String::from("0.0.0"),
@@ -6238,6 +6285,10 @@ session_picker_view = "dense"
 
         assert_eq!(row.path, None);
         assert_eq!(row.thread_id, Some(thread_id));
+        assert!(matches!(
+            row.status,
+            Some(codex_app_server_protocol::ThreadStatus::Active { .. })
+        ));
         assert_eq!(row.thread_name, Some(String::from("Named thread")));
     }
 
diff --git a/codex-rs/tui/src/session_resume.rs b/codex-rs/tui/src/session_resume.rs
index 2e86baed4b..88ae8b62f2 100644
--- a/codex-rs/tui/src/session_resume.rs
+++ b/codex-rs/tui/src/session_resume.rs
@@ -499,6 +499,7 @@ mod tests {
                 &SessionTarget {
                     path: Some(rollout_path.clone()),
                     thread_id,
+                    status: None,
                 },
                 CwdPromptAction::Fork,
                 ResumeCwdContext {
@@ -532,6 +533,7 @@ mod tests {
             &SessionTarget {
                 path: None,
                 thread_id: ThreadId::new(),
+                status: None,
             },
             CwdPromptAction::Resume,
             ResumeCwdContext {
