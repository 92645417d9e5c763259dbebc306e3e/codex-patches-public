diff --git a/codex-rs/core/src/compact_remote_v2.rs b/codex-rs/core/src/compact_remote_v2.rs
index 043fcbb5de..1429163eb5 100644
--- a/codex-rs/core/src/compact_remote_v2.rs
+++ b/codex-rs/core/src/compact_remote_v2.rs
@@ -55,9 +55,6 @@ use attempt::run_remote_compact_v2_attempt;
 // Mirror the current /responses/compact retained-message default while the
 // server-side path remains the reference implementation.
 const RETAINED_MESSAGE_TOKEN_BUDGET: usize = 64_000;
-// Compact attempts can run much longer than normal turns, so keep the per-transport
-// retry budget smaller than the general Responses stream retry budget.
-const MAX_REMOTE_COMPACTION_V2_STREAM_RETRIES: u64 = 2;
 
 pub(crate) async fn run_inline_remote_auto_compact_task(
     sess: Arc<Session>,
@@ -352,11 +349,7 @@ async fn run_remote_compaction_request_v2(
     prompt: &Prompt,
     responses_metadata: &CodexResponsesMetadata,
 ) -> CodexResult<RemoteCompactionV2Output> {
-    let max_retries = turn_context
-        .provider
-        .info()
-        .stream_max_retries()
-        .min(MAX_REMOTE_COMPACTION_V2_STREAM_RETRIES);
+    let max_retries = turn_context.provider.info().stream_max_retries();
     let mut retries = 0;
     loop {
         let result = match client_session
diff --git a/codex-rs/core/src/responses_retry.rs b/codex-rs/core/src/responses_retry.rs
index 1c0a3a2366..4b990f4224 100644
--- a/codex-rs/core/src/responses_retry.rs
+++ b/codex-rs/core/src/responses_retry.rs
@@ -1,11 +1,15 @@
 //! Shared retry and transport fallback decisions for Responses requests.
 
 use std::time::Duration;
+use std::time::SystemTime;
+use std::time::UNIX_EPOCH;
 
 use crate::client::ModelClientSession;
 use crate::session::session::Session;
 use crate::session::turn_context::TurnContext;
 use crate::util::backoff;
+use chrono::DateTime;
+use chrono::Local;
 use codex_protocol::error::CodexErr;
 use codex_protocol::protocol::EventMsg;
 use codex_protocol::protocol::WarningEvent;
@@ -17,6 +21,84 @@ pub(crate) enum ResponsesStreamRequest {
     RemoteCompactionV2,
 }
 
+const STREAM_RETRY_MAX_BACKOFF_DELAY: Duration = Duration::from_secs(30);
+const EXTERNAL_LIMIT_CAPACITY_RETRY_INTERVAL: Duration = Duration::from_secs(5 * 60);
+const EXTERNAL_LIMIT_ACCOUNT_QUOTA_RETRY_INTERVAL: Duration = Duration::from_secs(30 * 60);
+
+#[derive(Debug, Clone, Copy, PartialEq, Eq)]
+pub(crate) enum ExternalLimitRetryKind {
+    Capacity,
+    AccountQuota,
+}
+
+#[derive(Debug, Clone, Copy, PartialEq, Eq)]
+pub(crate) struct ExternalLimitRetry {
+    pub(crate) kind: ExternalLimitRetryKind,
+    pub(crate) delay: Duration,
+    pub(crate) retry_at: SystemTime,
+}
+
+pub(crate) fn external_limit_retry_delay(err: &CodexErr) -> Option<Duration> {
+    external_limit_retry(err).map(|retry| retry.delay)
+}
+
+fn external_limit_retry(err: &CodexErr) -> Option<ExternalLimitRetry> {
+    external_limit_retry_at(err, SystemTime::now())
+}
+
+fn external_limit_retry_at(err: &CodexErr, now: SystemTime) -> Option<ExternalLimitRetry> {
+    let (kind, interval) = match err {
+        CodexErr::ServerOverloaded => (
+            ExternalLimitRetryKind::Capacity,
+            EXTERNAL_LIMIT_CAPACITY_RETRY_INTERVAL,
+        ),
+        CodexErr::QuotaExceeded | CodexErr::UsageLimitReached(_) => (
+            ExternalLimitRetryKind::AccountQuota,
+            EXTERNAL_LIMIT_ACCOUNT_QUOTA_RETRY_INTERVAL,
+        ),
+        _ => return None,
+    };
+
+    let delay = delay_until_next_boundary(now, interval);
+    Some(ExternalLimitRetry {
+        kind,
+        delay,
+        retry_at: now + delay,
+    })
+}
+
+fn delay_until_next_boundary(now: SystemTime, interval: Duration) -> Duration {
+    let Ok(elapsed) = now.duration_since(UNIX_EPOCH) else {
+        return interval;
+    };
+    let interval_ms = interval.as_millis();
+    let elapsed_ms = elapsed.as_millis();
+    let remainder = elapsed_ms % interval_ms;
+    let delay_ms = if remainder == 0 {
+        interval_ms
+    } else {
+        interval_ms - remainder
+    };
+    Duration::from_millis(delay_ms.try_into().unwrap_or(u64::MAX))
+}
+
+fn format_retry_at_local_time(retry_at: SystemTime) -> String {
+    let retry_at: DateTime<Local> = retry_at.into();
+    retry_at.format("%H:%M").to_string()
+}
+
+fn external_limit_retry_warning(retry: ExternalLimitRetry) -> String {
+    let retry_at = format_retry_at_local_time(retry.retry_at);
+    match retry.kind {
+        ExternalLimitRetryKind::Capacity => format!(
+            "External capacity limit reached. Retrying at {retry_at} (next 5-minute boundary)."
+        ),
+        ExternalLimitRetryKind::AccountQuota => format!(
+            "Account quota limit reached. Retrying at {retry_at} (next 30-minute boundary)."
+        ),
+    }
+}
+
 pub(crate) fn sampling_error_is_retryable(
     err: &CodexErr,
     responses_websocket_enabled: bool,
@@ -44,10 +126,37 @@ pub(crate) async fn handle_retryable_response_stream_error(
         turn_context.model_info.automatic_limit_retry,
         request,
         &err,
-    ) {
+    ) || (matches!(request, ResponsesStreamRequest::Sampling)
+        && max_retries == 0
+        && external_limit_retry_delay(&err).is_some())
+    {
         return Err(err);
     }
 
+    if matches!(request, ResponsesStreamRequest::Sampling)
+        && let Some(retry) = retryable_external_limit(
+            turn_context.model_info.automatic_limit_retry,
+            &err,
+            max_retries,
+        )
+    {
+        warn!(
+            error = %err,
+            retry_kind = ?retry.kind,
+            delay = ?retry.delay,
+            "external limit reached; retrying sampling request"
+        );
+        sess.send_event(
+            turn_context,
+            EventMsg::Warning(WarningEvent {
+                message: external_limit_retry_warning(retry),
+            }),
+        )
+        .await;
+        tokio::time::sleep(retry.delay).await;
+        return Ok(());
+    }
+
     if *retries >= max_retries
         && client_session.try_switch_fallback_transport(
             &turn_context.session_telemetry,
@@ -68,12 +177,7 @@ pub(crate) async fn handle_retryable_response_stream_error(
     if *retries < max_retries {
         *retries += 1;
         let retry_count = *retries;
-        let delay = match &err {
-            CodexErr::Stream(_, requested_delay) => {
-                requested_delay.unwrap_or_else(|| backoff(retry_count))
-            }
-            _ => backoff(retry_count),
-        };
+        let delay = stream_retry_delay(&err, retry_count);
         log_retry(request, turn_context, &err, retry_count, max_retries, delay);
 
         // In release builds, hide the first websocket retry notification to reduce noisy
@@ -98,6 +202,25 @@ pub(crate) async fn handle_retryable_response_stream_error(
     Err(err)
 }
 
+fn stream_retry_delay(err: &CodexErr, retry_count: u64) -> Duration {
+    if let CodexErr::Stream(_, Some(requested_delay)) = err {
+        *requested_delay
+    } else {
+        backoff(retry_count).min(STREAM_RETRY_MAX_BACKOFF_DELAY)
+    }
+}
+
+fn retryable_external_limit(
+    automatic_limit_retry: bool,
+    err: &CodexErr,
+    max_retries: u64,
+) -> Option<ExternalLimitRetry> {
+    if !automatic_limit_retry || max_retries == 0 {
+        return None;
+    }
+    external_limit_retry(err)
+}
+
 fn sampling_external_limit_should_surface(
     automatic_limit_retry: bool,
     request: ResponsesStreamRequest,
diff --git a/codex-rs/core/src/responses_retry_tests.rs b/codex-rs/core/src/responses_retry_tests.rs
index 7ed952a5a5..de8043d625 100644
--- a/codex-rs/core/src/responses_retry_tests.rs
+++ b/codex-rs/core/src/responses_retry_tests.rs
@@ -1,12 +1,23 @@
+use super::EXTERNAL_LIMIT_ACCOUNT_QUOTA_RETRY_INTERVAL;
+use super::EXTERNAL_LIMIT_CAPACITY_RETRY_INTERVAL;
+use super::ExternalLimitRetry;
+use super::ExternalLimitRetryKind;
 use super::ResponsesStreamRequest;
+use super::STREAM_RETRY_MAX_BACKOFF_DELAY;
+use super::external_limit_retry_at;
+use super::external_limit_retry_warning;
 use super::log_retry;
+use super::retryable_external_limit;
 use super::sampling_external_limit_should_surface;
+use super::stream_retry_delay;
 use crate::session::tests::make_session_and_context;
 use codex_protocol::error::CodexErr;
 use codex_protocol::error::UnexpectedResponseError;
 use codex_protocol::error::UsageLimitReachedError;
 use http::StatusCode;
 use std::time::Duration;
+use std::time::SystemTime;
+use std::time::UNIX_EPOCH;
 use tracing_test::internal::MockWriter;
 
 #[tokio::test]
@@ -105,3 +116,107 @@ fn sampling_client_errors_retry_only_for_websocket_transport() {
     assert!(super::sampling_error_is_retryable(&error, true));
     assert!(!super::sampling_error_is_retryable(&error, false));
 }
+
+#[test]
+fn stream_retry_backoff_is_bounded_and_preserves_requested_delay() {
+    let automatic = CodexErr::Stream("disconnected".to_string(), None);
+    let delays: Vec<Duration> = (1..=30)
+        .map(|retry_count| stream_retry_delay(&automatic, retry_count))
+        .collect();
+
+    assert!(
+        delays
+            .iter()
+            .all(|delay| *delay <= STREAM_RETRY_MAX_BACKOFF_DELAY)
+    );
+    assert_eq!(delays.last(), Some(&STREAM_RETRY_MAX_BACKOFF_DELAY));
+    let total: Duration = delays.into_iter().sum();
+    assert!(total <= Duration::from_secs(30 * 60));
+
+    let requested = CodexErr::Stream("retry later".to_string(), Some(Duration::from_secs(45)));
+    assert_eq!(stream_retry_delay(&requested, 30), Duration::from_secs(45));
+}
+
+fn usage_limit_reached_error() -> CodexErr {
+    CodexErr::UsageLimitReached(UsageLimitReachedError {
+        plan_type: None,
+        resets_at: None,
+        rate_limits: None,
+        promo_message: None,
+        rate_limit_reached_type: None,
+    })
+}
+
+#[test]
+fn external_limit_errors_align_to_retry_boundaries() {
+    let now = UNIX_EPOCH + Duration::from_secs(6 * 60 * 60 + 21 * 60 + 10);
+
+    assert_eq!(
+        external_limit_retry_at(&CodexErr::ServerOverloaded, now),
+        Some(ExternalLimitRetry {
+            kind: ExternalLimitRetryKind::Capacity,
+            delay: Duration::from_secs(3 * 60 + 50),
+            retry_at: now + Duration::from_secs(3 * 60 + 50),
+        })
+    );
+    for err in [CodexErr::QuotaExceeded, usage_limit_reached_error()] {
+        assert_eq!(
+            external_limit_retry_at(&err, now),
+            Some(ExternalLimitRetry {
+                kind: ExternalLimitRetryKind::AccountQuota,
+                delay: Duration::from_secs(8 * 60 + 50),
+                retry_at: now + Duration::from_secs(8 * 60 + 50),
+            })
+        );
+    }
+    assert_eq!(
+        external_limit_retry_at(&CodexErr::UsageNotIncluded, now),
+        None
+    );
+
+    let boundary = UNIX_EPOCH + Duration::from_secs(6 * 60 * 60 + 30 * 60);
+    assert_eq!(
+        external_limit_retry_at(&CodexErr::ServerOverloaded, boundary)
+            .expect("capacity retry")
+            .delay,
+        EXTERNAL_LIMIT_CAPACITY_RETRY_INTERVAL
+    );
+    assert_eq!(
+        external_limit_retry_at(&CodexErr::QuotaExceeded, boundary)
+            .expect("quota retry")
+            .delay,
+        EXTERNAL_LIMIT_ACCOUNT_QUOTA_RETRY_INTERVAL
+    );
+}
+
+#[test]
+fn external_limit_retry_requires_model_opt_in_and_nonzero_budget() {
+    assert!(retryable_external_limit(true, &CodexErr::QuotaExceeded, 30).is_some());
+    assert_eq!(
+        retryable_external_limit(false, &CodexErr::QuotaExceeded, 30),
+        None
+    );
+    assert_eq!(
+        retryable_external_limit(true, &CodexErr::QuotaExceeded, 0),
+        None
+    );
+}
+
+#[test]
+fn boundary_retry_warning_messages_are_specific() {
+    let capacity = external_limit_retry_warning(ExternalLimitRetry {
+        kind: ExternalLimitRetryKind::Capacity,
+        delay: Duration::from_secs(230),
+        retry_at: SystemTime::now() + Duration::from_secs(230),
+    });
+    assert!(capacity.contains("External capacity limit reached"));
+    assert!(capacity.contains("next 5-minute boundary"));
+
+    let quota = external_limit_retry_warning(ExternalLimitRetry {
+        kind: ExternalLimitRetryKind::AccountQuota,
+        delay: Duration::from_secs(530),
+        retry_at: SystemTime::now() + Duration::from_secs(530),
+    });
+    assert!(quota.contains("Account quota limit reached"));
+    assert!(quota.contains("next 30-minute boundary"));
+}
diff --git a/codex-rs/core/src/session/turn.rs b/codex-rs/core/src/session/turn.rs
index 33425fc0f4..7be444e6dc 100644
--- a/codex-rs/core/src/session/turn.rs
+++ b/codex-rs/core/src/session/turn.rs
@@ -40,6 +40,7 @@ use crate::plugins::build_plugin_injections;
 use crate::responses_metadata::CodexResponsesMetadata;
 use crate::responses_metadata::CodexResponsesRequestKind;
 use crate::responses_retry::ResponsesStreamRequest;
+use crate::responses_retry::external_limit_retry_delay;
 use crate::responses_retry::handle_retryable_response_stream_error;
 use crate::responses_retry::sampling_error_is_retryable;
 use crate::session::McpRuntimeSnapshot;
@@ -1267,7 +1268,7 @@ async fn run_sampling_request(
                 if let Some(rate_limits) = rate_limits {
                     sess.update_rate_limits(&turn_context, *rate_limits).await;
                 }
-                return Err(CodexErr::UsageLimitReached(e));
+                CodexErr::UsageLimitReached(e)
             }
             Err(err) => err,
         };
@@ -1276,10 +1277,14 @@ async fn run_sampling_request(
             original_input = Some(prompt.input);
         }
 
-        if !sampling_error_is_retryable(
-            &err,
-            sess.services.model_client.responses_websocket_enabled(),
-        ) {
+        let automatic_external_limit_retry = turn_context.model_info.automatic_limit_retry
+            && external_limit_retry_delay(&err).is_some();
+        if !automatic_external_limit_retry
+            && !sampling_error_is_retryable(
+                &err,
+                sess.services.model_client.responses_websocket_enabled(),
+            )
+        {
             return Err(err);
         }
 
diff --git a/codex-rs/core/tests/suite/client.rs b/codex-rs/core/tests/suite/client.rs
index dc92bc402a..71b55c0948 100644
--- a/codex-rs/core/tests/suite/client.rs
+++ b/codex-rs/core/tests/suite/client.rs
@@ -53,6 +53,8 @@ use codex_protocol::protocol::RolloutLine;
 use codex_protocol::protocol::SessionMeta;
 use codex_protocol::protocol::SessionMetaLine;
 use codex_protocol::protocol::SessionSource;
+use codex_protocol::protocol::TurnAbortReason;
+use codex_protocol::protocol::WarningEvent;
 use codex_protocol::user_input::UserInput;
 use core_test_support::PathBufExt;
 use core_test_support::TestCodexResponsesRequestKind;
@@ -66,6 +68,7 @@ use core_test_support::responses::ev_function_call;
 use core_test_support::responses::ev_message_item_added;
 use core_test_support::responses::ev_output_text_delta;
 use core_test_support::responses::ev_response_created;
+use core_test_support::responses::mount_response_sequence;
 use core_test_support::responses::mount_sse_once;
 use core_test_support::responses::mount_sse_once_match;
 use core_test_support::responses::mount_sse_sequence;
@@ -3541,7 +3544,9 @@ async fn usage_limit_error_emits_rate_limit_event() -> anyhow::Result<()> {
         .mount(&server)
         .await;
 
-    let mut builder = test_codex();
+    let mut builder = test_codex().with_config(|config| {
+        config.model_provider.stream_max_retries = Some(0);
+    });
     let codex_fixture = builder.build(&server).await?;
     let codex = codex_fixture.codex.clone();
 
@@ -3610,6 +3615,68 @@ async fn usage_limit_error_emits_rate_limit_event() -> anyhow::Result<()> {
     Ok(())
 }
 
+#[tokio::test(flavor = "multi_thread", worker_threads = 2)]
+async fn usage_limit_error_warns_and_waits_for_boundary_until_interrupted() -> anyhow::Result<()> {
+    skip_if_no_network!(Ok(()));
+    let server = MockServer::start().await;
+    let responses_mock = mount_response_sequence(
+        &server,
+        vec![ResponseTemplate::new(429).set_body_json(json!({
+            "error": {
+                "type": "usage_limit_reached",
+                "message": "limit reached",
+                "resets_at": 1704067242,
+                "plan_type": "pro"
+            }
+        }))],
+    )
+    .await;
+
+    let codex = test_codex()
+        .with_model("gpt-5.4")
+        .with_config(|config| {
+            config.model_provider.stream_max_retries = Some(1);
+        })
+        .build(&server)
+        .await?
+        .codex;
+    codex
+        .submit(Op::UserInput {
+            items: vec![UserInput::Text {
+                text: "hello".into(),
+                text_elements: Vec::new(),
+            }],
+            final_output_json_schema: None,
+            responsesapi_client_metadata: None,
+            additional_context: Default::default(),
+            thread_settings: Default::default(),
+        })
+        .await?;
+
+    let warning = wait_for_event(&codex, |msg| {
+        matches!(
+            msg,
+            EventMsg::Warning(WarningEvent { message })
+                if message.contains("next 30-minute boundary")
+        )
+    })
+    .await;
+    let EventMsg::Warning(WarningEvent { message }) = warning else {
+        unreachable!();
+    };
+    assert!(message.contains("Account quota limit reached"));
+
+    codex.submit(Op::Interrupt).await?;
+    let aborted = wait_for_event(&codex, |msg| matches!(msg, EventMsg::TurnAborted(_))).await;
+    let EventMsg::TurnAborted(aborted) = aborted else {
+        unreachable!();
+    };
+    assert_eq!(aborted.reason, TurnAbortReason::Interrupted);
+    assert_eq!(responses_mock.requests().len(), 1);
+
+    Ok(())
+}
+
 #[tokio::test(flavor = "multi_thread", worker_threads = 2)]
 async fn context_window_error_records_model_window_without_synthetic_usage() -> anyhow::Result<()> {
     skip_if_no_network!(Ok(()));
diff --git a/codex-rs/core/tests/suite/compact_remote.rs b/codex-rs/core/tests/suite/compact_remote.rs
index 4e3b8615f9..619a7132b6 100644
--- a/codex-rs/core/tests/suite/compact_remote.rs
+++ b/codex-rs/core/tests/suite/compact_remote.rs
@@ -1054,7 +1054,7 @@ async fn remote_compact_v2_reuses_compaction_trigger_for_followups() -> Result<(
 }
 
 #[tokio::test(flavor = "multi_thread", worker_threads = 2)]
-async fn remote_compact_v2_retries_failures_with_stream_retry_budget() -> Result<()> {
+async fn remote_compact_v2_uses_provider_stream_retry_budget() -> Result<()> {
     skip_if_no_network!(Ok(()));
 
     let harness = TestCodexHarness::with_builder(
@@ -1063,7 +1063,7 @@ async fn remote_compact_v2_retries_failures_with_stream_retry_budget() -> Result
             .with_config(|config| {
                 let _ = config.features.enable(Feature::RemoteCompactionV2);
                 config.model_provider.request_max_retries = Some(0);
-                config.model_provider.stream_max_retries = Some(2);
+                config.model_provider.stream_max_retries = Some(3);
             }),
     )
     .await?;
@@ -1077,6 +1077,7 @@ async fn remote_compact_v2_retries_failures_with_stream_retry_budget() -> Result
                 responses::ev_completed("resp-1"),
             ])),
             ResponseTemplate::new(500).set_body_string("first compact open failed"),
+            ResponseTemplate::new(500).set_body_string("second compact open failed"),
             responses::sse_response(responses::sse(vec![serde_json::json!({
                 "type": "response.output_item.done",
                 "item": {
@@ -1135,12 +1136,12 @@ async fn remote_compact_v2_retries_failures_with_stream_retry_budget() -> Result
 
     let response_requests = responses_mock.requests();
     assert_eq!(
-        5,
+        6,
         response_requests.len(),
-        "expected initial turn, failed open, failed stream, compact retry, and follow-up turn"
+        "expected initial turn, three compact failures, compact success, and follow-up turn"
     );
 
-    for compact_request in &response_requests[1..=3] {
+    for compact_request in &response_requests[1..=4] {
         assert_eq!("/v1/responses", compact_request.path());
         assert!(
             compact_request
diff --git a/codex-rs/model-provider-info/src/lib.rs b/codex-rs/model-provider-info/src/lib.rs
index 4a335b5633..1d9140e9af 100644
--- a/codex-rs/model-provider-info/src/lib.rs
+++ b/codex-rs/model-provider-info/src/lib.rs
@@ -24,7 +24,7 @@ use std::fmt;
 use std::time::Duration;
 
 const DEFAULT_STREAM_IDLE_TIMEOUT_MS: u64 = 300_000;
-const DEFAULT_STREAM_MAX_RETRIES: u64 = 5;
+const DEFAULT_STREAM_MAX_RETRIES: u64 = 30;
 const DEFAULT_REQUEST_MAX_RETRIES: u64 = 4;
 pub const DEFAULT_WEBSOCKET_CONNECT_TIMEOUT_MS: u64 = 15_000;
 /// Hard cap for user-configured `stream_max_retries`.
diff --git a/codex-rs/model-provider-info/src/model_provider_info_tests.rs b/codex-rs/model-provider-info/src/model_provider_info_tests.rs
index 456fc84c20..49421143da 100644
--- a/codex-rs/model-provider-info/src/model_provider_info_tests.rs
+++ b/codex-rs/model-provider-info/src/model_provider_info_tests.rs
@@ -170,6 +170,17 @@ fn custom_openai_base_url_requires_explicit_websocket_opt_in() {
     assert!(!custom.supports_websockets);
 }
 
+#[test]
+fn default_stream_retry_budget_is_thirty_retries() {
+    let provider = ModelProviderInfo::default();
+
+    assert_eq!(
+        provider.stream_max_retries(),
+        30,
+        "30 retries allow 31 total stream attempts"
+    );
+}
+
 #[test]
 fn only_openai_disables_websocket_http_fallback() {
     let openai = ModelProviderInfo::create_openai_provider(/*base_url*/ None);
