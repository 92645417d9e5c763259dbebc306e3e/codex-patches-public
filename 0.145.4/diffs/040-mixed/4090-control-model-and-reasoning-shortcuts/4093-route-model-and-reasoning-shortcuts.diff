diff --git a/codex-rs/tui/src/chatwidget.rs b/codex-rs/tui/src/chatwidget.rs
index f9f8bde1bd..402295b3ef 100644
--- a/codex-rs/tui/src/chatwidget.rs
+++ b/codex-rs/tui/src/chatwidget.rs
@@ -378,6 +378,7 @@ use self::plugins::PluginsCacheState;
 mod plan_implementation;
 use self::plan_implementation::PLAN_IMPLEMENTATION_TITLE;
 mod model_popups;
+mod model_shortcuts;
 mod notifications;
 use self::notifications::Notification;
 mod permission_popups;
diff --git a/codex-rs/tui/src/chatwidget/interaction.rs b/codex-rs/tui/src/chatwidget/interaction.rs
index 8699cdd452..638719f879 100644
--- a/codex-rs/tui/src/chatwidget/interaction.rs
+++ b/codex-rs/tui/src/chatwidget/interaction.rs
@@ -45,6 +45,13 @@ impl ChatWidget {
             return;
         }
 
+        if self.handle_model_shortcut(key_event) {
+            self.bottom_pane.clear_quit_shortcut_hint();
+            self.quit_shortcut_expires_at = None;
+            self.quit_shortcut_key = None;
+            return;
+        }
+
         if key_event.kind == KeyEventKind::Press
             && self.copy_last_response_binding.is_pressed(key_event)
         {
diff --git a/codex-rs/tui/src/chatwidget/model_popups.rs b/codex-rs/tui/src/chatwidget/model_popups.rs
index 4e8ac736ee..fec2657a0d 100644
--- a/codex-rs/tui/src/chatwidget/model_popups.rs
+++ b/codex-rs/tui/src/chatwidget/model_popups.rs
@@ -173,11 +173,11 @@ impl ChatWidget {
         });
     }
 
-    fn is_auto_model(model: &str) -> bool {
+    pub(super) fn is_auto_model(model: &str) -> bool {
         model.starts_with("codex-auto-")
     }
 
-    fn auto_model_order(model: &str) -> usize {
+    pub(super) fn auto_model_order(model: &str) -> usize {
         match model {
             "codex-auto-fast" => 0,
             "codex-auto-balanced" => 1,
diff --git a/codex-rs/tui/src/chatwidget/model_shortcuts.rs b/codex-rs/tui/src/chatwidget/model_shortcuts.rs
new file mode 100644
index 0000000000..0e0658b59a
--- /dev/null
+++ b/codex-rs/tui/src/chatwidget/model_shortcuts.rs
@@ -0,0 +1,99 @@
+//! Keyboard shortcuts for stepping through model picker choices.
+
+use codex_protocol::openai_models::ModelPreset;
+use crossterm::event::KeyEvent;
+
+use super::ChatWidget;
+use super::PARENT_OWNED_INPUT_MESSAGE;
+use crate::app_event::AppEvent;
+use crate::key_hint::KeyBindingListExt;
+
+#[derive(Clone, Copy, Debug, Eq, PartialEq)]
+enum ModelShortcutDirection {
+    Previous,
+    Next,
+}
+
+impl ChatWidget {
+    pub(super) fn handle_model_shortcut(&mut self, key_event: KeyEvent) -> bool {
+        let direction = if self.chat_keymap.previous_model.is_pressed(key_event) {
+            ModelShortcutDirection::Previous
+        } else if self.chat_keymap.next_model.is_pressed(key_event) {
+            ModelShortcutDirection::Next
+        } else {
+            return false;
+        };
+
+        if !self.bottom_pane.no_modal_or_popup_active() {
+            return false;
+        }
+
+        if self.blocks_direct_input {
+            self.add_error_message(PARENT_OWNED_INPUT_MESSAGE.to_string());
+            return true;
+        }
+
+        if self.bottom_pane.is_task_running() {
+            self.add_to_history(crate::history_cell::new_error_event(
+                "Model shortcuts are disabled while a task is in progress.".to_string(),
+            ));
+            self.request_redraw();
+            return true;
+        }
+
+        if !self.is_session_configured() {
+            return true;
+        }
+
+        let choices = self.model_shortcut_choices();
+        let Some(next_preset) = next_model_preset(&choices, self.current_model(), direction) else {
+            return true;
+        };
+
+        self.apply_model_and_effort_without_persist(
+            next_preset.model.clone(),
+            Some(next_preset.default_reasoning_effort.clone()),
+        );
+        self.app_event_tx
+            .send(AppEvent::PersistModelSelectionSilently {
+                model: next_preset.model,
+                effort: Some(next_preset.default_reasoning_effort),
+            });
+
+        true
+    }
+
+    fn model_shortcut_choices(&self) -> Vec<ModelPreset> {
+        let presets = match self.model_catalog.try_list_models() {
+            Ok(presets) => presets,
+            Err(err) => match err {},
+        };
+        let (mut auto_presets, other_presets): (Vec<ModelPreset>, Vec<ModelPreset>) = presets
+            .into_iter()
+            .filter(|preset| preset.show_in_picker)
+            .partition(|preset| Self::is_auto_model(&preset.model));
+        auto_presets.sort_by_key(|preset| Self::auto_model_order(&preset.model));
+        auto_presets.into_iter().chain(other_presets).collect()
+    }
+}
+
+fn next_model_preset(
+    choices: &[ModelPreset],
+    current_model: &str,
+    direction: ModelShortcutDirection,
+) -> Option<ModelPreset> {
+    if choices.is_empty() {
+        return None;
+    }
+
+    let current_idx = choices
+        .iter()
+        .position(|preset| preset.model.as_str() == current_model);
+    let next_idx = match (direction, current_idx) {
+        (ModelShortcutDirection::Previous, Some(0) | None) => choices.len() - 1,
+        (ModelShortcutDirection::Previous, Some(idx)) => idx - 1,
+        (ModelShortcutDirection::Next, Some(idx)) => (idx + 1) % choices.len(),
+        (ModelShortcutDirection::Next, None) => 0,
+    };
+    choices.get(next_idx).cloned()
+}
diff --git a/codex-rs/tui/src/chatwidget/reasoning_shortcuts.rs b/codex-rs/tui/src/chatwidget/reasoning_shortcuts.rs
index 7ef88ac73f..af5c14fc8e 100644
--- a/codex-rs/tui/src/chatwidget/reasoning_shortcuts.rs
+++ b/codex-rs/tui/src/chatwidget/reasoning_shortcuts.rs
@@ -49,9 +49,9 @@ impl ChatWidget {
     /// input flow, which is important while a popup or modal has focus.
     ///
     /// Callers should route recognized shortcuts through this method rather than
-    /// directly mutating reasoning state. It applies normal-mode changes without
-    /// persisting them. In Plan mode, shortcuts apply only to the active
-    /// Plan-mode override and skip the global-vs-Plan scope prompt.
+    /// directly mutating reasoning state. It persists shortcut changes
+    /// immediately. In Plan mode, shortcuts apply only to the active Plan-mode
+    /// override and skip the global-vs-Plan scope prompt.
     pub(super) fn handle_reasoning_shortcut(&mut self, key_event: KeyEvent) -> bool {
         let direction = if self
             .chat_keymap
@@ -78,6 +78,14 @@ impl ChatWidget {
             return true;
         }
 
+        if self.bottom_pane.is_task_running() {
+            self.add_to_history(crate::history_cell::new_error_event(
+                "Reasoning shortcuts are disabled while a task is in progress.".to_string(),
+            ));
+            self.request_redraw();
+            return true;
+        }
+
         if !self.is_session_configured() {
             self.add_info_message(
                 "Reasoning shortcuts are disabled until startup completes.".to_string(),
@@ -147,7 +155,11 @@ impl ChatWidget {
         if self.collaboration_modes_enabled() && self.active_mode_kind() == ModeKind::Plan {
             let warning = self.ultra_reasoning_concurrency_warning(&next_effort);
             self.app_event_tx
-                .send(AppEvent::UpdatePlanModeReasoningEffort(Some(next_effort)));
+                .send(AppEvent::UpdatePlanModeReasoningEffort(Some(
+                    next_effort.clone(),
+                )));
+            self.app_event_tx
+                .send(AppEvent::PersistPlanModeReasoningEffort(Some(next_effort)));
             if let Some(warning) = warning {
                 self.app_event_tx.send(AppEvent::InsertHistoryCell(Box::new(
                     crate::history_cell::new_warning_event(warning),
@@ -155,7 +167,12 @@ impl ChatWidget {
             }
         } else {
             self.app_event_tx
-                .send(AppEvent::UpdateReasoningEffort(Some(next_effort)));
+                .send(AppEvent::UpdateReasoningEffort(Some(next_effort.clone())));
+            self.app_event_tx
+                .send(AppEvent::PersistModelSelectionSilently {
+                    model: current_model,
+                    effort: Some(next_effort),
+                });
         }
 
         true
diff --git a/codex-rs/tui/src/chatwidget/settings.rs b/codex-rs/tui/src/chatwidget/settings.rs
index 1e4a9000a6..ffeac60668 100644
--- a/codex-rs/tui/src/chatwidget/settings.rs
+++ b/codex-rs/tui/src/chatwidget/settings.rs
@@ -4,6 +4,12 @@ use super::*;
 use crate::app_event::AppEvent;
 use crate::chatwidget::rate_limits::RATE_LIMIT_SWITCH_PROMPT_VIEW_ID;
 
+#[derive(Clone, Copy)]
+enum CollaborationModeChangeNotice {
+    Show,
+    Suppress,
+}
+
 impl ChatWidget {
     pub(crate) fn set_app_server_active_client_count(&mut self, count: usize) {
         self.app_server_active_client_count = Some(count);
@@ -687,7 +693,8 @@ impl ChatWidget {
             self.model_catalog.as_ref(),
             self.active_collaboration_mask.as_ref(),
         ) {
-            self.set_collaboration_mask_from_user_action(next_mask);
+            self.set_collaboration_mask_inner(next_mask, CollaborationModeChangeNotice::Suppress);
+            self.submit_collaboration_mode_settings_update();
         }
     }
 
@@ -700,7 +707,15 @@ impl ChatWidget {
     ///
     /// When collaboration modes are enabled and a preset is selected,
     /// the current mode is attached to submissions as `Op::UserTurn { collaboration_mode: Some(...) }`.
-    pub(crate) fn set_collaboration_mask(&mut self, mut mask: CollaborationModeMask) {
+    pub(crate) fn set_collaboration_mask(&mut self, mask: CollaborationModeMask) {
+        self.set_collaboration_mask_inner(mask, CollaborationModeChangeNotice::Show);
+    }
+
+    fn set_collaboration_mask_inner(
+        &mut self,
+        mut mask: CollaborationModeMask,
+        notice: CollaborationModeChangeNotice,
+    ) {
         if !self.collaboration_modes_enabled() {
             return;
         }
@@ -725,6 +740,7 @@ impl ChatWidget {
         let next_effort = self.effective_reasoning_effort();
         if previous_mode != next_mode
             && (previous_model != next_model || previous_effort != next_effort)
+            && matches!(notice, CollaborationModeChangeNotice::Show)
         {
             let mut message = format!("Model changed to {next_model}");
             if !next_model.starts_with("codex-auto-") {
diff --git a/codex-rs/tui/src/chatwidget/tests/helpers.rs b/codex-rs/tui/src/chatwidget/tests/helpers.rs
index db81556119..2311429ec9 100644
--- a/codex-rs/tui/src/chatwidget/tests/helpers.rs
+++ b/codex-rs/tui/src/chatwidget/tests/helpers.rs
@@ -342,6 +342,22 @@ pub(super) fn drain_insert_history(
     out
 }
 
+pub(super) fn drain_insert_history_from_events(
+    events: &[AppEvent],
+) -> Vec<Vec<ratatui::text::Line<'static>>> {
+    let mut out = Vec::new();
+    for event in events {
+        if let AppEvent::InsertHistoryCell(cell) = event {
+            let mut lines = cell.display_lines(/*width*/ 80);
+            if !cell.is_stream_continuation() && !out.is_empty() && !lines.is_empty() {
+                lines.insert(0, "".into());
+            }
+            out.push(lines);
+        }
+    }
+    out
+}
+
 pub(super) fn lines_to_single_string(lines: &[ratatui::text::Line<'static>]) -> String {
     let mut s = String::new();
     for line in lines {
diff --git a/codex-rs/tui/src/chatwidget/tests/plan_mode.rs b/codex-rs/tui/src/chatwidget/tests/plan_mode.rs
index a41f2cb799..527da07b8c 100644
--- a/codex-rs/tui/src/chatwidget/tests/plan_mode.rs
+++ b/codex-rs/tui/src/chatwidget/tests/plan_mode.rs
@@ -389,7 +389,7 @@ async fn reasoning_selection_in_plan_mode_matching_plan_effort_but_different_glo
 }
 
 #[tokio::test]
-async fn reasoning_shortcut_in_plan_mode_updates_plan_override_without_prompt_or_persist() {
+async fn reasoning_shortcut_in_plan_mode_updates_and_persists_plan_override_without_prompt() {
     let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(Some("gpt-5.4")).await;
     chat.thread_id = Some(ThreadId::new());
     chat.set_feature_enabled(Feature::CollaborationModes, /*enabled*/ true);
@@ -416,15 +416,17 @@ async fn reasoning_shortcut_in_plan_mode_updates_plan_override_without_prompt_or
         "expected no Plan reasoning scope prompt event; events: {events:?}"
     );
     assert!(
-        events
-            .iter()
-            .all(|event| !matches!(event, AppEvent::PersistPlanModeReasoningEffort(_))),
-        "expected no Plan reasoning persistence event; events: {events:?}"
+        events.iter().any(|event| matches!(
+            event,
+            AppEvent::PersistPlanModeReasoningEffort(Some(ReasoningEffortConfig::High))
+        )),
+        "expected Plan reasoning persistence event; events: {events:?}"
     );
     assert!(
-        events
-            .iter()
-            .all(|event| !matches!(event, AppEvent::PersistModelSelection { .. })),
+        events.iter().all(|event| !matches!(
+            event,
+            AppEvent::PersistModelSelection { .. } | AppEvent::PersistModelSelectionSilently { .. }
+        )),
         "expected no global model persistence event; events: {events:?}"
     );
     assert!(
@@ -468,6 +470,7 @@ async fn advanced_reasoning_selection_in_plan_mode_uses_expected_scope() {
                 AppEvent::OpenPlanReasoningScopePrompt { .. }
                     | AppEvent::PersistPlanModeReasoningEffort(_)
                     | AppEvent::PersistModelSelection { .. }
+                    | AppEvent::PersistModelSelectionSilently { .. }
             )));
         } else {
             assert!(events.iter().any(|event| matches!(
@@ -1372,16 +1375,18 @@ async fn enter_submits_when_plan_stream_is_not_active() {
 
 #[tokio::test]
 async fn collab_mode_shift_tab_cycles_only_when_idle() {
-    let (mut chat, _rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+    let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
 
     let initial = chat.current_collaboration_mode().clone();
     chat.handle_key_event(KeyEvent::from(KeyCode::BackTab));
     assert_eq!(chat.active_collaboration_mode_kind(), ModeKind::Plan);
     assert_eq!(chat.current_collaboration_mode(), &initial);
+    assert!(drain_insert_history(&mut rx).is_empty());
 
     chat.handle_key_event(KeyEvent::from(KeyCode::BackTab));
     assert_eq!(chat.active_collaboration_mode_kind(), ModeKind::Default);
     assert_eq!(chat.current_collaboration_mode(), &initial);
+    assert!(drain_insert_history(&mut rx).is_empty());
 
     chat.on_task_started();
     let before = chat.active_collaboration_mode_kind();
diff --git a/codex-rs/tui/src/chatwidget/tests/popups_and_settings.rs b/codex-rs/tui/src/chatwidget/tests/popups_and_settings.rs
index ac5aa2ef86..72c11b2970 100644
--- a/codex-rs/tui/src/chatwidget/tests/popups_and_settings.rs
+++ b/codex-rs/tui/src/chatwidget/tests/popups_and_settings.rs
@@ -3413,11 +3413,16 @@ async fn assert_reasoning_shortcuts_update_effort(
             "expected reasoning update event for {key_event:?}; events: {events:?}"
         );
         assert!(
-            events
-                .iter()
-                .all(|event| !matches!(event, AppEvent::PersistModelSelection { .. })),
-            "expected no model persistence event for {key_event:?}; events: {events:?}"
+            events.iter().any(|event| matches!(
+                event,
+                AppEvent::PersistModelSelectionSilently {
+                    model,
+                    effort: Some(effort),
+                } if model == "gpt-5.4" && effort == &expected_effort
+            )),
+            "expected silent model persistence event for {key_event:?}; events: {events:?}"
         );
+        assert!(drain_insert_history_from_events(&events).is_empty());
     }
 }
 
@@ -3483,9 +3488,10 @@ async fn reasoning_shortcut_is_ignored_with_model_popup_open() {
         "did not expect reasoning update while popup is active; events: {events:?}"
     );
     assert!(
-        events
-            .iter()
-            .all(|event| !matches!(event, AppEvent::PersistModelSelection { .. })),
+        events.iter().all(|event| !matches!(
+            event,
+            AppEvent::PersistModelSelection { .. } | AppEvent::PersistModelSelectionSilently { .. }
+        )),
         "did not expect model persistence while popup is active; events: {events:?}"
     );
 }
@@ -3572,14 +3578,150 @@ async fn reasoning_down_shortcut_can_leave_advanced_effort() {
             event,
             AppEvent::UpdateReasoningEffort(Some(effort)) if effort == &expected
         )));
-        assert!(
-            events
-                .iter()
-                .all(|event| !matches!(event, AppEvent::PersistModelSelection { .. }))
-        );
+        assert!(events.iter().any(|event| matches!(
+            event,
+            AppEvent::PersistModelSelectionSilently {
+                model,
+                effort: Some(effort),
+            } if model == "gpt-5.4" && effort == &expected
+        )));
+        assert!(drain_insert_history_from_events(&events).is_empty());
     }
 }
 
+#[tokio::test]
+async fn reasoning_shortcut_is_disabled_while_task_is_running() {
+    let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(Some("gpt-5.4")).await;
+    chat.thread_id = Some(ThreadId::new());
+    chat.set_reasoning_effort(Some(ReasoningEffortConfig::Medium));
+    chat.bottom_pane.set_task_running(/*running*/ true);
+
+    chat.handle_key_event(KeyEvent::new(KeyCode::Char('.'), KeyModifiers::ALT));
+
+    let events = std::iter::from_fn(|| rx.try_recv().ok()).collect::<Vec<_>>();
+    assert!(events.iter().all(|event| !matches!(
+        event,
+        AppEvent::UpdateReasoningEffort(_)
+            | AppEvent::PersistModelSelection { .. }
+            | AppEvent::PersistModelSelectionSilently { .. }
+    )));
+    let rendered = drain_insert_history_from_events(&events)
+        .into_iter()
+        .map(|lines| lines_to_single_string(&lines))
+        .collect::<Vec<_>>()
+        .join("\n");
+    assert!(rendered.contains("Reasoning shortcuts are disabled while a task is in progress."));
+}
+
+#[tokio::test]
+async fn alt_apostrophe_selects_next_model_without_submitting_prompt() {
+    let (mut chat, mut rx, mut op_rx) = make_chatwidget_manual(Some("gpt-5.4")).await;
+    chat.thread_id = Some(ThreadId::new());
+    set_fast_mode_test_catalog(&mut chat);
+
+    chat.handle_key_event(KeyEvent::new(KeyCode::Char('\''), KeyModifiers::ALT));
+
+    let events = std::iter::from_fn(|| rx.try_recv().ok()).collect::<Vec<_>>();
+    assert!(
+        events
+            .iter()
+            .any(|event| matches!(event, AppEvent::UpdateModel(model) if model == "gpt-5.2")),
+        "expected model update event; events: {events:?}"
+    );
+    assert!(
+        events.iter().any(|event| matches!(
+            event,
+            AppEvent::UpdateReasoningEffort(Some(ReasoningEffortConfig::Medium))
+        )),
+        "expected reasoning update event; events: {events:?}"
+    );
+    assert!(
+        events.iter().any(|event| matches!(
+            event,
+            AppEvent::PersistModelSelectionSilently {
+                model,
+                effort: Some(ReasoningEffortConfig::Medium),
+            } if model == "gpt-5.2"
+        )),
+        "expected model persistence event; events: {events:?}"
+    );
+    assert!(drain_insert_history_from_events(&events).is_empty());
+    assert_no_submit_op(&mut op_rx);
+}
+
+#[tokio::test]
+async fn model_shortcut_is_disabled_while_task_is_running() {
+    let (mut chat, mut rx, mut op_rx) = make_chatwidget_manual(Some("gpt-5.4")).await;
+    chat.thread_id = Some(ThreadId::new());
+    set_fast_mode_test_catalog(&mut chat);
+    chat.bottom_pane.set_task_running(/*running*/ true);
+
+    chat.handle_key_event(KeyEvent::new(KeyCode::Char('\''), KeyModifiers::ALT));
+
+    let events = std::iter::from_fn(|| rx.try_recv().ok()).collect::<Vec<_>>();
+    assert!(events.iter().all(|event| !matches!(
+        event,
+        AppEvent::UpdateModel(_)
+            | AppEvent::UpdateReasoningEffort(_)
+            | AppEvent::PersistModelSelection { .. }
+            | AppEvent::PersistModelSelectionSilently { .. }
+    )));
+    let rendered = drain_insert_history_from_events(&events)
+        .into_iter()
+        .map(|lines| lines_to_single_string(&lines))
+        .collect::<Vec<_>>()
+        .join("\n");
+    assert!(rendered.contains("Model shortcuts are disabled while a task is in progress."));
+    assert_no_submit_op(&mut op_rx);
+}
+
+#[tokio::test]
+async fn model_shortcut_is_ignored_with_model_popup_open() {
+    let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(Some("gpt-5.4")).await;
+    chat.thread_id = Some(ThreadId::new());
+    set_fast_mode_test_catalog(&mut chat);
+    chat.open_model_popup();
+
+    chat.handle_key_event(KeyEvent::new(KeyCode::Char('\''), KeyModifiers::ALT));
+
+    let events = std::iter::from_fn(|| rx.try_recv().ok()).collect::<Vec<_>>();
+    assert!(events.iter().all(|event| !matches!(
+        event,
+        AppEvent::UpdateModel(_)
+            | AppEvent::UpdateReasoningEffort(_)
+            | AppEvent::PersistModelSelection { .. }
+            | AppEvent::PersistModelSelectionSilently { .. }
+    )));
+}
+
+#[tokio::test]
+async fn alt_semicolon_selects_previous_model_with_wraparound() {
+    let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(Some("gpt-5.4")).await;
+    chat.thread_id = Some(ThreadId::new());
+    set_fast_mode_test_catalog(&mut chat);
+
+    chat.handle_key_event(KeyEvent::new(KeyCode::Char(';'), KeyModifiers::ALT));
+
+    let events = std::iter::from_fn(|| rx.try_recv().ok()).collect::<Vec<_>>();
+    assert!(
+        events
+            .iter()
+            .any(|event| matches!(event, AppEvent::UpdateModel(model) if model == "gpt-5.2")),
+        "expected model update event; events: {events:?}"
+    );
+    assert!(
+        events.iter().any(|event| matches!(
+            event,
+            AppEvent::PersistModelSelectionSilently {
+                model,
+                effort: Some(ReasoningEffortConfig::Medium),
+            } if model == "gpt-5.2"
+        )),
+        "expected model persistence event; events: {events:?}"
+    );
+    assert!(drain_insert_history_from_events(&events).is_empty());
+}
+
 #[tokio::test]
 async fn reasoning_popup_shows_extra_high_with_space() {
     let (mut chat, _rx, _op_rx) = make_chatwidget_manual(Some("gpt-5.4")).await;
@@ -3667,6 +3809,7 @@ async fn advanced_only_reasoning_option_requires_explicit_selection() {
         AppEvent::UpdateReasoningEffort(_)
             | AppEvent::ApplyAdvancedReasoning { .. }
             | AppEvent::PersistModelSelection { .. }
+            | AppEvent::PersistModelSelectionSilently { .. }
     )));
 }
 
@@ -3694,6 +3837,7 @@ async fn auto_model_advertising_advanced_effort_opens_reasoning_picker() {
         AppEvent::UpdateReasoningEffort(_)
             | AppEvent::ApplyAdvancedReasoning { .. }
             | AppEvent::PersistModelSelection { .. }
+            | AppEvent::PersistModelSelectionSilently { .. }
     )));
     assert!(
         events
diff --git a/codex-rs/tui/src/keymap.rs b/codex-rs/tui/src/keymap.rs
index d8ecd14fad..e1159f98e1 100644
--- a/codex-rs/tui/src/keymap.rs
+++ b/codex-rs/tui/src/keymap.rs
@@ -86,6 +86,10 @@ pub(crate) struct ChatKeymap {
     pub(crate) decrease_reasoning_effort: Vec<KeyBinding>,
     /// Increase the active reasoning effort.
     pub(crate) increase_reasoning_effort: Vec<KeyBinding>,
+    /// Select the previous model in model picker order.
+    pub(crate) previous_model: Vec<KeyBinding>,
+    /// Select the next model in model picker order.
+    pub(crate) next_model: Vec<KeyBinding>,
     /// Edit the most recently queued message.
     pub(crate) edit_queued_message: Vec<KeyBinding>,
 }
@@ -440,6 +444,16 @@ impl RuntimeKeymap {
                 &defaults.chat.increase_reasoning_effort,
                 "tui.keymap.chat.increase_reasoning_effort",
             )?,
+            previous_model: resolve_bindings(
+                keymap.chat.previous_model.as_ref(),
+                &defaults.chat.previous_model,
+                "tui.keymap.chat.previous_model",
+            )?,
+            next_model: resolve_bindings(
+                keymap.chat.next_model.as_ref(),
+                &defaults.chat.next_model,
+                "tui.keymap.chat.next_model",
+            )?,
             edit_queued_message: resolve_bindings(
                 keymap.chat.edit_queued_message.as_ref(),
                 &defaults.chat.edit_queued_message,
@@ -927,6 +941,8 @@ impl RuntimeKeymap {
                     alt(KeyCode::Char('.')),
                     shift(KeyCode::Up)
                 ],
+                previous_model: default_bindings![alt(KeyCode::Char(';'))],
+                next_model: default_bindings![alt(KeyCode::Char('\''))],
                 edit_queued_message: default_bindings![alt(KeyCode::Up), shift(KeyCode::Left)],
             },
             composer: ComposerKeymap {
@@ -1184,6 +1200,8 @@ impl RuntimeKeymap {
                     "chat.increase_reasoning_effort",
                     self.chat.increase_reasoning_effort.as_slice(),
                 ),
+                ("chat.previous_model", self.chat.previous_model.as_slice()),
+                ("chat.next_model", self.chat.next_model.as_slice()),
                 (
                     "chat.edit_queued_message",
                     self.chat.edit_queued_message.as_slice(),
@@ -1227,6 +1245,8 @@ impl RuntimeKeymap {
                     "chat.increase_reasoning_effort",
                     self.chat.increase_reasoning_effort.as_slice(),
                 ),
+                ("chat.previous_model", self.chat.previous_model.as_slice()),
+                ("chat.next_model", self.chat.next_model.as_slice()),
                 (
                     "chat.edit_queued_message",
                     self.chat.edit_queued_message.as_slice(),
@@ -1337,6 +1357,8 @@ impl RuntimeKeymap {
                     "chat.increase_reasoning_effort",
                     self.chat.increase_reasoning_effort.as_slice(),
                 ),
+                ("chat.previous_model", self.chat.previous_model.as_slice()),
+                ("chat.next_model", self.chat.next_model.as_slice()),
                 ("composer.submit", self.composer.submit.as_slice()),
                 ("toggle_vim_mode", self.app.toggle_vim_mode.as_slice()),
                 ("toggle_fast_mode", self.app.toggle_fast_mode.as_slice()),
@@ -2224,6 +2246,14 @@ mod tests {
                 key_hint::shift(KeyCode::Up),
             ]
         );
+        assert_eq!(
+            runtime.chat.previous_model,
+            vec![key_hint::alt(KeyCode::Char(';'))]
+        );
+        assert_eq!(
+            runtime.chat.next_model,
+            vec![key_hint::alt(KeyCode::Char('\''))]
+        );
         assert_eq!(
             runtime.chat.edit_queued_message,
             vec![key_hint::alt(KeyCode::Up), key_hint::shift(KeyCode::Left)]
diff --git a/codex-rs/tui/src/keymap_setup/actions.rs b/codex-rs/tui/src/keymap_setup/actions.rs
index b355cda69f..84618ea35e 100644
--- a/codex-rs/tui/src/keymap_setup/actions.rs
+++ b/codex-rs/tui/src/keymap_setup/actions.rs
@@ -96,6 +96,8 @@ pub(super) const KEYMAP_ACTIONS: &[KeymapActionDescriptor] = &[
     action("chat", "Chat", "interrupt_turn", "Interrupt the active turn."),
     action("chat", "Chat", "decrease_reasoning_effort", "Decrease reasoning effort."),
     action("chat", "Chat", "increase_reasoning_effort", "Increase reasoning effort."),
+    action("chat", "Chat", "previous_model", "Select the previous model."),
+    action("chat", "Chat", "next_model", "Select the next model."),
     action("chat", "Chat", "edit_queued_message", "Edit the most recently queued message."),
     action("composer", "Composer", "submit", "Submit the current composer draft."),
     action("composer", "Composer", "queue", "Queue the draft while a task is running."),
@@ -239,6 +241,8 @@ pub(super) fn binding_slot<'a>(
         ("chat", "interrupt_turn") => Some(&mut keymap.chat.interrupt_turn),
         ("chat", "decrease_reasoning_effort") => Some(&mut keymap.chat.decrease_reasoning_effort),
         ("chat", "increase_reasoning_effort") => Some(&mut keymap.chat.increase_reasoning_effort),
+        ("chat", "previous_model") => Some(&mut keymap.chat.previous_model),
+        ("chat", "next_model") => Some(&mut keymap.chat.next_model),
         ("chat", "edit_queued_message") => Some(&mut keymap.chat.edit_queued_message),
         ("composer", "submit") => Some(&mut keymap.composer.submit),
         ("composer", "queue") => Some(&mut keymap.composer.queue),
@@ -364,6 +368,8 @@ pub(super) fn bindings_for_action<'a>(
         ("chat", "interrupt_turn") => Some(runtime_keymap.chat.interrupt_turn.as_slice()),
         ("chat", "decrease_reasoning_effort") => Some(runtime_keymap.chat.decrease_reasoning_effort.as_slice()),
         ("chat", "increase_reasoning_effort") => Some(runtime_keymap.chat.increase_reasoning_effort.as_slice()),
+        ("chat", "previous_model") => Some(runtime_keymap.chat.previous_model.as_slice()),
+        ("chat", "next_model") => Some(runtime_keymap.chat.next_model.as_slice()),
         ("chat", "edit_queued_message") => Some(runtime_keymap.chat.edit_queued_message.as_slice()),
         ("composer", "submit") => Some(runtime_keymap.composer.submit.as_slice()),
         ("composer", "queue") => Some(runtime_keymap.composer.queue.as_slice()),
diff --git a/codex-rs/tui/src/snapshots/codex_tui__keymap_setup__tests__keymap_picker_all_tab_search.snap b/codex-rs/tui/src/snapshots/codex_tui__keymap_setup__tests__keymap_picker_all_tab_search.snap
index 454f2adb6a..27ee2808e2 100644
--- a/codex-rs/tui/src/snapshots/codex_tui__keymap_setup__tests__keymap_picker_all_tab_search.snap
+++ b/codex-rs/tui/src/snapshots/codex_tui__keymap_setup__tests__keymap_picker_all_tab_search.snap
@@ -11,6 +11,6 @@ Toggle Raw Output | alt-r | Global toggle_raw_output Toggle Raw Output Toggle ra
 Interrupt Turn | esc | Chat interrupt_turn Interrupt Turn Interrupt the active turn. esc Default
 Decrease Reasoning Effort | alt-,, shift-down | Chat decrease_reasoning_effort Decrease Reasoning Effort Decrease reasoning effort. alt-,, shift-down Default
 Increase Reasoning Effort | alt-., shift-up | Chat increase_reasoning_effort Increase Reasoning Effort Increase reasoning effort. alt-., shift-up Default
+Previous Model | alt-; | Chat previous_model Previous Model Select the previous model. alt-; Default
+Next Model | alt-' | Chat next_model Next Model Select the next model. alt-' Default
 Edit Queued Message | alt-up, shift-left | Chat edit_queued_message Edit Queued Message Edit the most recently queued message. alt-up, shift-left Default
-Submit | enter | Composer submit Submit Submit the current composer draft. enter Default
-Queue | tab | Composer queue Queue Queue the draft while a task is running. tab Default
diff --git a/codex-rs/tui/src/snapshots/codex_tui__keymap_setup__tests__keymap_picker_custom.snap b/codex-rs/tui/src/snapshots/codex_tui__keymap_setup__tests__keymap_picker_custom.snap
index fee0d19a29..18ed9e17da 100644
--- a/codex-rs/tui/src/snapshots/codex_tui__keymap_setup__tests__keymap_picker_custom.snap
+++ b/codex-rs/tui/src/snapshots/codex_tui__keymap_setup__tests__keymap_picker_custom.snap
@@ -5,7 +5,7 @@ expression: "render_picker(params, 120)"
 
   Keymap
   All configurable shortcuts.
-  108 actions, 1 customized, 2 unbound.
+  110 actions, 1 customized, 2 unbound.
 
   [All]  Common  Customized (1)  Unbound (2)  App  Composer  Editor  Vim  Navigation  Approval  Debug
 
diff --git a/codex-rs/tui/src/snapshots/codex_tui__keymap_setup__tests__keymap_picker_fast_mode_enabled.snap b/codex-rs/tui/src/snapshots/codex_tui__keymap_setup__tests__keymap_picker_fast_mode_enabled.snap
index 4ceac6b97c..d40c1b05de 100644
--- a/codex-rs/tui/src/snapshots/codex_tui__keymap_setup__tests__keymap_picker_fast_mode_enabled.snap
+++ b/codex-rs/tui/src/snapshots/codex_tui__keymap_setup__tests__keymap_picker_fast_mode_enabled.snap
@@ -5,7 +5,7 @@ expression: "render_picker(params, 120)"
 
   Keymap
   All configurable shortcuts.
-  109 actions, 0 customized, 3 unbound.
+  111 actions, 0 customized, 3 unbound.
 
   [All]  Common  Customized (0)  Unbound (3)  App  Composer  Editor  Vim  Navigation  Approval  Debug
 
diff --git a/codex-rs/tui/src/snapshots/codex_tui__keymap_setup__tests__keymap_picker_first_actions.snap b/codex-rs/tui/src/snapshots/codex_tui__keymap_setup__tests__keymap_picker_first_actions.snap
index 7a1e796cd7..f08a5ad721 100644
--- a/codex-rs/tui/src/snapshots/codex_tui__keymap_setup__tests__keymap_picker_first_actions.snap
+++ b/codex-rs/tui/src/snapshots/codex_tui__keymap_setup__tests__keymap_picker_first_actions.snap
@@ -2,11 +2,11 @@
 source: tui/src/keymap_setup.rs
 expression: snapshot
 ---
-tab: All (108 selectable)
+tab: All (110 selectable)
 tab: Common (20 selectable)
 tab: Customized (0) (0 selectable)
 tab: Unbound (2) (2 selectable)
-tab: App (10 selectable)
+tab: App (12 selectable)
 tab: Composer (5 selectable)
 tab: Editor (17 selectable)
 tab: Vim (48 selectable)
@@ -22,6 +22,6 @@ Toggle Raw Output | alt-r | Global toggle_raw_output Toggle Raw Output Toggle ra
 Interrupt Turn | esc | Chat interrupt_turn Interrupt Turn Interrupt the active turn. esc Default
 Decrease Reasoning Effort | alt-,, shift-down | Chat decrease_reasoning_effort Decrease Reasoning Effort Decrease reasoning effort. alt-,, shift-down Default
 Increase Reasoning Effort | alt-., shift-up | Chat increase_reasoning_effort Increase Reasoning Effort Increase reasoning effort. alt-., shift-up Default
+Previous Model | alt-; | Chat previous_model Previous Model Select the previous model. alt-; Default
+Next Model | alt-' | Chat next_model Next Model Select the next model. alt-' Default
 Edit Queued Message | alt-up, shift-left | Chat edit_queued_message Edit Queued Message Edit the most recently queued message. alt-up, shift-left Default
-Submit | enter | Composer submit Submit Submit the current composer draft. enter Default
-Queue | tab | Composer queue Queue Queue the draft while a task is running. tab Default
diff --git a/codex-rs/tui/src/snapshots/codex_tui__keymap_setup__tests__keymap_picker_narrow.snap b/codex-rs/tui/src/snapshots/codex_tui__keymap_setup__tests__keymap_picker_narrow.snap
index 8b52d91b4e..3d02b83f83 100644
--- a/codex-rs/tui/src/snapshots/codex_tui__keymap_setup__tests__keymap_picker_narrow.snap
+++ b/codex-rs/tui/src/snapshots/codex_tui__keymap_setup__tests__keymap_picker_narrow.snap
@@ -5,7 +5,7 @@ expression: "render_picker(params, 78)"
 
   Keymap
   All configurable shortcuts.
-  108 actions, 0 customized, 2 unbound.
+  110 actions, 0 customized, 2 unbound.
 
   [All]  Common  Customized (0)  Unbound (2)  App  Composer  Editor  Vim
   Navigation  Approval  Debug
diff --git a/codex-rs/tui/src/snapshots/codex_tui__keymap_setup__tests__keymap_picker_wide.snap b/codex-rs/tui/src/snapshots/codex_tui__keymap_setup__tests__keymap_picker_wide.snap
index d2c5d34a5c..7fba0c41fa 100644
--- a/codex-rs/tui/src/snapshots/codex_tui__keymap_setup__tests__keymap_picker_wide.snap
+++ b/codex-rs/tui/src/snapshots/codex_tui__keymap_setup__tests__keymap_picker_wide.snap
@@ -5,7 +5,7 @@ expression: "render_picker(params, 120)"
 
   Keymap
   All configurable shortcuts.
-  108 actions, 0 customized, 2 unbound.
+  110 actions, 0 customized, 2 unbound.
 
   [All]  Common  Customized (0)  Unbound (2)  App  Composer  Editor  Vim  Navigation  Approval  Debug
 
