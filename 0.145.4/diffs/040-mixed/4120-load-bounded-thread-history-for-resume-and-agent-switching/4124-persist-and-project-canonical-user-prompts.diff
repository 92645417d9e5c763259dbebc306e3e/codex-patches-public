diff --git a/codex-rs/app-server-protocol/src/protocol/thread_history.rs b/codex-rs/app-server-protocol/src/protocol/thread_history.rs
index 340ae7a7eb..bb144b2cb0 100644
--- a/codex-rs/app-server-protocol/src/protocol/thread_history.rs
+++ b/codex-rs/app-server-protocol/src/protocol/thread_history.rs
@@ -231,6 +231,12 @@ impl ThreadHistoryChangeAccumulator {
     }
 }
 
+struct CanonicalUserMessageAlias {
+    turn_id: String,
+    item_id: String,
+    payload: UserMessageEvent,
+}
+
 pub struct ThreadHistoryBuilder {
     turns: Vec<Turn>,
     current_turn: Option<PendingTurn>,
@@ -238,6 +244,7 @@ pub struct ThreadHistoryBuilder {
     current_rollout_index: usize,
     next_rollout_index: usize,
     active_change_set: Option<ThreadHistoryChangeSet>,
+    canonical_user_message_aliases: Vec<CanonicalUserMessageAlias>,
 }
 
 impl Default for ThreadHistoryBuilder {
@@ -255,6 +262,7 @@ impl ThreadHistoryBuilder {
             current_rollout_index: 0,
             next_rollout_index: 0,
             active_change_set: None,
+            canonical_user_message_aliases: Vec::new(),
         }
     }
 
@@ -470,6 +478,13 @@ impl ThreadHistoryBuilder {
     }
 
     fn handle_user_message(&mut self, payload: &UserMessageEvent) {
+        if self
+            .canonical_user_message_aliases
+            .iter()
+            .any(|canonical| canonical.payload == *payload)
+        {
+            return;
+        }
         // User messages should stay in explicitly opened turns. For backward
         // compatibility with older streams that did not open turns explicitly,
         // close any implicit/inactive turn and start a fresh one for this input.
@@ -592,6 +607,23 @@ impl ThreadHistoryBuilder {
     }
 
     fn handle_item_completed(&mut self, payload: &ItemCompletedEvent) {
+        if let codex_protocol::items::TurnItem::UserMessage(user_message) = &payload.item {
+            let item = self.build_canonical_user_message(user_message);
+            let materialized = self.upsert_item_in_turn_id(&payload.turn_id, item);
+            if materialized
+                && !self.canonical_user_message_aliases.iter().any(|canonical| {
+                    canonical.turn_id == payload.turn_id && canonical.item_id == user_message.id
+                })
+            {
+                self.canonical_user_message_aliases
+                    .push(CanonicalUserMessageAlias {
+                        turn_id: payload.turn_id.clone(),
+                        item_id: user_message.id.clone(),
+                        payload: user_message.as_legacy_user_message_event(),
+                    });
+            }
+            return;
+        }
         self.handle_materialized_item_lifecycle(&payload.turn_id, &payload.item);
     }
 
@@ -1202,6 +1234,14 @@ impl ThreadHistoryBuilder {
     }
 
     fn handle_turn_aborted(&mut self, payload: &TurnAbortedEvent) {
+        let aborted_turn_id = payload
+            .turn_id
+            .clone()
+            .or_else(|| self.current_turn.as_ref().map(|turn| turn.id.clone()));
+        if let Some(turn_id) = aborted_turn_id {
+            self.canonical_user_message_aliases
+                .retain(|canonical| canonical.turn_id != turn_id);
+        }
         let apply_abort = |turn: &mut PendingTurn| {
             turn.status = TurnStatus::Interrupted;
             turn.completed_at = payload.completed_at;
@@ -1234,6 +1274,8 @@ impl ThreadHistoryBuilder {
     }
 
     fn handle_turn_started(&mut self, payload: &TurnStartedEvent) {
+        self.canonical_user_message_aliases
+            .retain(|canonical| canonical.turn_id != payload.turn_id);
         self.finish_current_turn();
         let turn = self
             .new_turn(Some(payload.turn_id.clone()))
@@ -1245,6 +1287,8 @@ impl ThreadHistoryBuilder {
     }
 
     fn handle_turn_complete(&mut self, payload: &TurnCompleteEvent) {
+        self.canonical_user_message_aliases
+            .retain(|canonical| canonical.turn_id != payload.turn_id);
         let mark_completed = |turn: &mut PendingTurn| {
             if matches!(turn.status, TurnStatus::Completed | TurnStatus::InProgress) {
                 turn.status = TurnStatus::Completed;
@@ -1299,6 +1343,7 @@ impl ThreadHistoryBuilder {
     }
 
     fn handle_thread_rollback(&mut self, payload: &ThreadRolledBackEvent) {
+        self.canonical_user_message_aliases.clear();
         self.finish_current_turn();
 
         let n = usize::try_from(payload.num_turns).unwrap_or(usize::MAX);
@@ -1382,7 +1427,7 @@ impl ThreadHistoryBuilder {
         }
     }
 
-    fn upsert_item_in_turn_id(&mut self, turn_id: &str, item: ThreadItem) {
+    fn upsert_item_in_turn_id(&mut self, turn_id: &str, item: ThreadItem) -> bool {
         let tracking_changes = self.is_tracking_changes();
         if let Some(turn) = self.current_turn.as_mut()
             && turn.id == turn_id
@@ -1394,7 +1439,7 @@ impl ThreadHistoryBuilder {
             if let Some((turn_id, item)) = changed_item {
                 self.record_changed_item(turn_id, item);
             }
-            return;
+            return true;
         }
 
         if let Some(turn) = self.turns.iter_mut().find(|turn| turn.id == turn_id) {
@@ -1405,13 +1450,14 @@ impl ThreadHistoryBuilder {
             if let Some((turn_id, item)) = changed_item {
                 self.record_changed_item(turn_id, item);
             }
-            return;
+            return true;
         }
 
         warn!(
             item_id = item.id(),
             "dropping turn-scoped item for unknown turn id `{turn_id}`"
         );
+        false
     }
 
     fn upsert_item_in_current_turn(&mut self, item: ThreadItem) {
@@ -1501,6 +1547,22 @@ impl ThreadHistoryBuilder {
         );
         content
     }
+
+    fn build_canonical_user_message(
+        &self,
+        user_message: &codex_protocol::items::UserMessageItem,
+    ) -> ThreadItem {
+        ThreadItem::UserMessage {
+            id: user_message.id.clone(),
+            client_id: user_message.client_id.clone(),
+            content: user_message
+                .content
+                .iter()
+                .cloned()
+                .map(Into::into)
+                .collect(),
+        }
+    }
 }
 
 fn convert_dynamic_tool_content_items(
@@ -1922,9 +1984,24 @@ mod tests {
     }
 
     #[test]
-    fn ignores_user_message_item_lifecycle_events() {
+    fn canonical_user_message_completion_materializes_and_deduplicates_legacy_event() {
         let turn_id = "turn-1";
         let thread_id = ThreadId::new();
+        let canonical = CoreUserMessageItem {
+            id: "user-item-id".to_string(),
+            client_id: Some("client-1".to_string()),
+            content: vec![
+                codex_protocol::user_input::UserInput::Text {
+                    text: "hello $lint".to_string(),
+                    text_elements: Vec::new(),
+                },
+                codex_protocol::user_input::UserInput::Skill {
+                    name: "lint".to_string(),
+                    path: PathBuf::from("/skills/lint/SKILL.md"),
+                },
+            ],
+        };
+        let legacy = canonical.as_legacy_user_message_event();
         let events = vec![
             EventMsg::TurnStarted(TurnStartedEvent {
                 turn_id: turn_id.to_string(),
@@ -1933,24 +2010,13 @@ mod tests {
                 model_context_window: None,
                 collaboration_mode_kind: Default::default(),
             }),
-            EventMsg::UserMessage(UserMessageEvent {
-                client_id: None,
-                message: "hello".into(),
-                images: None,
-                text_elements: Vec::new(),
-                local_images: Vec::new(),
-                ..Default::default()
-            }),
-            EventMsg::ItemStarted(ItemStartedEvent {
+            EventMsg::ItemCompleted(ItemCompletedEvent {
                 thread_id,
                 turn_id: turn_id.to_string(),
-                item: CoreTurnItem::UserMessage(CoreUserMessageItem {
-                    id: "user-item-id".to_string(),
-                    client_id: None,
-                    content: Vec::new(),
-                }),
-                started_at_ms: 0,
+                item: CoreTurnItem::UserMessage(canonical),
+                completed_at_ms: 0,
             }),
+            EventMsg::UserMessage(legacy),
             EventMsg::TurnComplete(TurnCompleteEvent {
                 turn_id: turn_id.to_string(),
                 started_at: None,
@@ -1966,22 +2032,128 @@ mod tests {
             .into_iter()
             .map(RolloutItem::EventMsg)
             .collect::<Vec<_>>();
-        let turns = build_turns_from_rollout_items(&items);
+        let mut builder = ThreadHistoryBuilder::new();
+        let changes = builder.handle_rollout_items_with_changes(&items);
+        let turns = builder.finish();
         assert_eq!(turns.len(), 1);
-        assert_eq!(turns[0].items.len(), 1);
-        assert_eq!(
-            turns[0].items[0],
-            ThreadItem::UserMessage {
-                id: "item-1".into(),
-                client_id: None,
-                content: vec![UserInput::Text {
-                    text: "hello".into(),
+        let expected = ThreadItem::UserMessage {
+            id: "user-item-id".into(),
+            client_id: Some("client-1".into()),
+            content: vec![
+                UserInput::Text {
+                    text: "hello $lint".into(),
                     text_elements: Vec::new(),
-                }],
-            }
+                },
+                UserInput::Skill {
+                    name: "lint".into(),
+                    path: PathBuf::from("/skills/lint/SKILL.md"),
+                },
+            ],
+        };
+        assert_eq!(turns[0].items, vec![expected.clone()]);
+        assert_eq!(
+            changes.changed_items,
+            vec![ThreadHistoryItemChange {
+                turn_id: turn_id.to_string(),
+                item: expected,
+            }]
         );
     }
 
+    #[test]
+    fn canonical_user_message_aliases_clear_at_turn_boundaries() {
+        let canonical = || CoreUserMessageItem {
+            id: "user-item-id".to_string(),
+            client_id: None,
+            content: vec![codex_protocol::user_input::UserInput::Text {
+                text: "same prompt".to_string(),
+                text_elements: Vec::new(),
+            }],
+        };
+
+        for boundary in [
+            EventMsg::TurnComplete(TurnCompleteEvent {
+                turn_id: "turn-1".to_string(),
+                started_at: None,
+                last_agent_message: None,
+                error: None,
+                completed_at: None,
+                duration_ms: None,
+                time_to_first_token_ms: None,
+            }),
+            EventMsg::TurnAborted(TurnAbortedEvent {
+                turn_id: Some("turn-1".to_string()),
+                reason: TurnAbortReason::Interrupted,
+                started_at: None,
+                completed_at: None,
+                duration_ms: None,
+            }),
+            EventMsg::ThreadRolledBack(ThreadRolledBackEvent { num_turns: 1 }),
+            EventMsg::TurnStarted(TurnStartedEvent {
+                turn_id: "turn-1".to_string(),
+                trace_id: None,
+                started_at: None,
+                model_context_window: None,
+                collaboration_mode_kind: Default::default(),
+            }),
+        ] {
+            let mut builder = ThreadHistoryBuilder::new();
+            builder.handle_event(&EventMsg::TurnStarted(TurnStartedEvent {
+                turn_id: "turn-1".to_string(),
+                trace_id: None,
+                started_at: None,
+                model_context_window: None,
+                collaboration_mode_kind: Default::default(),
+            }));
+            builder.handle_event(&EventMsg::ItemCompleted(ItemCompletedEvent {
+                thread_id: ThreadId::new(),
+                turn_id: "turn-1".to_string(),
+                item: CoreTurnItem::UserMessage(canonical()),
+                completed_at_ms: 0,
+            }));
+            assert_eq!(builder.canonical_user_message_aliases.len(), 1);
+            builder.handle_event(&boundary);
+            assert!(builder.canonical_user_message_aliases.is_empty());
+        }
+    }
+
+    #[test]
+    fn unmaterialized_canonical_user_message_does_not_suppress_legacy_fallback() {
+        let canonical = CoreUserMessageItem {
+            id: "user-canonical".to_string(),
+            client_id: Some("client-1".to_string()),
+            content: vec![codex_protocol::user_input::UserInput::Text {
+                text: "recover this prompt".to_string(),
+                text_elements: Vec::new(),
+            }],
+        };
+        let legacy = canonical.as_legacy_user_message_event();
+        let mut builder = ThreadHistoryBuilder::new();
+
+        builder.handle_event(&EventMsg::ItemCompleted(ItemCompletedEvent {
+            thread_id: ThreadId::new(),
+            turn_id: "turn-late".to_string(),
+            item: CoreTurnItem::UserMessage(canonical),
+            completed_at_ms: 0,
+        }));
+        assert!(builder.canonical_user_message_aliases.is_empty());
+
+        builder.handle_event(&EventMsg::TurnStarted(TurnStartedEvent {
+            turn_id: "turn-late".to_string(),
+            trace_id: None,
+            started_at: None,
+            model_context_window: None,
+            collaboration_mode_kind: Default::default(),
+        }));
+        builder.handle_event(&EventMsg::UserMessage(legacy));
+
+        let turns = builder.finish();
+        assert!(matches!(
+            turns[0].items.as_slice(),
+            [ThreadItem::UserMessage { id, .. }] if id != "user-canonical"
+        ));
+    }
+
     #[test]
     fn rebuilds_sleep_item_from_persisted_completion() {
         let turn_id = "turn-1";
diff --git a/codex-rs/core/src/session/session.rs b/codex-rs/core/src/session/session.rs
index 20b2bf26bd..b36fb0bba1 100644
--- a/codex-rs/core/src/session/session.rs
+++ b/codex-rs/core/src/session/session.rs
@@ -13,8 +13,10 @@ use codex_protocol::SessionId;
 use codex_protocol::capabilities::SelectedCapabilityRoot;
 use codex_protocol::config_types::SERVICE_TIER_DEFAULT_REQUEST_VALUE;
 use codex_protocol::config_types::ServiceTier;
+use codex_protocol::items::TurnItem;
 use codex_protocol::permissions::FileSystemPath;
 use codex_protocol::permissions::FileSystemSpecialPath;
+use codex_protocol::protocol::EventMsg;
 use codex_protocol::protocol::MultiAgentVersion;
 use codex_protocol::protocol::ThreadHistoryMode;
 use codex_protocol::protocol::ThreadSource;
@@ -22,6 +24,55 @@ use codex_protocol::protocol::TurnEnvironmentSelections;
 use std::sync::OnceLock;
 use tokio::sync::Semaphore;
 
+fn initial_messages_for_session(
+    initial_history: &InitialHistory,
+    history_mode: ThreadHistoryMode,
+) -> Option<Vec<EventMsg>> {
+    let events = initial_history.get_event_msgs()?;
+    if history_mode != ThreadHistoryMode::Legacy {
+        return Some(events);
+    }
+
+    let mut canonical_user_messages = Vec::new();
+    let mut projected = Vec::with_capacity(events.len());
+    for event in events {
+        match &event {
+            EventMsg::ItemCompleted(completed) => {
+                if let TurnItem::UserMessage(user_message) = &completed.item {
+                    let legacy = user_message.as_legacy_user_message_event();
+                    canonical_user_messages.push((completed.turn_id.clone(), legacy.clone()));
+                    projected.push(EventMsg::UserMessage(legacy));
+                    continue;
+                }
+            }
+            EventMsg::UserMessage(user_message)
+                if canonical_user_messages
+                    .iter()
+                    .any(|(_, canonical)| canonical == user_message) =>
+            {
+                continue;
+            }
+            EventMsg::TurnStarted(started) => {
+                canonical_user_messages.retain(|(turn_id, _)| turn_id != &started.turn_id)
+            }
+            EventMsg::TurnComplete(completed) => {
+                canonical_user_messages.retain(|(turn_id, _)| turn_id != &completed.turn_id)
+            }
+            EventMsg::TurnAborted(aborted) => {
+                if let Some(aborted_turn_id) = aborted.turn_id.as_ref() {
+                    canonical_user_messages.retain(|(turn_id, _)| turn_id != aborted_turn_id);
+                } else {
+                    canonical_user_messages.clear();
+                }
+            }
+            EventMsg::ThreadRolledBack(_) => canonical_user_messages.clear(),
+            _ => {}
+        }
+        projected.push(event);
+    }
+    Some(projected)
+}
+
 /// Context for an initialized model agent
 ///
 /// A session has at most 1 running task at a time, and can be interrupted by user input.
@@ -1168,7 +1219,10 @@ impl Session {
             }
             // Dispatch the SessionConfiguredEvent first and then report any errors.
             // If resuming, include converted initial messages in the payload so UIs can render them immediately.
-            let initial_messages = initial_history.get_event_msgs();
+            let initial_messages = initial_messages_for_session(
+                &initial_history,
+                session_configuration.history_mode,
+            );
             let events = std::iter::once(Event {
                 id: INITIAL_SUBMIT_ID.to_owned(),
                 msg: EventMsg::SessionConfigured(SessionConfiguredEvent {
@@ -1288,3 +1342,85 @@ impl Session {
         }
     }
 }
+
+#[cfg(test)]
+mod initial_message_tests {
+    use super::*;
+    use codex_protocol::ThreadId;
+    use codex_protocol::items::UserMessageItem;
+    use codex_protocol::protocol::ItemCompletedEvent;
+    use codex_protocol::protocol::RolloutItem;
+    use codex_protocol::protocol::TurnStartedEvent;
+    use codex_protocol::user_input::UserInput;
+
+    #[test]
+    fn paginated_initial_messages_keep_canonical_user_items() {
+        let canonical = EventMsg::ItemCompleted(ItemCompletedEvent {
+            thread_id: ThreadId::new(),
+            turn_id: "turn-1".to_string(),
+            item: TurnItem::UserMessage(UserMessageItem {
+                id: "user-1".to_string(),
+                client_id: None,
+                content: vec![UserInput::Text {
+                    text: "hello".to_string(),
+                    text_elements: Vec::new(),
+                }],
+            }),
+            completed_at_ms: 0,
+        });
+        let history = InitialHistory::Forked(vec![RolloutItem::EventMsg(canonical)]);
+
+        let projected = initial_messages_for_session(&history, ThreadHistoryMode::Paginated)
+            .expect("paginated initial messages");
+
+        assert!(matches!(
+            projected.as_slice(),
+            [EventMsg::ItemCompleted(ItemCompletedEvent {
+                item: TurnItem::UserMessage(_),
+                ..
+            })]
+        ));
+    }
+
+    #[test]
+    fn legacy_initial_messages_project_canonical_user_and_deduplicate_alias() {
+        let user_message = UserMessageItem {
+            id: "user-1".to_string(),
+            client_id: None,
+            content: vec![UserInput::Text {
+                text: "review these changes".to_string(),
+                text_elements: Vec::new(),
+            }],
+        };
+        let legacy = user_message.as_legacy_user_message_event();
+        let history = InitialHistory::Forked(
+            vec![
+                EventMsg::ItemCompleted(ItemCompletedEvent {
+                    thread_id: ThreadId::new(),
+                    turn_id: "review-child".to_string(),
+                    item: TurnItem::UserMessage(user_message),
+                    completed_at_ms: 0,
+                }),
+                EventMsg::TurnStarted(TurnStartedEvent {
+                    turn_id: "review-parent".to_string(),
+                    trace_id: None,
+                    started_at: None,
+                    model_context_window: None,
+                    collaboration_mode_kind: Default::default(),
+                }),
+                EventMsg::UserMessage(legacy),
+            ]
+            .into_iter()
+            .map(RolloutItem::EventMsg)
+            .collect(),
+        );
+
+        let projected = initial_messages_for_session(&history, ThreadHistoryMode::Legacy)
+            .expect("legacy initial messages");
+
+        assert!(matches!(
+            projected.as_slice(),
+            [EventMsg::UserMessage(_), EventMsg::TurnStarted(_)]
+        ));
+    }
+}
diff --git a/codex-rs/core/src/tasks/review.rs b/codex-rs/core/src/tasks/review.rs
index 0e2e2712e0..ed26d24238 100644
--- a/codex-rs/core/src/tasks/review.rs
+++ b/codex-rs/core/src/tasks/review.rs
@@ -145,6 +145,7 @@ async fn process_review_events(
     receiver: async_channel::Receiver<Event>,
 ) -> Option<ReviewOutputEvent> {
     let mut prev_agent_message: Option<Event> = None;
+    let mut pending_user_alias = None;
     while let Ok(event) = receiver.recv().await {
         match event.clone().msg {
             EventMsg::AgentMessage(_) => {
@@ -164,6 +165,22 @@ async fn process_review_events(
                 ..
             })
             | EventMsg::AgentMessageContentDelta(AgentMessageContentDeltaEvent { .. }) => {}
+            EventMsg::ItemCompleted(completed) => {
+                if let TurnItem::UserMessage(user_message) = &completed.item {
+                    pending_user_alias = Some(user_message.as_legacy_user_message_event());
+                }
+                session
+                    .clone_session()
+                    .send_event(ctx.as_ref(), EventMsg::ItemCompleted(completed))
+                    .await;
+            }
+            EventMsg::UserMessage(user_message)
+                if pending_user_alias.as_ref() == Some(&user_message) =>
+            {
+                // Forwarding the canonical completion through the parent already emits this
+                // alias. Consume only that matching duplicate; legacy-only input still flows.
+                pending_user_alias = None;
+            }
             EventMsg::TurnComplete(task_complete) => {
                 // Parse review output from the last agent message (if present).
                 let out = task_complete
diff --git a/codex-rs/core/tests/suite/review.rs b/codex-rs/core/tests/suite/review.rs
index b3ba4f2597..b3b0a83159 100644
--- a/codex-rs/core/tests/suite/review.rs
+++ b/codex-rs/core/tests/suite/review.rs
@@ -1,5 +1,8 @@
+use codex_app_server_protocol::ThreadItem as AppThreadItem;
+use codex_app_server_protocol::UserInput as AppUserInput;
 use codex_core::CodexThread;
 use codex_core::REVIEW_PROMPT;
+use codex_core::RolloutRecorder;
 use codex_core::config::Config;
 use codex_protocol::items::TurnItem;
 use codex_protocol::models::ContentItem;
@@ -184,6 +187,56 @@ async fn review_op_emits_lifecycle_and_review_output() {
 
     let path = codex.rollout_path().expect("rollout path");
     let text = std::fs::read_to_string(&path).expect("read rollout file");
+    let (rollout_items, _, parse_errors) = RolloutRecorder::load_rollout_items(&path)
+        .await
+        .expect("load review rollout");
+    assert_eq!(parse_errors, 0);
+    let review_prompt = "Please review my changes";
+    let canonical_review_users = rollout_items
+        .iter()
+        .filter_map(|item| match item {
+            RolloutItem::EventMsg(EventMsg::ItemCompleted(event)) => match &event.item {
+                TurnItem::UserMessage(user) if user.message().contains(review_prompt) => Some(user),
+                _ => None,
+            },
+            _ => None,
+        })
+        .collect::<Vec<_>>();
+    let legacy_review_users = rollout_items
+        .iter()
+        .filter(|item| {
+            matches!(
+                item,
+                RolloutItem::EventMsg(EventMsg::UserMessage(user))
+                    if user.message.contains(review_prompt)
+            )
+        })
+        .count();
+    assert_eq!(canonical_review_users.len(), 1);
+    assert_eq!(legacy_review_users, 1);
+
+    let turns = codex_app_server_protocol::build_turns_from_rollout_items(&rollout_items);
+    let materialized_review_users = turns
+        .iter()
+        .flat_map(|turn| turn.items.iter())
+        .filter_map(|item| match item {
+            AppThreadItem::UserMessage { id, content, .. }
+                if content.iter().any(|input| {
+                    matches!(
+                        input,
+                        AppUserInput::Text { text, .. } if text.contains(review_prompt)
+                    )
+                }) =>
+            {
+                Some(id)
+            }
+            _ => None,
+        })
+        .collect::<Vec<_>>();
+    assert_eq!(
+        materialized_review_users,
+        vec![&canonical_review_users[0].id]
+    );
     let parent_thread_id = text
         .lines()
         .filter(|line| !line.trim().is_empty())
diff --git a/codex-rs/protocol/src/protocol.rs b/codex-rs/protocol/src/protocol.rs
index 6628b5885a..acbb2e0aac 100644
--- a/codex-rs/protocol/src/protocol.rs
+++ b/codex-rs/protocol/src/protocol.rs
@@ -2356,7 +2356,7 @@ pub struct AgentMessageEvent {
     pub memory_citation: Option<MemoryCitation>,
 }
 
-#[derive(Debug, Clone, Default, Deserialize, Serialize, JsonSchema, TS)]
+#[derive(Debug, Clone, Default, Deserialize, Serialize, JsonSchema, TS, PartialEq)]
 pub struct UserMessageEvent {
     #[serde(default, skip_serializing_if = "Option::is_none")]
     pub client_id: Option<String>,
diff --git a/codex-rs/rollout/src/persistence_metrics_tests.rs b/codex-rs/rollout/src/persistence_metrics_tests.rs
index 6b859675d6..49f47b1994 100644
--- a/codex-rs/rollout/src/persistence_metrics_tests.rs
+++ b/codex-rs/rollout/src/persistence_metrics_tests.rs
@@ -277,7 +277,7 @@ fn invalid_turn_boundaries_reset_partial_measurements() {
 }
 
 #[test]
-fn item_completion_persistence_depends_on_history_mode() {
+fn user_message_completion_persists_in_both_history_modes() {
     let item = RolloutItem::EventMsg(EventMsg::ItemCompleted(ItemCompletedEvent {
         thread_id: ThreadId::default(),
         turn_id: "turn".to_string(),
@@ -289,16 +289,20 @@ fn item_completion_persistence_depends_on_history_mode() {
         completed_at_ms: 0,
     }));
 
-    let (_, legacy_measurement) =
+    let (legacy_persisted, legacy_measurement) =
         measure_and_filter_rollout_items(std::slice::from_ref(&item), ThreadHistoryMode::Legacy);
 
+    assert_eq!(
+        serde_json::to_value(legacy_persisted).expect("serialize legacy persisted items"),
+        serde_json::to_value([item.clone()]).expect("serialize expected legacy items")
+    );
     assert_eq!(
         legacy_measurement.items[0].rollout_item_type,
         "event.item_completed.user_message"
     );
     assert_eq!(
         legacy_measurement.items[0].decision,
-        super::PersistenceDecision::Dropped
+        super::PersistenceDecision::Kept
     );
 
     let (persisted, paginated_measurement) =
diff --git a/codex-rs/rollout/src/policy.rs b/codex-rs/rollout/src/policy.rs
index ce805ab923..d8ab5fc2b0 100644
--- a/codex-rs/rollout/src/policy.rs
+++ b/codex-rs/rollout/src/policy.rs
@@ -88,11 +88,14 @@ pub fn should_persist_event_msg(ev: &EventMsg, history_mode: ThreadHistoryMode)
     match ev {
         EventMsg::ItemCompleted(event) => {
             // Paginated rollouts store TurnItems.
-            // Legacy rollouts keep only items with no raw ResponseItem or legacy equivalent.
+            // Legacy rollouts also keep canonical user items: their legacy event flattens
+            // structured skill and mention bindings that prompt editing must restore.
             matches!(history_mode, ThreadHistoryMode::Paginated)
                 || matches!(
-                    event.item,
-                    TurnItem::Plan(_) | TurnItem::Extension(ExtensionItem::Sleep(_))
+                    &event.item,
+                    TurnItem::UserMessage(_)
+                        | TurnItem::Plan(_)
+                        | TurnItem::Extension(ExtensionItem::Sleep(_))
                 )
         }
         EventMsg::TokenCount(_)
@@ -183,3 +186,35 @@ pub fn should_persist_event_msg(ev: &EventMsg, history_mode: ThreadHistoryMode)
         | EventMsg::CollabResumeBegin(_) => false,
     }
 }
+
+#[cfg(test)]
+mod tests {
+    use super::*;
+    use codex_protocol::ThreadId;
+    use codex_protocol::items::UserMessageItem;
+    use codex_protocol::protocol::ItemCompletedEvent;
+    use codex_protocol::user_input::UserInput;
+
+    #[test]
+    fn legacy_rollouts_persist_canonical_user_message_completions() {
+        let event = EventMsg::ItemCompleted(ItemCompletedEvent {
+            thread_id: ThreadId::new(),
+            turn_id: "turn-1".to_string(),
+            item: TurnItem::UserMessage(UserMessageItem {
+                id: "user-1".to_string(),
+                client_id: None,
+                content: vec![UserInput::Skill {
+                    name: "lint".to_string(),
+                    path: "/skills/lint/SKILL.md".into(),
+                }],
+            }),
+            completed_at_ms: 0,
+        });
+
+        assert!(should_persist_event_msg(&event, ThreadHistoryMode::Legacy));
+        assert!(should_persist_event_msg(
+            &event,
+            ThreadHistoryMode::Paginated
+        ));
+    }
+}
