diff --git a/codex-rs/collaboration-mode-templates/src/lib.rs b/codex-rs/collaboration-mode-templates/src/lib.rs
index 345990db0b..9c05baf9be 100644
--- a/codex-rs/collaboration-mode-templates/src/lib.rs
+++ b/codex-rs/collaboration-mode-templates/src/lib.rs
@@ -2,3 +2,21 @@ pub const PLAN: &str = include_str!("../templates/plan.md");
 pub const DEFAULT: &str = include_str!("../templates/default.md");
 pub const EXECUTE: &str = include_str!("../templates/execute.md");
 pub const PAIR_PROGRAMMING: &str = include_str!("../templates/pair_programming.md");
+
+#[cfg(test)]
+mod tests {
+    use super::PLAN;
+
+    #[test]
+    fn plan_allows_only_scratch_planning_documents() {
+        assert!(PLAN.contains(
+            "you may create or update planning documents beneath the supplied \
+             `$PROJECT_SCRATCH_ROOT`"
+        ));
+        assert!(PLAN.contains(
+            "this does not authorize source code, scripts, build artifacts, caches, generated \
+             product files, or implementation work"
+        ));
+        assert!(PLAN.contains("You must not perform **mutating** actions"));
+    }
+}
diff --git a/codex-rs/collaboration-mode-templates/templates/plan.md b/codex-rs/collaboration-mode-templates/templates/plan.md
index ca68f41c89..54a59424c9 100644
--- a/codex-rs/collaboration-mode-templates/templates/plan.md
+++ b/codex-rs/collaboration-mode-templates/templates/plan.md
@@ -16,7 +16,7 @@ Separately, `update_plan` is a checklist/progress/TODOs tool; it does not enter
 
 ## Execution vs. mutation in Plan Mode
 
-You may explore and execute **non-mutating** actions that improve the plan. You must not perform **mutating** actions.
+You may explore and execute **non-mutating** actions that improve the plan. You must not perform **mutating** actions, with one narrow exception: you may create or update planning documents beneath the supplied `$PROJECT_SCRATCH_ROOT`.
 
 ### Allowed (non-mutating, plan-improving)
 
@@ -26,12 +26,13 @@ Actions that gather truth, reduce ambiguity, or validate feasibility without cha
 * Static analysis, inspection, and repo exploration
 * Dry-run style commands when they do not edit repo-tracked files
 * Tests, builds, or checks that may write to caches or build artifacts (for example, `target/`, `.cache/`, or snapshots) so long as they do not edit repo-tracked files
+* Creating or updating planning documents beneath `$PROJECT_SCRATCH_ROOT`; this does not authorize source code, scripts, build artifacts, caches, generated product files, or implementation work
 
 ### Not allowed (mutating, plan-executing)
 
 Actions that implement the plan or change repo-tracked state. Examples:
 
-* Editing or writing files
+* Editing or writing files outside the planning-document exception under `$PROJECT_SCRATCH_ROOT`
 * Running formatters or linters that rewrite files
 * Applying patches, migrations, or codegen that updates repo-tracked files
 * Side-effectful commands whose purpose is to carry out the plan rather than refine it
