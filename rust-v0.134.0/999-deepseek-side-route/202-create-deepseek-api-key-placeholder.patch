diff --git a/codex-rs/core/src/config/config_tests.rs b/codex-rs/core/src/config/config_tests.rs
index f821e4dcaa..829084abad 100644
--- a/codex-rs/core/src/config/config_tests.rs
+++ b/codex-rs/core/src/config/config_tests.rs
@@ -68,8 +68,10 @@ use codex_features::FeaturesToml;
 use codex_model_provider_info::LMSTUDIO_OSS_PROVIDER_ID;
 use codex_model_provider_info::OLLAMA_OSS_PROVIDER_ID;
 use codex_model_provider_info::WireApi;
+use codex_models_manager::DEEPSEEK_API_KEY_FILE;
 use codex_models_manager::MIMO_API_KEY_FILE;
 use codex_models_manager::bundled_models_response;
+use codex_models_manager::with_deepseek_side_route_models;
 use codex_network_proxy::NetworkMode;
 use codex_protocol::config_types::SERVICE_TIER_DEFAULT_REQUEST_VALUE;
 use codex_protocol::config_types::ServiceTier;
@@ -7682,7 +7684,9 @@ async fn model_catalog_json_loads_from_path() -> std::io::Result<()> {
     )
     .await?;
 
+    catalog.models = with_deepseek_side_route_models(catalog.models);
     assert_eq!(config.model_catalog, Some(catalog));
+    assert!(codex_home.path().join(DEEPSEEK_API_KEY_FILE).exists());
     assert!(codex_home.path().join(MIMO_API_KEY_FILE).exists());
     Ok(())
 }
@@ -7709,7 +7713,9 @@ async fn model_catalog_defaults_to_models_cache_path() -> std::io::Result<()> {
     )
     .await?;
 
+    catalog.models = with_deepseek_side_route_models(catalog.models);
     assert_eq!(config.model_catalog, Some(catalog));
+    assert!(codex_home.path().join(DEEPSEEK_API_KEY_FILE).exists());
     assert!(codex_home.path().join(MIMO_API_KEY_FILE).exists());
     Ok(())
 }
@@ -7753,7 +7759,11 @@ async fn explicit_model_catalog_json_overrides_models_cache_path() -> std::io::R
     )
     .await?;
 
+    let explicit_catalog = ModelsResponse {
+        models: with_deepseek_side_route_models(explicit_catalog.models),
+    };
     assert_eq!(config.model_catalog, Some(explicit_catalog));
+    assert!(codex_home.path().join(DEEPSEEK_API_KEY_FILE).exists());
     assert!(codex_home.path().join(MIMO_API_KEY_FILE).exists());
     Ok(())
 }
diff --git a/codex-rs/core/src/config/mod.rs b/codex-rs/core/src/config/mod.rs
index 71139122a4..4e8e7d9e82 100644
--- a/codex-rs/core/src/config/mod.rs
+++ b/codex-rs/core/src/config/mod.rs
@@ -76,7 +76,9 @@ use codex_model_provider_info::OLLAMA_CHAT_PROVIDER_REMOVED_ERROR;
 use codex_model_provider_info::built_in_model_providers;
 use codex_model_provider_info::merge_configured_model_providers;
 use codex_models_manager::ModelsManagerConfig;
+use codex_models_manager::ensure_deepseek_api_key_file;
 use codex_models_manager::ensure_mimo_api_key_file;
+use codex_models_manager::with_deepseek_side_route_models;
 use codex_protocol::config_types::AltScreenMode;
 use codex_protocol::config_types::AutoCompactTokenLimitScope;
 use codex_protocol::config_types::ForcedLoginMethod;
@@ -1626,15 +1628,22 @@ fn load_model_catalog(
     model_catalog_json: Option<AbsolutePathBuf>,
     codex_home: &AbsolutePathBuf,
 ) -> std::io::Result<Option<ModelsResponse>> {
+    ensure_deepseek_api_key_file(codex_home.as_path());
     ensure_mimo_api_key_file(codex_home.as_path());
 
     if let Some(path) = model_catalog_json {
-        return load_catalog_json(&path).map(Some);
+        let mut catalog = load_catalog_json(&path)?;
+        catalog.models = with_deepseek_side_route_models(catalog.models);
+        return Ok(Some(catalog));
     }
 
     let cache_path = codex_home.join(DEFAULT_MODEL_CATALOG_CACHE_FILENAME);
     match std::fs::metadata(cache_path.as_path()) {
-        Ok(_) => load_catalog_from_models_cache(&cache_path).map(Some),
+        Ok(_) => {
+            let mut catalog = load_catalog_from_models_cache(&cache_path)?;
+            catalog.models = with_deepseek_side_route_models(catalog.models);
+            Ok(Some(catalog))
+        }
         Err(err) if err.kind() == ErrorKind::NotFound => Ok(None),
         Err(err) => Err(err),
     }
diff --git a/codex-rs/models-manager/src/lib.rs b/codex-rs/models-manager/src/lib.rs
index f38485b1db..c809af1969 100644
--- a/codex-rs/models-manager/src/lib.rs
+++ b/codex-rs/models-manager/src/lib.rs
@@ -12,7 +12,13 @@ pub use config::ModelsManagerConfig;
 use std::fs;
 use std::path::Path;
 
+pub const DEEPSEEK_API_KEY_FILE: &str = "deepseek.toml";
 pub const MIMO_API_KEY_FILE: &str = "mimo.toml";
+const DEEPSEEK_API_KEY_EXAMPLE: &str = concat!(
+    "# DeepSeek side-route credentials for Codex.\n",
+    "api_key = \"sk-dummy\"\n",
+    "base_url = \"https://api.deepseek.com/v1\"\n",
+);
 const MIMO_API_KEY_EXAMPLE: &str = concat!(
     "# MiMo side-route credentials for Codex.\n",
     "api_key = \"sk-dummy\"\n",
@@ -35,6 +41,16 @@ pub fn with_deepseek_side_route_models(mut models: Vec<ModelInfo>) -> Vec<ModelI
     models
 }
 
+pub fn ensure_deepseek_api_key_file(codex_home: &Path) {
+    let path = codex_home.join(DEEPSEEK_API_KEY_FILE);
+    if path.exists() {
+        return;
+    }
+    if fs::create_dir_all(codex_home).is_ok() {
+        let _ = fs::write(path, DEEPSEEK_API_KEY_EXAMPLE);
+    }
+}
+
 pub fn ensure_mimo_api_key_file(codex_home: &Path) {
     let path = codex_home.join(MIMO_API_KEY_FILE);
     if path.exists() {
diff --git a/codex-rs/models-manager/src/manager.rs b/codex-rs/models-manager/src/manager.rs
index af510c8d73..101ef8f5a2 100644
--- a/codex-rs/models-manager/src/manager.rs
+++ b/codex-rs/models-manager/src/manager.rs
@@ -107,7 +107,7 @@ pub trait ModelsManager: fmt::Debug + Send + Sync {
 
     /// Build picker-ready presets from the active catalog snapshot.
     fn build_available_models(&self, mut remote_models: Vec<ModelInfo>) -> Vec<ModelPreset> {
-        remote_models.sort_by(|a, b| a.priority.cmp(&b.priority));
+        remote_models.sort_by_key(|a| a.priority);
 
         let mut presets: Vec<ModelPreset> = remote_models.into_iter().map(Into::into).collect();
         let uses_codex_backend = self
@@ -201,9 +201,12 @@ impl OpenAiModelsManager {
         endpoint_client: Arc<dyn ModelsEndpointClient>,
         auth_manager: Option<Arc<AuthManager>>,
     ) -> Self {
+        crate::ensure_deepseek_api_key_file(&codex_home);
         let cache_path = codex_home.join(MODEL_CACHE_FILE);
         let cache_manager = ModelsCacheManager::new(cache_path, DEFAULT_MODEL_CACHE_TTL);
-        let remote_models = load_remote_models_from_file().unwrap_or_default();
+        let remote_models = crate::with_deepseek_side_route_models(
+            load_remote_models_from_file().unwrap_or_default(),
+        );
         Self {
             remote_models: RwLock::new(remote_models),
             etag: RwLock::new(None),
@@ -334,7 +337,7 @@ impl OpenAiModelsManager {
                 )
             });
         if should_use_remote_models_only {
-            *self.remote_models.write().await = models;
+            *self.remote_models.write().await = crate::with_deepseek_side_route_models(models);
             return;
         }
 
@@ -349,7 +352,7 @@ impl OpenAiModelsManager {
                 existing_models.push(model);
             }
         }
-        *self.remote_models.write().await = existing_models;
+        *self.remote_models.write().await = crate::with_deepseek_side_route_models(existing_models);
     }
 
     /// Attempt to satisfy the refresh from the cache when it matches the provider and TTL.
diff --git a/codex-rs/models-manager/src/manager_tests.rs b/codex-rs/models-manager/src/manager_tests.rs
index ede7cfe795..67f4a61c66 100644
--- a/codex-rs/models-manager/src/manager_tests.rs
+++ b/codex-rs/models-manager/src/manager_tests.rs
@@ -10,6 +10,7 @@ use codex_login::ExternalAuthRefreshContext;
 use codex_login::ExternalAuthTokens;
 use codex_login::TokenData;
 use codex_protocol::openai_models::ModelsResponse;
+use codex_protocol::openai_models::ReasoningEffort;
 use pretty_assertions::assert_eq;
 use serde_json::json;
 use std::collections::VecDeque;
@@ -70,6 +71,45 @@ fn assert_models_contain(actual: &[ModelInfo], expected: &[ModelInfo]) {
     }
 }
 
+fn with_builtin_side_route_models(models: Vec<ModelInfo>) -> Vec<ModelInfo> {
+    crate::with_deepseek_side_route_models(models)
+}
+
+#[test]
+fn side_route_models_replace_obsolete_split_models() {
+    let models = crate::with_deepseek_side_route_models(vec![
+        remote_model("deepseek-v4-pro", "old pro", 1),
+        remote_model("deepseek-v4-flash", "old flash", 2),
+        remote_model("mimo-v2.5-pro", "old mimo pro", 3),
+    ]);
+
+    let slugs = models
+        .iter()
+        .map(|model| model.slug.as_str())
+        .collect::<Vec<_>>();
+
+    assert_eq!(slugs, vec!["deepseek-v4", "mimo-v2.5"]);
+    for model in &models {
+        assert_eq!(model.context_window, Some(384_000));
+        assert_eq!(model.max_context_window, Some(384_000));
+        assert_eq!(model.effective_context_window_percent, 100);
+    }
+    assert_eq!(
+        models[0]
+            .supported_reasoning_levels
+            .iter()
+            .map(|preset| preset.effort)
+            .collect::<Vec<_>>(),
+        vec![
+            ReasoningEffort::Low,
+            ReasoningEffort::Medium,
+            ReasoningEffort::High,
+            ReasoningEffort::XHigh,
+        ]
+    );
+    assert!(!models[0].supports_reasoning_summaries);
+}
+
 #[derive(Debug)]
 struct TestModelsEndpoint {
     has_command_auth: bool,
@@ -382,7 +422,10 @@ async fn refresh_available_models_uses_remote_only_catalog_for_chatgpt_auth() {
         .await
         .expect("refresh succeeds");
 
-    assert_eq!(manager.get_remote_models().await, remote_models);
+    assert_eq!(
+        manager.get_remote_models().await,
+        with_builtin_side_route_models(remote_models)
+    );
     assert_eq!(endpoint.fetch_count(), 1, "expected a single model fetch");
 }
 
@@ -412,7 +455,10 @@ async fn refresh_available_models_uses_cached_remote_only_catalog_for_chatgpt_au
         .await
         .expect("cached refresh succeeds");
 
-    assert_eq!(cache_manager.get_remote_models().await, remote_models);
+    assert_eq!(
+        cache_manager.get_remote_models().await,
+        with_builtin_side_route_models(remote_models)
+    );
     assert_eq!(
         cache_endpoint.fetch_count(),
         0,
@@ -455,7 +501,9 @@ async fn refresh_available_models_preserves_bundled_catalog_for_empty_chatgpt_re
     let codex_home = tempdir().expect("temp dir");
     let endpoint = TestModelsEndpoint::new(vec![Vec::new()]);
     let manager = openai_manager_for_tests(codex_home.path().to_path_buf(), endpoint);
-    let expected = load_remote_models_from_file().expect("bundled models should parse");
+    let expected = with_builtin_side_route_models(
+        load_remote_models_from_file().expect("bundled models should parse"),
+    );
 
     manager
         .refresh_available_models(RefreshStrategy::OnlineIfUncached)
@@ -478,6 +526,7 @@ async fn refresh_available_models_merges_hidden_only_chatgpt_remote_with_bundled
     let manager = openai_manager_for_tests(codex_home.path().to_path_buf(), endpoint);
     let mut expected = load_remote_models_from_file().expect("bundled models should parse");
     expected.push(hidden_remote);
+    let expected = with_builtin_side_route_models(expected);
 
     manager
         .refresh_available_models(RefreshStrategy::OnlineIfUncached)
@@ -510,6 +559,7 @@ async fn refresh_available_models_keeps_merging_for_api_auth() {
     );
     let mut expected = load_remote_models_from_file().expect("bundled models should parse");
     expected.extend(remote_models);
+    let expected = with_builtin_side_route_models(expected);
 
     manager
         .refresh_available_models(RefreshStrategy::OnlineIfUncached)
diff --git a/codex-rs/models-manager/src/test_support.rs b/codex-rs/models-manager/src/test_support.rs
index aff2838907..1b62724c36 100644
--- a/codex-rs/models-manager/src/test_support.rs
+++ b/codex-rs/models-manager/src/test_support.rs
@@ -14,7 +14,7 @@ pub fn get_model_offline_for_tests(model: Option<&str>) -> String {
         return model.to_string();
     }
     let mut response = bundled_models_response().unwrap_or_default();
-    response.models.sort_by(|a, b| a.priority.cmp(&b.priority));
+    response.models.sort_by_key(|a| a.priority);
     let presets: Vec<ModelPreset> = response.models.into_iter().map(Into::into).collect();
     presets
         .iter()
