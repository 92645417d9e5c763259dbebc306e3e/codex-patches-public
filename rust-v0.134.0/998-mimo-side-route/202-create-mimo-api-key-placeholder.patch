diff --git a/codex-rs/core/src/config/config_tests.rs b/codex-rs/core/src/config/config_tests.rs
index 45a8d5f352..f821e4dcaa 100644
--- a/codex-rs/core/src/config/config_tests.rs
+++ b/codex-rs/core/src/config/config_tests.rs
@@ -68,6 +68,7 @@ use codex_features::FeaturesToml;
 use codex_model_provider_info::LMSTUDIO_OSS_PROVIDER_ID;
 use codex_model_provider_info::OLLAMA_OSS_PROVIDER_ID;
 use codex_model_provider_info::WireApi;
+use codex_models_manager::MIMO_API_KEY_FILE;
 use codex_models_manager::bundled_models_response;
 use codex_network_proxy::NetworkMode;
 use codex_protocol::config_types::SERVICE_TIER_DEFAULT_REQUEST_VALUE;
@@ -7682,6 +7683,7 @@ async fn model_catalog_json_loads_from_path() -> std::io::Result<()> {
     .await?;
 
     assert_eq!(config.model_catalog, Some(catalog));
+    assert!(codex_home.path().join(MIMO_API_KEY_FILE).exists());
     Ok(())
 }
 
@@ -7708,6 +7710,7 @@ async fn model_catalog_defaults_to_models_cache_path() -> std::io::Result<()> {
     .await?;
 
     assert_eq!(config.model_catalog, Some(catalog));
+    assert!(codex_home.path().join(MIMO_API_KEY_FILE).exists());
     Ok(())
 }
 
@@ -7751,6 +7754,7 @@ async fn explicit_model_catalog_json_overrides_models_cache_path() -> std::io::R
     .await?;
 
     assert_eq!(config.model_catalog, Some(explicit_catalog));
+    assert!(codex_home.path().join(MIMO_API_KEY_FILE).exists());
     Ok(())
 }
 
diff --git a/codex-rs/core/src/config/mod.rs b/codex-rs/core/src/config/mod.rs
index 0c0ebd4031..71139122a4 100644
--- a/codex-rs/core/src/config/mod.rs
+++ b/codex-rs/core/src/config/mod.rs
@@ -76,6 +76,7 @@ use codex_model_provider_info::OLLAMA_CHAT_PROVIDER_REMOVED_ERROR;
 use codex_model_provider_info::built_in_model_providers;
 use codex_model_provider_info::merge_configured_model_providers;
 use codex_models_manager::ModelsManagerConfig;
+use codex_models_manager::ensure_mimo_api_key_file;
 use codex_protocol::config_types::AltScreenMode;
 use codex_protocol::config_types::AutoCompactTokenLimitScope;
 use codex_protocol::config_types::ForcedLoginMethod;
@@ -1625,6 +1626,8 @@ fn load_model_catalog(
     model_catalog_json: Option<AbsolutePathBuf>,
     codex_home: &AbsolutePathBuf,
 ) -> std::io::Result<Option<ModelsResponse>> {
+    ensure_mimo_api_key_file(codex_home.as_path());
+
     if let Some(path) = model_catalog_json {
         return load_catalog_json(&path).map(Some);
     }
diff --git a/codex-rs/models-manager/src/lib.rs b/codex-rs/models-manager/src/lib.rs
index 5774e12a7a..474cf037d5 100644
--- a/codex-rs/models-manager/src/lib.rs
+++ b/codex-rs/models-manager/src/lib.rs
@@ -9,6 +9,15 @@ pub mod test_support;
 pub use codex_app_server_protocol::AuthMode;
 use codex_protocol::openai_models::ModelInfo;
 pub use config::ModelsManagerConfig;
+use std::fs;
+use std::path::Path;
+
+pub const MIMO_API_KEY_FILE: &str = "mimo.toml";
+const MIMO_API_KEY_EXAMPLE: &str = concat!(
+    "# MiMo side-route credentials for Codex.\n",
+    "api_key = \"sk-dummy\"\n",
+    "base_url = \"https://api.xiaomimimo.com/v1\"\n",
+);
 
 pub fn with_deepseek_side_route_models(mut models: Vec<ModelInfo>) -> Vec<ModelInfo> {
     models.retain(|model| !model_info::is_obsolete_side_route_model(&model.slug));
@@ -25,6 +34,16 @@ pub fn with_deepseek_side_route_models(mut models: Vec<ModelInfo>) -> Vec<ModelI
     models
 }
 
+pub fn ensure_mimo_api_key_file(codex_home: &Path) {
+    let path = codex_home.join(MIMO_API_KEY_FILE);
+    if path.exists() {
+        return;
+    }
+    if fs::create_dir_all(codex_home).is_ok() {
+        let _ = fs::write(path, MIMO_API_KEY_EXAMPLE);
+    }
+}
+
 /// Load the bundled model catalog shipped with `codex-models-manager`.
 pub fn bundled_models_response()
 -> std::result::Result<codex_protocol::openai_models::ModelsResponse, serde_json::Error> {
