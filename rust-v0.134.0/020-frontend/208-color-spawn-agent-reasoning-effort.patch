diff --git a/codex-rs/tui/src/multi_agents.rs b/codex-rs/tui/src/multi_agents.rs
index 97ba4070d1..609c27ff41 100644
--- a/codex-rs/tui/src/multi_agents.rs
+++ b/codex-rs/tui/src/multi_agents.rs
@@ -6,6 +6,7 @@
 
 use crate::history_cell::PlainHistoryCell;
 use crate::render::line_utils::prefix_lines;
+use crate::shimmer::rainbow_spans;
 use crate::text_formatting::truncate_text;
 use codex_app_server_protocol::CollabAgentState;
 use codex_app_server_protocol::CollabAgentStatus;
@@ -20,6 +21,7 @@ use crossterm::event::KeyEvent;
 use crossterm::event::KeyEventKind;
 #[cfg(target_os = "macos")]
 use crossterm::event::KeyModifiers;
+use ratatui::style::Color;
 use ratatui::style::Stylize;
 use ratatui::text::Line;
 use ratatui::text::Span;
@@ -471,13 +473,32 @@ fn spawn_request_spans(spawn_request: Option<&SpawnRequestSummary>) -> Vec<Span<
         return Vec::new();
     }
 
-    let details = if model.is_empty() {
-        format!("({})", spawn_request.reasoning_effort)
+    let effort = spawn_request.reasoning_effort;
+    let mut spans = vec![Span::from(" ").dim(), Span::from("(").magenta()];
+    if model.is_empty() {
+        spans.extend(reasoning_effort_spans(effort));
     } else {
-        format!("({model} {})", spawn_request.reasoning_effort)
-    };
+        spans.push(Span::from(model.to_string()).magenta());
+        spans.push(Span::from(" ").magenta());
+        spans.extend(reasoning_effort_spans(effort));
+    }
+    spans.push(Span::from(")").magenta());
 
-    vec![Span::from(" ").dim(), Span::from(details).magenta()]
+    spans
+}
+
+fn reasoning_effort_spans(effort: ReasoningEffortConfig) -> Vec<Span<'static>> {
+    let label = effort.to_string();
+    match effort {
+        ReasoningEffortConfig::Minimal | ReasoningEffortConfig::Low => {
+            vec![Span::from(label).yellow()]
+        }
+        #[allow(clippy::disallowed_methods)]
+        ReasoningEffortConfig::Medium => vec![Span::from(label).fg(Color::Rgb(255, 128, 0))],
+        ReasoningEffortConfig::High => vec![Span::from(label).red()],
+        ReasoningEffortConfig::XHigh => rainbow_spans(&label),
+        ReasoningEffortConfig::None => vec![Span::from(label).magenta()],
+    }
 }
 
 fn prompt_line(prompt: &str) -> Option<Line<'static>> {
@@ -822,8 +843,43 @@ mod tests {
         assert_eq!(title.spans[4].content.as_ref(), "[explorer]");
         assert_eq!(title.spans[4].style.fg, None);
         assert!(!title.spans[4].style.add_modifier.contains(Modifier::DIM));
-        assert_eq!(title.spans[6].content.as_ref(), "(gpt-5 high)");
+        assert_eq!(title.spans[6].content.as_ref(), "(");
         assert_eq!(title.spans[6].style.fg, Some(Color::Magenta));
+        assert_eq!(title.spans[7].content.as_ref(), "gpt-5");
+        assert_eq!(title.spans[7].style.fg, Some(Color::Magenta));
+        assert_eq!(title.spans[9].content.as_ref(), "high");
+        assert_eq!(title.spans[9].style.fg, Some(Color::Red));
+        assert!(!title.spans[9].style.add_modifier.contains(Modifier::DIM));
+        assert!(!title.spans[9].style.sub_modifier.contains(Modifier::DIM));
+        assert_eq!(title.spans[10].content.as_ref(), ")");
+        assert_eq!(title.spans[10].style.fg, Some(Color::Magenta));
+    }
+
+    #[test]
+    fn spawn_request_xhigh_effort_uses_rainbow_spans() {
+        let spans = spawn_request_spans(Some(&SpawnRequestSummary {
+            model: "gpt-5.5".to_string(),
+            reasoning_effort: ReasoningEffortConfig::XHigh,
+        }));
+        let effort = &spans[4..9];
+
+        assert_eq!(
+            effort
+                .iter()
+                .map(|span| span.content.as_ref())
+                .collect::<String>(),
+            "xhigh"
+        );
+        assert!(
+            effort
+                .iter()
+                .all(|span| !span.style.add_modifier.contains(Modifier::DIM))
+        );
+        assert!(
+            effort
+                .iter()
+                .all(|span| !span.style.sub_modifier.contains(Modifier::DIM))
+        );
     }
 
     #[test]
