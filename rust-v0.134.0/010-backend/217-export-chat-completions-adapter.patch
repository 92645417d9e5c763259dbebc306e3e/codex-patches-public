diff --git a/codex-rs/codex-api/src/endpoint/mod.rs b/codex-rs/codex-api/src/endpoint/mod.rs
index 106c5d73ff..b84f983cc0 100644
--- a/codex-rs/codex-api/src/endpoint/mod.rs
+++ b/codex-rs/codex-api/src/endpoint/mod.rs
@@ -1,3 +1,4 @@
+pub(crate) mod chat_completions;
 pub(crate) mod compact;
 pub(crate) mod images;
 pub(crate) mod memories;
@@ -9,6 +10,26 @@ pub(crate) mod responses_websocket;
 pub(crate) mod search;
 mod session;
 
+pub use chat_completions::ChatCompletionChunk;
+pub use chat_completions::ChatCompletionsClient;
+pub use chat_completions::ChatCompletionsOptions;
+pub use chat_completions::ChatCompletionsRequest;
+pub use chat_completions::ChatFunctionCall;
+pub use chat_completions::ChatFunctionTool;
+pub use chat_completions::ChatMessage;
+pub use chat_completions::ChatReasoningEffort;
+pub use chat_completions::ChatResponseFormat;
+pub use chat_completions::ChatResponseFormatType;
+pub use chat_completions::ChatStreamOptions;
+pub use chat_completions::ChatThinking;
+pub use chat_completions::ChatThinkingType;
+pub use chat_completions::ChatTool;
+pub use chat_completions::ChatToolCall;
+pub use chat_completions::ChatToolCallDelta;
+pub use chat_completions::ChatToolChoice;
+pub use chat_completions::ChatToolChoiceMode;
+pub use chat_completions::ChatToolType;
+pub use chat_completions::ChatUsage;
 pub use compact::CompactClient;
 pub use images::ImagesClient;
 pub use memories::MemoriesClient;
diff --git a/codex-rs/codex-api/src/lib.rs b/codex-rs/codex-api/src/lib.rs
index 08176d8dec..29f500360a 100644
--- a/codex-rs/codex-api/src/lib.rs
+++ b/codex-rs/codex-api/src/lib.rs
@@ -41,6 +41,25 @@ pub use crate::common::WS_REQUEST_HEADER_TRACEPARENT_CLIENT_METADATA_KEY;
 pub use crate::common::WS_REQUEST_HEADER_TRACESTATE_CLIENT_METADATA_KEY;
 pub use crate::common::create_text_param_for_request;
 pub use crate::common::response_create_client_metadata;
+pub use crate::endpoint::ChatCompletionChunk;
+pub use crate::endpoint::ChatCompletionsClient;
+pub use crate::endpoint::ChatCompletionsOptions;
+pub use crate::endpoint::ChatCompletionsRequest;
+pub use crate::endpoint::ChatFunctionCall;
+pub use crate::endpoint::ChatFunctionTool;
+pub use crate::endpoint::ChatMessage;
+pub use crate::endpoint::ChatReasoningEffort;
+pub use crate::endpoint::ChatResponseFormat;
+pub use crate::endpoint::ChatResponseFormatType;
+pub use crate::endpoint::ChatStreamOptions;
+pub use crate::endpoint::ChatThinking;
+pub use crate::endpoint::ChatThinkingType;
+pub use crate::endpoint::ChatTool;
+pub use crate::endpoint::ChatToolCall;
+pub use crate::endpoint::ChatToolChoice;
+pub use crate::endpoint::ChatToolChoiceMode;
+pub use crate::endpoint::ChatToolType;
+pub use crate::endpoint::ChatUsage;
 pub use crate::endpoint::CompactClient;
 pub use crate::endpoint::ImagesClient;
 pub use crate::endpoint::MemoriesClient;
@@ -105,3 +124,5 @@ pub use crate::telemetry::SseTelemetry;
 pub use crate::telemetry::WebsocketTelemetry;
 pub use codex_protocol::protocol::RealtimeAudioFrame;
 pub use codex_protocol::protocol::RealtimeEvent;
+pub use sse::ChatToolAlias;
+pub use sse::spawn_chat_completions_stream;
diff --git a/codex-rs/codex-api/src/sse/mod.rs b/codex-rs/codex-api/src/sse/mod.rs
index 441078bdcb..b2bcaae25a 100644
--- a/codex-rs/codex-api/src/sse/mod.rs
+++ b/codex-rs/codex-api/src/sse/mod.rs
@@ -1,5 +1,8 @@
+pub(crate) mod chat_completions;
 pub(crate) mod responses;
 
+pub use chat_completions::ChatToolAlias;
+pub use chat_completions::spawn_chat_completions_stream;
 pub(crate) use responses::ResponsesStreamEvent;
 pub(crate) use responses::process_responses_event;
 pub use responses::spawn_response_stream;
