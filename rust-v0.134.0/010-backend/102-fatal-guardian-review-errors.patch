diff --git a/codex-rs/core/src/guardian/mod.rs b/codex-rs/core/src/guardian/mod.rs
index 058c19d008..e502c3a972 100644
--- a/codex-rs/core/src/guardian/mod.rs
+++ b/codex-rs/core/src/guardian/mod.rs
@@ -29,6 +29,7 @@ pub(crate) use approval_request::GuardianNetworkAccessTrigger;
 #[cfg(test)]
 pub(crate) use approval_request::guardian_approval_request_to_json;
 pub(crate) use review::guardian_rejection_message;
+pub(crate) use review::guardian_rejection_message_from_rejection;
 pub(crate) use review::guardian_timeout_message;
 pub(crate) use review::is_guardian_reviewer_source;
 pub(crate) use review::new_guardian_review_id;
@@ -39,6 +40,7 @@ pub(crate) use review::review_approval_request;
 pub(crate) use review::review_approval_request_with_cancel;
 pub(crate) use review::routes_approval_to_guardian;
 pub(crate) use review::spawn_approval_request_review;
+pub(crate) use review::take_guardian_rejection;
 pub(crate) use review_session::GuardianReviewSessionManager;
 
 pub(crate) const GUARDIAN_REVIEW_TIMEOUT: Duration = Duration::from_secs(90);
@@ -69,6 +71,7 @@ pub(crate) struct GuardianAssessment {
 pub(crate) struct GuardianRejection {
     pub(crate) rationale: String,
     pub(crate) source: GuardianAssessmentDecisionSource,
+    pub(crate) fatal: bool,
 }
 
 #[derive(Debug, Default)]
diff --git a/codex-rs/core/src/guardian/review.rs b/codex-rs/core/src/guardian/review.rs
index 7df7a96928..f592d1de7f 100644
--- a/codex-rs/core/src/guardian/review.rs
+++ b/codex-rs/core/src/guardian/review.rs
@@ -60,8 +60,11 @@ pub(crate) fn new_guardian_review_id() -> String {
     uuid::Uuid::new_v4().to_string()
 }
 
-pub(crate) async fn guardian_rejection_message(session: &Session, review_id: &str) -> String {
-    let rejection = session
+pub(crate) async fn take_guardian_rejection(
+    session: &Session,
+    review_id: &str,
+) -> GuardianRejection {
+    session
         .services
         .guardian_rejections
         .lock()
@@ -71,7 +74,16 @@ pub(crate) async fn guardian_rejection_message(session: &Session, review_id: &st
         .unwrap_or_else(|| GuardianRejection {
             rationale: "Auto-reviewer denied the action without a specific rationale.".to_string(),
             source: GuardianAssessmentDecisionSource::Agent,
-        });
+            fatal: false,
+        })
+}
+
+pub(crate) async fn guardian_rejection_message(session: &Session, review_id: &str) -> String {
+    let rejection = take_guardian_rejection(session, review_id).await;
+    guardian_rejection_message_from_rejection(rejection)
+}
+
+pub(crate) fn guardian_rejection_message_from_rejection(rejection: GuardianRejection) -> String {
     match rejection.source {
         GuardianAssessmentDecisionSource::Agent => format!(
             "This action was rejected due to unacceptable risk.\nReason: {}\n{}",
@@ -502,6 +514,7 @@ async fn run_guardian_review(
             let rejection = GuardianRejection {
                 rationale: assessment.rationale.clone(),
                 source: GuardianAssessmentDecisionSource::Agent,
+                fatal: !count_denial_for_circuit_breaker,
             };
             rationales.insert(review_id.clone(), rejection);
         }
@@ -533,6 +546,11 @@ async fn run_guardian_review(
 
     if approved {
         ReviewDecision::Approved
+    } else if !count_denial_for_circuit_breaker {
+        session
+            .abort_turn_if_active(&assessment_turn_id, TurnAbortReason::Interrupted)
+            .await;
+        ReviewDecision::Denied
     } else {
         ReviewDecision::Denied
     }
diff --git a/codex-rs/core/src/mcp_tool_call_tests.rs b/codex-rs/core/src/mcp_tool_call_tests.rs
index f9984c7f2d..49cc3bffbb 100644
--- a/codex-rs/core/src/mcp_tool_call_tests.rs
+++ b/codex-rs/core/src/mcp_tool_call_tests.rs
@@ -1675,6 +1675,7 @@ async fn guardian_review_decision_maps_to_mcp_tool_decision() {
         crate::guardian::GuardianRejection {
             rationale: "too risky".to_string(),
             source: codex_protocol::protocol::GuardianAssessmentDecisionSource::Agent,
+            fatal: false,
         },
     );
     let denial = mcp_tool_approval_decision_from_guardian(
diff --git a/codex-rs/core/src/tools/events.rs b/codex-rs/core/src/tools/events.rs
index 747143ff69..0c49649f5b 100644
--- a/codex-rs/core/src/tools/events.rs
+++ b/codex-rs/core/src/tools/events.rs
@@ -366,6 +366,11 @@ impl ToolEmitter {
                 let result = Err(FunctionCallError::RespondToModel(response));
                 (event, result)
             }
+            Err(ToolError::Codex(CodexErr::Fatal(message))) => {
+                let event = ToolEventStage::Failure(ToolEventFailure::Message(message.clone()));
+                let result = Err(FunctionCallError::Fatal(message));
+                (event, result)
+            }
             Err(ToolError::Codex(err)) => {
                 let message = format!("execution error: {err:?}");
                 let event = ToolEventStage::Failure(ToolEventFailure::Message(message.clone()));
diff --git a/codex-rs/core/src/tools/orchestrator.rs b/codex-rs/core/src/tools/orchestrator.rs
index deb9ae596e..24f83bb3d8 100644
--- a/codex-rs/core/src/tools/orchestrator.rs
+++ b/codex-rs/core/src/tools/orchestrator.rs
@@ -6,10 +6,11 @@ simple sequence for any ToolRuntime: approval → select sandbox → attempt →
 retry with an escalated sandbox strategy on denial (no re‑approval thanks to
 caching).
 */
-use crate::guardian::guardian_rejection_message;
+use crate::guardian::guardian_rejection_message_from_rejection;
 use crate::guardian::guardian_timeout_message;
 use crate::guardian::new_guardian_review_id;
 use crate::guardian::routes_approval_to_guardian;
+use crate::guardian::take_guardian_rejection;
 use crate::hook_runtime::run_permission_request_hooks;
 use crate::network_policy_decision::network_approval_context_from_payload;
 use crate::tools::flat_tool_name;
@@ -463,7 +464,12 @@ impl ToolOrchestrator {
         match decision {
             ReviewDecision::Denied | ReviewDecision::Abort => {
                 let reason = if let Some(review_id) = guardian_review_id {
-                    guardian_rejection_message(tool_ctx.session.as_ref(), review_id).await
+                    let rejection =
+                        take_guardian_rejection(tool_ctx.session.as_ref(), review_id).await;
+                    if rejection.fatal {
+                        return Err(ToolError::Codex(CodexErr::Fatal(rejection.rationale)));
+                    }
+                    guardian_rejection_message_from_rejection(rejection)
                 } else {
                     "rejected by user".to_string()
                 };
