diff --git a/codex-rs/app-server-protocol/schema/json/ServerNotification.json b/codex-rs/app-server-protocol/schema/json/ServerNotification.json
index 2d75c3feee..ce8e8b4e5e 100644
--- a/codex-rs/app-server-protocol/schema/json/ServerNotification.json
+++ b/codex-rs/app-server-protocol/schema/json/ServerNotification.json
@@ -2435,6 +2435,29 @@
       ],
       "type": "object"
     },
+    "ModelWebsocketStatusNotification": {
+      "properties": {
+        "connected": {
+          "type": "boolean"
+        },
+        "rttMs": {
+          "format": "uint64",
+          "minimum": 0.0,
+          "type": [
+            "integer",
+            "null"
+          ]
+        },
+        "threadId": {
+          "type": "string"
+        }
+      },
+      "required": [
+        "connected",
+        "threadId"
+      ],
+      "type": "object"
+    },
     "NetworkAccess": {
       "enum": [
         "restricted",
@@ -6229,6 +6252,26 @@
       "title": "Model/verificationNotification",
       "type": "object"
     },
+    {
+      "properties": {
+        "method": {
+          "enum": [
+            "model/websocketStatus"
+          ],
+          "title": "Model/websocketStatusNotificationMethod",
+          "type": "string"
+        },
+        "params": {
+          "$ref": "#/definitions/ModelWebsocketStatusNotification"
+        }
+      },
+      "required": [
+        "method",
+        "params"
+      ],
+      "title": "Model/websocketStatusNotification",
+      "type": "object"
+    },
     {
       "properties": {
         "method": {
diff --git a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json
index 3e9245fc2b..cd926040ff 100644
--- a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json
+++ b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json
@@ -4918,6 +4918,26 @@
           "title": "Model/verificationNotification",
           "type": "object"
         },
+        {
+          "properties": {
+            "method": {
+              "enum": [
+                "model/websocketStatus"
+              ],
+              "title": "Model/websocketStatusNotificationMethod",
+              "type": "string"
+            },
+            "params": {
+              "$ref": "#/definitions/v2/ModelWebsocketStatusNotification"
+            }
+          },
+          "required": [
+            "method",
+            "params"
+          ],
+          "title": "Model/websocketStatusNotification",
+          "type": "object"
+        },
         {
           "properties": {
             "method": {
@@ -11687,6 +11707,31 @@
         "title": "ModelVerificationNotification",
         "type": "object"
       },
+      "ModelWebsocketStatusNotification": {
+        "$schema": "http://json-schema.org/draft-07/schema#",
+        "properties": {
+          "connected": {
+            "type": "boolean"
+          },
+          "rttMs": {
+            "format": "uint64",
+            "minimum": 0.0,
+            "type": [
+              "integer",
+              "null"
+            ]
+          },
+          "threadId": {
+            "type": "string"
+          }
+        },
+        "required": [
+          "connected",
+          "threadId"
+        ],
+        "title": "ModelWebsocketStatusNotification",
+        "type": "object"
+      },
       "NetworkAccess": {
         "enum": [
           "restricted",
diff --git a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json
index f68b2bdce6..84d9b8fdb0 100644
--- a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json
+++ b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json
@@ -8195,6 +8195,31 @@
       "title": "ModelVerificationNotification",
       "type": "object"
     },
+    "ModelWebsocketStatusNotification": {
+      "$schema": "http://json-schema.org/draft-07/schema#",
+      "properties": {
+        "connected": {
+          "type": "boolean"
+        },
+        "rttMs": {
+          "format": "uint64",
+          "minimum": 0.0,
+          "type": [
+            "integer",
+            "null"
+          ]
+        },
+        "threadId": {
+          "type": "string"
+        }
+      },
+      "required": [
+        "connected",
+        "threadId"
+      ],
+      "title": "ModelWebsocketStatusNotification",
+      "type": "object"
+    },
     "NetworkAccess": {
       "enum": [
         "restricted",
@@ -12175,6 +12200,26 @@
           "title": "Model/verificationNotification",
           "type": "object"
         },
+        {
+          "properties": {
+            "method": {
+              "enum": [
+                "model/websocketStatus"
+              ],
+              "title": "Model/websocketStatusNotificationMethod",
+              "type": "string"
+            },
+            "params": {
+              "$ref": "#/definitions/ModelWebsocketStatusNotification"
+            }
+          },
+          "required": [
+            "method",
+            "params"
+          ],
+          "title": "Model/websocketStatusNotification",
+          "type": "object"
+        },
         {
           "properties": {
             "method": {
diff --git a/codex-rs/app-server-protocol/schema/typescript/ServerNotification.ts b/codex-rs/app-server-protocol/schema/typescript/ServerNotification.ts
index 3ed710efc0..c36c73f570 100644
--- a/codex-rs/app-server-protocol/schema/typescript/ServerNotification.ts
+++ b/codex-rs/app-server-protocol/schema/typescript/ServerNotification.ts
@@ -30,6 +30,7 @@ import type { McpServerStatusUpdatedNotification } from "./v2/McpServerStatusUpd
 import type { McpToolCallProgressNotification } from "./v2/McpToolCallProgressNotification";
 import type { ModelReroutedNotification } from "./v2/ModelReroutedNotification";
 import type { ModelVerificationNotification } from "./v2/ModelVerificationNotification";
+import type { ModelWebsocketStatusNotification } from "./v2/ModelWebsocketStatusNotification";
 import type { PlanDeltaNotification } from "./v2/PlanDeltaNotification";
 import type { ProcessExitedNotification } from "./v2/ProcessExitedNotification";
 import type { ProcessOutputDeltaNotification } from "./v2/ProcessOutputDeltaNotification";
@@ -70,4 +71,4 @@ import type { WindowsWorldWritableWarningNotification } from "./v2/WindowsWorldW
 /**
  * Notification sent from the server to the client.
  */
-export type ServerNotification = { "method": "error", "params": ErrorNotification } | { "method": "thread/started", "params": ThreadStartedNotification } | { "method": "thread/status/changed", "params": ThreadStatusChangedNotification } | { "method": "thread/archived", "params": ThreadArchivedNotification } | { "method": "thread/unarchived", "params": ThreadUnarchivedNotification } | { "method": "thread/closed", "params": ThreadClosedNotification } | { "method": "skills/changed", "params": SkillsChangedNotification } | { "method": "thread/name/updated", "params": ThreadNameUpdatedNotification } | { "method": "thread/goal/updated", "params": ThreadGoalUpdatedNotification } | { "method": "thread/goal/cleared", "params": ThreadGoalClearedNotification } | { "method": "thread/settings/updated", "params": ThreadSettingsUpdatedNotification } | { "method": "thread/tokenUsage/updated", "params": ThreadTokenUsageUpdatedNotification } | { "method": "turn/started", "params": TurnStartedNotification } | { "method": "hook/started", "params": HookStartedNotification } | { "method": "turn/completed", "params": TurnCompletedNotification } | { "method": "hook/completed", "params": HookCompletedNotification } | { "method": "turn/diff/updated", "params": TurnDiffUpdatedNotification } | { "method": "turn/plan/updated", "params": TurnPlanUpdatedNotification } | { "method": "item/started", "params": ItemStartedNotification } | { "method": "item/autoApprovalReview/started", "params": ItemGuardianApprovalReviewStartedNotification } | { "method": "item/autoApprovalReview/completed", "params": ItemGuardianApprovalReviewCompletedNotification } | { "method": "item/completed", "params": ItemCompletedNotification } | { "method": "rawResponseItem/completed", "params": RawResponseItemCompletedNotification } | { "method": "item/agentMessage/delta", "params": AgentMessageDeltaNotification } | { "method": "item/plan/delta", "params": PlanDeltaNotification } | { "method": "command/exec/outputDelta", "params": CommandExecOutputDeltaNotification } | { "method": "process/outputDelta", "params": ProcessOutputDeltaNotification } | { "method": "process/exited", "params": ProcessExitedNotification } | { "method": "item/commandExecution/outputDelta", "params": CommandExecutionOutputDeltaNotification } | { "method": "item/commandExecution/terminalInteraction", "params": TerminalInteractionNotification } | { "method": "item/fileChange/outputDelta", "params": FileChangeOutputDeltaNotification } | { "method": "item/fileChange/patchUpdated", "params": FileChangePatchUpdatedNotification } | { "method": "serverRequest/resolved", "params": ServerRequestResolvedNotification } | { "method": "item/mcpToolCall/progress", "params": McpToolCallProgressNotification } | { "method": "mcpServer/oauthLogin/completed", "params": McpServerOauthLoginCompletedNotification } | { "method": "mcpServer/startupStatus/updated", "params": McpServerStatusUpdatedNotification } | { "method": "account/updated", "params": AccountUpdatedNotification } | { "method": "account/rateLimits/updated", "params": AccountRateLimitsUpdatedNotification } | { "method": "app/list/updated", "params": AppListUpdatedNotification } | { "method": "remoteControl/status/changed", "params": RemoteControlStatusChangedNotification } | { "method": "externalAgentConfig/import/completed", "params": ExternalAgentConfigImportCompletedNotification } | { "method": "fs/changed", "params": FsChangedNotification } | { "method": "item/reasoning/summaryTextDelta", "params": ReasoningSummaryTextDeltaNotification } | { "method": "item/reasoning/summaryPartAdded", "params": ReasoningSummaryPartAddedNotification } | { "method": "item/reasoning/textDelta", "params": ReasoningTextDeltaNotification } | { "method": "thread/compacted", "params": ContextCompactedNotification } | { "method": "model/rerouted", "params": ModelReroutedNotification } | { "method": "model/verification", "params": ModelVerificationNotification } | { "method": "warning", "params": WarningNotification } | { "method": "guardianWarning", "params": GuardianWarningNotification } | { "method": "deprecationNotice", "params": DeprecationNoticeNotification } | { "method": "configWarning", "params": ConfigWarningNotification } | { "method": "fuzzyFileSearch/sessionUpdated", "params": FuzzyFileSearchSessionUpdatedNotification } | { "method": "fuzzyFileSearch/sessionCompleted", "params": FuzzyFileSearchSessionCompletedNotification } | { "method": "thread/realtime/started", "params": ThreadRealtimeStartedNotification } | { "method": "thread/realtime/itemAdded", "params": ThreadRealtimeItemAddedNotification } | { "method": "thread/realtime/transcript/delta", "params": ThreadRealtimeTranscriptDeltaNotification } | { "method": "thread/realtime/transcript/done", "params": ThreadRealtimeTranscriptDoneNotification } | { "method": "thread/realtime/outputAudio/delta", "params": ThreadRealtimeOutputAudioDeltaNotification } | { "method": "thread/realtime/sdp", "params": ThreadRealtimeSdpNotification } | { "method": "thread/realtime/error", "params": ThreadRealtimeErrorNotification } | { "method": "thread/realtime/closed", "params": ThreadRealtimeClosedNotification } | { "method": "windows/worldWritableWarning", "params": WindowsWorldWritableWarningNotification } | { "method": "windowsSandbox/setupCompleted", "params": WindowsSandboxSetupCompletedNotification } | { "method": "account/login/completed", "params": AccountLoginCompletedNotification };
+export type ServerNotification = { "method": "error", "params": ErrorNotification } | { "method": "thread/started", "params": ThreadStartedNotification } | { "method": "thread/status/changed", "params": ThreadStatusChangedNotification } | { "method": "thread/archived", "params": ThreadArchivedNotification } | { "method": "thread/unarchived", "params": ThreadUnarchivedNotification } | { "method": "thread/closed", "params": ThreadClosedNotification } | { "method": "skills/changed", "params": SkillsChangedNotification } | { "method": "thread/name/updated", "params": ThreadNameUpdatedNotification } | { "method": "thread/goal/updated", "params": ThreadGoalUpdatedNotification } | { "method": "thread/goal/cleared", "params": ThreadGoalClearedNotification } | { "method": "thread/settings/updated", "params": ThreadSettingsUpdatedNotification } | { "method": "thread/tokenUsage/updated", "params": ThreadTokenUsageUpdatedNotification } | { "method": "turn/started", "params": TurnStartedNotification } | { "method": "hook/started", "params": HookStartedNotification } | { "method": "turn/completed", "params": TurnCompletedNotification } | { "method": "hook/completed", "params": HookCompletedNotification } | { "method": "turn/diff/updated", "params": TurnDiffUpdatedNotification } | { "method": "turn/plan/updated", "params": TurnPlanUpdatedNotification } | { "method": "item/started", "params": ItemStartedNotification } | { "method": "item/autoApprovalReview/started", "params": ItemGuardianApprovalReviewStartedNotification } | { "method": "item/autoApprovalReview/completed", "params": ItemGuardianApprovalReviewCompletedNotification } | { "method": "item/completed", "params": ItemCompletedNotification } | { "method": "rawResponseItem/completed", "params": RawResponseItemCompletedNotification } | { "method": "item/agentMessage/delta", "params": AgentMessageDeltaNotification } | { "method": "item/plan/delta", "params": PlanDeltaNotification } | { "method": "command/exec/outputDelta", "params": CommandExecOutputDeltaNotification } | { "method": "process/outputDelta", "params": ProcessOutputDeltaNotification } | { "method": "process/exited", "params": ProcessExitedNotification } | { "method": "item/commandExecution/outputDelta", "params": CommandExecutionOutputDeltaNotification } | { "method": "item/commandExecution/terminalInteraction", "params": TerminalInteractionNotification } | { "method": "item/fileChange/outputDelta", "params": FileChangeOutputDeltaNotification } | { "method": "item/fileChange/patchUpdated", "params": FileChangePatchUpdatedNotification } | { "method": "serverRequest/resolved", "params": ServerRequestResolvedNotification } | { "method": "item/mcpToolCall/progress", "params": McpToolCallProgressNotification } | { "method": "mcpServer/oauthLogin/completed", "params": McpServerOauthLoginCompletedNotification } | { "method": "mcpServer/startupStatus/updated", "params": McpServerStatusUpdatedNotification } | { "method": "account/updated", "params": AccountUpdatedNotification } | { "method": "account/rateLimits/updated", "params": AccountRateLimitsUpdatedNotification } | { "method": "app/list/updated", "params": AppListUpdatedNotification } | { "method": "remoteControl/status/changed", "params": RemoteControlStatusChangedNotification } | { "method": "externalAgentConfig/import/completed", "params": ExternalAgentConfigImportCompletedNotification } | { "method": "fs/changed", "params": FsChangedNotification } | { "method": "item/reasoning/summaryTextDelta", "params": ReasoningSummaryTextDeltaNotification } | { "method": "item/reasoning/summaryPartAdded", "params": ReasoningSummaryPartAddedNotification } | { "method": "item/reasoning/textDelta", "params": ReasoningTextDeltaNotification } | { "method": "thread/compacted", "params": ContextCompactedNotification } | { "method": "model/rerouted", "params": ModelReroutedNotification } | { "method": "model/verification", "params": ModelVerificationNotification } | { "method": "model/websocketStatus", "params": ModelWebsocketStatusNotification } | { "method": "warning", "params": WarningNotification } | { "method": "guardianWarning", "params": GuardianWarningNotification } | { "method": "deprecationNotice", "params": DeprecationNoticeNotification } | { "method": "configWarning", "params": ConfigWarningNotification } | { "method": "fuzzyFileSearch/sessionUpdated", "params": FuzzyFileSearchSessionUpdatedNotification } | { "method": "fuzzyFileSearch/sessionCompleted", "params": FuzzyFileSearchSessionCompletedNotification } | { "method": "thread/realtime/started", "params": ThreadRealtimeStartedNotification } | { "method": "thread/realtime/itemAdded", "params": ThreadRealtimeItemAddedNotification } | { "method": "thread/realtime/transcript/delta", "params": ThreadRealtimeTranscriptDeltaNotification } | { "method": "thread/realtime/transcript/done", "params": ThreadRealtimeTranscriptDoneNotification } | { "method": "thread/realtime/outputAudio/delta", "params": ThreadRealtimeOutputAudioDeltaNotification } | { "method": "thread/realtime/sdp", "params": ThreadRealtimeSdpNotification } | { "method": "thread/realtime/error", "params": ThreadRealtimeErrorNotification } | { "method": "thread/realtime/closed", "params": ThreadRealtimeClosedNotification } | { "method": "windows/worldWritableWarning", "params": WindowsWorldWritableWarningNotification } | { "method": "windowsSandbox/setupCompleted", "params": WindowsSandboxSetupCompletedNotification } | { "method": "account/login/completed", "params": AccountLoginCompletedNotification };
diff --git a/codex-rs/app-server-protocol/schema/typescript/v2/index.ts b/codex-rs/app-server-protocol/schema/typescript/v2/index.ts
index 490e094cd0..36988b3010 100644
--- a/codex-rs/app-server-protocol/schema/typescript/v2/index.ts
+++ b/codex-rs/app-server-protocol/schema/typescript/v2/index.ts
@@ -243,6 +243,7 @@ export type { ModelServiceTier } from "./ModelServiceTier";
 export type { ModelUpgradeInfo } from "./ModelUpgradeInfo";
 export type { ModelVerification } from "./ModelVerification";
 export type { ModelVerificationNotification } from "./ModelVerificationNotification";
+export type { ModelWebsocketStatusNotification } from "./ModelWebsocketStatusNotification";
 export type { NetworkAccess } from "./NetworkAccess";
 export type { NetworkApprovalContext } from "./NetworkApprovalContext";
 export type { NetworkApprovalProtocol } from "./NetworkApprovalProtocol";
diff --git a/codex-rs/app-server-protocol/src/protocol/common.rs b/codex-rs/app-server-protocol/src/protocol/common.rs
index af633a544b..58035d7b1a 100644
--- a/codex-rs/app-server-protocol/src/protocol/common.rs
+++ b/codex-rs/app-server-protocol/src/protocol/common.rs
@@ -1531,6 +1531,7 @@ server_notification_definitions! {
     ContextCompacted => "thread/compacted" (v2::ContextCompactedNotification),
     ModelRerouted => "model/rerouted" (v2::ModelReroutedNotification),
     ModelVerification => "model/verification" (v2::ModelVerificationNotification),
+    ModelWebsocketStatus => "model/websocketStatus" (v2::ModelWebsocketStatusNotification),
     Warning => "warning" (v2::WarningNotification),
     GuardianWarning => "guardianWarning" (v2::GuardianWarningNotification),
     DeprecationNotice => "deprecationNotice" (v2::DeprecationNoticeNotification),
diff --git a/codex-rs/app-server-protocol/src/protocol/v2/model.rs b/codex-rs/app-server-protocol/src/protocol/v2/model.rs
index 7f97d791a6..8c05057727 100644
--- a/codex-rs/app-server-protocol/src/protocol/v2/model.rs
+++ b/codex-rs/app-server-protocol/src/protocol/v2/model.rs
@@ -152,3 +152,12 @@ pub struct ModelVerificationNotification {
     pub turn_id: String,
     pub verifications: Vec<ModelVerification>,
 }
+
+#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, JsonSchema, TS)]
+#[serde(rename_all = "camelCase")]
+#[ts(export_to = "v2/")]
+pub struct ModelWebsocketStatusNotification {
+    pub thread_id: String,
+    pub connected: bool,
+    pub rtt_ms: Option<u64>,
+}
diff --git a/codex-rs/app-server/src/bespoke_event_handling.rs b/codex-rs/app-server/src/bespoke_event_handling.rs
index 5f784ea23a..695ecfe865 100644
--- a/codex-rs/app-server/src/bespoke_event_handling.rs
+++ b/codex-rs/app-server/src/bespoke_event_handling.rs
@@ -41,6 +41,7 @@ use codex_app_server_protocol::McpServerStartupState;
 use codex_app_server_protocol::McpServerStatusUpdatedNotification;
 use codex_app_server_protocol::ModelReroutedNotification;
 use codex_app_server_protocol::ModelVerificationNotification;
+use codex_app_server_protocol::ModelWebsocketStatusNotification;
 use codex_app_server_protocol::NetworkApprovalContext as V2NetworkApprovalContext;
 use codex_app_server_protocol::NetworkPolicyAmendment as V2NetworkPolicyAmendment;
 use codex_app_server_protocol::NetworkPolicyRuleAction as V2NetworkPolicyRuleAction;
@@ -337,6 +338,16 @@ pub(crate) async fn apply_bespoke_event_handling(
                 .send_server_notification(ServerNotification::ModelVerification(notification))
                 .await;
         }
+        EventMsg::ModelWebsocketStatus(event) => {
+            let notification = ModelWebsocketStatusNotification {
+                thread_id: event.thread_id.to_string(),
+                connected: event.connected,
+                rtt_ms: event.rtt_ms,
+            };
+            outgoing
+                .send_server_notification(ServerNotification::ModelWebsocketStatus(notification))
+                .await;
+        }
         EventMsg::RealtimeConversationStarted(event) => {
             let notification = ThreadRealtimeStartedNotification {
                 thread_id: conversation_id.to_string(),
diff --git a/codex-rs/codex-api/src/endpoint/responses_websocket.rs b/codex-rs/codex-api/src/endpoint/responses_websocket.rs
index f0a0019817..a82d252c9c 100644
--- a/codex-rs/codex-api/src/endpoint/responses_websocket.rs
+++ b/codex-rs/codex-api/src/endpoint/responses_websocket.rs
@@ -29,6 +29,7 @@ use tokio::sync::Mutex;
 use tokio::sync::mpsc;
 use tokio::sync::oneshot;
 use tokio::time::Instant;
+use tokio::time::MissedTickBehavior;
 use tokio_tungstenite::MaybeTlsStream;
 use tokio_tungstenite::WebSocketStream;
 use tokio_tungstenite::connect_async_tls_with_config;
@@ -62,14 +63,41 @@ enum WsCommand {
 }
 
 impl WsStream {
-    fn new(inner: WebSocketStream<MaybeTlsStream<TcpStream>>) -> Self {
+    fn new(
+        inner: WebSocketStream<MaybeTlsStream<TcpStream>>,
+        telemetry: Option<Arc<dyn WebsocketTelemetry>>,
+    ) -> Self {
         let (tx_command, mut rx_command) = mpsc::channel::<WsCommand>(32);
         let (tx_message, rx_message) = mpsc::unbounded_channel::<Result<Message, WsError>>();
 
         let pump_task = tokio::spawn(async move {
             let mut inner = inner;
+            let mut ping_interval = tokio::time::interval_at(
+                Instant::now() + WEBSOCKET_PING_INTERVAL,
+                WEBSOCKET_PING_INTERVAL,
+            );
+            ping_interval.set_missed_tick_behavior(MissedTickBehavior::Delay);
+            let measure_ping = telemetry
+                .as_ref()
+                .is_some_and(|t| t.should_measure_ws_rtt());
+            let mut next_ping_id: u64 = 0;
+            let mut pending_ping: Option<(Vec<u8>, Instant)> = None;
             loop {
                 tokio::select! {
+                    _ = ping_interval.tick(), if measure_ping => {
+                        let payload = next_ping_id.to_be_bytes().to_vec();
+                        next_ping_id = next_ping_id.wrapping_add(1);
+                        match inner.send(Message::Ping(payload.clone().into())).await {
+                            Ok(()) => {
+                                pending_ping = Some((payload, Instant::now()));
+                            }
+                            Err(err) => {
+                                notify_websocket_disconnected(&telemetry);
+                                let _ = tx_message.send(Err(err));
+                                break;
+                            }
+                        }
+                    }
                     command = rx_command.recv() => {
                         let Some(command) = command else {
                             break;
@@ -78,6 +106,9 @@ impl WsStream {
                             WsCommand::Send { message, tx_result } => {
                                 let result = inner.send(message).await;
                                 let should_break = result.is_err();
+                                if should_break {
+                                    notify_websocket_disconnected(&telemetry);
+                                }
                                 let _ = tx_result.send(result);
                                 if should_break {
                                     break;
@@ -87,16 +118,25 @@ impl WsStream {
                     }
                     message = inner.next() => {
                         let Some(message) = message else {
+                            notify_websocket_disconnected(&telemetry);
                             break;
                         };
                         match message {
                             Ok(Message::Ping(payload)) => {
                                 if let Err(err) = inner.send(Message::Pong(payload)).await {
+                                    notify_websocket_disconnected(&telemetry);
                                     let _ = tx_message.send(Err(err));
                                     break;
                                 }
                             }
-                            Ok(Message::Pong(_)) => {}
+                            Ok(Message::Pong(payload)) => {
+                                if let Some((pending_payload, sent_at)) = pending_ping.take()
+                                    && payload.as_ref() == pending_payload.as_slice()
+                                    && let Some(t) = telemetry.as_ref()
+                                {
+                                    t.on_ws_ping_rtt(sent_at.elapsed());
+                                }
+                            }
                             Ok(message @ (Message::Text(_)
                             | Message::Binary(_)
                             | Message::Close(_)
@@ -106,10 +146,12 @@ impl WsStream {
                                     break;
                                 }
                                 if is_close {
+                                    notify_websocket_disconnected(&telemetry);
                                     break;
                                 }
                             }
                             Err(err) => {
+                                notify_websocket_disconnected(&telemetry);
                                 let _ = tx_message.send(Err(err));
                                 break;
                             }
@@ -147,6 +189,12 @@ impl WsStream {
     }
 }
 
+fn notify_websocket_disconnected(telemetry: &Option<Arc<dyn WebsocketTelemetry>>) {
+    if let Some(t) = telemetry.as_ref() {
+        t.on_ws_disconnected();
+    }
+}
+
 impl Drop for WsStream {
     fn drop(&mut self) {
         self.pump_task.abort();
@@ -157,6 +205,7 @@ const X_CODEX_TURN_STATE_HEADER: &str = "x-codex-turn-state";
 const X_MODELS_ETAG_HEADER: &str = "x-models-etag";
 const X_REASONING_INCLUDED_HEADER: &str = "x-reasoning-included";
 const OPENAI_MODEL_HEADER: &str = "openai-model";
+const WEBSOCKET_PING_INTERVAL: Duration = Duration::from_secs(5);
 const WEBSOCKET_CONNECTION_LIMIT_REACHED_CODE: &str = "websocket_connection_limit_reached";
 const WEBSOCKET_CONNECTION_LIMIT_REACHED_MESSAGE: &str = "Responses websocket connection limit reached (60 minutes). Create a new websocket connection to continue.";
 
@@ -383,7 +432,7 @@ impl ResponsesWebsocketClient {
         self.auth.add_auth_headers(&mut headers);
 
         let (stream, _status, server_reasoning_included, models_etag, server_model) =
-            connect_websocket(ws_url, headers, turn_state.clone()).await?;
+            connect_websocket(ws_url, headers, turn_state.clone(), telemetry.clone()).await?;
         Ok(ResponsesWebsocketConnection::new(
             stream,
             self.provider.stream_idle_timeout,
@@ -417,7 +466,13 @@ impl ResponsesWebsocketClient {
         self.auth.add_auth_headers(&mut headers);
 
         let (mut stream, status, reasoning_included, models_etag, server_model) =
-            connect_websocket(ws_url.clone(), headers, /*turn_state*/ None).await?;
+            connect_websocket(
+                ws_url.clone(),
+                headers,
+                /*turn_state*/ None,
+                /*telemetry*/ None,
+            )
+            .await?;
         let immediate_close = tokio::time::timeout(immediate_close_timeout, stream.next())
             .await
             .ok()
@@ -472,6 +527,7 @@ async fn connect_websocket(
     url: Url,
     headers: HeaderMap,
     turn_state: Option<Arc<OnceLock<String>>>,
+    telemetry: Option<Arc<dyn WebsocketTelemetry>>,
 ) -> Result<(WsStream, StatusCode, bool, Option<String>, Option<String>), ApiError> {
     ensure_rustls_crypto_provider();
     info!("connecting to websocket: {url}");
@@ -531,7 +587,7 @@ async fn connect_websocket(
         let _ = turn_state.set(header_value.to_string());
     }
     Ok((
-        WsStream::new(stream),
+        WsStream::new(stream, telemetry),
         response.status(),
         reasoning_included,
         models_etag,
diff --git a/codex-rs/codex-api/src/telemetry.rs b/codex-rs/codex-api/src/telemetry.rs
index 91918a65b9..b377085954 100644
--- a/codex-rs/codex-api/src/telemetry.rs
+++ b/codex-rs/codex-api/src/telemetry.rs
@@ -33,6 +33,10 @@ pub trait SseTelemetry: Send + Sync {
 
 /// Telemetry for Responses WebSocket transport.
 pub trait WebsocketTelemetry: Send + Sync {
+    fn should_measure_ws_rtt(&self) -> bool {
+        false
+    }
+
     fn on_ws_request(&self, duration: Duration, error: Option<&ApiError>, connection_reused: bool);
 
     fn on_ws_event(
@@ -40,6 +44,10 @@ pub trait WebsocketTelemetry: Send + Sync {
         result: &Result<Option<Result<Message, Error>>, ApiError>,
         duration: Duration,
     );
+
+    fn on_ws_ping_rtt(&self, duration: Duration);
+
+    fn on_ws_disconnected(&self);
 }
 
 pub(crate) trait WithStatus {
diff --git a/codex-rs/core/src/client.rs b/codex-rs/core/src/client.rs
index dacb5126c1..e93683d4fb 100644
--- a/codex-rs/core/src/client.rs
+++ b/codex-rs/core/src/client.rs
@@ -77,7 +77,10 @@ use codex_protocol::config_types::Verbosity as VerbosityConfig;
 use codex_protocol::models::ResponseItem;
 use codex_protocol::openai_models::ModelInfo;
 use codex_protocol::openai_models::ReasoningEffort as ReasoningEffortConfig;
+use codex_protocol::protocol::Event as CodexEvent;
+use codex_protocol::protocol::EventMsg;
 use codex_protocol::protocol::InternalSessionSource;
+use codex_protocol::protocol::ModelWebsocketStatusEvent;
 use codex_protocol::protocol::SessionSource;
 use codex_protocol::protocol::SubAgentSource;
 use codex_protocol::protocol::W3cTraceContext;
@@ -179,6 +182,7 @@ struct ModelClientState {
     beta_features_header: Option<String>,
     include_attestation: bool,
     attestation_provider: Option<Arc<dyn AttestationProvider>>,
+    tx_event: Option<async_channel::Sender<CodexEvent>>,
     disable_websockets: AtomicBool,
     cached_websocket_session: StdMutex<WebsocketSession>,
 }
@@ -322,6 +326,7 @@ impl ModelClient {
         include_timing_metrics: bool,
         beta_features_header: Option<String>,
         attestation_provider: Option<Arc<dyn AttestationProvider>>,
+        tx_event: Option<async_channel::Sender<CodexEvent>>,
     ) -> Self {
         let model_provider = create_model_provider(provider_info, auth_manager);
         let codex_api_key_env_enabled = model_provider
@@ -346,6 +351,7 @@ impl ModelClient {
                 beta_features_header,
                 include_attestation,
                 attestation_provider,
+                tx_event,
                 disable_websockets: AtomicBool::new(false),
                 cached_websocket_session: StdMutex::new(WebsocketSession::default()),
             }),
@@ -802,6 +808,8 @@ impl ModelClient {
             .build_websocket_headers(turn_state.as_ref(), turn_metadata_header)
             .await;
         let websocket_telemetry = ModelClientSession::build_websocket_telemetry(
+            self.state.thread_id,
+            self.state.tx_event.clone(),
             session_telemetry,
             auth_context,
             request_route_telemetry,
@@ -1501,17 +1509,22 @@ impl ModelClientSession {
 
     /// Builds telemetry for the Responses API WebSocket transport.
     fn build_websocket_telemetry(
+        thread_id: ThreadId,
+        tx_event: Option<async_channel::Sender<CodexEvent>>,
         session_telemetry: &SessionTelemetry,
         auth_context: AuthRequestTelemetryContext,
         request_route_telemetry: RequestRouteTelemetry,
         auth_env_telemetry: AuthEnvTelemetry,
     ) -> Arc<dyn WebsocketTelemetry> {
-        let telemetry = Arc::new(ApiTelemetry::new(
-            session_telemetry.clone(),
-            auth_context,
-            request_route_telemetry,
-            auth_env_telemetry,
-        ));
+        let telemetry = Arc::new(
+            ApiTelemetry::new(
+                session_telemetry.clone(),
+                auth_context,
+                request_route_telemetry,
+                auth_env_telemetry,
+            )
+            .with_model_websocket_status(thread_id, tx_event),
+        );
         let websocket_telemetry: Arc<dyn WebsocketTelemetry> = telemetry;
         websocket_telemetry
     }
@@ -1933,6 +1946,7 @@ impl PendingUnauthorizedRetry {
 #[derive(Clone, Copy, Debug, Default)]
 struct AuthRequestTelemetryContext {
     auth_mode: Option<&'static str>,
+    is_chatgpt_account: bool,
     auth_header_attached: bool,
     auth_header_name: Option<&'static str>,
     retry_after_unauthorized: bool,
@@ -1948,6 +1962,10 @@ impl AuthRequestTelemetryContext {
     ) -> Self {
         let auth_telemetry = auth_header_telemetry(api_auth);
         Self {
+            is_chatgpt_account: matches!(
+                auth_mode,
+                Some(AuthMode::Chatgpt | AuthMode::ChatgptAuthTokens)
+            ),
             auth_mode: auth_mode.map(|mode| match mode {
                 AuthMode::ApiKey => "ApiKey",
                 AuthMode::Chatgpt | AuthMode::ChatgptAuthTokens | AuthMode::AgentIdentity => {
@@ -2101,6 +2119,12 @@ struct ApiTelemetry {
     auth_context: AuthRequestTelemetryContext,
     request_route_telemetry: RequestRouteTelemetry,
     auth_env_telemetry: AuthEnvTelemetry,
+    model_websocket_status: Option<ModelWebsocketStatusSink>,
+}
+
+struct ModelWebsocketStatusSink {
+    thread_id: ThreadId,
+    tx_event: async_channel::Sender<CodexEvent>,
 }
 
 impl ApiTelemetry {
@@ -2115,6 +2139,40 @@ impl ApiTelemetry {
             auth_context,
             request_route_telemetry,
             auth_env_telemetry,
+            model_websocket_status: None,
+        }
+    }
+
+    fn with_model_websocket_status(
+        mut self,
+        thread_id: ThreadId,
+        tx_event: Option<async_channel::Sender<CodexEvent>>,
+    ) -> Self {
+        if self.auth_context.is_chatgpt_account
+            && let Some(tx_event) = tx_event
+        {
+            self.model_websocket_status = Some(ModelWebsocketStatusSink {
+                thread_id,
+                tx_event,
+            });
+        }
+        self
+    }
+
+    fn send_model_websocket_status(&self, connected: bool, rtt_ms: Option<u64>) {
+        let Some(status) = self.model_websocket_status.as_ref() else {
+            return;
+        };
+        let event = CodexEvent {
+            id: "model_websocket_status".to_string(),
+            msg: EventMsg::ModelWebsocketStatus(ModelWebsocketStatusEvent {
+                thread_id: status.thread_id,
+                connected,
+                rtt_ms,
+            }),
+        };
+        if let Err(err) = status.tx_event.try_send(event) {
+            debug!("dropping model websocket status update because channel is unavailable: {err}");
         }
     }
 }
@@ -2191,6 +2249,10 @@ impl SseTelemetry for ApiTelemetry {
 }
 
 impl WebsocketTelemetry for ApiTelemetry {
+    fn should_measure_ws_rtt(&self) -> bool {
+        self.model_websocket_status.is_some()
+    }
+
     fn on_ws_request(&self, duration: Duration, error: Option<&ApiError>, connection_reused: bool) {
         let error_message = error.map(telemetry_api_error_message);
         let status = error.and_then(api_error_http_status);
@@ -2238,6 +2300,15 @@ impl WebsocketTelemetry for ApiTelemetry {
         self.session_telemetry
             .record_websocket_event(result, duration);
     }
+
+    fn on_ws_ping_rtt(&self, duration: Duration) {
+        let rtt_ms = duration.as_millis().try_into().unwrap_or(u64::MAX);
+        self.send_model_websocket_status(/*connected*/ true, Some(rtt_ms));
+    }
+
+    fn on_ws_disconnected(&self) {
+        self.send_model_websocket_status(/*connected*/ false, /*rtt_ms*/ None);
+    }
 }
 
 #[cfg(test)]
diff --git a/codex-rs/core/src/client_tests.rs b/codex-rs/core/src/client_tests.rs
index b9d9172c83..50fd12181e 100644
--- a/codex-rs/core/src/client_tests.rs
+++ b/codex-rs/core/src/client_tests.rs
@@ -75,6 +75,7 @@ fn test_model_client(session_source: SessionSource) -> ModelClient {
         /*include_timing_metrics*/ false,
         /*beta_features_header*/ None,
         /*attestation_provider*/ None,
+        /*tx_event*/ None,
     )
 }
 
@@ -527,6 +528,7 @@ fn model_client_with_counting_attestation(
         Some(Arc::new(CountingAttestationProvider {
             calls: attestation_calls.clone(),
         })),
+        /*tx_event*/ None,
     );
     (model_client, attestation_calls)
 }
diff --git a/codex-rs/core/src/session/session.rs b/codex-rs/core/src/session/session.rs
index 6ef8aad91e..c8d707e829 100644
--- a/codex-rs/core/src/session/session.rs
+++ b/codex-rs/core/src/session/session.rs
@@ -1028,6 +1028,7 @@ impl Session {
                     config.features.enabled(Feature::RuntimeMetrics),
                     Self::build_model_client_beta_features_header(config.as_ref()),
                     attestation_provider,
+                    Some(tx_event.clone()),
                 ),
                 code_mode_service: crate::tools::code_mode::CodeModeService::new(),
                 environment_manager,
diff --git a/codex-rs/core/src/session/tests.rs b/codex-rs/core/src/session/tests.rs
index 9b244c4cd4..5dd625a4fc 100644
--- a/codex-rs/core/src/session/tests.rs
+++ b/codex-rs/core/src/session/tests.rs
@@ -432,6 +432,7 @@ fn test_model_client_session() -> crate::client::ModelClientSession {
         /*include_timing_metrics*/ false,
         /*beta_features_header*/ None,
         /*attestation_provider*/ None,
+        /*tx_event*/ None,
     )
     .new_session()
 }
@@ -4582,6 +4583,7 @@ pub(crate) async fn make_session_and_context() -> (Session, TurnContext) {
             config.features.enabled(Feature::RuntimeMetrics),
             Session::build_model_client_beta_features_header(config.as_ref()),
             /*attestation_provider*/ None,
+            /*tx_event*/ None,
         ),
         code_mode_service: crate::tools::code_mode::CodeModeService::new(),
         environment_manager: Arc::new(codex_exec_server::EnvironmentManager::default_for_tests()),
@@ -6413,6 +6415,7 @@ where
             config.features.enabled(Feature::RuntimeMetrics),
             Session::build_model_client_beta_features_header(config.as_ref()),
             /*attestation_provider*/ None,
+            /*tx_event*/ None,
         ),
         code_mode_service: crate::tools::code_mode::CodeModeService::new(),
         environment_manager: Arc::new(codex_exec_server::EnvironmentManager::default_for_tests()),
diff --git a/codex-rs/core/src/session/turn.rs b/codex-rs/core/src/session/turn.rs
index a0e1c61d5a..4d4be66055 100644
--- a/codex-rs/core/src/session/turn.rs
+++ b/codex-rs/core/src/session/turn.rs
@@ -1344,6 +1344,7 @@ pub(super) fn realtime_text_for_event(msg: &EventMsg) -> Option<String> {
         | EventMsg::RealtimeConversationClosed(_)
         | EventMsg::ModelReroute(_)
         | EventMsg::ModelVerification(_)
+        | EventMsg::ModelWebsocketStatus(_)
         | EventMsg::ContextCompacted(_)
         | EventMsg::ThreadRolledBack(_)
         | EventMsg::TurnStarted(_)
diff --git a/codex-rs/core/tests/responses_headers.rs b/codex-rs/core/tests/responses_headers.rs
index 6f0429e644..7c134fe215 100644
--- a/codex-rs/core/tests/responses_headers.rs
+++ b/codex-rs/core/tests/responses_headers.rs
@@ -110,6 +110,7 @@ async fn responses_stream_includes_subagent_header_on_review() {
         /*include_timing_metrics*/ false,
         /*beta_features_header*/ None,
         /*attestation_provider*/ None,
+        /*tx_event*/ None,
     );
     let mut client_session = client.new_session();
 
@@ -238,6 +239,7 @@ async fn responses_stream_includes_subagent_header_on_other() {
         /*include_timing_metrics*/ false,
         /*beta_features_header*/ None,
         /*attestation_provider*/ None,
+        /*tx_event*/ None,
     );
     let mut client_session = client.new_session();
 
@@ -355,6 +357,7 @@ async fn responses_respects_model_info_overrides_from_config() {
         /*include_timing_metrics*/ false,
         /*beta_features_header*/ None,
         /*attestation_provider*/ None,
+        /*tx_event*/ None,
     );
     let mut client_session = client.new_session();
 
diff --git a/codex-rs/core/tests/suite/client.rs b/codex-rs/core/tests/suite/client.rs
index 7b07efeada..5e2047d9eb 100644
--- a/codex-rs/core/tests/suite/client.rs
+++ b/codex-rs/core/tests/suite/client.rs
@@ -907,6 +907,7 @@ async fn send_provider_auth_request(server: &MockServer, auth: ModelProviderAuth
         /*include_timing_metrics*/ false,
         /*beta_features_header*/ None,
         /*attestation_provider*/ None,
+        /*tx_event*/ None,
     );
     let mut client_session = client.new_session();
     let mut prompt = Prompt::default();
@@ -2363,6 +2364,7 @@ async fn azure_responses_request_includes_store_and_reasoning_ids() {
         /*include_timing_metrics*/ false,
         /*beta_features_header*/ None,
         /*attestation_provider*/ None,
+        /*tx_event*/ None,
     );
     let mut client_session = client.new_session();
 
diff --git a/codex-rs/core/tests/suite/client_websockets.rs b/codex-rs/core/tests/suite/client_websockets.rs
index bb0bb79060..95cc30b401 100755
--- a/codex-rs/core/tests/suite/client_websockets.rs
+++ b/codex-rs/core/tests/suite/client_websockets.rs
@@ -2160,6 +2160,7 @@ async fn websocket_harness_with_provider_options(
         runtime_metrics_enabled,
         /*beta_features_header*/ None,
         /*attestation_provider*/ None,
+        /*tx_event*/ None,
     );
 
     WebsocketTestHarness {
diff --git a/codex-rs/mcp-server/src/codex_tool_runner.rs b/codex-rs/mcp-server/src/codex_tool_runner.rs
index b6d65e95d9..89c772f838 100644
--- a/codex-rs/mcp-server/src/codex_tool_runner.rs
+++ b/codex-rs/mcp-server/src/codex_tool_runner.rs
@@ -376,6 +376,7 @@ async fn run_codex_tool_session_inner(
                     | EventMsg::DynamicToolCallResponse(_)
                     | EventMsg::ContextCompacted(_)
                     | EventMsg::ModelReroute(_)
+                    | EventMsg::ModelWebsocketStatus(_)
                     | EventMsg::ThreadRolledBack(_)
                     | EventMsg::CollabAgentSpawnBegin(_)
                     | EventMsg::CollabAgentSpawnEnd(_)
diff --git a/codex-rs/memories/write/src/runtime.rs b/codex-rs/memories/write/src/runtime.rs
index e1a0fae436..08f9d15fbd 100644
--- a/codex-rs/memories/write/src/runtime.rs
+++ b/codex-rs/memories/write/src/runtime.rs
@@ -184,6 +184,7 @@ impl MemoryStartupContext {
             config.features.enabled(Feature::RuntimeMetrics),
             /*beta_features_header*/ None,
             /*attestation_provider*/ None,
+            /*tx_event*/ None,
         );
 
         let mut client_session = model_client.new_session();
diff --git a/codex-rs/protocol/src/protocol.rs b/codex-rs/protocol/src/protocol.rs
index 5ae2faab99..4e4ea7eedc 100644
--- a/codex-rs/protocol/src/protocol.rs
+++ b/codex-rs/protocol/src/protocol.rs
@@ -1167,6 +1167,9 @@ pub enum EventMsg {
     /// Backend recommends additional account verification for this turn.
     ModelVerification(ModelVerificationEvent),
 
+    /// Responses WebSocket connection health for the active model transport.
+    ModelWebsocketStatus(ModelWebsocketStatusEvent),
+
     /// Conversation history was compacted (either automatically or manually).
     ContextCompacted(ContextCompactedEvent),
 
@@ -1830,6 +1833,13 @@ pub struct ModelVerificationEvent {
     pub verifications: Vec<ModelVerification>,
 }
 
+#[derive(Debug, Clone, Deserialize, Serialize, PartialEq, Eq, JsonSchema, TS)]
+pub struct ModelWebsocketStatusEvent {
+    pub thread_id: ThreadId,
+    pub connected: bool,
+    pub rtt_ms: Option<u64>,
+}
+
 #[derive(Debug, Clone, Deserialize, Serialize, JsonSchema, TS)]
 pub struct ContextCompactedEvent;
 
diff --git a/codex-rs/rollout-trace/src/protocol_event.rs b/codex-rs/rollout-trace/src/protocol_event.rs
index a862af116c..1bdb846c8c 100644
--- a/codex-rs/rollout-trace/src/protocol_event.rs
+++ b/codex-rs/rollout-trace/src/protocol_event.rs
@@ -224,6 +224,7 @@ pub(crate) fn tool_runtime_trace_event(event: &EventMsg) -> Option<ToolRuntimeTr
         | EventMsg::RealtimeConversationSdp(_)
         | EventMsg::ModelReroute(_)
         | EventMsg::ModelVerification(_)
+        | EventMsg::ModelWebsocketStatus(_)
         | EventMsg::ContextCompacted(_)
         | EventMsg::ThreadRolledBack(_)
         | EventMsg::ThreadGoalUpdated(_)
@@ -297,6 +298,7 @@ pub(crate) fn wrapped_protocol_event_type(event: &EventMsg) -> Option<&'static s
         | EventMsg::RealtimeConversationSdp(_)
         | EventMsg::ModelReroute(_)
         | EventMsg::ModelVerification(_)
+        | EventMsg::ModelWebsocketStatus(_)
         | EventMsg::ContextCompacted(_)
         | EventMsg::ThreadSettingsApplied(_)
         | EventMsg::TokenCount(_)
diff --git a/codex-rs/rollout/src/policy.rs b/codex-rs/rollout/src/policy.rs
index b6dbba98f8..84d32bb002 100644
--- a/codex-rs/rollout/src/policy.rs
+++ b/codex-rs/rollout/src/policy.rs
@@ -180,6 +180,7 @@ fn event_msg_persistence_mode(ev: &EventMsg) -> Option<EventPersistenceMode> {
         | EventMsg::RealtimeConversationClosed(_)
         | EventMsg::ModelReroute(_)
         | EventMsg::ModelVerification(_)
+        | EventMsg::ModelWebsocketStatus(_)
         | EventMsg::AgentReasoningSectionBreak(_)
         | EventMsg::RawResponseItem(_)
         | EventMsg::SessionConfigured(_)
diff --git a/codex-rs/tui/src/app/app_server_event_targets.rs b/codex-rs/tui/src/app/app_server_event_targets.rs
index 0b2143b429..5b19fdb02c 100644
--- a/codex-rs/tui/src/app/app_server_event_targets.rs
+++ b/codex-rs/tui/src/app/app_server_event_targets.rs
@@ -118,6 +118,9 @@ pub(super) fn server_notification_thread_target(
         ServerNotification::ModelVerification(notification) => {
             Some(notification.thread_id.as_str())
         }
+        ServerNotification::ModelWebsocketStatus(notification) => {
+            Some(notification.thread_id.as_str())
+        }
         ServerNotification::ThreadRealtimeStarted(notification) => {
             Some(notification.thread_id.as_str())
         }
diff --git a/codex-rs/tui/src/bottom_pane/status_line_setup.rs b/codex-rs/tui/src/bottom_pane/status_line_setup.rs
index 984df9ad91..09efc43fa9 100644
--- a/codex-rs/tui/src/bottom_pane/status_line_setup.rs
+++ b/codex-rs/tui/src/bottom_pane/status_line_setup.rs
@@ -134,6 +134,9 @@ pub(crate) enum StatusLineItem {
     /// Active ChatGPT account (if any).
     Account,
 
+    /// Model WebSocket RTT or disconnect state.
+    RemoteConnection,
+
     /// Current thread title (if set by user).
     ThreadTitle,
 
@@ -182,6 +185,9 @@ impl StatusLineItem {
             StatusLineItem::FastMode => "Whether Fast mode is currently active",
             StatusLineItem::RawOutput => "Whether raw scrollback mode is active",
             StatusLineItem::Account => "Active ChatGPT account (omitted when using API key auth)",
+            StatusLineItem::RemoteConnection => {
+                "Model WebSocket RTT or disconnect state (omitted when unavailable)"
+            }
             StatusLineItem::ThreadTitle => {
                 "Current thread title, or thread identifier when unnamed"
             }
@@ -216,6 +222,7 @@ impl StatusLineItem {
             StatusLineItem::FastMode => StatusSurfacePreviewItem::FastMode,
             StatusLineItem::RawOutput => StatusSurfacePreviewItem::RawOutput,
             StatusLineItem::Account => StatusSurfacePreviewItem::Account,
+            StatusLineItem::RemoteConnection => StatusSurfacePreviewItem::RemoteConnection,
             StatusLineItem::ThreadTitle => StatusSurfacePreviewItem::ThreadTitle,
             StatusLineItem::TaskProgress => StatusSurfacePreviewItem::TaskProgress,
         }
diff --git a/codex-rs/tui/src/bottom_pane/status_line_style.rs b/codex-rs/tui/src/bottom_pane/status_line_style.rs
index bd8a31144d..08f94a5ed9 100644
--- a/codex-rs/tui/src/bottom_pane/status_line_style.rs
+++ b/codex-rs/tui/src/bottom_pane/status_line_style.rs
@@ -40,7 +40,7 @@ impl StatusLineAccent {
             StatusLineItem::GitBranch
             | StatusLineItem::PullRequestNumber
             | StatusLineItem::BranchChanges => Self::Branch,
-            StatusLineItem::Status => Self::State,
+            StatusLineItem::Status | StatusLineItem::RemoteConnection => Self::State,
             StatusLineItem::ContextRemaining
             | StatusLineItem::ContextUsed
             | StatusLineItem::ContextWindowSize
@@ -141,6 +141,10 @@ where
             spans.extend(limit_spans);
             continue;
         }
+        if item == StatusLineItem::RemoteConnection && line.to_string() == "X" {
+            spans.push("X".red().bold());
+            continue;
+        }
         for mut span in line.spans {
             if span.style == Style::default() {
                 span.style = style;
diff --git a/codex-rs/tui/src/bottom_pane/status_surface_preview.rs b/codex-rs/tui/src/bottom_pane/status_surface_preview.rs
index 9b2761503b..b86429b62b 100644
--- a/codex-rs/tui/src/bottom_pane/status_surface_preview.rs
+++ b/codex-rs/tui/src/bottom_pane/status_surface_preview.rs
@@ -31,6 +31,7 @@ pub(crate) enum StatusSurfacePreviewItem {
     FastMode,
     RawOutput,
     Account,
+    RemoteConnection,
     Model,
     ModelWithReasoning,
     TaskProgress,
@@ -63,6 +64,7 @@ impl StatusSurfacePreviewItem {
             StatusSurfacePreviewItem::FastMode => "Fast on",
             StatusSurfacePreviewItem::RawOutput => "raw output",
             StatusSurfacePreviewItem::Account => "u***r@g***l.com Pro",
+            StatusSurfacePreviewItem::RemoteConnection => "42ms",
             StatusSurfacePreviewItem::Model => "gpt-5.2-codex",
             StatusSurfacePreviewItem::ModelWithReasoning => "gpt-5.2-codex medium",
             StatusSurfacePreviewItem::TaskProgress => "Tasks 0/0",
@@ -95,6 +97,7 @@ impl StatusSurfacePreviewItem {
             Self::FastMode,
             Self::RawOutput,
             Self::Account,
+            Self::RemoteConnection,
             Self::Model,
             Self::ModelWithReasoning,
             Self::TaskProgress,
diff --git a/codex-rs/tui/src/chatwidget.rs b/codex-rs/tui/src/chatwidget.rs
index b3c2911cfa..e3c255e170 100644
--- a/codex-rs/tui/src/chatwidget.rs
+++ b/codex-rs/tui/src/chatwidget.rs
@@ -400,6 +400,7 @@ mod settings_popups;
 mod side;
 mod status_state;
 mod windows_sandbox_prompts;
+use self::status_state::ModelWebsocketStatus;
 use self::status_state::StatusIndicatorState;
 use self::status_state::StatusState;
 use self::status_state::TerminalTitleStatusKind;
@@ -483,7 +484,8 @@ use unicode_segmentation::UnicodeSegmentation;
 const USER_SHELL_COMMAND_HELP_TITLE: &str = "Prefix a command with ! to run it locally";
 const USER_SHELL_COMMAND_HELP_HINT: &str = "Example: !ls";
 const AUTO_REVIEW_DESCRIPTION: &str = "Same workspace-write permissions as Default, but eligible `on-request` approvals are routed through the auto-reviewer subagent.";
-const DEFAULT_STATUS_LINE_ITEMS: [&str; 2] = ["model-with-reasoning", "current-dir"];
+const DEFAULT_STATUS_LINE_ITEMS: [&str; 3] =
+    ["model-with-reasoning", "remote-connection", "current-dir"];
 const MAX_AGENT_COPY_HISTORY: usize = 32;
 
 /// Common initialization parameters shared by all `ChatWidget` constructors.
@@ -559,6 +561,7 @@ pub(crate) struct ChatWidget {
     runtime_model_provider_base_url: Option<String>,
     token_info: Option<TokenUsageInfo>,
     rate_limit_snapshots_by_limit_id: BTreeMap<String, RateLimitSnapshotDisplay>,
+    model_websocket_status: Option<ModelWebsocketStatus>,
     refreshing_status_outputs: Vec<(u64, StatusHistoryHandle)>,
     next_status_refresh_request_id: u64,
     active_turn_model_context: Option<ActiveTurnModelContext>,
diff --git a/codex-rs/tui/src/chatwidget/constructor.rs b/codex-rs/tui/src/chatwidget/constructor.rs
index 536fa467d3..94d6e5b021 100644
--- a/codex-rs/tui/src/chatwidget/constructor.rs
+++ b/codex-rs/tui/src/chatwidget/constructor.rs
@@ -122,6 +122,7 @@ impl ChatWidget {
             runtime_model_provider_base_url,
             token_info: None,
             rate_limit_snapshots_by_limit_id: BTreeMap::new(),
+            model_websocket_status: None,
             refreshing_status_outputs: Vec::new(),
             next_status_refresh_request_id: 0,
             active_turn_model_context: None,
diff --git a/codex-rs/tui/src/chatwidget/protocol.rs b/codex-rs/tui/src/chatwidget/protocol.rs
index 0e3d0c6c9d..248a1ec5c2 100644
--- a/codex-rs/tui/src/chatwidget/protocol.rs
+++ b/codex-rs/tui/src/chatwidget/protocol.rs
@@ -161,6 +161,9 @@ impl ChatWidget {
             ServerNotification::ModelVerification(notification) => {
                 self.on_app_server_model_verification(&notification.verifications)
             }
+            ServerNotification::ModelWebsocketStatus(notification) => {
+                self.set_model_websocket_status(notification.connected, notification.rtt_ms)
+            }
             ServerNotification::Warning(notification) => self.on_warning(notification.message),
             ServerNotification::GuardianWarning(notification) => {
                 self.on_warning(notification.message)
diff --git a/codex-rs/tui/src/chatwidget/status_controls.rs b/codex-rs/tui/src/chatwidget/status_controls.rs
index 06f69c3f9a..107268a2da 100644
--- a/codex-rs/tui/src/chatwidget/status_controls.rs
+++ b/codex-rs/tui/src/chatwidget/status_controls.rs
@@ -96,6 +96,11 @@ impl ChatWidget {
         self.refresh_status_surfaces();
     }
 
+    pub(crate) fn set_model_websocket_status(&mut self, connected: bool, rtt_ms: Option<u64>) {
+        self.model_websocket_status = Some(ModelWebsocketStatus { connected, rtt_ms });
+        self.refresh_status_line();
+    }
+
     /// Records that status-line setup was canceled.
     ///
     /// Cancellation is intentionally side-effect free for config state; the existing configuration
diff --git a/codex-rs/tui/src/chatwidget/status_state.rs b/codex-rs/tui/src/chatwidget/status_state.rs
index 682e6adb95..ad703cba65 100644
--- a/codex-rs/tui/src/chatwidget/status_state.rs
+++ b/codex-rs/tui/src/chatwidget/status_state.rs
@@ -9,6 +9,12 @@ pub(super) struct StatusIndicatorState {
     pub(super) details_max_lines: usize,
 }
 
+#[derive(Clone, Debug, PartialEq, Eq)]
+pub(super) struct ModelWebsocketStatus {
+    pub(super) connected: bool,
+    pub(super) rtt_ms: Option<u64>,
+}
+
 impl StatusIndicatorState {
     pub(super) fn working() -> Self {
         Self {
diff --git a/codex-rs/tui/src/chatwidget/status_surfaces.rs b/codex-rs/tui/src/chatwidget/status_surfaces.rs
index d8baccc26f..6f11a8f871 100644
--- a/codex-rs/tui/src/chatwidget/status_surfaces.rs
+++ b/codex-rs/tui/src/chatwidget/status_surfaces.rs
@@ -653,6 +653,7 @@ impl ChatWidget {
             ),
             StatusLineItem::RawOutput => self.raw_output_mode().then(|| "raw output".to_string()),
             StatusLineItem::Account => self.status_line_account_display(),
+            StatusLineItem::RemoteConnection => self.status_line_remote_connection_display(),
             StatusLineItem::ThreadTitle => self.thread_name.as_ref().map_or_else(
                 || self.thread_id.map(|id| id.to_string()),
                 |name| {
@@ -790,11 +791,21 @@ impl ChatWidget {
             StatusSurfacePreviewItem::FastMode => StatusLineItem::FastMode,
             StatusSurfacePreviewItem::RawOutput => StatusLineItem::RawOutput,
             StatusSurfacePreviewItem::Account => StatusLineItem::Account,
+            StatusSurfacePreviewItem::RemoteConnection => StatusLineItem::RemoteConnection,
             StatusSurfacePreviewItem::Model => StatusLineItem::ModelName,
             StatusSurfacePreviewItem::ModelWithReasoning => StatusLineItem::ModelWithReasoning,
         };
         self.status_line_value_for_item(status_line_item)
     }
+
+    fn status_line_remote_connection_display(&self) -> Option<String> {
+        let status = self.model_websocket_status.as_ref()?;
+        if !status.connected {
+            return Some("X".to_string());
+        }
+        status.rtt_ms.map(|rtt_ms| format!("{rtt_ms}ms"))
+    }
+
     /// Resolves one configured terminal-title item into a displayable segment.
     ///
     /// Returning `None` means "omit this segment for now" so callers can keep
diff --git a/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs b/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs
index 3fbde85989..c3da6b23b1 100644
--- a/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs
+++ b/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs
@@ -2251,6 +2251,21 @@ async fn status_line_account_hides_api_key_auth() {
     assert_eq!(status_line_text(&chat), Some("Fast off".to_string()));
 }
 
+#[tokio::test]
+async fn status_line_model_websocket_renders_rtt_and_disconnect() {
+    let (mut chat, _rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+    chat.config.tui_status_line = Some(vec!["remote-connection".to_string()]);
+
+    chat.refresh_status_line();
+    assert_eq!(status_line_text(&chat), None);
+
+    chat.set_model_websocket_status(/*connected*/ true, Some(123));
+    assert_eq!(status_line_text(&chat), Some("123ms".to_string()));
+
+    chat.set_model_websocket_status(/*connected*/ false, /*rtt_ms*/ None);
+    assert_eq!(status_line_text(&chat), Some("X".to_string()));
+}
+
 #[tokio::test]
 async fn status_line_account_footer_snapshot() {
     use ratatui::Terminal;

diff --git a/codex-rs/app-server-protocol/schema/json/v2/ModelWebsocketStatusNotification.json b/codex-rs/app-server-protocol/schema/json/v2/ModelWebsocketStatusNotification.json
new file mode 100644
index 0000000000..3a7417cef5
--- /dev/null
+++ b/codex-rs/app-server-protocol/schema/json/v2/ModelWebsocketStatusNotification.json
@@ -0,0 +1,25 @@
+{
+  "$schema": "http://json-schema.org/draft-07/schema#",
+  "properties": {
+    "connected": {
+      "type": "boolean"
+    },
+    "rttMs": {
+      "format": "uint64",
+      "minimum": 0.0,
+      "type": [
+        "integer",
+        "null"
+      ]
+    },
+    "threadId": {
+      "type": "string"
+    }
+  },
+  "required": [
+    "connected",
+    "threadId"
+  ],
+  "title": "ModelWebsocketStatusNotification",
+  "type": "object"
+}
\ No newline at end of file

diff --git a/codex-rs/app-server-protocol/schema/typescript/v2/ModelWebsocketStatusNotification.ts b/codex-rs/app-server-protocol/schema/typescript/v2/ModelWebsocketStatusNotification.ts
new file mode 100644
index 0000000000..28bcc88cc3
--- /dev/null
+++ b/codex-rs/app-server-protocol/schema/typescript/v2/ModelWebsocketStatusNotification.ts
@@ -0,0 +1,5 @@
+// GENERATED CODE! DO NOT MODIFY BY HAND!
+
+// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
+
+export type ModelWebsocketStatusNotification = { threadId: string, connected: boolean, rttMs: bigint | null, };
