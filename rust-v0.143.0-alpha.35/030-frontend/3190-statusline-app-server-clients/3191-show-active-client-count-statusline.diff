diff --git a/codex-rs/tui/src/app/app_server_events.rs b/codex-rs/tui/src/app/app_server_events.rs
index b8312458bd..abbf25acfd 100644
--- a/codex-rs/tui/src/app/app_server_events.rs
+++ b/codex-rs/tui/src/app/app_server_events.rs
@@ -103,6 +103,11 @@ impl App {
                 );
                 return;
             }
+            ServerNotification::ActiveClientCountChanged(notification) => {
+                self.chat_widget
+                    .set_app_server_active_client_count(notification.active_client_count);
+                return;
+            }
             ServerNotification::ExternalAgentConfigImportCompleted(notification) => {
                 let should_report_completion =
                     app_server_client.consume_external_agent_config_import_completion();
diff --git a/codex-rs/tui/src/bottom_pane/status_line_setup.rs b/codex-rs/tui/src/bottom_pane/status_line_setup.rs
index bdd12d739f..457c9e3104 100644
--- a/codex-rs/tui/src/bottom_pane/status_line_setup.rs
+++ b/codex-rs/tui/src/bottom_pane/status_line_setup.rs
@@ -96,6 +96,9 @@ pub(crate) enum StatusLineItem {
     )]
     ResponsesStreamConnection,
 
+    /// Active app-server client count.
+    ActiveClients,
+
     /// Active permission profile or sandbox summary.
     Permissions,
 
@@ -187,6 +190,7 @@ impl StatusLineItem {
             StatusLineItem::ResponsesStreamConnection => {
                 "Current Responses stream connection latency or disconnect state (omitted when unavailable)"
             }
+            StatusLineItem::ActiveClients => "Active app-server client count",
             StatusLineItem::Permissions => {
                 "Active permission profile (omitted when not one of the built-in profiles)"
             }
@@ -251,6 +255,7 @@ impl StatusLineItem {
             StatusLineItem::ResponsesStreamConnection => {
                 StatusSurfacePreviewItem::ResponsesStreamConnection
             }
+            StatusLineItem::ActiveClients => StatusSurfacePreviewItem::ActiveClients,
             StatusLineItem::Permissions => StatusSurfacePreviewItem::Permissions,
             StatusLineItem::ApprovalMode => StatusSurfacePreviewItem::ApprovalMode,
             StatusLineItem::ContextRemaining => StatusSurfacePreviewItem::ContextRemaining,
@@ -588,6 +593,15 @@ mod tests {
         );
     }
 
+    #[test]
+    fn active_clients_is_selectable_id() {
+        assert_eq!(
+            "active-clients".parse::<StatusLineItem>(),
+            Ok(StatusLineItem::ActiveClients)
+        );
+        assert_eq!(StatusLineItem::ActiveClients.to_string(), "active-clients");
+    }
+
     #[test]
     fn parse_status_line_items_accepts_title_only_variants() {
         let items = ["run-state", "task-progress"]
diff --git a/codex-rs/tui/src/bottom_pane/status_line_style.rs b/codex-rs/tui/src/bottom_pane/status_line_style.rs
index f305a30299..765331fde9 100644
--- a/codex-rs/tui/src/bottom_pane/status_line_style.rs
+++ b/codex-rs/tui/src/bottom_pane/status_line_style.rs
@@ -49,7 +49,9 @@ impl StatusLineAccent {
             | StatusLineItem::TimeToFirstToken
             | StatusLineItem::CacheHitRate => Self::Usage,
             StatusLineItem::FiveHourLimit | StatusLineItem::WeeklyLimit => Self::Limit,
-            StatusLineItem::CodexVersion | StatusLineItem::SessionId => Self::Metadata,
+            StatusLineItem::CodexVersion
+            | StatusLineItem::SessionId
+            | StatusLineItem::ActiveClients => Self::Metadata,
             StatusLineItem::FastMode | StatusLineItem::RawOutput => Self::Mode,
             StatusLineItem::Permissions => Self::Mode,
             StatusLineItem::ApprovalMode => Self::Mode,
diff --git a/codex-rs/tui/src/bottom_pane/status_surface_preview.rs b/codex-rs/tui/src/bottom_pane/status_surface_preview.rs
index 546654d117..4cbe4a5229 100644
--- a/codex-rs/tui/src/bottom_pane/status_surface_preview.rs
+++ b/codex-rs/tui/src/bottom_pane/status_surface_preview.rs
@@ -19,6 +19,7 @@ pub(crate) enum StatusSurfacePreviewItem {
     Permissions,
     ApprovalMode,
     ResponsesStreamConnection,
+    ActiveClients,
     ContextRemaining,
     ContextUsed,
     FiveHourLimit,
@@ -57,6 +58,7 @@ impl StatusSurfacePreviewItem {
             StatusSurfacePreviewItem::Permissions => "Ask For Approval",
             StatusSurfacePreviewItem::ApprovalMode => "on-request",
             StatusSurfacePreviewItem::ResponsesStreamConnection => "42ms",
+            StatusSurfacePreviewItem::ActiveClients => "1",
             StatusSurfacePreviewItem::ContextRemaining => "0% of 0 left",
             StatusSurfacePreviewItem::ContextUsed => "Context 0% used",
             StatusSurfacePreviewItem::FiveHourLimit => "primary 0%",
@@ -95,6 +97,7 @@ impl StatusSurfacePreviewItem {
             Self::Permissions,
             Self::ApprovalMode,
             Self::ResponsesStreamConnection,
+            Self::ActiveClients,
             Self::ContextRemaining,
             Self::ContextUsed,
             Self::FiveHourLimit,
diff --git a/codex-rs/tui/src/chatwidget.rs b/codex-rs/tui/src/chatwidget.rs
index ffe89d365d..1f1823fe2c 100644
--- a/codex-rs/tui/src/chatwidget.rs
+++ b/codex-rs/tui/src/chatwidget.rs
@@ -504,8 +504,9 @@ const WORKSPACE_AUTO_REVIEW_DESCRIPTION: &str = "Workspace write sandbox. Ask th
 const FULL_ACCESS_AUTO_REVIEW_DESCRIPTION: &str =
     "Full filesystem and network access. Ask the auto-review agent before privileged actions.";
 const YOLO_DESCRIPTION: &str = "Full filesystem and network access without approval. Dangerous.";
-const DEFAULT_STATUS_LINE_ITEMS: [&str; 3] = [
+const DEFAULT_STATUS_LINE_ITEMS: [&str; 4] = [
     "model-with-reasoning",
+    "active-clients",
     "responses-stream-connection",
     "current-dir",
 ];
@@ -598,6 +599,7 @@ pub(crate) struct ChatWidget {
     has_chatgpt_account: bool,
     has_codex_backend_auth: bool,
     app_server_supports_detach: bool,
+    app_server_active_client_count: Option<usize>,
     model_catalog: Arc<ModelCatalog>,
     session_telemetry: SessionTelemetry,
     session_header: SessionHeader,
diff --git a/codex-rs/tui/src/chatwidget/constructor.rs b/codex-rs/tui/src/chatwidget/constructor.rs
index 9bef166e0b..fcbba10ab5 100644
--- a/codex-rs/tui/src/chatwidget/constructor.rs
+++ b/codex-rs/tui/src/chatwidget/constructor.rs
@@ -120,6 +120,7 @@ impl ChatWidget {
             has_chatgpt_account,
             has_codex_backend_auth,
             app_server_supports_detach: false,
+            app_server_active_client_count: None,
             model_catalog,
             session_telemetry,
             session_header: SessionHeader::new(header_model),
diff --git a/codex-rs/tui/src/chatwidget/settings.rs b/codex-rs/tui/src/chatwidget/settings.rs
index ee7872ad1b..04302b89a3 100644
--- a/codex-rs/tui/src/chatwidget/settings.rs
+++ b/codex-rs/tui/src/chatwidget/settings.rs
@@ -229,6 +229,11 @@ impl ChatWidget {
             .set_app_server_supports_detach(supports_detach);
     }
 
+    pub(crate) fn set_app_server_active_client_count(&mut self, count: usize) {
+        self.app_server_active_client_count = Some(count);
+        self.refresh_status_line();
+    }
+
     pub(crate) fn update_account_state(
         &mut self,
         status_account_display: Option<StatusAccountDisplay>,
diff --git a/codex-rs/tui/src/chatwidget/status_surfaces.rs b/codex-rs/tui/src/chatwidget/status_surfaces.rs
index 884beab4a3..768745372f 100644
--- a/codex-rs/tui/src/chatwidget/status_surfaces.rs
+++ b/codex-rs/tui/src/chatwidget/status_surfaces.rs
@@ -700,6 +700,9 @@ impl ChatWidget {
             StatusLineItem::ResponsesStreamConnection => {
                 self.status_line_responses_stream_connection_display()
             }
+            StatusLineItem::ActiveClients => self
+                .app_server_active_client_count
+                .map(|count| count.to_string()),
             StatusLineItem::Permissions => permissions_display(&self.config),
             StatusLineItem::ApprovalMode => Some(approval_mode_display(&self.config)),
             StatusLineItem::UsedTokens => {
@@ -934,6 +937,7 @@ impl ChatWidget {
             StatusSurfacePreviewItem::ResponsesStreamConnection => {
                 StatusLineItem::ResponsesStreamConnection
             }
+            StatusSurfacePreviewItem::ActiveClients => StatusLineItem::ActiveClients,
             StatusSurfacePreviewItem::ThreadTitle => {
                 return self
                     .status_line_thread_title_display_line()
@@ -1100,13 +1104,13 @@ impl ChatWidget {
 
     pub(crate) fn model_with_reasoning_display_line(&self) -> Line<'static> {
         let fallback_effort;
-        let (model, effort) = if let Some(context) = self.input_queue.submitted_turn_context.as_ref()
-        {
-            (context.model.as_str(), context.reasoning_effort.as_ref())
-        } else {
-            fallback_effort = self.effective_reasoning_effort();
-            (self.model_display_name(), fallback_effort.as_ref())
-        };
+        let (model, effort) =
+            if let Some(context) = self.input_queue.submitted_turn_context.as_ref() {
+                (context.model.as_str(), context.reasoning_effort.as_ref())
+            } else {
+                fallback_effort = self.effective_reasoning_effort();
+                (self.model_display_name(), fallback_effort.as_ref())
+            };
         let service_tier_label = self
             .current_service_tier()
             .and_then(|service_tier| {
diff --git a/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs b/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs
index 46f1608470..bc2d0a3998 100644
--- a/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs
+++ b/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs
@@ -47,6 +47,17 @@ async fn token_count_none_resets_context_indicator() {
     assert_eq!(chat.bottom_pane.context_window_percent(), None);
 }
 
+#[tokio::test]
+async fn status_line_active_clients_displays_integer_only() {
+    let (mut chat, _rx, _ops) = make_chatwidget_manual(/*model_override*/ None).await;
+    chat.set_app_server_active_client_count(3);
+
+    assert_eq!(
+        chat.status_line_value_for_item(crate::bottom_pane::StatusLineItem::ActiveClients),
+        Some("3".to_string())
+    );
+}
+
 #[tokio::test]
 async fn app_server_cyber_policy_error_renders_dedicated_notice() {
     let (mut chat, mut rx, _ops) = make_chatwidget_manual(/*model_override*/ None).await;
