diff --git a/codex-rs/app-server-protocol/src/protocol/thread_history.rs b/codex-rs/app-server-protocol/src/protocol/thread_history.rs
index ec43092022..103e2ebe0c 100644
--- a/codex-rs/app-server-protocol/src/protocol/thread_history.rs
+++ b/codex-rs/app-server-protocol/src/protocol/thread_history.rs
@@ -517,7 +517,11 @@ impl ThreadHistoryBuilder {
         let existing_item_change = {
             let tracking_changes = self.is_tracking_changes();
             let turn = self.ensure_turn();
-            if let Some(ThreadItem::Reasoning { summary, .. }) = turn.items.last_mut() {
+            if let Some(ThreadItem::Reasoning {
+                summary, continued, ..
+            }) = turn.items.last_mut()
+                && *continued == payload.continued
+            {
                 summary.push(payload.text.clone());
                 let changed_item = if tracking_changes {
                     turn.items
@@ -545,6 +549,7 @@ impl ThreadHistoryBuilder {
             id,
             summary: vec![payload.text.clone()],
             content: Vec::new(),
+            continued: payload.continued,
         });
     }
 
@@ -557,7 +562,11 @@ impl ThreadHistoryBuilder {
         let existing_item_change = {
             let tracking_changes = self.is_tracking_changes();
             let turn = self.ensure_turn();
-            if let Some(ThreadItem::Reasoning { content, .. }) = turn.items.last_mut() {
+            if let Some(ThreadItem::Reasoning {
+                content, continued, ..
+            }) = turn.items.last_mut()
+                && *continued == payload.continued
+            {
                 content.push(payload.text.clone());
                 let changed_item = if tracking_changes {
                     turn.items
@@ -585,6 +594,7 @@ impl ThreadHistoryBuilder {
             id,
             summary: Vec::new(),
             content: vec![payload.text.clone()],
+            continued: payload.continued,
         });
     }
 
@@ -1784,9 +1794,11 @@ mod tests {
             }),
             EventMsg::AgentReasoning(AgentReasoningEvent {
                 text: "thinking".into(),
+                continued: false,
             }),
             EventMsg::AgentReasoningRawContent(AgentReasoningRawContentEvent {
                 text: "full reasoning".into(),
+                continued: false,
             }),
             EventMsg::UserMessage(UserMessageEvent {
                 client_id: None,
@@ -1846,6 +1858,7 @@ mod tests {
                 id: "item-3".into(),
                 summary: vec!["thinking".into()],
                 content: vec!["full reasoning".into()],
+                continued: false,
             }
         );
 
@@ -2347,9 +2360,11 @@ mod tests {
             }),
             EventMsg::AgentReasoning(AgentReasoningEvent {
                 text: "first summary".into(),
+                continued: false,
             }),
             EventMsg::AgentReasoningRawContent(AgentReasoningRawContentEvent {
                 text: "first content".into(),
+                continued: false,
             }),
             EventMsg::AgentMessage(AgentMessageEvent {
                 message: "interlude".into(),
@@ -2358,6 +2373,7 @@ mod tests {
             }),
             EventMsg::AgentReasoning(AgentReasoningEvent {
                 text: "second summary".into(),
+                continued: false,
             }),
         ];
 
@@ -2376,6 +2392,7 @@ mod tests {
                 id: "item-2".into(),
                 summary: vec!["first summary".into()],
                 content: vec!["first content".into()],
+                continued: false,
             }
         );
         assert_eq!(
@@ -2384,6 +2401,53 @@ mod tests {
                 id: "item-4".into(),
                 summary: vec!["second summary".into()],
                 content: Vec::new(),
+                continued: false,
+            }
+        );
+    }
+
+    #[test]
+    fn splits_reasoning_when_continued_flag_changes() {
+        let events = vec![
+            EventMsg::AgentReasoning(AgentReasoningEvent {
+                text: "continued summary".into(),
+                continued: true,
+            }),
+            EventMsg::AgentReasoningRawContent(AgentReasoningRawContentEvent {
+                text: "continued content".into(),
+                continued: true,
+            }),
+            EventMsg::AgentReasoning(AgentReasoningEvent {
+                text: "final summary".into(),
+                continued: false,
+            }),
+        ];
+
+        let items = events
+            .into_iter()
+            .map(RolloutItem::EventMsg)
+            .collect::<Vec<_>>();
+        let turns = build_turns_from_rollout_items(&items);
+        assert_eq!(turns.len(), 1);
+        let turn = &turns[0];
+        assert_eq!(turn.items.len(), 2);
+
+        assert_eq!(
+            turn.items[0],
+            ThreadItem::Reasoning {
+                id: "item-1".into(),
+                summary: vec!["continued summary".into()],
+                content: vec!["continued content".into()],
+                continued: true,
+            }
+        );
+        assert_eq!(
+            turn.items[1],
+            ThreadItem::Reasoning {
+                id: "item-2".into(),
+                summary: vec!["final summary".into()],
+                content: Vec::new(),
+                continued: false,
             }
         );
     }
@@ -4263,12 +4327,14 @@ mod tests {
         builder.handle_rollout_item_with_changes(&RolloutItem::EventMsg(EventMsg::AgentReasoning(
             AgentReasoningEvent {
                 text: "summary".into(),
+                continued: false,
             },
         )));
 
         let changes = builder.handle_rollout_item_with_changes(&RolloutItem::EventMsg(
             EventMsg::AgentReasoningRawContent(AgentReasoningRawContentEvent {
                 text: "raw content".into(),
+                continued: false,
             }),
         ));
 
@@ -4281,6 +4347,7 @@ mod tests {
                         id: "item-1".into(),
                         summary: vec!["summary".into()],
                         content: vec!["raw content".into()],
+                        continued: false,
                     },
                 }],
                 changed_turns: Vec::new(),
