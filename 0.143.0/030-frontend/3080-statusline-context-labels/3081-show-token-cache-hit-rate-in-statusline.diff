diff --git a/codex-rs/tui/src/bottom_pane/status_line_setup.rs b/codex-rs/tui/src/bottom_pane/status_line_setup.rs
index e475883b6c..29eb43d883 100644
--- a/codex-rs/tui/src/bottom_pane/status_line_setup.rs
+++ b/codex-rs/tui/src/bottom_pane/status_line_setup.rs
@@ -14,7 +14,7 @@
 //! - Git information (branch name)
 //! - Permissions profile
 //! - Approval mode
-//! - Context usage (remaining %, used %, window size)
+//! - Context usage (remaining %, used %, window size, cache hit rate)
 //! - Usage limits (primary, secondary)
 //! - Responses stream connection latency
 //! - Session info (thread title, thread ID, tokens used)
@@ -143,7 +143,6 @@ pub(crate) enum StatusLineItem {
     /// Last response time to first token.
     #[strum(to_string = "last-ttft", serialize = "ttft")]
     TimeToFirstToken,
-
     /// Prompt cache hit rate for input tokens.
     CacheHitRate,
 
diff --git a/codex-rs/tui/src/chatwidget/tests/status_surface_previews.rs b/codex-rs/tui/src/chatwidget/tests/status_surface_previews.rs
index 9af8eb7673..d277c5b85c 100644
--- a/codex-rs/tui/src/chatwidget/tests/status_surface_previews.rs
+++ b/codex-rs/tui/src/chatwidget/tests/status_surface_previews.rs
@@ -301,6 +301,48 @@ async fn status_surface_preview_omits_unavailable_rate_limit_items() {
     );
 }
 
+#[tokio::test]
+async fn cache_hit_rate_status_line_item_uses_token_info() {
+    let (mut chat, _rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+    chat.set_token_info(Some(TokenUsageInfo {
+        total_token_usage: TokenUsage {
+            input_tokens: 1_000,
+            cached_input_tokens: 800,
+            output_tokens: 50,
+            reasoning_output_tokens: 0,
+            total_tokens: 1_050,
+        },
+        last_token_usage: TokenUsage::default(),
+        model_context_window: Some(100_000),
+    }));
+
+    assert_eq!(
+        status_preview_line(&mut chat, &[StatusLineItem::CacheHitRate]),
+        "80.0% hit"
+    );
+}
+
+#[tokio::test]
+async fn cache_hit_rate_status_line_item_omits_missing_cache_data() {
+    let (mut chat, _rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+    chat.set_token_info(Some(TokenUsageInfo {
+        total_token_usage: TokenUsage {
+            input_tokens: 1_000,
+            cached_input_tokens: 0,
+            output_tokens: 50,
+            reasoning_output_tokens: 0,
+            total_tokens: 1_050,
+        },
+        last_token_usage: TokenUsage::default(),
+        model_context_window: Some(100_000),
+    }));
+
+    assert_eq!(
+        chat.status_line_value_for_item(StatusLineItem::CacheHitRate),
+        None
+    );
+}
+
 #[tokio::test]
 async fn status_line_setup_popup_rate_limits_snapshot() {
     let (mut chat, _rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
