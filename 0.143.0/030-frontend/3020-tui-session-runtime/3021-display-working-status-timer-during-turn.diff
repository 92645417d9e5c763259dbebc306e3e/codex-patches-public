diff --git a/codex-rs/tui/src/app.rs b/codex-rs/tui/src/app.rs
index 4382f982ad..3a314a9062 100644
--- a/codex-rs/tui/src/app.rs
+++ b/codex-rs/tui/src/app.rs
@@ -395,6 +395,9 @@ fn managed_filesystem_sandbox_is_restricted(permission_profile: &PermissionProfi
 /// perceived typing speed for non-backlogged output.
 const COMMIT_ANIMATION_TICK: Duration = tui::TARGET_FRAME_INTERVAL;
 
+/// Low-rate redraw cadence while a task is pending or running.
+const TASK_FRAME_HEARTBEAT_TICK: Duration = Duration::from_millis(250);
+
 #[derive(Debug, Clone)]
 pub struct AppExitInfo {
     pub token_usage: TokenUsage,
@@ -533,6 +536,9 @@ pub(crate) struct App {
 
     /// Controls the animation thread that sends CommitTick events.
     pub(crate) commit_anim_running: Arc<AtomicBool>,
+    /// Controls a low-rate redraw heartbeat while a task is pending or running.
+    pub(crate) task_frame_heartbeat_running: Arc<AtomicBool>,
+    pub(crate) task_frame_heartbeat_active: Arc<AtomicBool>,
     // Shared across ChatWidget instances so invalid status-line config warnings only emit once.
     status_line_invalid_items_warned: Arc<AtomicBool>,
     // Shared across ChatWidget instances so invalid terminal-title config warnings only emit once.
@@ -1015,6 +1021,22 @@ See the Codex keymap documentation for supported actions and examples."
         #[cfg(not(debug_assertions))]
         let upgrade_version = crate::updates::get_upgrade_version(&config);
 
+        let task_frame_heartbeat_running = Arc::new(AtomicBool::new(true));
+        let task_frame_heartbeat_active = Arc::new(AtomicBool::new(false));
+        {
+            let tx = app_event_tx.clone();
+            let running = task_frame_heartbeat_running.clone();
+            let active = task_frame_heartbeat_active.clone();
+            thread::spawn(move || {
+                while running.load(Ordering::Relaxed) {
+                    thread::sleep(TASK_FRAME_HEARTBEAT_TICK);
+                    if active.load(Ordering::Relaxed) {
+                        tx.send(AppEvent::TaskFrameHeartbeat);
+                    }
+                }
+            });
+        }
+
         let mut app = Self {
             model_catalog,
             session_telemetry: session_telemetry.clone(),
@@ -1039,6 +1061,8 @@ See the Codex keymap documentation for supported actions and examples."
             transcript_reflow: TranscriptReflowState::default(),
             initial_history_replay_buffer: None,
             commit_anim_running: Arc::new(AtomicBool::new(false)),
+            task_frame_heartbeat_running,
+            task_frame_heartbeat_active,
             status_line_invalid_items_warned: status_line_invalid_items_warned.clone(),
             terminal_title_invalid_items_warned: terminal_title_invalid_items_warned.clone(),
             skill_load_warnings: SkillLoadWarningState::default(),
@@ -1370,8 +1394,19 @@ See the Codex keymap documentation for supported actions and examples."
     }
 }
 
+impl App {
+    pub(crate) fn sync_task_frame_heartbeat_active(&self) {
+        self.task_frame_heartbeat_active.store(
+            self.chat_widget.is_user_turn_pending_or_running(),
+            Ordering::Release,
+        );
+    }
+}
+
 impl Drop for App {
     fn drop(&mut self) {
+        self.task_frame_heartbeat_running
+            .store(false, Ordering::Release);
         if let Err(err) = self.chat_widget.clear_managed_terminal_title() {
             tracing::debug!(error = %err, "failed to clear terminal title on app drop");
         }
diff --git a/codex-rs/tui/src/app/event_dispatch.rs b/codex-rs/tui/src/app/event_dispatch.rs
index 6e00b3d849..c38777472d 100644
--- a/codex-rs/tui/src/app/event_dispatch.rs
+++ b/codex-rs/tui/src/app/event_dispatch.rs
@@ -319,6 +319,14 @@ impl App {
             AppEvent::CommitTick => {
                 self.chat_widget.on_commit_tick();
             }
+            AppEvent::TaskFrameHeartbeat => {
+                if self.chat_widget.is_user_turn_pending_or_running() {
+                    tui.frame_requester().schedule_frame();
+                } else {
+                    self.task_frame_heartbeat_active
+                        .store(false, Ordering::Release);
+                }
+            }
             AppEvent::Exit(mode) => {
                 if mode == ExitMode::ShutdownFirst
                     || (mode == ExitMode::Detach && !self.app_server_target.supports_detach())
@@ -345,6 +353,7 @@ impl App {
             }
             AppEvent::CodexOp(op) => {
                 self.chat_widget.prepare_local_op_submission(&op);
+                self.sync_task_frame_heartbeat_active();
                 self.submit_active_thread_op(app_server, op).await?;
             }
             AppEvent::RetrySafetyBufferedTurn {
diff --git a/codex-rs/tui/src/app/test_support.rs b/codex-rs/tui/src/app/test_support.rs
index e0eafaa23b..db8f58cd1a 100644
--- a/codex-rs/tui/src/app/test_support.rs
+++ b/codex-rs/tui/src/app/test_support.rs
@@ -40,6 +40,8 @@ pub(super) async fn make_test_app() -> App {
         enhanced_keys_supported: false,
         keymap: crate::keymap::RuntimeKeymap::defaults(),
         commit_anim_running: Arc::new(AtomicBool::new(false)),
+        task_frame_heartbeat_running: Arc::new(AtomicBool::new(false)),
+        task_frame_heartbeat_active: Arc::new(AtomicBool::new(false)),
         status_line_invalid_items_warned: Arc::new(AtomicBool::new(false)),
         terminal_title_invalid_items_warned: Arc::new(AtomicBool::new(false)),
         skill_load_warnings: SkillLoadWarningState::default(),
diff --git a/codex-rs/tui/src/app/tests.rs b/codex-rs/tui/src/app/tests.rs
index 9b76b8260e..98a3ab2ccf 100644
--- a/codex-rs/tui/src/app/tests.rs
+++ b/codex-rs/tui/src/app/tests.rs
@@ -4143,6 +4143,8 @@ async fn make_test_app() -> App {
         enhanced_keys_supported: false,
         keymap: crate::keymap::RuntimeKeymap::defaults(),
         commit_anim_running: Arc::new(AtomicBool::new(false)),
+        task_frame_heartbeat_running: Arc::new(AtomicBool::new(false)),
+        task_frame_heartbeat_active: Arc::new(AtomicBool::new(false)),
         status_line_invalid_items_warned: Arc::new(AtomicBool::new(false)),
         terminal_title_invalid_items_warned: Arc::new(AtomicBool::new(false)),
         skill_load_warnings: SkillLoadWarningState::default(),
@@ -4208,6 +4210,8 @@ async fn make_test_app_with_channels() -> (
             enhanced_keys_supported: false,
             keymap: crate::keymap::RuntimeKeymap::defaults(),
             commit_anim_running: Arc::new(AtomicBool::new(false)),
+            task_frame_heartbeat_running: Arc::new(AtomicBool::new(false)),
+            task_frame_heartbeat_active: Arc::new(AtomicBool::new(false)),
             status_line_invalid_items_warned: Arc::new(AtomicBool::new(false)),
             terminal_title_invalid_items_warned: Arc::new(AtomicBool::new(false)),
             skill_load_warnings: SkillLoadWarningState::default(),
diff --git a/codex-rs/tui/src/app/thread_routing.rs b/codex-rs/tui/src/app/thread_routing.rs
index a6a247754c..314825e3b2 100644
--- a/codex-rs/tui/src/app/thread_routing.rs
+++ b/codex-rs/tui/src/app/thread_routing.rs
@@ -1571,6 +1571,7 @@ impl App {
                 self.cache_collab_receiver_threads_for_notification(&notification);
                 self.chat_widget
                     .handle_server_notification(notification, /*replay_kind*/ None);
+                self.sync_task_frame_heartbeat_active();
             }
             ThreadBufferedEvent::Request(request) => {
                 if self
diff --git a/codex-rs/tui/src/app_event.rs b/codex-rs/tui/src/app_event.rs
index e7f828b2c7..bbf2c544cc 100644
--- a/codex-rs/tui/src/app_event.rs
+++ b/codex-rs/tui/src/app_event.rs
@@ -713,6 +713,7 @@ pub(crate) enum AppEvent {
     StartCommitAnimation,
     StopCommitAnimation,
     CommitTick,
+    TaskFrameHeartbeat,
 
     /// Update the current reasoning effort in the running app and widget.
     UpdateReasoningEffort(Option<ReasoningEffort>),
diff --git a/codex-rs/tui/src/bottom_pane/mod.rs b/codex-rs/tui/src/bottom_pane/mod.rs
index 57d1789b37..708f5a0df9 100644
--- a/codex-rs/tui/src/bottom_pane/mod.rs
+++ b/codex-rs/tui/src/bottom_pane/mod.rs
@@ -228,6 +228,7 @@ pub(crate) struct BottomPane {
 
     /// Inline status indicator shown above the composer while a task is running.
     status: Option<StatusIndicatorWidget>,
+    status_timer_started_at: Option<Instant>,
     /// Unified exec session summary source.
     ///
     /// When a status row exists, this summary is mirrored inline in that row;
@@ -289,6 +290,7 @@ impl BottomPane {
             disable_paste_burst,
             is_task_running: false,
             status: None,
+            status_timer_started_at: None,
             unified_exec_footer: UnifiedExecFooter::new(),
             pending_input_preview: PendingInputPreview::new(),
             pending_thread_approvals: PendingThreadApprovals::new(),
@@ -1003,11 +1005,15 @@ impl BottomPane {
         if running {
             if !was_running {
                 if self.status.is_none() {
-                    self.status = Some(StatusIndicatorWidget::new(
+                    let mut status = StatusIndicatorWidget::new(
                         self.app_event_tx.clone(),
                         self.frame_requester.clone(),
                         self.animations_enabled,
-                    ));
+                    );
+                    if let Some(started_at) = self.status_timer_started_at {
+                        status.set_timer_started_at(started_at);
+                    }
+                    self.status = Some(status);
                 }
                 if let Some(status) = self.status.as_mut() {
                     status.set_interrupt_hint_visible(/*visible*/ true);
@@ -1017,6 +1023,7 @@ impl BottomPane {
                 self.request_redraw();
             }
         } else {
+            self.status_timer_started_at = None;
             // Hide the status indicator when a task completes, but keep other modal views.
             self.hide_status_indicator();
         }
@@ -1035,11 +1042,15 @@ impl BottomPane {
 
     pub(crate) fn ensure_status_indicator(&mut self) {
         if self.status.is_none() {
-            self.status = Some(StatusIndicatorWidget::new(
+            let mut status = StatusIndicatorWidget::new(
                 self.app_event_tx.clone(),
                 self.frame_requester.clone(),
                 self.animations_enabled,
-            ));
+            );
+            if let Some(started_at) = self.status_timer_started_at {
+                status.set_timer_started_at(started_at);
+            }
+            self.status = Some(status);
             if let Some(status) = self.status.as_mut() {
                 status.set_interrupt_binding(primary_binding(&self.keymap.chat.interrupt_turn));
             }
@@ -1048,6 +1059,14 @@ impl BottomPane {
         }
     }
 
+    pub(crate) fn set_status_timer_started_at(&mut self, started_at: Instant) {
+        self.status_timer_started_at = Some(started_at);
+        if let Some(status) = self.status.as_mut() {
+            status.set_timer_started_at(started_at);
+            self.request_redraw();
+        }
+    }
+
     pub(crate) fn set_interrupt_hint_visible(&mut self, visible: bool) {
         if let Some(status) = self.status.as_mut() {
             status.set_interrupt_hint_visible(visible);
@@ -2388,6 +2407,27 @@ mod tests {
         assert!(bufs.contains("■ Working"), "expected Working header");
     }
 
+    #[test]
+    fn status_indicator_recreation_preserves_timer_start() {
+        let (tx_raw, _rx) = unbounded_channel::<AppEvent>();
+        let tx = AppEventSender::new(tx_raw);
+        let mut pane = test_pane(tx);
+
+        pane.set_task_running(/*running*/ true);
+        pane.set_status_timer_started_at(Instant::now() - Duration::from_secs(90));
+
+        pane.hide_status_indicator();
+        assert!(pane.status_widget().is_none());
+
+        pane.ensure_status_indicator();
+
+        let elapsed_seconds = pane
+            .status_widget()
+            .expect("status indicator should be restored")
+            .elapsed_seconds();
+        assert!(elapsed_seconds >= 90, "elapsed_seconds={elapsed_seconds}");
+    }
+
     #[test]
     fn status_and_composer_fill_height_without_bottom_padding() {
         let (tx_raw, _rx) = unbounded_channel::<AppEvent>();
diff --git a/codex-rs/tui/src/chatwidget.rs b/codex-rs/tui/src/chatwidget.rs
index 78265451ff..1734c366cb 100644
--- a/codex-rs/tui/src/chatwidget.rs
+++ b/codex-rs/tui/src/chatwidget.rs
@@ -40,6 +40,8 @@ use std::sync::atomic::AtomicBool;
 use std::sync::atomic::Ordering;
 use std::time::Duration;
 use std::time::Instant;
+use std::time::SystemTime;
+use std::time::UNIX_EPOCH;
 
 use crate::app::app_server_requests::ResolvedAppServerRequest;
 use crate::app_command::AppCommand;
@@ -432,6 +434,23 @@ use self::user_messages::QueuedUserMessage;
 use self::user_messages::ShellEscapePolicy;
 use self::user_messages::ThreadComposerState;
 pub(crate) use self::user_messages::ThreadInputState;
+
+fn instant_from_unix_timestamp_secs(started_at: i64) -> Option<Instant> {
+    let started_at = u64::try_from(started_at).ok()?;
+    let started_at = UNIX_EPOCH.checked_add(Duration::from_secs(started_at))?;
+    let now_system = SystemTime::now();
+    let now_instant = Instant::now();
+    match now_system.duration_since(started_at) {
+        Ok(age) => now_instant.checked_sub(age).or(Some(now_instant)),
+        Err(_) => Some(now_instant),
+    }
+}
+
+fn elapsed_seconds_from_duration_ms(duration_ms: Option<i64>) -> Option<u64> {
+    duration_ms
+        .and_then(|duration_ms| u64::try_from(duration_ms).ok())
+        .map(|duration_ms| duration_ms / 1_000)
+}
 pub(crate) use self::user_messages::UserMessage;
 use self::user_messages::UserMessageDisplay;
 #[cfg(test)]
diff --git a/codex-rs/tui/src/chatwidget/input_flow.rs b/codex-rs/tui/src/chatwidget/input_flow.rs
index 429c061213..3e2dfba37e 100644
--- a/codex-rs/tui/src/chatwidget/input_flow.rs
+++ b/codex-rs/tui/src/chatwidget/input_flow.rs
@@ -164,8 +164,10 @@ impl ChatWidget {
         submitted_follow_up
     }
 
-    pub(super) fn is_user_turn_pending_or_running(&self) -> bool {
-        self.input_queue.user_turn_pending_start || self.bottom_pane.is_task_running()
+    pub(crate) fn is_user_turn_pending_or_running(&self) -> bool {
+        self.input_queue.user_turn_pending_start
+            || self.bottom_pane.is_task_running()
+            || self.turn_lifecycle.agent_turn_running
     }
 
     pub(super) fn only_user_shell_commands_running(&self) -> bool {
diff --git a/codex-rs/tui/src/chatwidget/input_restore.rs b/codex-rs/tui/src/chatwidget/input_restore.rs
index bcc4e956a7..f0b7677960 100644
--- a/codex-rs/tui/src/chatwidget/input_restore.rs
+++ b/codex-rs/tui/src/chatwidget/input_restore.rs
@@ -381,6 +381,11 @@ impl ChatWidget {
             active_collaboration_mask: self.active_collaboration_mask.clone(),
             task_running: self.bottom_pane.is_task_running(),
             agent_turn_running: self.turn_lifecycle.agent_turn_running,
+            active_turn_started_at: if self.turn_lifecycle.agent_turn_running {
+                self.turn_lifecycle.active_turn_started_at
+            } else {
+                None
+            },
         })
     }
 
@@ -389,8 +394,11 @@ impl ChatWidget {
         if let Some(input_state) = input_state {
             self.current_collaboration_mode = input_state.current_collaboration_mode;
             self.active_collaboration_mask = input_state.active_collaboration_mask;
-            self.turn_lifecycle
-                .restore_running(input_state.agent_turn_running, Instant::now());
+            let active_turn_started_at = input_state.active_turn_started_at;
+            self.turn_lifecycle.restore_running(
+                input_state.agent_turn_running,
+                active_turn_started_at.unwrap_or_else(Instant::now),
+            );
             self.input_queue.user_turn_pending_start = input_state.user_turn_pending_start;
             self.update_collaboration_mode_indicator();
             self.refresh_model_dependent_surfaces();
@@ -437,13 +445,14 @@ impl ChatWidget {
             self.input_queue.clear();
             self.restore_composer_state(Default::default());
         }
-        self.turn_lifecycle
-            .restore_running(self.turn_lifecycle.agent_turn_running, Instant::now());
         self.update_task_running_state();
         if restored_task_running && !self.bottom_pane.is_task_running() {
             self.bottom_pane.set_task_running(/*running*/ true);
             self.refresh_status_surfaces();
         }
+        if let Some(started_at) = self.turn_lifecycle.active_turn_started_at {
+            self.bottom_pane.set_status_timer_started_at(started_at);
+        }
         self.refresh_pending_input_preview();
         self.request_redraw();
     }
diff --git a/codex-rs/tui/src/chatwidget/input_submission.rs b/codex-rs/tui/src/chatwidget/input_submission.rs
index 2bf7aa9d35..e00d8afc15 100644
--- a/codex-rs/tui/src/chatwidget/input_submission.rs
+++ b/codex-rs/tui/src/chatwidget/input_submission.rs
@@ -443,6 +443,9 @@ impl ChatWidget {
         });
         if let Some(display) = display_user_message {
             self.on_user_message_display(display);
+            self.add_to_history(history_cell::PromptSubmittedAtSeparator::new(
+                chrono::Local::now().format("%m-%d %H:%M:%S").to_string(),
+            ));
         }
 
         self.transcript.needs_final_message_separator = false;
diff --git a/codex-rs/tui/src/chatwidget/replay.rs b/codex-rs/tui/src/chatwidget/replay.rs
index 77030e4f8d..d46f6cda54 100644
--- a/codex-rs/tui/src/chatwidget/replay.rs
+++ b/codex-rs/tui/src/chatwidget/replay.rs
@@ -26,7 +26,7 @@ impl ChatWidget {
             if matches!(status, TurnStatus::InProgress) {
                 self.turn_lifecycle.last_turn_id = Some(turn_id.clone());
                 self.last_non_retry_error = None;
-                self.on_task_started();
+                self.on_task_started_at(started_at.and_then(instant_from_unix_timestamp_secs));
             }
             for item in items {
                 self.replay_thread_item(item, turn_id.clone(), replay_kind);
diff --git a/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__final_worked_for_uses_cumulative_turn_duration.snap b/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__final_worked_for_uses_cumulative_turn_duration.snap
index bf8458a570..ad6b5d704c 100644
--- a/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__final_worked_for_uses_cumulative_turn_duration.snap
+++ b/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__final_worked_for_uses_cumulative_turn_duration.snap
@@ -1,6 +1,6 @@
 ---
 source: tui/src/chatwidget/tests/exec_flow.rs
-expression: combined
+expression: snapshot
 ---
 ■ Ran echo preparing
   └ preparing
@@ -9,4 +9,4 @@ expression: combined
 
 ■ Final response.
 
-─ Worked for 2m 05s ────────────────────────────────────────────────────────────
+─ Worked for 2m 05s at <finished_at> ──────────────────────────────────────────
diff --git a/codex-rs/tui/src/chatwidget/streaming.rs b/codex-rs/tui/src/chatwidget/streaming.rs
index 7a9e9be79c..0f7eacdb57 100644
--- a/codex-rs/tui/src/chatwidget/streaming.rs
+++ b/codex-rs/tui/src/chatwidget/streaming.rs
@@ -799,7 +799,8 @@ impl ChatWidget {
             // calls), render a separator before starting the next streamed assistant message.
             if self.transcript.needs_final_message_separator && self.transcript.had_work_activity {
                 self.add_to_history(history_cell::FinalMessageSeparator::new(
-                    /*elapsed_seconds*/ None, /*runtime_metrics*/ None,
+                    /*elapsed_seconds*/ None, /*finished_at*/ None,
+                    /*runtime_metrics*/ None,
                 ));
                 self.transcript.needs_final_message_separator = false;
             } else if self.transcript.needs_final_message_separator {
diff --git a/codex-rs/tui/src/chatwidget/tests/app_server.rs b/codex-rs/tui/src/chatwidget/tests/app_server.rs
index d2f6b4e44d..de25d771f0 100644
--- a/codex-rs/tui/src/chatwidget/tests/app_server.rs
+++ b/codex-rs/tui/src/chatwidget/tests/app_server.rs
@@ -487,8 +487,11 @@ async fn live_app_server_user_message_item_completed_does_not_duplicate_rendered
     }
 
     let inserted = drain_insert_history(&mut rx);
-    assert_eq!(inserted.len(), 1);
-    assert!(lines_to_single_string(&inserted[0]).contains("Hi, are you there?"));
+    let rendered_prompt_count = inserted
+        .iter()
+        .filter(|lines| lines_to_single_string(lines).contains("Hi, are you there?"))
+        .count();
+    assert_eq!(rendered_prompt_count, 1);
 
     chat.handle_server_notification(
         ServerNotification::ItemCompleted(ItemCompletedNotification {
diff --git a/codex-rs/tui/src/chatwidget/tests/composer_submission.rs b/codex-rs/tui/src/chatwidget/tests/composer_submission.rs
index ee940e2c7c..cac765c3a0 100644
--- a/codex-rs/tui/src/chatwidget/tests/composer_submission.rs
+++ b/codex-rs/tui/src/chatwidget/tests/composer_submission.rs
@@ -96,6 +96,69 @@ async fn submission_preserves_text_elements_and_local_images() {
     assert!(stored_remote_image_urls.is_empty());
 }
 
+#[tokio::test]
+async fn submission_renders_prompt_submitted_timestamp_after_user_message() {
+    let (mut chat, mut rx, mut op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+
+    let thread_id = ThreadId::new();
+    let rollout_file = NamedTempFile::new().unwrap();
+    let configured = crate::session_state::ThreadSessionState {
+        thread_id,
+        forked_from_id: None,
+        fork_parent_title: None,
+        thread_name: None,
+        model: "test-model".to_string(),
+        model_provider_id: "test-provider".to_string(),
+        service_tier: None,
+        approval_policy: AskForApproval::Never,
+        approvals_reviewer: ApprovalsReviewer::User,
+        permission_profile: PermissionProfile::read_only(),
+        active_permission_profile: None,
+        cwd: test_path_buf("/home/user/project").abs(),
+        runtime_workspace_roots: Vec::new(),
+        instruction_source_paths: Vec::new(),
+        reasoning_effort: Some(ReasoningEffortConfig::default()),
+        collaboration_mode: None,
+        personality: None,
+        message_history: None,
+        network_proxy: None,
+        rollout_path: Some(rollout_file.path().to_path_buf()),
+    };
+    chat.handle_thread_session(configured);
+    drain_insert_history(&mut rx);
+
+    chat.bottom_pane
+        .set_composer_text("timestamped submit".to_string(), Vec::new(), Vec::new());
+    chat.handle_key_event(KeyEvent::new(KeyCode::Enter, KeyModifiers::NONE));
+
+    match next_submit_op(&mut op_rx) {
+        Op::UserTurn { .. } => {}
+        other => panic!("expected Op::UserTurn, got {other:?}"),
+    }
+
+    let cells = drain_insert_history(&mut rx);
+    assert_eq!(cells.len(), 2);
+    assert!(lines_to_single_string(&cells[0]).contains("timestamped submit"));
+
+    let separator = lines_to_single_string(&cells[1]);
+    let Some(after_label) = separator.split("Submitted at ").nth(1) else {
+        panic!("expected submitted-at separator, got {separator:?}");
+    };
+    let timestamp = after_label.chars().take(14).collect::<String>();
+    assert_eq!(timestamp.len(), 14);
+    assert_eq!(timestamp.chars().nth(2), Some('-'));
+    assert_eq!(timestamp.chars().nth(5), Some(' '));
+    assert_eq!(timestamp.chars().nth(8), Some(':'));
+    assert_eq!(timestamp.chars().nth(11), Some(':'));
+    assert!(
+        timestamp
+            .chars()
+            .enumerate()
+            .all(|(idx, ch)| matches!(idx, 2 | 5 | 8 | 11) || ch.is_ascii_digit()),
+        "timestamp should use MM-DD HH:MM:SS, got {timestamp:?}"
+    );
+}
+
 #[tokio::test]
 async fn submission_includes_configured_active_permission_profile() {
     let (mut chat, mut rx, mut op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
@@ -1065,6 +1128,7 @@ async fn restore_thread_input_state_syncs_sleep_inhibitor_state() {
         active_collaboration_mask: chat.active_collaboration_mask.clone(),
         task_running: true,
         agent_turn_running: true,
+        active_turn_started_at: None,
     }));
 
     assert!(chat.turn_lifecycle.agent_turn_running);
diff --git a/codex-rs/tui/src/chatwidget/tests/exec_flow.rs b/codex-rs/tui/src/chatwidget/tests/exec_flow.rs
index 9ad2fa9aa1..71ac998fb2 100644
--- a/codex-rs/tui/src/chatwidget/tests/exec_flow.rs
+++ b/codex-rs/tui/src/chatwidget/tests/exec_flow.rs
@@ -668,9 +668,11 @@ async fn unified_exec_wait_before_streamed_agent_message_snapshot() {
 }
 
 #[tokio::test]
-async fn final_worked_for_uses_cumulative_turn_duration_snapshot() {
+async fn final_worked_for_prefers_local_active_turn_duration_snapshot() {
     let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
     handle_turn_started(&mut chat, "turn-1");
+    chat.turn_lifecycle.active_turn_started_at =
+        Some(std::time::Instant::now() - std::time::Duration::from_secs(125));
 
     let exec = begin_exec_with_source(
         &mut chat,
@@ -686,7 +688,7 @@ async fn final_worked_for_uses_cumulative_turn_duration_snapshot() {
         "Final response.",
         Some(MessagePhase::FinalAnswer),
     );
-    handle_turn_completed(&mut chat, "turn-1", Some(125_000));
+    handle_turn_completed(&mut chat, "turn-1", Some(597_000));
 
     let cells = drain_insert_history(&mut rx);
     let combined = cells
@@ -695,9 +697,116 @@ async fn final_worked_for_uses_cumulative_turn_duration_snapshot() {
         .collect::<String>();
     assert!(
         combined.contains("Worked for 2m 05s"),
-        "expected final separator to use cumulative turn duration, got:\n{combined}"
+        "expected final separator to use local active-turn duration, got:\n{combined}"
+    );
+    assert!(
+        !combined.contains("Worked for 9m 57s"),
+        "expected final separator to ignore protocol duration, got:\n{combined}"
+    );
+    let snapshot = regex_lite::Regex::new(r" at \d{2}-\d{2} \d{2}:\d{2}:\d{2}")
+        .expect("valid timestamp regex")
+        .replace(&combined, " at <finished_at>")
+        .into_owned();
+    assert_chatwidget_snapshot!("final_worked_for_uses_cumulative_turn_duration", snapshot);
+}
+
+#[tokio::test]
+async fn duplicate_turn_started_keeps_active_turn_elapsed() {
+    let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+    handle_turn_started(&mut chat, "turn-1");
+    chat.turn_lifecycle.active_turn_started_at =
+        Some(std::time::Instant::now() - std::time::Duration::from_secs(125));
+
+    let now_unix_secs = std::time::SystemTime::now()
+        .duration_since(std::time::UNIX_EPOCH)
+        .expect("system time should be after UNIX epoch")
+        .as_secs() as i64;
+    chat.handle_server_notification(
+        ServerNotification::TurnStarted(TurnStartedNotification {
+            thread_id: "thread-1".to_string(),
+            turn: AppServerTurn {
+                id: "turn-1".to_string(),
+                items_view: codex_app_server_protocol::TurnItemsView::Full,
+                items: Vec::new(),
+                status: AppServerTurnStatus::InProgress,
+                error: None,
+                started_at: Some(now_unix_secs),
+                completed_at: None,
+                duration_ms: None,
+            },
+        }),
+        /*replay_kind*/ None,
+    );
+
+    let exec = begin_exec_with_source(
+        &mut chat,
+        "call-1",
+        "echo after compact",
+        ExecCommandSource::Agent,
+    );
+    end_exec(&mut chat, exec, "after compact\n", "", /*exit_code*/ 0);
+
+    complete_assistant_message(
+        &mut chat,
+        "msg-final",
+        "Final response.",
+        Some(MessagePhase::FinalAnswer),
+    );
+    handle_turn_completed(&mut chat, "turn-1", Some(597_000));
+
+    let cells = drain_insert_history(&mut rx);
+    let combined = cells
+        .iter()
+        .map(|lines| lines_to_single_string(lines))
+        .collect::<String>();
+    assert!(
+        combined.contains("Worked for 2m 05s"),
+        "expected duplicate TurnStarted to preserve active-turn duration, got:\n{combined}"
+    );
+    assert!(
+        !combined.contains("Worked for 0s"),
+        "expected duplicate TurnStarted not to reset active-turn duration, got:\n{combined}"
+    );
+}
+
+#[tokio::test]
+async fn final_worked_for_prefers_local_elapsed_with_runtime_metrics() {
+    let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+    handle_turn_started(&mut chat, "turn-1");
+    chat.turn_lifecycle.active_turn_started_at =
+        Some(std::time::Instant::now() - std::time::Duration::from_secs(61));
+
+    let exec = begin_exec_with_source(
+        &mut chat,
+        "call-1",
+        "echo metrics",
+        ExecCommandSource::Agent,
+    );
+    end_exec(&mut chat, exec, "metrics\n", "", /*exit_code*/ 0);
+    chat.apply_runtime_metrics_delta(RuntimeMetricsSummary {
+        responses_api_overhead_ms: 1_200,
+        ..RuntimeMetricsSummary::default()
+    });
+
+    handle_turn_completed(&mut chat, "turn-1", Some(597_000));
+
+    let cells = drain_insert_history(&mut rx);
+    let combined = cells
+        .iter()
+        .map(|lines| lines_to_single_string(lines))
+        .collect::<String>();
+    assert!(
+        combined.contains("Worked for 1m 01s"),
+        "expected final separator to use local active-turn duration, got:\n{combined}"
+    );
+    assert!(
+        combined.contains("Responses API overhead: 1.2s"),
+        "expected final separator to keep runtime metrics, got:\n{combined}"
+    );
+    assert!(
+        !combined.contains("Worked for 9m 57s"),
+        "expected final separator to ignore protocol duration, got:\n{combined}"
     );
-    assert_chatwidget_snapshot!("final_worked_for_uses_cumulative_turn_duration", combined);
 }
 
 #[tokio::test]
diff --git a/codex-rs/tui/src/chatwidget/tests/review_mode.rs b/codex-rs/tui/src/chatwidget/tests/review_mode.rs
index a8303d9585..59ca8b2508 100644
--- a/codex-rs/tui/src/chatwidget/tests/review_mode.rs
+++ b/codex-rs/tui/src/chatwidget/tests/review_mode.rs
@@ -387,6 +387,7 @@ async fn restore_thread_input_state_restores_pending_steers_without_downgrading_
         active_collaboration_mask: chat.active_collaboration_mask.clone(),
         task_running: false,
         agent_turn_running: false,
+        active_turn_started_at: None,
     }));
 
     assert_eq!(
diff --git a/codex-rs/tui/src/chatwidget/turn_lifecycle.rs b/codex-rs/tui/src/chatwidget/turn_lifecycle.rs
index 6ecc29e02f..3e6a01b8ff 100644
--- a/codex-rs/tui/src/chatwidget/turn_lifecycle.rs
+++ b/codex-rs/tui/src/chatwidget/turn_lifecycle.rs
@@ -12,6 +12,8 @@ pub(super) struct TurnLifecycleState {
     pub(super) agent_turn_running: bool,
     pub(super) last_turn_id: Option<String>,
     pub(super) budget_limited_turn_ids: HashSet<String>,
+    /// Local monotonic start time for the active turn, derived from the app-server timestamp.
+    pub(super) active_turn_started_at: Option<Instant>,
     pub(super) goal_status_active_turn_started_at: Option<Instant>,
 }
 
@@ -22,18 +24,21 @@ impl TurnLifecycleState {
             agent_turn_running: false,
             last_turn_id: None,
             budget_limited_turn_ids: HashSet::new(),
+            active_turn_started_at: None,
             goal_status_active_turn_started_at: None,
         }
     }
 
     pub(super) fn start(&mut self, now: Instant) {
         self.agent_turn_running = true;
+        self.active_turn_started_at = Some(now);
         self.goal_status_active_turn_started_at = Some(now);
         self.sleep_inhibitor.set_turn_running(/*turn_running*/ true);
     }
 
     pub(super) fn finish(&mut self) {
         self.agent_turn_running = false;
+        self.active_turn_started_at = None;
         self.goal_status_active_turn_started_at = None;
         self.sleep_inhibitor
             .set_turn_running(/*turn_running*/ false);
@@ -41,6 +46,7 @@ impl TurnLifecycleState {
 
     pub(super) fn restore_running(&mut self, running: bool, now: Instant) {
         self.agent_turn_running = running;
+        self.active_turn_started_at = running.then_some(now);
         self.goal_status_active_turn_started_at = running.then_some(now);
         self.sleep_inhibitor.set_turn_running(running);
     }
diff --git a/codex-rs/tui/src/chatwidget/user_messages.rs b/codex-rs/tui/src/chatwidget/user_messages.rs
index 6cfb739da5..9e452ece08 100644
--- a/codex-rs/tui/src/chatwidget/user_messages.rs
+++ b/codex-rs/tui/src/chatwidget/user_messages.rs
@@ -10,6 +10,7 @@ use std::collections::HashSet;
 use std::collections::VecDeque;
 use std::ops::Deref;
 use std::path::PathBuf;
+use std::time::Instant;
 
 use crate::bottom_pane::LocalImageAttachment;
 use crate::bottom_pane::MentionBinding;
@@ -133,6 +134,7 @@ pub(crate) struct ThreadInputState {
     pub(super) active_collaboration_mask: Option<CollaborationModeMask>,
     pub(super) task_running: bool,
     pub(super) agent_turn_running: bool,
+    pub(super) active_turn_started_at: Option<Instant>,
 }
 
 impl From<String> for UserMessage {
diff --git a/codex-rs/tui/src/history_cell/separators.rs b/codex-rs/tui/src/history_cell/separators.rs
index 3ca20626a2..fe5b92d030 100644
--- a/codex-rs/tui/src/history_cell/separators.rs
+++ b/codex-rs/tui/src/history_cell/separators.rs
@@ -10,22 +10,36 @@ use super::*;
 /// divider.
 pub struct FinalMessageSeparator {
     elapsed_seconds: Option<u64>,
+    finished_at: Option<String>,
     runtime_metrics: Option<RuntimeMetricsSummary>,
 }
 
 impl FinalMessageSeparator {
-    /// Creates a separator; completed turns should pass protocol turn duration when available.
+    /// Creates a separator; completed turns should pass the UI's local active-turn elapsed time.
     pub(crate) fn new(
         elapsed_seconds: Option<u64>,
+        finished_at: Option<String>,
         runtime_metrics: Option<RuntimeMetricsSummary>,
     ) -> Self {
         Self {
             elapsed_seconds,
+            finished_at,
             runtime_metrics,
         }
     }
 }
 
+#[derive(Debug)]
+pub struct PromptSubmittedAtSeparator {
+    submitted_at: String,
+}
+
+impl PromptSubmittedAtSeparator {
+    pub(crate) fn new(submitted_at: String) -> Self {
+        Self { submitted_at }
+    }
+}
+
 #[derive(Debug, Clone, Copy)]
 pub enum InterruptedStreamKind {
     Answer,
@@ -127,40 +141,79 @@ impl HistoryCell for ResumeHistoryLoadedSeparator {
 }
 impl HistoryCell for FinalMessageSeparator {
     fn display_lines(&self, width: u16) -> Vec<Line<'static>> {
-        let mut label_parts = Vec::new();
+        let mut label_text_parts = Vec::new();
+        let mut label_spans = Vec::<Span<'static>>::new();
         if let Some(elapsed_seconds) = self
             .elapsed_seconds
-            .filter(|seconds| *seconds > 60)
+            .filter(|seconds| *seconds > 0)
             .map(crate::status_indicator_widget::fmt_elapsed_compact)
         {
-            label_parts.push(format!("Worked for {elapsed_seconds}"));
+            let mut label = format!("Worked for {elapsed_seconds}");
+            label_spans.push("Worked for ".dim());
+            label_spans.push(elapsed_seconds.fg(Color::Cyan).add_modifier(Modifier::BOLD));
+            if let Some(finished_at) = &self.finished_at {
+                label.push_str(" at ");
+                label.push_str(finished_at);
+                label_spans.push(" at ".dim());
+                label_spans.push(
+                    finished_at
+                        .clone()
+                        .fg(Color::Yellow)
+                        .add_modifier(Modifier::BOLD),
+                );
+            }
+            label_text_parts.push(label);
         }
         if let Some(metrics_label) = self.runtime_metrics.and_then(runtime_metrics_label) {
-            label_parts.push(metrics_label);
+            if !label_spans.is_empty() {
+                label_spans.push(" ■ ".dim());
+            }
+            label_spans.push(metrics_label.clone().dim());
+            label_text_parts.push(metrics_label);
         }
-        if label_parts.is_empty() {
+        if label_text_parts.is_empty() {
             return vec![Line::from_iter(["─".repeat(width as usize).dim()])];
         }
 
-        let label = format!("─ {} ─", label_parts.join(" ■ "));
-        let (label, _suffix, label_width) = take_prefix_by_width(&label, width as usize);
-        vec![
-            Line::from_iter([
-                label,
-                "─".repeat((width as usize).saturating_sub(label_width)),
-            ])
-            .dim(),
-        ]
+        let label_text = label_text_parts.join(" ■ ");
+        let label = format!("─ {label_text} ─");
+        let label_width = UnicodeWidthStr::width(label.as_str());
+        if label_width > width as usize {
+            let (label, _suffix, label_width) = take_prefix_by_width(&label, width as usize);
+            return vec![
+                Line::from_iter([
+                    label,
+                    "─".repeat((width as usize).saturating_sub(label_width)),
+                ])
+                .dim(),
+            ];
+        }
+
+        let mut spans = Vec::new();
+        spans.push("─ ".dim());
+        spans.extend(label_spans);
+        spans.push(" ─".dim());
+        spans.push(
+            "─"
+                .repeat((width as usize).saturating_sub(label_width))
+                .dim(),
+        );
+        vec![Line::from(spans)]
     }
 
     fn raw_lines(&self) -> Vec<Line<'static>> {
         let mut label_parts = Vec::new();
         if let Some(elapsed_seconds) = self
             .elapsed_seconds
-            .filter(|seconds| *seconds > 60)
+            .filter(|seconds| *seconds > 0)
             .map(crate::status_indicator_widget::fmt_elapsed_compact)
         {
-            label_parts.push(format!("Worked for {elapsed_seconds}"));
+            let mut label = format!("Worked for {elapsed_seconds}");
+            if let Some(finished_at) = &self.finished_at {
+                label.push_str(" at ");
+                label.push_str(finished_at);
+            }
+            label_parts.push(label);
         }
         if let Some(metrics_label) = self.runtime_metrics.and_then(runtime_metrics_label) {
             label_parts.push(metrics_label);
@@ -173,6 +226,42 @@ impl HistoryCell for FinalMessageSeparator {
     }
 }
 
+impl HistoryCell for PromptSubmittedAtSeparator {
+    fn display_lines(&self, width: u16) -> Vec<Line<'static>> {
+        let label_text = format!("Submitted at {}", self.submitted_at);
+        let label = format!("─ {label_text} ─");
+        let label_width = UnicodeWidthStr::width(label.as_str());
+        if label_width > width as usize {
+            let (label, _suffix, label_width) = take_prefix_by_width(&label, width as usize);
+            return vec![
+                Line::from_iter([
+                    label,
+                    "─".repeat((width as usize).saturating_sub(label_width)),
+                ])
+                .dim(),
+            ];
+        }
+
+        let spans = vec![
+            "─ ".dim(),
+            "Submitted at ".dim(),
+            self.submitted_at
+                .clone()
+                .fg(Color::Yellow)
+                .add_modifier(Modifier::BOLD),
+            " ─".dim(),
+            "─"
+                .repeat((width as usize).saturating_sub(label_width))
+                .dim(),
+        ];
+        vec![Line::from(spans)]
+    }
+
+    fn raw_lines(&self) -> Vec<Line<'static>> {
+        vec![Line::from(format!("Submitted at {}", self.submitted_at))]
+    }
+}
+
 impl HistoryCell for InterruptedStreamSeparator {
     fn display_lines(&self, width: u16) -> Vec<Line<'static>> {
         let label_text = self.label();
diff --git a/codex-rs/tui/src/history_cell/tests.rs b/codex-rs/tui/src/history_cell/tests.rs
index 56796d1171..df67c460be 100644
--- a/codex-rs/tui/src/history_cell/tests.rs
+++ b/codex-rs/tui/src/history_cell/tests.rs
@@ -545,7 +545,7 @@ fn unified_exec_interaction_cell_renders_wait() {
 }
 
 #[test]
-fn final_message_separator_hides_short_worked_label_and_includes_runtime_metrics() {
+fn final_message_separator_includes_short_worked_label_and_runtime_metrics() {
     let summary = RuntimeMetricsSummary {
         tool_calls: RuntimeMetricTotals {
             count: 3,
@@ -576,11 +576,11 @@ fn final_message_separator_hides_short_worked_label_and_includes_runtime_metrics
         turn_ttft_ms: 0,
         turn_ttfm_ms: 0,
     };
-    let cell = FinalMessageSeparator::new(Some(12), Some(summary));
+    let cell = FinalMessageSeparator::new(Some(12), /*finished_at*/ None, Some(summary));
     let rendered = render_lines(&cell.display_lines(/*width*/ 600));
 
     assert_eq!(rendered.len(), 1);
-    assert!(!rendered[0].contains("Worked for"));
+    assert!(rendered[0].contains("Worked for 12s"));
     assert!(rendered[0].contains("Local tools: 3 calls (2.5s)"));
     assert!(rendered[0].contains("Inference: 2 calls (1.2s)"));
     assert!(rendered[0].contains("WebSocket: 1 events send (700ms)"));
@@ -594,7 +594,11 @@ fn final_message_separator_hides_short_worked_label_and_includes_runtime_metrics
 
 #[test]
 fn final_message_separator_includes_worked_label_after_one_minute() {
-    let cell = FinalMessageSeparator::new(Some(61), /*runtime_metrics*/ None);
+    let cell = FinalMessageSeparator::new(
+        Some(61),
+        /*finished_at*/ None,
+        /*runtime_metrics*/ None,
+    );
     let rendered = render_lines(&cell.display_lines(/*width*/ 200));
 
     assert_eq!(rendered.len(), 1);
diff --git a/codex-rs/tui/src/status_indicator_widget.rs b/codex-rs/tui/src/status_indicator_widget.rs
index 2a37aa7c27..60ffdf54e1 100644
--- a/codex-rs/tui/src/status_indicator_widget.rs
+++ b/codex-rs/tui/src/status_indicator_widget.rs
@@ -169,6 +169,13 @@ impl StatusIndicatorWidget {
         self.show_interrupt_hint = visible;
     }
 
+    pub(crate) fn set_timer_started_at(&mut self, started_at: Instant) {
+        self.elapsed_running = Duration::ZERO;
+        self.last_resume_at = started_at;
+        self.is_paused = false;
+        self.frame_requester.schedule_frame();
+    }
+
     pub(crate) fn set_interrupt_binding(&mut self, binding: Option<KeyBinding>) {
         self.interrupt_binding = binding;
     }
