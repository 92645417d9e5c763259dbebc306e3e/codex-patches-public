diff --git a/codex-rs/analytics/src/analytics_client_tests.rs b/codex-rs/analytics/src/analytics_client_tests.rs
index 0f759fd938..bc6f29a2bb 100644
--- a/codex-rs/analytics/src/analytics_client_tests.rs
+++ b/codex-rs/analytics/src/analytics_client_tests.rs
@@ -295,6 +295,7 @@ fn sample_thread_resume_response_with_source(
             thread_source,
             parent_thread_id,
         ),
+        history_window_metadata: Default::default(),
         model: model.to_string(),
         model_provider: "openai".to_string(),
         service_tier: None,
diff --git a/codex-rs/analytics/src/client_tests.rs b/codex-rs/analytics/src/client_tests.rs
index ac50d6ebc7..95f2ee935e 100644
--- a/codex-rs/analytics/src/client_tests.rs
+++ b/codex-rs/analytics/src/client_tests.rs
@@ -323,6 +323,7 @@ fn sample_thread_start_response() -> ClientResponsePayload {
 fn sample_thread_resume_response() -> ClientResponsePayload {
     ClientResponsePayload::ThreadResume(ThreadResumeResponse {
         thread: sample_thread("thread-2"),
+        history_window_metadata: Default::default(),
         model: "gpt-5".to_string(),
         model_provider: "openai".to_string(),
         service_tier: None,
diff --git a/codex-rs/app-server-client/src/lib.rs b/codex-rs/app-server-client/src/lib.rs
index 4eec8a2348..08cbaf8bba 100644
--- a/codex-rs/app-server-client/src/lib.rs
+++ b/codex-rs/app-server-client/src/lib.rs
@@ -1279,6 +1279,7 @@ mod tests {
                 params: codex_app_server_protocol::ThreadReadParams {
                     thread_id: "missing-thread".to_string(),
                     include_turns: false,
+                    history_window: None,
                 },
             })
             .await
@@ -1333,6 +1334,7 @@ mod tests {
                     params: codex_app_server_protocol::ThreadReadParams {
                         thread_id: response.thread.id.clone(),
                         include_turns: false,
+                        history_window: None,
                     },
                 },
             )
diff --git a/codex-rs/app-server-protocol/schema/json/ClientRequest.json b/codex-rs/app-server-protocol/schema/json/ClientRequest.json
index 8fc34b76e6..9fa5d11c47 100644
--- a/codex-rs/app-server-protocol/schema/json/ClientRequest.json
+++ b/codex-rs/app-server-protocol/schema/json/ClientRequest.json
@@ -3790,6 +3790,24 @@
       ],
       "type": "string"
     },
+    "ThreadHistoryWindow": {
+      "oneOf": [
+        {
+          "description": "Include the complete persisted display history.",
+          "enum": [
+            "full"
+          ],
+          "type": "string"
+        },
+        {
+          "description": "Include only the display history after the latest compaction checkpoint that carries replacement history.",
+          "enum": [
+            "sinceLatestCompaction"
+          ],
+          "type": "string"
+        }
+      ]
+    },
     "ThreadInjectItemsParams": {
       "properties": {
         "items": {
@@ -4085,6 +4103,17 @@
     },
     "ThreadResumeInitialTurnsPageParams": {
       "properties": {
+        "historyWindow": {
+          "anyOf": [
+            {
+              "$ref": "#/definitions/ThreadHistoryWindow"
+            },
+            {
+              "type": "null"
+            }
+          ],
+          "description": "Which portion of persisted display history to expose; defaults to full history."
+        },
         "itemsView": {
           "anyOf": [
             {
@@ -6981,4 +7010,4 @@
     }
   ],
   "title": "ClientRequest"
-}
\ No newline at end of file
+}
diff --git a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json
index d841e6b8e5..8b2823bd07 100644
--- a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json
+++ b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json
@@ -17934,6 +17934,24 @@
         ],
         "type": "string"
       },
+      "ThreadHistoryWindow": {
+        "oneOf": [
+          {
+            "description": "Include the complete persisted display history.",
+            "enum": [
+              "full"
+            ],
+            "type": "string"
+          },
+          {
+            "description": "Include only the display history after the latest compaction checkpoint that carries replacement history.",
+            "enum": [
+              "sinceLatestCompaction"
+            ],
+            "type": "string"
+          }
+        ]
+      },
       "ThreadId": {
         "type": "string"
       },
@@ -19245,8 +19263,50 @@
         "title": "ThreadRealtimeTranscriptDoneNotification",
         "type": "object"
       },
+      "ThreadResumeHistoryWindowMetadata": {
+        "properties": {
+          "baselineCompaction": {
+            "description": "Whether the selected window began at a replacement compaction.",
+            "type": "boolean"
+          },
+          "baselineCompactionRendered": {
+            "description": "Whether that baseline compaction is included as a visible transcript item.",
+            "type": "boolean"
+          },
+          "loadedContextWindows": {
+            "description": "Number of effective context windows included in this resume payload.",
+            "format": "uint32",
+            "minimum": 0.0,
+            "type": "integer"
+          },
+          "omittedContextWindows": {
+            "description": "Number of older context windows omitted from this resume payload.",
+            "format": "uint32",
+            "minimum": 0.0,
+            "type": "integer"
+          }
+        },
+        "required": [
+          "baselineCompaction",
+          "baselineCompactionRendered",
+          "loadedContextWindows",
+          "omittedContextWindows"
+        ],
+        "type": "object"
+      },
       "ThreadResumeInitialTurnsPageParams": {
         "properties": {
+          "historyWindow": {
+            "anyOf": [
+              {
+                "$ref": "#/definitions/v2/ThreadHistoryWindow"
+              },
+              {
+                "type": "null"
+              }
+            ],
+            "description": "Which portion of persisted display history to expose; defaults to full history."
+          },
           "itemsView": {
             "anyOf": [
               {
@@ -21251,4 +21311,4 @@
   },
   "title": "CodexAppServerProtocol",
   "type": "object"
-}
\ No newline at end of file
+}
diff --git a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json
index c6b7941c69..d12ff7b73f 100644
--- a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json
+++ b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json
@@ -15697,6 +15697,24 @@
       ],
       "type": "string"
     },
+    "ThreadHistoryWindow": {
+      "oneOf": [
+        {
+          "description": "Include the complete persisted display history.",
+          "enum": [
+            "full"
+          ],
+          "type": "string"
+        },
+        {
+          "description": "Include only the display history after the latest compaction checkpoint that carries replacement history.",
+          "enum": [
+            "sinceLatestCompaction"
+          ],
+          "type": "string"
+        }
+      ]
+    },
     "ThreadId": {
       "type": "string"
     },
@@ -17008,8 +17026,50 @@
       "title": "ThreadRealtimeTranscriptDoneNotification",
       "type": "object"
     },
+    "ThreadResumeHistoryWindowMetadata": {
+      "properties": {
+        "baselineCompaction": {
+          "description": "Whether the selected window began at a replacement compaction.",
+          "type": "boolean"
+        },
+        "baselineCompactionRendered": {
+          "description": "Whether that baseline compaction is included as a visible transcript item.",
+          "type": "boolean"
+        },
+        "loadedContextWindows": {
+          "description": "Number of effective context windows included in this resume payload.",
+          "format": "uint32",
+          "minimum": 0.0,
+          "type": "integer"
+        },
+        "omittedContextWindows": {
+          "description": "Number of older context windows omitted from this resume payload.",
+          "format": "uint32",
+          "minimum": 0.0,
+          "type": "integer"
+        }
+      },
+      "required": [
+        "baselineCompaction",
+        "baselineCompactionRendered",
+        "loadedContextWindows",
+        "omittedContextWindows"
+      ],
+      "type": "object"
+    },
     "ThreadResumeInitialTurnsPageParams": {
       "properties": {
+        "historyWindow": {
+          "anyOf": [
+            {
+              "$ref": "#/definitions/ThreadHistoryWindow"
+            },
+            {
+              "type": "null"
+            }
+          ],
+          "description": "Which portion of persisted display history to expose; defaults to full history."
+        },
         "itemsView": {
           "anyOf": [
             {
@@ -19013,4 +19073,4 @@
   },
   "title": "CodexAppServerProtocolV2",
   "type": "object"
-}
\ No newline at end of file
+}
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadReadParams.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadReadParams.json
index 5fb1bcc17b..f00dfb47f0 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadReadParams.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadReadParams.json
@@ -1,5 +1,25 @@
 {
   "$schema": "http://json-schema.org/draft-07/schema#",
+  "definitions": {
+    "ThreadHistoryWindow": {
+      "oneOf": [
+        {
+          "description": "Include the complete persisted display history.",
+          "enum": [
+            "full"
+          ],
+          "type": "string"
+        },
+        {
+          "description": "Include only the display history after the latest compaction checkpoint that carries replacement history.",
+          "enum": [
+            "sinceLatestCompaction"
+          ],
+          "type": "string"
+        }
+      ]
+    }
+  },
   "properties": {
     "includeTurns": {
       "description": "When true, include turns and their items from rollout history.",
@@ -14,4 +34,4 @@
   ],
   "title": "ThreadReadParams",
   "type": "object"
-}
\ No newline at end of file
+}
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadResumeParams.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadResumeParams.json
index 0cd7d9ae7b..980566a961 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadResumeParams.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadResumeParams.json
@@ -1265,8 +1265,37 @@
       ],
       "type": "string"
     },
+    "ThreadHistoryWindow": {
+      "oneOf": [
+        {
+          "description": "Include the complete persisted display history.",
+          "enum": [
+            "full"
+          ],
+          "type": "string"
+        },
+        {
+          "description": "Include only the display history after the latest compaction checkpoint that carries replacement history.",
+          "enum": [
+            "sinceLatestCompaction"
+          ],
+          "type": "string"
+        }
+      ]
+    },
     "ThreadResumeInitialTurnsPageParams": {
       "properties": {
+        "historyWindow": {
+          "anyOf": [
+            {
+              "$ref": "#/definitions/ThreadHistoryWindow"
+            },
+            {
+              "type": "null"
+            }
+          ],
+          "description": "Which portion of persisted display history to expose; defaults to full history."
+        },
         "itemsView": {
           "anyOf": [
             {
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadResumeResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadResumeResponse.json
index 4459c84b1e..ea496466e0 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadResumeResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadResumeResponse.json
@@ -1969,6 +1969,37 @@
         }
       ]
     },
+    "ThreadResumeHistoryWindowMetadata": {
+      "properties": {
+        "baselineCompaction": {
+          "description": "Whether the selected window began at a replacement compaction.",
+          "type": "boolean"
+        },
+        "baselineCompactionRendered": {
+          "description": "Whether that baseline compaction is included as a visible transcript item.",
+          "type": "boolean"
+        },
+        "loadedContextWindows": {
+          "description": "Number of effective context windows included in this resume payload.",
+          "format": "uint32",
+          "minimum": 0.0,
+          "type": "integer"
+        },
+        "omittedContextWindows": {
+          "description": "Number of older context windows omitted from this resume payload.",
+          "format": "uint32",
+          "minimum": 0.0,
+          "type": "integer"
+        }
+      },
+      "required": [
+        "baselineCompaction",
+        "baselineCompactionRendered",
+        "loadedContextWindows",
+        "omittedContextWindows"
+      ],
+      "type": "object"
+    },
     "ThreadSource": {
       "type": "string"
     },
@@ -2515,4 +2546,4 @@
   ],
   "title": "ThreadResumeResponse",
   "type": "object"
-}
\ No newline at end of file
+}
diff --git a/codex-rs/app-server-protocol/schema/typescript/v2/ThreadHistoryWindow.ts b/codex-rs/app-server-protocol/schema/typescript/v2/ThreadHistoryWindow.ts
new file mode 100644
index 0000000000..f6b6bb0302
--- /dev/null
+++ b/codex-rs/app-server-protocol/schema/typescript/v2/ThreadHistoryWindow.ts
@@ -0,0 +1,5 @@
+// GENERATED CODE! DO NOT MODIFY BY HAND!
+
+// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
+
+export type ThreadHistoryWindow = "full" | "sinceLatestCompaction";
diff --git a/codex-rs/app-server-protocol/schema/typescript/v2/ThreadReadParams.ts b/codex-rs/app-server-protocol/schema/typescript/v2/ThreadReadParams.ts
index c26e896481..1f59997d4e 100644
--- a/codex-rs/app-server-protocol/schema/typescript/v2/ThreadReadParams.ts
+++ b/codex-rs/app-server-protocol/schema/typescript/v2/ThreadReadParams.ts
@@ -2,8 +2,7 @@
 
 // This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
 
-export type ThreadReadParams = { threadId: string,
-/**
+export type ThreadReadParams = {threadId: string, /**
  * When true, include turns and their items from rollout history.
  */
-includeTurns?: boolean, };
+includeTurns?: boolean};
diff --git a/codex-rs/app-server-protocol/schema/typescript/v2/ThreadResumeHistoryWindowMetadata.ts b/codex-rs/app-server-protocol/schema/typescript/v2/ThreadResumeHistoryWindowMetadata.ts
new file mode 100644
index 0000000000..ffff8770ac
--- /dev/null
+++ b/codex-rs/app-server-protocol/schema/typescript/v2/ThreadResumeHistoryWindowMetadata.ts
@@ -0,0 +1,21 @@
+// GENERATED CODE! DO NOT MODIFY BY HAND!
+
+// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
+
+export type ThreadResumeHistoryWindowMetadata = {
+/**
+ * Number of effective context windows included in this resume payload.
+ */
+loadedContextWindows: number,
+/**
+ * Number of older context windows omitted from this resume payload.
+ */
+omittedContextWindows: number,
+/**
+ * Whether the selected window began at a replacement compaction.
+ */
+baselineCompaction: boolean,
+/**
+ * Whether that baseline compaction is included as a visible transcript item.
+ */
+baselineCompactionRendered: boolean, };
diff --git a/codex-rs/app-server-protocol/schema/typescript/v2/ThreadResumeInitialTurnsPageParams.ts b/codex-rs/app-server-protocol/schema/typescript/v2/ThreadResumeInitialTurnsPageParams.ts
index 2dbcd97807..f6a3136ac4 100644
--- a/codex-rs/app-server-protocol/schema/typescript/v2/ThreadResumeInitialTurnsPageParams.ts
+++ b/codex-rs/app-server-protocol/schema/typescript/v2/ThreadResumeInitialTurnsPageParams.ts
@@ -2,6 +2,7 @@
 
 // This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
 import type { SortDirection } from "./SortDirection";
+import type { ThreadHistoryWindow } from "./ThreadHistoryWindow";
 import type { TurnItemsView } from "./TurnItemsView";
 
 export type ThreadResumeInitialTurnsPageParams = {
@@ -16,4 +17,8 @@ sortDirection?: SortDirection | null,
 /**
  * How much item detail to include for each returned turn; defaults to summary.
  */
-itemsView?: TurnItemsView | null, };
+itemsView?: TurnItemsView | null,
+/**
+ * Which portion of persisted display history to expose; defaults to full history.
+ */
+historyWindow?: ThreadHistoryWindow | null, };
diff --git a/codex-rs/app-server-protocol/schema/typescript/v2/index.ts b/codex-rs/app-server-protocol/schema/typescript/v2/index.ts
index 0dc4d96165..07738eb337 100644
--- a/codex-rs/app-server-protocol/schema/typescript/v2/index.ts
+++ b/codex-rs/app-server-protocol/schema/typescript/v2/index.ts
@@ -420,6 +420,7 @@ export type { ThreadGoalSetResponse } from "./ThreadGoalSetResponse";
 export type { ThreadGoalStatus } from "./ThreadGoalStatus";
 export type { ThreadGoalUpdatedNotification } from "./ThreadGoalUpdatedNotification";
 export type { ThreadHistoryMode } from "./ThreadHistoryMode";
+export type { ThreadHistoryWindow } from "./ThreadHistoryWindow";
 export type { ThreadInjectItemsParams } from "./ThreadInjectItemsParams";
 export type { ThreadInjectItemsResponse } from "./ThreadInjectItemsResponse";
 export type { ThreadItem } from "./ThreadItem";
@@ -443,6 +444,7 @@ export type { ThreadRealtimeStartTransport } from "./ThreadRealtimeStartTranspor
 export type { ThreadRealtimeStartedNotification } from "./ThreadRealtimeStartedNotification";
 export type { ThreadRealtimeTranscriptDeltaNotification } from "./ThreadRealtimeTranscriptDeltaNotification";
 export type { ThreadRealtimeTranscriptDoneNotification } from "./ThreadRealtimeTranscriptDoneNotification";
+export type { ThreadResumeHistoryWindowMetadata } from "./ThreadResumeHistoryWindowMetadata";
 export type { ThreadResumeInitialTurnsPageParams } from "./ThreadResumeInitialTurnsPageParams";
 export type { ThreadResumeParams } from "./ThreadResumeParams";
 export type { ThreadResumeResponse } from "./ThreadResumeResponse";
diff --git a/codex-rs/app-server-protocol/src/protocol/common.rs b/codex-rs/app-server-protocol/src/protocol/common.rs
index a551ff3036..c28273624e 100644
--- a/codex-rs/app-server-protocol/src/protocol/common.rs
+++ b/codex-rs/app-server-protocol/src/protocol/common.rs
@@ -631,6 +631,7 @@ client_request_definitions! {
     },
     ThreadRead => "thread/read" {
         params: v2::ThreadReadParams,
+        inspect_params: true,
         serialization: thread_id(params.thread_id),
         response: v2::ThreadReadResponse,
     },
@@ -2107,6 +2108,7 @@ mod tests {
                 limit: None,
                 sort_direction: None,
                 items_view: None,
+                history_window: None,
             },
         };
         assert_eq!(thread_turns_list.serialization_scope(), None);
diff --git a/codex-rs/app-server-protocol/src/protocol/thread_history.rs b/codex-rs/app-server-protocol/src/protocol/thread_history.rs
index 5978f5b7d6..db0ce4c596 100644
--- a/codex-rs/app-server-protocol/src/protocol/thread_history.rs
+++ b/codex-rs/app-server-protocol/src/protocol/thread_history.rs
@@ -583,6 +583,10 @@ impl ThreadHistoryBuilder {
             return;
         }
 
+        if self.agent_message_already_visible(&text, phase.as_ref(), memory_citation.as_ref()) {
+            return;
+        }
+
         let id = self.next_item_id();
         self.push_item_in_current_turn(ThreadItem::AgentMessage {
             id,
@@ -1217,6 +1221,12 @@ impl ThreadHistoryBuilder {
     }
 
     fn handle_context_compacted(&mut self, _payload: &ContextCompactedEvent) {
+        if let Some(turn) = self.current_turn.as_ref()
+            && turn.saw_compaction
+            && turn.items.is_empty()
+        {
+            return;
+        }
         let id = self.next_item_id();
         self.push_item_in_current_turn(ThreadItem::ContextCompaction { id });
     }
@@ -1324,7 +1334,23 @@ impl ThreadHistoryBuilder {
             .filter(|turn| turn.id == payload.turn_id)
         {
             let changed_turn = mark_completed(current_turn);
+            let appended_item = if current_turn.push_last_agent_message_if_missing(
+                self.next_item_index,
+                payload.last_agent_message.as_deref(),
+            ) {
+                self.next_item_index = self.next_item_index.saturating_add(1);
+                current_turn
+                    .items
+                    .last()
+                    .cloned()
+                    .map(|item| (current_turn.id.clone(), item))
+            } else {
+                None
+            };
             self.record_changed_turn(changed_turn);
+            if let Some((turn_id, item)) = appended_item {
+                self.record_changed_item(turn_id, item);
+            }
             self.finish_current_turn();
             return;
         }
@@ -1340,14 +1366,46 @@ impl ThreadHistoryBuilder {
             turn.completed_at = payload.completed_at;
             turn.duration_ms = payload.duration_ms;
             let changed_turn = ThreadHistoryTurnChange::from_turn(turn);
+            let appended_item = if append_last_agent_message_to_turn(
+                turn,
+                self.next_item_index,
+                payload.last_agent_message.as_deref(),
+            ) {
+                self.next_item_index = self.next_item_index.saturating_add(1);
+                turn.items
+                    .last()
+                    .cloned()
+                    .map(|item| (turn.id.clone(), item))
+            } else {
+                None
+            };
             self.record_changed_turn(changed_turn);
+            if let Some((turn_id, item)) = appended_item {
+                self.record_changed_item(turn_id, item);
+            }
             return;
         }
 
         // If the completion event cannot be matched, apply it to the active turn.
         if let Some(current_turn) = self.current_turn.as_mut() {
             let changed_turn = mark_completed(current_turn);
+            let appended_item = if current_turn.push_last_agent_message_if_missing(
+                self.next_item_index,
+                payload.last_agent_message.as_deref(),
+            ) {
+                self.next_item_index = self.next_item_index.saturating_add(1);
+                current_turn
+                    .items
+                    .last()
+                    .cloned()
+                    .map(|item| (current_turn.id.clone(), item))
+            } else {
+                None
+            };
             self.record_changed_turn(changed_turn);
+            if let Some((turn_id, item)) = appended_item {
+                self.record_changed_item(turn_id, item);
+            }
             self.finish_current_turn();
         }
     }
@@ -1445,6 +1503,30 @@ impl ThreadHistoryBuilder {
         }
     }
 
+    fn agent_message_already_visible(
+        &self,
+        text: &str,
+        phase: Option<&MessagePhase>,
+        memory_citation: Option<&crate::protocol::v2::MemoryCitation>,
+    ) -> bool {
+        let item = self
+            .current_turn
+            .as_ref()
+            .and_then(|turn| turn.items.last())
+            .or_else(|| self.turns.last().and_then(|turn| turn.items.last()));
+        matches!(
+            item,
+            Some(ThreadItem::AgentMessage {
+                text: existing_text,
+                phase: existing_phase,
+                memory_citation: existing_memory_citation,
+                ..
+            }) if existing_text == text
+                && existing_phase.as_ref() == phase
+                && existing_memory_citation.as_ref() == memory_citation
+        )
+    }
+
     fn agent_message_visible_in_current_turn(
         &self,
         text: &str,
@@ -1633,6 +1715,30 @@ fn upsert_turn_item(items: &mut Vec<ThreadItem>, item: ThreadItem) -> &ThreadIte
     &items[inserted_item_index]
 }
 
+fn append_last_agent_message_to_turn(
+    turn: &mut Turn,
+    next_item_index: i64,
+    last_agent_message: Option<&str>,
+) -> bool {
+    let Some(last_agent_message) = last_agent_message.filter(|message| !message.is_empty()) else {
+        return false;
+    };
+    if turn
+        .items
+        .iter()
+        .any(|item| matches!(item, ThreadItem::AgentMessage { .. }))
+    {
+        return false;
+    }
+    turn.items.push(ThreadItem::AgentMessage {
+        id: format!("item-{next_item_index}"),
+        text: last_agent_message.to_string(),
+        phase: Some(MessagePhase::FinalAnswer),
+        memory_citation: None,
+    });
+    true
+}
+
 struct PendingTurn {
     id: String,
     items: Vec<ThreadItem>,
@@ -1652,6 +1758,33 @@ struct PendingTurn {
 }
 
 impl PendingTurn {
+    fn has_agent_message(&self) -> bool {
+        self.items
+            .iter()
+            .any(|item| matches!(item, ThreadItem::AgentMessage { .. }))
+    }
+
+    fn push_last_agent_message_if_missing(
+        &mut self,
+        next_item_index: i64,
+        last_agent_message: Option<&str>,
+    ) -> bool {
+        let Some(last_agent_message) = last_agent_message.filter(|message| !message.is_empty())
+        else {
+            return false;
+        };
+        if self.has_agent_message() {
+            return false;
+        }
+        self.items.push(ThreadItem::AgentMessage {
+            id: format!("item-{next_item_index}"),
+            text: last_agent_message.to_string(),
+            phase: Some(MessagePhase::FinalAnswer),
+            memory_citation: None,
+        });
+        true
+    }
+
     fn opened_explicitly(mut self) -> Self {
         self.opened_explicitly = true;
         self
@@ -1853,6 +1986,94 @@ mod tests {
         );
     }
 
+    #[test]
+    fn restores_final_agent_message_from_turn_complete() {
+        let turn_id = "turn-1".to_string();
+        let events = vec![
+            EventMsg::TurnStarted(TurnStartedEvent {
+                turn_id: turn_id.clone(),
+                trace_id: None,
+                started_at: None,
+                model_context_window: None,
+                collaboration_mode_kind: Default::default(),
+            }),
+            EventMsg::UserMessage(UserMessageEvent {
+                client_id: None,
+                message: "message 1".into(),
+                images: None,
+                text_elements: Vec::new(),
+                local_images: Vec::new(),
+                ..Default::default()
+            }),
+            EventMsg::TurnComplete(TurnCompleteEvent {
+                turn_id,
+                last_agent_message: Some("message 2".into()),
+                completed_at: None,
+                duration_ms: None,
+                time_to_first_token_ms: None,
+            }),
+        ];
+
+        let turns = build_turns_from_rollout_items(
+            &events
+                .into_iter()
+                .map(RolloutItem::EventMsg)
+                .collect::<Vec<_>>(),
+        );
+
+        assert_eq!(
+            turns[0].items[1],
+            ThreadItem::AgentMessage {
+                id: "item-2".into(),
+                text: "message 2".into(),
+                phase: Some(MessagePhase::FinalAnswer),
+                memory_citation: None,
+            }
+        );
+    }
+
+    #[test]
+    fn response_item_agent_message_prevents_turn_complete_duplicate() {
+        let turn_id = "turn-1".to_string();
+        let items = vec![
+            RolloutItem::EventMsg(EventMsg::TurnStarted(TurnStartedEvent {
+                turn_id: turn_id.clone(),
+                trace_id: None,
+                started_at: None,
+                model_context_window: None,
+                collaboration_mode_kind: Default::default(),
+            })),
+            RolloutItem::ResponseItem(codex_protocol::models::ResponseItem::Message {
+                id: None,
+                role: "assistant".into(),
+                content: vec![codex_protocol::models::ContentItem::OutputText {
+                    text: "message 2".into(),
+                }],
+                phase: Some(MessagePhase::FinalAnswer),
+                internal_chat_message_metadata_passthrough: None,
+            }),
+            RolloutItem::EventMsg(EventMsg::TurnComplete(TurnCompleteEvent {
+                turn_id,
+                last_agent_message: Some("message 2".into()),
+                completed_at: None,
+                duration_ms: None,
+                time_to_first_token_ms: None,
+            })),
+        ];
+
+        let turns = build_turns_from_rollout_items(&items);
+
+        assert_eq!(
+            turns[0].items,
+            vec![ThreadItem::AgentMessage {
+                id: "item-1".into(),
+                text: "message 2".into(),
+                phase: Some(MessagePhase::FinalAnswer),
+                memory_citation: None,
+            }]
+        );
+    }
+
     #[test]
     fn response_item_agent_message_skips_visible_legacy_duplicate_after_reasoning() {
         let turn_id = "turn-1".to_string();
@@ -3698,6 +3919,50 @@ mod tests {
         );
     }
 
+    #[test]
+    fn compacted_rollout_marker_suppresses_context_compacted_item() {
+        let items = vec![
+            RolloutItem::EventMsg(EventMsg::TurnStarted(TurnStartedEvent {
+                turn_id: "turn-compact".into(),
+                trace_id: None,
+                started_at: None,
+                model_context_window: None,
+                collaboration_mode_kind: Default::default(),
+            })),
+            RolloutItem::Compacted(CompactedItem {
+                message: String::new(),
+                replacement_history: Some(Vec::new()),
+                window_number: None,
+                first_window_id: None,
+                previous_window_id: None,
+                window_id: None,
+            }),
+            RolloutItem::EventMsg(EventMsg::ContextCompacted(ContextCompactedEvent)),
+            RolloutItem::EventMsg(EventMsg::TurnComplete(TurnCompleteEvent {
+                turn_id: "turn-compact".into(),
+                last_agent_message: None,
+                completed_at: None,
+                duration_ms: None,
+                time_to_first_token_ms: None,
+            })),
+        ];
+
+        let turns = build_turns_from_rollout_items(&items);
+        assert_eq!(
+            turns,
+            vec![Turn {
+                id: "turn-compact".into(),
+                status: TurnStatus::Completed,
+                error: None,
+                started_at: None,
+                completed_at: None,
+                duration_ms: None,
+                items_view: TurnItemsView::Full,
+                items: Vec::new(),
+            }]
+        );
+    }
+
     #[test]
     fn reconstructs_collab_resume_end_item() {
         let events = vec![
diff --git a/codex-rs/app-server-protocol/src/protocol/v2/tests.rs b/codex-rs/app-server-protocol/src/protocol/v2/tests.rs
index b30e176dbd..b12a1f748e 100644
--- a/codex-rs/app-server-protocol/src/protocol/v2/tests.rs
+++ b/codex-rs/app-server-protocol/src/protocol/v2/tests.rs
@@ -170,6 +170,7 @@ fn thread_resume_params_accept_turns_page_bootstrap() {
             limit: Some(25),
             sort_direction: Some(SortDirection::Asc),
             items_view: Some(TurnItemsView::Full),
+            history_window: None,
         })
     );
 }
@@ -202,6 +203,7 @@ fn thread_resume_response_round_trips_initial_turns_page() {
             name: None,
             turns: Vec::new(),
         },
+        history_window_metadata: Default::default(),
         model: "gpt-5".to_string(),
         model_provider: "openai".to_string(),
         service_tier: None,
diff --git a/codex-rs/app-server-protocol/src/protocol/v2/thread.rs b/codex-rs/app-server-protocol/src/protocol/v2/thread.rs
index 367a4a64c8..0eff0462a0 100644
--- a/codex-rs/app-server-protocol/src/protocol/v2/thread.rs
+++ b/codex-rs/app-server-protocol/src/protocol/v2/thread.rs
@@ -390,6 +390,11 @@ pub struct ThreadResumeParams {
     #[experimental("thread/resume.excludeTurns")]
     #[serde(default, skip_serializing_if = "std::ops::Not::not")]
     pub exclude_turns: bool,
+    /// Which portion of persisted display history to populate in `thread.turns`
+    /// when `excludeTurns` is false; defaults to full history.
+    #[experimental("thread/resume.historyWindow")]
+    #[ts(optional = nullable)]
+    pub history_window: Option<ThreadHistoryWindow>,
     /// When present, include a `thread/turns/list` page in the resume response
     /// so clients can bootstrap recent turns without a second request.
     #[experimental("thread/resume.initialTurnsPage")]
@@ -402,6 +407,10 @@ pub struct ThreadResumeParams {
 #[ts(export_to = "v2/")]
 pub struct ThreadResumeResponse {
     pub thread: Thread,
+    /// Metadata describing how `thread.turns` was selected for this resume.
+    #[experimental("thread/resume.historyWindowMetadata")]
+    #[serde(default)]
+    pub history_window_metadata: ThreadResumeHistoryWindowMetadata,
     pub model: String,
     pub model_provider: String,
     pub service_tier: Option<String>,
@@ -444,6 +453,20 @@ impl ThreadResumeResponse {
     }
 }
 
+#[derive(Default, Serialize, Deserialize, Debug, Clone, Copy, PartialEq, Eq, JsonSchema, TS)]
+#[serde(rename_all = "camelCase")]
+#[ts(export_to = "v2/")]
+pub struct ThreadResumeHistoryWindowMetadata {
+    /// Number of effective context windows included in this resume payload.
+    pub loaded_context_windows: u32,
+    /// Number of older context windows omitted from this resume payload.
+    pub omitted_context_windows: u32,
+    /// Whether the selected window began at a replacement compaction.
+    pub baseline_compaction: bool,
+    /// Whether that baseline compaction is included as a visible transcript item.
+    pub baseline_compaction_rendered: bool,
+}
+
 #[derive(Serialize, Deserialize, Debug, Clone, PartialEq, JsonSchema, TS)]
 #[serde(rename_all = "camelCase")]
 #[ts(export_to = "v2/")]
@@ -457,6 +480,21 @@ pub struct ThreadResumeInitialTurnsPageParams {
     /// How much item detail to include for each returned turn; defaults to summary.
     #[ts(optional = nullable)]
     pub items_view: Option<TurnItemsView>,
+    /// Which portion of persisted display history to expose; defaults to full history.
+    #[ts(optional = nullable)]
+    pub history_window: Option<ThreadHistoryWindow>,
+}
+
+#[derive(Serialize, Deserialize, Debug, Default, Clone, Copy, PartialEq, Eq, JsonSchema, TS)]
+#[serde(rename_all = "camelCase")]
+#[ts(export_to = "v2/")]
+pub enum ThreadHistoryWindow {
+    /// Include the complete persisted display history.
+    #[default]
+    Full,
+    /// Include only the display history after the latest compaction checkpoint
+    /// that carries replacement history.
+    SinceLatestCompaction,
 }
 
 #[derive(Serialize, Deserialize, Debug, Clone, PartialEq, JsonSchema, TS)]
@@ -1269,7 +1307,7 @@ pub enum ThreadActiveFlag {
     WaitingOnUserInput,
 }
 
-#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, JsonSchema, TS)]
+#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, JsonSchema, TS, ExperimentalApi)]
 #[serde(rename_all = "camelCase")]
 #[ts(export_to = "v2/")]
 pub struct ThreadReadParams {
@@ -1277,6 +1315,11 @@ pub struct ThreadReadParams {
     /// When true, include turns and their items from rollout history.
     #[serde(default, skip_serializing_if = "std::ops::Not::not")]
     pub include_turns: bool,
+    /// Which portion of persisted display history to expose when `includeTurns`
+    /// is true; defaults to full history.
+    #[experimental("thread/read.historyWindow")]
+    #[ts(optional = nullable)]
+    pub history_window: Option<ThreadHistoryWindow>,
 }
 
 #[derive(Serialize, Deserialize, Debug, Clone, PartialEq, JsonSchema, TS)]
@@ -1317,6 +1360,9 @@ pub struct ThreadTurnsListParams {
     /// How much item detail to include for each returned turn; defaults to summary.
     #[ts(optional = nullable)]
     pub items_view: Option<TurnItemsView>,
+    /// Which portion of persisted display history to expose; defaults to full history.
+    #[ts(optional = nullable)]
+    pub history_window: Option<ThreadHistoryWindow>,
 }
 
 #[derive(Serialize, Deserialize, Debug, Clone, PartialEq, JsonSchema, TS)]
diff --git a/codex-rs/app-server/README.md b/codex-rs/app-server/README.md
index 23d3e10543..19f038c2d2 100644
--- a/codex-rs/app-server/README.md
+++ b/codex-rs/app-server/README.md
@@ -316,7 +316,7 @@ Valid `personality` values are `"friendly"`, `"pragmatic"`, and `"none"`. When `
 
 To continue a stored session, call `thread/resume` with the `thread.id` you previously recorded. The response shape matches `thread/start`. When the stored session includes persisted token usage, the server emits `thread/tokenUsage/updated` immediately after the response so clients can render restored usage before the next turn starts. You can also pass the same configuration overrides supported by `thread/start`, including `approvalsReviewer`.
 
-By default, `thread/resume` includes the reconstructed turn history in `thread.turns`. Experimental clients can pass `excludeTurns: true` to return only thread metadata and live resume state, then call `thread/turns/list` separately if they want to page the turn history over the network. In that mode the server also skips replaying restored `thread/tokenUsage/updated`, which avoids rebuilding turns just to attribute historical usage.
+By default, `thread/resume` includes the reconstructed turn history in `thread.turns`. Experimental clients can pass `historyWindow: "sinceLatestCompaction"` on `thread/resume`, `thread/read`, or `thread/turns/list` to expose only display history from the latest replacement-history compaction checkpoint while ordinary clients keep full-history defaults. Experimental clients can pass `excludeTurns: true` to return only thread metadata and live resume state, then call `thread/turns/list` separately if they want to page the turn history over the network. In that mode the server also skips replaying restored `thread/tokenUsage/updated`, which avoids rebuilding turns just to attribute historical usage.
 
 Experimental clients that want the live resume subscription plus a turns page in one round trip can pass `initialTurnsPage`. It accepts the same `limit`, `sortDirection`, and `itemsView` controls as `thread/turns/list`; omitted controls use its defaults. The response includes `initialTurnsPage` with `nextCursor` and `backwardsCursor` for follow-up pagination.
 
diff --git a/codex-rs/app-server/src/request_processors.rs b/codex-rs/app-server/src/request_processors.rs
index 6c0fb99a56..74ab7fbd86 100644
--- a/codex-rs/app-server/src/request_processors.rs
+++ b/codex-rs/app-server/src/request_processors.rs
@@ -214,6 +214,7 @@ use codex_app_server_protocol::ThreadGoalUpdatedNotification;
 use codex_app_server_protocol::ThreadHistoryBuilder;
 #[cfg(test)]
 use codex_app_server_protocol::ThreadHistoryMode;
+use codex_app_server_protocol::ThreadHistoryWindow;
 use codex_app_server_protocol::ThreadIncrementElicitationParams;
 use codex_app_server_protocol::ThreadIncrementElicitationResponse;
 use codex_app_server_protocol::ThreadInjectItemsParams;
diff --git a/codex-rs/app-server/src/request_processors/thread_lifecycle.rs b/codex-rs/app-server/src/request_processors/thread_lifecycle.rs
index 82a49bae0b..84c3af0420 100644
--- a/codex-rs/app-server/src/request_processors/thread_lifecycle.rs
+++ b/codex-rs/app-server/src/request_processors/thread_lifecycle.rs
@@ -559,10 +559,17 @@ pub(super) async fn handle_pending_thread_resume_request(
     let request_id = pending.request_id;
     let connection_id = request_id.connection_id;
     let mut thread = pending.thread_summary;
+    let mut history_window_metadata =
+        codex_app_server_protocol::ThreadResumeHistoryWindowMetadata::default();
     if pending.include_turns {
+        let history_window = super::thread_processor::display_history_window(
+            &pending.history_items,
+            pending.history_window,
+        );
+        history_window_metadata = history_window.metadata;
         populate_thread_turns_from_history(
             &mut thread,
-            &pending.history_items,
+            &history_window.items,
             active_turn.as_ref(),
         );
     }
@@ -652,6 +659,7 @@ pub(super) async fn handle_pending_thread_resume_request(
 
     let response = ThreadResumeResponse {
         thread,
+        history_window_metadata,
         model,
         model_provider: model_provider_id,
         service_tier,
diff --git a/codex-rs/app-server/src/request_processors/thread_processor.rs b/codex-rs/app-server/src/request_processors/thread_processor.rs
index af8525873f..c2af08cddf 100644
--- a/codex-rs/app-server/src/request_processors/thread_processor.rs
+++ b/codex-rs/app-server/src/request_processors/thread_processor.rs
@@ -2206,13 +2206,15 @@ impl ThreadRequestProcessor {
         let ThreadReadParams {
             thread_id,
             include_turns,
+            history_window,
         } = params;
+        let history_window = history_window.unwrap_or_default();
 
         let thread_uuid = ThreadId::from_string(&thread_id)
             .map_err(|err| invalid_request(format!("invalid thread id: {err}")))?;
 
         let thread = self
-            .read_thread_view(thread_uuid, include_turns)
+            .read_thread_view(thread_uuid, include_turns, history_window)
             .await
             .map_err(thread_read_view_error)?;
         Ok(ThreadReadResponse { thread })
@@ -2223,6 +2225,7 @@ impl ThreadRequestProcessor {
         &self,
         thread_id: ThreadId,
         include_turns: bool,
+        history_window: ThreadHistoryWindow,
     ) -> Result<Thread, ThreadReadViewError> {
         let loaded_thread = self.thread_manager.get_thread(thread_id).await.ok();
         let mut thread = if include_turns {
@@ -2230,17 +2233,22 @@ impl ThreadRequestProcessor {
                 // Loaded thread with turns: use persisted metadata when it exists,
                 // but reconstruct turns from the live ThreadStore history.
                 let persisted_thread = self
-                    .load_persisted_thread_for_read(thread_id, /*include_turns*/ false)
+                    .load_persisted_thread_for_read(
+                        thread_id,
+                        /*include_turns*/ false,
+                        ThreadHistoryWindow::Full,
+                    )
                     .await?;
                 self.load_live_thread_view(
                     thread_id,
                     include_turns,
+                    history_window,
                     loaded_thread,
                     persisted_thread,
                 )
                 .await?
             } else if let Some(thread) = self
-                .load_persisted_thread_for_read(thread_id, include_turns)
+                .load_persisted_thread_for_read(thread_id, include_turns, history_window)
                 .await?
             {
                 // Unloaded thread with turns: load metadata and history together
@@ -2252,7 +2260,7 @@ impl ThreadRequestProcessor {
                 )));
             }
         } else if let Some(thread) = self
-            .load_persisted_thread_for_read(thread_id, include_turns)
+            .load_persisted_thread_for_read(thread_id, include_turns, history_window)
             .await?
         {
             // Persisted metadata-only read: no live thread state is needed.
@@ -2263,6 +2271,7 @@ impl ThreadRequestProcessor {
             self.load_live_thread_view(
                 thread_id,
                 include_turns,
+                history_window,
                 loaded_thread,
                 /*persisted_thread*/ None,
             )
@@ -2296,6 +2305,7 @@ impl ThreadRequestProcessor {
         &self,
         thread_id: ThreadId,
         include_turns: bool,
+        history_window: ThreadHistoryWindow,
     ) -> Result<Option<Thread>, ThreadReadViewError> {
         let fallback_provider = self.config.model_provider_id.as_str();
         match self
@@ -2311,7 +2321,8 @@ impl ThreadRequestProcessor {
                 let (mut thread, history) =
                     thread_from_stored_thread(stored_thread, fallback_provider, &self.config.cwd);
                 if include_turns && let Some(history) = history {
-                    thread.turns = build_api_turns_from_rollout_items(&history.items);
+                    let window = display_history_window(&history.items, history_window);
+                    thread.turns = build_api_turns_from_rollout_items(&window.items);
                 }
                 Ok(Some(thread))
             }
@@ -2340,6 +2351,7 @@ impl ThreadRequestProcessor {
         &self,
         thread_id: ThreadId,
         include_turns: bool,
+        history_window: ThreadHistoryWindow,
         loaded_thread: &CodexThread,
         persisted_thread: Option<Thread>,
     ) -> Result<Thread, ThreadReadViewError> {
@@ -2361,8 +2373,14 @@ impl ThreadRequestProcessor {
         } else {
             fallback_thread
         };
-        self.apply_thread_read_store_fields(thread_id, &mut thread, include_turns, loaded_thread)
-            .await?;
+        self.apply_thread_read_store_fields(
+            thread_id,
+            &mut thread,
+            include_turns,
+            history_window,
+            loaded_thread,
+        )
+        .await?;
         Ok(thread)
     }
 
@@ -2371,6 +2389,7 @@ impl ThreadRequestProcessor {
         thread_id: ThreadId,
         thread: &mut Thread,
         include_turns: bool,
+        history_window: ThreadHistoryWindow,
         loaded_thread: &CodexThread,
     ) -> Result<(), ThreadReadViewError> {
         self.attach_thread_name(thread_id, thread).await;
@@ -2380,7 +2399,8 @@ impl ThreadRequestProcessor {
                 .load_history(/*include_archived*/ true)
                 .await
                 .map_err(|err| thread_read_history_load_error(thread_id, err))?;
-            thread.turns = build_api_turns_from_rollout_items(&history.items);
+            let window = display_history_window(&history.items, history_window);
+            thread.turns = build_api_turns_from_rollout_items(&window.items);
         }
 
         Ok(())
@@ -2396,6 +2416,7 @@ impl ThreadRequestProcessor {
             limit,
             sort_direction,
             items_view,
+            history_window,
         } = params;
         let thread_uuid = ThreadId::from_string(&thread_id)
             .map_err(|err| invalid_request(format!("invalid thread id: {err}")))?;
@@ -2436,6 +2457,7 @@ impl ThreadRequestProcessor {
                 limit,
                 sort_direction: sort_direction.unwrap_or(SortDirection::Desc),
                 items_view: items_view.unwrap_or(TurnItemsView::Summary),
+                history_window: history_window.unwrap_or_default(),
             },
         )
     }
@@ -2713,9 +2735,11 @@ impl ThreadRequestProcessor {
             developer_instructions,
             personality,
             exclude_turns,
+            history_window,
             initial_turns_page,
         } = params;
         let include_turns = !exclude_turns;
+        let history_window = history_window.unwrap_or_default();
 
         let resume_result = if let Some(history) = history {
             self.resume_thread_from_history(history.as_slice())
@@ -2825,7 +2849,7 @@ impl ThreadRequestProcessor {
                     "thread",
                 );
 
-                let mut thread = match self
+                let (mut thread, history_window_metadata) = match self
                     .load_thread_from_resume_source_or_send_internal(
                         thread_id,
                         codex_thread.as_ref(),
@@ -2833,6 +2857,7 @@ impl ThreadRequestProcessor {
                         rollout_path.as_path(),
                         resume_source_thread,
                         include_turns,
+                        history_window,
                     )
                     .await
                 {
@@ -2900,6 +2925,7 @@ impl ThreadRequestProcessor {
                 let thread_originator = config_snapshot.originator.clone();
                 let response = ThreadResumeResponse {
                     thread,
+                    history_window_metadata,
                     model: session_configured.model,
                     model_provider: session_configured.model_provider_id,
                     service_tier: session_configured.service_tier,
@@ -3141,6 +3167,7 @@ impl ThreadRequestProcessor {
                     emit_thread_goal_update,
                     thread_goal_state_db,
                     include_turns: !params.exclude_turns,
+                    history_window: params.history_window.unwrap_or_default(),
                     initial_turns_page: params.initial_turns_page.clone(),
                     redact_resume_payloads,
                 }),
@@ -3290,7 +3317,14 @@ impl ThreadRequestProcessor {
         rollout_path: &Path,
         resume_source_thread: Option<StoredThread>,
         include_turns: bool,
-    ) -> std::result::Result<Thread, String> {
+        history_window: ThreadHistoryWindow,
+    ) -> std::result::Result<
+        (
+            Thread,
+            codex_app_server_protocol::ThreadResumeHistoryWindowMetadata,
+        ),
+        String,
+    > {
         let config_snapshot = thread.config_snapshot().await;
         let session_id = thread.session_configured().session_id.to_string();
         let thread = match thread_history {
@@ -3366,19 +3400,23 @@ impl ThreadRequestProcessor {
             )),
         };
         let mut thread = thread?;
+        let mut history_window_metadata =
+            codex_app_server_protocol::ThreadResumeHistoryWindowMetadata::default();
         thread.id = thread_id.to_string();
         thread.session_id = session_id;
         thread.path = Some(rollout_path.to_path_buf());
         if include_turns {
             let history_items = thread_history.get_rollout_items();
+            let window = display_history_window(&history_items, history_window);
+            history_window_metadata = window.metadata;
             populate_thread_turns_from_history(
                 &mut thread,
-                history_items,
+                &window.items,
                 /*active_turn*/ None,
             );
         }
         self.attach_thread_name(thread_id, &mut thread).await;
-        Ok(thread)
+        Ok((thread, history_window_metadata))
     }
 
     async fn attach_thread_name(&self, thread_id: ThreadId, thread: &mut Thread) {
@@ -3980,6 +4018,7 @@ struct ThreadTurnsPageOptions<'a> {
     limit: Option<u32>,
     sort_direction: SortDirection,
     items_view: TurnItemsView,
+    history_window: ThreadHistoryWindow,
 }
 
 fn build_thread_turns_page_response(
@@ -3989,8 +4028,9 @@ fn build_thread_turns_page_response(
     active_turn: Option<Turn>,
     options: ThreadTurnsPageOptions<'_>,
 ) -> Result<ThreadTurnsListResponse, JSONRPCErrorError> {
+    let items = display_history_items(items, options.history_window);
     let mut turns = reconstruct_thread_turns_for_turns_list(
-        items,
+        &items,
         loaded_status,
         has_live_running_thread,
         active_turn,
@@ -4021,11 +4061,120 @@ pub(super) fn build_thread_resume_initial_turns_page(
             limit: params.limit,
             sort_direction: params.sort_direction.unwrap_or(SortDirection::Desc),
             items_view: params.items_view.unwrap_or(TurnItemsView::Summary),
+            history_window: params.history_window.unwrap_or_default(),
         },
     )
     .map(Into::into)
 }
 
+pub(super) struct DisplayHistoryWindow {
+    pub(super) items: Vec<RolloutItem>,
+    pub(super) metadata: codex_app_server_protocol::ThreadResumeHistoryWindowMetadata,
+}
+
+pub(super) fn display_history_items(
+    items: &[RolloutItem],
+    history_window: ThreadHistoryWindow,
+) -> Vec<RolloutItem> {
+    display_history_window(items, history_window).items
+}
+
+pub(super) fn display_history_window(
+    items: &[RolloutItem],
+    history_window: ThreadHistoryWindow,
+) -> DisplayHistoryWindow {
+    let replacement_count = replacement_compaction_indices(items).count() as u32;
+    match history_window {
+        ThreadHistoryWindow::Full => DisplayHistoryWindow {
+            items: items.to_vec(),
+            metadata: codex_app_server_protocol::ThreadResumeHistoryWindowMetadata {
+                loaded_context_windows: replacement_count.saturating_add(1),
+                omitted_context_windows: 0,
+                baseline_compaction: false,
+                baseline_compaction_rendered: false,
+            },
+        },
+        ThreadHistoryWindow::SinceLatestCompaction => {
+            let Some(index) = latest_effective_replacement_compaction_index(items) else {
+                return DisplayHistoryWindow {
+                    items: items.to_vec(),
+                    metadata: codex_app_server_protocol::ThreadResumeHistoryWindowMetadata {
+                        loaded_context_windows: 1,
+                        omitted_context_windows: 0,
+                        baseline_compaction: false,
+                        baseline_compaction_rendered: false,
+                    },
+                };
+            };
+            let turn_start = items[..=index]
+                .iter()
+                .rposition(|item| matches!(item, RolloutItem::EventMsg(EventMsg::TurnStarted(_))))
+                .unwrap_or(index);
+            let mut window = items[turn_start..].to_vec();
+            let baseline_offset = index.saturating_sub(turn_start);
+            let baseline_compaction_rendered =
+                remove_baseline_context_compaction(&mut window, baseline_offset).is_none();
+            DisplayHistoryWindow {
+                items: window,
+                metadata: codex_app_server_protocol::ThreadResumeHistoryWindowMetadata {
+                    loaded_context_windows: 1,
+                    omitted_context_windows: replacement_compaction_indices(&items[..index]).count()
+                        as u32,
+                    baseline_compaction: true,
+                    baseline_compaction_rendered,
+                },
+            }
+        }
+    }
+}
+
+fn replacement_compaction_indices(items: &[RolloutItem]) -> impl Iterator<Item = usize> + '_ {
+    items.iter().enumerate().filter_map(|(index, item)| {
+        matches!(
+            item,
+            RolloutItem::Compacted(compacted) if compacted.replacement_history.is_some()
+        )
+        .then_some(index)
+    })
+}
+
+fn latest_effective_replacement_compaction_index(items: &[RolloutItem]) -> Option<usize> {
+    let effective_turn_ids: Vec<String> = build_api_turns_from_rollout_items(items)
+        .into_iter()
+        .map(|turn| turn.id)
+        .collect();
+    let replacement_indices = replacement_compaction_indices(items).collect::<Vec<_>>();
+    replacement_indices
+        .into_iter()
+        .rev()
+        .find(|index| match turn_id_at_or_before(items, *index) {
+            Some(turn_id) => effective_turn_ids.iter().any(|id| id == &turn_id),
+            None => true,
+        })
+}
+
+fn turn_id_at_or_before(items: &[RolloutItem], index: usize) -> Option<String> {
+    items[..=index].iter().rev().find_map(|item| match item {
+        RolloutItem::EventMsg(EventMsg::TurnStarted(started)) => Some(started.turn_id.clone()),
+        _ => None,
+    })
+}
+
+fn remove_baseline_context_compaction(
+    items: &mut Vec<RolloutItem>,
+    baseline_offset: usize,
+) -> Option<RolloutItem> {
+    let remove_offset = items
+        .iter()
+        .enumerate()
+        .skip(baseline_offset.saturating_add(1))
+        .take_while(|(_, item)| !matches!(item, RolloutItem::EventMsg(EventMsg::TurnStarted(_))))
+        .find_map(|(offset, item)| {
+            matches!(item, RolloutItem::EventMsg(EventMsg::ContextCompacted(_))).then_some(offset)
+        })?;
+    Some(items.remove(remove_offset))
+}
+
 fn apply_thread_turns_items_view(turns: &mut [Turn], items_view: TurnItemsView) {
     for turn in turns {
         match items_view {
diff --git a/codex-rs/app-server/src/request_processors/thread_processor_tests.rs b/codex-rs/app-server/src/request_processors/thread_processor_tests.rs
index 8c0d59efed..33271a297a 100644
--- a/codex-rs/app-server/src/request_processors/thread_processor_tests.rs
+++ b/codex-rs/app-server/src/request_processors/thread_processor_tests.rs
@@ -752,6 +752,7 @@ mod thread_processor_behavior_tests {
             developer_instructions: None,
             personality: None,
             exclude_turns: false,
+            history_window: None,
             initial_turns_page: None,
         };
         let config_snapshot = ThreadConfigSnapshot {
@@ -1510,4 +1511,78 @@ mod thread_processor_behavior_tests {
         );
         Ok(())
     }
+
+    #[test]
+    fn display_history_window_uses_latest_effective_replacement_compaction() {
+        let turn_started = |turn_id: &str| {
+            RolloutItem::EventMsg(codex_protocol::protocol::EventMsg::TurnStarted(
+                codex_protocol::protocol::TurnStartedEvent {
+                    turn_id: turn_id.to_string(),
+                    trace_id: None,
+                    started_at: None,
+                    model_context_window: None,
+                    collaboration_mode_kind: ModeKind::default(),
+                },
+            ))
+        };
+        let before = RolloutItem::Compacted(codex_protocol::protocol::CompactedItem {
+            message: "legacy".to_string(),
+            replacement_history: None,
+            window_number: None,
+            first_window_id: None,
+            previous_window_id: None,
+            window_id: None,
+        });
+        let compacted = RolloutItem::Compacted(codex_protocol::protocol::CompactedItem {
+            message: String::new(),
+            replacement_history: Some(Vec::new()),
+            window_number: None,
+            first_window_id: None,
+            previous_window_id: None,
+            window_id: None,
+        });
+        let context_compacted =
+            RolloutItem::EventMsg(codex_protocol::protocol::EventMsg::ContextCompacted(
+                codex_protocol::protocol::ContextCompactedEvent {},
+            ));
+        let rolled_back =
+            RolloutItem::EventMsg(codex_protocol::protocol::EventMsg::ThreadRolledBack(
+                codex_protocol::protocol::ThreadRolledBackEvent { num_turns: 1 },
+            ));
+        let items = vec![
+            before,
+            turn_started("baseline"),
+            compacted,
+            context_compacted,
+            turn_started("removed"),
+            rolled_back,
+            turn_started("replacement"),
+        ];
+
+        assert_eq!(
+            display_history_items(&items, ThreadHistoryWindow::Full).len(),
+            7
+        );
+        let window = display_history_window(&items, ThreadHistoryWindow::SinceLatestCompaction);
+        assert_eq!(window.metadata.loaded_context_windows, 1);
+        assert!(window.metadata.baseline_compaction);
+        assert!(!window.metadata.baseline_compaction_rendered);
+        assert!(
+            matches!(
+                &window.items[0],
+                RolloutItem::EventMsg(codex_protocol::protocol::EventMsg::TurnStarted(started))
+                    if started.turn_id == "baseline"
+            ),
+            "window should start at the turn boundary before the latest replacement-history compaction"
+        );
+        assert!(
+            !window.items.iter().any(|item| {
+                matches!(
+                    item,
+                    RolloutItem::EventMsg(codex_protocol::protocol::EventMsg::ContextCompacted(_))
+                )
+            }),
+            "baseline compaction marker should not replay as a visible banner"
+        );
+    }
 }
diff --git a/codex-rs/app-server/src/thread_state.rs b/codex-rs/app-server/src/thread_state.rs
index fec786373c..3efb12a230 100644
--- a/codex-rs/app-server/src/thread_state.rs
+++ b/codex-rs/app-server/src/thread_state.rs
@@ -38,6 +38,7 @@ pub(crate) struct PendingThreadResumeRequest {
     pub(crate) emit_thread_goal_update: bool,
     pub(crate) thread_goal_state_db: Option<StateDbHandle>,
     pub(crate) include_turns: bool,
+    pub(crate) history_window: codex_app_server_protocol::ThreadHistoryWindow,
     pub(crate) initial_turns_page:
         Option<codex_app_server_protocol::ThreadResumeInitialTurnsPageParams>,
     pub(crate) redact_resume_payloads: bool,
diff --git a/codex-rs/app-server/tests/suite/v2/external_agent_config.rs b/codex-rs/app-server/tests/suite/v2/external_agent_config.rs
index 3d4b09579e..53a7c776be 100644
--- a/codex-rs/app-server/tests/suite/v2/external_agent_config.rs
+++ b/codex-rs/app-server/tests/suite/v2/external_agent_config.rs
@@ -736,6 +736,7 @@ async fn external_agent_config_import_creates_session_rollouts() -> Result<()> {
         .send_thread_read_request(ThreadReadParams {
             thread_id: thread.id.clone(),
             include_turns: true,
+            history_window: None,
         })
         .await?;
     let response: JSONRPCResponse = timeout(
@@ -796,6 +797,7 @@ async fn external_agent_config_import_creates_session_rollouts() -> Result<()> {
         .send_thread_read_request(ThreadReadParams {
             thread_id: thread.id,
             include_turns: true,
+            history_window: None,
         })
         .await?;
     let response: JSONRPCResponse = timeout(
diff --git a/codex-rs/app-server/tests/suite/v2/thread_metadata_update.rs b/codex-rs/app-server/tests/suite/v2/thread_metadata_update.rs
index e20a5fab44..0d6c5f9c2e 100644
--- a/codex-rs/app-server/tests/suite/v2/thread_metadata_update.rs
+++ b/codex-rs/app-server/tests/suite/v2/thread_metadata_update.rs
@@ -108,6 +108,7 @@ async fn thread_metadata_update_patches_git_branch_and_returns_updated_thread()
         .send_thread_read_request(ThreadReadParams {
             thread_id: thread.id,
             include_turns: false,
+            history_window: None,
         })
         .await?;
     let read_resp: JSONRPCResponse = timeout(
@@ -471,6 +472,7 @@ async fn thread_metadata_update_can_clear_stored_git_fields() -> Result<()> {
         .send_thread_read_request(ThreadReadParams {
             thread_id,
             include_turns: false,
+            history_window: None,
         })
         .await?;
     let read_resp: JSONRPCResponse = timeout(
diff --git a/codex-rs/app-server/tests/suite/v2/thread_read.rs b/codex-rs/app-server/tests/suite/v2/thread_read.rs
index 22adba37c8..e8fb10b1cc 100644
--- a/codex-rs/app-server/tests/suite/v2/thread_read.rs
+++ b/codex-rs/app-server/tests/suite/v2/thread_read.rs
@@ -111,6 +111,7 @@ async fn thread_read_returns_summary_without_turns() -> Result<()> {
         .send_thread_read_request(ThreadReadParams {
             thread_id: conversation_id.clone(),
             include_turns: false,
+            history_window: None,
         })
         .await?;
     let read_resp: JSONRPCResponse = timeout(
@@ -166,6 +167,7 @@ async fn thread_read_can_include_turns() -> Result<()> {
         .send_thread_read_request(ThreadReadParams {
             thread_id: conversation_id.clone(),
             include_turns: true,
+            history_window: None,
         })
         .await?;
     let read_resp: JSONRPCResponse = timeout(
@@ -225,6 +227,7 @@ async fn paginated_stored_thread_allows_metadata_discovery_and_rejects_legacy_hi
         .send_thread_read_request(ThreadReadParams {
             thread_id: conversation_id.clone(),
             include_turns: false,
+            history_window: None,
         })
         .await?;
     let read_resp: JSONRPCResponse = timeout(
@@ -268,6 +271,7 @@ async fn paginated_stored_thread_allows_metadata_discovery_and_rejects_legacy_hi
         .send_thread_read_request(ThreadReadParams {
             thread_id: conversation_id.clone(),
             include_turns: true,
+            history_window: None,
         })
         .await?;
     assert_paginated_threads_unsupported(
@@ -285,6 +289,7 @@ async fn paginated_stored_thread_allows_metadata_discovery_and_rejects_legacy_hi
             limit: None,
             sort_direction: None,
             items_view: None,
+            history_window: None,
         })
         .await?;
     assert_paginated_threads_unsupported(
@@ -342,6 +347,7 @@ async fn thread_turns_list_can_page_backward_and_forward() -> Result<()> {
             limit: Some(2),
             sort_direction: Some(SortDirection::Desc),
             items_view: None,
+            history_window: None,
         })
         .await?;
     let read_resp: JSONRPCResponse = timeout(
@@ -369,6 +375,7 @@ async fn thread_turns_list_can_page_backward_and_forward() -> Result<()> {
             limit: Some(10),
             sort_direction: Some(SortDirection::Desc),
             items_view: None,
+            history_window: None,
         })
         .await?;
     let read_resp: JSONRPCResponse = timeout(
@@ -388,6 +395,7 @@ async fn thread_turns_list_can_page_backward_and_forward() -> Result<()> {
             limit: Some(10),
             sort_direction: Some(SortDirection::Asc),
             items_view: None,
+            history_window: None,
         })
         .await?;
     let read_resp: JSONRPCResponse = timeout(
@@ -525,6 +533,7 @@ async fn thread_turns_list_reads_store_history_without_rollout_path() -> Result<
                 limit: Some(10),
                 sort_direction: Some(SortDirection::Asc),
                 items_view: None,
+                history_window: None,
             },
         })
         .await?
@@ -609,6 +618,7 @@ async fn thread_read_loaded_include_turns_reads_store_history_without_rollout_pa
             params: ThreadReadParams {
                 thread_id: thread.id,
                 include_turns: true,
+                history_window: None,
             },
         })
         .await?
@@ -732,6 +742,7 @@ async fn thread_read_can_return_archived_threads_by_id() -> Result<()> {
         .send_thread_read_request(ThreadReadParams {
             thread_id: conversation_id.clone(),
             include_turns: false,
+            history_window: None,
         })
         .await?;
     let read_resp: JSONRPCResponse = timeout(
@@ -779,6 +790,7 @@ async fn thread_resume_initial_turns_page_matches_requested_turns_list_page() ->
             limit: Some(2),
             sort_direction: Some(SortDirection::Asc),
             items_view: Some(TurnItemsView::NotLoaded),
+            history_window: None,
         })
         .await?;
     let turns_list_resp: JSONRPCResponse = timeout(
@@ -796,6 +808,7 @@ async fn thread_resume_initial_turns_page_matches_requested_turns_list_page() ->
                 limit: Some(2),
                 sort_direction: Some(SortDirection::Asc),
                 items_view: Some(TurnItemsView::NotLoaded),
+                history_window: None,
             }),
             ..Default::default()
         })
@@ -850,6 +863,7 @@ async fn thread_turns_list_rejects_cursor_when_anchor_turn_is_rolled_back() -> R
             limit: Some(2),
             sort_direction: Some(SortDirection::Desc),
             items_view: None,
+            history_window: None,
         })
         .await?;
     let read_resp: JSONRPCResponse = timeout(
@@ -875,6 +889,7 @@ async fn thread_turns_list_rejects_cursor_when_anchor_turn_is_rolled_back() -> R
             limit: Some(10),
             sort_direction: Some(SortDirection::Asc),
             items_view: None,
+            history_window: None,
         })
         .await?;
     let read_err: JSONRPCError = timeout(
@@ -927,6 +942,7 @@ async fn thread_read_returns_forked_from_id_for_forked_threads() -> Result<()> {
         .send_thread_read_request(ThreadReadParams {
             thread_id: forked.id,
             include_turns: false,
+            history_window: None,
         })
         .await?;
     let read_resp: JSONRPCResponse = timeout(
@@ -972,6 +988,7 @@ async fn thread_read_loaded_thread_returns_precomputed_path_before_materializati
         .send_thread_read_request(ThreadReadParams {
             thread_id: thread.id.clone(),
             include_turns: false,
+            history_window: None,
         })
         .await?;
     let read_resp: JSONRPCResponse = timeout(
@@ -1039,6 +1056,7 @@ async fn thread_name_set_is_reflected_in_read_list_and_resume() -> Result<()> {
         .send_thread_read_request(ThreadReadParams {
             thread_id: conversation_id.clone(),
             include_turns: false,
+            history_window: None,
         })
         .await?;
     let read_resp: JSONRPCResponse = timeout(
@@ -1180,6 +1198,7 @@ async fn thread_read_include_turns_rejects_unmaterialized_loaded_thread() -> Res
         .send_thread_read_request(ThreadReadParams {
             thread_id: thread.id.clone(),
             include_turns: true,
+            history_window: None,
         })
         .await?;
     let read_err: JSONRPCError = timeout(
@@ -1234,6 +1253,7 @@ async fn thread_turns_list_rejects_unmaterialized_loaded_thread() -> Result<()>
             limit: None,
             sort_direction: None,
             items_view: None,
+            history_window: None,
         })
         .await?;
     let read_err: JSONRPCError = timeout(
@@ -1341,6 +1361,7 @@ async fn thread_read_reports_system_error_idle_flag_after_failed_turn() -> Resul
         .send_thread_read_request(ThreadReadParams {
             thread_id: thread.id,
             include_turns: false,
+            history_window: None,
         })
         .await?;
     let read_resp: JSONRPCResponse = timeout(
@@ -1419,6 +1440,7 @@ async fn read_single_turn_items_view(
             limit: Some(10),
             sort_direction: Some(SortDirection::Asc),
             items_view,
+            history_window: None,
         })
         .await?;
     let read_resp: JSONRPCResponse = timeout(
diff --git a/codex-rs/app-server/tests/suite/v2/thread_resume.rs b/codex-rs/app-server/tests/suite/v2/thread_resume.rs
index a6e851f1d7..8b7b69e743 100644
--- a/codex-rs/app-server/tests/suite/v2/thread_resume.rs
+++ b/codex-rs/app-server/tests/suite/v2/thread_resume.rs
@@ -965,6 +965,7 @@ async fn resume_redaction_fixture(client_name: Option<&str>) -> Result<ThreadRes
                 limit: None,
                 sort_direction: None,
                 items_view: Some(TurnItemsView::Full),
+                history_window: None,
             }),
             ..Default::default()
         })
@@ -2355,6 +2356,7 @@ async fn thread_resume_and_read_interrupt_incomplete_rollout_turn_when_thread_is
         .send_thread_read_request(ThreadReadParams {
             thread_id: resumed_again.id,
             include_turns: true,
+            history_window: None,
         })
         .await?;
     let read_resp: JSONRPCResponse = timeout(
@@ -2389,6 +2391,7 @@ async fn thread_resume_defers_updated_at_until_turn_start() -> Result<()> {
         .send_thread_read_request(ThreadReadParams {
             thread_id: thread_id.clone(),
             include_turns: false,
+            history_window: None,
         })
         .await?;
     let read_resp: JSONRPCResponse = timeout(
@@ -2473,6 +2476,7 @@ async fn thread_resume_defers_updated_at_until_turn_start() -> Result<()> {
         .send_thread_read_request(ThreadReadParams {
             thread_id: thread_id.clone(),
             include_turns: false,
+            history_window: None,
         })
         .await?;
     let read_resp: JSONRPCResponse = timeout(
@@ -2973,6 +2977,7 @@ async fn thread_resume_rejoins_running_thread_even_with_override_mismatch() -> R
                 limit: None,
                 sort_direction: None,
                 items_view: None,
+                history_window: None,
             }),
             ..Default::default()
         })
@@ -3781,6 +3786,7 @@ async fn start_materialized_thread_and_restart(
         .send_thread_read_request(ThreadReadParams {
             thread_id: thread.id.clone(),
             include_turns: false,
+            history_window: None,
         })
         .await?;
     let read_resp: JSONRPCResponse = timeout(
diff --git a/codex-rs/app-server/tests/suite/v2/thread_settings_update.rs b/codex-rs/app-server/tests/suite/v2/thread_settings_update.rs
index 29dbee26d1..636a357d8d 100644
--- a/codex-rs/app-server/tests/suite/v2/thread_settings_update.rs
+++ b/codex-rs/app-server/tests/suite/v2/thread_settings_update.rs
@@ -399,6 +399,7 @@ async fn read_thread_with_turns(
         .send_thread_read_request(ThreadReadParams {
             thread_id: thread_id.to_string(),
             include_turns: true,
+            history_window: None,
         })
         .await?;
     let response: JSONRPCResponse = timeout(
diff --git a/codex-rs/app-server/tests/suite/v2/thread_shell_command.rs b/codex-rs/app-server/tests/suite/v2/thread_shell_command.rs
index df7b4991d6..d80b9d2b69 100644
--- a/codex-rs/app-server/tests/suite/v2/thread_shell_command.rs
+++ b/codex-rs/app-server/tests/suite/v2/thread_shell_command.rs
@@ -132,6 +132,7 @@ async fn thread_shell_command_history_responses_exclude_persisted_command_execut
         .send_thread_read_request(ThreadReadParams {
             thread_id: thread.id.clone(),
             include_turns: true,
+            history_window: None,
         })
         .await?;
     let read_resp: JSONRPCResponse = timeout(
@@ -150,6 +151,7 @@ async fn thread_shell_command_history_responses_exclude_persisted_command_execut
             limit: None,
             sort_direction: Some(SortDirection::Asc),
             items_view: None,
+            history_window: None,
         })
         .await?;
     let turns_list_resp: JSONRPCResponse = timeout(
@@ -362,6 +364,7 @@ async fn thread_shell_command_uses_existing_active_turn() -> Result<()> {
         .send_thread_read_request(ThreadReadParams {
             thread_id: thread.id,
             include_turns: true,
+            history_window: None,
         })
         .await?;
     let read_resp: JSONRPCResponse = timeout(
diff --git a/codex-rs/app-server/tests/suite/v2/thread_unsubscribe.rs b/codex-rs/app-server/tests/suite/v2/thread_unsubscribe.rs
index 9c9ff4b767..1102b098c6 100644
--- a/codex-rs/app-server/tests/suite/v2/thread_unsubscribe.rs
+++ b/codex-rs/app-server/tests/suite/v2/thread_unsubscribe.rs
@@ -285,6 +285,7 @@ async fn thread_unsubscribe_preserves_cached_status_before_idle_unload() -> Resu
         .send_thread_read_request(ThreadReadParams {
             thread_id: thread_id.clone(),
             include_turns: false,
+            history_window: None,
         })
         .await?;
     let read_resp: JSONRPCResponse = timeout(
diff --git a/codex-rs/app-server/tests/suite/v2/web_search.rs b/codex-rs/app-server/tests/suite/v2/web_search.rs
index f8c115d746..00ca3ebce3 100644
--- a/codex-rs/app-server/tests/suite/v2/web_search.rs
+++ b/codex-rs/app-server/tests/suite/v2/web_search.rs
@@ -141,7 +141,7 @@ async fn standalone_web_search_round_trips_output() -> Result<()> {
     );
 
     let search_body = search_request_body(&server).await?;
-    assert_eq!(search_body["model"], json!("mock-model"));
+    assert_eq!(search_body["model"], json!("gpt-5.4"));
     assert_eq!(
         search_body["commands"],
         json!({
@@ -203,6 +203,7 @@ async fn standalone_web_search_round_trips_output() -> Result<()> {
         .send_thread_read_request(ThreadReadParams {
             thread_id,
             include_turns: true,
+            history_window: None,
         })
         .await?;
     let read_resp: JSONRPCResponse = timeout(
@@ -295,11 +296,12 @@ fn create_config_toml(codex_home: &Path, server_uri: &str) -> std::io::Result<()
         codex_home.join("config.toml"),
         format!(
             r#"
-model = "mock-model"
+model = "gpt-5.4"
 approval_policy = "never"
 sandbox_mode = "read-only"
 model_provider = "openai-custom"
 chatgpt_base_url = "{server_uri}"
+web_search = "live"
 
 [features]
 standalone_web_search = true
diff --git a/codex-rs/exec/src/lib.rs b/codex-rs/exec/src/lib.rs
index 7ea8461b00..043efc8131 100644
--- a/codex-rs/exec/src/lib.rs
+++ b/codex-rs/exec/src/lib.rs
@@ -1360,6 +1360,7 @@ async fn maybe_backfill_turn_completed_items(
             params: ThreadReadParams {
                 thread_id: payload.thread_id.clone(),
                 include_turns: true,
+                history_window: None,
             },
         },
         "thread/read",
