diff --git a/codex-rs/app-server-protocol/schema/json/ClientRequest.json b/codex-rs/app-server-protocol/schema/json/ClientRequest.json
index bda34c91f5..dba21e578f 100644
--- a/codex-rs/app-server-protocol/schema/json/ClientRequest.json
+++ b/codex-rs/app-server-protocol/schema/json/ClientRequest.json
@@ -1325,6 +1325,9 @@
     "InternalChatMessageMetadataPassthrough": {
       "description": "Internal Responses API passthrough metadata copied into underlying chat messages.\n\nResponses API strongly types this payload. Do not modify it without first getting API approval and making the corresponding Responses API change.",
       "properties": {
+        "continuation_hidden": {
+          "type": "boolean"
+        },
         "turn_id": {
           "type": [
             "string",
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ItemCompletedNotification.json b/codex-rs/app-server-protocol/schema/json/v2/ItemCompletedNotification.json
index 5dc80fd904..833d0f5294 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ItemCompletedNotification.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ItemCompletedNotification.json
@@ -213,6 +213,13 @@
       ],
       "type": "string"
     },
+    "ContinuationNoticeKind": {
+      "enum": [
+        "hidden_assistant_output",
+        "skipped_tool_execution"
+      ],
+      "type": "string"
+    },
     "DynamicToolCallOutputContentItem": {
       "oneOf": [
         {
@@ -619,6 +626,21 @@
         },
         {
           "properties": {
+            "continuationHidden": {
+              "default": false,
+              "type": "boolean"
+            },
+            "continuationNotice": {
+              "anyOf": [
+                {
+                  "$ref": "#/definitions/ContinuationNoticeKind"
+                },
+                {
+                  "type": "null"
+                }
+              ],
+              "default": null
+            },
             "id": {
               "type": "string"
             },
@@ -697,6 +719,9 @@
               },
               "type": "array"
             },
+            "continuationLimitHit": {
+              "type": "boolean"
+            },
             "continued": {
               "default": false,
               "type": "boolean"
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ItemStartedNotification.json b/codex-rs/app-server-protocol/schema/json/v2/ItemStartedNotification.json
index c45351da7e..934e94f88e 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ItemStartedNotification.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ItemStartedNotification.json
@@ -213,6 +213,13 @@
       ],
       "type": "string"
     },
+    "ContinuationNoticeKind": {
+      "enum": [
+        "hidden_assistant_output",
+        "skipped_tool_execution"
+      ],
+      "type": "string"
+    },
     "DynamicToolCallOutputContentItem": {
       "oneOf": [
         {
@@ -619,6 +626,21 @@
         },
         {
           "properties": {
+            "continuationHidden": {
+              "default": false,
+              "type": "boolean"
+            },
+            "continuationNotice": {
+              "anyOf": [
+                {
+                  "$ref": "#/definitions/ContinuationNoticeKind"
+                },
+                {
+                  "type": "null"
+                }
+              ],
+              "default": null
+            },
             "id": {
               "type": "string"
             },
@@ -697,6 +719,9 @@
               },
               "type": "array"
             },
+            "continuationLimitHit": {
+              "type": "boolean"
+            },
             "continued": {
               "default": false,
               "type": "boolean"
diff --git a/codex-rs/app-server-protocol/schema/json/v2/RawResponseItemCompletedNotification.json b/codex-rs/app-server-protocol/schema/json/v2/RawResponseItemCompletedNotification.json
index dad669b139..0d5122ee59 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/RawResponseItemCompletedNotification.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/RawResponseItemCompletedNotification.json
@@ -219,6 +219,9 @@
     "InternalChatMessageMetadataPassthrough": {
       "description": "Internal Responses API passthrough metadata copied into underlying chat messages.\n\nResponses API strongly types this payload. Do not modify it without first getting API approval and making the corresponding Responses API change.",
       "properties": {
+        "continuation_hidden": {
+          "type": "boolean"
+        },
         "turn_id": {
           "type": [
             "string",
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ReviewStartResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ReviewStartResponse.json
index 5030b979ee..cf4a0caeed 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ReviewStartResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ReviewStartResponse.json
@@ -351,6 +351,13 @@
       ],
       "type": "string"
     },
+    "ContinuationNoticeKind": {
+      "enum": [
+        "hidden_assistant_output",
+        "skipped_tool_execution"
+      ],
+      "type": "string"
+    },
     "DynamicToolCallOutputContentItem": {
       "oneOf": [
         {
@@ -764,6 +771,21 @@
         },
         {
           "properties": {
+            "continuationHidden": {
+              "default": false,
+              "type": "boolean"
+            },
+            "continuationNotice": {
+              "anyOf": [
+                {
+                  "$ref": "#/definitions/ContinuationNoticeKind"
+                },
+                {
+                  "type": "null"
+                }
+              ],
+              "default": null
+            },
             "id": {
               "type": "string"
             },
@@ -842,6 +864,9 @@
               },
               "type": "array"
             },
+            "continuationLimitHit": {
+              "type": "boolean"
+            },
             "continued": {
               "default": false,
               "type": "boolean"
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadForkResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadForkResponse.json
index e35c1a989b..5a9d652574 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadForkResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadForkResponse.json
@@ -432,6 +432,13 @@
       ],
       "type": "string"
     },
+    "ContinuationNoticeKind": {
+      "enum": [
+        "hidden_assistant_output",
+        "skipped_tool_execution"
+      ],
+      "type": "string"
+    },
     "DynamicToolCallOutputContentItem": {
       "oneOf": [
         {
@@ -1292,6 +1299,21 @@
         },
         {
           "properties": {
+            "continuationHidden": {
+              "default": false,
+              "type": "boolean"
+            },
+            "continuationNotice": {
+              "anyOf": [
+                {
+                  "$ref": "#/definitions/ContinuationNoticeKind"
+                },
+                {
+                  "type": "null"
+                }
+              ],
+              "default": null
+            },
             "id": {
               "type": "string"
             },
@@ -1370,6 +1392,9 @@
               },
               "type": "array"
             },
+            "continuationLimitHit": {
+              "type": "boolean"
+            },
             "continued": {
               "default": false,
               "type": "boolean"
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadListResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadListResponse.json
index e7eb6aba0c..13f0318f84 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadListResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadListResponse.json
@@ -354,6 +354,13 @@
       ],
       "type": "string"
     },
+    "ContinuationNoticeKind": {
+      "enum": [
+        "hidden_assistant_output",
+        "skipped_tool_execution"
+      ],
+      "type": "string"
+    },
     "DynamicToolCallOutputContentItem": {
       "oneOf": [
         {
@@ -1083,6 +1090,21 @@
         },
         {
           "properties": {
+            "continuationHidden": {
+              "default": false,
+              "type": "boolean"
+            },
+            "continuationNotice": {
+              "anyOf": [
+                {
+                  "$ref": "#/definitions/ContinuationNoticeKind"
+                },
+                {
+                  "type": "null"
+                }
+              ],
+              "default": null
+            },
             "id": {
               "type": "string"
             },
@@ -1161,6 +1183,9 @@
               },
               "type": "array"
             },
+            "continuationLimitHit": {
+              "type": "boolean"
+            },
             "continued": {
               "default": false,
               "type": "boolean"
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadMetadataUpdateResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadMetadataUpdateResponse.json
index 3eab5ba287..a9c7256469 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadMetadataUpdateResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadMetadataUpdateResponse.json
@@ -354,6 +354,13 @@
       ],
       "type": "string"
     },
+    "ContinuationNoticeKind": {
+      "enum": [
+        "hidden_assistant_output",
+        "skipped_tool_execution"
+      ],
+      "type": "string"
+    },
     "DynamicToolCallOutputContentItem": {
       "oneOf": [
         {
@@ -1083,6 +1090,21 @@
         },
         {
           "properties": {
+            "continuationHidden": {
+              "default": false,
+              "type": "boolean"
+            },
+            "continuationNotice": {
+              "anyOf": [
+                {
+                  "$ref": "#/definitions/ContinuationNoticeKind"
+                },
+                {
+                  "type": "null"
+                }
+              ],
+              "default": null
+            },
             "id": {
               "type": "string"
             },
@@ -1161,6 +1183,9 @@
               },
               "type": "array"
             },
+            "continuationLimitHit": {
+              "type": "boolean"
+            },
             "continued": {
               "default": false,
               "type": "boolean"
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadReadParams.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadReadParams.json
index f00dfb47f0..8fd07b7b95 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadReadParams.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadReadParams.json
@@ -34,4 +34,4 @@
   ],
   "title": "ThreadReadParams",
   "type": "object"
-}
+}
\ No newline at end of file
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadReadResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadReadResponse.json
index f2499201d6..875bf8ecdf 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadReadResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadReadResponse.json
@@ -354,6 +354,13 @@
       ],
       "type": "string"
     },
+    "ContinuationNoticeKind": {
+      "enum": [
+        "hidden_assistant_output",
+        "skipped_tool_execution"
+      ],
+      "type": "string"
+    },
     "DynamicToolCallOutputContentItem": {
       "oneOf": [
         {
@@ -1083,6 +1090,21 @@
         },
         {
           "properties": {
+            "continuationHidden": {
+              "default": false,
+              "type": "boolean"
+            },
+            "continuationNotice": {
+              "anyOf": [
+                {
+                  "$ref": "#/definitions/ContinuationNoticeKind"
+                },
+                {
+                  "type": "null"
+                }
+              ],
+              "default": null
+            },
             "id": {
               "type": "string"
             },
@@ -1161,6 +1183,9 @@
               },
               "type": "array"
             },
+            "continuationLimitHit": {
+              "type": "boolean"
+            },
             "continued": {
               "default": false,
               "type": "boolean"
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadResumeParams.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadResumeParams.json
index 980566a961..1d184a54f7 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadResumeParams.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadResumeParams.json
@@ -281,6 +281,9 @@
     "InternalChatMessageMetadataPassthrough": {
       "description": "Internal Responses API passthrough metadata copied into underlying chat messages.\n\nResponses API strongly types this payload. Do not modify it without first getting API approval and making the corresponding Responses API change.",
       "properties": {
+        "continuation_hidden": {
+          "type": "boolean"
+        },
         "turn_id": {
           "type": [
             "string",
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadResumeResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadResumeResponse.json
index 1e3c2664f6..be8b33ebcf 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadResumeResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadResumeResponse.json
@@ -432,6 +432,13 @@
       ],
       "type": "string"
     },
+    "ContinuationNoticeKind": {
+      "enum": [
+        "hidden_assistant_output",
+        "skipped_tool_execution"
+      ],
+      "type": "string"
+    },
     "DynamicToolCallOutputContentItem": {
       "oneOf": [
         {
@@ -1292,6 +1299,21 @@
         },
         {
           "properties": {
+            "continuationHidden": {
+              "default": false,
+              "type": "boolean"
+            },
+            "continuationNotice": {
+              "anyOf": [
+                {
+                  "$ref": "#/definitions/ContinuationNoticeKind"
+                },
+                {
+                  "type": "null"
+                }
+              ],
+              "default": null
+            },
             "id": {
               "type": "string"
             },
@@ -1370,6 +1392,9 @@
               },
               "type": "array"
             },
+            "continuationLimitHit": {
+              "type": "boolean"
+            },
             "continued": {
               "default": false,
               "type": "boolean"
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadRollbackResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadRollbackResponse.json
index d57da1f41e..0199cd82b6 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadRollbackResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadRollbackResponse.json
@@ -354,6 +354,13 @@
       ],
       "type": "string"
     },
+    "ContinuationNoticeKind": {
+      "enum": [
+        "hidden_assistant_output",
+        "skipped_tool_execution"
+      ],
+      "type": "string"
+    },
     "DynamicToolCallOutputContentItem": {
       "oneOf": [
         {
@@ -1083,6 +1090,21 @@
         },
         {
           "properties": {
+            "continuationHidden": {
+              "default": false,
+              "type": "boolean"
+            },
+            "continuationNotice": {
+              "anyOf": [
+                {
+                  "$ref": "#/definitions/ContinuationNoticeKind"
+                },
+                {
+                  "type": "null"
+                }
+              ],
+              "default": null
+            },
             "id": {
               "type": "string"
             },
@@ -1161,6 +1183,9 @@
               },
               "type": "array"
             },
+            "continuationLimitHit": {
+              "type": "boolean"
+            },
             "continued": {
               "default": false,
               "type": "boolean"
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadStartResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadStartResponse.json
index 1b9ee0a8e0..2cd3270a34 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadStartResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadStartResponse.json
@@ -432,6 +432,13 @@
       ],
       "type": "string"
     },
+    "ContinuationNoticeKind": {
+      "enum": [
+        "hidden_assistant_output",
+        "skipped_tool_execution"
+      ],
+      "type": "string"
+    },
     "DynamicToolCallOutputContentItem": {
       "oneOf": [
         {
@@ -1292,6 +1299,21 @@
         },
         {
           "properties": {
+            "continuationHidden": {
+              "default": false,
+              "type": "boolean"
+            },
+            "continuationNotice": {
+              "anyOf": [
+                {
+                  "$ref": "#/definitions/ContinuationNoticeKind"
+                },
+                {
+                  "type": "null"
+                }
+              ],
+              "default": null
+            },
             "id": {
               "type": "string"
             },
@@ -1370,6 +1392,9 @@
               },
               "type": "array"
             },
+            "continuationLimitHit": {
+              "type": "boolean"
+            },
             "continued": {
               "default": false,
               "type": "boolean"
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadStartedNotification.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadStartedNotification.json
index 939b3a9785..940f289dbf 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadStartedNotification.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadStartedNotification.json
@@ -354,6 +354,13 @@
       ],
       "type": "string"
     },
+    "ContinuationNoticeKind": {
+      "enum": [
+        "hidden_assistant_output",
+        "skipped_tool_execution"
+      ],
+      "type": "string"
+    },
     "DynamicToolCallOutputContentItem": {
       "oneOf": [
         {
@@ -1083,6 +1090,21 @@
         },
         {
           "properties": {
+            "continuationHidden": {
+              "default": false,
+              "type": "boolean"
+            },
+            "continuationNotice": {
+              "anyOf": [
+                {
+                  "$ref": "#/definitions/ContinuationNoticeKind"
+                },
+                {
+                  "type": "null"
+                }
+              ],
+              "default": null
+            },
             "id": {
               "type": "string"
             },
@@ -1161,6 +1183,9 @@
               },
               "type": "array"
             },
+            "continuationLimitHit": {
+              "type": "boolean"
+            },
             "continued": {
               "default": false,
               "type": "boolean"
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadUnarchiveResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadUnarchiveResponse.json
index a7cd496755..6d8b4280a6 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadUnarchiveResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadUnarchiveResponse.json
@@ -354,6 +354,13 @@
       ],
       "type": "string"
     },
+    "ContinuationNoticeKind": {
+      "enum": [
+        "hidden_assistant_output",
+        "skipped_tool_execution"
+      ],
+      "type": "string"
+    },
     "DynamicToolCallOutputContentItem": {
       "oneOf": [
         {
@@ -1083,6 +1090,21 @@
         },
         {
           "properties": {
+            "continuationHidden": {
+              "default": false,
+              "type": "boolean"
+            },
+            "continuationNotice": {
+              "anyOf": [
+                {
+                  "$ref": "#/definitions/ContinuationNoticeKind"
+                },
+                {
+                  "type": "null"
+                }
+              ],
+              "default": null
+            },
             "id": {
               "type": "string"
             },
@@ -1161,6 +1183,9 @@
               },
               "type": "array"
             },
+            "continuationLimitHit": {
+              "type": "boolean"
+            },
             "continued": {
               "default": false,
               "type": "boolean"
diff --git a/codex-rs/app-server-protocol/schema/json/v2/TurnCompletedNotification.json b/codex-rs/app-server-protocol/schema/json/v2/TurnCompletedNotification.json
index d0f6cccbc5..cc213724cc 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/TurnCompletedNotification.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/TurnCompletedNotification.json
@@ -351,6 +351,13 @@
       ],
       "type": "string"
     },
+    "ContinuationNoticeKind": {
+      "enum": [
+        "hidden_assistant_output",
+        "skipped_tool_execution"
+      ],
+      "type": "string"
+    },
     "DynamicToolCallOutputContentItem": {
       "oneOf": [
         {
@@ -764,6 +771,21 @@
         },
         {
           "properties": {
+            "continuationHidden": {
+              "default": false,
+              "type": "boolean"
+            },
+            "continuationNotice": {
+              "anyOf": [
+                {
+                  "$ref": "#/definitions/ContinuationNoticeKind"
+                },
+                {
+                  "type": "null"
+                }
+              ],
+              "default": null
+            },
             "id": {
               "type": "string"
             },
@@ -842,6 +864,9 @@
               },
               "type": "array"
             },
+            "continuationLimitHit": {
+              "type": "boolean"
+            },
             "continued": {
               "default": false,
               "type": "boolean"
diff --git a/codex-rs/app-server-protocol/schema/json/v2/TurnStartResponse.json b/codex-rs/app-server-protocol/schema/json/v2/TurnStartResponse.json
index dc4ffaf3eb..5b90a0deb6 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/TurnStartResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/TurnStartResponse.json
@@ -351,6 +351,13 @@
       ],
       "type": "string"
     },
+    "ContinuationNoticeKind": {
+      "enum": [
+        "hidden_assistant_output",
+        "skipped_tool_execution"
+      ],
+      "type": "string"
+    },
     "DynamicToolCallOutputContentItem": {
       "oneOf": [
         {
@@ -764,6 +771,21 @@
         },
         {
           "properties": {
+            "continuationHidden": {
+              "default": false,
+              "type": "boolean"
+            },
+            "continuationNotice": {
+              "anyOf": [
+                {
+                  "$ref": "#/definitions/ContinuationNoticeKind"
+                },
+                {
+                  "type": "null"
+                }
+              ],
+              "default": null
+            },
             "id": {
               "type": "string"
             },
@@ -842,6 +864,9 @@
               },
               "type": "array"
             },
+            "continuationLimitHit": {
+              "type": "boolean"
+            },
             "continued": {
               "default": false,
               "type": "boolean"
diff --git a/codex-rs/app-server-protocol/schema/json/v2/TurnStartedNotification.json b/codex-rs/app-server-protocol/schema/json/v2/TurnStartedNotification.json
index 36ddeb47fa..c17b25b5ee 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/TurnStartedNotification.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/TurnStartedNotification.json
@@ -351,6 +351,13 @@
       ],
       "type": "string"
     },
+    "ContinuationNoticeKind": {
+      "enum": [
+        "hidden_assistant_output",
+        "skipped_tool_execution"
+      ],
+      "type": "string"
+    },
     "DynamicToolCallOutputContentItem": {
       "oneOf": [
         {
@@ -764,6 +771,21 @@
         },
         {
           "properties": {
+            "continuationHidden": {
+              "default": false,
+              "type": "boolean"
+            },
+            "continuationNotice": {
+              "anyOf": [
+                {
+                  "$ref": "#/definitions/ContinuationNoticeKind"
+                },
+                {
+                  "type": "null"
+                }
+              ],
+              "default": null
+            },
             "id": {
               "type": "string"
             },
@@ -842,6 +864,9 @@
               },
               "type": "array"
             },
+            "continuationLimitHit": {
+              "type": "boolean"
+            },
             "continued": {
               "default": false,
               "type": "boolean"
diff --git a/codex-rs/app-server-protocol/schema/typescript/InternalChatMessageMetadataPassthrough.ts b/codex-rs/app-server-protocol/schema/typescript/InternalChatMessageMetadataPassthrough.ts
index 6ccf386884..e88541f022 100644
--- a/codex-rs/app-server-protocol/schema/typescript/InternalChatMessageMetadataPassthrough.ts
+++ b/codex-rs/app-server-protocol/schema/typescript/InternalChatMessageMetadataPassthrough.ts
@@ -8,4 +8,4 @@
  * Responses API strongly types this payload. Do not modify it without first getting API
  * approval and making the corresponding Responses API change.
  */
-export type InternalChatMessageMetadataPassthrough = { turn_id?: string, };
+export type InternalChatMessageMetadataPassthrough = { turn_id?: string, continuation_hidden?: boolean, };
diff --git a/codex-rs/app-server-protocol/schema/typescript/ServerNotification.ts b/codex-rs/app-server-protocol/schema/typescript/ServerNotification.ts
index 33d5aaeb23..1021bf1eb7 100644
--- a/codex-rs/app-server-protocol/schema/typescript/ServerNotification.ts
+++ b/codex-rs/app-server-protocol/schema/typescript/ServerNotification.ts
@@ -62,7 +62,6 @@ import type { ThreadRealtimeTranscriptDoneNotification } from "./v2/ThreadRealti
 import type { ThreadSettingsUpdatedNotification } from "./v2/ThreadSettingsUpdatedNotification";
 import type { ThreadStartedNotification } from "./v2/ThreadStartedNotification";
 import type { ThreadStatusChangedNotification } from "./v2/ThreadStatusChangedNotification";
-import type { ThreadResponseMetricsUpdatedNotification } from "./v2/ThreadResponseMetricsUpdatedNotification";
 import type { ThreadTokenUsageUpdatedNotification } from "./v2/ThreadTokenUsageUpdatedNotification";
 import type { ThreadUnarchivedNotification } from "./v2/ThreadUnarchivedNotification";
 import type { TurnCompletedNotification } from "./v2/TurnCompletedNotification";
diff --git a/codex-rs/app-server-protocol/schema/typescript/v2/ContinuationNoticeKind.ts b/codex-rs/app-server-protocol/schema/typescript/v2/ContinuationNoticeKind.ts
new file mode 100644
index 0000000000..973bd0e2ae
--- /dev/null
+++ b/codex-rs/app-server-protocol/schema/typescript/v2/ContinuationNoticeKind.ts
@@ -0,0 +1,5 @@
+// GENERATED CODE! DO NOT MODIFY BY HAND!
+
+// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
+
+export type ContinuationNoticeKind = "hidden_assistant_output" | "skipped_tool_execution";
diff --git a/codex-rs/app-server-protocol/schema/typescript/v2/ThreadItem.ts b/codex-rs/app-server-protocol/schema/typescript/v2/ThreadItem.ts
index adf22e90d9..4c006604c6 100644
--- a/codex-rs/app-server-protocol/schema/typescript/v2/ThreadItem.ts
+++ b/codex-rs/app-server-protocol/schema/typescript/v2/ThreadItem.ts
@@ -12,6 +12,7 @@ import type { CollabAgentToolCallStatus } from "./CollabAgentToolCallStatus";
 import type { CommandAction } from "./CommandAction";
 import type { CommandExecutionSource } from "./CommandExecutionSource";
 import type { CommandExecutionStatus } from "./CommandExecutionStatus";
+import type { ContinuationNoticeKind } from "./ContinuationNoticeKind";
 import type { DynamicToolCallOutputContentItem } from "./DynamicToolCallOutputContentItem";
 import type { DynamicToolCallStatus } from "./DynamicToolCallStatus";
 import type { FileUpdateChange } from "./FileUpdateChange";
@@ -26,7 +27,7 @@ import type { SubAgentActivityKind } from "./SubAgentActivityKind";
 import type { UserInput } from "./UserInput";
 import type { WebSearchAction } from "./WebSearchAction";
 
-export type ThreadItem = { "type": "userMessage", id: string, clientId: string | null, content: Array<UserInput>, } | { "type": "hookPrompt", id: string, fragments: Array<HookPromptFragment>, } | { "type": "agentMessage", id: string, text: string, phase: MessagePhase | null, memoryCitation: MemoryCitation | null, } | { "type": "plan", id: string, text: string, } | { "type": "reasoning", id: string, summary: Array<string>, content: Array<string>, continued: boolean, } | { "type": "commandExecution", id: string,
+export type ThreadItem = { "type": "userMessage", id: string, clientId: string | null, content: Array<UserInput>, } | { "type": "hookPrompt", id: string, fragments: Array<HookPromptFragment>, } | { "type": "agentMessage", id: string, text: string, phase: MessagePhase | null, memoryCitation: MemoryCitation | null, continuationNotice: ContinuationNoticeKind | null, continuationHidden: boolean, } | { "type": "plan", id: string, text: string, } | { "type": "reasoning", id: string, summary: Array<string>, content: Array<string>, continued: boolean, continuationLimitHit?: boolean, reasoningOutputTokens?: bigint, } | { "type": "commandExecution", id: string,
 /**
  * The command to be executed.
  */
diff --git a/codex-rs/app-server-protocol/schema/typescript/v2/index.ts b/codex-rs/app-server-protocol/schema/typescript/v2/index.ts
index 4fc78e5ddc..1b83c8e70f 100644
--- a/codex-rs/app-server-protocol/schema/typescript/v2/index.ts
+++ b/codex-rs/app-server-protocol/schema/typescript/v2/index.ts
@@ -90,6 +90,7 @@ export type { ConsumeAccountRateLimitResetCreditOutcome } from "./ConsumeAccount
 export type { ConsumeAccountRateLimitResetCreditParams } from "./ConsumeAccountRateLimitResetCreditParams";
 export type { ConsumeAccountRateLimitResetCreditResponse } from "./ConsumeAccountRateLimitResetCreditResponse";
 export type { ContextCompactedNotification } from "./ContextCompactedNotification";
+export type { ContinuationNoticeKind } from "./ContinuationNoticeKind";
 export type { CreditsSnapshot } from "./CreditsSnapshot";
 export type { DeprecationNoticeNotification } from "./DeprecationNoticeNotification";
 export type { DynamicToolCallOutputContentItem } from "./DynamicToolCallOutputContentItem";
diff --git a/codex-rs/app-server-protocol/src/protocol/thread_history.rs b/codex-rs/app-server-protocol/src/protocol/thread_history.rs
index 2ec8c2f1e5..c6db7efa9c 100644
--- a/codex-rs/app-server-protocol/src/protocol/thread_history.rs
+++ b/codex-rs/app-server-protocol/src/protocol/thread_history.rs
@@ -519,6 +519,7 @@ impl ThreadHistoryBuilder {
                     id: existing_id,
                     summary: existing_summary,
                     content: existing_content,
+                    ..
                 } = item
                 else {
                     return None;
@@ -556,6 +557,9 @@ impl ThreadHistoryBuilder {
                 .unwrap_or_else(|| self.next_item_id()),
             summary,
             content,
+            continued: false,
+            continuation_limit_hit: false,
+            reasoning_output_tokens: None,
         };
         self.push_item_in_current_turn(item);
     }
@@ -619,9 +623,13 @@ impl ThreadHistoryBuilder {
             let tracking_changes = self.is_tracking_changes();
             let turn = self.ensure_turn();
             if let Some(ThreadItem::Reasoning {
-                summary, continued, ..
+                summary,
+                continued,
+                continuation_limit_hit,
+                ..
             }) = turn.items.last_mut()
                 && *continued == payload.continued
+                && *continuation_limit_hit == payload.continuation_limit_hit
             {
                 summary.push(payload.text.clone());
                 let changed_item = if tracking_changes {
@@ -651,6 +659,7 @@ impl ThreadHistoryBuilder {
             summary: vec![payload.text.clone()],
             content: Vec::new(),
             continued: payload.continued,
+            continuation_limit_hit: payload.continuation_limit_hit,
             reasoning_output_tokens: None,
         });
     }
@@ -665,9 +674,13 @@ impl ThreadHistoryBuilder {
             let tracking_changes = self.is_tracking_changes();
             let turn = self.ensure_turn();
             if let Some(ThreadItem::Reasoning {
-                content, continued, ..
+                content,
+                continued,
+                continuation_limit_hit,
+                ..
             }) = turn.items.last_mut()
                 && *continued == payload.continued
+                && *continuation_limit_hit == payload.continuation_limit_hit
             {
                 content.push(payload.text.clone());
                 let changed_item = if tracking_changes {
@@ -697,6 +710,7 @@ impl ThreadHistoryBuilder {
             summary: Vec::new(),
             content: vec![payload.text.clone()],
             continued: payload.continued,
+            continuation_limit_hit: payload.continuation_limit_hit,
             reasoning_output_tokens: None,
         });
     }
@@ -754,6 +768,7 @@ impl ThreadHistoryBuilder {
                 summary,
                 content,
                 continued,
+                continuation_limit_hit,
                 reasoning_output_tokens,
             }) = items.last_mut()
                 && *summary == reasoning.summary_text
@@ -761,6 +776,7 @@ impl ThreadHistoryBuilder {
             {
                 *id = reasoning.id.clone();
                 *continued = reasoning.continued;
+                *continuation_limit_hit = reasoning.continuation_limit_hit;
                 *reasoning_output_tokens = reasoning.reasoning_output_tokens;
                 return items.last().cloned();
             }
@@ -1975,10 +1991,12 @@ mod tests {
             EventMsg::AgentReasoning(AgentReasoningEvent {
                 text: "thinking".into(),
                 continued: false,
+                continuation_limit_hit: false,
             }),
             EventMsg::AgentReasoningRawContent(AgentReasoningRawContentEvent {
                 text: "full reasoning".into(),
                 continued: false,
+                continuation_limit_hit: false,
             }),
             EventMsg::UserMessage(UserMessageEvent {
                 client_id: None,
@@ -2041,6 +2059,7 @@ mod tests {
                 summary: vec!["thinking".into()],
                 content: vec!["full reasoning".into()],
                 continued: false,
+                continuation_limit_hit: false,
                 reasoning_output_tokens: None,
             }
         );
@@ -2183,6 +2202,8 @@ mod tests {
             })),
             RolloutItem::EventMsg(EventMsg::AgentReasoning(AgentReasoningEvent {
                 text: "thinking".into(),
+                continued: false,
+                continuation_limit_hit: false,
             })),
             RolloutItem::ResponseItem(codex_protocol::models::ResponseItem::Message {
                 id: None,
@@ -2232,10 +2253,14 @@ mod tests {
             })),
             RolloutItem::EventMsg(EventMsg::AgentReasoning(AgentReasoningEvent {
                 text: "thinking".into(),
+                continued: true,
+                continuation_limit_hit: true,
             })),
             RolloutItem::EventMsg(EventMsg::AgentReasoningRawContent(
                 AgentReasoningRawContentEvent {
                     text: "raw thinking".into(),
+                    continued: true,
+                    continuation_limit_hit: true,
                 },
             )),
             RolloutItem::ResponseItem(codex_protocol::models::ResponseItem::Reasoning {
@@ -2270,6 +2295,9 @@ mod tests {
                 id: "reasoning-response".into(),
                 summary: vec!["thinking".into()],
                 content: vec!["raw thinking".into()],
+                continued: true,
+                continuation_limit_hit: true,
+                reasoning_output_tokens: None,
             }]
         );
     }
@@ -2739,10 +2767,12 @@ mod tests {
             EventMsg::AgentReasoning(AgentReasoningEvent {
                 text: "first summary".into(),
                 continued: false,
+                continuation_limit_hit: false,
             }),
             EventMsg::AgentReasoningRawContent(AgentReasoningRawContentEvent {
                 text: "first content".into(),
                 continued: false,
+                continuation_limit_hit: false,
             }),
             EventMsg::AgentMessage(AgentMessageEvent {
                 message: "interlude".into(),
@@ -2752,6 +2782,7 @@ mod tests {
             EventMsg::AgentReasoning(AgentReasoningEvent {
                 text: "second summary".into(),
                 continued: false,
+                continuation_limit_hit: false,
             }),
         ];
 
@@ -2771,6 +2802,7 @@ mod tests {
                 summary: vec!["first summary".into()],
                 content: vec!["first content".into()],
                 continued: false,
+                continuation_limit_hit: false,
                 reasoning_output_tokens: None,
             }
         );
@@ -2781,6 +2813,7 @@ mod tests {
                 summary: vec!["second summary".into()],
                 content: Vec::new(),
                 continued: false,
+                continuation_limit_hit: false,
                 reasoning_output_tokens: None,
             }
         );
@@ -2792,14 +2825,17 @@ mod tests {
             EventMsg::AgentReasoning(AgentReasoningEvent {
                 text: "continued summary".into(),
                 continued: true,
+                continuation_limit_hit: false,
             }),
             EventMsg::AgentReasoningRawContent(AgentReasoningRawContentEvent {
                 text: "continued content".into(),
                 continued: true,
+                continuation_limit_hit: false,
             }),
             EventMsg::AgentReasoning(AgentReasoningEvent {
                 text: "final summary".into(),
                 continued: false,
+                continuation_limit_hit: false,
             }),
         ];
 
@@ -2819,6 +2855,7 @@ mod tests {
                 summary: vec!["continued summary".into()],
                 content: vec!["continued content".into()],
                 continued: true,
+                continuation_limit_hit: false,
                 reasoning_output_tokens: None,
             }
         );
@@ -2829,6 +2866,7 @@ mod tests {
                 summary: vec!["final summary".into()],
                 content: Vec::new(),
                 continued: false,
+                continuation_limit_hit: false,
                 reasoning_output_tokens: None,
             }
         );
@@ -2898,6 +2936,7 @@ mod tests {
                 summary_text: vec!["summary".into()],
                 raw_content: vec!["raw".into()],
                 continued: true,
+                continuation_limit_hit: false,
                 reasoning_output_tokens: Some(516),
             });
         let items = vec![
@@ -2911,11 +2950,13 @@ mod tests {
             RolloutItem::EventMsg(EventMsg::AgentReasoning(AgentReasoningEvent {
                 text: "summary".into(),
                 continued: true,
+                continuation_limit_hit: false,
             })),
             RolloutItem::EventMsg(EventMsg::AgentReasoningRawContent(
                 AgentReasoningRawContentEvent {
                     text: "raw".into(),
                     continued: true,
+                    continuation_limit_hit: false,
                 },
             )),
             RolloutItem::EventMsg(EventMsg::ItemCompleted(ItemCompletedEvent {
@@ -2935,6 +2976,7 @@ mod tests {
                 summary: vec!["summary".into()],
                 content: vec!["raw".into()],
                 continued: true,
+                continuation_limit_hit: false,
                 reasoning_output_tokens: Some(516),
             }
         );
@@ -4824,6 +4866,7 @@ mod tests {
             AgentReasoningEvent {
                 text: "summary".into(),
                 continued: false,
+                continuation_limit_hit: false,
             },
         )));
 
@@ -4831,6 +4874,7 @@ mod tests {
             EventMsg::AgentReasoningRawContent(AgentReasoningRawContentEvent {
                 text: "raw content".into(),
                 continued: false,
+                continuation_limit_hit: false,
             }),
         ));
 
@@ -4844,6 +4888,7 @@ mod tests {
                         summary: vec!["summary".into()],
                         content: vec!["raw content".into()],
                         continued: false,
+                        continuation_limit_hit: false,
                         reasoning_output_tokens: None,
                     },
                 }],
diff --git a/codex-rs/app-server-protocol/src/protocol/v2/item.rs b/codex-rs/app-server-protocol/src/protocol/v2/item.rs
index 6ed31d8a33..b257d58c12 100644
--- a/codex-rs/app-server-protocol/src/protocol/v2/item.rs
+++ b/codex-rs/app-server-protocol/src/protocol/v2/item.rs
@@ -16,10 +16,10 @@ use codex_protocol::approvals::GuardianAssessmentDecisionSource as CoreGuardianA
 use codex_protocol::approvals::GuardianCommandSource as CoreGuardianCommandSource;
 use codex_protocol::approvals::GuardianReviewer as CoreGuardianReviewer;
 use codex_protocol::items::AgentMessageContent as CoreAgentMessageContent;
-use codex_protocol::items::ContinuationNoticeKind as CoreContinuationNoticeKind;
 use codex_protocol::items::CollabAgentTool as CoreCollabAgentTool;
 use codex_protocol::items::CollabAgentToolCallStatus as CoreCollabAgentToolCallStatus;
 use codex_protocol::items::CommandExecutionStatus as CoreCommandExecutionStatus;
+use codex_protocol::items::ContinuationNoticeKind as CoreContinuationNoticeKind;
 use codex_protocol::items::DynamicToolCallStatus as CoreDynamicToolCallStatus;
 use codex_protocol::items::McpToolCallStatus as CoreMcpToolCallStatus;
 use codex_protocol::items::TurnItem as CoreTurnItem;
@@ -264,6 +264,8 @@ pub enum ThreadItem {
         content: Vec<String>,
         #[serde(default)]
         continued: bool,
+        #[serde(default, skip_serializing_if = "std::ops::Not::not")]
+        continuation_limit_hit: bool,
         #[serde(default, skip_serializing_if = "Option::is_none")]
         #[ts(optional)]
         reasoning_output_tokens: Option<i64>,
@@ -909,6 +911,7 @@ impl From<CoreTurnItem> for ThreadItem {
                 summary: reasoning.summary_text,
                 content: reasoning.raw_content,
                 continued: reasoning.continued,
+                continuation_limit_hit: reasoning.continuation_limit_hit,
                 reasoning_output_tokens: reasoning.reasoning_output_tokens,
             },
             CoreTurnItem::CommandExecution(command) => ThreadItem::CommandExecution {
diff --git a/codex-rs/app-server-protocol/src/protocol/v2/tests.rs b/codex-rs/app-server-protocol/src/protocol/v2/tests.rs
index e07c13dafc..a3254f5504 100644
--- a/codex-rs/app-server-protocol/src/protocol/v2/tests.rs
+++ b/codex-rs/app-server-protocol/src/protocol/v2/tests.rs
@@ -2645,6 +2645,7 @@ fn core_turn_item_into_thread_item_converts_supported_variants() {
         summary_text: vec!["line one".to_string(), "line two".to_string()],
         raw_content: vec![],
         continued: false,
+        continuation_limit_hit: false,
         reasoning_output_tokens: Some(516),
     });
 
@@ -2655,6 +2656,7 @@ fn core_turn_item_into_thread_item_converts_supported_variants() {
             summary: vec!["line one".to_string(), "line two".to_string()],
             content: vec![],
             continued: false,
+            continuation_limit_hit: false,
             reasoning_output_tokens: Some(516),
         }
     );
diff --git a/codex-rs/core/src/event_mapping.rs b/codex-rs/core/src/event_mapping.rs
index 55e40347fb..64c75706e7 100644
--- a/codex-rs/core/src/event_mapping.rs
+++ b/codex-rs/core/src/event_mapping.rs
@@ -196,6 +196,7 @@ pub fn parse_turn_item(item: &ResponseItem) -> Option<TurnItem> {
                 summary_text,
                 raw_content,
                 continued: false,
+                continuation_limit_hit: false,
                 reasoning_output_tokens: None,
             }))
         }
diff --git a/codex-rs/core/src/session/turn.rs b/codex-rs/core/src/session/turn.rs
index a71ed7f032..d5ce4283c9 100644
--- a/codex-rs/core/src/session/turn.rs
+++ b/codex-rs/core/src/session/turn.rs
@@ -1381,7 +1381,7 @@ struct DeferredNonToolItem {
 }
 
 const REASONING_CONTINUATION_FINGERPRINT_STEP: i64 = 518;
-const REASONING_CONTINUATION_MAX_ROUNDS: usize = 3;
+const REASONING_CONTINUATION_MAX_ROUNDS: usize = 5;
 const REASONING_CONTINUATION_MARKER: &str = "Continue the same turn from the preserved reasoning chain. Unaccepted replies and tool calls have not taken effect; request any still-needed tool again.";
 
 fn hidden_continuation_notice_item(kind: ContinuationNoticeKind) -> TurnItem {
@@ -1397,13 +1397,14 @@ fn hidden_continuation_notice_item(kind: ContinuationNoticeKind) -> TurnItem {
     })
 }
 
-async fn emit_hidden_continuation_notice(
-    ctx: &mut HandleOutputCtx,
-    kind: ContinuationNoticeKind,
-) {
+async fn emit_hidden_continuation_notice(ctx: &mut HandleOutputCtx, kind: ContinuationNoticeKind) {
     let item = hidden_continuation_notice_item(kind);
-    ctx.sess.emit_turn_item_started(&ctx.turn_context, &item).await;
-    ctx.sess.emit_turn_item_completed(&ctx.turn_context, item).await;
+    ctx.sess
+        .emit_turn_item_started(&ctx.turn_context, &item)
+        .await;
+    ctx.sess
+        .emit_turn_item_completed(&ctx.turn_context, item)
+        .await;
 }
 
 fn reasoning_continuation_fingerprint_tier(reasoning_output_tokens: i64) -> Option<i64> {
@@ -1471,6 +1472,7 @@ async fn flush_deferred_non_tool_items(
     deferred_non_tool_items: Vec<DeferredNonToolItem>,
     reasoning_only: bool,
     reasoning_continued: bool,
+    reasoning_continuation_limit_hit: bool,
     reasoning_output_tokens: Option<i64>,
     continuation_hidden: bool,
 ) -> CodexResult<Option<String>> {
@@ -1494,6 +1496,12 @@ async fn flush_deferred_non_tool_items(
         {
             reasoning.continued = true;
         }
+        if reasoning_continuation_limit_hit
+            && let Some(finalized) = finalized_turn_item.as_mut()
+            && let TurnItem::Reasoning(reasoning) = &mut finalized.turn_item
+        {
+            reasoning.continuation_limit_hit = true;
+        }
         if let Some(finalized) = finalized_turn_item.as_mut()
             && let TurnItem::Reasoning(reasoning) = &mut finalized.turn_item
         {
@@ -1547,6 +1555,7 @@ async fn flush_one_deferred_non_tool_item(
     ctx: &mut HandleOutputCtx,
     deferred: DeferredNonToolItem,
     reasoning_continued: bool,
+    reasoning_continuation_limit_hit: bool,
     reasoning_output_tokens: Option<i64>,
 ) -> CodexResult<Option<String>> {
     flush_deferred_non_tool_items(
@@ -1554,6 +1563,7 @@ async fn flush_one_deferred_non_tool_item(
         vec![deferred],
         /*reasoning_only*/ false,
         reasoning_continued,
+        reasoning_continuation_limit_hit,
         reasoning_output_tokens,
         /*continuation_hidden*/ false,
     )
@@ -2399,6 +2409,7 @@ async fn try_run_sampling_request(
                         std::mem::take(&mut deferred_non_tool_items),
                         /*reasoning_only*/ false,
                         /*reasoning_continued*/ false,
+                        /*reasoning_continuation_limit_hit*/ false,
                         /*reasoning_output_tokens*/ None,
                         /*continuation_hidden*/ false,
                     )
@@ -2571,6 +2582,9 @@ async fn try_run_sampling_request(
                 let should_continue = saw_encrypted_reasoning
                     && continuation_rounds < REASONING_CONTINUATION_MAX_ROUNDS
                     && continuation_tier.is_some_and(|tier| tier < 5);
+                let continuation_limit_hit = saw_encrypted_reasoning
+                    && continuation_rounds >= REASONING_CONTINUATION_MAX_ROUNDS
+                    && continuation_tier.is_some_and(|tier| tier < 5);
                 if should_continue {
                     sess.record_hidden_rollout_response_items(&turn_context, &round_response_items)
                         .await;
@@ -2586,6 +2600,7 @@ async fn try_run_sampling_request(
                         deferred_non_tool_items,
                         /*reasoning_only*/ false,
                         /*reasoning_continued*/ true,
+                        /*reasoning_continuation_limit_hit*/ false,
                         token_usage
                             .as_ref()
                             .map(|usage| usage.reasoning_output_tokens),
@@ -2642,7 +2657,8 @@ async fn try_run_sampling_request(
                         let agent_message = match flush_one_deferred_non_tool_item(
                             &mut completion_ctx,
                             item,
-                            /*reasoning_continued*/ false,
+                            /*reasoning_continued*/ continuation_limit_hit,
+                            continuation_limit_hit,
                             token_usage
                                 .as_ref()
                                 .map(|usage| usage.reasoning_output_tokens),
diff --git a/codex-rs/core/tests/suite/client.rs b/codex-rs/core/tests/suite/client.rs
index c25f07d24f..eea7824ff3 100644
--- a/codex-rs/core/tests/suite/client.rs
+++ b/codex-rs/core/tests/suite/client.rs
@@ -388,8 +388,8 @@ async fn truncated_encrypted_reasoning_round_without_tool_call_builds_continuati
 }
 
 #[tokio::test(flavor = "multi_thread", worker_threads = 2)]
-async fn truncated_encrypted_reasoning_round_strips_local_continuation_metadata_before_openai_request(
-) {
+async fn truncated_encrypted_reasoning_round_strips_local_continuation_metadata_before_openai_request()
+ {
     skip_if_no_network!();
 
     let server = MockServer::start().await;
@@ -644,6 +644,70 @@ async fn large_reasoning_fingerprint_tier_does_not_continue() {
     assert_eq!(response_mock.requests().len(), 1);
 }
 
+#[tokio::test(flavor = "multi_thread", worker_threads = 2)]
+async fn truncated_encrypted_reasoning_continues_at_most_five_rounds() -> anyhow::Result<()> {
+    skip_if_no_network!(Ok(()));
+
+    let server = MockServer::start().await;
+    let mut responses = Vec::new();
+    for index in 1..=6 {
+        let response_id = format!("resp-{index}");
+        let reasoning_id = format!("rs-{index}");
+        let mut events = vec![
+            ev_response_created(&response_id),
+            ev_reasoning_item(&reasoning_id, &["thinking"], &["raw"]),
+        ];
+        if index == 6 {
+            events.push(ev_assistant_message("msg-6", "done"));
+        }
+        events.push(json!({
+            "type": "response.completed",
+            "response": {
+                "id": response_id,
+                "usage": {
+                    "input_tokens": 10,
+                    "input_tokens_details": null,
+                    "output_tokens": 520,
+                    "output_tokens_details": { "reasoning_tokens": 516 },
+                    "total_tokens": 530
+                }
+            }
+        }));
+        responses.push(sse(events));
+    }
+    let response_mock = mount_sse_sequence(&server, responses).await;
+    let test = test_codex().build(&server).await?;
+
+    test.submit_turn("hello").await?;
+
+    assert_eq!(
+        response_mock.requests().len(),
+        6,
+        "five hidden continuation rounds plus one accepted cap-hit round"
+    );
+
+    test.codex.ensure_rollout_materialized().await;
+    test.codex.flush_rollout().await?;
+    let rollout_path = test.codex.rollout_path().expect("rollout path");
+    let rollout_text = std::fs::read_to_string(&rollout_path)?;
+    assert!(
+        rollout_text.lines().any(|line| {
+            serde_json::from_str::<RolloutLine>(line)
+                .ok()
+                .and_then(|line| match line.item {
+                    RolloutItem::EventMsg(EventMsg::AgentReasoning(event)) => {
+                        Some(event.continuation_limit_hit)
+                    }
+                    _ => None,
+                })
+                .unwrap_or(false)
+        }),
+        "accepted cap-hit reasoning item should be marked for distinct rendering"
+    );
+
+    Ok(())
+}
+
 #[tokio::test(flavor = "multi_thread", worker_threads = 2)]
 async fn openai_stateless_responses_requests_preserve_item_turn_metadata_across_turns() {
     let server = MockServer::start().await;
diff --git a/codex-rs/protocol/src/items.rs b/codex-rs/protocol/src/items.rs
index 0fdba9fb6f..15da0bfbe2 100644
--- a/codex-rs/protocol/src/items.rs
+++ b/codex-rs/protocol/src/items.rs
@@ -151,6 +151,8 @@ pub struct ReasoningItem {
     pub raw_content: Vec<String>,
     #[serde(default)]
     pub continued: bool,
+    #[serde(default, skip_serializing_if = "std::ops::Not::not")]
+    pub continuation_limit_hit: bool,
     #[serde(default, skip_serializing_if = "Option::is_none")]
     #[ts(optional)]
     pub reasoning_output_tokens: Option<i64>,
diff --git a/codex-rs/protocol/src/legacy_events.rs b/codex-rs/protocol/src/legacy_events.rs
index 30f134bd87..da331159f7 100644
--- a/codex-rs/protocol/src/legacy_events.rs
+++ b/codex-rs/protocol/src/legacy_events.rs
@@ -111,6 +111,7 @@ impl ReasoningItem {
             events.push(EventMsg::AgentReasoning(AgentReasoningEvent {
                 text: summary.clone(),
                 continued: self.continued,
+                continuation_limit_hit: self.continuation_limit_hit,
             }));
         }
 
@@ -120,6 +121,7 @@ impl ReasoningItem {
                     AgentReasoningRawContentEvent {
                         text: entry.clone(),
                         continued: self.continued,
+                        continuation_limit_hit: self.continuation_limit_hit,
                     },
                 ));
             }
diff --git a/codex-rs/protocol/src/protocol.rs b/codex-rs/protocol/src/protocol.rs
index 86b50422eb..ffae813e57 100644
--- a/codex-rs/protocol/src/protocol.rs
+++ b/codex-rs/protocol/src/protocol.rs
@@ -2288,6 +2288,8 @@ pub struct AgentReasoningEvent {
     pub text: String,
     #[serde(default)]
     pub continued: bool,
+    #[serde(default, skip_serializing_if = "std::ops::Not::not")]
+    pub continuation_limit_hit: bool,
 }
 
 #[derive(Debug, Clone, Deserialize, Serialize, JsonSchema, TS)]
@@ -2295,6 +2297,8 @@ pub struct AgentReasoningRawContentEvent {
     pub text: String,
     #[serde(default)]
     pub continued: bool,
+    #[serde(default, skip_serializing_if = "std::ops::Not::not")]
+    pub continuation_limit_hit: bool,
 }
 
 #[derive(Debug, Clone, Deserialize, Serialize, JsonSchema, TS)]
diff --git a/codex-rs/tui/src/app/agent_status_feed_tests.rs b/codex-rs/tui/src/app/agent_status_feed_tests.rs
index 578e3fafef..297e053401 100644
--- a/codex-rs/tui/src/app/agent_status_feed_tests.rs
+++ b/codex-rs/tui/src/app/agent_status_feed_tests.rs
@@ -74,6 +74,7 @@ fn agent_status_uses_reasoning_summaries_only() {
                 summary: vec!["safe summary".to_string()],
                 content: vec!["hidden raw reasoning".to_string()],
                 continued: false,
+                continuation_limit_hit: false,
                 reasoning_output_tokens: None,
             },
             thread_id: "thread-child".to_string(),
@@ -88,6 +89,7 @@ fn agent_status_uses_reasoning_summaries_only() {
                 summary: Vec::new(),
                 content: vec!["raw-only reasoning".to_string()],
                 continued: false,
+                continuation_limit_hit: false,
                 reasoning_output_tokens: None,
             },
             thread_id: "thread-child".to_string(),
diff --git a/codex-rs/tui/src/chatwidget.rs b/codex-rs/tui/src/chatwidget.rs
index 0116007b8e..77bf19240e 100644
--- a/codex-rs/tui/src/chatwidget.rs
+++ b/codex-rs/tui/src/chatwidget.rs
@@ -586,6 +586,7 @@ pub(crate) struct ChatWidget {
     adaptive_chunking: AdaptiveChunkingPolicy,
     // Stream lifecycle controller
     stream_controller: Option<StreamController>,
+    continuation_turn_ids_rendered: HashSet<String>,
     // Stream lifecycle controller for proposed plan output.
     plan_stream_controller: Option<PlanStreamController>,
     pending_stream_consolidations: usize,
diff --git a/codex-rs/tui/src/chatwidget/constructor.rs b/codex-rs/tui/src/chatwidget/constructor.rs
index 8bb457705e..70fcb58ab1 100644
--- a/codex-rs/tui/src/chatwidget/constructor.rs
+++ b/codex-rs/tui/src/chatwidget/constructor.rs
@@ -149,6 +149,7 @@ impl ChatWidget {
             add_credits_nudge_email_in_flight: None,
             adaptive_chunking: AdaptiveChunkingPolicy::default(),
             stream_controller: None,
+            continuation_turn_ids_rendered: HashSet::new(),
             plan_stream_controller: None,
             pending_stream_consolidations: 0,
             clipboard_lease: None,
diff --git a/codex-rs/tui/src/chatwidget/replay.rs b/codex-rs/tui/src/chatwidget/replay.rs
index eaeb86ec8b..290a732f5e 100644
--- a/codex-rs/tui/src/chatwidget/replay.rs
+++ b/codex-rs/tui/src/chatwidget/replay.rs
@@ -73,6 +73,9 @@ impl ChatWidget {
     ) {
         let from_replay = render_source.is_replay();
         let replay_kind = render_source.replay_kind();
+        if item_should_show_turn_id(&item) {
+            self.add_turn_id_once(Some(&turn_id));
+        }
         match item {
             ThreadItem::UserMessage { content, .. } => {
                 self.on_committed_user_message(&content, from_replay);
@@ -121,6 +124,7 @@ impl ChatWidget {
                         continuation_hidden,
                     },
                     from_replay,
+                    Some(&turn_id),
                 );
             }
             ThreadItem::Plan { text, .. } => self.on_plan_item_completed(text),
@@ -128,13 +132,16 @@ impl ChatWidget {
                 summary,
                 content,
                 continued,
+                continuation_limit_hit,
                 reasoning_output_tokens,
                 ..
             } => self.on_agent_reasoning_final(
                 &summary,
                 &content,
                 continued,
+                continuation_limit_hit,
                 reasoning_output_tokens,
+                Some(&turn_id),
             ),
             item @ ThreadItem::CommandExecution {
                 status: codex_app_server_protocol::CommandExecutionStatus::InProgress,
@@ -214,3 +221,19 @@ impl ChatWidget {
         }
     }
 }
+
+fn item_should_show_turn_id(item: &ThreadItem) -> bool {
+    match item {
+        ThreadItem::UserMessage { .. } => false,
+        ThreadItem::AgentMessage {
+            continuation_hidden: true,
+            continuation_notice: None,
+            ..
+        } => false,
+        ThreadItem::FileChange {
+            status: codex_app_server_protocol::PatchApplyStatus::InProgress,
+            ..
+        } => false,
+        _ => true,
+    }
+}
diff --git a/codex-rs/tui/src/chatwidget/session_flow.rs b/codex-rs/tui/src/chatwidget/session_flow.rs
index 6d913a21a5..802c1a2528 100644
--- a/codex-rs/tui/src/chatwidget/session_flow.rs
+++ b/codex-rs/tui/src/chatwidget/session_flow.rs
@@ -25,6 +25,7 @@ impl ChatWidget {
         if previous_thread_id != self.thread_id {
             self.review.recent_auto_review_denials = RecentAutoReviewDenials::default();
         }
+        self.continuation_turn_ids_rendered.clear();
         self.refresh_plan_mode_nudge();
         self.turn_lifecycle.reset_thread();
         self.clear_safety_buffering();
diff --git a/codex-rs/tui/src/chatwidget/streaming.rs b/codex-rs/tui/src/chatwidget/streaming.rs
index 5273afcfe7..6cff5dd5c7 100644
--- a/codex-rs/tui/src/chatwidget/streaming.rs
+++ b/codex-rs/tui/src/chatwidget/streaming.rs
@@ -329,20 +329,33 @@ impl ChatWidget {
         self.on_agent_reasoning_delta(delta);
     }
 
+    pub(super) fn add_turn_id_once(&mut self, turn_id: Option<&str>) {
+        if let Some(turn_id) = turn_id.filter(|turn_id| !turn_id.is_empty()) {
+            let turn_id = turn_id.to_string();
+            if self.continuation_turn_ids_rendered.insert(turn_id.clone()) {
+                self.add_to_history(history_cell::new_continuation_turn_id(&turn_id));
+            }
+        }
+    }
+
     pub(super) fn on_agent_reasoning_final(
         &mut self,
         summary: &[String],
         content: &[String],
         continued: bool,
+        continuation_limit_hit: bool,
         reasoning_output_tokens: Option<i64>,
+        turn_id: Option<&str>,
     ) {
         if let Some(reasoning_markdown) =
             self.reasoning_markdown_for_completed_item(summary, content, reasoning_output_tokens)
         {
+            self.add_turn_id_once(turn_id);
             let cell = history_cell::new_reasoning_summary_block(
                 reasoning_markdown,
                 &self.config.cwd,
                 continued,
+                continuation_limit_hit,
                 reasoning_output_tokens,
             );
             self.add_boxed_history(cell);
@@ -371,10 +384,13 @@ impl ChatWidget {
 
     pub(super) fn flush_pending_live_reasoning_ordering_state(&mut self) {
         if let Some(reasoning_markdown) = self.current_reasoning_markdown() {
+            let turn_id = self.turn_lifecycle.last_turn_id.clone();
+            self.add_turn_id_once(turn_id.as_deref());
             let cell = history_cell::new_reasoning_summary_block(
                 reasoning_markdown,
                 &self.config.cwd,
                 /*continued*/ false,
+                /*continuation_limit_hit*/ false,
                 /*reasoning_output_tokens*/ None,
             );
             self.add_boxed_history(cell);
@@ -404,10 +420,13 @@ impl ChatWidget {
         }
 
         if let Some(reasoning_markdown) = self.current_reasoning_markdown() {
+            let turn_id = self.turn_lifecycle.last_turn_id.clone();
+            self.add_turn_id_once(turn_id.as_deref());
             let cell = history_cell::new_reasoning_summary_block(
                 reasoning_markdown,
                 &self.config.cwd,
                 /*continued*/ false,
+                /*continuation_limit_hit*/ false,
                 /*reasoning_output_tokens*/ None,
             );
             self.add_boxed_history(cell);
@@ -490,9 +509,11 @@ impl ChatWidget {
         &mut self,
         item: AgentMessageItem,
         from_replay: bool,
+        turn_id: Option<&str>,
     ) {
         if let Some(notice) = item.continuation_notice {
             self.finalize_completed_assistant_message(/*content*/ None);
+            self.add_turn_id_once(turn_id);
             self.add_to_history(history_cell::new_continuation_notice(notice));
             self.status_state.pending_status_indicator_restore = true;
             self.maybe_restore_status_indicator_after_stream_idle();
@@ -506,6 +527,7 @@ impl ChatWidget {
             }
         }
         let parsed = parse_assistant_markdown(&message, self.config.cwd.as_path());
+        self.add_turn_id_once(turn_id);
         self.finalize_completed_assistant_message(
             (!parsed.visible_markdown.is_empty()).then_some(parsed.visible_markdown.as_str()),
         );
diff --git a/codex-rs/tui/src/chatwidget/tests/helpers.rs b/codex-rs/tui/src/chatwidget/tests/helpers.rs
index 38b519de0c..0570ed933e 100644
--- a/codex-rs/tui/src/chatwidget/tests/helpers.rs
+++ b/codex-rs/tui/src/chatwidget/tests/helpers.rs
@@ -561,6 +561,7 @@ pub(super) fn handle_agent_reasoning_final(chat: &mut ChatWidget) {
                 summary: Vec::new(),
                 content: Vec::new(),
                 continued: false,
+                continuation_limit_hit: false,
                 reasoning_output_tokens: None,
             },
         }),
diff --git a/codex-rs/tui/src/chatwidget/tests/history_replay.rs b/codex-rs/tui/src/chatwidget/tests/history_replay.rs
index 489f5e44ee..1df489555e 100644
--- a/codex-rs/tui/src/chatwidget/tests/history_replay.rs
+++ b/codex-rs/tui/src/chatwidget/tests/history_replay.rs
@@ -10,6 +10,18 @@ use codex_protocol::permissions::FileSystemSpecialPath;
 use codex_protocol::permissions::NetworkSandboxPolicy;
 use pretty_assertions::assert_eq;
 
+fn drain_insert_history_transcript(
+    rx: &mut tokio::sync::mpsc::UnboundedReceiver<AppEvent>,
+) -> Vec<Vec<ratatui::text::Line<'static>>> {
+    let mut out = Vec::new();
+    while let Ok(ev) = rx.try_recv() {
+        if let AppEvent::InsertHistoryCell(cell) = ev {
+            out.push(cell.transcript_lines(/*width*/ 80));
+        }
+    }
+    out
+}
+
 #[tokio::test]
 async fn resumed_initial_messages_render_history() {
     let (mut chat, mut rx, _ops) = make_chatwidget_manual(/*model_override*/ None).await;
@@ -927,6 +939,7 @@ async fn replayed_reasoning_item_hides_raw_reasoning_when_disabled() {
             summary: vec!["Summary only".to_string()],
             content: vec!["Raw reasoning".to_string()],
             continued: false,
+            continuation_limit_hit: false,
             reasoning_output_tokens: None,
         },
         "turn-1".to_string(),
@@ -981,6 +994,7 @@ async fn replayed_reasoning_summary_parts_render_as_separate_sections() {
             ],
             content: Vec::new(),
             continued: false,
+            continuation_limit_hit: false,
             reasoning_output_tokens: None,
         },
         "turn-1".to_string(),
@@ -1034,17 +1048,17 @@ async fn replayed_continued_reasoning_summary_uses_orange_style() {
             summary: vec!["Summary only".to_string()],
             content: Vec::new(),
             continued: true,
+            continuation_limit_hit: false,
             reasoning_output_tokens: None,
         },
         "turn-1".to_string(),
         ReplayKind::ThreadSnapshot,
     );
 
-    let cell = match rx.try_recv() {
-        Ok(AppEvent::InsertHistoryCell(cell)) => cell,
-        other => panic!("expected InsertHistoryCell, got {other:?}"),
-    };
-    let lines = cell.transcript_lines(/*width*/ 80);
+    let lines = drain_insert_history_transcript(&mut rx)
+        .into_iter()
+        .find(|lines| lines_to_single_string(lines).contains("Summary only"))
+        .expect("expected reasoning summary cell");
     assert_eq!(
         lines[0].spans[1].style.fg,
         Some(ratatui::style::Color::Rgb(255, 128, 0))
@@ -1100,6 +1114,7 @@ async fn rollout_replayed_continued_reasoning_summary_stays_orange() {
                 codex_protocol::protocol::AgentReasoningEvent {
                     text: "Summary only".into(),
                     continued: true,
+                    continuation_limit_hit: false,
                 },
             ),
         ),
@@ -1114,6 +1129,7 @@ async fn rollout_replayed_continued_reasoning_summary_stays_orange() {
                             summary_text: vec!["Summary only".into()],
                             raw_content: Vec::new(),
                             continued: true,
+                            continuation_limit_hit: false,
                             reasoning_output_tokens: Some(516),
                         },
                     ),
@@ -1124,21 +1140,25 @@ async fn rollout_replayed_continued_reasoning_summary_stays_orange() {
     ]);
     chat.replay_thread_turns(turns, ReplayKind::ThreadSnapshot);
 
-    let cell = match rx.try_recv() {
-        Ok(AppEvent::InsertHistoryCell(cell)) => cell,
-        other => panic!("expected InsertHistoryCell, got {other:?}"),
-    };
-    let lines = cell.transcript_lines(/*width*/ 80);
+    let lines = drain_insert_history_transcript(&mut rx)
+        .into_iter()
+        .find(|lines| lines_to_single_string(lines).contains("Summary only"))
+        .expect("expected reasoning summary cell");
     assert_eq!(
         lines[0].spans[1].style.fg,
         Some(ratatui::style::Color::Rgb(255, 128, 0))
     );
+    assert_eq!(
+        lines[0].spans[1].style.bg,
+        Some(ratatui::style::Color::Reset)
+    );
     let suffix = lines[0]
         .spans
         .last()
         .expect("expected reasoning token suffix");
     assert_eq!(suffix.content.as_ref(), " ■ 516 tokens");
     assert_eq!(suffix.style.fg, Some(ratatui::style::Color::Red));
+    assert_eq!(suffix.style.bg, Some(ratatui::style::Color::Reset));
 }
 
 #[tokio::test]
@@ -1175,6 +1195,7 @@ async fn replayed_reasoning_item_shows_raw_reasoning_when_enabled() {
             summary: vec!["Summary only".to_string()],
             content: vec!["Raw reasoning".to_string()],
             continued: false,
+            continuation_limit_hit: false,
             reasoning_output_tokens: None,
         },
         "turn-1".to_string(),
@@ -1253,12 +1274,11 @@ async fn hidden_agent_message_notice_renders_placeholder_on_replay() {
         ReplayKind::ThreadSnapshot,
     );
 
-    let rendered = match rx.try_recv() {
-        Ok(AppEvent::InsertHistoryCell(cell)) => {
-            lines_to_single_string(&cell.transcript_lines(/*width*/ 80))
-        }
-        other => panic!("expected InsertHistoryCell, got {other:?}"),
-    };
+    let rendered = drain_insert_history_transcript(&mut rx)
+        .into_iter()
+        .map(|lines| lines_to_single_string(&lines))
+        .collect::<Vec<_>>()
+        .join("\n");
     assert!(rendered.contains("[Hidden assistant output]"));
     assert!(!rendered.contains("hidden"));
 }
@@ -1287,12 +1307,62 @@ async fn hidden_tool_notice_renders_placeholder_live() {
         /*replay_kind*/ None,
     );
 
-    let rendered = match rx.try_recv() {
-        Ok(AppEvent::InsertHistoryCell(cell)) => {
-            lines_to_single_string(&cell.transcript_lines(/*width*/ 80))
-        }
-        other => panic!("expected InsertHistoryCell, got {other:?}"),
-    };
+    let rendered = drain_insert_history_transcript(&mut rx)
+        .into_iter()
+        .map(|lines| lines_to_single_string(&lines))
+        .collect::<Vec<_>>()
+        .join("\n");
+    assert!(rendered.contains("[Skipped tool execution]"));
+}
+
+#[tokio::test]
+async fn live_turn_id_renders_once_for_visible_and_continuation_items() {
+    let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+    chat.show_welcome_banner = false;
+    let turn_id = "turn-visible";
+
+    chat.handle_server_notification(
+        ServerNotification::ItemCompleted(ItemCompletedNotification {
+            thread_id: "thread-1".to_string(),
+            turn_id: turn_id.to_string(),
+            completed_at_ms: 0,
+            item: AppServerThreadItem::AgentMessage {
+                id: "agent-visible".to_string(),
+                text: "visible answer".to_string(),
+                phase: Some(MessagePhase::FinalAnswer),
+                memory_citation: None,
+                continuation_notice: None,
+                continuation_hidden: false,
+            },
+        }),
+        /*replay_kind*/ None,
+    );
+    chat.handle_server_notification(
+        ServerNotification::ItemCompleted(ItemCompletedNotification {
+            thread_id: "thread-1".to_string(),
+            turn_id: turn_id.to_string(),
+            completed_at_ms: 1,
+            item: AppServerThreadItem::AgentMessage {
+                id: "agent-hidden-tool".to_string(),
+                text: "[Skipped tool execution]".to_string(),
+                phase: Some(MessagePhase::Commentary),
+                memory_citation: None,
+                continuation_notice: Some(
+                    codex_app_server_protocol::ContinuationNoticeKind::SkippedToolExecution,
+                ),
+                continuation_hidden: true,
+            },
+        }),
+        /*replay_kind*/ None,
+    );
+
+    let rendered = drain_insert_history_transcript(&mut rx)
+        .into_iter()
+        .map(|lines| lines_to_single_string(&lines))
+        .collect::<Vec<_>>()
+        .join("\n");
+    assert_eq!(rendered.matches(turn_id).count(), 1, "{rendered}");
+    assert!(rendered.contains("visible answer"));
     assert!(rendered.contains("[Skipped tool execution]"));
 }
 
@@ -1369,6 +1439,7 @@ async fn live_reasoning_summary_is_not_rendered_twice_when_item_completes() {
                 summary: vec!["Summary only".to_string()],
                 content: Vec::new(),
                 continued: false,
+                continuation_limit_hit: false,
                 reasoning_output_tokens: None,
             },
         }),
@@ -1524,6 +1595,7 @@ async fn live_agent_output_waits_for_pending_reasoning_final() {
                 summary: vec!["**Reasoning first**\n\nSummary".to_string()],
                 content: Vec::new(),
                 continued: false,
+                continuation_limit_hit: false,
                 reasoning_output_tokens: Some(292),
             },
         }),
@@ -1589,17 +1661,17 @@ async fn live_reasoning_summary_shows_red_round_token_suffix_for_truncation_fing
                 summary: vec!["**Considering installation updates**\n\nSummary only".to_string()],
                 content: Vec::new(),
                 continued: true,
+                continuation_limit_hit: false,
                 reasoning_output_tokens: Some(516),
             },
         }),
         /*replay_kind*/ None,
     );
 
-    let cell = match rx.try_recv() {
-        Ok(AppEvent::InsertHistoryCell(cell)) => cell,
-        other => panic!("expected InsertHistoryCell, got {other:?}"),
-    };
-    let lines = cell.transcript_lines(/*width*/ 120);
+    let lines = drain_insert_history_transcript(&mut rx)
+        .into_iter()
+        .find(|lines| lines_to_single_string(lines).contains("Considering installation updates"))
+        .expect("expected reasoning summary cell");
     let suffix = lines[0]
         .spans
         .last()
@@ -1655,18 +1727,18 @@ async fn live_raw_reasoning_without_summary_uses_fallback_title() {
                 summary: Vec::new(),
                 content: vec!["raw reasoning".to_string()],
                 continued: true,
+                continuation_limit_hit: false,
                 reasoning_output_tokens: Some(516),
             },
         }),
         /*replay_kind*/ None,
     );
 
-    let rendered = match rx.try_recv() {
-        Ok(AppEvent::InsertHistoryCell(cell)) => {
-            lines_to_single_string(&cell.transcript_lines(/*width*/ 80))
-        }
-        other => panic!("expected InsertHistoryCell, got {other:?}"),
-    };
+    let rendered = drain_insert_history_transcript(&mut rx)
+        .into_iter()
+        .map(|lines| lines_to_single_string(&lines))
+        .collect::<Vec<_>>()
+        .join("\n");
     assert!(rendered.contains("[No reasoning summary]"));
     assert!(rendered.contains("raw reasoning"));
 }
@@ -1705,18 +1777,18 @@ async fn live_completed_raw_reasoning_without_deltas_uses_fallback_title() {
                 summary: Vec::new(),
                 content: vec!["raw reasoning".to_string()],
                 continued: true,
+                continuation_limit_hit: false,
                 reasoning_output_tokens: Some(516),
             },
         }),
         /*replay_kind*/ None,
     );
 
-    let rendered = match rx.try_recv() {
-        Ok(AppEvent::InsertHistoryCell(cell)) => {
-            lines_to_single_string(&cell.transcript_lines(/*width*/ 80))
-        }
-        other => panic!("expected InsertHistoryCell, got {other:?}"),
-    };
+    let rendered = drain_insert_history_transcript(&mut rx)
+        .into_iter()
+        .map(|lines| lines_to_single_string(&lines))
+        .collect::<Vec<_>>()
+        .join("\n");
     assert!(rendered.contains("[No reasoning summary]"));
     assert!(rendered.contains("raw reasoning"));
 }
@@ -1755,18 +1827,18 @@ async fn live_completed_encrypted_only_reasoning_uses_fallback_title() {
                 summary: Vec::new(),
                 content: Vec::new(),
                 continued: true,
+                continuation_limit_hit: false,
                 reasoning_output_tokens: Some(123),
             },
         }),
         /*replay_kind*/ None,
     );
 
-    let rendered = match rx.try_recv() {
-        Ok(AppEvent::InsertHistoryCell(cell)) => {
-            lines_to_single_string(&cell.transcript_lines(/*width*/ 80))
-        }
-        other => panic!("expected InsertHistoryCell, got {other:?}"),
-    };
+    let rendered = drain_insert_history_transcript(&mut rx)
+        .into_iter()
+        .map(|lines| lines_to_single_string(&lines))
+        .collect::<Vec<_>>()
+        .join("\n");
     assert!(rendered.contains("[No reasoning summary]"));
     assert!(rendered.contains("123 tokens"));
 }
@@ -1829,6 +1901,7 @@ async fn live_reasoning_summary_sections_render_titles_on_separate_lines() {
                 ],
                 content: Vec::new(),
                 continued: false,
+                continuation_limit_hit: false,
                 reasoning_output_tokens: None,
             },
         }),
diff --git a/codex-rs/tui/src/history_cell/messages.rs b/codex-rs/tui/src/history_cell/messages.rs
index 8d24aaedcf..df6c91f472 100644
--- a/codex-rs/tui/src/history_cell/messages.rs
+++ b/codex-rs/tui/src/history_cell/messages.rs
@@ -209,6 +209,7 @@ pub(crate) struct ReasoningSummaryCell {
     cwd: PathBuf,
     transcript_only: bool,
     continued: bool,
+    continuation_limit_hit: bool,
     reasoning_output_tokens: Option<i64>,
     show_usage_suffix: bool,
 }
@@ -222,6 +223,7 @@ impl ReasoningSummaryCell {
         cwd: &Path,
         transcript_only: bool,
         continued: bool,
+        continuation_limit_hit: bool,
         reasoning_output_tokens: Option<i64>,
         show_usage_suffix: bool,
     ) -> Self {
@@ -231,6 +233,7 @@ impl ReasoningSummaryCell {
             cwd: cwd.to_path_buf(),
             transcript_only,
             continued,
+            continuation_limit_hit,
             reasoning_output_tokens,
             show_usage_suffix,
         }
@@ -244,7 +247,14 @@ impl ReasoningSummaryCell {
             Some(self.cwd.as_path()),
             &mut lines,
         );
-        let summary_style = if self.continued {
+        let fingerprint_hit = self
+            .reasoning_output_tokens
+            .is_some_and(is_reasoning_truncation_fingerprint);
+        let summary_style = if self.continuation_limit_hit {
+            Style::default().fg(Color::Red).italic()
+        } else if self.continued && fingerprint_hit {
+            Style::default().fg(Color::Rgb(255, 128, 0)).italic()
+        } else if self.continued {
             Style::default().fg(Color::Rgb(255, 128, 0)).italic()
         } else {
             Style::default().dim().italic()
@@ -258,7 +268,9 @@ impl ReasoningSummaryCell {
                     .into_iter()
                     .map(|span| {
                         let mut style = span.style.patch(summary_style);
-                        style.bg = Some(Color::Reset);
+                        if summary_style.bg.is_none() {
+                            style.bg = Some(Color::Reset);
+                        }
                         Span::styled(span.content, style)
                     })
                     .collect();
@@ -270,11 +282,14 @@ impl ReasoningSummaryCell {
             && let Some(tokens) = self.reasoning_output_tokens.filter(|tokens| *tokens > 0)
             && let Some(first_line) = summary_lines.first_mut()
         {
-            let usage_style = if is_reasoning_truncation_fingerprint(tokens) {
+            let usage_style = if self.continuation_limit_hit {
+                Style::default().fg(Color::Red)
+            } else if is_reasoning_truncation_fingerprint(tokens) {
                 Style::default().fg(Color::Red)
             } else {
                 Style::default().fg(Color::Rgb(192, 160, 0))
-            };
+            }
+            .bg(Color::Reset);
             first_line.spans.push(Span::styled(
                 format!(" ■ {} tokens", format_with_separators(tokens)),
                 usage_style,
@@ -525,6 +540,7 @@ pub(crate) fn new_reasoning_summary_block(
     full_reasoning_buffer: String,
     cwd: &Path,
     continued: bool,
+    continuation_limit_hit: bool,
     reasoning_output_tokens: Option<i64>,
 ) -> Box<dyn HistoryCell> {
     let cwd = cwd.to_path_buf();
@@ -538,6 +554,7 @@ pub(crate) fn new_reasoning_summary_block(
                 &cwd,
                 /*transcript_only*/ false,
                 continued,
+                continuation_limit_hit,
                 reasoning_output_tokens,
                 /*show_usage_suffix*/ true,
             ));
@@ -549,6 +566,7 @@ pub(crate) fn new_reasoning_summary_block(
         &cwd,
         /*transcript_only*/ true,
         continued,
+        continuation_limit_hit,
         reasoning_output_tokens,
         /*show_usage_suffix*/ true,
     ))
diff --git a/codex-rs/tui/src/history_cell/notices.rs b/codex-rs/tui/src/history_cell/notices.rs
index 7c01b19c50..2c1a345b29 100644
--- a/codex-rs/tui/src/history_cell/notices.rs
+++ b/codex-rs/tui/src/history_cell/notices.rs
@@ -218,6 +218,14 @@ pub(crate) fn new_continuation_notice(kind: ContinuationNoticeKind) -> PlainHist
     ])])
 }
 
+pub(crate) fn new_continuation_turn_id(turn_id: &str) -> PlainHistoryCell {
+    let short_id = turn_id.chars().take(18).collect::<String>();
+    PlainHistoryCell::new(vec![Line::from(vec![
+        "■ ".dark_gray(),
+        short_id.dark_gray(),
+    ])])
+}
+
 #[derive(Debug)]
 struct ContextCompactedHistoryCell {
     duration_ms: Option<i64>,
diff --git a/codex-rs/tui/src/history_cell/tests.rs b/codex-rs/tui/src/history_cell/tests.rs
index da9243eabe..c78bd5118e 100644
--- a/codex-rs/tui/src/history_cell/tests.rs
+++ b/codex-rs/tui/src/history_cell/tests.rs
@@ -239,6 +239,7 @@ fn source_backed_cells_render_raw_source_without_prefix_or_style() {
         &test_cwd(),
         /*transcript_only*/ false,
         /*continued*/ false,
+        /*continuation_limit_hit*/ false,
         /*reasoning_output_tokens*/ None,
         /*show_usage_suffix*/ false,
     );
@@ -2288,6 +2289,7 @@ fn reasoning_summary_block() {
         "**High level reasoning**\n\nDetailed reasoning goes here.".to_string(),
         &test_cwd(),
         /*continued*/ false,
+        /*continuation_limit_hit*/ false,
         /*reasoning_output_tokens*/ None,
     );
 
@@ -2321,6 +2323,7 @@ fn reasoning_summary_height_matches_wrapped_rendering_for_url_like_content() {
         &test_cwd(),
         /*transcript_only*/ false,
         /*continued*/ false,
+        /*continuation_limit_hit*/ false,
         /*reasoning_output_tokens*/ None,
         /*show_usage_suffix*/ false,
     ));
@@ -2366,6 +2369,7 @@ fn reasoning_summary_block_returns_reasoning_cell_when_feature_disabled() {
         "Detailed reasoning goes here.".to_string(),
         &test_cwd(),
         /*continued*/ false,
+        /*continuation_limit_hit*/ false,
         /*reasoning_output_tokens*/ None,
     );
 
@@ -2382,6 +2386,7 @@ async fn reasoning_summary_block_respects_config_overrides() {
         "**High level reasoning**\n\nDetailed reasoning goes here.".to_string(),
         &test_cwd(),
         /*continued*/ false,
+        /*continuation_limit_hit*/ false,
         /*reasoning_output_tokens*/ None,
     );
 
@@ -2402,6 +2407,7 @@ fn reasoning_summary_block_falls_back_when_header_is_missing() {
         "**High level reasoning without closing".to_string(),
         &test_cwd(),
         /*continued*/ false,
+        /*continuation_limit_hit*/ false,
         /*reasoning_output_tokens*/ None,
     );
 
@@ -2415,6 +2421,7 @@ fn reasoning_summary_block_falls_back_when_summary_is_missing() {
         "**High level reasoning without closing**".to_string(),
         &test_cwd(),
         /*continued*/ false,
+        /*continuation_limit_hit*/ false,
         /*reasoning_output_tokens*/ None,
     );
 
@@ -2425,6 +2432,7 @@ fn reasoning_summary_block_falls_back_when_summary_is_missing() {
         "**High level reasoning without closing**\n\n  ".to_string(),
         &test_cwd(),
         /*continued*/ false,
+        /*continuation_limit_hit*/ false,
         /*reasoning_output_tokens*/ None,
     );
 
@@ -2438,6 +2446,7 @@ fn reasoning_summary_block_splits_header_and_summary_when_present() {
         "**High level plan**\n\nWe should fix the bug next.".to_string(),
         &test_cwd(),
         /*continued*/ false,
+        /*continuation_limit_hit*/ false,
         /*reasoning_output_tokens*/ None,
     );
 
@@ -2460,6 +2469,7 @@ fn reasoning_summary_block_keeps_each_section_header_on_its_own_line() {
         "**First title**\n\nFirst summary.\n\n**Second title**\n\nSecond summary.".to_string(),
         &test_cwd(),
         /*continued*/ false,
+        /*continuation_limit_hit*/ false,
         /*reasoning_output_tokens*/ None,
     );
 
@@ -2484,6 +2494,7 @@ fn continued_reasoning_summary_uses_orange_text() {
         "Detailed reasoning goes here.".to_string(),
         &test_cwd(),
         /*continued*/ true,
+        /*continuation_limit_hit*/ false,
         /*reasoning_output_tokens*/ None,
     );
 
@@ -2501,7 +2512,10 @@ fn continuation_notice_uses_dark_gray_text() {
     let lines = cell.transcript_lines(/*width*/ 80);
     assert_eq!(lines[0].spans[0].content.as_ref(), "■ ");
     assert_eq!(lines[0].spans[0].style.fg, Some(Color::DarkGray));
-    assert_eq!(lines[0].spans[1].content.as_ref(), "[Skipped tool execution]");
+    assert_eq!(
+        lines[0].spans[1].content.as_ref(),
+        "[Skipped tool execution]"
+    );
     assert_eq!(lines[0].spans[1].style.fg, Some(Color::DarkGray));
 }
 
@@ -2511,6 +2525,7 @@ fn reasoning_summary_usage_suffix_is_dark_yellow_for_normal_rounds() {
         "**Investigating status updates**\n\nDetailed reasoning goes here.".to_string(),
         &test_cwd(),
         /*continued*/ true,
+        /*continuation_limit_hit*/ false,
         /*reasoning_output_tokens*/ Some(292),
     );
 
@@ -2529,6 +2544,7 @@ fn reasoning_summary_usage_suffix_is_red_for_truncation_fingerprint_rounds() {
         "**Considering installation updates**\n\nDetailed reasoning goes here.".to_string(),
         &test_cwd(),
         /*continued*/ true,
+        /*continuation_limit_hit*/ false,
         /*reasoning_output_tokens*/ Some(516),
     );
 
@@ -2539,6 +2555,41 @@ fn reasoning_summary_usage_suffix_is_red_for_truncation_fingerprint_rounds() {
         .expect("expected usage suffix on first reasoning line");
     assert_eq!(suffix.content.as_ref(), " ■ 516 tokens");
     assert_eq!(suffix.style.fg, Some(Color::Red));
+    assert_eq!(suffix.style.bg, Some(Color::Reset));
+    assert_eq!(lines[0].spans[1].style.fg, Some(Color::Rgb(255, 128, 0)));
+    assert_eq!(lines[0].spans[1].style.bg, Some(Color::Reset));
+}
+
+#[test]
+fn reasoning_summary_limit_hit_uses_red_text() {
+    let cell = new_reasoning_summary_block(
+        "**Considering installation updates**\n\nDetailed reasoning goes here.".to_string(),
+        &test_cwd(),
+        /*continued*/ true,
+        /*continuation_limit_hit*/ true,
+        /*reasoning_output_tokens*/ Some(516),
+    );
+
+    let lines = cell.transcript_lines(/*width*/ 120);
+    let suffix = lines[0]
+        .spans
+        .last()
+        .expect("expected usage suffix on first reasoning line");
+    assert_eq!(lines[0].spans[1].style.fg, Some(Color::Red));
+    assert_eq!(lines[0].spans[1].style.bg, Some(Color::Reset));
+    assert_eq!(suffix.style.fg, Some(Color::Red));
+    assert_eq!(suffix.style.bg, Some(Color::Reset));
+}
+
+#[test]
+fn continuation_turn_id_uses_dark_gray_text() {
+    let cell = new_continuation_turn_id("019f3b99-f26a-75e1-98b4-c1425cf8e192");
+
+    let lines = cell.transcript_lines(/*width*/ 80);
+    assert_eq!(lines[0].spans[0].content.as_ref(), "■ ");
+    assert_eq!(lines[0].spans[0].style.fg, Some(Color::DarkGray));
+    assert_eq!(lines[0].spans[1].content.as_ref(), "019f3b99-f26a-75e1");
+    assert_eq!(lines[0].spans[1].style.fg, Some(Color::DarkGray));
 }
 
 #[test]
@@ -2645,6 +2696,7 @@ fn wrapped_and_prefixed_cells_handle_tiny_widths() {
         &test_cwd(),
         /*transcript_only*/ false,
         /*continued*/ false,
+        /*continuation_limit_hit*/ false,
         /*reasoning_output_tokens*/ None,
         /*show_usage_suffix*/ false,
     );
diff --git a/codex-rs/tui/src/resume_picker.rs b/codex-rs/tui/src/resume_picker.rs
index f30c5df946..4d229d8d96 100644
--- a/codex-rs/tui/src/resume_picker.rs
+++ b/codex-rs/tui/src/resume_picker.rs
@@ -6272,6 +6272,7 @@ session_picker_view = "dense"
                     summary: Vec::new(),
                     content: vec![String::from("private raw chain of thought")],
                     continued: false,
+                    continuation_limit_hit: false,
                     reasoning_output_tokens: None,
                 }],
                 status: codex_app_server_protocol::TurnStatus::Completed,
@@ -6335,6 +6336,7 @@ session_picker_view = "dense"
                     summary: vec![String::from("public summary")],
                     content: vec![String::from("raw reasoning content")],
                     continued: false,
+                    continuation_limit_hit: false,
                     reasoning_output_tokens: None,
                 }],
                 status: codex_app_server_protocol::TurnStatus::Completed,
diff --git a/codex-rs/tui/src/thread_transcript.rs b/codex-rs/tui/src/thread_transcript.rs
index e19910d9cd..fb366a1b6d 100644
--- a/codex-rs/tui/src/thread_transcript.rs
+++ b/codex-rs/tui/src/thread_transcript.rs
@@ -103,6 +103,7 @@ pub(crate) fn thread_to_transcript_cells(
                         cwd,
                         /*transcript_only*/ false,
                         /*continued*/ false,
+                        /*continuation_limit_hit*/ false,
                         /*reasoning_output_tokens*/ None,
                         /*show_usage_suffix*/ false,
                     )));
