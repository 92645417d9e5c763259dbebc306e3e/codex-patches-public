diff --git a/codex-rs/tui/src/app/agent_status_feed_tests.rs b/codex-rs/tui/src/app/agent_status_feed_tests.rs
index b62aafac51..2b0a057330 100644
--- a/codex-rs/tui/src/app/agent_status_feed_tests.rs
+++ b/codex-rs/tui/src/app/agent_status_feed_tests.rs
@@ -71,6 +71,8 @@ fn agent_status_uses_reasoning_summaries_only() {
                 id: "reasoning-with-summary".to_string(),
                 summary: vec!["safe summary".to_string()],
                 content: vec!["hidden raw reasoning".to_string()],
+                continued: false,
+                reasoning_output_tokens: None,
             },
             thread_id: "thread-child".to_string(),
             turn_id: "turn-1".to_string(),
@@ -83,6 +85,8 @@ fn agent_status_uses_reasoning_summaries_only() {
                 id: "reasoning-without-summary".to_string(),
                 summary: Vec::new(),
                 content: vec!["raw-only reasoning".to_string()],
+                continued: false,
+                reasoning_output_tokens: None,
             },
             thread_id: "thread-child".to_string(),
             turn_id: "turn-1".to_string(),
diff --git a/codex-rs/tui/src/chatwidget/replay.rs b/codex-rs/tui/src/chatwidget/replay.rs
index 48fdc4cc86..f155eb1f68 100644
--- a/codex-rs/tui/src/chatwidget/replay.rs
+++ b/codex-rs/tui/src/chatwidget/replay.rs
@@ -110,7 +110,11 @@ impl ChatWidget {
             }
             ThreadItem::Plan { text, .. } => self.on_plan_item_completed(text),
             ThreadItem::Reasoning {
-                summary, content, ..
+                summary,
+                content,
+                continued,
+                reasoning_output_tokens,
+                ..
             } => {
                 if from_replay {
                     for (summary_index, delta) in summary.into_iter().enumerate() {
@@ -122,7 +126,7 @@ impl ChatWidget {
                         }
                     }
                 }
-                self.on_agent_reasoning_final();
+                self.on_agent_reasoning_final(continued, reasoning_output_tokens);
             }
             item @ ThreadItem::CommandExecution {
                 status: codex_app_server_protocol::CommandExecutionStatus::InProgress,
diff --git a/codex-rs/tui/src/chatwidget/streaming.rs b/codex-rs/tui/src/chatwidget/streaming.rs
index 5951b2f051..739c9a05a9 100644
--- a/codex-rs/tui/src/chatwidget/streaming.rs
+++ b/codex-rs/tui/src/chatwidget/streaming.rs
@@ -247,13 +247,19 @@ impl ChatWidget {
         self.on_agent_reasoning_delta(delta);
     }
 
-    pub(super) fn on_agent_reasoning_final(&mut self) {
+    pub(super) fn on_agent_reasoning_final(
+        &mut self,
+        continued: bool,
+        reasoning_output_tokens: Option<i64>,
+    ) {
         // At the end of a reasoning block, record transcript-only content.
         self.full_reasoning_buffer.push_str(&self.reasoning_buffer);
         if !self.full_reasoning_buffer.is_empty() {
             let cell = history_cell::new_reasoning_summary_block(
                 self.full_reasoning_buffer.clone(),
                 &self.config.cwd,
+                continued,
+                reasoning_output_tokens,
             );
             self.add_boxed_history(cell);
         }
@@ -294,6 +300,8 @@ impl ChatWidget {
             let cell = history_cell::new_reasoning_summary_block(
                 self.full_reasoning_buffer.clone(),
                 &self.config.cwd,
+                /*continued*/ false,
+                /*reasoning_output_tokens*/ None,
             );
             self.add_boxed_history(cell);
             self.add_to_history(history_cell::InterruptedStreamSeparator::new(
diff --git a/codex-rs/tui/src/chatwidget/tests/helpers.rs b/codex-rs/tui/src/chatwidget/tests/helpers.rs
index 61bfac354d..57c0e8d023 100644
--- a/codex-rs/tui/src/chatwidget/tests/helpers.rs
+++ b/codex-rs/tui/src/chatwidget/tests/helpers.rs
@@ -560,6 +560,8 @@ pub(super) fn handle_agent_reasoning_final(chat: &mut ChatWidget) {
                 id: "reasoning-1".to_string(),
                 summary: Vec::new(),
                 content: Vec::new(),
+                continued: false,
+                reasoning_output_tokens: None,
             },
         }),
         /*replay_kind*/ None,
diff --git a/codex-rs/tui/src/chatwidget/tests/history_replay.rs b/codex-rs/tui/src/chatwidget/tests/history_replay.rs
index f8c6096fd7..1eecbeb766 100644
--- a/codex-rs/tui/src/chatwidget/tests/history_replay.rs
+++ b/codex-rs/tui/src/chatwidget/tests/history_replay.rs
@@ -927,6 +927,7 @@ async fn replayed_reasoning_item_hides_raw_reasoning_when_disabled() {
             summary: vec!["Summary only".to_string()],
             content: vec!["Raw reasoning".to_string()],
             continued: false,
+            reasoning_output_tokens: None,
         },
         "turn-1".to_string(),
         ReplayKind::ThreadSnapshot,
@@ -979,6 +980,7 @@ async fn replayed_reasoning_summary_parts_render_as_separate_sections() {
             ],
             content: Vec::new(),
             continued: false,
+            reasoning_output_tokens: None,
         },
         "turn-1".to_string(),
         ReplayKind::ThreadSnapshot,
@@ -1031,6 +1033,7 @@ async fn replayed_continued_reasoning_summary_uses_orange_style() {
             summary: vec!["Summary only".to_string()],
             content: Vec::new(),
             continued: true,
+            reasoning_output_tokens: None,
         },
         "turn-1".to_string(),
         ReplayKind::ThreadSnapshot,
@@ -1085,6 +1088,7 @@ async fn replayed_reasoning_item_shows_raw_reasoning_when_enabled() {
             summary: vec!["Summary only".to_string()],
             content: vec!["Raw reasoning".to_string()],
             continued: false,
+            reasoning_output_tokens: None,
         },
         "turn-1".to_string(),
         ReplayKind::ThreadSnapshot,
@@ -1172,6 +1176,7 @@ async fn live_reasoning_summary_is_not_rendered_twice_when_item_completes() {
                 summary: vec!["Summary only".to_string()],
                 content: Vec::new(),
                 continued: false,
+                reasoning_output_tokens: None,
             },
         }),
         /*replay_kind*/ None,
@@ -1186,6 +1191,69 @@ async fn live_reasoning_summary_is_not_rendered_twice_when_item_completes() {
     assert_eq!(rendered.matches("Summary only").count(), 1);
 }
 
+#[tokio::test]
+async fn live_reasoning_summary_shows_round_token_suffix() {
+    let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+    chat.show_welcome_banner = false;
+
+    chat.handle_server_notification(
+        ServerNotification::TurnStarted(TurnStartedNotification {
+            thread_id: "thread-1".to_string(),
+            turn: AppServerTurn {
+                id: "turn-1".to_string(),
+                items_view: codex_app_server_protocol::TurnItemsView::Full,
+                items: Vec::new(),
+                status: AppServerTurnStatus::InProgress,
+                error: None,
+                started_at: Some(0),
+                completed_at: None,
+                duration_ms: None,
+            },
+        }),
+        /*replay_kind*/ None,
+    );
+    let _ = drain_insert_history(&mut rx);
+
+    chat.handle_server_notification(
+        ServerNotification::ReasoningSummaryTextDelta(ReasoningSummaryTextDeltaNotification {
+            thread_id: "thread-1".to_string(),
+            turn_id: "turn-1".to_string(),
+            item_id: "reasoning-1".to_string(),
+            delta: "**Considering installation updates**\n\nSummary only".to_string(),
+            summary_index: 0,
+        }),
+        /*replay_kind*/ None,
+    );
+
+    chat.handle_server_notification(
+        ServerNotification::ItemCompleted(ItemCompletedNotification {
+            thread_id: "thread-1".to_string(),
+            turn_id: "turn-1".to_string(),
+            completed_at_ms: 0,
+            item: AppServerThreadItem::Reasoning {
+                id: "reasoning-1".to_string(),
+                summary: vec!["**Considering installation updates**\n\nSummary only".to_string()],
+                content: Vec::new(),
+                continued: true,
+                reasoning_output_tokens: Some(516),
+            },
+        }),
+        /*replay_kind*/ None,
+    );
+
+    let cell = match rx.try_recv() {
+        Ok(AppEvent::InsertHistoryCell(cell)) => cell,
+        other => panic!("expected InsertHistoryCell, got {other:?}"),
+    };
+    let lines = cell.transcript_lines(/*width*/ 120);
+    let suffix = lines[0]
+        .spans
+        .last()
+        .expect("expected reasoning token suffix");
+    assert_eq!(suffix.content.as_ref(), " ■ 516 tokens");
+    assert_eq!(suffix.style.fg, Some(ratatui::style::Color::DarkGray));
+}
+
 #[tokio::test]
 async fn live_reasoning_summary_sections_render_titles_on_separate_lines() {
     let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
@@ -1244,6 +1312,7 @@ async fn live_reasoning_summary_sections_render_titles_on_separate_lines() {
                 ],
                 content: Vec::new(),
                 continued: false,
+                reasoning_output_tokens: None,
             },
         }),
         /*replay_kind*/ None,
diff --git a/codex-rs/tui/src/history_cell/messages.rs b/codex-rs/tui/src/history_cell/messages.rs
index 89124f2cad..2b295ac842 100644
--- a/codex-rs/tui/src/history_cell/messages.rs
+++ b/codex-rs/tui/src/history_cell/messages.rs
@@ -1,6 +1,7 @@
 //! User, assistant, reasoning, and streaming message history cells.
 
 use super::*;
+use codex_protocol::num_format::format_with_separators;
 
 #[derive(Debug)]
 pub(crate) struct UserHistoryCell {
@@ -201,6 +202,8 @@ pub(crate) struct ReasoningSummaryCell {
     cwd: PathBuf,
     transcript_only: bool,
     continued: bool,
+    reasoning_output_tokens: Option<i64>,
+    show_usage_suffix: bool,
 }
 
 impl ReasoningSummaryCell {
@@ -212,6 +215,8 @@ impl ReasoningSummaryCell {
         cwd: &Path,
         transcript_only: bool,
         continued: bool,
+        reasoning_output_tokens: Option<i64>,
+        show_usage_suffix: bool,
     ) -> Self {
         Self {
             _header: header,
@@ -219,6 +224,8 @@ impl ReasoningSummaryCell {
             cwd: cwd.to_path_buf(),
             transcript_only,
             continued,
+            reasoning_output_tokens,
+            show_usage_suffix,
         }
     }
 
@@ -251,6 +258,16 @@ impl ReasoningSummaryCell {
                 line
             })
             .collect::<Vec<_>>();
+        let mut summary_lines = summary_lines;
+        if self.show_usage_suffix
+            && let Some(tokens) = self.reasoning_output_tokens.filter(|tokens| *tokens > 0)
+            && let Some(first_line) = summary_lines.first_mut()
+        {
+            first_line.spans.push(Span::styled(
+                format!(" ■ {} tokens", format_with_separators(tokens)),
+                Style::default().fg(Color::DarkGray),
+            ));
+        }
 
         adaptive_wrap_lines(
             &summary_lines,
@@ -496,6 +513,7 @@ pub(crate) fn new_reasoning_summary_block(
     full_reasoning_buffer: String,
     cwd: &Path,
     continued: bool,
+    reasoning_output_tokens: Option<i64>,
 ) -> Box<dyn HistoryCell> {
     let cwd = cwd.to_path_buf();
     let full_reasoning_buffer = full_reasoning_buffer.trim();
@@ -510,6 +528,8 @@ pub(crate) fn new_reasoning_summary_block(
                     &cwd,
                     /*transcript_only*/ false,
                     continued,
+                    reasoning_output_tokens,
+                    /*show_usage_suffix*/ true,
                 ));
             }
         }
@@ -520,5 +540,7 @@ pub(crate) fn new_reasoning_summary_block(
         &cwd,
         /*transcript_only*/ true,
         continued,
+        reasoning_output_tokens,
+        /*show_usage_suffix*/ true,
     ))
 }
diff --git a/codex-rs/tui/src/history_cell/tests.rs b/codex-rs/tui/src/history_cell/tests.rs
index ca141a341a..4ae2ae8757 100644
--- a/codex-rs/tui/src/history_cell/tests.rs
+++ b/codex-rs/tui/src/history_cell/tests.rs
@@ -239,6 +239,8 @@ fn source_backed_cells_render_raw_source_without_prefix_or_style() {
         &test_cwd(),
         /*transcript_only*/ false,
         /*continued*/ false,
+        /*reasoning_output_tokens*/ None,
+        /*show_usage_suffix*/ false,
     );
     let plan = new_proposed_plan(
         "1. Inspect\n\n```sh\ncargo test\n```".to_string(),
@@ -2286,6 +2288,7 @@ fn reasoning_summary_block() {
         "**High level reasoning**\n\nDetailed reasoning goes here.".to_string(),
         &test_cwd(),
         /*continued*/ false,
+        /*reasoning_output_tokens*/ None,
     );
 
     let rendered_display = render_lines(&cell.display_lines(/*width*/ 80));
@@ -2318,6 +2321,8 @@ fn reasoning_summary_height_matches_wrapped_rendering_for_url_like_content() {
         &test_cwd(),
         /*transcript_only*/ false,
         /*continued*/ false,
+        /*reasoning_output_tokens*/ None,
+        /*show_usage_suffix*/ false,
     ));
     let width: u16 = 24;
 
@@ -2361,6 +2366,7 @@ fn reasoning_summary_block_returns_reasoning_cell_when_feature_disabled() {
         "Detailed reasoning goes here.".to_string(),
         &test_cwd(),
         /*continued*/ false,
+        /*reasoning_output_tokens*/ None,
     );
 
     let rendered = render_transcript(cell.as_ref());
@@ -2376,6 +2382,7 @@ async fn reasoning_summary_block_respects_config_overrides() {
         "**High level reasoning**\n\nDetailed reasoning goes here.".to_string(),
         &test_cwd(),
         /*continued*/ false,
+        /*reasoning_output_tokens*/ None,
     );
 
     let rendered_display = render_lines(&cell.display_lines(/*width*/ 80));
@@ -2395,6 +2402,7 @@ fn reasoning_summary_block_falls_back_when_header_is_missing() {
         "**High level reasoning without closing".to_string(),
         &test_cwd(),
         /*continued*/ false,
+        /*reasoning_output_tokens*/ None,
     );
 
     let rendered = render_transcript(cell.as_ref());
@@ -2407,6 +2415,7 @@ fn reasoning_summary_block_falls_back_when_summary_is_missing() {
         "**High level reasoning without closing**".to_string(),
         &test_cwd(),
         /*continued*/ false,
+        /*reasoning_output_tokens*/ None,
     );
 
     let rendered = render_transcript(cell.as_ref());
@@ -2416,6 +2425,7 @@ fn reasoning_summary_block_falls_back_when_summary_is_missing() {
         "**High level reasoning without closing**\n\n  ".to_string(),
         &test_cwd(),
         /*continued*/ false,
+        /*reasoning_output_tokens*/ None,
     );
 
     let rendered = render_transcript(cell.as_ref());
@@ -2428,6 +2438,7 @@ fn reasoning_summary_block_splits_header_and_summary_when_present() {
         "**High level plan**\n\nWe should fix the bug next.".to_string(),
         &test_cwd(),
         /*continued*/ false,
+        /*reasoning_output_tokens*/ None,
     );
 
     let rendered_display = render_lines(&cell.display_lines(/*width*/ 80));
@@ -2449,6 +2460,7 @@ fn reasoning_summary_block_keeps_each_section_header_on_its_own_line() {
         "**First title**\n\nFirst summary.\n\n**Second title**\n\nSecond summary.".to_string(),
         &test_cwd(),
         /*continued*/ false,
+        /*reasoning_output_tokens*/ None,
     );
 
     let rendered_transcript = render_transcript(cell.as_ref());
@@ -2472,6 +2484,7 @@ fn continued_reasoning_summary_uses_orange_text() {
         "Detailed reasoning goes here.".to_string(),
         &test_cwd(),
         /*continued*/ true,
+        /*reasoning_output_tokens*/ None,
     );
 
     let lines = cell.transcript_lines(/*width*/ 80);
@@ -2479,6 +2492,24 @@ fn continued_reasoning_summary_uses_orange_text() {
     assert_eq!(lines[0].spans[1].style.bg, Some(Color::Reset));
 }
 
+#[test]
+fn reasoning_summary_usage_suffix_is_gray() {
+    let cell = new_reasoning_summary_block(
+        "**Considering installation updates**\n\nDetailed reasoning goes here.".to_string(),
+        &test_cwd(),
+        /*continued*/ true,
+        /*reasoning_output_tokens*/ Some(516),
+    );
+
+    let lines = cell.transcript_lines(/*width*/ 120);
+    let suffix = lines[0]
+        .spans
+        .last()
+        .expect("expected usage suffix on first reasoning line");
+    assert_eq!(suffix.content.as_ref(), " ■ 516 tokens");
+    assert_eq!(suffix.style.fg, Some(Color::DarkGray));
+}
+
 #[test]
 fn deprecation_notice_renders_summary_with_details() {
     let cell = new_deprecation_notice(
@@ -2583,6 +2614,8 @@ fn wrapped_and_prefixed_cells_handle_tiny_widths() {
         &test_cwd(),
         /*transcript_only*/ false,
         /*continued*/ false,
+        /*reasoning_output_tokens*/ None,
+        /*show_usage_suffix*/ false,
     );
     let agent_markdown_cell =
         AgentMarkdownCell::new("tiny width agent markdown line\n".to_string(), &test_cwd());
diff --git a/codex-rs/tui/src/resume_picker.rs b/codex-rs/tui/src/resume_picker.rs
index a447f53bdb..65e4558a00 100644
--- a/codex-rs/tui/src/resume_picker.rs
+++ b/codex-rs/tui/src/resume_picker.rs
@@ -6269,6 +6269,8 @@ session_picker_view = "dense"
                     id: String::from("reasoning-1"),
                     summary: Vec::new(),
                     content: vec![String::from("private raw chain of thought")],
+                    continued: false,
+                    reasoning_output_tokens: None,
                 }],
                 status: codex_app_server_protocol::TurnStatus::Completed,
                 error: None,
@@ -6330,6 +6332,8 @@ session_picker_view = "dense"
                     id: String::from("reasoning-1"),
                     summary: vec![String::from("public summary")],
                     content: vec![String::from("raw reasoning content")],
+                    continued: false,
+                    reasoning_output_tokens: None,
                 }],
                 status: codex_app_server_protocol::TurnStatus::Completed,
                 error: None,
diff --git a/codex-rs/tui/src/thread_transcript.rs b/codex-rs/tui/src/thread_transcript.rs
index cdffaab27c..e19910d9cd 100644
--- a/codex-rs/tui/src/thread_transcript.rs
+++ b/codex-rs/tui/src/thread_transcript.rs
@@ -102,6 +102,9 @@ pub(crate) fn thread_to_transcript_cells(
                         text,
                         cwd,
                         /*transcript_only*/ false,
+                        /*continued*/ false,
+                        /*reasoning_output_tokens*/ None,
+                        /*show_usage_suffix*/ false,
                     )));
                 }
             }
