diff --git a/codex-rs/app-server/tests/suite/v2/compaction.rs b/codex-rs/app-server/tests/suite/v2/compaction.rs
index 092c64505f..fcdf42f68c 100644
--- a/codex-rs/app-server/tests/suite/v2/compaction.rs
+++ b/codex-rs/app-server/tests/suite/v2/compaction.rs
@@ -149,10 +149,11 @@ async fn auto_compaction_remote_emits_started_and_completed_items() -> Result<()
     .await;
 
     let codex_home = TempDir::new()?;
+    let feature_flags = BTreeMap::from([(Feature::RemoteCompactionV2, false)]);
     write_mock_responses_config_toml(
         codex_home.path(),
         &server.uri(),
-        &BTreeMap::from([(Feature::RemoteCompactionV2, false)]),
+        &feature_flags,
         REMOTE_AUTO_COMPACT_LIMIT,
         Some(true),
         "mock_provider",
diff --git a/codex-rs/core/tests/common/test_codex.rs b/codex-rs/core/tests/common/test_codex.rs
index d843e0097b..a24120b457 100644
--- a/codex-rs/core/tests/common/test_codex.rs
+++ b/codex-rs/core/tests/common/test_codex.rs
@@ -687,6 +687,7 @@ impl TestCodexBuilder {
         };
         config.cwd = cwd_override;
         config.model_provider = model_provider;
+        let _ = config.features.disable(Feature::RemoteCompactionV2);
         if let Ok(path) = codex_utils_cargo_bin::cargo_bin("codex") {
             config.codex_self_exe = Some(path);
         } else if let Ok(path) = codex_utils_cargo_bin::cargo_bin("codex-exec") {
diff --git a/codex-rs/core/tests/suite/compact_remote.rs b/codex-rs/core/tests/suite/compact_remote.rs
index 6de7dfe301..e5881e98bb 100644
--- a/codex-rs/core/tests/suite/compact_remote.rs
+++ b/codex-rs/core/tests/suite/compact_remote.rs
@@ -1693,6 +1693,14 @@ async fn auto_remote_compact_trims_function_call_history_to_fit_context_window()
         "REMOTE_AUTO_COMPACT_SUMMARY",
     )
     .await;
+    mount_sse_once(
+        harness.server(),
+        sse(vec![
+            responses::ev_assistant_message("post-compact-assistant", "post compact complete"),
+            responses::ev_completed("post-compact-response"),
+        ]),
+    )
+    .await;
 
     codex
         .submit(Op::UserInput {
diff --git a/codex-rs/core/tests/suite/compact_remote_parity.rs b/codex-rs/core/tests/suite/compact_remote_parity.rs
index 432e5815aa..51d10e47f1 100644
--- a/codex-rs/core/tests/suite/compact_remote_parity.rs
+++ b/codex-rs/core/tests/suite/compact_remote_parity.rs
@@ -531,6 +531,8 @@ async fn build_harness_inner(
         }
         if mode == Mode::Legacy {
             let _ = config.features.disable(Feature::RemoteCompactionV2);
+        } else {
+            let _ = config.features.enable(Feature::RemoteCompactionV2);
         }
     }))
     .await
diff --git a/codex-rs/core/tests/suite/hooks.rs b/codex-rs/core/tests/suite/hooks.rs
index 0017cbeff4..d058c029de 100644
--- a/codex-rs/core/tests/suite/hooks.rs
+++ b/codex-rs/core/tests/suite/hooks.rs
@@ -1497,6 +1497,7 @@ async fn resumed_thread_runs_resume_then_compact_session_start_hooks() -> Result
 
     let mut resume_builder = test_codex().with_config(move |config| {
         config.model_auto_compact_token_limit = Some(limit);
+        let _ = config.features.enable(Feature::RemoteCompactionV2);
         trust_discovered_hooks(config);
     });
     let resumed = resume_builder.resume(&server, home, rollout_path).await?;
diff --git a/codex-rs/features/src/tests.rs b/codex-rs/features/src/tests.rs
index 476c80aedc..6fca8c6057 100644
--- a/codex-rs/features/src/tests.rs
+++ b/codex-rs/features/src/tests.rs
@@ -149,6 +149,16 @@ fn request_permissions_tool_is_under_development() {
     assert_eq!(Feature::RequestPermissionsTool.default_enabled(), false);
 }
 
+#[test]
+fn remote_compaction_v2_is_stable_and_enabled_by_default() {
+    assert_eq!(Feature::RemoteCompactionV2.stage(), Stage::Stable);
+    assert_eq!(Feature::RemoteCompactionV2.default_enabled(), true);
+    assert_eq!(
+        feature_for_key("remote_compaction_v2"),
+        Some(Feature::RemoteCompactionV2)
+    );
+}
+
 #[test]
 fn terminal_resize_reflow_is_removed_and_enabled_by_default() {
     assert_eq!(
