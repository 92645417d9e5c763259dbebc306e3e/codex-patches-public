diff --git a/codex-rs/tui/src/bottom_pane/status_line_setup.rs b/codex-rs/tui/src/bottom_pane/status_line_setup.rs
index 0272881e16..8d38e6f04b 100644
--- a/codex-rs/tui/src/bottom_pane/status_line_setup.rs
+++ b/codex-rs/tui/src/bottom_pane/status_line_setup.rs
@@ -133,6 +133,9 @@ pub(crate) enum StatusLineItem {
     /// Total output tokens generated.
     TotalOutputTokens,
 
+    /// Total reasoning output tokens generated.
+    ReasoningTokens,
+
     /// Prompt cache hit rate for input tokens.
     CacheHitRate,
 
@@ -197,6 +200,9 @@ impl StatusLineItem {
             StatusLineItem::UsedTokens => "Total tokens used in session (omitted when zero)",
             StatusLineItem::TotalInputTokens => "Total input tokens used in session",
             StatusLineItem::TotalOutputTokens => "Total output tokens used in session",
+            StatusLineItem::ReasoningTokens => {
+                "Reasoning output tokens and percentage of output (omitted when zero)"
+            }
             StatusLineItem::CacheHitRate => {
                 "Prompt cache hit rate for input tokens (omitted when unavailable)"
             }
@@ -240,6 +246,7 @@ impl StatusLineItem {
             StatusLineItem::UsedTokens => StatusSurfacePreviewItem::UsedTokens,
             StatusLineItem::TotalInputTokens => StatusSurfacePreviewItem::TotalInputTokens,
             StatusLineItem::TotalOutputTokens => StatusSurfacePreviewItem::TotalOutputTokens,
+            StatusLineItem::ReasoningTokens => StatusSurfacePreviewItem::ReasoningTokens,
             StatusLineItem::CacheHitRate => StatusSurfacePreviewItem::CacheHitRate,
             StatusLineItem::SessionId => StatusSurfacePreviewItem::SessionId,
             StatusLineItem::FastMode => StatusSurfacePreviewItem::FastMode,
@@ -527,6 +534,14 @@ mod tests {
         assert_eq!(StatusLineItem::CacheHitRate.to_string(), "cache-hit-rate");
     }
 
+    #[test]
+    fn reasoning_tokens_is_selectable_id() {
+        assert_eq!(
+            "reasoning-tokens".parse::<StatusLineItem>(),
+            Ok(StatusLineItem::ReasoningTokens)
+        );
+    }
+
     #[test]
     fn parse_status_line_items_accepts_title_only_variants() {
         let items = ["run-state", "task-progress"]
diff --git a/codex-rs/tui/src/bottom_pane/status_line_style.rs b/codex-rs/tui/src/bottom_pane/status_line_style.rs
index c232c656bd..7f51c9d45e 100644
--- a/codex-rs/tui/src/bottom_pane/status_line_style.rs
+++ b/codex-rs/tui/src/bottom_pane/status_line_style.rs
@@ -44,6 +44,7 @@ impl StatusLineAccent {
             | StatusLineItem::UsedTokens
             | StatusLineItem::TotalInputTokens
             | StatusLineItem::TotalOutputTokens
+            | StatusLineItem::ReasoningTokens
             | StatusLineItem::CacheHitRate => Self::Usage,
             StatusLineItem::FiveHourLimit | StatusLineItem::WeeklyLimit => Self::Limit,
             StatusLineItem::CodexVersion | StatusLineItem::SessionId => Self::Metadata,
diff --git a/codex-rs/tui/src/bottom_pane/status_surface_preview.rs b/codex-rs/tui/src/bottom_pane/status_surface_preview.rs
index 28af9b6f35..f4f9b3e175 100644
--- a/codex-rs/tui/src/bottom_pane/status_surface_preview.rs
+++ b/codex-rs/tui/src/bottom_pane/status_surface_preview.rs
@@ -28,6 +28,7 @@ pub(crate) enum StatusSurfacePreviewItem {
     UsedTokens,
     TotalInputTokens,
     TotalOutputTokens,
+    ReasoningTokens,
     CacheHitRate,
     SessionId,
     FastMode,
@@ -63,6 +64,7 @@ impl StatusSurfacePreviewItem {
             StatusSurfacePreviewItem::UsedTokens => "0 used",
             StatusSurfacePreviewItem::TotalInputTokens => "0 in",
             StatusSurfacePreviewItem::TotalOutputTokens => "0 out",
+            StatusSurfacePreviewItem::ReasoningTokens => "0 0.0% reasoning",
             StatusSurfacePreviewItem::CacheHitRate => "Cache 0.0% hit",
             StatusSurfacePreviewItem::SessionId => "550e8400-e29b-41d4",
             StatusSurfacePreviewItem::FastMode => "Fast on",
@@ -98,6 +100,7 @@ impl StatusSurfacePreviewItem {
             Self::UsedTokens,
             Self::TotalInputTokens,
             Self::TotalOutputTokens,
+            Self::ReasoningTokens,
             Self::CacheHitRate,
             Self::SessionId,
             Self::FastMode,
diff --git a/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__status_surface_previews_hardcoded_only.snap b/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__status_surface_previews_hardcoded_only.snap
index fc84693d5f..3bd9c0371f 100644
--- a/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__status_surface_previews_hardcoded_only.snap
+++ b/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__status_surface_previews_hardcoded_only.snap
@@ -1,6 +1,7 @@
 ---
 source: tui/src/chatwidget/tests/status_surface_previews.rs
+assertion_line: 157
 expression: snapshot
 ---
-status line: my-project · > feat/awesome-feature · thread title · Custom permissions · Ask for approval
+status line: my-project · > feat/awesome-feature · thread title · Ask For Approval · Ask for approval
 terminal title: thread title | feat/awesome-feature | Tasks 0/0
diff --git a/codex-rs/tui/src/chatwidget/status_surfaces.rs b/codex-rs/tui/src/chatwidget/status_surfaces.rs
index 0fd9c3049c..1fccb28894 100644
--- a/codex-rs/tui/src/chatwidget/status_surfaces.rs
+++ b/codex-rs/tui/src/chatwidget/status_surfaces.rs
@@ -731,14 +731,12 @@ impl ChatWidget {
             StatusLineItem::ContextWindowSize => self
                 .status_line_context_window_size()
                 .map(|cws| format!("{} window", format_tokens_compact(cws))),
-            StatusLineItem::TotalInputTokens => Some(format!(
-                "{} in",
-                format_tokens_compact(self.status_line_total_usage().input_tokens)
-            )),
+            StatusLineItem::TotalInputTokens => Some(self.status_line_input_tokens_text()),
             StatusLineItem::TotalOutputTokens => Some(format!(
                 "{} out",
                 format_tokens_compact(self.status_line_total_usage().output_tokens)
             )),
+            StatusLineItem::ReasoningTokens => self.status_line_reasoning_tokens_text(),
             StatusLineItem::CacheHitRate => self
                 .status_line_total_usage()
                 .cache_hit_rate_percent()
@@ -775,6 +773,45 @@ impl ChatWidget {
             .map(|pull_request| pull_request.url.clone())
     }
 
+    fn status_line_input_tokens_text(&self) -> String {
+        let usage = self.status_line_total_usage();
+        format!("{} in", format_tokens_compact(usage.input_tokens))
+    }
+
+    fn status_line_input_tokens_display_line(&self) -> Line<'static> {
+        Line::from(self.status_line_input_tokens_text())
+    }
+
+    fn status_line_reasoning_tokens_text(&self) -> Option<String> {
+        let usage = self.status_line_total_usage();
+        let reasoning_tokens = usage.reasoning_output_tokens;
+        let rate = usage.reasoning_output_percent()?;
+        (reasoning_tokens > 0).then(|| {
+            format!(
+                "{} {rate:.1}% reasoning",
+                format_tokens_compact(reasoning_tokens)
+            )
+        })
+    }
+
+    fn status_line_reasoning_tokens_display_line(&self) -> Option<Line<'static>> {
+        let usage = self.status_line_total_usage();
+        let reasoning_tokens = usage.reasoning_output_tokens;
+        let rate = usage.reasoning_output_percent()?;
+        if reasoning_tokens <= 0 {
+            return None;
+        }
+        let percent_style = status_line_style_for_item(
+            StatusLineItem::FiveHourLimit,
+            self.config.tui_status_line_use_colors,
+        );
+        Some(Line::from(vec![
+            Span::from(format!("{} ", format_tokens_compact(reasoning_tokens))),
+            Span::styled(format!("{rate:.1}%"), percent_style),
+            Span::from(" reasoning"),
+        ]))
+    }
+
     fn status_line_line_for_item(&mut self, item: &StatusLineItem) -> Option<Line<'static>> {
         match item {
             StatusLineItem::ModelName => Some(self.model_display_line()),
@@ -782,6 +819,8 @@ impl ChatWidget {
             StatusLineItem::FastMode => Some(self.fast_mode_display_line()),
             StatusLineItem::CodexVersion => Some(self.status_line_version_display_line()),
             StatusLineItem::ContextRemaining => self.status_line_context_remaining_display_line(),
+            StatusLineItem::TotalInputTokens => Some(self.status_line_input_tokens_display_line()),
+            StatusLineItem::ReasoningTokens => self.status_line_reasoning_tokens_display_line(),
             StatusLineItem::FiveHourLimit => {
                 self.status_line_limit_display_line(five_hour_status_window)
             }
@@ -925,6 +964,7 @@ impl ChatWidget {
             StatusSurfacePreviewItem::UsedTokens => StatusLineItem::UsedTokens,
             StatusSurfacePreviewItem::TotalInputTokens => StatusLineItem::TotalInputTokens,
             StatusSurfacePreviewItem::TotalOutputTokens => StatusLineItem::TotalOutputTokens,
+            StatusSurfacePreviewItem::ReasoningTokens => StatusLineItem::ReasoningTokens,
             StatusSurfacePreviewItem::CacheHitRate => StatusLineItem::CacheHitRate,
             StatusSurfacePreviewItem::SessionId => StatusLineItem::SessionId,
             StatusSurfacePreviewItem::FastMode => StatusLineItem::FastMode,
diff --git a/codex-rs/tui/src/chatwidget/tests/status_surface_previews.rs b/codex-rs/tui/src/chatwidget/tests/status_surface_previews.rs
index d277c5b85c..2896d4b2c9 100644
--- a/codex-rs/tui/src/chatwidget/tests/status_surface_previews.rs
+++ b/codex-rs/tui/src/chatwidget/tests/status_surface_previews.rs
@@ -320,6 +320,10 @@ async fn cache_hit_rate_status_line_item_uses_token_info() {
         status_preview_line(&mut chat, &[StatusLineItem::CacheHitRate]),
         "80.0% hit"
     );
+    assert_eq!(
+        status_preview_line(&mut chat, &[StatusLineItem::TotalInputTokens]),
+        "1K in"
+    );
 }
 
 #[tokio::test]
@@ -343,6 +347,47 @@ async fn cache_hit_rate_status_line_item_omits_missing_cache_data() {
     );
 }
 
+#[tokio::test]
+async fn reasoning_tokens_status_line_item_uses_token_info() {
+    let (mut chat, _rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+    chat.set_token_info(Some(TokenUsageInfo {
+        total_token_usage: TokenUsage {
+            input_tokens: 4_153_953,
+            cached_input_tokens: 128_980_352,
+            output_tokens: 382_248,
+            reasoning_output_tokens: 56_728,
+            total_tokens: 4_536_201,
+        },
+        last_token_usage: TokenUsage::default(),
+        model_context_window: Some(1_000_000),
+    }));
+
+    assert_eq!(
+        status_preview_line(&mut chat, &[StatusLineItem::ReasoningTokens]),
+        "12 reasoning"
+    );
+}
+
+#[tokio::test]
+async fn reasoning_tokens_status_line_item_omits_missing_reasoning_data() {
+    let (mut chat, _rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+    chat.set_token_info(Some(TokenUsageInfo {
+        total_token_usage: TokenUsage {
+            output_tokens: 50,
+            reasoning_output_tokens: 0,
+            total_tokens: 50,
+            ..TokenUsage::default()
+        },
+        last_token_usage: TokenUsage::default(),
+        model_context_window: Some(100_000),
+    }));
+
+    assert_eq!(
+        chat.status_line_value_for_item(StatusLineItem::ReasoningTokens),
+        None
+    );
+}
+
 #[tokio::test]
 async fn status_line_setup_popup_rate_limits_snapshot() {
     let (mut chat, _rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
diff --git a/codex-rs/tui/src/token_usage.rs b/codex-rs/tui/src/token_usage.rs
index d84f75af9c..f68c14f472 100644
--- a/codex-rs/tui/src/token_usage.rs
+++ b/codex-rs/tui/src/token_usage.rs
@@ -43,6 +43,15 @@ impl TokenUsage {
         Some((cached as f64 / input as f64) * 100.0)
     }
 
+    pub(crate) fn reasoning_output_percent(&self) -> Option<f64> {
+        let output = self.output_tokens.max(0);
+        let reasoning = self.reasoning_output_tokens.max(0).min(output);
+        if output == 0 || reasoning == 0 {
+            return None;
+        }
+        Some((reasoning as f64 / output as f64) * 100.0)
+    }
+
     /// Returns the raw `total_tokens` value. For `last_token_usage`, this is the latest active
     /// context size; for `total_token_usage`, this is the accumulated session total.
     pub(crate) fn tokens_in_context_window(&self) -> i64 {
@@ -135,4 +144,18 @@ mod tests {
             "Token usage: total=110 input=100 output=10"
         );
     }
+
+    #[test]
+    fn reasoning_output_percent_uses_output_tokens() {
+        let usage = TokenUsage {
+            output_tokens: 382_248,
+            reasoning_output_tokens: 56_728,
+            ..TokenUsage::default()
+        };
+
+        assert_eq!(
+            format!("{:.1}", usage.reasoning_output_percent().unwrap()),
+            "14.8"
+        );
+    }
 }
