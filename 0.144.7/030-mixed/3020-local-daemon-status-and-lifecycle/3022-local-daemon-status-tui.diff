diff --git a/codex-rs/Cargo.lock b/codex-rs/Cargo.lock
index a849dd5636..e27d9b76cb 100644
--- a/codex-rs/Cargo.lock
+++ b/codex-rs/Cargo.lock
@@ -4123,6 +4123,7 @@ dependencies = [
  "clap",
  "codex-ansi-escape",
  "codex-app-server-client",
+ "codex-app-server-daemon",
  "codex-app-server-protocol",
  "codex-arg0",
  "codex-cli",
diff --git a/codex-rs/cli/src/main.rs b/codex-rs/cli/src/main.rs
index 3ae8bf7d60..c336bf0726 100644
--- a/codex-rs/cli/src/main.rs
+++ b/codex-rs/cli/src/main.rs
@@ -2289,12 +2289,14 @@ async fn run_interactive_tui(
         }
         Err(err) => return Err(err),
     };
+    let default_local_daemon = false;
     let start_tui = || {
         codex_tui::run_main(
             interactive.clone(),
             arg0_paths.clone(),
             codex_config::LoaderOverrides::default(),
             remote_endpoint.clone(),
+            default_local_daemon,
         )
     };
     let mut attempted_backups = HashSet::new();
diff --git a/codex-rs/tui/Cargo.toml b/codex-rs/tui/Cargo.toml
index 6c0b83a72e..69c339a401 100644
--- a/codex-rs/tui/Cargo.toml
+++ b/codex-rs/tui/Cargo.toml
@@ -28,6 +28,7 @@ chrono = { workspace = true, features = ["serde"] }
 clap = { workspace = true, features = ["derive"] }
 codex-ansi-escape = { workspace = true }
 codex-app-server-client = { workspace = true }
+codex-app-server-daemon = { workspace = true }
 codex-app-server-protocol = { workspace = true }
 codex-arg0 = { workspace = true }
 codex-install-context = { workspace = true }
diff --git a/codex-rs/tui/src/lib.rs b/codex-rs/tui/src/lib.rs
index 17473faf58..2b1d4c9fa6 100644
--- a/codex-rs/tui/src/lib.rs
+++ b/codex-rs/tui/src/lib.rs
@@ -224,10 +224,6 @@ pub use public_widgets::composer_input::ComposerInput;
 
 const TUI_LOG_FILE_NAME: &str = "codex-tui.log";
 
-#[cfg(unix)]
-const AUTO_CONNECT_DAEMON_CONNECT_TIMEOUT: std::time::Duration =
-    std::time::Duration::from_millis(50);
-
 #[allow(clippy::too_many_arguments)]
 async fn start_embedded_app_server(
     arg0_paths: Arg0DispatchPaths,
@@ -409,36 +405,20 @@ async fn connect_remote_app_server(
 }
 
 #[cfg(unix)]
-async fn maybe_probe_default_daemon_socket(codex_home: &Path) -> Option<AbsolutePathBuf> {
-    let socket_path = codex_app_server_client::app_server_control_socket_path(codex_home).ok()?;
-    if !socket_path.as_path().try_exists().unwrap_or(false) {
-        return None;
-    }
-
-    match tokio::time::timeout(
-        AUTO_CONNECT_DAEMON_CONNECT_TIMEOUT,
-        tokio::net::UnixStream::connect(socket_path.as_path()),
-    )
-    .await
-    {
-        Ok(Ok(_stream)) => Some(socket_path),
-        Ok(Err(err)) => {
-            tracing::debug!(%err, socket_path = %socket_path.display(), "skipping default app-server daemon socket");
-            None
-        }
-        Err(_) => {
-            tracing::debug!(
-                socket_path = %socket_path.display(),
-                timeout_ms = AUTO_CONNECT_DAEMON_CONNECT_TIMEOUT.as_millis(),
-                "timed out probing default app-server daemon socket"
-            );
+async fn maybe_start_default_daemon_socket(_codex_home: &Path) -> Option<AbsolutePathBuf> {
+    match codex_app_server_daemon::run(codex_app_server_daemon::LifecycleCommand::Start).await {
+        Ok(output) => AbsolutePathBuf::from_absolute_path(output.socket_path)
+            .inspect_err(|err| tracing::debug!(%err, "default app-server daemon returned invalid socket path"))
+            .ok(),
+        Err(err) => {
+            tracing::debug!(%err, "failed to start default app-server daemon");
             None
         }
     }
 }
 
 #[cfg(not(unix))]
-async fn maybe_probe_default_daemon_socket(_codex_home: &Path) -> Option<AbsolutePathBuf> {
+async fn maybe_start_default_daemon_socket(_codex_home: &Path) -> Option<AbsolutePathBuf> {
     None
 }
 
@@ -798,10 +778,14 @@ fn latest_session_cwd_filter<'a>(
 
 fn app_server_target_for_launch(
     explicit_remote_endpoint: Option<RemoteAppServerEndpoint>,
+    explicit_remote_endpoint_is_default_local_daemon: bool,
     default_daemon_socket: Option<AbsolutePathBuf>,
     can_reuse_implicit_local_daemon: bool,
 ) -> AppServerTarget {
     match explicit_remote_endpoint {
+        Some(endpoint) if explicit_remote_endpoint_is_default_local_daemon => {
+            AppServerTarget::LocalDaemon { endpoint }
+        }
         Some(endpoint) => AppServerTarget::Remote { endpoint },
         None if can_reuse_implicit_local_daemon => {
             default_daemon_socket.map_or(AppServerTarget::Embedded, |socket_path| {
@@ -845,11 +829,25 @@ fn can_reuse_implicit_local_daemon(
         && !has_non_replayable_launch_overrides
 }
 
+fn launched_as_codex_embedded() -> bool {
+    std::env::args_os()
+        .next()
+        .is_some_and(|arg0| is_codex_embedded_arg0(&arg0))
+}
+
+fn is_codex_embedded_arg0(arg0: &std::ffi::OsStr) -> bool {
+    std::path::Path::new(arg0)
+        .file_name()
+        .and_then(|name| name.to_str())
+        .is_some_and(|name| name == "codex-embedded")
+}
+
 pub async fn run_main(
     mut cli: Cli,
     arg0_paths: Arg0DispatchPaths,
     loader_overrides: LoaderOverrides,
     explicit_remote_endpoint: Option<RemoteAppServerEndpoint>,
+    explicit_remote_endpoint_is_default_local_daemon: bool,
 ) -> std::io::Result<AppExitInfo> {
     let strict_config = cli.strict_config;
     let (sandbox_mode, approval_policy) = if cli.dangerously_bypass_approvals_and_sandbox {
@@ -902,19 +900,21 @@ pub async fn run_main(
         launch_loader_overrides.user_config_path = Some(user_config_path);
         launch_loader_overrides.user_config_profile = Some(profile_v2.clone());
     }
-    let reuse_implicit_local_daemon = can_reuse_implicit_local_daemon(
-        &cli_kv_overrides,
-        &launch_loader_overrides,
-        strict_config,
-        cli.bypass_hook_trust,
-    );
+    let reuse_implicit_local_daemon = !launched_as_codex_embedded()
+        && can_reuse_implicit_local_daemon(
+            &cli_kv_overrides,
+            &launch_loader_overrides,
+            strict_config,
+            cli.bypass_hook_trust,
+        );
     let default_daemon = if explicit_remote_endpoint.is_none() && reuse_implicit_local_daemon {
-        maybe_probe_default_daemon_socket(&codex_home).await
+        maybe_start_default_daemon_socket(&codex_home).await
     } else {
         None
     };
     let app_server_target = app_server_target_for_launch(
         explicit_remote_endpoint,
+        explicit_remote_endpoint_is_default_local_daemon,
         default_daemon,
         reuse_implicit_local_daemon,
     );
@@ -2206,39 +2206,13 @@ mod tests {
         }
     }
 
-    #[tokio::test]
-    async fn default_daemon_auto_connect_skips_missing_socket() -> color_eyre::Result<()> {
-        let codex_home = TempDir::new()?;
-        assert!(
-            maybe_probe_default_daemon_socket(codex_home.path())
-                .await
-                .is_none()
-        );
-        Ok(())
-    }
-
-    #[cfg(unix)]
-    #[tokio::test]
-    async fn default_daemon_auto_connect_probes_socket_only() -> color_eyre::Result<()> {
-        let codex_home = TempDir::new()?;
-        let socket_path =
-            codex_app_server_client::app_server_control_socket_path(codex_home.path())?;
-        std::fs::create_dir_all(socket_path.as_path().parent().expect("socket parent"))?;
-        let _listener = tokio::net::UnixListener::bind(socket_path.as_path())?;
-
-        assert_eq!(
-            maybe_probe_default_daemon_socket(codex_home.path()).await,
-            Some(socket_path)
-        );
-        Ok(())
-    }
-
     #[test]
     fn app_server_target_for_launch_uses_local_daemon_for_default_socket() -> color_eyre::Result<()>
     {
         let socket_path = AbsolutePathBuf::relative_to_current_dir("codex.sock")?;
         let target = app_server_target_for_launch(
             /*explicit_remote_endpoint*/ None,
+            /*explicit_remote_endpoint_is_default_local_daemon*/ false,
             Some(socket_path.clone()),
             /*can_reuse_implicit_local_daemon*/ true,
         );
@@ -2261,6 +2235,7 @@ mod tests {
         };
         let target = app_server_target_for_launch(
             Some(explicit_endpoint.clone()),
+            /*explicit_remote_endpoint_is_default_local_daemon*/ false,
             Some(AbsolutePathBuf::relative_to_current_dir("default.sock")?),
             /*can_reuse_implicit_local_daemon*/ false,
         );
@@ -2276,12 +2251,37 @@ mod tests {
         Ok(())
     }
 
+    #[test]
+    fn app_server_target_for_launch_accepts_explicit_default_local_daemon() -> color_eyre::Result<()>
+    {
+        let explicit_endpoint = RemoteAppServerEndpoint::UnixSocket {
+            socket_path: AbsolutePathBuf::relative_to_current_dir("explicit.sock")?,
+        };
+        let target = app_server_target_for_launch(
+            Some(explicit_endpoint.clone()),
+            /*explicit_remote_endpoint_is_default_local_daemon*/ true,
+            /*default_daemon_socket*/ None,
+            /*can_reuse_implicit_local_daemon*/ false,
+        );
+
+        assert_eq!(
+            target,
+            AppServerTarget::LocalDaemon {
+                endpoint: explicit_endpoint,
+            }
+        );
+        assert!(!target.uses_remote_workspace());
+        assert_eq!(target.thread_params_mode(), ThreadParamsMode::Embedded);
+        Ok(())
+    }
+
     #[test]
     fn app_server_target_for_launch_skips_local_daemon_when_launch_config_is_not_replayable()
     -> color_eyre::Result<()> {
         let socket_path = AbsolutePathBuf::relative_to_current_dir("codex.sock")?;
         let target = app_server_target_for_launch(
             /*explicit_remote_endpoint*/ None,
+            /*explicit_remote_endpoint_is_default_local_daemon*/ false,
             Some(socket_path),
             /*can_reuse_implicit_local_daemon*/ false,
         );
@@ -2329,6 +2329,13 @@ mod tests {
         Ok(())
     }
 
+    #[test]
+    fn codex_embedded_arg0_matches_basename_only() {
+        assert!(is_codex_embedded_arg0("codex-embedded".as_ref()));
+        assert!(is_codex_embedded_arg0("~/bin/codex-embedded".as_ref()));
+        assert!(!is_codex_embedded_arg0("codex".as_ref()));
+    }
+
     #[test]
     fn should_load_configured_environments_for_local_daemon() -> color_eyre::Result<()> {
         let target = AppServerTarget::LocalDaemon {
diff --git a/codex-rs/tui/src/main.rs b/codex-rs/tui/src/main.rs
index 486dbe6499..814683e331 100644
--- a/codex-rs/tui/src/main.rs
+++ b/codex-rs/tui/src/main.rs
@@ -60,6 +60,7 @@ fn main() -> anyhow::Result<()> {
             arg0_paths,
             LoaderOverrides::default(),
             /*explicit_remote_endpoint*/ None,
+            /*explicit_remote_endpoint_is_default_local_daemon*/ false,
         )
         .await?;
         let is_fatal = match &exit_info.exit_reason {
diff --git a/codex-rs/tui/src/session_archive_commands.rs b/codex-rs/tui/src/session_archive_commands.rs
index d3e728f5f8..b6bab22337 100644
--- a/codex-rs/tui/src/session_archive_commands.rs
+++ b/codex-rs/tui/src/session_archive_commands.rs
@@ -275,12 +275,13 @@ async fn start_app_server_for_archive_command(
         cli.bypass_hook_trust,
     );
     let default_daemon = if explicit_remote_endpoint.is_none() && reuse_implicit_local_daemon {
-        super::maybe_probe_default_daemon_socket(codex_home.as_path()).await
+        super::maybe_start_default_daemon_socket(codex_home.as_path()).await
     } else {
         None
     };
     let app_server_target = super::app_server_target_for_launch(
         explicit_remote_endpoint,
+        /*explicit_remote_endpoint_is_default_local_daemon*/ false,
         default_daemon,
         reuse_implicit_local_daemon,
     );
