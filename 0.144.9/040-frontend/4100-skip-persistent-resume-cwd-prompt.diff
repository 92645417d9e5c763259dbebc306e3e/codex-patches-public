diff --git a/codex-rs/tui/src/app/tests.rs b/codex-rs/tui/src/app/tests.rs
index d219224fc3..0cdde52561 100644
--- a/codex-rs/tui/src/app/tests.rs
+++ b/codex-rs/tui/src/app/tests.rs
@@ -4900,6 +4900,7 @@ fn session_start_error_surfaces_archived_guidance_without_rollout_path() {
             "/Users/me/.codex/archived_sessions/rollout.jsonl",
         )),
         thread_id,
+        status: None,
     };
     let expected = format!(
         "session {thread_id} is archived. Run `codex unarchive {thread_id}` to unarchive it first."
diff --git a/codex-rs/tui/src/app/tests/startup.rs b/codex-rs/tui/src/app/tests/startup.rs
index 51d6622e16..75199becab 100644
--- a/codex-rs/tui/src/app/tests/startup.rs
+++ b/codex-rs/tui/src/app/tests/startup.rs
@@ -19,6 +19,7 @@ fn startup_waiting_gate_is_only_for_fresh_or_exit_session_selection() {
             crate::resume_picker::SessionTarget {
                 path: Some(PathBuf::from("/tmp/restore")),
                 thread_id: ThreadId::new(),
+                status: None,
             }
         )),
         false
@@ -28,6 +29,7 @@ fn startup_waiting_gate_is_only_for_fresh_or_exit_session_selection() {
             crate::resume_picker::SessionTarget {
                 path: Some(PathBuf::from("/tmp/fork")),
                 thread_id: ThreadId::new(),
+                status: None,
             }
         )),
         false
@@ -39,10 +41,12 @@ fn startup_paused_goal_prompt_gate_is_only_for_quiet_resume() {
     let resume = SessionSelection::Resume(crate::resume_picker::SessionTarget {
         path: Some(PathBuf::from("/tmp/restore")),
         thread_id: ThreadId::new(),
+        status: None,
     });
     let fork = SessionSelection::Fork(crate::resume_picker::SessionTarget {
         path: Some(PathBuf::from("/tmp/fork")),
         thread_id: ThreadId::new(),
+        status: None,
     });
     let no_images: Vec<PathBuf> = Vec::new();
     let initial_images = vec![PathBuf::from("/tmp/image.png")];
@@ -111,6 +115,7 @@ fn startup_waiting_gate_not_applied_for_resume_or_fork_session_selection() {
         crate::resume_picker::SessionTarget {
             path: Some(PathBuf::from("/tmp/restore")),
             thread_id: ThreadId::new(),
+            status: None,
         },
     ));
     assert_eq!(
@@ -124,6 +129,7 @@ fn startup_waiting_gate_not_applied_for_resume_or_fork_session_selection() {
         crate::resume_picker::SessionTarget {
             path: Some(PathBuf::from("/tmp/fork")),
             thread_id: ThreadId::new(),
+            status: None,
         },
     ));
     assert_eq!(
@@ -266,6 +272,7 @@ async fn ignore_same_thread_resume_reports_noop_for_current_thread() {
     let ignored = app.ignore_same_thread_resume(&crate::resume_picker::SessionTarget {
         path: Some(test_path_buf("/tmp/project")),
         thread_id,
+        status: None,
     });
 
     assert!(ignored);
@@ -290,6 +297,7 @@ async fn ignore_same_thread_resume_allows_reattaching_displayed_inactive_thread(
     let ignored = app.ignore_same_thread_resume(&crate::resume_picker::SessionTarget {
         path: Some(test_path_buf("/tmp/project")),
         thread_id,
+        status: None,
     });
 
     assert!(!ignored);
diff --git a/codex-rs/tui/src/lib.rs b/codex-rs/tui/src/lib.rs
index 8e66327b6d..eba51fd37c 100644
--- a/codex-rs/tui/src/lib.rs
+++ b/codex-rs/tui/src/lib.rs
@@ -37,6 +37,7 @@ use codex_app_server_protocol::ThreadListCwdFilter;
 use codex_app_server_protocol::ThreadListParams;
 use codex_app_server_protocol::ThreadSortKey as AppServerThreadSortKey;
 use codex_app_server_protocol::ThreadSourceKind;
+use codex_app_server_protocol::ThreadStatus;
 use codex_cloud_config::cloud_config_bundle_loader_for_storage;
 use codex_config::CloudConfigBundleLoader;
 use codex_config::ConfigLoadError;
@@ -604,6 +605,7 @@ fn session_target_from_app_server_thread(
         Ok(thread_id) => Some(resume_picker::SessionTarget {
             path: thread.path,
             thread_id,
+            status: Some(thread.status),
         }),
         Err(err) => {
             warn!(
@@ -859,6 +861,40 @@ fn latest_session_cwd_filter<'a>(
     }
 }
 
+fn is_active_app_server_resume(
+    app_server_target: &AppServerTarget,
+    session_selection: &resume_picker::SessionSelection,
+) -> bool {
+    is_persistent_app_server_resume(app_server_target, session_selection)
+        && matches!(
+            session_selection,
+            resume_picker::SessionSelection::Resume(resume_picker::SessionTarget {
+                status: Some(ThreadStatus::Active { .. }),
+                ..
+            })
+        )
+}
+
+fn is_persistent_app_server_resume(
+    app_server_target: &AppServerTarget,
+    session_selection: &resume_picker::SessionSelection,
+) -> bool {
+    !matches!(app_server_target, AppServerTarget::Embedded)
+        && matches!(
+            session_selection,
+            resume_picker::SessionSelection::Resume(_)
+        )
+}
+
+fn persistent_app_server_resume_fallback_cwd(
+    app_server_target: &AppServerTarget,
+    session_selection: &resume_picker::SessionSelection,
+    current_cwd: &Path,
+) -> Option<PathBuf> {
+    is_active_app_server_resume(app_server_target, session_selection)
+        .then(|| current_cwd.to_path_buf())
+}
+
 fn app_server_target_for_launch(
     explicit_remote_endpoint: Option<RemoteAppServerEndpoint>,
     explicit_remote_endpoint_is_default_local_daemon: bool,
@@ -1672,9 +1708,17 @@ async fn run_ratatui_app(
         }
         _ => None,
     };
+    let is_persistent_resume =
+        is_persistent_app_server_resume(&app_server_target, &session_selection);
     let fallback_cwd = match action_and_target_session_if_resume_or_fork {
         Some((action, target_session)) => {
-            if uses_remote_workspace {
+            if let Some(cwd) = persistent_app_server_resume_fallback_cwd(
+                &app_server_target,
+                &session_selection,
+                current_cwd.as_path(),
+            ) {
+                Some(cwd)
+            } else if uses_remote_workspace {
                 Some(current_cwd.to_path_buf())
             } else {
                 match resolve_cwd_for_resume_or_fork(
@@ -1808,11 +1852,6 @@ async fn run_ratatui_app(
 
     // Persistent app-server resumes may attach to an already-running thread,
     // where resume config overrides are ignored.
-    let is_persistent_resume = !matches!(&app_server_target, AppServerTarget::Embedded)
-        && matches!(
-            &session_selection,
-            resume_picker::SessionSelection::Resume(_)
-        );
     let bypass_hook_trust_for_startup_review = config.bypass_hook_trust && !is_persistent_resume;
     let hooks_request_handle = app_server.request_handle();
     let hooks_cwd = config.cwd.to_path_buf();
@@ -2294,6 +2333,7 @@ mod tests {
         let target = crate::resume_picker::SessionTarget {
             path: None,
             thread_id,
+            status: None,
         };
 
         assert_eq!(target.display_label(), format!("thread {thread_id}"));
@@ -2581,6 +2621,56 @@ mod tests {
         Ok(())
     }
 
+    #[test]
+    fn active_app_server_resume_skips_cwd_prompt() -> color_eyre::Result<()> {
+        let active_session = resume_picker::SessionTarget {
+            path: Some(PathBuf::from(
+                "/tmp/codex/sessions/2026/06/05/rollout.jsonl",
+            )),
+            thread_id: ThreadId::new(),
+            status: Some(ThreadStatus::Active {
+                active_flags: Vec::new(),
+            }),
+        };
+        let idle_session = resume_picker::SessionTarget {
+            path: None,
+            thread_id: ThreadId::new(),
+            status: Some(ThreadStatus::Idle),
+        };
+        let current_cwd = PathBuf::from("/workspace/project");
+        let local_daemon = AppServerTarget::LocalDaemon {
+            endpoint: RemoteAppServerEndpoint::UnixSocket {
+                socket_path: AbsolutePathBuf::relative_to_current_dir("codex.sock")?,
+            },
+        };
+
+        assert!(is_active_app_server_resume(
+            &local_daemon,
+            &resume_picker::SessionSelection::Resume(active_session.clone())
+        ));
+        assert_eq!(
+            persistent_app_server_resume_fallback_cwd(
+                &local_daemon,
+                &resume_picker::SessionSelection::Resume(active_session.clone()),
+                current_cwd.as_path()
+            ),
+            Some(current_cwd.clone())
+        );
+        assert!(!is_active_app_server_resume(
+            &local_daemon,
+            &resume_picker::SessionSelection::Resume(idle_session)
+        ));
+        assert!(!is_active_app_server_resume(
+            &AppServerTarget::Embedded,
+            &resume_picker::SessionSelection::Resume(active_session.clone())
+        ));
+        assert!(!is_active_app_server_resume(
+            &local_daemon,
+            &resume_picker::SessionSelection::Fork(active_session)
+        ));
+        Ok(())
+    }
+
     #[tokio::test]
     async fn latest_session_lookup_params_omit_local_filters_for_remote_sessions()
     -> std::io::Result<()> {
diff --git a/codex-rs/tui/src/resume_picker.rs b/codex-rs/tui/src/resume_picker.rs
index 54f1cc82dd..4a687e70a6 100644
--- a/codex-rs/tui/src/resume_picker.rs
+++ b/codex-rs/tui/src/resume_picker.rs
@@ -40,6 +40,7 @@ use codex_app_server_protocol::ThreadListCwdFilter;
 use codex_app_server_protocol::ThreadListParams;
 use codex_app_server_protocol::ThreadSortKey;
 use codex_app_server_protocol::ThreadSourceKind;
+use codex_app_server_protocol::ThreadStatus;
 use codex_config::types::SessionPickerViewMode;
 use codex_protocol::ThreadId;
 use codex_utils_path as path_utils;
@@ -87,6 +88,7 @@ const SESSION_INDEX_FILE: &str = "session_index.jsonl";
 pub struct SessionTarget {
     pub path: Option<PathBuf>,
     pub thread_id: ThreadId,
+    pub status: Option<ThreadStatus>,
 }
 
 impl SessionTarget {
@@ -133,8 +135,17 @@ impl SessionPickerAction {
         }
     }
 
-    fn selection(self, path: Option<PathBuf>, thread_id: ThreadId) -> SessionSelection {
-        let target_session = SessionTarget { path, thread_id };
+    fn selection(
+        self,
+        path: Option<PathBuf>,
+        thread_id: ThreadId,
+        status: Option<ThreadStatus>,
+    ) -> SessionSelection {
+        let target_session = SessionTarget {
+            path,
+            thread_id,
+            status,
+        };
         match self {
             SessionPickerAction::Resume => SessionSelection::Resume(target_session),
             SessionPickerAction::Fork => SessionSelection::Fork(target_session),
@@ -961,6 +972,7 @@ struct Row {
     path: Option<PathBuf>,
     preview: String,
     thread_id: Option<ThreadId>,
+    status: Option<ThreadStatus>,
     thread_name: Option<String>,
     pinned: bool,
     created_at: Option<DateTime<Utc>>,
@@ -1249,7 +1261,11 @@ impl PickerState {
                         },
                     };
                     if let Some(thread_id) = thread_id {
-                        return Ok(Some(self.action.selection(path, thread_id)));
+                        return Ok(Some(self.action.selection(
+                            path,
+                            thread_id,
+                            row.status.clone(),
+                        )));
                     }
                     self.inline_error = Some(match path {
                         Some(path) => {
@@ -1938,6 +1954,7 @@ fn row_from_app_server_thread(thread: Thread) -> Option<Row> {
             preview.to_string()
         },
         thread_id: Some(thread_id),
+        status: Some(thread.status),
         thread_name: thread.name,
         pinned: false,
         created_at: chrono::DateTime::from_timestamp(thread.created_at, 0)
@@ -3478,6 +3495,7 @@ mod tests {
             path: Some(PathBuf::from(path)),
             preview: preview.to_string(),
             thread_id: None,
+            status: None,
             thread_name: None,
             pinned: false,
             created_at: Some(timestamp),
@@ -3604,6 +3622,7 @@ mod tests {
             path: Some(PathBuf::from("/tmp/a.jsonl")),
             preview: String::from("first message"),
             thread_id: None,
+            status: None,
             thread_name: Some(String::from("My session")),
             pinned: false,
             created_at: None,
@@ -3645,6 +3664,7 @@ mod tests {
             path: Some(PathBuf::from("/tmp/a.jsonl")),
             preview: String::from("first message"),
             thread_id: Some(thread_id),
+            status: None,
             thread_name: Some(String::from("My session")),
             pinned: false,
             created_at: None,
@@ -3706,6 +3726,7 @@ mod tests {
             path: Some(PathBuf::from("/tmp/a.jsonl")),
             preview: String::from("first message"),
             thread_id: Some(thread_id),
+            status: None,
             thread_name: Some(String::from("feat(tui): add raw scrollback mode")),
             pinned: false,
             created_at: parse_timestamp_str("2026-05-02T14:31:08Z"),
@@ -3917,6 +3938,7 @@ mod tests {
             path: None,
             preview: String::from("remote session"),
             thread_id: Some(ThreadId::new()),
+            status: None,
             thread_name: None,
             pinned: false,
             created_at: None,
@@ -3943,6 +3965,7 @@ mod tests {
             path: None,
             preview: String::from("remote session"),
             thread_id: Some(ThreadId::new()),
+            status: None,
             thread_name: None,
             pinned: false,
             created_at: None,
@@ -3975,6 +3998,7 @@ mod tests {
                 path: Some(PathBuf::from("/tmp/a.jsonl")),
                 preview: String::from("Fix resume picker timestamps"),
                 thread_id: None,
+                status: None,
                 thread_name: None,
                 pinned: false,
                 created_at: Some(now - Duration::minutes(16)),
@@ -3986,6 +4010,7 @@ mod tests {
                 path: Some(PathBuf::from("/tmp/b.jsonl")),
                 preview: String::from("Investigate lazy pagination cap"),
                 thread_id: None,
+                status: None,
                 thread_name: None,
                 pinned: false,
                 created_at: Some(now - Duration::hours(1)),
@@ -3997,6 +4022,7 @@ mod tests {
                 path: Some(PathBuf::from("/tmp/c.jsonl")),
                 preview: String::from("Explain the codebase"),
                 thread_id: None,
+                status: None,
                 thread_name: None,
                 pinned: false,
                 created_at: Some(now - Duration::hours(2)),
@@ -4426,6 +4452,7 @@ mod tests {
             path: None,
             preview: String::from("preview"),
             thread_id: Some(thread_id),
+            status: None,
             thread_name: None,
             pinned: false,
             created_at: None,
@@ -4465,6 +4492,7 @@ mod tests {
                 path: None,
                 preview: String::from("one"),
                 thread_id: Some(ThreadId::new()),
+                status: None,
                 thread_name: None,
                 pinned: false,
                 created_at: None,
@@ -4476,6 +4504,7 @@ mod tests {
                 path: None,
                 preview: String::from("two"),
                 thread_id: Some(ThreadId::new()),
+                status: None,
                 thread_name: None,
                 pinned: false,
                 created_at: None,
@@ -4546,6 +4575,7 @@ mod tests {
                 path: None,
                 preview: String::from("Find pending threads and emails"),
                 thread_id: Some(thread_id),
+                status: None,
                 thread_name: None,
                 pinned: false,
                 created_at: None,
@@ -4557,6 +4587,7 @@ mod tests {
                 path: None,
                 preview: String::from("Plan raw scrollback mode"),
                 thread_id: Some(ThreadId::new()),
+                status: None,
                 thread_name: None,
                 pinned: false,
                 created_at: None,
@@ -4613,6 +4644,7 @@ mod tests {
             path: None,
             preview: String::from("preview"),
             thread_id: Some(thread_id),
+            status: None,
             thread_name: None,
             pinned: false,
             created_at: None,
@@ -4644,6 +4676,7 @@ mod tests {
             path: Some(PathBuf::from("/tmp/a.jsonl")),
             preview: String::from("preview"),
             thread_id: None,
+            status: None,
             thread_name: None,
             pinned: false,
             created_at: None,
@@ -4721,6 +4754,7 @@ mod tests {
             path: None,
             preview: String::from("preview"),
             thread_id: Some(thread_id),
+            status: None,
             thread_name: None,
             pinned: false,
             created_at: None,
@@ -4886,6 +4920,7 @@ session_picker_view = "dense"
             path: None,
             preview: String::from("preview"),
             thread_id: Some(thread_id),
+            status: None,
             thread_name: None,
             pinned: false,
             created_at: None,
@@ -4926,6 +4961,7 @@ session_picker_view = "dense"
             path: None,
             preview: String::from("preview"),
             thread_id: Some(thread_id),
+            status: None,
             thread_name: None,
             pinned: false,
             created_at: None,
@@ -5003,6 +5039,7 @@ session_picker_view = "dense"
             thread_id: Some(
                 ThreadId::from_string("019dabc1-0ef5-7431-b81c-03037f51f62c").expect("thread id"),
             ),
+            status: None,
             thread_name: None,
             pinned: false,
             created_at: parse_timestamp_str("2026-04-28T16:30:00Z"),
@@ -5327,6 +5364,7 @@ session_picker_view = "dense"
             path: Some(PathBuf::from("/tmp/a.jsonl")),
             preview: String::from("Investigate picker expansion"),
             thread_id: Some(thread_id),
+            status: None,
             thread_name: None,
             pinned: false,
             created_at: parse_timestamp_str("2026-04-28T16:30:00Z"),
@@ -5397,6 +5435,7 @@ session_picker_view = "dense"
             thread_id: Some(
                 ThreadId::from_string("019dabc1-0ef5-7431-b81c-03037f51f62c").expect("thread id"),
             ),
+            status: None,
             thread_name: None,
             pinned: false,
             created_at: parse_timestamp_str("2026-04-28T16:30:00Z"),
@@ -5456,6 +5495,7 @@ session_picker_view = "dense"
                 path: Some(PathBuf::from(format!("/tmp/{idx}.jsonl"))),
                 preview: format!("item-{idx}"),
                 thread_id: None,
+                status: None,
                 thread_name: None,
                 pinned: false,
                 created_at: Some(now - Duration::hours(idx)),
@@ -5509,6 +5549,7 @@ session_picker_view = "dense"
                 path: Some(PathBuf::from(format!("/tmp/{idx}.jsonl"))),
                 preview: format!("item-{idx}"),
                 thread_id: None,
+                status: None,
                 thread_name: None,
                 pinned: false,
                 created_at: Some(now - Duration::hours(idx)),
@@ -6061,6 +6102,7 @@ session_picker_view = "dense"
             path: Some(PathBuf::from("/tmp/missing.jsonl")),
             preview: String::from("missing metadata"),
             thread_id: None,
+            status: None,
             thread_name: None,
             pinned: false,
             created_at: None,
@@ -6101,6 +6143,7 @@ session_picker_view = "dense"
             path: None,
             preview: String::from("pathless thread"),
             thread_id: Some(thread_id),
+            status: None,
             thread_name: None,
             pinned: false,
             created_at: None,
@@ -6120,6 +6163,7 @@ session_picker_view = "dense"
             Some(SessionSelection::Resume(SessionTarget {
                 path: None,
                 thread_id: selected_thread_id,
+                status: None,
             })) => assert_eq!(selected_thread_id, thread_id),
             other => panic!("unexpected selection: {other:?}"),
         }
