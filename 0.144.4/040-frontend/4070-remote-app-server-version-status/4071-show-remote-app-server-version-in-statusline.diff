diff --git a/codex-rs/tui/src/status/card.rs b/codex-rs/tui/src/status/card.rs
index 027c324c62..fc7ac33fac 100644
--- a/codex-rs/tui/src/status/card.rs
+++ b/codex-rs/tui/src/status/card.rs
@@ -756,11 +756,24 @@ fn mask_email_part(part: &str) -> String {
 impl HistoryCell for StatusHistoryCell {
     fn display_lines(&self, width: u16) -> Vec<Line<'static>> {
         let mut lines: Vec<Line<'static>> = Vec::new();
+        let version_span = match self
+            .remote_connection
+            .as_ref()
+            .and_then(|conn| conn.version.as_deref())
+        {
+            None if self.remote_connection.is_none() => {
+                Span::from(format!("(v{CODEX_CLI_VERSION})")).red()
+            }
+            Some(version) if version == CODEX_CLI_VERSION => {
+                Span::from(format!("(v{CODEX_CLI_VERSION})")).green()
+            }
+            _ => Span::from(format!("(v{CODEX_CLI_VERSION})")).dim(),
+        };
         lines.push(Line::from(vec![
             Span::from(format!("{}>_ ", FieldFormatter::INDENT)).dim(),
             Span::from("OpenAI Codex").bold(),
             Span::from(" ").dim(),
-            Span::from(format!("(v{CODEX_CLI_VERSION})")).dim(),
+            version_span,
         ]));
 
         let available_inner_width = usize::from(width.saturating_sub(4));
@@ -836,13 +849,25 @@ impl HistoryCell for StatusHistoryCell {
             lines.push(Line::from(Vec::<Span<'static>>::new()));
         }
         if let Some(remote_connection) = self.remote_connection.as_ref() {
-            let wrapped_remote = word_wrap_lines(
-                [Line::from(vec![
+            let remote_spans = if remote_connection.version.as_deref() == Some(CODEX_CLI_VERSION) {
+                vec![Span::from(remote_connection.address.clone())]
+            } else {
+                vec![
                     Span::from(remote_connection.address.clone()),
                     Span::from(" (").dim(),
-                    Span::from(remote_connection.version.clone()).dim(),
+                    Span::from(
+                        remote_connection
+                            .version
+                            .as_ref()
+                            .map(|version| format!("v{version}"))
+                            .unwrap_or_else(|| "unknown".to_string()),
+                    )
+                    .dim(),
                     Span::from(")").dim(),
-                ])],
+                ]
+            };
+            let wrapped_remote = word_wrap_lines(
+                [Line::from(remote_spans)],
                 RtOptions::new(value_width.max(1)),
             );
             let mut wrapped_remote = wrapped_remote.into_iter();
diff --git a/codex-rs/tui/src/status/remote_connection.rs b/codex-rs/tui/src/status/remote_connection.rs
index 610af5815b..ce92ab001b 100644
--- a/codex-rs/tui/src/status/remote_connection.rs
+++ b/codex-rs/tui/src/status/remote_connection.rs
@@ -5,7 +5,7 @@ use url::Url;
 #[derive(Clone, Debug, PartialEq, Eq)]
 pub(crate) struct RemoteConnectionStatus {
     pub(crate) address: String,
-    pub(crate) version: String,
+    pub(crate) version: Option<String>,
 }
 
 pub(crate) fn remote_connection_status_value(
@@ -27,9 +27,7 @@ pub(crate) fn remote_connection_status_value(
             format!("unix://{}", socket_path.display())
         }
     };
-    let version = server_version
-        .map(|version| format!("v{version}"))
-        .unwrap_or_else(|| "unknown".to_string());
+    let version = server_version.map(ToString::to_string);
     Some(RemoteConnectionStatus { address, version })
 }
 
@@ -64,7 +62,7 @@ mod tests {
             remote_connection_status_value(&websocket_target, Some("1.2.3")),
             Some(RemoteConnectionStatus {
                 address: "ws://127.0.0.1:4500/".to_string(),
-                version: "v1.2.3".to_string(),
+                version: Some("1.2.3".to_string()),
             })
         );
 
@@ -78,7 +76,7 @@ mod tests {
             remote_connection_status_value(&daemon_target, /*server_version*/ None),
             Some(RemoteConnectionStatus {
                 address: format!("unix://{}", socket_path.display()),
-                version: "unknown".to_string(),
+                version: None,
             })
         );
         Ok(())
diff --git a/codex-rs/tui/src/status/tests.rs b/codex-rs/tui/src/status/tests.rs
index a84676fb50..16f5952f70 100644
--- a/codex-rs/tui/src/status/tests.rs
+++ b/codex-rs/tui/src/status/tests.rs
@@ -1544,7 +1544,7 @@ async fn status_snapshot_uses_default_reasoning_when_config_empty() {
         .expect("timestamp");
     let remote_connection = RemoteConnectionStatus {
         address: "unix:///tmp/codex-home/app-server-control/app-server-control.sock".to_string(),
-        version: "v0.133.0".to_string(),
+        version: Some("0.133.0".to_string()),
     };
 
     let model_slug = get_model_offline_for_tests(config.model.as_deref());
