diff --git a/codex-rs/tui/src/lib.rs b/codex-rs/tui/src/lib.rs
index 1e351ce16f..d961b93ee7 100644
--- a/codex-rs/tui/src/lib.rs
+++ b/codex-rs/tui/src/lib.rs
@@ -64,6 +64,10 @@ use codex_utils_oss::ensure_oss_provider_ready;
 use codex_utils_oss::get_default_model_for_oss_provider;
 use color_eyre::eyre::WrapErr;
 use cwd_prompt::CwdPromptAction;
+use ratatui::crossterm::cursor::MoveTo;
+use ratatui::crossterm::execute;
+use ratatui::crossterm::terminal::Clear;
+use ratatui::crossterm::terminal::ClearType;
 use serde::Deserialize;
 pub use session_archive_commands::DeleteConfirmation;
 pub use session_archive_commands::SessionArchiveAction;
@@ -71,6 +75,7 @@ pub use session_archive_commands::SessionArchiveCommandOptions;
 pub use session_archive_commands::run_session_archive_command;
 use std::collections::HashSet;
 use std::fs::OpenOptions;
+use std::io::stdout;
 use std::path::Path;
 use std::path::PathBuf;
 use std::sync::Arc;
@@ -1351,6 +1356,7 @@ async fn run_ratatui_app(
         tracing::error!("panic: {info}");
         prev_hook(info);
     }));
+    clear_terminal_before_tui_start()?;
     let mut initialized_terminal = tui::init()?;
     initialized_terminal.terminal.clear()?;
 
@@ -1865,6 +1871,10 @@ async fn run_ratatui_app(
     app_result
 }
 
+fn clear_terminal_before_tui_start() -> std::io::Result<()> {
+    execute!(stdout(), Clear(ClearType::All), MoveTo(0, 0))
+}
+
 #[expect(
     clippy::print_stderr,
     reason = "TUI should no longer be displayed, so we can write to stderr."
