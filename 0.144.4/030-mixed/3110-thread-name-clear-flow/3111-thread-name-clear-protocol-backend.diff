diff --git a/codex-rs/app-server-protocol/schema/json/ClientRequest.json b/codex-rs/app-server-protocol/schema/json/ClientRequest.json
index b6050351ae..7763a083da 100644
--- a/codex-rs/app-server-protocol/schema/json/ClientRequest.json
+++ b/codex-rs/app-server-protocol/schema/json/ClientRequest.json
@@ -4313,14 +4313,16 @@
     "ThreadSetNameParams": {
       "properties": {
         "name": {
-          "type": "string"
+          "type": [
+            "string",
+            "null"
+          ]
         },
         "threadId": {
           "type": "string"
         }
       },
       "required": [
-        "name",
         "threadId"
       ],
       "type": "object"
@@ -7056,4 +7058,4 @@
     }
   ],
   "title": "ClientRequest"
-}
+}
\ No newline at end of file
diff --git a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json
index e48ba4f21b..7df9a4f7d8 100644
--- a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json
+++ b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json
@@ -19785,14 +19785,16 @@
         "$schema": "http://json-schema.org/draft-07/schema#",
         "properties": {
           "name": {
-            "type": "string"
+            "type": [
+              "string",
+              "null"
+            ]
           },
           "threadId": {
             "type": "string"
           }
         },
         "required": [
-          "name",
           "threadId"
         ],
         "title": "ThreadSetNameParams",
@@ -21527,4 +21529,4 @@
   },
   "title": "CodexAppServerProtocol",
   "type": "object"
-}
+}
\ No newline at end of file
diff --git a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json
index 4ad584720f..1c80bedc70 100644
--- a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json
+++ b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json
@@ -17548,14 +17548,16 @@
       "$schema": "http://json-schema.org/draft-07/schema#",
       "properties": {
         "name": {
-          "type": "string"
+          "type": [
+            "string",
+            "null"
+          ]
         },
         "threadId": {
           "type": "string"
         }
       },
       "required": [
-        "name",
         "threadId"
       ],
       "title": "ThreadSetNameParams",
@@ -19289,4 +19291,4 @@
   },
   "title": "CodexAppServerProtocolV2",
   "type": "object"
-}
+}
\ No newline at end of file
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadSetNameParams.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadSetNameParams.json
index 9381c7cb12..ecd8533f79 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadSetNameParams.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadSetNameParams.json
@@ -2,14 +2,16 @@
   "$schema": "http://json-schema.org/draft-07/schema#",
   "properties": {
     "name": {
-      "type": "string"
+      "type": [
+        "string",
+        "null"
+      ]
     },
     "threadId": {
       "type": "string"
     }
   },
   "required": [
-    "name",
     "threadId"
   ],
   "title": "ThreadSetNameParams",
diff --git a/codex-rs/app-server-protocol/schema/typescript/v2/ThreadSetNameParams.ts b/codex-rs/app-server-protocol/schema/typescript/v2/ThreadSetNameParams.ts
index 82b9b3a1c6..80daf69387 100644
--- a/codex-rs/app-server-protocol/schema/typescript/v2/ThreadSetNameParams.ts
+++ b/codex-rs/app-server-protocol/schema/typescript/v2/ThreadSetNameParams.ts
@@ -2,4 +2,4 @@
 
 // This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
 
-export type ThreadSetNameParams = { threadId: string, name: string, };
+export type ThreadSetNameParams = { threadId: string, name?: string | null | null, };
diff --git a/codex-rs/app-server-protocol/src/protocol/v2/thread.rs b/codex-rs/app-server-protocol/src/protocol/v2/thread.rs
index 2277310cb7..1240d26010 100644
--- a/codex-rs/app-server-protocol/src/protocol/v2/thread.rs
+++ b/codex-rs/app-server-protocol/src/protocol/v2/thread.rs
@@ -756,7 +756,14 @@ pub struct ThreadDecrementElicitationResponse {
 #[ts(export_to = "v2/")]
 pub struct ThreadSetNameParams {
     pub thread_id: String,
-    pub name: String,
+    #[serde(
+        default,
+        deserialize_with = "crate::protocol::serde_helpers::deserialize_double_option",
+        serialize_with = "crate::protocol::serde_helpers::serialize_double_option",
+        skip_serializing_if = "Option::is_none"
+    )]
+    #[ts(optional = nullable)]
+    pub name: Option<Option<String>>,
 }
 
 #[derive(Serialize, Deserialize, Debug, Clone, PartialEq, JsonSchema, TS)]
diff --git a/codex-rs/app-server/src/request_processors/thread_processor.rs b/codex-rs/app-server/src/request_processors/thread_processor.rs
index bf5f442a43..6dd1a61a6e 100644
--- a/codex-rs/app-server/src/request_processors/thread_processor.rs
+++ b/codex-rs/app-server/src/request_processors/thread_processor.rs
@@ -1602,8 +1602,18 @@ impl ThreadRequestProcessor {
         let ThreadSetNameParams { thread_id, name } = params;
         let thread_id = ThreadId::from_string(&thread_id)
             .map_err(|err| invalid_request(format!("invalid thread id: {err}")))?;
-        let Some(name) = codex_core::util::normalize_thread_name(&name) else {
-            return Err(invalid_request("thread name must not be empty"));
+        let name = match name {
+            Some(Some(name)) => Some(
+                codex_core::util::normalize_thread_name(&name)
+                    .ok_or_else(|| invalid_request("thread name must not be empty"))?,
+            ),
+            Some(None) => None,
+            None => return Err(invalid_request("thread name is required")),
+        };
+
+        let name_patch = match name.clone() {
+            Some(name) => Some(Some(name)),
+            None => Some(None),
         };
 
         let _thread_list_state_permit = self.acquire_thread_list_state_permit().await?;
@@ -1611,7 +1621,7 @@ impl ThreadRequestProcessor {
             .update_thread_metadata(
                 thread_id,
                 StoreThreadMetadataPatch {
-                    name: Some(Some(name.clone())),
+                    name: name_patch,
                     ..Default::default()
                 },
                 /*include_archived*/ false,
@@ -1623,7 +1633,7 @@ impl ThreadRequestProcessor {
             ThreadSetNameResponse {},
             Some(ThreadNameUpdatedNotification {
                 thread_id: thread_id.to_string(),
-                thread_name: Some(name),
+                thread_name: name,
             }),
         ))
     }
diff --git a/codex-rs/app-server/tests/suite/v2/thread_name_websocket.rs b/codex-rs/app-server/tests/suite/v2/thread_name_websocket.rs
index 951e4d74e9..ea2ae18ae8 100644
--- a/codex-rs/app-server/tests/suite/v2/thread_name_websocket.rs
+++ b/codex-rs/app-server/tests/suite/v2/thread_name_websocket.rs
@@ -64,7 +64,7 @@ async fn thread_name_updated_broadcasts_for_loaded_threads() -> Result<()> {
             /*id*/ 11,
             Some(serde_json::to_value(ThreadSetNameParams {
                 thread_id: conversation_id.clone(),
-                name: renamed.to_string(),
+                name: Some(Some(renamed.to_string())),
             })?),
         )
         .await?;
@@ -116,7 +116,7 @@ async fn thread_name_updated_broadcasts_for_not_loaded_threads() -> Result<()> {
             /*id*/ 20,
             Some(serde_json::to_value(ThreadSetNameParams {
                 thread_id: conversation_id.clone(),
-                name: renamed.to_string(),
+                name: Some(Some(renamed.to_string())),
             })?),
         )
         .await?;
@@ -147,6 +147,62 @@ async fn thread_name_updated_broadcasts_for_not_loaded_threads() -> Result<()> {
     result
 }
 
+#[tokio::test]
+async fn thread_name_updated_broadcasts_when_name_is_cleared() -> Result<()> {
+    let server = create_mock_responses_server_repeating_assistant("Done").await;
+    let codex_home = TempDir::new()?;
+    create_config_toml(codex_home.path(), &server.uri(), "never")?;
+    let conversation_id = create_rollout(codex_home.path(), "2025-01-05T12-10-00")?;
+    let thread_id = ThreadId::from_string(&conversation_id)?;
+    codex_rollout::append_thread_name(codex_home.path(), thread_id, "Old name").await?;
+
+    let (mut process, bind_addr) = spawn_websocket_server(codex_home.path()).await?;
+
+    let result = async {
+        let mut ws1 = connect_websocket(bind_addr).await?;
+        let mut ws2 = connect_websocket(bind_addr).await?;
+        initialize_both_clients(&mut ws1, &mut ws2).await?;
+
+        send_request(
+            &mut ws1,
+            "thread/name/set",
+            /*id*/ 30,
+            Some(serde_json::to_value(ThreadSetNameParams {
+                thread_id: conversation_id.clone(),
+                name: Some(None),
+            })?),
+        )
+        .await?;
+        let (rename_resp, ws1_notification) = read_response_and_notification_for_method(
+            &mut ws1,
+            /*id*/ 30,
+            "thread/name/updated",
+        )
+        .await?;
+        let _: ThreadSetNameResponse = to_response::<ThreadSetNameResponse>(rename_resp)?;
+        assert_thread_name_updated_value(ws1_notification, &conversation_id, None)?;
+
+        let ws2_notification =
+            read_notification_for_method(&mut ws2, "thread/name/updated").await?;
+        assert_thread_name_updated_value(ws2_notification, &conversation_id, None)?;
+        assert_eq!(
+            find_thread_name_by_id(codex_home.path(), &thread_id).await?,
+            None
+        );
+
+        assert_no_message(&mut ws1, Duration::from_millis(250)).await?;
+        assert_no_message(&mut ws2, Duration::from_millis(250)).await?;
+        Ok(())
+    }
+    .await;
+
+    process
+        .kill()
+        .await
+        .context("failed to stop websocket app-server process")?;
+    result
+}
+
 async fn initialize_both_clients(ws1: &mut WsClient, ws2: &mut WsClient) -> Result<()> {
     send_initialize_request(ws1, /*id*/ 1, "ws_client_one").await?;
     timeout(DEFAULT_READ_TIMEOUT, read_response_for_id(ws1, /*id*/ 1)).await??;
@@ -172,11 +228,19 @@ fn assert_thread_name_updated(
     notification: JSONRPCNotification,
     thread_id: &str,
     thread_name: &str,
+) -> Result<()> {
+    assert_thread_name_updated_value(notification, thread_id, Some(thread_name))
+}
+
+fn assert_thread_name_updated_value(
+    notification: JSONRPCNotification,
+    thread_id: &str,
+    thread_name: Option<&str>,
 ) -> Result<()> {
     let notification: ThreadNameUpdatedNotification =
         serde_json::from_value(notification.params.context("thread/name/updated params")?)?;
     assert_eq!(notification.thread_id, thread_id);
-    assert_eq!(notification.thread_name.as_deref(), Some(thread_name));
+    assert_eq!(notification.thread_name.as_deref(), thread_name);
     Ok(())
 }
 
diff --git a/codex-rs/app-server/tests/suite/v2/thread_read.rs b/codex-rs/app-server/tests/suite/v2/thread_read.rs
index 9ad41cded5..91a9737190 100644
--- a/codex-rs/app-server/tests/suite/v2/thread_read.rs
+++ b/codex-rs/app-server/tests/suite/v2/thread_read.rs
@@ -1075,7 +1075,7 @@ async fn thread_name_set_is_reflected_in_read_list_and_resume() -> Result<()> {
     let set_id = mcp
         .send_thread_set_name_request(ThreadSetNameParams {
             thread_id: conversation_id.clone(),
-            name: new_name.to_string(),
+            name: Some(Some(new_name.to_string())),
         })
         .await?;
     let set_resp: JSONRPCResponse = timeout(
diff --git a/codex-rs/thread-store/src/local/update_thread_metadata.rs b/codex-rs/thread-store/src/local/update_thread_metadata.rs
index 580df1dc61..412204c648 100644
--- a/codex-rs/thread-store/src/local/update_thread_metadata.rs
+++ b/codex-rs/thread-store/src/local/update_thread_metadata.rs
@@ -14,6 +14,7 @@ use codex_rollout::append_thread_name;
 use codex_rollout::find_archived_thread_path_by_id_str;
 use codex_rollout::find_thread_path_by_id_str;
 use codex_rollout::read_session_meta_line;
+use codex_rollout::remove_thread_name_entries;
 use codex_state::ThreadMetadataBuilder;
 use tracing::warn;
 
@@ -108,7 +109,10 @@ pub(super) async fn update_thread_metadata(
     .await;
 
     if let Some(name) = name {
-        apply_thread_name(store, thread_id, name.unwrap_or_default()).await?;
+        match name {
+            Some(name) => apply_thread_name(store, thread_id, name).await?,
+            None => clear_thread_name(store, thread_id).await?,
+        }
     }
 
     let resolved_git_info = match git_info {
@@ -623,6 +627,14 @@ async fn apply_thread_name(
         })
 }
 
+async fn clear_thread_name(store: &LocalThreadStore, thread_id: ThreadId) -> ThreadStoreResult<()> {
+    remove_thread_name_entries(store.config.codex_home.as_path(), thread_id)
+        .await
+        .map_err(|err| ThreadStoreError::Internal {
+            message: format!("failed to clear thread name index entries: {err}"),
+        })
+}
+
 async fn apply_thread_memory_mode(
     rollout_path: &Path,
     thread_id: ThreadId,
@@ -766,6 +778,36 @@ mod tests {
         assert_eq!(latest_name.as_deref(), Some("A sharper name"));
     }
 
+    #[tokio::test]
+    async fn update_thread_metadata_clears_name_and_removes_index_entries() {
+        let home = TempDir::new().expect("temp dir");
+        let store = LocalThreadStore::new(test_config(home.path()), /*state_db*/ None);
+        let uuid = Uuid::from_u128(302);
+        let thread_id = ThreadId::from_string(&uuid.to_string()).expect("valid thread id");
+        write_session_file(home.path(), "2025-01-03T14-05-00", uuid).expect("session file");
+        codex_rollout::append_thread_name(home.path(), thread_id, "Old name")
+            .await
+            .expect("append old thread name");
+
+        let thread = store
+            .update_thread_metadata(UpdateThreadMetadataParams {
+                thread_id,
+                patch: ThreadMetadataPatch {
+                    name: Some(None),
+                    ..Default::default()
+                },
+                include_archived: false,
+            })
+            .await
+            .expect("clear thread name");
+
+        assert_eq!(thread.name, None);
+        let latest_name = codex_rollout::find_thread_name_by_id(home.path(), &thread_id)
+            .await
+            .expect("find thread name");
+        assert_eq!(latest_name, None);
+    }
+
     #[tokio::test]
     async fn update_thread_metadata_sets_memory_mode_on_active_rollout() {
         let home = TempDir::new().expect("temp dir");
diff --git a/codex-rs/tui/src/app/thread_routing.rs b/codex-rs/tui/src/app/thread_routing.rs
index 0ba71f7f1b..d48fffab4f 100644
--- a/codex-rs/tui/src/app/thread_routing.rs
+++ b/codex-rs/tui/src/app/thread_routing.rs
@@ -772,9 +772,7 @@ impl App {
                 Ok(true)
             }
             AppCommand::SetThreadName { name } => {
-                app_server
-                    .thread_set_name(thread_id, name.to_string())
-                    .await?;
+                app_server.thread_set_name(thread_id, name.clone()).await?;
                 Ok(true)
             }
             AppCommand::ThreadRollback { num_turns } => {
diff --git a/codex-rs/tui/src/app_command.rs b/codex-rs/tui/src/app_command.rs
index 2ebcb7171b..dfc5a9392e 100644
--- a/codex-rs/tui/src/app_command.rs
+++ b/codex-rs/tui/src/app_command.rs
@@ -92,7 +92,7 @@ pub(crate) enum AppCommand {
     },
     Compact,
     SetThreadName {
-        name: String,
+        name: Option<String>,
     },
     Shutdown,
     ThreadRollback {
@@ -255,7 +255,7 @@ impl AppCommand {
         Self::Compact
     }
 
-    pub(crate) fn set_thread_name(name: String) -> Self {
+    pub(crate) fn set_thread_name(name: Option<String>) -> Self {
         Self::SetThreadName { name }
     }
 
diff --git a/codex-rs/tui/src/app_event_sender.rs b/codex-rs/tui/src/app_event_sender.rs
index 1d5ac55d95..4730a99ae7 100644
--- a/codex-rs/tui/src/app_event_sender.rs
+++ b/codex-rs/tui/src/app_event_sender.rs
@@ -56,7 +56,7 @@ impl AppEventSender {
         self.send(AppEvent::CodexOp(AppCommand::compact()));
     }
 
-    pub(crate) fn set_thread_name(&self, name: String) {
+    pub(crate) fn set_thread_name(&self, name: Option<String>) {
         self.send(AppEvent::CodexOp(AppCommand::set_thread_name(name)));
     }
 
diff --git a/codex-rs/tui/src/app_server_session.rs b/codex-rs/tui/src/app_server_session.rs
index b41ea6f12a..ad32f78033 100644
--- a/codex-rs/tui/src/app_server_session.rs
+++ b/codex-rs/tui/src/app_server_session.rs
@@ -936,7 +936,7 @@ impl AppServerSession {
     pub(crate) async fn thread_set_name(
         &mut self,
         thread_id: ThreadId,
-        name: String,
+        name: Option<String>,
     ) -> Result<()> {
         let request_id = self.next_request_id();
         let _: ThreadSetNameResponse = self
@@ -945,7 +945,7 @@ impl AppServerSession {
                 request_id,
                 params: ThreadSetNameParams {
                     thread_id: thread_id.to_string(),
-                    name,
+                    name: Some(name),
                 },
             })
             .await
diff --git a/codex-rs/tui/src/chatwidget/interaction.rs b/codex-rs/tui/src/chatwidget/interaction.rs
index 96a4e696db..aa55492a0b 100644
--- a/codex-rs/tui/src/chatwidget/interaction.rs
+++ b/codex-rs/tui/src/chatwidget/interaction.rs
@@ -344,7 +344,7 @@ impl ChatWidget {
                     )));
                     return;
                 };
-                tx.set_thread_name(name);
+                tx.set_thread_name(Some(name));
             }),
         );
 
diff --git a/codex-rs/tui/src/chatwidget/slash_dispatch.rs b/codex-rs/tui/src/chatwidget/slash_dispatch.rs
index f2b0e89dc5..b0da383b84 100644
--- a/codex-rs/tui/src/chatwidget/slash_dispatch.rs
+++ b/codex-rs/tui/src/chatwidget/slash_dispatch.rs
@@ -725,7 +725,7 @@ impl ChatWidget {
                     self.add_error_message("Thread name cannot be empty.".to_string());
                     return;
                 };
-                self.app_event_tx.set_thread_name(name);
+                self.app_event_tx.set_thread_name(Some(name));
             }
             SlashCommand::Plan if !trimmed.is_empty() => {
                 if !self.apply_plan_slash_command() {
diff --git a/codex-rs/tui/src/chatwidget/tests/slash_commands.rs b/codex-rs/tui/src/chatwidget/tests/slash_commands.rs
index 0504a4c269..ba20c0b41a 100644
--- a/codex-rs/tui/src/chatwidget/tests/slash_commands.rs
+++ b/codex-rs/tui/src/chatwidget/tests/slash_commands.rs
@@ -469,7 +469,7 @@ async fn queued_bare_rename_drains_next_input_after_name_update() {
     assert!(
         events.iter().any(|event| matches!(
             event,
-            AppEvent::CodexOp(Op::SetThreadName { name }) if name == "Queued rename"
+            AppEvent::CodexOp(Op::SetThreadName { name }) if name.as_deref() == Some("Queued rename")
         )),
         "expected rename prompt to submit thread name; events: {events:?}"
     );
@@ -514,7 +514,7 @@ async fn queued_inline_rename_does_not_drain_again_before_turn_started() {
     assert!(
         events.iter().any(|event| matches!(
             event,
-            AppEvent::CodexOp(Op::SetThreadName { name }) if name == "Queued rename"
+            AppEvent::CodexOp(Op::SetThreadName { name }) if name.as_deref() == Some("Queued rename")
         )),
         "expected queued /rename to submit thread name; events: {events:?}"
     );
@@ -1182,7 +1182,7 @@ async fn slash_rename_prefills_existing_thread_name() {
 
     assert_matches!(
         rx.try_recv(),
-        Ok(AppEvent::CodexOp(Op::SetThreadName { name })) if name == "Current project title"
+        Ok(AppEvent::CodexOp(Op::SetThreadName { name })) if name.as_deref() == Some("Current project title")
     );
 }
 
