diff --git a/codex-rs/thread-store/src/live_thread.rs b/codex-rs/thread-store/src/live_thread.rs
index f2dc3cfeb0..55462586db 100644
--- a/codex-rs/thread-store/src/live_thread.rs
+++ b/codex-rs/thread-store/src/live_thread.rs
@@ -1,3 +1,4 @@
+use std::path::Path;
 use std::path::PathBuf;
 use std::sync::Arc;
 
@@ -6,9 +7,11 @@ use codex_protocol::protocol::RolloutItem;
 use codex_protocol::protocol::ThreadHistoryMode;
 use codex_protocol::protocol::ThreadMemoryMode;
 use codex_rollout::RolloutPersistenceTelemetry;
+use codex_rollout::append_rollout_item_to_path;
 use codex_rollout::measure_and_filter_rollout_items;
 use codex_rollout::persisted_rollout_items;
 use tokio::sync::Mutex;
+use tokio::sync::Semaphore;
 use tracing::warn;
 
 use crate::AppendThreadItemsParams;
@@ -36,6 +39,8 @@ pub struct LiveThread {
     thread_id: ThreadId,
     history_mode: ThreadHistoryMode,
     thread_store: Arc<dyn ThreadStore>,
+    fallback_rollout_path: Option<PathBuf>,
+    fallback_rollout_append_gate: Arc<Semaphore>,
     metadata_sync: Arc<Mutex<ThreadMetadataSync>>,
     persistence_telemetry: RolloutPersistenceTelemetry,
 }
@@ -97,11 +102,19 @@ impl LiveThread {
         let thread_id = params.thread_id;
         let history_mode = params.history_mode;
         let metadata_sync = ThreadMetadataSync::for_create(&params).await;
-        thread_store.create_thread(params).await?;
+        let fallback_rollout_path =
+            if let Some(local_store) = thread_store.as_any().downcast_ref::<LocalThreadStore>() {
+                Some(local_store.create_thread_with_rollout_path(params).await?)
+            } else {
+                thread_store.create_thread(params).await?;
+                None
+            };
         Ok(Self {
             thread_id,
             history_mode,
             thread_store,
+            fallback_rollout_path,
+            fallback_rollout_append_gate: Arc::new(Semaphore::new(1)),
             metadata_sync: Arc::new(Mutex::new(metadata_sync)),
             persistence_telemetry: RolloutPersistenceTelemetry::new(thread_id),
         })
@@ -152,7 +165,13 @@ impl LiveThread {
         let should_load_history = params.history.is_none();
         let include_archived = params.include_archived;
         let mut metadata_sync = ThreadMetadataSync::for_resume(&params);
-        thread_store.resume_thread(params).await?;
+        let fallback_rollout_path =
+            if let Some(local_store) = thread_store.as_any().downcast_ref::<LocalThreadStore>() {
+                Some(local_store.resume_thread_with_rollout_path(params).await?)
+            } else {
+                thread_store.resume_thread(params).await?;
+                None
+            };
         if should_load_history {
             match thread_store
                 .load_history(LoadThreadHistoryParams {
@@ -176,6 +195,8 @@ impl LiveThread {
             thread_id,
             history_mode,
             thread_store,
+            fallback_rollout_path,
+            fallback_rollout_append_gate: Arc::new(Semaphore::new(1)),
             metadata_sync: Arc::new(Mutex::new(metadata_sync)),
             persistence_telemetry: RolloutPersistenceTelemetry::new(thread_id),
         })
@@ -227,17 +248,47 @@ impl LiveThread {
         } else {
             (persisted_rollout_items(raw_items, self.history_mode), None)
         };
-        self.thread_store
+        let appended_to_fallback_rollout = match self
+            .thread_store
             .append_items(AppendThreadItemsParams {
                 thread_id: self.thread_id,
                 items: raw_items.to_vec(),
             })
-            .await?;
+            .await
+        {
+            Ok(()) => false,
+            Err(err @ ThreadStoreError::ThreadNotFound { thread_id })
+                if thread_id == self.thread_id =>
+            {
+                let _append_permit =
+                    self.fallback_rollout_append_gate
+                        .acquire()
+                        .await
+                        .map_err(|err| ThreadStoreError::Internal {
+                            message: format!("failed to acquire rollout fallback permit: {err}"),
+                        })?;
+                append_items_to_fallback_rollout(
+                    self.fallback_rollout_path.as_deref(),
+                    err,
+                    items.as_slice(),
+                )
+                .await?;
+                true
+            }
+            Err(err) => return Err(err),
+        };
         if let Some(measurement) = measurement.as_ref() {
             self.persistence_telemetry
                 .record_batch(raw_items, measurement);
         }
-        Ok(items)
+        if appended_to_fallback_rollout {
+            // The JSONL remains the canonical recovery source. A reopened local writer rebuilds
+            // paginated projections from it; this closed-writer fallback must not publish stale
+            // metadata or a second copy of the same item through the live projection path.
+            Ok(Vec::new())
+        } else {
+            Ok(items)
+        }
     }
 
     pub async fn persist(&self) -> ThreadStoreResult<()> {
@@ -373,3 +424,40 @@ impl LiveThread {
         Ok(())
     }
 }
+
+async fn append_items_to_fallback_rollout(
+    rollout_path: Option<&Path>,
+    fallback_error: ThreadStoreError,
+    items: &[RolloutItem],
+) -> ThreadStoreResult<()> {
+    let Some(rollout_path) = rollout_path else {
+        return Err(fallback_error);
+    };
+    for item in items {
+        append_rollout_item_to_path(rollout_path, item)
+            .await
+            .map_err(|err| ThreadStoreError::Internal {
+                message: format!("failed to append closed live thread item: {err}"),
+            })?;
+    }
+    Ok(())
+}
+
+#[cfg(test)]
+mod tests {
+    use super::*;
+
+    #[tokio::test]
+    async fn missing_rollout_fallback_preserves_thread_not_found() {
+        let thread_id = ThreadId::default();
+        let fallback_error = ThreadStoreError::ThreadNotFound { thread_id };
+
+        let err = append_items_to_fallback_rollout(None, fallback_error, &[])
+            .await
+            .expect_err("missing local fallback must preserve the store error");
+
+        assert!(
+            matches!(err, ThreadStoreError::ThreadNotFound { thread_id: missing } if missing == thread_id)
+        );
+    }
+}
diff --git a/codex-rs/thread-store/src/local/live_writer.rs b/codex-rs/thread-store/src/local/live_writer.rs
index 1457cc9e55..ebed5feb56 100644
--- a/codex-rs/thread-store/src/local/live_writer.rs
+++ b/codex-rs/thread-store/src/local/live_writer.rs
@@ -25,7 +25,7 @@ const ROLLOUT_SIZE_BYTES_METRIC: &str = "codex.rollout.size_bytes";
 pub(super) async fn create_thread(
     store: &LocalThreadStore,
     params: CreateThreadParams,
-) -> ThreadStoreResult<()> {
+) -> ThreadStoreResult<PathBuf> {
     let thread_id = params.thread_id;
     let _live_writer_guard = store.live_writer_locks.lock(thread_id).await;
     let history_mode = params.history_mode;
@@ -36,15 +36,17 @@ pub(super) async fn create_thread(
         None
     };
     let recorder = create_thread::create_thread(store, params).await?;
+    let rollout_path = recorder.rollout_path().to_path_buf();
     store
         .insert_live_recorder(thread_id, recorder, history_mode, writer_lock)
-        .await
+        .await?;
+    Ok(rollout_path)
 }
 
 pub(super) async fn resume_thread(
     store: &LocalThreadStore,
     params: ResumeThreadParams,
-) -> ThreadStoreResult<()> {
+) -> ThreadStoreResult<PathBuf> {
     let _live_writer_guard = store.live_writer_locks.lock(params.thread_id).await;
     store.ensure_live_recorder_absent(params.thread_id).await?;
     let history_mode = if let Some(history) = params.history.as_deref() {
@@ -113,9 +115,11 @@ pub(super) async fn resume_thread(
         .map_err(|err| ThreadStoreError::Internal {
             message: format!("failed to resume local thread recorder: {err}"),
         })?;
+    let rollout_path = recorder.rollout_path().to_path_buf();
     store
         .insert_live_recorder(params.thread_id, recorder, history_mode, writer_lock)
-        .await
+        .await?;
+    Ok(rollout_path)
 }
 
 #[tracing::instrument(
diff --git a/codex-rs/thread-store/src/local/mod.rs b/codex-rs/thread-store/src/local/mod.rs
index 5d59666348..782ef8ebf6 100644
--- a/codex-rs/thread-store/src/local/mod.rs
+++ b/codex-rs/thread-store/src/local/mod.rs
@@ -238,6 +238,34 @@ impl LocalThreadStore {
         live_writer::rollout_path(self, thread_id).await
     }
 
+    // Return the path from the same future that registers the recorder so callers do not add a
+    // cancellation point after opening local persistence.
+    pub(crate) async fn create_thread_with_rollout_path(
+        &self,
+        params: CreateThreadParams,
+    ) -> ThreadStoreResult<PathBuf> {
+        live_writer::create_thread(self, params).await
+    }
+
+    pub(crate) async fn resume_thread_with_rollout_path(
+        &self,
+        params: ResumeThreadParams,
+    ) -> ThreadStoreResult<PathBuf> {
+        live_writer::resume_thread(self, params).await
+    }
+
+    pub(super) async fn live_recorder(
+        &self,
+        thread_id: ThreadId,
+    ) -> ThreadStoreResult<RolloutRecorder> {
+        self.live_recorders
+            .lock()
+            .await
+            .get(&thread_id)
+            .map(|entry| entry.recorder.clone())
+            .ok_or(ThreadStoreError::ThreadNotFound { thread_id })
+    }
+
     pub(super) async fn ensure_live_recorder_absent(
         &self,
         thread_id: ThreadId,
@@ -389,11 +417,19 @@ impl ThreadStore for LocalThreadStore {
     }
 
     fn create_thread(&self, params: CreateThreadParams) -> ThreadStoreFuture<'_, ()> {
-        Box::pin(async move { live_writer::create_thread(self, params).await })
+        Box::pin(async move {
+            self.create_thread_with_rollout_path(params)
+                .await
+                .map(|_| ())
+        })
     }
 
     fn resume_thread(&self, params: ResumeThreadParams) -> ThreadStoreFuture<'_, ()> {
-        Box::pin(async move { live_writer::resume_thread(self, params).await })
+        Box::pin(async move {
+            self.resume_thread_with_rollout_path(params)
+                .await
+                .map(|_| ())
+        })
     }
 
     fn append_items(&self, params: AppendThreadItemsParams) -> ThreadStoreFuture<'_, ()> {
@@ -1137,6 +1173,161 @@ mod tests {
         }
     }
 
+    #[tokio::test]
+    async fn stale_live_thread_append_after_store_discard_appends_to_rollout() {
+        let home = TempDir::new().expect("temp dir");
+        let store = Arc::new(LocalThreadStore::new(
+            test_config(home.path()),
+            /*state_db*/ None,
+        ));
+        let thread_id = ThreadId::default();
+        let live_thread = LiveThread::create(store.clone(), create_thread_params(thread_id))
+            .await
+            .expect("create live thread");
+        let rollout_path = store
+            .live_rollout_path(thread_id)
+            .await
+            .expect("live rollout path");
+
+        live_thread
+            .append_items(&[user_message_item("write before external discard")])
+            .await
+            .expect("append before discard");
+        live_thread.flush().await.expect("flush before discard");
+
+        store
+            .discard_thread(thread_id)
+            .await
+            .expect("discard live thread outside handle");
+
+        live_thread
+            .append_items(&[user_message_item("write after external discard")])
+            .await
+            .expect("stale live thread append should fall back to rollout");
+
+        assert_rollout_contains_message(rollout_path.as_path(), "write before external discard")
+            .await;
+        assert_rollout_contains_message(rollout_path.as_path(), "write after external discard")
+            .await;
+    }
+
+    #[tokio::test]
+    async fn concurrent_stale_live_thread_appends_remain_serialized_in_rollout() {
+        let home = TempDir::new().expect("temp dir");
+        let store = Arc::new(LocalThreadStore::new(
+            test_config(home.path()),
+            /*state_db*/ None,
+        ));
+        let thread_id = ThreadId::default();
+        let live_thread = LiveThread::create(store.clone(), create_thread_params(thread_id))
+            .await
+            .expect("create live thread");
+        let rollout_path = store
+            .live_rollout_path(thread_id)
+            .await
+            .expect("live rollout path");
+        live_thread.persist().await.expect("materialize rollout");
+        store
+            .discard_thread(thread_id)
+            .await
+            .expect("discard live writer outside handle");
+
+        let first_items = [user_message_item("first concurrent fallback")];
+        let second_items = [user_message_item("second concurrent fallback")];
+        let (first, second) = tokio::join!(
+            live_thread.append_items(&first_items),
+            live_thread.append_items(&second_items),
+        );
+        first.expect("first fallback append");
+        second.expect("second fallback append");
+
+        assert_rollout_contains_message(rollout_path.as_path(), "first concurrent fallback").await;
+        assert_rollout_contains_message(rollout_path.as_path(), "second concurrent fallback").await;
+    }
+
+    #[tokio::test]
+    async fn deleted_rollout_is_not_recreated_by_stale_live_thread_fallback() {
+        let home = TempDir::new().expect("temp dir");
+        let store = Arc::new(LocalThreadStore::new(
+            test_config(home.path()),
+            /*state_db*/ None,
+        ));
+        let thread_id = ThreadId::default();
+        let live_thread = LiveThread::create(store.clone(), create_thread_params(thread_id))
+            .await
+            .expect("create live thread");
+        let rollout_path = store
+            .live_rollout_path(thread_id)
+            .await
+            .expect("live rollout path");
+        live_thread.persist().await.expect("materialize rollout");
+        store
+            .delete_thread(DeleteThreadParams { thread_id })
+            .await
+            .expect("delete persisted thread");
+
+        live_thread
+            .append_items(&[user_message_item("must not resurrect")])
+            .await
+            .expect_err("deleted rollout must reject stale fallback append");
+        assert!(!rollout_path.exists());
+    }
+
+    #[tokio::test]
+    async fn stale_resumed_live_thread_append_after_store_discard_appends_to_rollout() {
+        let home = TempDir::new().expect("temp dir");
+        let store = Arc::new(LocalThreadStore::new(
+            test_config(home.path()),
+            /*state_db*/ None,
+        ));
+        let thread_id = ThreadId::default();
+        let live_thread = LiveThread::create(store.clone(), create_thread_params(thread_id))
+            .await
+            .expect("create live thread");
+        let rollout_path = store
+            .live_rollout_path(thread_id)
+            .await
+            .expect("live rollout path");
+
+        live_thread
+            .append_items(&[user_message_item("write before resume")])
+            .await
+            .expect("append before resume");
+        live_thread.shutdown().await.expect("shutdown live thread");
+
+        let resumed_live_thread = LiveThread::resume(
+            store.clone(),
+            ThreadHistoryMode::Legacy,
+            ResumeThreadParams {
+                thread_id,
+                rollout_path: None,
+                history: None,
+                include_archived: true,
+                metadata: thread_metadata(),
+            },
+        )
+        .await
+        .expect("resume live thread");
+        resumed_live_thread
+            .append_items(&[user_message_item("write after resume")])
+            .await
+            .expect("append after resume");
+
+        store
+            .discard_thread(thread_id)
+            .await
+            .expect("discard resumed live thread outside handle");
+        resumed_live_thread
+            .append_items(&[user_message_item("write after resumed discard")])
+            .await
+            .expect("stale resumed live thread append should fall back to rollout");
+
+        assert_rollout_contains_message(rollout_path.as_path(), "write before resume").await;
+        assert_rollout_contains_message(rollout_path.as_path(), "write after resume").await;
+        assert_rollout_contains_message(rollout_path.as_path(), "write after resumed discard")
+            .await;
+    }
+
     #[tokio::test]
     async fn resume_thread_reopens_live_writer_and_appends() {
         let home = TempDir::new().expect("temp dir");
