diff --git a/codex-rs/app-server-client/src/lib.rs b/codex-rs/app-server-client/src/lib.rs
index abc844167b..5a5df392e5 100644
--- a/codex-rs/app-server-client/src/lib.rs
+++ b/codex-rs/app-server-client/src/lib.rs
@@ -1249,6 +1249,7 @@ mod tests {
                 request_id: RequestId::Integer(99),
                 params: codex_app_server_protocol::ThreadReadParams {
                     thread_id: "missing-thread".to_string(),
+                    history_window: None,
                     include_turns: false,
                 },
             })
@@ -1303,6 +1304,7 @@ mod tests {
                     request_id: RequestId::Integer(4),
                     params: codex_app_server_protocol::ThreadReadParams {
                         thread_id: response.thread.id.clone(),
+                        history_window: None,
                         include_turns: false,
                     },
                 },
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadReadParams.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadReadParams.json
index 5fb1bcc17b..f87b256894 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadReadParams.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadReadParams.json
@@ -1,5 +1,15 @@
 {
   "$schema": "http://json-schema.org/draft-07/schema#",
+  "definitions": {
+    "ThreadHistoryWindow": {
+      "description": "Controls which portion of a thread's history is returned when resuming it.",
+      "enum": [
+        "full",
+        "sinceLatestCompaction"
+      ],
+      "type": "string"
+    }
+  },
   "properties": {
     "includeTurns": {
       "description": "When true, include turns and their items from rollout history.",
diff --git a/codex-rs/app-server-protocol/schema/typescript/v2/ThreadReadParams.ts b/codex-rs/app-server-protocol/schema/typescript/v2/ThreadReadParams.ts
index c26e896481..1f59997d4e 100644
--- a/codex-rs/app-server-protocol/schema/typescript/v2/ThreadReadParams.ts
+++ b/codex-rs/app-server-protocol/schema/typescript/v2/ThreadReadParams.ts
@@ -2,8 +2,7 @@
 
 // This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
 
-export type ThreadReadParams = { threadId: string,
-/**
+export type ThreadReadParams = {threadId: string, /**
  * When true, include turns and their items from rollout history.
  */
-includeTurns?: boolean, };
+includeTurns?: boolean};
diff --git a/codex-rs/app-server-protocol/src/protocol/common.rs b/codex-rs/app-server-protocol/src/protocol/common.rs
index 7c7db5cd3a..139afba7be 100644
--- a/codex-rs/app-server-protocol/src/protocol/common.rs
+++ b/codex-rs/app-server-protocol/src/protocol/common.rs
@@ -653,6 +653,7 @@ client_request_definitions! {
     },
     ThreadRead => "thread/read" {
         params: v2::ThreadReadParams,
+        inspect_params: true,
         serialization: thread_id(params.thread_id),
         response: v2::ThreadReadResponse,
     },
diff --git a/codex-rs/app-server-protocol/src/protocol/v2/tests.rs b/codex-rs/app-server-protocol/src/protocol/v2/tests.rs
index 93332e584c..7e96b512c1 100644
--- a/codex-rs/app-server-protocol/src/protocol/v2/tests.rs
+++ b/codex-rs/app-server-protocol/src/protocol/v2/tests.rs
@@ -219,6 +219,38 @@ fn thread_history_window_defaults_to_full_and_serializes_as_camel_case() {
     );
 }
 
+#[test]
+fn thread_read_params_constructor_preserves_wire_shape() {
+    assert_eq!(
+        serde_json::to_value(ThreadReadParams::new("thr_123".to_string(), true)).unwrap(),
+        json!({"threadId": "thr_123", "includeTurns": true})
+    );
+
+    assert_eq!(
+        serde_json::to_value(
+            ThreadReadParams::new("thr_123".to_string(), true)
+                .with_history_window(ThreadHistoryWindow::SinceLatestCompaction)
+        )
+        .unwrap(),
+        json!({
+            "threadId": "thr_123",
+            "includeTurns": true,
+            "historyWindow": "sinceLatestCompaction"
+        })
+    );
+
+    assert_eq!(
+        crate::experimental_api::ExperimentalApi::experimental_reason(
+            &crate::ClientRequest::ThreadRead {
+                request_id: crate::RequestId::Integer(1),
+                params: ThreadReadParams::new("thr_123".to_string(), true)
+                    .with_history_window(ThreadHistoryWindow::SinceLatestCompaction),
+            }
+        ),
+        Some("thread/read.historyWindow")
+    );
+}
+
 #[test]
 fn thread_resume_response_round_trips_initial_turns_page() {
     let response = ThreadResumeResponse {
diff --git a/codex-rs/app-server-protocol/src/protocol/v2/thread.rs b/codex-rs/app-server-protocol/src/protocol/v2/thread.rs
index dbd51c5f3b..5762ff3896 100644
--- a/codex-rs/app-server-protocol/src/protocol/v2/thread.rs
+++ b/codex-rs/app-server-protocol/src/protocol/v2/thread.rs
@@ -1400,16 +1400,36 @@ pub enum ThreadActiveFlag {
     WaitingOnUserInput,
 }
 
-#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, JsonSchema, TS)]
+#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, JsonSchema, TS, ExperimentalApi)]
 #[serde(rename_all = "camelCase")]
 #[ts(export_to = "v2/")]
 pub struct ThreadReadParams {
     pub thread_id: String,
+    /// Controls which portion of persisted thread history is returned when turns are included.
+    #[experimental("thread/read.historyWindow")]
+    #[serde(default, skip_serializing_if = "Option::is_none")]
+    #[ts(optional = nullable)]
+    pub history_window: Option<ThreadHistoryWindow>,
     /// When true, include turns and their items from rollout history.
     #[serde(default, skip_serializing_if = "std::ops::Not::not")]
     pub include_turns: bool,
 }
 
+impl ThreadReadParams {
+    pub fn new(thread_id: String, include_turns: bool) -> Self {
+        Self {
+            thread_id,
+            history_window: None,
+            include_turns,
+        }
+    }
+
+    pub fn with_history_window(mut self, history_window: ThreadHistoryWindow) -> Self {
+        self.history_window = Some(history_window);
+        self
+    }
+}
+
 #[derive(Serialize, Deserialize, Debug, Clone, PartialEq, JsonSchema, TS)]
 #[serde(rename_all = "camelCase")]
 #[ts(export_to = "v2/")]
diff --git a/codex-rs/app-server/src/request_processors/thread_processor.rs b/codex-rs/app-server/src/request_processors/thread_processor.rs
index 7fd973496a..994a7210b3 100644
--- a/codex-rs/app-server/src/request_processors/thread_processor.rs
+++ b/codex-rs/app-server/src/request_processors/thread_processor.rs
@@ -2265,6 +2265,7 @@ impl ThreadRequestProcessor {
     ) -> Result<ThreadReadResponse, JSONRPCErrorError> {
         let ThreadReadParams {
             thread_id,
+            history_window,
             include_turns,
         } = params;
 
@@ -2272,7 +2273,11 @@ impl ThreadRequestProcessor {
             .map_err(|err| invalid_request(format!("invalid thread id: {err}")))?;
 
         let thread = self
-            .read_thread_view(thread_uuid, include_turns)
+            .read_thread_view(
+                thread_uuid,
+                include_turns,
+                history_window.unwrap_or_default(),
+            )
             .await
             .map_err(thread_read_view_error)?;
         Ok(ThreadReadResponse { thread })
@@ -2283,9 +2288,42 @@ impl ThreadRequestProcessor {
         &self,
         thread_id: ThreadId,
         include_turns: bool,
+        history_window: ThreadHistoryWindow,
     ) -> Result<Thread, ThreadReadViewError> {
         let loaded_thread = self.thread_manager.get_thread(thread_id).await.ok();
-        let mut thread = if include_turns {
+        let bounded_turns = include_turns
+            .then_some(history_window)
+            .filter(|window| matches!(window, ThreadHistoryWindow::SinceLatestCompaction));
+        let mut thread = if bounded_turns.is_some() {
+            let persisted_thread = self
+                .load_persisted_thread_for_read(thread_id, /*include_turns*/ false)
+                .await?;
+            let mut thread = if let Some(loaded_thread) = loaded_thread.as_ref() {
+                self.load_live_thread_view(
+                    thread_id,
+                    /*include_turns*/ false,
+                    loaded_thread,
+                    persisted_thread,
+                )
+                .await?
+            } else if let Some(thread) = persisted_thread {
+                thread
+            } else {
+                return Err(ThreadReadViewError::InvalidRequest(format!(
+                    "thread not loaded: {thread_id}"
+                )));
+            };
+            let history = self
+                .thread_store
+                .load_latest_model_context(StoreLoadThreadHistoryParams {
+                    thread_id,
+                    include_archived: true,
+                })
+                .await
+                .map_err(|err| thread_read_history_load_error(thread_id, err))?;
+            thread.turns = build_legacy_api_turns_from_rollout_items(&history.items);
+            thread
+        } else if include_turns {
             if let Some(loaded_thread) = loaded_thread.as_ref() {
                 // Loaded thread with turns: use persisted metadata when it exists,
                 // but reconstruct turns from the live ThreadStore history.
@@ -5072,6 +5110,13 @@ fn thread_read_history_load_error(
     err: ThreadStoreError,
 ) -> ThreadReadViewError {
     match err {
+        ThreadStoreError::InvalidRequest { message }
+            if message == format!("no rollout found for thread id {thread_id}") =>
+        {
+            ThreadReadViewError::InvalidRequest(format!(
+                "thread {thread_id} is not materialized yet; includeTurns is unavailable before first user message"
+            ))
+        }
         ThreadStoreError::InvalidRequest { message }
             if message.starts_with("failed to resolve rollout path `") =>
         {
diff --git a/codex-rs/app-server/tests/suite/v2/external_agent_config.rs b/codex-rs/app-server/tests/suite/v2/external_agent_config.rs
index f2ff5e077b..14ba31f26b 100644
--- a/codex-rs/app-server/tests/suite/v2/external_agent_config.rs
+++ b/codex-rs/app-server/tests/suite/v2/external_agent_config.rs
@@ -432,6 +432,7 @@ source = {:?}
     let request_id = mcp
         .send_thread_read_request(ThreadReadParams {
             thread_id: thread.id.clone(),
+            history_window: None,
             include_turns: true,
         })
         .await?;
@@ -1690,6 +1691,7 @@ async fn external_agent_config_import_creates_session_rollouts() -> Result<()> {
     let request_id = mcp
         .send_thread_read_request(ThreadReadParams {
             thread_id: thread.id.clone(),
+            history_window: None,
             include_turns: true,
         })
         .await?;
@@ -1763,6 +1765,7 @@ async fn external_agent_config_import_creates_session_rollouts() -> Result<()> {
     let request_id = mcp
         .send_thread_read_request(ThreadReadParams {
             thread_id: thread.id,
+            history_window: None,
             include_turns: true,
         })
         .await?;
diff --git a/codex-rs/app-server/tests/suite/v2/thread_fork.rs b/codex-rs/app-server/tests/suite/v2/thread_fork.rs
index fdbcc787ff..0fe222d4b7 100644
--- a/codex-rs/app-server/tests/suite/v2/thread_fork.rs
+++ b/codex-rs/app-server/tests/suite/v2/thread_fork.rs
@@ -965,6 +965,7 @@ async fn thread_fork_can_cut_before_unfinished_stored_turn() -> Result<()> {
     let read_id = mcp
         .send_thread_read_request(ThreadReadParams {
             thread_id: conversation_id.clone(),
+            history_window: None,
             include_turns: true,
         })
         .await?;
diff --git a/codex-rs/app-server/tests/suite/v2/thread_metadata_update.rs b/codex-rs/app-server/tests/suite/v2/thread_metadata_update.rs
index 2a51fb7f7f..ed84d0b19c 100644
--- a/codex-rs/app-server/tests/suite/v2/thread_metadata_update.rs
+++ b/codex-rs/app-server/tests/suite/v2/thread_metadata_update.rs
@@ -275,6 +275,7 @@ async fn thread_metadata_update_patches_git_branch_and_returns_updated_thread()
     let read_id = mcp
         .send_thread_read_request(ThreadReadParams {
             thread_id: thread.id,
+            history_window: None,
             include_turns: false,
         })
         .await?;
@@ -666,6 +667,7 @@ async fn thread_metadata_update_can_clear_stored_git_fields() -> Result<()> {
     let read_id = mcp
         .send_thread_read_request(ThreadReadParams {
             thread_id,
+            history_window: None,
             include_turns: false,
         })
         .await?;
diff --git a/codex-rs/app-server/tests/suite/v2/thread_name_websocket.rs b/codex-rs/app-server/tests/suite/v2/thread_name_websocket.rs
index ce3d09d5e6..bb60c131a9 100644
--- a/codex-rs/app-server/tests/suite/v2/thread_name_websocket.rs
+++ b/codex-rs/app-server/tests/suite/v2/thread_name_websocket.rs
@@ -195,6 +195,7 @@ async fn thread_name_updated_broadcasts_when_name_is_cleared() -> Result<()> {
             /*id*/ 31,
             Some(serde_json::to_value(ThreadReadParams {
                 thread_id: conversation_id.clone(),
+                history_window: None,
                 include_turns: false,
             })?),
         )
diff --git a/codex-rs/app-server/tests/suite/v2/thread_read.rs b/codex-rs/app-server/tests/suite/v2/thread_read.rs
index 65d46f5406..726a26f28d 100644
--- a/codex-rs/app-server/tests/suite/v2/thread_read.rs
+++ b/codex-rs/app-server/tests/suite/v2/thread_read.rs
@@ -21,6 +21,7 @@ use codex_app_server_protocol::SortDirection;
 use codex_app_server_protocol::ThreadForkParams;
 use codex_app_server_protocol::ThreadForkResponse;
 use codex_app_server_protocol::ThreadHistoryMode;
+use codex_app_server_protocol::ThreadHistoryWindow;
 use codex_app_server_protocol::ThreadItem;
 use codex_app_server_protocol::ThreadItemsListParams;
 use codex_app_server_protocol::ThreadItemsListResponse;
@@ -130,6 +131,7 @@ async fn thread_read_returns_summary_without_turns() -> Result<()> {
     let read_id = mcp
         .send_thread_read_request(ThreadReadParams {
             thread_id: conversation_id.clone(),
+            history_window: None,
             include_turns: false,
         })
         .await?;
@@ -184,6 +186,7 @@ async fn thread_read_can_include_turns() -> Result<()> {
     let read_id = mcp
         .send_thread_read_request(ThreadReadParams {
             thread_id: conversation_id.clone(),
+            history_window: None,
             include_turns: true,
         })
         .await?;
@@ -237,6 +240,7 @@ async fn paginated_stored_thread_routes_projected_turns_and_rejects_legacy_histo
     let read_id = mcp
         .send_thread_read_request(ThreadReadParams {
             thread_id: conversation_id.clone(),
+            history_window: None,
             include_turns: false,
         })
         .await?;
@@ -273,6 +277,7 @@ async fn paginated_stored_thread_routes_projected_turns_and_rejects_legacy_histo
     let read_id = mcp
         .send_thread_read_request(ThreadReadParams {
             thread_id: conversation_id.clone(),
+            history_window: None,
             include_turns: true,
         })
         .await?;
@@ -861,7 +866,8 @@ async fn thread_read_loaded_include_turns_reads_store_history_without_rollout_pa
         .request(ClientRequest::ThreadRead {
             request_id: RequestId::Integer(2),
             params: ThreadReadParams {
-                thread_id: thread.id,
+                thread_id: thread.id.clone(),
+                history_window: None,
                 include_turns: true,
             },
         })
@@ -889,6 +895,49 @@ async fn thread_read_loaded_include_turns_reads_store_history_without_rollout_pa
         ]
     );
 
+    let calls_before_bounded = store.calls().await;
+    let result = client
+        .request(ClientRequest::ThreadRead {
+            request_id: RequestId::Integer(3),
+            params: ThreadReadParams::new(thread.id.clone(), true)
+                .with_history_window(ThreadHistoryWindow::SinceLatestCompaction),
+        })
+        .await?
+        .expect("bounded thread/read should succeed");
+    let ThreadReadResponse {
+        thread: bounded_thread,
+        ..
+    } = serde_json::from_value(result)?;
+    let calls_after_bounded = store.calls().await;
+
+    assert_eq!(
+        calls_after_bounded.load_latest_model_context,
+        calls_before_bounded.load_latest_model_context + 1
+    );
+    assert_eq!(
+        calls_after_bounded.read_thread_with_history,
+        calls_before_bounded.read_thread_with_history
+    );
+    assert_eq!(
+        turn_user_texts(&bounded_thread.turns),
+        vec!["history from store"]
+    );
+
+    let result = client
+        .request(ClientRequest::ThreadRead {
+            request_id: RequestId::Integer(4),
+            params: ThreadReadParams::new(thread.id, false)
+                .with_history_window(ThreadHistoryWindow::SinceLatestCompaction),
+        })
+        .await?
+        .expect("metadata-only bounded thread/read should succeed");
+    let ThreadReadResponse { thread, .. } = serde_json::from_value(result)?;
+    assert!(thread.turns.is_empty());
+    assert_eq!(
+        store.calls().await.load_latest_model_context,
+        calls_after_bounded.load_latest_model_context
+    );
+
     client.shutdown().await?;
     Ok(())
 }
@@ -1011,6 +1060,7 @@ async fn thread_read_can_return_archived_threads_by_id() -> Result<()> {
     let read_id = mcp
         .send_thread_read_request(ThreadReadParams {
             thread_id: conversation_id.clone(),
+            history_window: None,
             include_turns: false,
         })
         .await?;
@@ -1197,6 +1247,7 @@ async fn thread_read_returns_forked_from_id_for_forked_threads() -> Result<()> {
     let read_id = mcp
         .send_thread_read_request(ThreadReadParams {
             thread_id: forked.id,
+            history_window: None,
             include_turns: false,
         })
         .await?;
@@ -1236,6 +1287,7 @@ async fn thread_read_loaded_thread_returns_precomputed_path_before_materializati
     let read_id = mcp
         .send_thread_read_request(ThreadReadParams {
             thread_id: thread.id.clone(),
+            history_window: None,
             include_turns: false,
         })
         .await?;
@@ -1296,6 +1348,7 @@ async fn paginated_thread_name_set_is_reflected_in_read_list_and_metadata_resume
     let read_id = mcp
         .send_thread_read_request(ThreadReadParams {
             thread_id: conversation_id.clone(),
+            history_window: None,
             include_turns: false,
         })
         .await?;
@@ -1438,6 +1491,7 @@ async fn thread_read_include_turns_rejects_unmaterialized_loaded_thread() -> Res
     let read_id = mcp
         .send_thread_read_request(ThreadReadParams {
             thread_id: thread.id.clone(),
+            history_window: None,
             include_turns: true,
         })
         .await?;
@@ -1456,6 +1510,27 @@ async fn thread_read_include_turns_rejects_unmaterialized_loaded_thread() -> Res
         read_err.error.message
     );
 
+    let read_id = mcp
+        .send_thread_read_request(
+            ThreadReadParams::new(thread.id, true)
+                .with_history_window(ThreadHistoryWindow::SinceLatestCompaction),
+        )
+        .await?;
+    let read_err: JSONRPCError = timeout(
+        DEFAULT_READ_TIMEOUT,
+        mcp.read_stream_until_error_message(RequestId::Integer(read_id)),
+    )
+    .await??;
+
+    assert!(
+        read_err
+            .error
+            .message
+            .contains("includeTurns is unavailable before first user message"),
+        "unexpected bounded error: {}",
+        read_err.error.message
+    );
+
     Ok(())
 }
 
@@ -2001,6 +2076,7 @@ async fn thread_read_reports_system_error_idle_flag_after_failed_turn() -> Resul
     let read_id = mcp
         .send_thread_read_request(ThreadReadParams {
             thread_id: thread.id,
+            history_window: None,
             include_turns: false,
         })
         .await?;
diff --git a/codex-rs/app-server/tests/suite/v2/thread_resume.rs b/codex-rs/app-server/tests/suite/v2/thread_resume.rs
index 4959ecf8cf..6983fd8445 100644
--- a/codex-rs/app-server/tests/suite/v2/thread_resume.rs
+++ b/codex-rs/app-server/tests/suite/v2/thread_resume.rs
@@ -2735,6 +2735,7 @@ async fn thread_resume_and_read_interrupt_incomplete_rollout_turn_when_thread_is
     let read_id = mcp
         .send_thread_read_request(ThreadReadParams {
             thread_id: resumed_again.id,
+            history_window: None,
             include_turns: true,
         })
         .await?;
@@ -2766,6 +2767,7 @@ async fn thread_resume_defers_updated_at_until_turn_start() -> Result<()> {
     let read_id = mcp
         .send_thread_read_request(ThreadReadParams {
             thread_id: thread_id.clone(),
+            history_window: None,
             include_turns: false,
         })
         .await?;
@@ -2837,6 +2839,7 @@ async fn thread_resume_defers_updated_at_until_turn_start() -> Result<()> {
     let read_id = mcp
         .send_thread_read_request(ThreadReadParams {
             thread_id: thread_id.clone(),
+            history_window: None,
             include_turns: false,
         })
         .await?;
@@ -4121,6 +4124,7 @@ async fn start_materialized_thread_and_restart(
     let read_id = first_mcp
         .send_thread_read_request(ThreadReadParams {
             thread_id: thread.id.clone(),
+            history_window: None,
             include_turns: false,
         })
         .await?;
diff --git a/codex-rs/app-server/tests/suite/v2/thread_settings_update.rs b/codex-rs/app-server/tests/suite/v2/thread_settings_update.rs
index bbab6e3d6c..554557963e 100644
--- a/codex-rs/app-server/tests/suite/v2/thread_settings_update.rs
+++ b/codex-rs/app-server/tests/suite/v2/thread_settings_update.rs
@@ -405,6 +405,7 @@ async fn read_thread_with_turns(
     let request_id = mcp
         .send_thread_read_request(ThreadReadParams {
             thread_id: thread_id.to_string(),
+            history_window: None,
             include_turns: true,
         })
         .await?;
diff --git a/codex-rs/app-server/tests/suite/v2/thread_shell_command.rs b/codex-rs/app-server/tests/suite/v2/thread_shell_command.rs
index 5d9ef748a7..525b0811f7 100644
--- a/codex-rs/app-server/tests/suite/v2/thread_shell_command.rs
+++ b/codex-rs/app-server/tests/suite/v2/thread_shell_command.rs
@@ -121,6 +121,7 @@ async fn thread_shell_command_history_responses_exclude_persisted_command_execut
             request_id,
             params: ThreadReadParams {
                 thread_id: thread.id.clone(),
+                history_window: None,
                 include_turns: true,
             },
         })
@@ -319,6 +320,7 @@ async fn thread_shell_command_uses_existing_active_turn() -> Result<()> {
             request_id,
             params: ThreadReadParams {
                 thread_id: thread.id,
+                history_window: None,
                 include_turns: true,
             },
         })
diff --git a/codex-rs/app-server/tests/suite/v2/thread_unsubscribe.rs b/codex-rs/app-server/tests/suite/v2/thread_unsubscribe.rs
index da456ac820..598297e752 100644
--- a/codex-rs/app-server/tests/suite/v2/thread_unsubscribe.rs
+++ b/codex-rs/app-server/tests/suite/v2/thread_unsubscribe.rs
@@ -284,6 +284,7 @@ async fn thread_unsubscribe_preserves_cached_status_before_idle_unload() -> Resu
             request_id,
             params: ThreadReadParams {
                 thread_id: thread_id.clone(),
+                history_window: None,
                 include_turns: false,
             },
         })
diff --git a/codex-rs/app-server/tests/suite/v2/web_search.rs b/codex-rs/app-server/tests/suite/v2/web_search.rs
index e282c4d781..0e4f3648b8 100644
--- a/codex-rs/app-server/tests/suite/v2/web_search.rs
+++ b/codex-rs/app-server/tests/suite/v2/web_search.rs
@@ -339,6 +339,7 @@ async fn assert_standalone_web_search_round_trips_output(
     let read_req = reloaded_mcp
         .send_thread_read_request(ThreadReadParams {
             thread_id,
+            history_window: None,
             include_turns: true,
         })
         .await?;
diff --git a/codex-rs/exec/src/lib.rs b/codex-rs/exec/src/lib.rs
index b022e6c467..7bbcbb3321 100644
--- a/codex-rs/exec/src/lib.rs
+++ b/codex-rs/exec/src/lib.rs
@@ -1373,6 +1373,7 @@ async fn maybe_backfill_turn_completed_items(
             request_id: request_ids.next(),
             params: ThreadReadParams {
                 thread_id: payload.thread_id.clone(),
+                history_window: None,
                 include_turns: true,
             },
         },
