diff --git a/codex-rs/tui/src/bottom_pane/chat_composer.rs b/codex-rs/tui/src/bottom_pane/chat_composer.rs
index 38bdd6300d..e69797cf27 100644
--- a/codex-rs/tui/src/bottom_pane/chat_composer.rs
+++ b/codex-rs/tui/src/bottom_pane/chat_composer.rs
@@ -255,7 +255,7 @@ use crate::render::Insets;
 use crate::render::RectExt;
 use crate::render::renderable::Renderable;
 use crate::slash_command::SlashCommand;
-use crate::style::user_message_style;
+use crate::style::user_input_style;
 use codex_protocol::ThreadId;
 use codex_protocol::user_input::ByteRange;
 use codex_protocol::user_input::MAX_USER_INPUT_TEXT_CHARS;
@@ -4743,7 +4743,7 @@ impl ChatComposer {
                 }
             }
         }
-        let style = user_message_style();
+        let style = user_input_style();
         Block::default().style(style).render_ref(composer_rect, buf);
         if !remote_images_rect.is_empty() {
             Paragraph::new(self.attachments.remote_image_lines())
diff --git a/codex-rs/tui/src/history_cell/messages.rs b/codex-rs/tui/src/history_cell/messages.rs
index 3b7fc95eee..a2988c1958 100644
--- a/codex-rs/tui/src/history_cell/messages.rs
+++ b/codex-rs/tui/src/history_cell/messages.rs
@@ -139,7 +139,7 @@ impl HistoryCell for UserHistoryCell {
             )
             .max(1);
 
-        let style = user_message_style();
+        let style = user_input_style();
         let element_style = style.fg(Color::Cyan);
 
         let wrapped_remote_images = if self.remote_image_urls.is_empty() {
diff --git a/codex-rs/tui/src/history_cell/mod.rs b/codex-rs/tui/src/history_cell/mod.rs
index d0ce17edc5..98c96b8b82 100644
--- a/codex-rs/tui/src/history_cell/mod.rs
+++ b/codex-rs/tui/src/history_cell/mod.rs
@@ -31,7 +31,7 @@ use crate::render::line_utils::push_owned_lines;
 use crate::render::renderable::Renderable;
 use crate::session_state::ThreadSessionState;
 use crate::style::proposed_plan_style;
-use crate::style::user_message_style;
+use crate::style::user_input_style;
 use crate::terminal_hyperlinks::HyperlinkLine;
 use crate::terminal_hyperlinks::mark_buffer_hyperlinks;
 use crate::terminal_hyperlinks::plain_hyperlink_lines;
diff --git a/codex-rs/tui/src/pager_overlay.rs b/codex-rs/tui/src/pager_overlay.rs
index 23e470cb00..750c624d1c 100644
--- a/codex-rs/tui/src/pager_overlay.rs
+++ b/codex-rs/tui/src/pager_overlay.rs
@@ -28,7 +28,7 @@ use crate::keymap::PagerKeymap;
 use crate::render::Insets;
 use crate::render::renderable::InsetRenderable;
 use crate::render::renderable::Renderable;
-use crate::style::user_message_style;
+use crate::style::user_input_style;
 use crate::terminal_hyperlinks::HyperlinkLine;
 use crate::terminal_hyperlinks::mark_buffer_hyperlinks;
 use crate::terminal_hyperlinks::visible_lines_ref;
@@ -401,9 +401,9 @@ impl Renderable for CellRenderable {
         let hyperlink_lines = self.cell.transcript_hyperlink_lines(area.width);
         let style = if self.cell.as_any().is::<UserHistoryCell>() {
             if self.highlighted {
-                user_message_style().reversed()
+                user_input_style().reversed()
             } else {
-                user_message_style()
+                user_input_style()
             }
         } else {
             Style::default()
diff --git a/codex-rs/tui/src/style.rs b/codex-rs/tui/src/style.rs
index e0ff5bdf2a..a191893595 100644
--- a/codex-rs/tui/src/style.rs
+++ b/codex-rs/tui/src/style.rs
@@ -11,6 +11,7 @@ use ratatui::style::Style;
 use ratatui::style::Stylize;
 
 const LIGHT_BG_ACCENT_RGB: (u8, u8, u8) = (0, 95, 135);
+const FALLBACK_USER_MESSAGE_BG: Color = Color::Indexed(236);
 // Decorative table rules should remain visible without competing with cell content.
 const TABLE_SEPARATOR_FG_ALPHA: f32 = 0.20;
 
@@ -18,6 +19,10 @@ pub fn user_message_style() -> Style {
     user_message_style_for(default_bg())
 }
 
+pub(crate) fn user_input_style() -> Style {
+    user_input_style_for(default_bg())
+}
+
 pub fn proposed_plan_style() -> Style {
     proposed_plan_style_for(default_bg())
 }
@@ -34,9 +39,15 @@ pub(crate) fn accent_style() -> Style {
 
 /// Returns the style for a user-authored message using the provided terminal background.
 pub fn user_message_style_for(terminal_bg: Option<(u8, u8, u8)>) -> Style {
+    terminal_bg
+        .map(|background| Style::default().bg(user_message_bg(background)))
+        .unwrap_or_default()
+}
+
+pub(crate) fn user_input_style_for(terminal_bg: Option<(u8, u8, u8)>) -> Style {
     match terminal_bg {
         Some(bg) => Style::default().bg(user_message_bg(bg)),
-        None => Style::default(),
+        None => Style::default().bg(FALLBACK_USER_MESSAGE_BG),
     }
 }
 
@@ -113,6 +124,22 @@ mod tests {
         assert_eq!(accent_style_for(/*terminal_bg*/ None), expected);
     }
 
+    #[test]
+    fn user_message_style_uses_no_background_when_background_is_unknown() {
+        assert_eq!(
+            user_message_style_for(/*terminal_bg*/ None),
+            Style::default()
+        );
+    }
+
+    #[test]
+    fn user_input_style_uses_gray_fallback_when_background_is_unknown() {
+        assert_eq!(
+            user_input_style_for(/*terminal_bg*/ None),
+            Style::default().bg(FALLBACK_USER_MESSAGE_BG)
+        );
+    }
+
     #[test]
     fn table_separator_blends_toward_dark_background() {
         let style = table_separator_style_for(
