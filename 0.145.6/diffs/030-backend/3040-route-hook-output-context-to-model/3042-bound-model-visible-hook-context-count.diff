diff --git a/codex-rs/hooks/src/events/common.rs b/codex-rs/hooks/src/events/common.rs
index c67602086c..d270b360f9 100644
--- a/codex-rs/hooks/src/events/common.rs
+++ b/codex-rs/hooks/src/events/common.rs
@@ -9,6 +9,8 @@ use crate::engine::ConfiguredHandler;
 use crate::engine::dispatcher;
 use crate::output_spill::AdditionalContext;
 
+const MAX_ADDITIONAL_CONTEXTS_FOR_MODEL: usize = 8;
+
 /// Identifies a thread-spawned subagent when a normal hook runs inside it.
 #[derive(Debug, Clone, PartialEq, Eq)]
 pub struct SubagentHookContext {
@@ -55,6 +57,7 @@ pub(crate) fn flatten_additional_contexts<'a>(
     additional_contexts
         .into_iter()
         .flat_map(|chunk| chunk.iter().cloned())
+        .take(MAX_ADDITIONAL_CONTEXTS_FOR_MODEL)
         .collect()
 }
 
@@ -175,9 +178,33 @@ mod tests {
     use codex_protocol::protocol::HookEventName;
     use pretty_assertions::assert_eq;
 
+    use super::flatten_additional_contexts;
     use super::matcher_pattern_for_event;
     use super::matches_matcher;
     use super::validate_matcher_pattern;
+    use crate::output_spill::AdditionalContext;
+
+    #[test]
+    fn additional_contexts_for_model_are_limited_to_first_eight() {
+        let expected = (1..=8)
+            .map(|index| AdditionalContext {
+                text: format!("context {index}"),
+                limit: Default::default(),
+            })
+            .collect::<Vec<_>>();
+        let chunks = [
+            expected.clone(),
+            vec![AdditionalContext {
+                text: "context 9".to_string(),
+                limit: Default::default(),
+            }],
+        ];
+
+        assert_eq!(
+            flatten_additional_contexts(chunks.iter().map(Vec::as_slice)),
+            expected,
+        );
+    }
 
     #[test]
     fn matcher_omitted_matches_all_occurrences() {
