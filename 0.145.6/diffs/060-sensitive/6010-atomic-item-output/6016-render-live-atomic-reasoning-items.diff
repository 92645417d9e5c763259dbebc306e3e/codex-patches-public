diff --git a/codex-rs/tui/src/app/agent_status_feed_tests.rs b/codex-rs/tui/src/app/agent_status_feed_tests.rs
index a197caeb04..fd136ec082 100644
--- a/codex-rs/tui/src/app/agent_status_feed_tests.rs
+++ b/codex-rs/tui/src/app/agent_status_feed_tests.rs
@@ -73,6 +73,7 @@ fn agent_status_uses_reasoning_summaries_only() {
                 id: "reasoning-with-summary".to_string(),
                 summary: vec!["safe summary".to_string()],
                 content: vec!["hidden raw reasoning".to_string()],
+                reasoning_output_tokens: None,
             },
             thread_id: "thread-child".to_string(),
             turn_id: "turn-1".to_string(),
@@ -85,6 +86,7 @@ fn agent_status_uses_reasoning_summaries_only() {
                 id: "reasoning-without-summary".to_string(),
                 summary: Vec::new(),
                 content: vec!["raw-only reasoning".to_string()],
+                reasoning_output_tokens: None,
             },
             thread_id: "thread-child".to_string(),
             turn_id: "turn-1".to_string(),
diff --git a/codex-rs/tui/src/chatwidget.rs b/codex-rs/tui/src/chatwidget.rs
index 4993bdd198..83d23b0723 100644
--- a/codex-rs/tui/src/chatwidget.rs
+++ b/codex-rs/tui/src/chatwidget.rs
@@ -647,6 +647,8 @@ pub(crate) struct ChatWidget {
     reasoning_header: Option<String>,
     // Preserves reasoning-summary part boundaries for transcript-only recording.
     reasoning_summary_parts: Vec<String>,
+    // Buffers every reasoning item in one model response until its exact usage arrives at the tail.
+    pending_atomic_reasoning_items: Vec<(String, Vec<String>)>,
     status_state: StatusState,
     review: ReviewState,
     // Active hook runs render in a dedicated live cell so they can run alongside tools.
diff --git a/codex-rs/tui/src/chatwidget/constructor.rs b/codex-rs/tui/src/chatwidget/constructor.rs
index 478966ae6a..189bae304d 100644
--- a/codex-rs/tui/src/chatwidget/constructor.rs
+++ b/codex-rs/tui/src/chatwidget/constructor.rs
@@ -187,6 +187,7 @@ impl ChatWidget {
             reasoning_buffer: String::new(),
             reasoning_header: None,
             reasoning_summary_parts: Vec::new(),
+            pending_atomic_reasoning_items: Vec::new(),
             status_state: StatusState::default(),
             review: ReviewState::default(),
             active_hook_cell: None,
diff --git a/codex-rs/tui/src/chatwidget/protocol.rs b/codex-rs/tui/src/chatwidget/protocol.rs
index dfa4cdb256..d4aa44c4f9 100644
--- a/codex-rs/tui/src/chatwidget/protocol.rs
+++ b/codex-rs/tui/src/chatwidget/protocol.rs
@@ -420,7 +420,13 @@ impl ChatWidget {
                         });
                 self.handle_context_compaction_item(duration_ms, render_source);
             }
-            item => self.handle_thread_item(item, notification.turn_id, render_source),
+            item => {
+                if let Some(replay_kind) = replay_kind {
+                    self.replay_thread_item(item, notification.turn_id, replay_kind);
+                } else {
+                    self.handle_thread_item(item, notification.turn_id, render_source);
+                }
+            }
         }
     }
 }
diff --git a/codex-rs/tui/src/chatwidget/replay.rs b/codex-rs/tui/src/chatwidget/replay.rs
index d97ba1c8fa..f04c26d3e1 100644
--- a/codex-rs/tui/src/chatwidget/replay.rs
+++ b/codex-rs/tui/src/chatwidget/replay.rs
@@ -74,7 +74,31 @@ impl ChatWidget {
         turn_id: String,
         replay_kind: ReplayKind,
     ) {
-        self.handle_thread_item(item, turn_id, ThreadItemRenderSource::Replay(replay_kind));
+        match item {
+            ThreadItem::Reasoning {
+                summary, content, ..
+            } => self.replay_reasoning_item(summary, content),
+            item => {
+                self.handle_thread_item(item, turn_id, ThreadItemRenderSource::Replay(replay_kind));
+            }
+        }
+    }
+
+    fn replay_reasoning_item(&mut self, summary: Vec<String>, content: Vec<String>) {
+        let reasoning_parts = summary.into_iter().chain(
+            self.config
+                .show_raw_agent_reasoning
+                .then_some(content)
+                .into_iter()
+                .flatten(),
+        );
+        for (index, delta) in reasoning_parts.enumerate() {
+            if index > 0 {
+                self.on_reasoning_section_break();
+            }
+            self.on_agent_reasoning_delta(delta);
+        }
+        self.on_agent_reasoning_final();
     }
 
     pub(super) fn handle_thread_item(
@@ -132,24 +156,30 @@ impl ChatWidget {
             }
             ThreadItem::Plan { text, .. } => self.on_plan_item_completed(text),
             ThreadItem::Reasoning {
-                summary, content, ..
+                id,
+                summary,
+                content,
+                reasoning_output_tokens,
             } => {
-                if from_replay {
-                    let reasoning_parts = summary.into_iter().chain(
+                debug_assert!(
+                    !from_replay,
+                    "replay reasoning must use replay_reasoning_item"
+                );
+                let reasoning_parts = summary
+                    .into_iter()
+                    .chain(
                         self.config
                             .show_raw_agent_reasoning
                             .then_some(content)
                             .into_iter()
                             .flatten(),
-                    );
-                    for (index, delta) in reasoning_parts.enumerate() {
-                        if index > 0 {
-                            self.on_reasoning_section_break();
-                        }
-                        self.on_agent_reasoning_delta(delta);
-                    }
-                }
-                self.on_agent_reasoning_final();
+                    )
+                    .collect();
+                self.on_agent_reasoning_item_completed(
+                    id,
+                    reasoning_parts,
+                    reasoning_output_tokens,
+                );
             }
             item @ ThreadItem::CommandExecution {
                 status: codex_app_server_protocol::CommandExecutionStatus::InProgress,
diff --git a/codex-rs/tui/src/chatwidget/streaming.rs b/codex-rs/tui/src/chatwidget/streaming.rs
index 3ca4769f97..b02c55aab8 100644
--- a/codex-rs/tui/src/chatwidget/streaming.rs
+++ b/codex-rs/tui/src/chatwidget/streaming.rs
@@ -262,8 +262,25 @@ impl ChatWidget {
             .or_else(|| extract_first_bold(&self.reasoning_buffer));
     }
 
+    pub(super) fn on_agent_reasoning_item_completed(
+        &mut self,
+        reasoning_item_id: String,
+        reasoning_parts: Vec<String>,
+        reasoning_output_tokens: Option<i64>,
+    ) {
+        self.pending_atomic_reasoning_items
+            .push((reasoning_item_id, reasoning_parts));
+        if let Some(reasoning_output_tokens) = reasoning_output_tokens.filter(|tokens| *tokens > 0)
+        {
+            self.flush_pending_atomic_reasoning_response(Some(reasoning_output_tokens));
+        }
+        self.reasoning_buffer.clear();
+        self.reasoning_header = None;
+        self.reasoning_summary_parts.clear();
+        self.request_redraw();
+    }
+
     pub(super) fn on_agent_reasoning_final(&mut self) {
-        // At the end of a reasoning block, record transcript-only content.
         if !self.reasoning_buffer.is_empty() {
             self.reasoning_summary_parts
                 .push(std::mem::take(&mut self.reasoning_buffer));
@@ -279,6 +296,23 @@ impl ChatWidget {
         self.request_redraw();
     }
 
+    pub(super) fn flush_pending_atomic_reasoning_response(
+        &mut self,
+        reasoning_output_tokens: Option<i64>,
+    ) {
+        if self.pending_atomic_reasoning_items.is_empty() {
+            return;
+        }
+        let cells = history_cell::new_atomic_reasoning_response_blocks(
+            std::mem::take(&mut self.pending_atomic_reasoning_items),
+            &self.config.cwd,
+            reasoning_output_tokens,
+        );
+        for cell in cells {
+            self.add_boxed_history(cell);
+        }
+    }
+
     pub(super) fn on_reasoning_section_break(&mut self) {
         // Start a new reasoning block for header extraction and accumulate transcript.
         if !self.reasoning_buffer.is_empty() {
@@ -307,6 +341,7 @@ impl ChatWidget {
         self.reasoning_buffer.clear();
         self.reasoning_header = None;
         self.reasoning_summary_parts.clear();
+        self.pending_atomic_reasoning_items.clear();
         self.status_state.remember_retry_status_header();
         self.bottom_pane.ensure_status_indicator();
         self.status_state.model_output_run_state = ModelOutputRunState::Thinking;
diff --git a/codex-rs/tui/src/chatwidget/tests/helpers.rs b/codex-rs/tui/src/chatwidget/tests/helpers.rs
index 55259bff5c..f3f1644d44 100644
--- a/codex-rs/tui/src/chatwidget/tests/helpers.rs
+++ b/codex-rs/tui/src/chatwidget/tests/helpers.rs
@@ -583,6 +583,7 @@ pub(super) fn handle_agent_reasoning_final(chat: &mut ChatWidget) {
                 id: "reasoning-1".to_string(),
                 summary: Vec::new(),
                 content: Vec::new(),
+                reasoning_output_tokens: Some(1),
             },
         }),
         /*replay_kind*/ None,
diff --git a/codex-rs/tui/src/chatwidget/tests/history_replay.rs b/codex-rs/tui/src/chatwidget/tests/history_replay.rs
index 478c2e6e1a..58ec9f2b89 100644
--- a/codex-rs/tui/src/chatwidget/tests/history_replay.rs
+++ b/codex-rs/tui/src/chatwidget/tests/history_replay.rs
@@ -1084,6 +1084,7 @@ async fn replayed_reasoning_item_preserves_summary_parts_and_hides_raw_reasoning
                 "**Checking tests**\n\n<!-- -->".to_string(),
             ],
             content: vec!["Raw reasoning".to_string()],
+            reasoning_output_tokens: Some(516),
         },
         "turn-1".to_string(),
         ReplayKind::ThreadSnapshot,
@@ -1097,6 +1098,8 @@ async fn replayed_reasoning_item_preserves_summary_parts_and_hides_raw_reasoning
     };
     assert_eq!(rendered, "▪ done\n");
     assert!(!rendered.contains("Raw reasoning"));
+    assert!(!rendered.contains("reasoning-1"));
+    assert!(!rendered.contains("516 tokens"));
 }
 
 #[tokio::test]
@@ -1132,6 +1135,7 @@ async fn replayed_reasoning_item_shows_raw_reasoning_when_enabled() {
             id: "reasoning-1".to_string(),
             summary: vec!["Summary only".to_string()],
             content: vec!["Raw reasoning".to_string()],
+            reasoning_output_tokens: Some(516),
         },
         "turn-1".to_string(),
         ReplayKind::ThreadSnapshot,
@@ -1144,6 +1148,8 @@ async fn replayed_reasoning_item_shows_raw_reasoning_when_enabled() {
         other => panic!("expected InsertHistoryCell, got {other:?}"),
     };
     assert!(rendered.contains("Raw reasoning"));
+    assert!(!rendered.contains("reasoning-1"));
+    assert!(!rendered.contains("516 tokens"));
 }
 
 #[tokio::test]
@@ -1306,6 +1312,7 @@ async fn live_reasoning_summary_is_not_rendered_twice_when_item_completes() {
                 id: "reasoning-1".to_string(),
                 summary: vec!["Summary only".to_string()],
                 content: Vec::new(),
+                reasoning_output_tokens: Some(516),
             },
         }),
         /*replay_kind*/ None,
@@ -1320,6 +1327,84 @@ async fn live_reasoning_summary_is_not_rendered_twice_when_item_completes() {
     assert_eq!(rendered.matches("Summary only").count(), 1);
 }
 
+#[tokio::test]
+async fn live_reasoning_response_waits_for_tail_usage_and_preserves_item_boundaries() {
+    let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+    chat.show_welcome_banner = false;
+
+    for (id, summary, reasoning_output_tokens) in [
+        (
+            "rs_first",
+            vec![
+                "**First head**",
+                "**First detail one**",
+                "**First detail two**",
+            ],
+            None,
+        ),
+        (
+            "rs_second",
+            vec![
+                "**Second head**",
+                "**Second detail one**",
+                "**Second detail two**",
+            ],
+            None,
+        ),
+        (
+            "rs_third",
+            vec![
+                "**Third head**",
+                "**Third detail one**",
+                "**Third detail two**",
+            ],
+            Some(1_402),
+        ),
+    ] {
+        chat.handle_server_notification(
+            ServerNotification::ItemCompleted(ItemCompletedNotification {
+                thread_id: "thread-1".to_string(),
+                turn_id: "turn-1".to_string(),
+                completed_at_ms: 0,
+                item: AppServerThreadItem::Reasoning {
+                    id: id.to_string(),
+                    summary: summary.into_iter().map(str::to_string).collect(),
+                    content: Vec::new(),
+                    reasoning_output_tokens,
+                },
+            }),
+            /*replay_kind*/ None,
+        );
+        if reasoning_output_tokens.is_none() {
+            assert!(drain_insert_history(&mut rx).is_empty());
+        }
+    }
+
+    let rendered = drain_insert_history(&mut rx)
+        .into_iter()
+        .flat_map(|lines| {
+            lines
+                .into_iter()
+                .map(|line| line.to_string())
+                .filter(|line| !line.is_empty())
+        })
+        .collect::<Vec<_>>();
+    assert_eq!(
+        rendered,
+        vec![
+            "▪ First head ▪ first ▪ 1,402 tokens",
+            "  First detail one",
+            "  First detail two",
+            "▪ Second head ▪ second",
+            "  Second detail one",
+            "  Second detail two",
+            "▪ Third head ▪ third",
+            "  Third detail one",
+            "  Third detail two",
+        ]
+    );
+}
+
 #[tokio::test]
 async fn live_reasoning_summary_drops_empty_parts_without_losing_content() {
     let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
@@ -1382,6 +1467,7 @@ async fn live_reasoning_summary_drops_empty_parts_without_losing_content() {
                     "**Checking tests**\n\n<!-- -->".to_string(),
                 ],
                 content: Vec::new(),
+                reasoning_output_tokens: Some(516),
             },
         }),
         /*replay_kind*/ None,
@@ -1393,7 +1479,7 @@ async fn live_reasoning_summary_drops_empty_parts_without_losing_content() {
         }
         other => panic!("expected InsertHistoryCell, got {other:?}"),
     };
-    assert_eq!(rendered, "▪ done\n");
+    assert_eq!(rendered, "▪ Plan ▪ 516 tokens\n  done\n");
 }
 
 #[tokio::test]
diff --git a/codex-rs/tui/src/chatwidget/turn_runtime.rs b/codex-rs/tui/src/chatwidget/turn_runtime.rs
index ba15071f5e..a81aefc893 100644
--- a/codex-rs/tui/src/chatwidget/turn_runtime.rs
+++ b/codex-rs/tui/src/chatwidget/turn_runtime.rs
@@ -99,6 +99,7 @@ impl ChatWidget {
             self.refresh_status_surfaces();
         }
         self.reasoning_summary_parts.clear();
+        self.pending_atomic_reasoning_items.clear();
         self.reasoning_buffer.clear();
         self.reasoning_header = None;
         self.set_ambient_pet_notification(
diff --git a/codex-rs/tui/src/history_cell/messages.rs b/codex-rs/tui/src/history_cell/messages.rs
index cb8b326c1f..b059d35c1b 100644
--- a/codex-rs/tui/src/history_cell/messages.rs
+++ b/codex-rs/tui/src/history_cell/messages.rs
@@ -359,6 +359,129 @@ impl ReasoningSummaryCell {
     }
 }
 
+#[derive(Debug)]
+struct LiveAtomicReasoningCell {
+    header: String,
+    content: String,
+    cwd: PathBuf,
+    item_head: bool,
+    reasoning_item_id: Option<String>,
+    reasoning_output_tokens: Option<i64>,
+}
+
+impl LiveAtomicReasoningCell {
+    fn new(
+        header: String,
+        content: String,
+        cwd: &Path,
+        reasoning_item_id: String,
+        reasoning_output_tokens: Option<i64>,
+        item_head: bool,
+    ) -> Self {
+        Self {
+            header,
+            content,
+            cwd: cwd.to_path_buf(),
+            item_head,
+            reasoning_item_id: reasoning_item_id
+                .strip_prefix("rs_")
+                .filter(|item_id| !item_id.is_empty())
+                .map(ToOwned::to_owned),
+            reasoning_output_tokens,
+        }
+    }
+
+    fn lines(&self, width: u16) -> Vec<Line<'static>> {
+        let mut lines = Vec::new();
+        append_markdown(
+            &self.content,
+            crate::width::usable_content_width_u16(width, /*reserved_cols*/ 2),
+            Some(self.cwd.as_path()),
+            &mut lines,
+        );
+        let summary_style = Style::default().dim().italic();
+        let mut summary_lines = lines
+            .into_iter()
+            .map(|mut line| {
+                line.spans = line
+                    .spans
+                    .into_iter()
+                    .map(|span| span.patch_style(summary_style))
+                    .collect();
+                line
+            })
+            .collect::<Vec<_>>();
+        if !self.header.is_empty() {
+            let header = self
+                .header
+                .strip_prefix("**")
+                .and_then(|header| header.strip_suffix("**"))
+                .unwrap_or(self.header.as_str())
+                .to_string();
+            let header_style = if header == "No reasoning summary" {
+                Style::default().fg(Color::DarkGray)
+            } else {
+                summary_style.add_modifier(Modifier::BOLD)
+            };
+            summary_lines.insert(0, Line::from(Span::styled(header, header_style)));
+        }
+        let mut metadata = Vec::new();
+        if let Some(item_id) = self.reasoning_item_id.as_deref() {
+            metadata.push(Span::styled(
+                item_id.to_string(),
+                Style::default().fg(Color::DarkGray),
+            ));
+        }
+        if let Some(tokens) = self.reasoning_output_tokens {
+            if !metadata.is_empty() {
+                metadata.push(Span::styled(" ▪ ", Style::default().dim()));
+            }
+            metadata.push(Span::styled(
+                format!(
+                    "{} tokens",
+                    codex_protocol::num_format::format_with_separators(tokens)
+                ),
+                Style::default().fg(Color::Yellow),
+            ));
+        }
+        if !metadata.is_empty() {
+            if let Some(first_line) = summary_lines.first_mut() {
+                first_line
+                    .spans
+                    .push(Span::styled(" ▪ ", Style::default().dim()));
+                first_line.spans.extend(metadata);
+            } else {
+                summary_lines.push(Line::from(metadata));
+            }
+        }
+
+        adaptive_wrap_lines(
+            &summary_lines,
+            RtOptions::new(width as usize)
+                .initial_indent(if self.item_head {
+                    "▪ ".dim().into()
+                } else {
+                    "  ".into()
+                })
+                .subsequent_indent("  ".into()),
+        )
+    }
+}
+
+impl HistoryCell for LiveAtomicReasoningCell {
+    fn display_lines(&self, width: u16) -> Vec<Line<'static>> {
+        self.lines(width)
+    }
+
+    fn transcript_lines(&self, width: u16) -> Vec<Line<'static>> {
+        self.lines(width)
+    }
+
+    fn raw_lines(&self) -> Vec<Line<'static>> {
+        raw_lines_from_source(self.content.trim())
+    }
+}
+
 impl HistoryCell for ReasoningSummaryCell {
     fn display_lines(&self, width: u16) -> Vec<Line<'static>> {
         if self.transcript_only {
@@ -780,6 +903,83 @@ pub(crate) fn new_reasoning_summary_block(
     ))
 }
 
+pub(crate) fn new_atomic_reasoning_summary_blocks(
+    reasoning_parts: Vec<String>,
+    cwd: &Path,
+    reasoning_item_id: String,
+    reasoning_output_tokens: Option<i64>,
+) -> Vec<Box<dyn HistoryCell>> {
+    new_atomic_reasoning_response_blocks(
+        vec![(reasoning_item_id, reasoning_parts)],
+        cwd,
+        reasoning_output_tokens,
+    )
+}
+
+pub(crate) fn new_atomic_reasoning_response_blocks(
+    reasoning_items: Vec<(String, Vec<String>)>,
+    cwd: &Path,
+    reasoning_output_tokens: Option<i64>,
+) -> Vec<Box<dyn HistoryCell>> {
+    let mut response_usage = reasoning_output_tokens;
+    let mut cells: Vec<Box<dyn HistoryCell>> = Vec::new();
+    for (reasoning_item_id, reasoning_parts) in reasoning_items {
+        let mut blocks = split_atomic_reasoning_summary_parts(&reasoning_parts);
+        if blocks.is_empty() {
+            blocks.push(("No reasoning summary".to_string(), String::new()));
+        }
+        for (index, (header, content)) in blocks.into_iter().enumerate() {
+            let atomic_head = index == 0;
+            cells.push(Box::new(LiveAtomicReasoningCell::new(
+                header,
+                content,
+                cwd,
+                atomic_head
+                    .then(|| reasoning_item_id.clone())
+                    .unwrap_or_default(),
+                atomic_head.then(|| response_usage.take()).flatten(),
+                atomic_head,
+            )));
+        }
+    }
+    cells
+}
+
+fn split_atomic_reasoning_summary_parts(reasoning_parts: &[String]) -> Vec<(String, String)> {
+    let mut leading_empty_part_header = None;
+    let mut blocks = Vec::with_capacity(reasoning_parts.len());
+
+    for part in reasoning_parts {
+        let part = part.trim();
+        if part.is_empty() {
+            continue;
+        }
+        let header_end = part.strip_prefix("**").and_then(|after_open| {
+            after_open
+                .find("**")
+                .and_then(|close| (close > 0).then_some(close + 4))
+        });
+        let body = header_end.map_or(part, |header_end| &part[header_end..]);
+        if body.trim() == "<!-- -->" {
+            if blocks.is_empty()
+                && leading_empty_part_header.is_none()
+                && let Some(header_end) = header_end
+            {
+                leading_empty_part_header = Some(part[..header_end].to_string());
+            }
+            continue;
+        }
+        blocks.push(split_reasoning_summary_parts(&[part.to_string()]));
+    }
+
+    if blocks.is_empty()
+        && let Some(header) = leading_empty_part_header
+    {
+        blocks.push((header, String::new()));
+    }
+    blocks
+}
+
 /// Split structured reasoning-summary parts into the status header and renderable content.
 pub(crate) fn split_reasoning_summary_parts(reasoning_parts: &[String]) -> (String, String) {
     let mut leading_empty_part_header = None;
diff --git a/codex-rs/tui/src/history_cell/tests.rs b/codex-rs/tui/src/history_cell/tests.rs
index 62d9f72fd3..cfe7be9bd9 100644
--- a/codex-rs/tui/src/history_cell/tests.rs
+++ b/codex-rs/tui/src/history_cell/tests.rs
@@ -2494,6 +2494,199 @@ fn reasoning_summary_block() {
     assert_eq!(rendered_transcript, vec!["▪ Detailed reasoning goes here."]);
 }
 
+#[test]
+fn atomic_reasoning_item_renders_id_and_exact_usage_without_summary() {
+    let mut cells = new_atomic_reasoning_summary_blocks(
+        Vec::new(),
+        &test_cwd(),
+        "rs_abc123".to_string(),
+        Some(516),
+    );
+    let cell = cells.remove(0);
+
+    let lines = cell.transcript_lines(/*width*/ 80);
+    assert_eq!(
+        render_lines(&lines),
+        vec!["▪ No reasoning summary ▪ abc123 ▪ 516 tokens"]
+    );
+    assert_eq!(
+        lines[0]
+            .spans
+            .iter()
+            .find(|span| span.content == "No reasoning summary")
+            .and_then(|span| span.style.fg),
+        Some(Color::DarkGray)
+    );
+    assert_eq!(
+        lines[0]
+            .spans
+            .iter()
+            .find(|span| span.content == "abc123")
+            .and_then(|span| span.style.fg),
+        Some(Color::DarkGray)
+    );
+    assert_eq!(
+        lines[0]
+            .spans
+            .iter()
+            .find(|span| span.content == "516 tokens")
+            .and_then(|span| span.style.fg),
+        Some(Color::Yellow)
+    );
+    assert!(
+        lines[0]
+            .spans
+            .iter()
+            .filter(|span| span.content == " ▪ ")
+            .all(|span| span.style.add_modifier.contains(Modifier::DIM))
+    );
+}
+
+#[test]
+fn atomic_reasoning_item_without_item_owned_usage_renders_id_only() {
+    let mut cells = new_atomic_reasoning_summary_blocks(
+        vec!["**First item**\n\nPartial summary.".to_string()],
+        &test_cwd(),
+        "rs_first".to_string(),
+        None,
+    );
+    let cell = cells.remove(0);
+
+    let lines = cell.transcript_lines(/*width*/ 80);
+    assert_eq!(
+        render_lines(&lines),
+        vec!["▪ First item ▪ first", "  Partial summary."]
+    );
+    assert_eq!(
+        lines[0].spans.last().and_then(|span| span.style.fg),
+        Some(Color::DarkGray)
+    );
+}
+
+#[test]
+fn atomic_reasoning_item_appends_metadata_to_visible_summary() {
+    let mut cells = new_atomic_reasoning_summary_blocks(
+        vec!["**Planning**\n\nInspect the response.".to_string()],
+        &test_cwd(),
+        "rs_abc123".to_string(),
+        Some(1_024),
+    );
+    let cell = cells.remove(0);
+
+    let lines = cell.transcript_lines(/*width*/ 120);
+    assert_eq!(
+        render_lines(&lines),
+        vec![
+            "▪ Planning ▪ abc123 ▪ 1,024 tokens",
+            "  Inspect the response."
+        ]
+    );
+    let span_count = lines[0].spans.len();
+    assert_eq!(
+        lines[0].spans[span_count - 3].style.fg,
+        Some(Color::DarkGray)
+    );
+    assert_eq!(lines[0].spans[span_count - 1].style.fg, Some(Color::Yellow));
+}
+
+#[test]
+fn atomic_reasoning_item_hides_synthetic_id_without_hiding_exact_usage() {
+    let mut cells = new_atomic_reasoning_summary_blocks(
+        vec!["**Recovered item**".to_string()],
+        &test_cwd(),
+        "item-42".to_string(),
+        Some(37),
+    );
+    let cell = cells.remove(0);
+
+    let lines = cell.transcript_lines(/*width*/ 80);
+    assert_eq!(render_lines(&lines), vec!["▪ Recovered item ▪ 37 tokens"]);
+    assert_eq!(
+        lines[0].spans.last().and_then(|span| span.style.fg),
+        Some(Color::Yellow)
+    );
+}
+
+#[test]
+fn atomic_reasoning_item_preserves_independent_summary_part_boundaries() {
+    let cells = new_atomic_reasoning_summary_blocks(
+        vec![
+            "**Planning focused testing and formatting**".to_string(),
+            "**Configuring test environment and filtering**".to_string(),
+        ],
+        &test_cwd(),
+        "rs_abc123".to_string(),
+        Some(173),
+    );
+
+    assert_eq!(cells.len(), 2);
+    assert_eq!(
+        render_transcript(cells[0].as_ref()),
+        vec!["▪ Planning focused testing and formatting ▪ abc123 ▪ 173 tokens"]
+    );
+    assert_eq!(
+        render_transcript(cells[1].as_ref()),
+        vec!["  Configuring test environment and filtering"]
+    );
+}
+
+#[test]
+fn atomic_reasoning_response_groups_parts_under_three_item_identities() {
+    let cells = new_atomic_reasoning_response_blocks(
+        vec![
+            (
+                "rs_first".to_string(),
+                vec![
+                    "**Distinguishing source changes from installed binary**".to_string(),
+                    "**Reviewing current improvements before committing**".to_string(),
+                    "**Preparing concise commentary for observation**".to_string(),
+                ],
+            ),
+            (
+                "rs_second".to_string(),
+                vec![
+                    "**Planning chain grouping implementation**".to_string(),
+                    "**Designing buffering and flushing logic**".to_string(),
+                    "**Defining PendingAtomicReasoningItem structure**".to_string(),
+                ],
+            ),
+            (
+                "rs_third".to_string(),
+                vec![
+                    "**Designing atomic reasoning response builder**".to_string(),
+                    "**Assessing replay preorganization scope**".to_string(),
+                    "**Outlining replay grouping plan**".to_string(),
+                ],
+            ),
+        ],
+        &test_cwd(),
+        Some(1_402),
+    );
+
+    let rendered = cells
+        .iter()
+        .flat_map(|cell| render_transcript(cell.as_ref()))
+        .collect::<Vec<_>>();
+    assert_eq!(
+        rendered,
+        vec![
+            "▪ Distinguishing source changes from installed binary ▪ first ▪ 1,402 tokens",
+            "  Reviewing current improvements before committing",
+            "  Preparing concise commentary for observation",
+            "▪ Planning chain grouping implementation ▪ second",
+            "  Designing buffering and flushing logic",
+            "  Defining PendingAtomicReasoningItem structure",
+            "▪ Designing atomic reasoning response builder ▪ third",
+            "  Assessing replay preorganization scope",
+            "  Outlining replay grouping plan",
+        ]
+    );
+    assert_eq!(
+        rendered.iter().filter(|line| line.starts_with('▪')).count(),
+        3
+    );
+}
+
 #[test]
 fn reasoning_summary_height_matches_wrapped_rendering_for_url_like_content() {
     let summary = "example.test/api/v1/projects/alpha-team/releases/2026-02-17/builds/1234567890/artifacts/reports/performance/summary/detail/with/a/very/long/path/that/keeps/going";
diff --git a/codex-rs/tui/src/resume_picker.rs b/codex-rs/tui/src/resume_picker.rs
index 1c0d6f3166..b10241fe5b 100644
--- a/codex-rs/tui/src/resume_picker.rs
+++ b/codex-rs/tui/src/resume_picker.rs
@@ -6404,9 +6404,10 @@ session_picker_view = "dense"
                 id: String::from("turn-1"),
                 items_view: codex_app_server_protocol::TurnItemsView::Full,
                 items: vec![ThreadItem::Reasoning {
-                    id: String::from("reasoning-1"),
-                    summary: Vec::new(),
+                    id: String::from("rs_abc123"),
+                    summary: vec![String::from("public summary")],
                     content: vec![String::from("private raw chain of thought")],
+                    reasoning_output_tokens: Some(516),
                 }],
                 status: codex_app_server_protocol::TurnStatus::Completed,
                 error: None,
@@ -6438,6 +6439,9 @@ session_picker_view = "dense"
         .join("\n");
 
         assert!(!hidden.contains("private raw chain of thought"));
+        assert!(hidden.contains("public summary"));
+        assert!(!hidden.contains("abc123"));
+        assert!(!hidden.contains("516 tokens"));
         assert!(visible.contains("private raw chain of thought"));
     }
 
@@ -6478,6 +6482,7 @@ session_picker_view = "dense"
                     id: String::from("reasoning-1"),
                     summary: vec![String::from("public summary")],
                     content: vec![String::from("raw reasoning content")],
+                    reasoning_output_tokens: None,
                 }],
                 status: codex_app_server_protocol::TurnStatus::Completed,
                 error: None,
