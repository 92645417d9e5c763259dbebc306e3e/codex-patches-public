diff --git a/codex-rs/code-mode-protocol/src/description.rs b/codex-rs/code-mode-protocol/src/description.rs
index 2452c635e9..d2d3e1947e 100644
--- a/codex-rs/code-mode-protocol/src/description.rs
+++ b/codex-rs/code-mode-protocol/src/description.rs
@@ -48,7 +48,7 @@ for (const result of results) {
 - `yield_control()`: yields the accumulated output to the model immediately while the script keeps running."#;
 const WAIT_DESCRIPTION_TEMPLATE: &str = r#"- Use `wait` only after `exec` returns `Script running with cell ID ...`.
 - `cell_id` identifies the running `exec` cell to resume.
-- `yield_time_ms` controls how long to wait for more output before yielding again. Defaults to 10000 ms.
+- `yield_time_ms` controls how long to wait for more output before yielding again. Defaults to 60000 ms and has a 60000 ms minimum. Choose a duration that fits the task; use at least 300000 ms for long builds or test runs.
 - `max_tokens` limits how much new output this wait call returns. Defaults to 10000 tokens.
 - `terminate: true` stops the running cell; false or omitted waits for output.
 - `wait` returns only the new output since the last yield, or the final completion or termination result for that cell.
diff --git a/codex-rs/code-mode-protocol/src/lib.rs b/codex-rs/code-mode-protocol/src/lib.rs
index bb47b1a4c4..8a33e5a130 100644
--- a/codex-rs/code-mode-protocol/src/lib.rs
+++ b/codex-rs/code-mode-protocol/src/lib.rs
@@ -27,6 +27,7 @@ pub use runtime::DEFAULT_MAX_OUTPUT_TOKENS_PER_EXEC_CALL;
 pub use runtime::DEFAULT_WAIT_YIELD_TIME_MS;
 pub use runtime::ExecuteRequest;
 pub use runtime::ExecuteToPendingOutcome;
+pub use runtime::MIN_WAIT_YIELD_TIME_MS;
 pub use runtime::RuntimeResponse;
 pub use runtime::WaitOutcome;
 pub use runtime::WaitRequest;
diff --git a/codex-rs/code-mode-protocol/src/runtime.rs b/codex-rs/code-mode-protocol/src/runtime.rs
index 147822063f..69e7d3ad25 100644
--- a/codex-rs/code-mode-protocol/src/runtime.rs
+++ b/codex-rs/code-mode-protocol/src/runtime.rs
@@ -9,7 +9,8 @@ use crate::FunctionCallOutputContentItem;
 use crate::ToolDefinition;
 
 pub const DEFAULT_EXEC_YIELD_TIME_MS: u64 = 10_000;
-pub const DEFAULT_WAIT_YIELD_TIME_MS: u64 = 10_000;
+pub const DEFAULT_WAIT_YIELD_TIME_MS: u64 = 60_000;
+pub const MIN_WAIT_YIELD_TIME_MS: u64 = 60_000;
 pub const DEFAULT_MAX_OUTPUT_TOKENS_PER_EXEC_CALL: usize = 10_000;
 
 #[derive(Clone, Debug, Deserialize, PartialEq, Serialize)]
diff --git a/codex-rs/core/src/tools/code_mode/wait_handler.rs b/codex-rs/core/src/tools/code_mode/wait_handler.rs
index c3c867f3ef..3237ffd899 100644
--- a/codex-rs/core/src/tools/code_mode/wait_handler.rs
+++ b/codex-rs/core/src/tools/code_mode/wait_handler.rs
@@ -35,6 +35,10 @@ fn default_wait_yield_time_ms() -> u64 {
     DEFAULT_WAIT_YIELD_TIME_MS
 }
 
+fn clamp_wait_yield_time_ms(yield_time_ms: u64) -> u64 {
+    yield_time_ms.max(codex_code_mode::MIN_WAIT_YIELD_TIME_MS)
+}
+
 fn parse_arguments<T>(arguments: &str) -> Result<T, FunctionCallError>
 where
     T: for<'de> Deserialize<'de>,
@@ -91,7 +95,7 @@ impl CodeModeWaitHandler {
                         .code_mode_service
                         .wait(codex_code_mode::WaitRequest {
                             cell_id,
-                            yield_time_ms: args.yield_time_ms,
+                            yield_time_ms: clamp_wait_yield_time_ms(args.yield_time_ms),
                         })
                         .await
                 }
@@ -133,6 +137,17 @@ impl CodeModeWaitHandler {
     }
 }
 
+#[cfg(test)]
+mod tests {
+    use super::*;
+
+    #[test]
+    fn wait_yield_time_has_one_minute_floor() {
+        assert_eq!(clamp_wait_yield_time_ms(1), 60_000);
+        assert_eq!(clamp_wait_yield_time_ms(300_000), 300_000);
+    }
+}
+
 impl CoreToolRuntime for CodeModeWaitHandler {
     fn pre_tool_use_payload(&self, _invocation: &ToolInvocation) -> Option<PreToolUsePayload> {
         // Code-mode `wait` is runtime control for an existing code cell, not a
diff --git a/codex-rs/core/src/tools/code_mode/wait_spec.rs b/codex-rs/core/src/tools/code_mode/wait_spec.rs
index 72bb09084c..fd45ddb4e5 100644
--- a/codex-rs/core/src/tools/code_mode/wait_spec.rs
+++ b/codex-rs/core/src/tools/code_mode/wait_spec.rs
@@ -12,7 +12,7 @@ pub(crate) fn create_wait_tool() -> ToolSpec {
         (
             "yield_time_ms".to_string(),
             JsonSchema::number(Some(
-                "Wait before yielding more output. Defaults to 10000 ms.".to_string(),
+                "Wait before yielding more output. Defaults to 60000 ms and has a 60000 ms minimum. Choose a duration that fits the task; use at least 300000 ms for long builds or test runs.".to_string(),
             )),
         ),
         (
@@ -90,7 +90,7 @@ mod tests {
                         (
                             "yield_time_ms".to_string(),
                             JsonSchema::number(Some(
-                                "Wait before yielding more output. Defaults to 10000 ms."
+                                "Wait before yielding more output. Defaults to 60000 ms and has a 60000 ms minimum. Choose a duration that fits the task; use at least 300000 ms for long builds or test runs."
                                     .to_string(),
                             )),
                         ),
diff --git a/codex-rs/core/src/tools/handlers/shell_spec.rs b/codex-rs/core/src/tools/handlers/shell_spec.rs
index 8ef9af9083..fde811f9c6 100644
--- a/codex-rs/core/src/tools/handlers/shell_spec.rs
+++ b/codex-rs/core/src/tools/handlers/shell_spec.rs
@@ -127,7 +127,7 @@ pub fn create_write_stdin_tool() -> ToolSpec {
         (
             "yield_time_ms".to_string(),
             JsonSchema::number(Some(
-                "Wait before yielding output. Non-empty writes default to 250 ms and cap at 30000 ms; empty polls wait 5000-300000 ms by default.".to_string(),
+                "Wait before yielding output. Non-empty writes default to 250 ms and cap at 30000 ms. Empty polls default to 60000 ms, with a 60000 ms minimum and 300000 ms maximum; choose a duration that fits the task, and use at least 300000 ms for long builds or test runs.".to_string(),
             )),
         ),
         (
diff --git a/codex-rs/core/src/tools/handlers/shell_spec_tests.rs b/codex-rs/core/src/tools/handlers/shell_spec_tests.rs
index 441d93a89d..c6d9d4d8c8 100644
--- a/codex-rs/core/src/tools/handlers/shell_spec_tests.rs
+++ b/codex-rs/core/src/tools/handlers/shell_spec_tests.rs
@@ -133,7 +133,7 @@ fn write_stdin_tool_matches_expected_spec() {
         (
             "yield_time_ms".to_string(),
             JsonSchema::number(Some(
-                "Wait before yielding output. Non-empty writes default to 250 ms and cap at 30000 ms; empty polls wait 5000-300000 ms by default.".to_string(),
+            "Wait before yielding output. Non-empty writes default to 250 ms and cap at 30000 ms. Empty polls default to 60000 ms, with a 60000 ms minimum and 300000 ms maximum; choose a duration that fits the task, and use at least 300000 ms for long builds or test runs.".to_string(),
             )),
         ),
         (
diff --git a/codex-rs/core/src/tools/handlers/unified_exec.rs b/codex-rs/core/src/tools/handlers/unified_exec.rs
index 71e776dbf0..dbe81c8a0f 100644
--- a/codex-rs/core/src/tools/handlers/unified_exec.rs
+++ b/codex-rs/core/src/tools/handlers/unified_exec.rs
@@ -62,7 +62,7 @@ fn default_exec_yield_time_ms() -> u64 {
 }
 
 fn default_write_stdin_yield_time_ms() -> u64 {
-    250
+    60_000
 }
 
 fn default_tty() -> bool {
diff --git a/codex-rs/core/src/tools/handlers/unified_exec/write_stdin.rs b/codex-rs/core/src/tools/handlers/unified_exec/write_stdin.rs
index 7f1709b045..a57466000a 100644
--- a/codex-rs/core/src/tools/handlers/unified_exec/write_stdin.rs
+++ b/codex-rs/core/src/tools/handlers/unified_exec/write_stdin.rs
@@ -115,3 +115,16 @@ impl CoreToolRuntime for WriteStdinHandler {
         post_unified_exec_tool_use_payload(invocation, result)
     }
 }
+
+#[cfg(test)]
+mod tests {
+    use super::*;
+
+    #[test]
+    fn defaults_empty_polls_to_one_minute() -> anyhow::Result<()> {
+        let args: WriteStdinArgs = parse_arguments(r#"{"session_id": 7}"#)?;
+        assert_eq!(args.yield_time_ms, 60_000);
+        assert!(args.chars.is_empty());
+        Ok(())
+    }
+}
diff --git a/codex-rs/core/src/unified_exec/mod.rs b/codex-rs/core/src/unified_exec/mod.rs
index e8dd31c2ce..27c690f00a 100644
--- a/codex-rs/core/src/unified_exec/mod.rs
+++ b/codex-rs/core/src/unified_exec/mod.rs
@@ -64,7 +64,7 @@ pub(crate) use process::UnifiedExecProcess;
 pub(crate) const MIN_YIELD_TIME_MS: u64 = 250;
 pub(crate) const WINDOWS_INITIAL_EXEC_YIELD_TIME_FLOOR_MS: u64 = 2_000;
 // Minimum yield time for an empty `write_stdin`.
-pub(crate) const MIN_EMPTY_YIELD_TIME_MS: u64 = 5_000;
+pub(crate) const MIN_EMPTY_YIELD_TIME_MS: u64 = 60_000;
 pub(crate) const MAX_YIELD_TIME_MS: u64 = 30_000;
 pub(crate) const DEFAULT_MAX_BACKGROUND_TERMINAL_TIMEOUT_MS: u64 = 300_000;
 pub(crate) const DEFAULT_MAX_OUTPUT_TOKENS: usize = 10_000;
