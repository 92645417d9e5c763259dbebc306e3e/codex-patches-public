diff --git a/codex-rs/tui/src/app/agent_navigation.rs b/codex-rs/tui/src/app/agent_navigation.rs
index 00d4b966f7..8c917dbf07 100644
--- a/codex-rs/tui/src/app/agent_navigation.rs
+++ b/codex-rs/tui/src/app/agent_navigation.rs
@@ -276,38 +276,33 @@ impl AgentNavigationState {
         primary_thread_id: Option<ThreadId>,
         direction: AgentNavigationDirection,
     ) -> Option<ThreadId> {
-        let ordered_threads = self
-            .ordered_threads()
-            .into_iter()
-            .filter(|(thread_id, entry)| {
-                Some(*thread_id) == primary_thread_id
-                    || (entry.is_running
-                        && !entry.is_closed
-                        && entry
-                            .agent_path
-                            .as_deref()
-                            .is_some_and(|path| !path.trim().is_empty()))
-            })
-            .collect::<Vec<_>>();
-        if ordered_threads.len() < 2 {
-            return None;
-        }
-
+        let ordered_threads = self.ordered_threads();
         let current_thread_id = current_displayed_thread_id?;
         let current_idx = ordered_threads
             .iter()
             .position(|(thread_id, _)| *thread_id == current_thread_id)?;
-        let next_idx = match direction {
-            AgentNavigationDirection::Next => (current_idx + 1) % ordered_threads.len(),
-            AgentNavigationDirection::Previous => {
-                if current_idx == 0 {
-                    ordered_threads.len() - 1
-                } else {
-                    current_idx - 1
+        let is_navigation_target = |thread_id: ThreadId, entry: &AgentPickerThreadEntry| {
+            Some(thread_id) == primary_thread_id
+                || (entry.is_running
+                    && !entry.is_closed
+                    && entry
+                        .agent_path
+                        .as_deref()
+                        .is_some_and(|path| !path.trim().is_empty()))
+        };
+        for offset in 1..ordered_threads.len() {
+            let candidate_idx = match direction {
+                AgentNavigationDirection::Next => (current_idx + offset) % ordered_threads.len(),
+                AgentNavigationDirection::Previous => {
+                    (current_idx + ordered_threads.len() - offset) % ordered_threads.len()
                 }
+            };
+            let (candidate_id, candidate) = ordered_threads[candidate_idx];
+            if is_navigation_target(candidate_id, candidate) {
+                return Some(candidate_id);
             }
-        };
-        Some(ordered_threads[next_idx].0)
+        }
+        None
     }
 
     /// Derives the contextual footer label for the currently displayed thread.
@@ -327,30 +322,40 @@ impl AgentNavigationState {
 
         let thread_id = current_displayed_thread_id?;
         let is_primary = primary_thread_id == Some(thread_id);
-        Some(
-            self.threads
-                .get(&thread_id)
-                .map(|entry| {
-                    if !is_primary
-                        && let Some(agent_path) = entry
+        let mut label = self
+            .threads
+            .get(&thread_id)
+            .map(|entry| {
+                format_agent_picker_item_name(
+                    entry.agent_nickname.as_deref(),
+                    entry.agent_role.as_deref(),
+                    is_primary,
+                )
+            })
+            .unwrap_or_else(|| {
+                format_agent_picker_item_name(
+                    /*agent_nickname*/ None, /*agent_role*/ None, is_primary,
+                )
+            });
+        if is_primary {
+            let running_subagents = self
+                .threads
+                .iter()
+                .filter(|(candidate_id, entry)| {
+                    Some(**candidate_id) != primary_thread_id
+                        && entry.is_running
+                        && !entry.is_closed
+                        && entry
                             .agent_path
                             .as_deref()
-                            .filter(|agent_path| !agent_path.trim().is_empty())
-                    {
-                        return format!("`{agent_path}`");
-                    }
-                    format_agent_picker_item_name(
-                        entry.agent_nickname.as_deref(),
-                        entry.agent_role.as_deref(),
-                        is_primary,
-                    )
+                            .is_some_and(|path| !path.trim().is_empty())
                 })
-                .unwrap_or_else(|| {
-                    format_agent_picker_item_name(
-                        /*agent_nickname*/ None, /*agent_role*/ None, is_primary,
-                    )
-                }),
-        )
+                .count();
+            if running_subagents > 0 {
+                label.push_str(&format!(" +{running_subagents}"));
+            }
+        }
+        Some(label)
     }
 
     /// Builds the `/agent` picker subtitle from the same canonical bindings used by key handling.
@@ -480,6 +485,30 @@ mod tests {
         );
     }
 
+    #[test]
+    fn active_agent_label_uses_nickname_and_counts_only_running_v2_children() {
+        let (mut state, main_thread_id, first_agent_id, second_agent_id) = populated_state();
+        state.set_agent_path(first_agent_id, Some("/root/first".to_string()));
+        state.set_agent_path(second_agent_id, Some("/root/second".to_string()));
+        state.mark_running(first_agent_id);
+        state.mark_running(second_agent_id);
+
+        assert_eq!(
+            state.active_agent_label(Some(main_thread_id), Some(main_thread_id)),
+            Some("Main [default] +2".to_string())
+        );
+        assert_eq!(
+            state.active_agent_label(Some(first_agent_id), Some(main_thread_id)),
+            Some("Robie [explorer]".to_string())
+        );
+
+        state.mark_stopped(second_agent_id);
+        assert_eq!(
+            state.active_agent_label(Some(main_thread_id), Some(main_thread_id)),
+            Some("Main [default] +1".to_string())
+        );
+    }
+
     #[test]
     fn adjacent_thread_id_skips_closed_stopped_and_unrelated_threads() {
         let (mut state, main_thread_id, first_agent_id, second_agent_id) = populated_state();
@@ -514,6 +543,34 @@ mod tests {
         );
     }
 
+    #[test]
+    fn adjacent_thread_id_returns_to_main_after_current_agent_stops() {
+        let (mut state, main_thread_id, first_agent_id, second_agent_id) = populated_state();
+        state.set_agent_path(first_agent_id, Some("/root/first".to_string()));
+        state.mark_running(first_agent_id);
+        state.set_agent_path(second_agent_id, Some("/root/second".to_string()));
+        state.mark_running(second_agent_id);
+        state.mark_stopped(first_agent_id);
+        state.mark_stopped(second_agent_id);
+
+        assert_eq!(
+            state.adjacent_thread_id(
+                Some(first_agent_id),
+                Some(main_thread_id),
+                AgentNavigationDirection::Next,
+            ),
+            Some(main_thread_id)
+        );
+        assert_eq!(
+            state.adjacent_thread_id(
+                Some(first_agent_id),
+                Some(main_thread_id),
+                AgentNavigationDirection::Previous,
+            ),
+            Some(main_thread_id)
+        );
+    }
+
     #[test]
     fn picker_subtitle_mentions_shortcuts() {
         let previous: Span<'static> = previous_agent_shortcut().into();
diff --git a/codex-rs/tui/src/app/agent_status_feed.rs b/codex-rs/tui/src/app/agent_status_feed.rs
index 7b5ecb74fe..9cb4e46ddf 100644
--- a/codex-rs/tui/src/app/agent_status_feed.rs
+++ b/codex-rs/tui/src/app/agent_status_feed.rs
@@ -64,21 +64,21 @@ impl HistoryCell for AgentStatusHistoryCell {
 
 #[derive(Debug)]
 pub(super) struct AgentStatusThreadPreview {
-    agent_path: String,
+    agent_label: String,
     activity: Vec<String>,
 }
 
 impl AgentStatusThreadPreview {
-    pub(super) fn from_store(agent_path: String, store: &ThreadEventStore) -> Self {
-        Self::from_events(agent_path, store.buffer.iter().rev())
+    pub(super) fn from_store(agent_label: String, store: &ThreadEventStore) -> Self {
+        Self::from_events(agent_label, store.buffer.iter().rev())
     }
 
-    pub(super) fn empty(agent_path: String) -> Self {
-        Self::from_events(agent_path, std::iter::empty())
+    pub(super) fn empty(agent_label: String) -> Self {
+        Self::from_events(agent_label, std::iter::empty())
     }
 
     fn from_events<'a>(
-        agent_path: String,
+        agent_label: String,
         events: impl Iterator<Item = &'a ThreadBufferedEvent>,
     ) -> Self {
         let mut seen_item_ids = HashSet::new();
@@ -108,13 +108,13 @@ impl AgentStatusThreadPreview {
         }
         activity.reverse();
         Self {
-            agent_path,
+            agent_label,
             activity,
         }
     }
 
     fn title_line(&self) -> Line<'static> {
-        vec!["  ▪ ".dim(), format!("`{}`", self.agent_path).cyan()].into()
+        vec!["  ▪ ".dim(), self.agent_label.clone().cyan()].into()
     }
 
     fn preview_lines(&self, width: u16) -> Vec<Line<'static>> {
diff --git a/codex-rs/tui/src/app/agent_status_feed_tests.rs b/codex-rs/tui/src/app/agent_status_feed_tests.rs
index fd136ec082..3bef540630 100644
--- a/codex-rs/tui/src/app/agent_status_feed_tests.rs
+++ b/codex-rs/tui/src/app/agent_status_feed_tests.rs
@@ -44,7 +44,7 @@ fn agent_status_uses_bounded_buffered_activity() {
         },
     ));
 
-    let preview = AgentStatusThreadPreview::from_store("/root/reviewer".to_string(), &store);
+    let preview = AgentStatusThreadPreview::from_store("Parfit [explorer]".to_string(), &store);
     let cell = AgentStatusHistoryCell::new(vec![preview]);
     let rendered = cell
         .display_lines(/*width*/ 80)
@@ -57,7 +57,7 @@ fn agent_status_uses_bounded_buffered_activity() {
     /agent
     Sub-agents running
 
-      ▪ `/root/reviewer`
+      ▪ Parfit [explorer]
         $ cargo test -p codex-tui
         Finished checking the focused TUI tests.
     "###);
@@ -94,7 +94,7 @@ fn agent_status_uses_reasoning_summaries_only() {
         },
     ));
 
-    let preview = AgentStatusThreadPreview::from_store("/root/reviewer".to_string(), &store);
+    let preview = AgentStatusThreadPreview::from_store("Parfit [explorer]".to_string(), &store);
     let cell = AgentStatusHistoryCell::new(vec![preview]);
     let rendered = cell
         .display_lines(/*width*/ 80)
@@ -107,7 +107,7 @@ fn agent_status_uses_reasoning_summaries_only() {
     /agent
     Sub-agents running
 
-      ▪ `/root/reviewer`
+      ▪ Parfit [explorer]
         safe summary
     "###);
     assert!(!rendered.contains("hidden raw reasoning"));
diff --git a/codex-rs/tui/src/app/session_lifecycle.rs b/codex-rs/tui/src/app/session_lifecycle.rs
index 35e6922045..0985fd0014 100644
--- a/codex-rs/tui/src/app/session_lifecycle.rs
+++ b/codex-rs/tui/src/app/session_lifecycle.rs
@@ -35,40 +35,8 @@ pub(super) struct ThreadSelectionHydration {
 
 impl App {
     pub(super) async fn open_agent_picker(&mut self, app_server: &mut AppServerSession) {
-        let backfill = self.backfill_loaded_subagent_threads(app_server).await;
-        // V2 subagents are identified by canonical paths observed from activity events or loaded
-        // thread metadata. A buffered active turn is positive liveness evidence; a completed
-        // snapshot is terminal evidence. An empty store does not clear a successful spawn hint.
-        let path_backed_thread_ids: Vec<_> = self
-            .agent_navigation
-            .ordered_path_backed_subagent_threads(self.primary_thread_id)
-            .into_iter()
-            .map(|(thread_id, _)| thread_id)
-            .collect();
-        for thread_id in path_backed_thread_ids.iter().copied() {
-            if let Some(channel) = self.thread_event_channels.get(&thread_id)
-                && channel.attachment() == ThreadEventAttachment::Live
-            {
-                let (has_active_turn, has_terminal_snapshot) = {
-                    let store = channel.store.lock().await;
-                    (
-                        store.active_turn_id().is_some(),
-                        store
-                            .turns
-                            .last()
-                            .is_some_and(|turn| !matches!(turn.status, TurnStatus::InProgress)),
-                    )
-                };
-                if has_active_turn {
-                    self.agent_navigation.mark_running(thread_id);
-                } else if has_terminal_snapshot {
-                    self.agent_navigation.mark_stopped(thread_id);
-                }
-            } else if !backfill.refreshed_thread_ids.contains(&thread_id) {
-                self.refresh_agent_picker_thread_liveness(app_server, thread_id)
-                    .await;
-            }
-        }
+        // Path-backed activity is the V2 protocol discriminator. V2 deliberately uses the
+        // transcript status summary instead of opening the legacy V1 thread picker.
         let path_backed_threads = self
             .agent_navigation
             .ordered_path_backed_subagent_threads(self.primary_thread_id);
@@ -79,18 +47,26 @@ impl App {
                     if !entry.is_running || entry.is_closed {
                         return None;
                     }
-                    Some((thread_id, entry.agent_path.as_deref()?.trim().to_string()))
+                    Some((
+                        thread_id,
+                        format_agent_picker_item_name(
+                            entry.agent_nickname.as_deref(),
+                            entry.agent_role.as_deref(),
+                            /*is_primary*/ false,
+                        ),
+                    ))
                 })
                 .collect();
             let mut entries = Vec::new();
-            for (thread_id, agent_path) in running_threads {
+            for (thread_id, agent_label) in running_threads {
                 let preview = if let Some(channel) = self.thread_event_channels.get(&thread_id) {
                     let store = channel.store.lock().await;
                     super::agent_status_feed::AgentStatusThreadPreview::from_store(
-                        agent_path, &store,
+                        agent_label,
+                        &store,
                     )
                 } else {
-                    super::agent_status_feed::AgentStatusThreadPreview::empty(agent_path)
+                    super::agent_status_feed::AgentStatusThreadPreview::empty(agent_label)
                 };
                 entries.push(preview);
             }
@@ -99,8 +75,16 @@ impl App {
                 .add_to_history(super::agent_status_feed::AgentStatusHistoryCell::new(
                     entries,
                 ));
+            return;
         }
 
+        let backfill = self.backfill_loaded_subagent_threads(app_server).await;
+        let path_backed_thread_ids: Vec<_> = self
+            .agent_navigation
+            .ordered_path_backed_subagent_threads(self.primary_thread_id)
+            .into_iter()
+            .map(|(thread_id, _)| thread_id)
+            .collect();
         let mut thread_ids = self.agent_navigation.tracked_thread_ids();
         for thread_id in self.thread_event_channels.keys().copied() {
             if !thread_ids.contains(&thread_id) {
@@ -218,11 +202,13 @@ impl App {
         agent_role: Option<String>,
         is_closed: bool,
     ) {
-        self.chat_widget.set_collab_agent_metadata(
-            thread_id,
-            agent_nickname.clone(),
-            agent_role.clone(),
-        );
+        let (display_nickname, display_role) = if self.primary_thread_id == Some(thread_id) {
+            (Some("Main".to_string()), Some("default".to_string()))
+        } else {
+            (agent_nickname.clone(), agent_role.clone())
+        };
+        self.chat_widget
+            .set_collab_agent_metadata(thread_id, display_nickname, display_role);
         self.agent_navigation
             .upsert(thread_id, agent_nickname, agent_role, is_closed);
         self.sync_active_agent_label();
@@ -439,11 +425,12 @@ impl App {
             );
         }
         for (thread_id, entry) in self.agent_navigation.ordered_threads() {
-            chat_widget.set_collab_agent_metadata(
-                thread_id,
-                entry.agent_nickname.clone(),
-                entry.agent_role.clone(),
-            );
+            let (agent_nickname, agent_role) = if self.primary_thread_id == Some(thread_id) {
+                (Some("Main".to_string()), Some("default".to_string()))
+            } else {
+                (entry.agent_nickname.clone(), entry.agent_role.clone())
+            };
+            chat_widget.set_collab_agent_metadata(thread_id, agent_nickname, agent_role);
         }
         let previous = std::mem::replace(&mut self.chat_widget, chat_widget);
         self.sync_active_agent_label();
diff --git a/codex-rs/tui/src/app/tests.rs b/codex-rs/tui/src/app/tests.rs
index b8e6201c7a..4210106c94 100644
--- a/codex-rs/tui/src/app/tests.rs
+++ b/codex-rs/tui/src/app/tests.rs
@@ -1490,6 +1490,12 @@ async fn open_agent_picker_preserves_running_hints_until_observed_completion() -
     let thread_id = ThreadId::new();
     app.thread_event_channels
         .insert(thread_id, ThreadEventChannel::new(/*capacity*/ 4));
+    app.agent_navigation.upsert(
+        thread_id,
+        Some("Robie".to_string()),
+        Some("explorer".to_string()),
+        /*is_closed*/ false,
+    );
     app.agent_navigation
         .record_sub_agent_activity(SubAgentActivityDisplay {
             thread_id,
@@ -1500,8 +1506,8 @@ async fn open_agent_picker_preserves_running_hints_until_observed_completion() -
     Box::pin(app.open_agent_picker(&mut app_server)).await;
 
     let mut expected_entry = AgentPickerThreadEntry {
-        agent_nickname: None,
-        agent_role: None,
+        agent_nickname: Some("Robie".to_string()),
+        agent_role: Some("explorer".to_string()),
         agent_path: Some("/root/child".to_string()),
         is_running: true,
         is_closed: false,
@@ -1520,7 +1526,7 @@ async fn open_agent_picker_preserves_running_hints_until_observed_completion() -
     /agent
     Sub-agents running
 
-      ▪ `/root/child`
+      ▪ Robie [explorer]
         No recent activity yet.
     "###);
 
@@ -1567,7 +1573,7 @@ async fn open_agent_picker_preserves_running_hints_until_observed_completion() -
 }
 
 #[tokio::test]
-async fn open_agent_picker_clears_running_hint_from_completed_snapshot() -> Result<()> {
+async fn v2_agent_status_does_not_recompute_liveness_from_a_cached_snapshot() -> Result<()> {
     let mut app = Box::pin(make_test_app()).await;
     let mut app_server = Box::pin(crate::start_embedded_app_server_for_picker(
         app.chat_widget.config_ref(),
@@ -1599,7 +1605,7 @@ async fn open_agent_picker_clears_running_hint_from_completed_snapshot() -> Resu
             agent_nickname: None,
             agent_role: None,
             agent_path: Some("/root/child".to_string()),
-            is_running: false,
+            is_running: true,
             is_closed: false,
         })
     );
@@ -1607,7 +1613,7 @@ async fn open_agent_picker_clears_running_hint_from_completed_snapshot() -> Resu
 }
 
 #[tokio::test]
-async fn open_agent_picker_selects_path_backed_agent() -> Result<()> {
+async fn v2_agent_status_does_not_open_the_legacy_picker() -> Result<()> {
     let (mut app, mut app_event_rx, _op_rx) = Box::pin(make_test_app_with_channels()).await;
     let mut app_server = Box::pin(crate::start_embedded_app_server_for_picker(
         app.chat_widget.config_ref(),
@@ -1618,6 +1624,12 @@ async fn open_agent_picker_selects_path_backed_agent() -> Result<()> {
         ThreadId::from_string("00000000-0000-0000-0000-000000000123").expect("valid thread id");
     app.thread_event_channels
         .insert(thread_id, ThreadEventChannel::new(/*capacity*/ 1));
+    app.agent_navigation.upsert(
+        thread_id,
+        Some("Parfit".to_string()),
+        Some("explorer".to_string()),
+        /*is_closed*/ false,
+    );
     app.agent_navigation
         .record_sub_agent_activity(SubAgentActivityDisplay {
             thread_id,
@@ -1627,22 +1639,23 @@ async fn open_agent_picker_selects_path_backed_agent() -> Result<()> {
 
     Box::pin(app.open_agent_picker(&mut app_server)).await;
 
-    assert_app_snapshot!(
-        "path_backed_agent_picker",
-        render_bottom_popup(&app.chat_widget, /*width*/ 80)
-    );
-    while app_event_rx.try_recv().is_ok() {}
-    app.chat_widget
-        .handle_key_event(KeyEvent::new(KeyCode::Enter, KeyModifiers::NONE));
-    assert_matches!(
-        app_event_rx.try_recv(),
-        Ok(AppEvent::SelectAgentThread(selected_thread_id)) if selected_thread_id == thread_id
-    );
+    assert!(app.chat_widget.no_modal_or_popup_active());
+    let rendered = std::iter::from_fn(|| app_event_rx.try_recv().ok())
+        .filter_map(|event| match event {
+            AppEvent::InsertHistoryCell(cell) => {
+                Some(lines_to_single_string(&cell.display_lines(/*width*/ 80)))
+            }
+            _ => None,
+        })
+        .collect::<Vec<_>>()
+        .join("\n");
+    assert!(rendered.contains("Sub-agents running"), "{rendered}");
+    assert!(rendered.contains("Parfit [explorer]"), "{rendered}");
     Ok(())
 }
 
 #[tokio::test]
-async fn open_agent_picker_refreshes_replay_only_path_backed_liveness() -> Result<()> {
+async fn v2_agent_status_does_not_block_on_replay_only_liveness_refresh() -> Result<()> {
     let mut app = Box::pin(make_test_app()).await;
     let mut app_server = Box::pin(crate::start_embedded_app_server_for_picker(
         app.chat_widget.config_ref(),
@@ -1672,8 +1685,8 @@ async fn open_agent_picker_refreshes_replay_only_path_backed_liveness() -> Resul
             agent_nickname: None,
             agent_role: None,
             agent_path: Some("/root/child".to_string()),
-            is_running: false,
-            is_closed: true,
+            is_running: true,
+            is_closed: false,
         })
     );
     Ok(())
@@ -1735,6 +1748,49 @@ async fn open_agent_picker_marks_terminal_read_errors_closed() -> Result<()> {
     Ok(())
 }
 
+#[tokio::test]
+async fn primary_sub_agent_activity_uses_main_default_identity() {
+    let (mut app, mut app_event_rx, _op_rx) = make_test_app_with_channels().await;
+    let primary_thread_id = ThreadId::new();
+    app.primary_thread_id = Some(primary_thread_id);
+    app.upsert_agent_picker_thread(
+        primary_thread_id,
+        /*agent_nickname*/ None,
+        /*agent_role*/ None,
+        /*is_closed*/ false,
+    );
+
+    app.chat_widget.handle_server_notification(
+        ServerNotification::ItemCompleted(codex_app_server_protocol::ItemCompletedNotification {
+            thread_id: primary_thread_id.to_string(),
+            turn_id: "turn-1".to_string(),
+            completed_at_ms: 0,
+            item: ThreadItem::SubAgentActivity {
+                id: "activity-1".to_string(),
+                kind: codex_app_server_protocol::SubAgentActivityKind::Interacted,
+                agent_thread_id: primary_thread_id.to_string(),
+                agent_path: "/root".to_string(),
+            },
+        }),
+        /*replay_kind*/ None,
+    );
+
+    let rendered = loop {
+        let event = app_event_rx.try_recv().expect("main activity history cell");
+        if let AppEvent::InsertHistoryCell(cell) = event {
+            break lines_to_single_string(&cell.display_lines(/*width*/ 80));
+        }
+    };
+    assert!(
+        rendered.contains("Interacted with Main [default]"),
+        "{rendered}"
+    );
+    assert!(
+        !rendered.contains(&primary_thread_id.to_string()),
+        "{rendered}"
+    );
+}
+
 #[test]
 fn open_agent_picker_marks_loaded_threads_open() -> Result<()> {
     const WORKER_THREADS: usize = 1;
diff --git a/codex-rs/tui/src/snapshots/codex_tui__app__tests__path_backed_agent_picker.snap b/codex-rs/tui/src/snapshots/codex_tui__app__tests__path_backed_agent_picker.snap
deleted file mode 100644
index a71cbc6662..0000000000
--- a/codex-rs/tui/src/snapshots/codex_tui__app__tests__path_backed_agent_picker.snap
+++ /dev/null
@@ -1,10 +0,0 @@
----
-source: tui/src/app/tests.rs
-expression: "render_bottom_popup(&app.chat_widget, 80)"
----
-  Subagents
-  Select an agent to watch. ⌥ + ← previous, ⌥ + → next.
-
-› 1. ▪ /root/worker  00000000-0000-0000-0000-000000000123
-
-  Press enter to confirm or esc to go back
