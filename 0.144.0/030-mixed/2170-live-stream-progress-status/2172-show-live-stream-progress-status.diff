diff --git a/codex-rs/tui/src/chatwidget.rs b/codex-rs/tui/src/chatwidget.rs
index a78a602916..9e056b127b 100644
--- a/codex-rs/tui/src/chatwidget.rs
+++ b/codex-rs/tui/src/chatwidget.rs
@@ -677,6 +677,8 @@ pub(crate) struct ChatWidget {
     current_reasoning_item_id: Option<String>,
     reasoning_summary_index: Option<i64>,
     reasoning_saw_summary: bool,
+    live_output_chars: usize,
+    live_plaintext_reasoning_chars: usize,
     deferred_live_thread_notifications: Vec<DeferredLiveThreadNotification>,
     status_state: StatusState,
     review: ReviewState,
diff --git a/codex-rs/tui/src/chatwidget/command_lifecycle.rs b/codex-rs/tui/src/chatwidget/command_lifecycle.rs
index 8e6630f8b3..1a5869e286 100644
--- a/codex-rs/tui/src/chatwidget/command_lifecycle.rs
+++ b/codex-rs/tui/src/chatwidget/command_lifecycle.rs
@@ -255,6 +255,7 @@ impl ChatWidget {
             command_execution_command_and_parsed(&command, &command_actions);
         // Ensure the status indicator is visible while the command runs.
         self.bottom_pane.ensure_status_indicator();
+        self.set_working_without_stream_count();
         let parsed_cmd = self.annotate_skill_reads_in_parsed_cmd(parsed_cmd);
         self.running_commands.insert(
             id.clone(),
diff --git a/codex-rs/tui/src/chatwidget/constructor.rs b/codex-rs/tui/src/chatwidget/constructor.rs
index 359493dc3f..003e268557 100644
--- a/codex-rs/tui/src/chatwidget/constructor.rs
+++ b/codex-rs/tui/src/chatwidget/constructor.rs
@@ -190,6 +190,8 @@ impl ChatWidget {
             current_reasoning_item_id: None,
             reasoning_summary_index: None,
             reasoning_saw_summary: false,
+            live_output_chars: 0,
+            live_plaintext_reasoning_chars: 0,
             deferred_live_thread_notifications: Vec::new(),
             status_state: StatusState::default(),
             review: ReviewState::default(),
diff --git a/codex-rs/tui/src/chatwidget/streaming.rs b/codex-rs/tui/src/chatwidget/streaming.rs
index d4db212e3b..7a9e9be79c 100644
--- a/codex-rs/tui/src/chatwidget/streaming.rs
+++ b/codex-rs/tui/src/chatwidget/streaming.rs
@@ -7,6 +7,11 @@ use super::*;
 const NO_REASONING_SUMMARY_LABEL: &str = "[No reasoning summary]";
 
 impl ChatWidget {
+    pub(super) fn clear_live_stream_progress(&mut self) {
+        self.live_output_chars = 0;
+        self.live_plaintext_reasoning_chars = 0;
+    }
+
     pub(super) fn clear_reasoning_stream_state(&mut self) {
         self.reasoning_buffer.clear();
         self.full_reasoning_buffer.clear();
@@ -16,6 +21,27 @@ impl ChatWidget {
         self.reasoning_saw_summary = false;
     }
 
+    fn estimated_tokens_for_chars(chars: usize) -> usize {
+        chars.saturating_add(3) / 4
+    }
+
+    fn set_working_output_progress(&mut self) {
+        let tokens = Self::estimated_tokens_for_chars(self.live_output_chars);
+        self.status_state.terminal_title_status_kind = TerminalTitleStatusKind::Working;
+        self.set_status_header(format!("Working · output ~{tokens} tokens"));
+    }
+
+    pub(super) fn set_working_without_stream_count(&mut self) {
+        self.status_state.terminal_title_status_kind = TerminalTitleStatusKind::Working;
+        self.set_status_header(String::from("Working"));
+    }
+
+    fn set_plaintext_reasoning_progress(&mut self, header: Option<&str>) {
+        let tokens = Self::estimated_tokens_for_chars(self.live_plaintext_reasoning_chars);
+        let label = header.unwrap_or("Thinking");
+        self.set_reasoning_status_header(&format!("{label} · reasoning ~{tokens} tokens"));
+    }
+
     fn set_reasoning_status_header(&mut self, base_header: &str) {
         self.status_state.terminal_title_status_kind = TerminalTitleStatusKind::Thinking;
         self.set_status_header(base_header.to_string());
@@ -160,6 +186,12 @@ impl ChatWidget {
         delta: String,
         message_style: history_cell::AgentMessageStyle,
     ) {
+        if message_style == history_cell::AgentMessageStyle::Normal && !delta.is_empty() {
+            self.live_output_chars = self.live_output_chars.saturating_add(delta.chars().count());
+            if !self.safety_buffering_is_waiting() {
+                self.set_working_output_progress();
+            }
+        }
         self.handle_streaming_delta(delta, message_style);
     }
 
@@ -260,6 +292,7 @@ impl ChatWidget {
         // For reasoning deltas, do not stream to history. Accumulate the
         // current reasoning block and extract the first bold element
         // (between **/**) as the chunk header. Show this header as status.
+        let plaintext_chars = delta.chars().count();
         self.reasoning_buffer.push_str(&delta);
 
         if self.safety_buffering_is_waiting() {
@@ -276,6 +309,11 @@ impl ChatWidget {
         if let Some(header) = extract_first_bold(&self.reasoning_buffer) {
             // Update the shimmer header to the extracted reasoning chunk header.
             self.set_reasoning_status_header(&header);
+        } else if plaintext_chars > 0 {
+            self.live_plaintext_reasoning_chars = self
+                .live_plaintext_reasoning_chars
+                .saturating_add(plaintext_chars);
+            self.set_plaintext_reasoning_progress(None);
         }
         self.request_redraw();
     }
@@ -357,7 +395,7 @@ impl ChatWidget {
         }
 
         if self.unified_exec_wait_streak.is_none() {
-            self.set_reasoning_status_header(NO_REASONING_SUMMARY_LABEL);
+            self.set_reasoning_status_header("Thinking");
         }
         self.request_redraw();
     }
diff --git a/codex-rs/tui/src/chatwidget/tests/helpers.rs b/codex-rs/tui/src/chatwidget/tests/helpers.rs
index 150844b0bd..5b82e1214e 100644
--- a/codex-rs/tui/src/chatwidget/tests/helpers.rs
+++ b/codex-rs/tui/src/chatwidget/tests/helpers.rs
@@ -545,6 +545,25 @@ pub(super) fn handle_agent_reasoning_delta(chat: &mut ChatWidget, delta: impl In
     );
 }
 
+pub(super) fn handle_agent_reasoning_raw_delta(chat: &mut ChatWidget, delta: impl Into<String>) {
+    chat.handle_server_notification(
+        ServerNotification::ReasoningTextDelta(
+            codex_app_server_protocol::ReasoningTextDeltaNotification {
+                thread_id: thread_id(chat),
+                turn_id: chat
+                    .turn_lifecycle
+                    .last_turn_id
+                    .clone()
+                    .unwrap_or_else(|| "turn-1".to_string()),
+                item_id: "reasoning-1".to_string(),
+                delta: delta.into(),
+                content_index: 0,
+            },
+        ),
+        /*replay_kind*/ None,
+    );
+}
+
 pub(super) fn handle_agent_reasoning_final(chat: &mut ChatWidget) {
     chat.handle_server_notification(
         ServerNotification::ItemCompleted(ItemCompletedNotification {
diff --git a/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs b/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs
index e349faed34..0a29400a38 100644
--- a/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs
+++ b/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs
@@ -2583,6 +2583,63 @@ async fn stream_error_marks_incomplete_reasoning() {
     assert!(chat.full_reasoning_buffer.is_empty());
 }
 
+#[tokio::test]
+async fn live_working_status_counts_visible_output_stream() {
+    let (mut chat, _rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+
+    handle_turn_started(&mut chat, "turn-1");
+    handle_agent_message_delta(&mut chat, "abcdefgh");
+
+    assert_eq!(
+        chat.status_state.current_status.header,
+        "Working · output ~2 tokens"
+    );
+}
+
+#[tokio::test]
+async fn live_working_status_does_not_count_openai_encrypted_reasoning() {
+    let (mut chat, _rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+
+    handle_turn_started(&mut chat, "turn-1");
+    handle_agent_reasoning_raw_delta(&mut chat, "encrypted-reasoning");
+
+    assert_eq!(chat.status_state.current_status.header, "Thinking");
+}
+
+#[tokio::test]
+async fn live_working_status_preserves_titled_openai_reasoning_summary() {
+    let (mut chat, _rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+
+    handle_turn_started(&mut chat, "turn-1");
+    handle_agent_reasoning_delta(&mut chat, "**Inspecting files**");
+
+    assert_eq!(chat.status_state.current_status.header, "Inspecting files");
+}
+
+#[tokio::test]
+async fn live_working_status_counts_untitled_reasoning_summary_stream() {
+    let (mut chat, _rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+
+    handle_turn_started(&mut chat, "turn-1");
+    handle_agent_reasoning_delta(&mut chat, "abcd");
+
+    assert_eq!(
+        chat.status_state.current_status.header,
+        "Thinking · reasoning ~1 tokens"
+    );
+}
+
+#[tokio::test]
+async fn live_working_status_clears_stream_counts_on_tool_start() {
+    let (mut chat, _rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+
+    handle_turn_started(&mut chat, "turn-1");
+    handle_agent_message_delta(&mut chat, "abcdefgh");
+    begin_exec_with_source(&mut chat, "call-1", "echo hi", ExecCommandSource::Agent);
+
+    assert_eq!(chat.status_state.current_status.header, "Working");
+}
+
 #[tokio::test]
 async fn warning_event_adds_warning_history_cell() {
     let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
diff --git a/codex-rs/tui/src/chatwidget/tool_lifecycle.rs b/codex-rs/tui/src/chatwidget/tool_lifecycle.rs
index 1f0b7c7faa..a7fd92219c 100644
--- a/codex-rs/tui/src/chatwidget/tool_lifecycle.rs
+++ b/codex-rs/tui/src/chatwidget/tool_lifecycle.rs
@@ -9,6 +9,7 @@ use codex_utils_path_uri::LegacyAppPathString;
 impl ChatWidget {
     pub(super) fn on_patch_apply_begin(&mut self, changes: HashMap<PathBuf, FileChange>) {
         self.record_visible_turn_activity();
+        self.set_working_without_stream_count();
         self.add_to_history(history_cell::new_patch_event(changes, &self.config.cwd));
     }
 
@@ -169,6 +170,7 @@ impl ChatWidget {
 
     pub(crate) fn handle_mcp_tool_call_started_now(&mut self, item: ThreadItem) {
         self.record_visible_turn_activity();
+        self.set_working_without_stream_count();
         let ThreadItem::McpToolCall {
             id,
             server,
diff --git a/codex-rs/tui/src/chatwidget/turn_runtime.rs b/codex-rs/tui/src/chatwidget/turn_runtime.rs
index 7147cf415f..8836e12903 100644
--- a/codex-rs/tui/src/chatwidget/turn_runtime.rs
+++ b/codex-rs/tui/src/chatwidget/turn_runtime.rs
@@ -77,6 +77,7 @@ impl ChatWidget {
         if self.plan_stream_controller.take().is_some() {
             self.request_pending_usage_output_insertion_after_stream_shutdown();
         }
+        self.clear_live_stream_progress();
         self.last_response_tps = None;
         self.last_response_ttft_ms = None;
         self.turn_runtime_metrics = RuntimeMetricsSummary::default();
@@ -392,6 +393,7 @@ impl ChatWidget {
         self.unified_exec_wait_streak = None;
         self.adaptive_chunking.reset();
         self.stream_controller = None;
+        self.clear_live_stream_progress();
         self.agent_message_phases.clear();
         self.plan_stream_controller = None;
         self.request_pending_usage_output_insertion_after_stream_shutdown();
