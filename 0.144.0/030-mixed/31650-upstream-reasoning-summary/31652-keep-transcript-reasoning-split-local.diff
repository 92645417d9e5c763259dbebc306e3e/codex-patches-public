diff --git a/codex-rs/protocol/src/legacy_events.rs b/codex-rs/protocol/src/legacy_events.rs
index 1c923fbce7..66fa9e4850 100644
--- a/codex-rs/protocol/src/legacy_events.rs
+++ b/codex-rs/protocol/src/legacy_events.rs
@@ -137,8 +137,11 @@ impl ReasoningItem {
     pub fn as_legacy_events(&self, show_raw_agent_reasoning: bool) -> Vec<EventMsg> {
         let mut events = Vec::new();
         for summary in &self.summary_text {
+            let Some(summary) = visible_reasoning_summary_text(summary) else {
+                continue;
+            };
             events.push(EventMsg::AgentReasoning(AgentReasoningEvent {
-                text: summary.clone(),
+                text: summary,
                 continued: self.continued,
                 continuation_limit_hit: self.continuation_limit_hit,
             }));
@@ -160,6 +163,22 @@ impl ReasoningItem {
     }
 }
 
+fn visible_reasoning_summary_text(summary: &str) -> Option<String> {
+    let summary = summary.trim();
+    if summary.is_empty() || summary == "<!-- -->" {
+        return None;
+    }
+    if let Some(after_open) = summary.strip_prefix("**")
+        && let Some(close) = after_open.find("**")
+    {
+        let after_close_idx = 2 + close + 2;
+        if summary[after_close_idx..].trim() == "<!-- -->" {
+            return None;
+        }
+    }
+    Some(summary.to_string())
+}
+
 impl CommandExecutionItem {
     pub(crate) fn as_legacy_begin_event(&self, turn_id: String, started_at_ms: i64) -> EventMsg {
         EventMsg::ExecCommandBegin(ExecCommandBeginEvent {
@@ -635,3 +654,46 @@ impl HasLegacyEvent for EventMsg {
         }
     }
 }
+
+#[cfg(test)]
+mod tests {
+    use super::*;
+
+    #[test]
+    fn reasoning_legacy_events_hide_empty_placeholder_summary() {
+        let item = ReasoningItem {
+            id: "rs_1".to_string(),
+            summary_text: vec!["**Planning read-only inspection**\n\n<!-- -->".to_string()],
+            raw_content: Vec::new(),
+            continued: false,
+            continuation_limit_hit: false,
+            reasoning_output_tokens: None,
+        };
+
+        assert!(
+            item.as_legacy_events(/*show_raw_agent_reasoning*/ false)
+                .is_empty()
+        );
+    }
+
+    #[test]
+    fn reasoning_legacy_events_keep_raw_content_when_summary_is_placeholder() {
+        let item = ReasoningItem {
+            id: "rs_1".to_string(),
+            summary_text: vec!["**Planning read-only inspection**\n\n<!-- -->".to_string()],
+            raw_content: vec!["raw reasoning".to_string()],
+            continued: false,
+            continuation_limit_hit: false,
+            reasoning_output_tokens: None,
+        };
+
+        let events = item.as_legacy_events(/*show_raw_agent_reasoning*/ true);
+        assert_eq!(events.len(), 1);
+        match &events[0] {
+            EventMsg::AgentReasoningRawContent(event) => {
+                assert_eq!(event.text, "raw reasoning");
+            }
+            event => panic!("expected raw reasoning event, got {event:?}"),
+        }
+    }
+}
diff --git a/codex-rs/tui/src/chatwidget/streaming.rs b/codex-rs/tui/src/chatwidget/streaming.rs
index 7a9e9be79c..7e0a4bd704 100644
--- a/codex-rs/tui/src/chatwidget/streaming.rs
+++ b/codex-rs/tui/src/chatwidget/streaming.rs
@@ -327,12 +327,7 @@ impl ChatWidget {
     fn current_reasoning_summary_markdown(&self) -> Option<String> {
         let mut rendered = self.full_reasoning_buffer.clone();
         rendered.push_str(&self.reasoning_buffer);
-        let rendered = rendered.trim().to_string();
-        if !rendered.is_empty() {
-            return Some(rendered);
-        }
-
-        None
+        history_cell::visible_reasoning_summary_markdown(&rendered)
     }
 
     fn current_reasoning_markdown(&self) -> Option<String> {
@@ -356,8 +351,7 @@ impl ChatWidget {
     ) -> Option<String> {
         let summary = summary
             .iter()
-            .map(|part| part.trim())
-            .filter(|part| !part.is_empty())
+            .filter_map(|part| history_cell::visible_reasoning_summary_markdown(part))
             .collect::<Vec<_>>();
         let raw = content.concat();
         let raw = raw.trim();
diff --git a/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs b/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs
index 0a29400a38..5cf9a9ddd1 100644
--- a/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs
+++ b/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs
@@ -3809,6 +3809,16 @@ async fn final_reasoning_then_message_without_deltas_are_rendered() {
     );
 }
 
+#[tokio::test]
+async fn empty_reasoning_placeholder_delta_is_not_rendered() {
+    let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+
+    handle_agent_reasoning_delta(&mut chat, "**Planning read-only inspection**\n\n<!-- -->");
+    handle_agent_reasoning_final(&mut chat);
+
+    assert!(drain_insert_history(&mut rx).is_empty());
+}
+
 #[tokio::test]
 async fn deltas_then_same_final_message_are_rendered_snapshot() {
     let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
diff --git a/codex-rs/tui/src/history_cell/messages.rs b/codex-rs/tui/src/history_cell/messages.rs
index 6b73654800..fd9d2488ef 100644
--- a/codex-rs/tui/src/history_cell/messages.rs
+++ b/codex-rs/tui/src/history_cell/messages.rs
@@ -690,7 +690,22 @@ pub(crate) fn new_reasoning_summary_block_with_metadata(
     continuation_notices: Vec<(ContinuationNoticeKind, usize)>,
 ) -> Box<dyn HistoryCell> {
     let cwd = cwd.to_path_buf();
-    let full_reasoning_buffer = full_reasoning_buffer.trim();
+    let Some(full_reasoning_buffer) = visible_reasoning_summary_markdown(&full_reasoning_buffer)
+    else {
+        return Box::new(ReasoningSummaryCell::new(
+            String::new(),
+            String::new(),
+            &cwd,
+            /*transcript_only*/ true,
+            continued,
+            continuation_limit_hit,
+            reasoning_output_tokens,
+            reasoning_item_id,
+            continuation_notices,
+            /*show_usage_suffix*/ true,
+        ));
+    };
+    let full_reasoning_buffer = full_reasoning_buffer.as_str();
     if let Some(open) = full_reasoning_buffer.find("**") {
         let after_open = &full_reasoning_buffer[(open + 2)..];
         if after_open.find("**").is_some() {
@@ -736,6 +751,22 @@ pub(crate) fn new_reasoning_summary_block_with_metadata(
     ))
 }
 
+pub(crate) fn visible_reasoning_summary_markdown(markdown: &str) -> Option<String> {
+    let markdown = markdown.trim();
+    if markdown.is_empty() || markdown == "<!-- -->" {
+        return None;
+    }
+    if let Some(after_open) = markdown.strip_prefix("**")
+        && let Some(close) = after_open.find("**")
+    {
+        let after_close_idx = 2 + close + 2;
+        if markdown[after_close_idx..].trim() == "<!-- -->" {
+            return None;
+        }
+    }
+    Some(markdown.to_string())
+}
+
 fn display_reasoning_item_id(item_id: String) -> Option<String> {
     let item_id = item_id.strip_prefix("rs_").unwrap_or(&item_id).trim();
     if item_id.starts_with("item-") {
diff --git a/codex-rs/tui/src/history_cell/tests.rs b/codex-rs/tui/src/history_cell/tests.rs
index d98c278bf2..8a5595651f 100644
--- a/codex-rs/tui/src/history_cell/tests.rs
+++ b/codex-rs/tui/src/history_cell/tests.rs
@@ -2454,6 +2454,20 @@ fn reasoning_summary_block_falls_back_when_summary_is_missing() {
     assert_eq!(rendered, vec!["■ High level reasoning without closing"]);
 }
 
+#[test]
+fn reasoning_summary_block_hides_empty_placeholder_summary() {
+    let cell = new_reasoning_summary_block(
+        "**Planning read-only inspection**\n\n<!-- -->".to_string(),
+        &test_cwd(),
+        /*continued*/ false,
+        /*continuation_limit_hit*/ false,
+        /*reasoning_output_tokens*/ None,
+    );
+
+    assert!(cell.display_lines(/*width*/ 80).is_empty());
+    assert!(cell.transcript_lines(/*width*/ 80).is_empty());
+}
+
 #[test]
 fn reasoning_summary_block_splits_header_and_summary_when_present() {
     let cell = new_reasoning_summary_block(
diff --git a/codex-rs/tui/src/thread_transcript.rs b/codex-rs/tui/src/thread_transcript.rs
index 58ecaca583..8baef2b327 100644
--- a/codex-rs/tui/src/thread_transcript.rs
+++ b/codex-rs/tui/src/thread_transcript.rs
@@ -9,7 +9,6 @@ use crate::history_cell::HistoryCell;
 use crate::history_cell::PlainHistoryCell;
 use crate::history_cell::ReasoningSummaryCell;
 use crate::history_cell::UserHistoryCell;
-use crate::history_cell::split_reasoning_summary_parts;
 use crate::multi_agents::sub_agent_activity_summary;
 use codex_app_server_protocol::Thread;
 use codex_app_server_protocol::ThreadItem;
@@ -243,3 +242,47 @@ fn fallback_transcript_cell(item: &ThreadItem) -> Option<PlainHistoryCell> {
     };
     (!lines.is_empty()).then(|| PlainHistoryCell::new(lines))
 }
+
+fn split_reasoning_summary_parts(reasoning_parts: &[String]) -> (String, String) {
+    let mut leading_empty_part_header = None;
+    let mut content_parts = Vec::with_capacity(reasoning_parts.len());
+    for part in reasoning_parts {
+        let part = part.trim();
+        if part.is_empty() {
+            continue;
+        }
+        let header_end = part.strip_prefix("**").and_then(|after_open| {
+            after_open
+                .find("**")
+                .and_then(|close| (close > 0).then_some(close + 4))
+        });
+        let body = header_end.map_or(part, |header_end| &part[header_end..]);
+        if body.trim() == "<!-- -->" {
+            if content_parts.is_empty()
+                && leading_empty_part_header.is_none()
+                && let Some(header_end) = header_end
+            {
+                leading_empty_part_header = Some(part[..header_end].to_string());
+            }
+            continue;
+        }
+        content_parts.push(part);
+    }
+    let content = content_parts.join("\n\n");
+    if content.is_empty() {
+        return (leading_empty_part_header.unwrap_or_default(), content);
+    }
+    if let Some(after_open) = content.strip_prefix("**")
+        && let Some(close) = after_open.find("**")
+    {
+        let after_close_idx = 2 + close + 2;
+        let after_close = &content[after_close_idx..];
+        if after_close.starts_with('\n') || after_close.starts_with('\r') {
+            return (
+                content[..after_close_idx].to_string(),
+                after_close.to_string(),
+            );
+        }
+    }
+    (leading_empty_part_header.unwrap_or_default(), content)
+}
