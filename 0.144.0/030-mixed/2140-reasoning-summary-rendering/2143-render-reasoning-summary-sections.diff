diff --git a/codex-rs/tui/src/chatwidget.rs b/codex-rs/tui/src/chatwidget.rs
index 3f4ab743d6..707ffe1a02 100644
--- a/codex-rs/tui/src/chatwidget.rs
+++ b/codex-rs/tui/src/chatwidget.rs
@@ -633,8 +633,9 @@ pub(crate) struct ChatWidget {
     interrupts: InterruptManager,
     // Accumulates the current reasoning block text to extract a header
     reasoning_buffer: String,
-    // Preserves reasoning-summary part boundaries for transcript-only recording.
-    reasoning_summary_parts: Vec<String>,
+    // Accumulates full reasoning content for transcript-only recording
+    full_reasoning_buffer: String,
+    reasoning_summary_index: Option<i64>,
     status_state: StatusState,
     review: ReviewState,
     // Active hook runs render in a dedicated live cell so they can run alongside tools.
diff --git a/codex-rs/tui/src/chatwidget/constructor.rs b/codex-rs/tui/src/chatwidget/constructor.rs
index b8e5d8baf4..f443db91e1 100644
--- a/codex-rs/tui/src/chatwidget/constructor.rs
+++ b/codex-rs/tui/src/chatwidget/constructor.rs
@@ -179,7 +179,8 @@ impl ChatWidget {
             newly_installed_marketplace_tab_id: None,
             interrupts: InterruptManager::new(),
             reasoning_buffer: String::new(),
-            reasoning_summary_parts: Vec::new(),
+            full_reasoning_buffer: String::new(),
+            reasoning_summary_index: None,
             status_state: StatusState::default(),
             review: ReviewState::default(),
             active_hook_cell: None,
diff --git a/codex-rs/tui/src/chatwidget/input_flow.rs b/codex-rs/tui/src/chatwidget/input_flow.rs
index 44ff2c4b8b..4b36eca408 100644
--- a/codex-rs/tui/src/chatwidget/input_flow.rs
+++ b/codex-rs/tui/src/chatwidget/input_flow.rs
@@ -38,7 +38,8 @@ impl ChatWidget {
                     // Submitted is emitted when user submits.
                     // Reset any reasoning header only when we are actually submitting a turn.
                     self.reasoning_buffer.clear();
-                    self.reasoning_summary_parts.clear();
+                    self.full_reasoning_buffer.clear();
+                    self.reasoning_summary_index = None;
                     self.set_status_header(String::from("Working"));
                     self.submit_user_message_with_user_prompt_submit_hook(
                         user_message,
diff --git a/codex-rs/tui/src/chatwidget/protocol.rs b/codex-rs/tui/src/chatwidget/protocol.rs
index 498a016414..d14f3897c6 100644
--- a/codex-rs/tui/src/chatwidget/protocol.rs
+++ b/codex-rs/tui/src/chatwidget/protocol.rs
@@ -78,14 +78,19 @@ impl ChatWidget {
             }
             ServerNotification::PlanDelta(notification) => self.on_plan_delta(notification.delta),
             ServerNotification::ReasoningSummaryTextDelta(notification) => {
-                self.on_agent_reasoning_delta(notification.delta);
+                self.on_agent_reasoning_summary_delta(
+                    notification.delta,
+                    notification.summary_index,
+                );
             }
             ServerNotification::ReasoningTextDelta(notification) => {
                 if self.config.show_raw_agent_reasoning {
                     self.on_agent_reasoning_delta(notification.delta);
                 }
             }
-            ServerNotification::ReasoningSummaryPartAdded(_) => self.on_reasoning_section_break(),
+            ServerNotification::ReasoningSummaryPartAdded(notification) => {
+                self.on_reasoning_section_break(notification.summary_index);
+            }
             ServerNotification::TerminalInteraction(notification) => {
                 self.on_terminal_interaction(notification.process_id, notification.stdin)
             }
diff --git a/codex-rs/tui/src/chatwidget/replay.rs b/codex-rs/tui/src/chatwidget/replay.rs
index bbf8478261..d725be05b2 100644
--- a/codex-rs/tui/src/chatwidget/replay.rs
+++ b/codex-rs/tui/src/chatwidget/replay.rs
@@ -113,18 +113,13 @@ impl ChatWidget {
                 summary, content, ..
             } => {
                 if from_replay {
-                    let reasoning_parts = summary.into_iter().chain(
-                        self.config
-                            .show_raw_agent_reasoning
-                            .then_some(content)
-                            .into_iter()
-                            .flatten(),
-                    );
-                    for (index, delta) in reasoning_parts.enumerate() {
-                        if index > 0 {
-                            self.on_reasoning_section_break();
+                    for (summary_index, delta) in summary.into_iter().enumerate() {
+                        self.on_agent_reasoning_summary_delta(delta, summary_index as i64);
+                    }
+                    if self.config.show_raw_agent_reasoning {
+                        for delta in content {
+                            self.on_agent_reasoning_delta(delta);
                         }
-                        self.on_agent_reasoning_delta(delta);
                     }
                 }
                 self.on_agent_reasoning_final();
diff --git a/codex-rs/tui/src/chatwidget/slash_dispatch.rs b/codex-rs/tui/src/chatwidget/slash_dispatch.rs
index b0da383b84..81920b7276 100644
--- a/codex-rs/tui/src/chatwidget/slash_dispatch.rs
+++ b/codex-rs/tui/src/chatwidget/slash_dispatch.rs
@@ -741,7 +741,8 @@ impl ChatWidget {
                 );
                 if self.is_session_configured() {
                     self.reasoning_buffer.clear();
-                    self.reasoning_summary_parts.clear();
+                    self.full_reasoning_buffer.clear();
+                    self.reasoning_summary_index = None;
                     self.set_status_header(String::from("Working"));
                     self.submit_user_message(user_message);
                 } else {
diff --git a/codex-rs/tui/src/chatwidget/streaming.rs b/codex-rs/tui/src/chatwidget/streaming.rs
index afeda47a79..5951b2f051 100644
--- a/codex-rs/tui/src/chatwidget/streaming.rs
+++ b/codex-rs/tui/src/chatwidget/streaming.rs
@@ -236,28 +236,44 @@ impl ChatWidget {
         self.request_redraw();
     }
 
+    pub(super) fn on_agent_reasoning_summary_delta(&mut self, delta: String, summary_index: i64) {
+        if self.reasoning_summary_index != Some(summary_index) {
+            if self.reasoning_summary_index.is_some() {
+                self.on_reasoning_section_break(summary_index);
+            } else {
+                self.reasoning_summary_index = Some(summary_index);
+            }
+        }
+        self.on_agent_reasoning_delta(delta);
+    }
+
     pub(super) fn on_agent_reasoning_final(&mut self) {
         // At the end of a reasoning block, record transcript-only content.
-        if !self.reasoning_buffer.is_empty() {
-            self.reasoning_summary_parts
-                .push(std::mem::take(&mut self.reasoning_buffer));
-        }
-        if !self.reasoning_summary_parts.is_empty() {
-            let reasoning_parts = std::mem::take(&mut self.reasoning_summary_parts);
-            let cell = history_cell::new_reasoning_summary_block(reasoning_parts, &self.config.cwd);
+        self.full_reasoning_buffer.push_str(&self.reasoning_buffer);
+        if !self.full_reasoning_buffer.is_empty() {
+            let cell = history_cell::new_reasoning_summary_block(
+                self.full_reasoning_buffer.clone(),
+                &self.config.cwd,
+            );
             self.add_boxed_history(cell);
         }
         self.reasoning_buffer.clear();
-        self.reasoning_summary_parts.clear();
+        self.full_reasoning_buffer.clear();
+        self.reasoning_summary_index = None;
         self.request_redraw();
     }
 
-    pub(super) fn on_reasoning_section_break(&mut self) {
+    pub(super) fn on_reasoning_section_break(&mut self, summary_index: i64) {
+        if self.reasoning_summary_index == Some(summary_index) && self.reasoning_buffer.is_empty() {
+            return;
+        }
         // Start a new reasoning block for header extraction and accumulate transcript.
         if !self.reasoning_buffer.is_empty() {
-            self.reasoning_summary_parts
-                .push(std::mem::take(&mut self.reasoning_buffer));
+            self.full_reasoning_buffer.push_str(&self.reasoning_buffer);
+            self.full_reasoning_buffer.push_str("\n\n");
         }
+        self.reasoning_buffer.clear();
+        self.reasoning_summary_index = Some(summary_index);
     }
 
     fn mark_interrupted_streams_for_retry(&mut self) {
diff --git a/codex-rs/tui/src/chatwidget/tests/history_replay.rs b/codex-rs/tui/src/chatwidget/tests/history_replay.rs
index 005ab90968..faf9ddf520 100644
--- a/codex-rs/tui/src/chatwidget/tests/history_replay.rs
+++ b/codex-rs/tui/src/chatwidget/tests/history_replay.rs
@@ -894,7 +894,7 @@ async fn replayed_thread_closed_notification_does_not_exit_tui() {
 }
 
 #[tokio::test]
-async fn replayed_reasoning_item_preserves_summary_parts_and_hides_raw_reasoning_when_disabled() {
+async fn replayed_reasoning_item_hides_raw_reasoning_when_disabled() {
     let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
     chat.config.show_raw_agent_reasoning = false;
     chat.handle_thread_session(crate::session_state::ThreadSessionState {
@@ -924,10 +924,7 @@ async fn replayed_reasoning_item_preserves_summary_parts_and_hides_raw_reasoning
     chat.replay_thread_item(
         AppServerThreadItem::Reasoning {
             id: "reasoning-1".to_string(),
-            summary: vec![
-                "**Plan**\n\ndone".to_string(),
-                "**Checking tests**\n\n<!-- -->".to_string(),
-            ],
+            summary: vec!["Summary only".to_string()],
             content: vec!["Raw reasoning".to_string()],
         },
         "turn-1".to_string(),
@@ -940,10 +937,64 @@ async fn replayed_reasoning_item_preserves_summary_parts_and_hides_raw_reasoning
         }
         other => panic!("expected InsertHistoryCell, got {other:?}"),
     };
-    assert_eq!(rendered, "• done\n");
+    assert!(!rendered.trim().is_empty());
     assert!(!rendered.contains("Raw reasoning"));
 }
 
+#[tokio::test]
+async fn replayed_reasoning_summary_parts_render_as_separate_sections() {
+    let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+    chat.config.show_raw_agent_reasoning = false;
+    chat.handle_thread_session(crate::session_state::ThreadSessionState {
+        thread_id: ThreadId::new(),
+        forked_from_id: None,
+        fork_parent_title: None,
+        thread_name: None,
+        model: "test-model".to_string(),
+        model_provider_id: "test-provider".to_string(),
+        service_tier: None,
+        approval_policy: AskForApproval::Never,
+        approvals_reviewer: ApprovalsReviewer::User,
+        permission_profile: PermissionProfile::read_only(),
+        active_permission_profile: None,
+        cwd: test_project_path().abs(),
+        runtime_workspace_roots: Vec::new(),
+        instruction_source_paths: Vec::new(),
+        reasoning_effort: None,
+        collaboration_mode: None,
+        personality: None,
+        message_history: None,
+        network_proxy: None,
+        rollout_path: None,
+    });
+    let _ = drain_insert_history(&mut rx);
+
+    chat.replay_thread_item(
+        AppServerThreadItem::Reasoning {
+            id: "reasoning-1".to_string(),
+            summary: vec![
+                "**Planning resolution execution**\n\nRecord the baseline.".to_string(),
+                "**Continuing workflow steps**\n\nClose the loop.".to_string(),
+            ],
+            content: Vec::new(),
+        },
+        "turn-1".to_string(),
+        ReplayKind::ThreadSnapshot,
+    );
+
+    let rendered = match rx.try_recv() {
+        Ok(AppEvent::InsertHistoryCell(cell)) => {
+            lines_to_single_string(&cell.transcript_lines(/*width*/ 80))
+        }
+        other => panic!("expected InsertHistoryCell, got {other:?}"),
+    };
+    assert!(rendered.contains("Planning resolution execution"));
+    assert!(rendered.contains("Continuing workflow steps"));
+    assert!(rendered.contains("Record the baseline."));
+    assert!(rendered.contains("Close the loop."));
+    assert!(!rendered.contains("baseline.Close"));
+}
+
 #[tokio::test]
 async fn replayed_reasoning_item_shows_raw_reasoning_when_enabled() {
     let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
@@ -1078,7 +1129,7 @@ async fn live_reasoning_summary_is_not_rendered_twice_when_item_completes() {
 }
 
 #[tokio::test]
-async fn live_reasoning_summary_drops_empty_parts_without_losing_content() {
+async fn live_reasoning_summary_sections_render_titles_on_separate_lines() {
     let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
     chat.show_welcome_banner = false;
 
@@ -1101,26 +1152,21 @@ async fn live_reasoning_summary_drops_empty_parts_without_losing_content() {
     let _ = drain_insert_history(&mut rx);
 
     for (summary_index, delta) in [
-        (0, "**Plan**\n\ndone"),
-        (1, "**Checking tests**\n\n<!-- -->"),
+        (
+            0,
+            "**Planning resolution execution**\n\nRecord the baseline.".to_string(),
+        ),
+        (
+            1,
+            "**Continuing workflow steps**\n\nClose the loop.".to_string(),
+        ),
     ] {
-        chat.handle_server_notification(
-            ServerNotification::ReasoningSummaryPartAdded(
-                codex_app_server_protocol::ReasoningSummaryPartAddedNotification {
-                    thread_id: "thread-1".to_string(),
-                    turn_id: "turn-1".to_string(),
-                    item_id: "reasoning-1".to_string(),
-                    summary_index,
-                },
-            ),
-            /*replay_kind*/ None,
-        );
         chat.handle_server_notification(
             ServerNotification::ReasoningSummaryTextDelta(ReasoningSummaryTextDeltaNotification {
                 thread_id: "thread-1".to_string(),
                 turn_id: "turn-1".to_string(),
                 item_id: "reasoning-1".to_string(),
-                delta: delta.to_string(),
+                delta,
                 summary_index,
             }),
             /*replay_kind*/ None,
@@ -1135,8 +1181,8 @@ async fn live_reasoning_summary_drops_empty_parts_without_losing_content() {
             item: AppServerThreadItem::Reasoning {
                 id: "reasoning-1".to_string(),
                 summary: vec![
-                    "**Plan**\n\ndone".to_string(),
-                    "**Checking tests**\n\n<!-- -->".to_string(),
+                    "**Planning resolution execution**\n\nRecord the baseline.".to_string(),
+                    "**Continuing workflow steps**\n\nClose the loop.".to_string(),
                 ],
                 content: Vec::new(),
             },
@@ -1150,7 +1196,10 @@ async fn live_reasoning_summary_drops_empty_parts_without_losing_content() {
         }
         other => panic!("expected InsertHistoryCell, got {other:?}"),
     };
-    assert_eq!(rendered, "• done\n");
+    assert!(rendered.contains("Continuing workflow steps"));
+    assert!(rendered.contains("Record the baseline."));
+    assert!(rendered.contains("Close the loop."));
+    assert!(!rendered.contains("baseline.Close"));
 }
 
 #[tokio::test]
diff --git a/codex-rs/tui/src/chatwidget/turn_runtime.rs b/codex-rs/tui/src/chatwidget/turn_runtime.rs
index d75e76598a..be679fb6a9 100644
--- a/codex-rs/tui/src/chatwidget/turn_runtime.rs
+++ b/codex-rs/tui/src/chatwidget/turn_runtime.rs
@@ -82,6 +82,7 @@ impl ChatWidget {
         }
         self.reasoning_summary_parts.clear();
         self.reasoning_buffer.clear();
+        self.reasoning_summary_index = None;
         self.set_ambient_pet_notification(
             crate::pets::PetNotificationKind::Running,
             /*body*/ None,
diff --git a/codex-rs/tui/src/history_cell/messages.rs b/codex-rs/tui/src/history_cell/messages.rs
index 89012d1ded..a2755ac1d8 100644
--- a/codex-rs/tui/src/history_cell/messages.rs
+++ b/codex-rs/tui/src/history_cell/messages.rs
@@ -495,70 +495,31 @@ pub(crate) fn new_user_prompt(
 /// Create the reasoning history cell emitted at the end of a reasoning block.
 ///
 /// The helper snapshots `cwd` into the returned cell so local file links render the same way they
-/// did while the turn was live, even if rendering happens after other app state has advanced. Part
-/// boundaries are preserved so standalone empty placeholders can be removed without changing
-/// literal HTML comments or bold-only summary content.
+/// did while the turn was live, even if rendering happens after other app state has advanced.
 pub(crate) fn new_reasoning_summary_block(
-    reasoning_parts: Vec<String>,
+    full_reasoning_buffer: String,
     cwd: &Path,
 ) -> Box<dyn HistoryCell> {
-    let (header, content) = split_reasoning_summary_parts(&reasoning_parts);
-    let transcript_only = header.is_empty();
-    Box::new(ReasoningSummaryCell::new(
-        header,
-        content,
-        cwd,
-        transcript_only,
-    ))
-}
-
-/// Split structured reasoning-summary parts into the status header and renderable content.
-pub(crate) fn split_reasoning_summary_parts(reasoning_parts: &[String]) -> (String, String) {
-    let mut leading_empty_part_header = None;
-    let mut content_parts = Vec::with_capacity(reasoning_parts.len());
-
-    for part in reasoning_parts {
-        let part = part.trim();
-        if part.is_empty() {
-            continue;
-        }
-
-        let header_end = part.strip_prefix("**").and_then(|after_open| {
-            after_open
-                .find("**")
-                .and_then(|close| (close > 0).then_some(close + 4))
-        });
-        let body = header_end.map_or(part, |header_end| &part[header_end..]);
-        if body.trim() == "<!-- -->" {
-            if content_parts.is_empty()
-                && leading_empty_part_header.is_none()
-                && let Some(header_end) = header_end
-            {
-                leading_empty_part_header = Some(part[..header_end].to_string());
+    let cwd = cwd.to_path_buf();
+    let full_reasoning_buffer = full_reasoning_buffer.trim();
+    if let Some(open) = full_reasoning_buffer.find("**") {
+        let after_open = &full_reasoning_buffer[(open + 2)..];
+        if let Some(close) = after_open.find("**") {
+            let after_close_idx = open + 2 + close + 2;
+            if after_close_idx < full_reasoning_buffer.len() {
+                return Box::new(ReasoningSummaryCell::new(
+                    String::new(),
+                    full_reasoning_buffer.to_string(),
+                    &cwd,
+                    /*transcript_only*/ false,
+                ));
             }
-            continue;
         }
-
-        content_parts.push(part);
     }
-
-    let content = content_parts.join("\n\n");
-    if content.is_empty() {
-        return (leading_empty_part_header.unwrap_or_default(), content);
-    }
-
-    if let Some(after_open) = content.strip_prefix("**")
-        && let Some(close) = after_open.find("**")
-    {
-        let after_close_idx = 2 + close + 2;
-        let after_close = &content[after_close_idx..];
-        if after_close.starts_with('\n') || after_close.starts_with('\r') {
-            return (
-                content[..after_close_idx].to_string(),
-                after_close.to_string(),
-            );
-        }
-    }
-
-    (leading_empty_part_header.unwrap_or_default(), content)
+    Box::new(ReasoningSummaryCell::new(
+        String::new(),
+        full_reasoning_buffer.to_string(),
+        &cwd,
+        /*transcript_only*/ true,
+    ))
 }
diff --git a/codex-rs/tui/src/history_cell/tests.rs b/codex-rs/tui/src/history_cell/tests.rs
index 5f5a31a1bc..51be409ee0 100644
--- a/codex-rs/tui/src/history_cell/tests.rs
+++ b/codex-rs/tui/src/history_cell/tests.rs
@@ -2297,10 +2297,24 @@ fn reasoning_summary_block() {
     );
 
     let rendered_display = render_lines(&cell.display_lines(/*width*/ 80));
-    assert_eq!(rendered_display, vec!["■ Detailed reasoning goes here."]);
+    assert_eq!(
+        rendered_display,
+        vec![
+            "■ High level reasoning",
+            "  ",
+            "  Detailed reasoning goes here."
+        ]
+    );
 
     let rendered_transcript = render_transcript(cell.as_ref());
-    assert_eq!(rendered_transcript, vec!["■ Detailed reasoning goes here."]);
+    assert_eq!(
+        rendered_transcript,
+        vec![
+            "■ High level reasoning",
+            "  ",
+            "  Detailed reasoning goes here."
+        ]
+    );
 }
 
 #[test]
@@ -2370,7 +2384,14 @@ async fn reasoning_summary_block_respects_config_overrides() {
     );
 
     let rendered_display = render_lines(&cell.display_lines(/*width*/ 80));
-    assert_eq!(rendered_display, vec!["■ Detailed reasoning goes here."]);
+    assert_eq!(
+        rendered_display,
+        vec![
+            "■ High level reasoning",
+            "  ",
+            "  Detailed reasoning goes here."
+        ]
+    );
 }
 
 #[test]
@@ -2411,10 +2432,38 @@ fn reasoning_summary_block_splits_header_and_summary_when_present() {
     );
 
     let rendered_display = render_lines(&cell.display_lines(/*width*/ 80));
-    assert_eq!(rendered_display, vec!["■ We should fix the bug next."]);
+    assert_eq!(
+        rendered_display,
+        vec!["■ High level plan", "  ", "  We should fix the bug next."]
+    );
 
     let rendered_transcript = render_transcript(cell.as_ref());
-    assert_eq!(rendered_transcript, vec!["■ We should fix the bug next."]);
+    assert_eq!(
+        rendered_transcript,
+        vec!["■ High level plan", "  ", "  We should fix the bug next."]
+    );
+}
+
+#[test]
+fn reasoning_summary_block_keeps_each_section_header_on_its_own_line() {
+    let cell = new_reasoning_summary_block(
+        "**First title**\n\nFirst summary.\n\n**Second title**\n\nSecond summary.".to_string(),
+        &test_cwd(),
+    );
+
+    let rendered_transcript = render_transcript(cell.as_ref());
+    assert_eq!(
+        rendered_transcript,
+        vec![
+            "■ First title",
+            "  ",
+            "  First summary.",
+            "  ",
+            "  Second title",
+            "  ",
+            "  Second summary.",
+        ]
+    );
 }
 
 #[test]
