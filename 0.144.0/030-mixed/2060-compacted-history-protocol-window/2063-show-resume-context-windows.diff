diff --git a/codex-rs/tui/src/app.rs b/codex-rs/tui/src/app.rs
index e664816b0a..985c38e95e 100644
--- a/codex-rs/tui/src/app.rs
+++ b/codex-rs/tui/src/app.rs
@@ -1072,10 +1072,15 @@ See the Codex keymap documentation for supported actions and examples."
         }
         let initial_session_started_at = Instant::now();
         if let Some(started) = initial_started_thread {
-            let thread_id = started.session.thread_id;
+            let AppServerStartedThread {
+                session,
+                turns,
+                history_window_metadata,
+            } = started;
+            let thread_id = session.thread_id;
             app.primary_thread_id = Some(thread_id);
             app.backfill_loaded_subagent_threads(&mut app_server).await;
-            app.enqueue_primary_thread_session(started.session, started.turns)
+            app.enqueue_primary_thread_session(session, turns, history_window_metadata)
                 .await?;
             if should_prompt_for_paused_goal_after_startup_resume {
                 app.maybe_prompt_resume_paused_goal_after_resume(&mut app_server, thread_id)
diff --git a/codex-rs/tui/src/app/session_lifecycle.rs b/codex-rs/tui/src/app/session_lifecycle.rs
index 812345a984..38455f93c3 100644
--- a/codex-rs/tui/src/app/session_lifecycle.rs
+++ b/codex-rs/tui/src/app/session_lifecycle.rs
@@ -519,7 +519,12 @@ impl App {
             .set_queue_submissions_until_session_configured(/*queue*/ false);
         match result {
             Ok(started) => {
-                self.enqueue_primary_thread_session(started.session, started.turns)
+                let AppServerStartedThread {
+                    session,
+                    turns,
+                    history_window_metadata,
+                } = started;
+                self.enqueue_primary_thread_session(session, turns, history_window_metadata)
                     .await?;
                 self.chat_widget.maybe_send_next_queued_input();
             }
@@ -623,9 +628,14 @@ impl App {
             initial_user_message,
         );
         self.replace_chat_widget(ChatWidget::new_with_app_event(init));
-        self.primary_thread_id = Some(started.session.thread_id);
+        let AppServerStartedThread {
+            session,
+            turns,
+            history_window_metadata,
+        } = started;
+        self.primary_thread_id = Some(session.thread_id);
         self.backfill_loaded_subagent_threads(app_server).await;
-        self.enqueue_primary_thread_session(started.session, started.turns)
+        self.enqueue_primary_thread_session(session, turns, history_window_metadata)
             .await?;
         Ok(())
     }
diff --git a/codex-rs/tui/src/app/tests.rs b/codex-rs/tui/src/app/tests.rs
index 78e36bec74..5db7df5859 100644
--- a/codex-rs/tui/src/app/tests.rs
+++ b/codex-rs/tui/src/app/tests.rs
@@ -210,6 +210,7 @@ async fn enqueue_primary_thread_session_replays_buffered_approval_after_attach()
     app.enqueue_primary_thread_session(
         test_thread_session(thread_id, test_path_buf("/tmp/project")),
         Vec::new(),
+        Default::default(),
     )
     .await?;
 
@@ -258,6 +259,7 @@ async fn resolved_buffered_approval_does_not_become_actionable_after_drain() ->
     app.enqueue_primary_thread_session(
         test_thread_session(thread_id, test_path_buf("/tmp/project")),
         Vec::new(),
+        Default::default(),
     )
     .await?;
     while app_event_rx.try_recv().is_ok() {}
@@ -355,6 +357,7 @@ async fn enqueue_primary_thread_session_replays_turns_before_initial_prompt_subm
                 }],
             }],
         )],
+        Default::default(),
     )
     .await?;
 
@@ -3590,6 +3593,7 @@ async fn primary_thread_ignores_child_mcp_startup_notifications() {
         AppServerStartedThread {
             session: test_thread_session(child_thread_id, test_path_buf("/tmp/child")),
             turns: Vec::new(),
+            history_window_metadata: Default::default(),
         },
         &mut child_snapshot,
     )
@@ -4245,8 +4249,12 @@ async fn set_thread_goal_draft_materializes_long_objective_and_confirms_before_p
         .start_thread(app.chat_widget.config_ref())
         .await?;
     let thread_id = started.session.thread_id;
-    app.enqueue_primary_thread_session(started.session, started.turns)
-        .await?;
+    app.enqueue_primary_thread_session(
+        started.session,
+        started.turns,
+        started.history_window_metadata,
+    )
+    .await?;
     let objective = "x".repeat(MAX_THREAD_GOAL_OBJECTIVE_CHARS + 1);
 
     app.set_thread_goal_draft(
@@ -5556,6 +5564,7 @@ async fn refreshed_snapshot_session_persists_resumed_turns() {
         AppServerStartedThread {
             session: resumed_session.clone(),
             turns: resumed_turns.clone(),
+            history_window_metadata: Default::default(),
         },
         &mut snapshot,
     )
@@ -5901,9 +5910,13 @@ async fn interrupt_without_active_turn_is_treated_as_handled() {
             .await
             .expect("thread/start should succeed");
         let thread_id = started.session.thread_id;
-        app.enqueue_primary_thread_session(started.session, started.turns)
-            .await
-            .expect("primary thread should be registered");
+        app.enqueue_primary_thread_session(
+            started.session,
+            started.turns,
+            started.history_window_metadata,
+        )
+        .await
+        .expect("primary thread should be registered");
         let op = AppCommand::interrupt();
 
         let handled = Box::pin(app.try_submit_active_thread_op_via_app_server(
@@ -5934,9 +5947,13 @@ async fn override_turn_context_sends_thread_settings_update() {
         let thread_id = started.session.thread_id;
         let initial_model = started.session.model.clone();
         let initial_effort = started.session.reasoning_effort.clone();
-        app.enqueue_primary_thread_session(started.session, started.turns)
-            .await
-            .expect("primary thread should be registered");
+        app.enqueue_primary_thread_session(
+            started.session,
+            started.turns,
+            started.history_window_metadata,
+        )
+        .await
+        .expect("primary thread should be registered");
         let service_tier = ServiceTier::Fast.request_value().to_string();
         let collaboration_mode = CollaborationMode {
             mode: ModeKind::Plan,
diff --git a/codex-rs/tui/src/app/tests/startup.rs b/codex-rs/tui/src/app/tests/startup.rs
index 51d6622e16..3d9b199d78 100644
--- a/codex-rs/tui/src/app/tests/startup.rs
+++ b/codex-rs/tui/src/app/tests/startup.rs
@@ -162,6 +162,7 @@ async fn startup_thread_started_submits_queued_startup_input() {
         Ok(AppServerStartedThread {
             session: test_thread_session(thread_id, test_path_buf("/tmp/project")),
             turns: Vec::new(),
+            history_window_metadata: Default::default(),
         }),
     )
     .await
@@ -239,6 +240,7 @@ fn stale_startup_thread_started_removes_local_routing_state() -> Result<()> {
                 Ok(AppServerStartedThread {
                     session: test_thread_session(stale_thread_id, test_path_buf("/tmp/project")),
                     turns: Vec::new(),
+                    history_window_metadata: Default::default(),
                 }),
             )
             .await?;
diff --git a/codex-rs/tui/src/app/thread_routing.rs b/codex-rs/tui/src/app/thread_routing.rs
index fe6d3ea979..035136bfb7 100644
--- a/codex-rs/tui/src/app/thread_routing.rs
+++ b/codex-rs/tui/src/app/thread_routing.rs
@@ -21,7 +21,10 @@ struct ResumeHistoryLoadSummary {
     context_windows: u64,
 }
 
-fn resume_history_load_summary(turns: &[Turn]) -> Option<ResumeHistoryLoadSummary> {
+fn resume_history_load_summary(
+    turns: &[Turn],
+    metadata: codex_app_server_protocol::ThreadResumeHistoryWindowMetadata,
+) -> Option<ResumeHistoryLoadSummary> {
     if turns.is_empty() {
         return None;
     }
@@ -52,7 +55,11 @@ fn resume_history_load_summary(turns: &[Turn]) -> Option<ResumeHistoryLoadSummar
         messages,
         turns: visible_turns,
         app_server_turns: turns.len() as u64,
-        context_windows,
+        context_windows: if metadata.loaded_context_windows == 0 {
+            context_windows
+        } else {
+            u64::from(metadata.loaded_context_windows)
+        },
     })
 }
 
@@ -1189,6 +1196,7 @@ impl App {
         &mut self,
         session: ThreadSessionState,
         turns: Vec<Turn>,
+        history_window_metadata: codex_app_server_protocol::ThreadResumeHistoryWindowMetadata,
     ) -> Result<()> {
         let thread_id = session.thread_id;
         self.primary_thread_id = Some(thread_id);
@@ -1206,7 +1214,7 @@ impl App {
         self.chat_widget
             .set_initial_user_message_submit_suppressed(/*suppressed*/ true);
         self.chat_widget.handle_thread_session(session);
-        let resume_history_summary = resume_history_load_summary(&turns);
+        let resume_history_summary = resume_history_load_summary(&turns, history_window_metadata);
         let should_buffer_initial_replay =
             self.terminal_resize_reflow_enabled() && !turns.is_empty();
         if should_buffer_initial_replay {
@@ -1328,7 +1336,11 @@ impl App {
         started: AppServerStartedThread,
         snapshot: &mut ThreadEventSnapshot,
     ) {
-        let AppServerStartedThread { session, turns } = started;
+        let AppServerStartedThread {
+            session,
+            turns,
+            history_window_metadata: _,
+        } = started;
         if let Some(channel) = self.thread_event_channels.get(&thread_id) {
             let mut store = channel.store.lock().await;
             store.set_session(session.clone(), turns.clone());
@@ -1705,38 +1717,41 @@ mod tests {
 
     #[test]
     fn resume_history_summary_counts_visible_turns() {
-        let summary = resume_history_load_summary(&[
-            test_turn(
-                "turn-1",
-                vec![
-                    ThreadItem::UserMessage {
-                        id: "user-1".to_string(),
+        let summary = resume_history_load_summary(
+            &[
+                test_turn(
+                    "turn-1",
+                    vec![
+                        ThreadItem::UserMessage {
+                            id: "user-1".to_string(),
+                            client_id: None,
+                            content: Vec::new(),
+                        },
+                        ThreadItem::AgentMessage {
+                            id: "agent-1".to_string(),
+                            text: "reply".to_string(),
+                            phase: None,
+                            memory_citation: None,
+                        },
+                    ],
+                ),
+                test_turn(
+                    "compact-only",
+                    vec![ThreadItem::ContextCompaction {
+                        id: "compact-1".to_string(),
+                    }],
+                ),
+                test_turn(
+                    "turn-2",
+                    vec![ThreadItem::UserMessage {
+                        id: "user-2".to_string(),
                         client_id: None,
                         content: Vec::new(),
-                    },
-                    ThreadItem::AgentMessage {
-                        id: "agent-1".to_string(),
-                        text: "reply".to_string(),
-                        phase: None,
-                        memory_citation: None,
-                    },
-                ],
-            ),
-            test_turn(
-                "compact-only",
-                vec![ThreadItem::ContextCompaction {
-                    id: "compact-1".to_string(),
-                }],
-            ),
-            test_turn(
-                "turn-2",
-                vec![ThreadItem::UserMessage {
-                    id: "user-2".to_string(),
-                    client_id: None,
-                    content: Vec::new(),
-                }],
-            ),
-        ])
+                    }],
+                ),
+            ],
+            Default::default(),
+        )
         .expect("summary");
 
         assert_eq!(
@@ -1750,6 +1765,26 @@ mod tests {
         );
     }
 
+    #[test]
+    fn resume_history_summary_uses_metadata_window_count_when_present() {
+        let summary = resume_history_load_summary(
+            &[test_turn(
+                "compact-only",
+                vec![ThreadItem::ContextCompaction {
+                    id: "compact-1".to_string(),
+                }],
+            )],
+            codex_app_server_protocol::ThreadResumeHistoryWindowMetadata {
+                loaded_context_windows: 3,
+                ..Default::default()
+            },
+        )
+        .expect("summary");
+
+        assert_eq!(summary.context_windows, 3);
+        assert_eq!(summary.app_server_turns, 1);
+    }
+
     fn test_turn(id: &str, items: Vec<ThreadItem>) -> Turn {
         Turn {
             id: id.to_string(),
diff --git a/codex-rs/tui/src/app_server_session.rs b/codex-rs/tui/src/app_server_session.rs
index a7766918a1..b179b80711 100644
--- a/codex-rs/tui/src/app_server_session.rs
+++ b/codex-rs/tui/src/app_server_session.rs
@@ -209,6 +209,8 @@ impl ThreadParamsMode {
 pub(crate) struct AppServerStartedThread {
     pub(crate) session: ThreadSessionState,
     pub(crate) turns: Vec<Turn>,
+    pub(crate) history_window_metadata:
+        codex_app_server_protocol::ThreadResumeHistoryWindowMetadata,
 }
 
 #[derive(Debug, Clone, PartialEq, Eq)]
@@ -1592,6 +1594,7 @@ async fn started_thread_from_start_response(
     Ok(AppServerStartedThread {
         session,
         turns: response.thread.turns,
+        history_window_metadata: Default::default(),
     })
 }
 
@@ -1607,6 +1610,7 @@ async fn started_thread_from_resume_response(
     Ok(AppServerStartedThread {
         session,
         turns: response.thread.turns,
+        history_window_metadata: response.history_window_metadata,
     })
 }
 
@@ -1622,6 +1626,7 @@ async fn started_thread_from_fork_response(
     Ok(AppServerStartedThread {
         session,
         turns: response.thread.turns,
+        history_window_metadata: Default::default(),
     })
 }
 
