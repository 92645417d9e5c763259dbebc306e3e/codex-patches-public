diff --git a/codex-rs/tui/src/style.rs b/codex-rs/tui/src/style.rs
index 0284df9142..1900434186 100644
--- a/codex-rs/tui/src/style.rs
+++ b/codex-rs/tui/src/style.rs
@@ -11,6 +11,7 @@ use ratatui::style::Style;
 use ratatui::style::Stylize;
 
 const LIGHT_BG_ACCENT_RGB: (u8, u8, u8) = (0, 95, 135);
+const STABLE_DIM_FG: Color = Color::Indexed(244);
 // Decorative table rules should remain visible without competing with cell content.
 const TABLE_SEPARATOR_FG_ALPHA: f32 = 0.20;
 
@@ -32,6 +33,11 @@ pub(crate) fn accent_style() -> Style {
     accent_style_for(default_bg())
 }
 
+/// Returns a low-contrast style that remains gray in terminals where dim text is rendered poorly.
+pub(crate) fn stable_dim_style() -> Style {
+    Style::default().fg(STABLE_DIM_FG)
+}
+
 /// Returns the style for a user-authored message using the provided terminal background.
 pub fn user_message_style_for(terminal_bg: Option<(u8, u8, u8)>) -> Style {
     match terminal_bg {
