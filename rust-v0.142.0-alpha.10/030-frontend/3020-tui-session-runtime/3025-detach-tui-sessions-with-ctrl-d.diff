diff --git a/codex-rs/cli/src/main.rs b/codex-rs/cli/src/main.rs
index bf90b1c475..932a81ebf3 100644
--- a/codex-rs/cli/src/main.rs
+++ b/codex-rs/cli/src/main.rs
@@ -706,6 +706,7 @@ fn format_exit_messages(exit_info: AppExitInfo, color_enabled: bool) -> Vec<Stri
         token_usage,
         thread_id: conversation_id,
         resume_hint,
+        exit_reason,
         ..
     } = exit_info;
 
@@ -720,7 +721,14 @@ fn format_exit_messages(exit_info: AppExitInfo, color_enabled: bool) -> Vec<Stri
         } else {
             resume_cmd
         };
-        lines.push(format!("To continue this session, run {command}"));
+        if matches!(exit_reason, ExitReason::Detached) {
+            if let Some(conversation_id) = conversation_id {
+                lines.push(format!("Detached session: {conversation_id}"));
+            }
+            lines.push(format!("Reattach with: {command}"));
+        } else {
+            lines.push(format!("To continue this session, run {command}"));
+        }
     } else if is_fatal && let Some(conversation_id) = conversation_id {
         lines.push(format!("Session ID: {conversation_id}"));
     }
@@ -735,7 +743,7 @@ fn handle_app_exit(exit_info: AppExitInfo) -> anyhow::Result<()> {
             eprintln!("ERROR: {message}");
             true
         }
-        ExitReason::UserRequested => false,
+        ExitReason::UserRequested | ExitReason::Detached => false,
     };
 
     let update_action = exit_info.update_action;
@@ -3074,6 +3082,14 @@ mod tests {
     }
 
     fn sample_exit_info(conversation_id: Option<&str>, thread_name: Option<&str>) -> AppExitInfo {
+        sample_exit_info_with_reason(conversation_id, thread_name, ExitReason::UserRequested)
+    }
+
+    fn sample_exit_info_with_reason(
+        conversation_id: Option<&str>,
+        thread_name: Option<&str>,
+        exit_reason: ExitReason,
+    ) -> AppExitInfo {
         let token_usage = TokenUsage {
             output_tokens: 2,
             total_tokens: 2,
@@ -3087,7 +3103,7 @@ mod tests {
             thread_id,
             resume_hint: codex_utils_cli::resume_hint(thread_name, thread_id),
             update_action: None,
-            exit_reason: ExitReason::UserRequested,
+            exit_reason,
         }
     }
 
@@ -3182,6 +3198,24 @@ mod tests {
         );
     }
 
+    #[test]
+    fn format_exit_messages_reports_detached_session() {
+        let exit_info = sample_exit_info_with_reason(
+            Some("123e4567-e89b-12d3-a456-426614174000"),
+            /*thread_name*/ None,
+            ExitReason::Detached,
+        );
+        let lines = format_exit_messages(exit_info, /*color_enabled*/ false);
+        assert_eq!(
+            lines,
+            vec![
+                "Token usage: total=2 input=0 output=2".to_string(),
+                "Detached session: 123e4567-e89b-12d3-a456-426614174000".to_string(),
+                "Reattach with: codex resume 123e4567-e89b-12d3-a456-426614174000".to_string(),
+            ]
+        );
+    }
+
     #[test]
     fn resume_model_flag_applies_when_no_root_flags() {
         let interactive =
diff --git a/codex-rs/tui/src/app.rs b/codex-rs/tui/src/app.rs
index 2f598c00af..eabf277274 100644
--- a/codex-rs/tui/src/app.rs
+++ b/codex-rs/tui/src/app.rs
@@ -428,6 +428,7 @@ pub(crate) enum AppRunControl {
 #[derive(Debug, Clone)]
 pub enum ExitReason {
     UserRequested,
+    Detached,
     Fatal(String),
 }
 
diff --git a/codex-rs/tui/src/app/event_dispatch.rs b/codex-rs/tui/src/app/event_dispatch.rs
index 55f976e3b8..78bb483391 100644
--- a/codex-rs/tui/src/app/event_dispatch.rs
+++ b/codex-rs/tui/src/app/event_dispatch.rs
@@ -320,7 +320,9 @@ impl App {
                 }
             }
             AppEvent::Exit(mode) => {
-                if mode == ExitMode::ShutdownFirst {
+                if mode == ExitMode::ShutdownFirst
+                    || (mode == ExitMode::Detach && !self.app_server_target.supports_detach())
+                {
                     self.show_shutdown_feedback(tui)?;
                 }
                 return Ok(self.handle_exit_mode(app_server, mode).await);
@@ -2290,6 +2292,14 @@ impl App {
                 self.pending_shutdown_exit_thread_id = None;
                 AppRunControl::Exit(ExitReason::UserRequested)
             }
+            ExitMode::Detach => {
+                if self.app_server_target.supports_detach() {
+                    self.pending_shutdown_exit_thread_id = None;
+                    AppRunControl::Exit(ExitReason::Detached)
+                } else {
+                    Box::pin(self.handle_exit_mode(app_server, ExitMode::ShutdownFirst)).await
+                }
+            }
             ExitMode::Immediate => {
                 self.pending_shutdown_exit_thread_id = None;
                 AppRunControl::Exit(ExitReason::UserRequested)
diff --git a/codex-rs/tui/src/app/tests.rs b/codex-rs/tui/src/app/tests.rs
index 082dcd1f1c..afb97b7d81 100644
--- a/codex-rs/tui/src/app/tests.rs
+++ b/codex-rs/tui/src/app/tests.rs
@@ -6175,6 +6175,56 @@ async fn shutdown_first_exit_uses_app_server_shutdown_without_submitting_op() {
     );
 }
 
+#[tokio::test]
+async fn detach_exit_falls_back_to_shutdown_first_for_embedded_app_server() {
+    let (mut app, _app_event_rx, mut op_rx) = Box::pin(make_test_app_with_channels()).await;
+    let thread_id = ThreadId::new();
+    app.active_thread_id = Some(thread_id);
+
+    let mut app_server = Box::pin(crate::start_embedded_app_server_for_picker(
+        app.chat_widget.config_ref(),
+    ))
+    .await
+    .expect("embedded app server");
+    let control = Box::pin(app.handle_exit_mode(&mut app_server, ExitMode::Detach)).await;
+
+    assert_eq!(app.pending_shutdown_exit_thread_id, None);
+    assert!(matches!(
+        control,
+        AppRunControl::Exit(ExitReason::UserRequested)
+    ));
+    assert!(
+        op_rx.try_recv().is_err(),
+        "embedded detach fallback should use app-server shutdown, not Op::Shutdown"
+    );
+}
+
+#[tokio::test]
+async fn detach_exit_leaves_local_daemon_thread_running() {
+    let (mut app, _app_event_rx, mut op_rx) = Box::pin(make_test_app_with_channels()).await;
+    let thread_id = ThreadId::new();
+    app.active_thread_id = Some(thread_id);
+    app.app_server_target = crate::AppServerTarget::LocalDaemon {
+        endpoint: crate::RemoteAppServerEndpoint::UnixSocket {
+            socket_path: test_absolute_path("/tmp/codex-test.sock"),
+        },
+    };
+
+    let mut app_server = Box::pin(crate::start_embedded_app_server_for_picker(
+        app.chat_widget.config_ref(),
+    ))
+    .await
+    .expect("embedded app server");
+    let control = Box::pin(app.handle_exit_mode(&mut app_server, ExitMode::Detach)).await;
+
+    assert_eq!(app.pending_shutdown_exit_thread_id, None);
+    assert!(matches!(control, AppRunControl::Exit(ExitReason::Detached)));
+    assert!(
+        op_rx.try_recv().is_err(),
+        "detaching from a separate app-server should not submit shutdown"
+    );
+}
+
 #[tokio::test]
 async fn interrupt_without_active_turn_is_treated_as_handled() {
     Box::pin(async {
diff --git a/codex-rs/tui/src/app_event.rs b/codex-rs/tui/src/app_event.rs
index 6f80bac27b..47f463629d 100644
--- a/codex-rs/tui/src/app_event.rs
+++ b/codex-rs/tui/src/app_event.rs
@@ -1072,6 +1072,11 @@ pub(crate) struct PermissionProfileSelection {
 pub(crate) enum ExitMode {
     /// Shutdown core and exit after completion.
     ShutdownFirst,
+    /// Detach from a thread owned by a separate app-server process.
+    ///
+    /// Embedded sessions fall back to `ShutdownFirst` because there is no
+    /// independent process to keep the thread alive after the TUI exits.
+    Detach,
     /// Exit the UI loop immediately without waiting for shutdown.
     ///
     /// This skips `Op::Shutdown`, so any in-flight work may be dropped and
diff --git a/codex-rs/tui/src/chatwidget.rs b/codex-rs/tui/src/chatwidget.rs
index 3226d6ca3c..6c1fc25709 100644
--- a/codex-rs/tui/src/chatwidget.rs
+++ b/codex-rs/tui/src/chatwidget.rs
@@ -1429,12 +1429,20 @@ impl ChatWidget {
     /// Request a shutdown-first quit.
     ///
     /// This is used for explicit quit commands (`/quit`, `/exit`, `/logout`) and for
-    /// the double-press Ctrl+C/Ctrl+D quit shortcut.
+    /// the double-press Ctrl+C quit shortcut.
     fn request_quit_without_confirmation(&self) {
         self.app_event_tx
             .send(AppEvent::Exit(ExitMode::ShutdownFirst));
     }
 
+    /// Request a detach-or-quit exit.
+    ///
+    /// The app layer decides whether the active app-server target can keep the
+    /// thread alive outside this TUI process.
+    fn request_detach_without_confirmation(&self) {
+        self.app_event_tx.send(AppEvent::Exit(ExitMode::Detach));
+    }
+
     pub(crate) fn show_shutdown_in_progress(&mut self) {
         self.bottom_pane.show_shutdown_in_progress();
     }
diff --git a/codex-rs/tui/src/chatwidget/interaction.rs b/codex-rs/tui/src/chatwidget/interaction.rs
index 4ee2703170..0eebfb1ea3 100644
--- a/codex-rs/tui/src/chatwidget/interaction.rs
+++ b/codex-rs/tui/src/chatwidget/interaction.rs
@@ -462,8 +462,9 @@ impl ChatWidget {
 
     /// Handles a Ctrl+D press at the chat-widget layer.
     ///
-    /// Ctrl-D only participates in quit when the composer is empty and no modal/popup is active.
-    /// Otherwise it should be routed to the active view and not attempt to quit.
+    /// Ctrl-D only participates in detach/quit when the composer is empty and no
+    /// modal/popup is active. Otherwise it should be routed to the active view
+    /// and not attempt to exit.
     fn on_ctrl_d(&mut self) -> bool {
         let key = key_hint::ctrl(KeyCode::Char('d'));
         if !DOUBLE_PRESS_QUIT_SHORTCUT_ENABLED {
@@ -472,14 +473,14 @@ impl ChatWidget {
                 return false;
             }
 
-            self.request_quit_without_confirmation();
+            self.request_detach_without_confirmation();
             return true;
         }
 
         if self.quit_shortcut_active_for(key) {
             self.quit_shortcut_expires_at = None;
             self.quit_shortcut_key = None;
-            self.request_quit_without_confirmation();
+            self.request_detach_without_confirmation();
             return true;
         }
 
diff --git a/codex-rs/tui/src/chatwidget/slash_dispatch.rs b/codex-rs/tui/src/chatwidget/slash_dispatch.rs
index e0f6714278..96b9541931 100644
--- a/codex-rs/tui/src/chatwidget/slash_dispatch.rs
+++ b/codex-rs/tui/src/chatwidget/slash_dispatch.rs
@@ -236,6 +236,12 @@ impl ChatWidget {
             SlashCommand::Resume => {
                 self.app_event_tx.send(AppEvent::OpenResumePicker);
             }
+            SlashCommand::Detach => {
+                self.add_error_message(
+                    "'/detach' is not exposed by this build; use Ctrl+D to detach when supported."
+                        .to_string(),
+                );
+            }
             SlashCommand::Fork => {
                 self.app_event_tx.send(AppEvent::ForkCurrentSession);
             }
@@ -1065,6 +1071,7 @@ impl ChatWidget {
             | SlashCommand::Delete
             | SlashCommand::Clear
             | SlashCommand::Resume
+            | SlashCommand::Detach
             | SlashCommand::Fork
             | SlashCommand::Init
             | SlashCommand::Compact
diff --git a/codex-rs/tui/src/chatwidget/tests/slash_commands.rs b/codex-rs/tui/src/chatwidget/tests/slash_commands.rs
index e3475bc270..077e86d343 100644
--- a/codex-rs/tui/src/chatwidget/tests/slash_commands.rs
+++ b/codex-rs/tui/src/chatwidget/tests/slash_commands.rs
@@ -606,11 +606,11 @@ async fn queued_unknown_slash_reports_error_when_dequeued() {
 }
 
 #[tokio::test]
-async fn ctrl_d_quits_without_prompt() {
+async fn ctrl_d_requests_detach_without_prompt() {
     let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
 
     chat.handle_key_event(KeyEvent::new(KeyCode::Char('d'), KeyModifiers::CONTROL));
-    assert_matches!(rx.try_recv(), Ok(AppEvent::Exit(ExitMode::ShutdownFirst)));
+    assert_matches!(rx.try_recv(), Ok(AppEvent::Exit(ExitMode::Detach)));
 }
 
 #[tokio::test]
@@ -1693,6 +1693,24 @@ async fn slash_quit_requests_exit() {
     assert_matches!(rx.try_recv(), Ok(AppEvent::Exit(ExitMode::ShutdownFirst)));
 }
 
+#[tokio::test]
+async fn slash_detach_is_hidden_shortcut_anchor_only() {
+    let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+
+    chat.dispatch_command(SlashCommand::Detach);
+
+    let cells = drain_insert_history(&mut rx);
+    let rendered = cells
+        .iter()
+        .map(|lines| lines_to_single_string(lines))
+        .collect::<Vec<_>>()
+        .join("\n");
+    assert!(
+        rendered.contains("use Ctrl+D to detach"),
+        "expected hidden /detach guidance, got {rendered:?}"
+    );
+}
+
 #[tokio::test]
 async fn slash_logout_requests_app_server_logout() {
     let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
diff --git a/codex-rs/tui/src/lib.rs b/codex-rs/tui/src/lib.rs
index 6bfe8b6e35..57f6bf0f07 100644
--- a/codex-rs/tui/src/lib.rs
+++ b/codex-rs/tui/src/lib.rs
@@ -271,6 +271,10 @@ impl AppServerTarget {
         matches!(self, Self::Remote { .. })
     }
 
+    pub(crate) fn supports_detach(&self) -> bool {
+        !matches!(self, Self::Embedded)
+    }
+
     fn thread_params_mode(&self) -> ThreadParamsMode {
         if self.uses_remote_workspace() {
             ThreadParamsMode::Remote
diff --git a/codex-rs/tui/src/main.rs b/codex-rs/tui/src/main.rs
index 814683e331..d20139b42a 100644
--- a/codex-rs/tui/src/main.rs
+++ b/codex-rs/tui/src/main.rs
@@ -16,6 +16,7 @@ fn format_exit_messages(exit_info: AppExitInfo, color_enabled: bool) -> Vec<Stri
         token_usage,
         thread_id,
         resume_hint,
+        exit_reason,
         ..
     } = exit_info;
 
@@ -30,7 +31,14 @@ fn format_exit_messages(exit_info: AppExitInfo, color_enabled: bool) -> Vec<Stri
         } else {
             resume_cmd
         };
-        lines.push(format!("To continue this session, run {command}"));
+        if matches!(exit_reason, ExitReason::Detached) {
+            if let Some(thread_id) = thread_id {
+                lines.push(format!("Detached session: {thread_id}"));
+            }
+            lines.push(format!("Reattach with: {command}"));
+        } else {
+            lines.push(format!("To continue this session, run {command}"));
+        }
     } else if is_fatal && let Some(thread_id) = thread_id {
         lines.push(format!("Session ID: {thread_id}"));
     }
@@ -68,7 +76,7 @@ fn main() -> anyhow::Result<()> {
                 eprintln!("ERROR: {message}");
                 true
             }
-            ExitReason::UserRequested => false,
+            ExitReason::UserRequested | ExitReason::Detached => false,
         };
 
         let color_enabled = supports_color::on(Stream::Stdout).is_some();
diff --git a/codex-rs/tui/src/slash_command.rs b/codex-rs/tui/src/slash_command.rs
index 5e3e906bae..b68ffe416f 100644
--- a/codex-rs/tui/src/slash_command.rs
+++ b/codex-rs/tui/src/slash_command.rs
@@ -34,6 +34,7 @@ pub enum SlashCommand {
     Archive,
     Delete,
     Resume,
+    Detach,
     Fork,
     App,
     Init,
@@ -91,6 +92,7 @@ impl SlashCommand {
             SlashCommand::Resume => "resume a saved chat",
             SlashCommand::Archive => "archive this session and exit",
             SlashCommand::Delete => "permanently delete this session and exit",
+            SlashCommand::Detach => "detach from the current chat",
             SlashCommand::Clear => "clear the terminal and start a new chat",
             SlashCommand::Fork => "fork the current chat",
             SlashCommand::App => "continue this session in Codex Desktop",
@@ -190,9 +192,14 @@ impl SlashCommand {
             SlashCommand::New
             | SlashCommand::Archive
             | SlashCommand::Delete
+            | SlashCommand::Resume
+            | SlashCommand::Detach
             | SlashCommand::Fork
             | SlashCommand::Init
             | SlashCommand::Compact
+            | SlashCommand::Model
+            | SlashCommand::Personality
+            | SlashCommand::Permissions
             | SlashCommand::Keymap
             | SlashCommand::Vim
             | SlashCommand::ElevateSandbox
@@ -207,10 +214,6 @@ impl SlashCommand {
             | SlashCommand::MemoryDrop
             | SlashCommand::MemoryUpdate => false,
             SlashCommand::Diff
-            | SlashCommand::Resume
-            | SlashCommand::Model
-            | SlashCommand::Personality
-            | SlashCommand::Permissions
             | SlashCommand::Copy
             | SlashCommand::Raw
             | SlashCommand::Rename
@@ -249,6 +252,7 @@ impl SlashCommand {
             SlashCommand::Copy => !cfg!(target_os = "android"),
             SlashCommand::App => cfg!(any(target_os = "macos", target_os = "windows")),
             SlashCommand::Rollout | SlashCommand::TestApproval => cfg!(debug_assertions),
+            SlashCommand::Detach => false,
             _ => true,
         }
     }
