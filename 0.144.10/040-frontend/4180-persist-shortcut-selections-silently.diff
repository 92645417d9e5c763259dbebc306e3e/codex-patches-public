diff --git a/codex-rs/tui/src/app/event_dispatch.rs b/codex-rs/tui/src/app/event_dispatch.rs
index f23e29a536..181b79727b 100644
--- a/codex-rs/tui/src/app/event_dispatch.rs
+++ b/codex-rs/tui/src/app/event_dispatch.rs
@@ -1503,38 +1503,12 @@ impl App {
                 }
             }
             AppEvent::PersistModelSelection { model, effort } => {
-                match crate::config_update::write_config_batch(
-                    app_server.request_handle(),
-                    crate::config_update::build_model_selection_edits(
-                        model.as_str(),
-                        effort.as_ref(),
-                    ),
-                )
-                .await
-                {
-                    Ok(_) => {
-                        let effort_label = effort
-                            .as_ref()
-                            .map(std::string::ToString::to_string)
-                            .unwrap_or_else(|| "default".to_string());
-                        tracing::info!("Selected model: {model}, Selected effort: {effort_label}");
-                        let mut message = format!("Model changed to {model}");
-                        if let Some(label) = Self::reasoning_label_for(&model, effort.as_ref()) {
-                            message.push(' ');
-                            message.push_str(&label);
-                        }
-                        self.chat_widget.add_info_message(message, /*hint*/ None);
-                    }
-                    Err(err) => {
-                        let error = format_config_error(&err);
-                        tracing::error!(
-                            error = %error,
-                            "failed to persist model selection"
-                        );
-                        self.chat_widget
-                            .add_error_message(format!("Failed to save default model: {error}"));
-                    }
-                }
+                self.persist_model_selection(app_server, model, effort, /*notify*/ true)
+                    .await;
+            }
+            AppEvent::PersistModelSelectionSilently { model, effort } => {
+                self.persist_model_selection(app_server, model, effort, /*notify*/ false)
+                    .await;
             }
             AppEvent::PluginUninstallLoaded {
                 cwd,
@@ -2322,6 +2296,46 @@ impl App {
         }
     }
 
+    async fn persist_model_selection(
+        &mut self,
+        app_server: &mut AppServerSession,
+        model: String,
+        effort: Option<ReasoningEffortConfig>,
+        notify: bool,
+    ) {
+        match crate::config_update::write_config_batch(
+            app_server.request_handle(),
+            crate::config_update::build_model_selection_edits(model.as_str(), effort.as_ref()),
+        )
+        .await
+        {
+            Ok(_) => {
+                let effort_label = effort
+                    .as_ref()
+                    .map(std::string::ToString::to_string)
+                    .unwrap_or_else(|| "default".to_string());
+                tracing::info!("Selected model: {model}, Selected effort: {effort_label}");
+                if notify {
+                    let mut message = format!("Model changed to {model}");
+                    if let Some(label) = Self::reasoning_label_for(&model, effort.as_ref()) {
+                        message.push(' ');
+                        message.push_str(&label);
+                    }
+                    self.chat_widget.add_info_message(message, /*hint*/ None);
+                }
+            }
+            Err(err) => {
+                let error = format_config_error(&err);
+                tracing::error!(
+                    error = %error,
+                    "failed to persist model selection"
+                );
+                self.chat_widget
+                    .add_error_message(format!("Failed to save default model: {error}"));
+            }
+        }
+    }
+
     pub(super) async fn handle_exit_mode(
         &mut self,
         app_server: &mut AppServerSession,
diff --git a/codex-rs/tui/src/app_event.rs b/codex-rs/tui/src/app_event.rs
index f1a2d6de60..1631cedd83 100644
--- a/codex-rs/tui/src/app_event.rs
+++ b/codex-rs/tui/src/app_event.rs
@@ -687,6 +687,12 @@ pub(crate) enum AppEvent {
         effort: Option<ReasoningEffort>,
     },
 
+    /// Persist a shortcut-selected model and effort without adding transcript output.
+    PersistModelSelectionSilently {
+        model: String,
+        effort: Option<ReasoningEffort>,
+    },
+
     /// Persist the selected personality to the appropriate config.
     PersistPersonalitySelection {
         personality: Personality,
diff --git a/codex-rs/tui/src/chatwidget/model_shortcuts.rs b/codex-rs/tui/src/chatwidget/model_shortcuts.rs
index 15e5d6fec4..3383845316 100644
--- a/codex-rs/tui/src/chatwidget/model_shortcuts.rs
+++ b/codex-rs/tui/src/chatwidget/model_shortcuts.rs
@@ -49,10 +49,11 @@ impl ChatWidget {
         self.app_event_tx.send(AppEvent::UpdateReasoningEffort(Some(
             next_preset.default_reasoning_effort.clone(),
         )));
-        self.app_event_tx.send(AppEvent::PersistModelSelection {
-            model: next_preset.model,
-            effort: Some(next_preset.default_reasoning_effort),
-        });
+        self.app_event_tx
+            .send(AppEvent::PersistModelSelectionSilently {
+                model: next_preset.model,
+                effort: Some(next_preset.default_reasoning_effort),
+            });
 
         true
     }
diff --git a/codex-rs/tui/src/chatwidget/reasoning_shortcuts.rs b/codex-rs/tui/src/chatwidget/reasoning_shortcuts.rs
index 92c391fec0..4a07faeaed 100644
--- a/codex-rs/tui/src/chatwidget/reasoning_shortcuts.rs
+++ b/codex-rs/tui/src/chatwidget/reasoning_shortcuts.rs
@@ -164,10 +164,11 @@ impl ChatWidget {
                 current_model.clone(),
                 Some(next_effort.clone()),
             );
-            self.app_event_tx.send(AppEvent::PersistModelSelection {
-                model: current_model,
-                effort: Some(next_effort),
-            });
+            self.app_event_tx
+                .send(AppEvent::PersistModelSelectionSilently {
+                    model: current_model,
+                    effort: Some(next_effort),
+                });
         }
 
         true
diff --git a/codex-rs/tui/src/chatwidget/tests/plan_mode.rs b/codex-rs/tui/src/chatwidget/tests/plan_mode.rs
index 1e5b01f552..e54c2182b3 100644
--- a/codex-rs/tui/src/chatwidget/tests/plan_mode.rs
+++ b/codex-rs/tui/src/chatwidget/tests/plan_mode.rs
@@ -421,9 +421,10 @@ async fn reasoning_shortcut_in_plan_mode_updates_and_persists_plan_override_with
         "expected Plan reasoning persistence event; events: {events:?}"
     );
     assert!(
-        events
-            .iter()
-            .all(|event| !matches!(event, AppEvent::PersistModelSelection { .. })),
+        events.iter().all(|event| !matches!(
+            event,
+            AppEvent::PersistModelSelection { .. } | AppEvent::PersistModelSelectionSilently { .. }
+        )),
         "expected no global model persistence event; events: {events:?}"
     );
     assert!(
@@ -467,6 +468,7 @@ async fn advanced_reasoning_selection_in_plan_mode_uses_expected_scope() {
                 AppEvent::OpenPlanReasoningScopePrompt { .. }
                     | AppEvent::PersistPlanModeReasoningEffort(_)
                     | AppEvent::PersistModelSelection { .. }
+                    | AppEvent::PersistModelSelectionSilently { .. }
             )));
         } else {
             assert!(events.iter().any(|event| matches!(
diff --git a/codex-rs/tui/src/chatwidget/tests/popups_and_settings.rs b/codex-rs/tui/src/chatwidget/tests/popups_and_settings.rs
index f2333c3049..9abb879f16 100644
--- a/codex-rs/tui/src/chatwidget/tests/popups_and_settings.rs
+++ b/codex-rs/tui/src/chatwidget/tests/popups_and_settings.rs
@@ -3430,7 +3430,7 @@ async fn assert_reasoning_shortcuts_update_effort(
         assert!(
             events.iter().any(|event| matches!(
                 event,
-                AppEvent::PersistModelSelection {
+                AppEvent::PersistModelSelectionSilently {
                     model,
                     effort: Some(effort),
                 } if model == "gpt-5.4" && effort == &expected_effort
@@ -3504,9 +3504,10 @@ async fn reasoning_shortcut_is_ignored_with_model_popup_open() {
         "did not expect reasoning update while popup is active; events: {events:?}"
     );
     assert!(
-        events
-            .iter()
-            .all(|event| !matches!(event, AppEvent::PersistModelSelection { .. })),
+        events.iter().all(|event| !matches!(
+            event,
+            AppEvent::PersistModelSelection { .. } | AppEvent::PersistModelSelectionSilently { .. }
+        )),
         "did not expect model persistence while popup is active; events: {events:?}"
     );
 }
@@ -3595,7 +3596,7 @@ async fn reasoning_down_shortcut_can_leave_advanced_effort() {
         )));
         assert!(events.iter().any(|event| matches!(
             event,
-            AppEvent::PersistModelSelection {
+            AppEvent::PersistModelSelectionSilently {
                 model,
                 effort: Some(effort),
             } if model == "gpt-5.4" && effort == &expected
@@ -3655,7 +3656,7 @@ async fn alt_apostrophe_selects_next_model_without_submitting_prompt() {
     assert!(
         events.iter().any(|event| matches!(
             event,
-            AppEvent::PersistModelSelection {
+            AppEvent::PersistModelSelectionSilently {
                 model,
                 effort: Some(ReasoningEffortConfig::Medium),
             } if model == "gpt-5.2"
@@ -3717,7 +3718,7 @@ async fn alt_semicolon_selects_previous_model_with_wraparound() {
     assert!(
         events.iter().any(|event| matches!(
             event,
-            AppEvent::PersistModelSelection {
+            AppEvent::PersistModelSelectionSilently {
                 model,
                 effort: Some(ReasoningEffortConfig::Medium),
             } if model == "gpt-5.2"
@@ -3813,6 +3814,7 @@ async fn advanced_only_reasoning_option_requires_explicit_selection() {
         AppEvent::UpdateReasoningEffort(_)
             | AppEvent::ApplyAdvancedReasoning { .. }
             | AppEvent::PersistModelSelection { .. }
+            | AppEvent::PersistModelSelectionSilently { .. }
     )));
 }
 
@@ -3840,6 +3842,7 @@ async fn auto_model_advertising_advanced_effort_opens_reasoning_picker() {
         AppEvent::UpdateReasoningEffort(_)
             | AppEvent::ApplyAdvancedReasoning { .. }
             | AppEvent::PersistModelSelection { .. }
+            | AppEvent::PersistModelSelectionSilently { .. }
     )));
     assert!(
         events
