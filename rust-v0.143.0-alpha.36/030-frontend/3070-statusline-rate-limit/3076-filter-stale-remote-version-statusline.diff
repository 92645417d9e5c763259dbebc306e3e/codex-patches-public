diff --git a/codex-rs/tui/src/app.rs b/codex-rs/tui/src/app.rs
index 2f879c928f..054e84b8a0 100644
--- a/codex-rs/tui/src/app.rs
+++ b/codex-rs/tui/src/app.rs
@@ -816,10 +816,15 @@ impl App {
         }
         let mut model = config.model.clone().unwrap_or(bootstrap.default_model);
         let available_models = bootstrap.available_models;
-        let remote_connection = crate::status::remote_connection::remote_connection_status_value(
-            &app_server_target,
-            app_server.server_version(),
-        );
+        let remote_connection = app_server
+            .server_version()
+            .filter(|version| *version == CODEX_CLI_VERSION)
+            .and_then(|version| {
+                crate::status::remote_connection::remote_connection_status_value(
+                    &app_server_target,
+                    Some(version),
+                )
+            });
         let exit_info = handle_model_migration_prompt_if_needed(
             tui,
             &mut config,
diff --git a/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs b/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs
index 4a728fe65a..546d7bef69 100644
--- a/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs
+++ b/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs
@@ -3,6 +3,7 @@ use crate::bottom_pane::goal_status_indicator_line;
 use crate::chatwidget::rate_limits::NUDGE_MODEL_SLUG;
 use crate::chatwidget::rate_limits::get_limits_duration;
 use crate::status::remote_connection::RemoteConnectionStatus;
+use crate::version::CODEX_CLI_VERSION;
 use codex_app_server_protocol::SpendControlLimitSnapshot;
 use pretty_assertions::assert_eq;
 use ratatui::backend::TestBackend;
@@ -709,12 +710,12 @@ async fn status_line_codex_version_uses_remote_connection_version() {
     let (mut chat, _rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
     chat.remote_connection = Some(RemoteConnectionStatus {
         address: "unix:///tmp/codex.sock".to_string(),
-        version: Some("0.135.0".to_string()),
+        version: Some(CODEX_CLI_VERSION.to_string()),
     });
 
     assert_eq!(
         chat.status_line_value_for_item(crate::bottom_pane::StatusLineItem::CodexVersion),
-        Some(format!("{CODEX_CLI_VERSION} 0.135.0"))
+        Some(CODEX_CLI_VERSION.to_string())
     );
 }
 
