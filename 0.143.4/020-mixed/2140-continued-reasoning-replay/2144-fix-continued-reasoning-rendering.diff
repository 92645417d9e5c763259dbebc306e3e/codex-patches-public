diff --git a/codex-rs/tui/src/chatwidget/tests/history_replay.rs b/codex-rs/tui/src/chatwidget/tests/history_replay.rs
index faf9ddf520..f8c6096fd7 100644
--- a/codex-rs/tui/src/chatwidget/tests/history_replay.rs
+++ b/codex-rs/tui/src/chatwidget/tests/history_replay.rs
@@ -926,6 +926,7 @@ async fn replayed_reasoning_item_hides_raw_reasoning_when_disabled() {
             id: "reasoning-1".to_string(),
             summary: vec!["Summary only".to_string()],
             content: vec!["Raw reasoning".to_string()],
+            continued: false,
         },
         "turn-1".to_string(),
         ReplayKind::ThreadSnapshot,
@@ -977,6 +978,7 @@ async fn replayed_reasoning_summary_parts_render_as_separate_sections() {
                 "**Continuing workflow steps**\n\nClose the loop.".to_string(),
             ],
             content: Vec::new(),
+            continued: false,
         },
         "turn-1".to_string(),
         ReplayKind::ThreadSnapshot,
@@ -995,6 +997,60 @@ async fn replayed_reasoning_summary_parts_render_as_separate_sections() {
     assert!(!rendered.contains("baseline.Close"));
 }
 
+#[tokio::test]
+async fn replayed_continued_reasoning_summary_uses_orange_style() {
+    let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+    chat.config.show_raw_agent_reasoning = false;
+    chat.handle_thread_session(crate::session_state::ThreadSessionState {
+        thread_id: ThreadId::new(),
+        forked_from_id: None,
+        fork_parent_title: None,
+        thread_name: None,
+        model: "test-model".to_string(),
+        model_provider_id: "test-provider".to_string(),
+        service_tier: None,
+        approval_policy: AskForApproval::Never,
+        approvals_reviewer: ApprovalsReviewer::User,
+        permission_profile: PermissionProfile::read_only(),
+        active_permission_profile: None,
+        cwd: test_project_path().abs(),
+        runtime_workspace_roots: Vec::new(),
+        instruction_source_paths: Vec::new(),
+        reasoning_effort: None,
+        collaboration_mode: None,
+        personality: None,
+        message_history: None,
+        network_proxy: None,
+        rollout_path: None,
+    });
+    let _ = drain_insert_history(&mut rx);
+
+    chat.replay_thread_item(
+        AppServerThreadItem::Reasoning {
+            id: "reasoning-1".to_string(),
+            summary: vec!["Summary only".to_string()],
+            content: Vec::new(),
+            continued: true,
+        },
+        "turn-1".to_string(),
+        ReplayKind::ThreadSnapshot,
+    );
+
+    let cell = match rx.try_recv() {
+        Ok(AppEvent::InsertHistoryCell(cell)) => cell,
+        other => panic!("expected InsertHistoryCell, got {other:?}"),
+    };
+    let lines = cell.transcript_lines(/*width*/ 80);
+    assert_eq!(
+        lines[0].spans[1].style.fg,
+        Some(ratatui::style::Color::Rgb(255, 128, 0))
+    );
+    assert_eq!(
+        lines[0].spans[1].style.bg,
+        Some(ratatui::style::Color::Reset)
+    );
+}
+
 #[tokio::test]
 async fn replayed_reasoning_item_shows_raw_reasoning_when_enabled() {
     let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
@@ -1028,6 +1084,7 @@ async fn replayed_reasoning_item_shows_raw_reasoning_when_enabled() {
             id: "reasoning-1".to_string(),
             summary: vec!["Summary only".to_string()],
             content: vec!["Raw reasoning".to_string()],
+            continued: false,
         },
         "turn-1".to_string(),
         ReplayKind::ThreadSnapshot,
@@ -1114,6 +1171,7 @@ async fn live_reasoning_summary_is_not_rendered_twice_when_item_completes() {
                 id: "reasoning-1".to_string(),
                 summary: vec!["Summary only".to_string()],
                 content: Vec::new(),
+                continued: false,
             },
         }),
         /*replay_kind*/ None,
@@ -1185,6 +1243,7 @@ async fn live_reasoning_summary_sections_render_titles_on_separate_lines() {
                     "**Continuing workflow steps**\n\nClose the loop.".to_string(),
                 ],
                 content: Vec::new(),
+                continued: false,
             },
         }),
         /*replay_kind*/ None,
diff --git a/codex-rs/tui/src/history_cell/messages.rs b/codex-rs/tui/src/history_cell/messages.rs
index a2755ac1d8..ba7d9e4c59 100644
--- a/codex-rs/tui/src/history_cell/messages.rs
+++ b/codex-rs/tui/src/history_cell/messages.rs
@@ -221,17 +221,25 @@ pub(crate) struct ReasoningSummaryCell {
     /// Session cwd used to render local file links inside the reasoning body.
     cwd: PathBuf,
     transcript_only: bool,
+    continued: bool,
 }
 
 impl ReasoningSummaryCell {
     /// Create a reasoning summary cell that will render local file links relative to the session
     /// cwd active when the summary was recorded.
-    pub(crate) fn new(header: String, content: String, cwd: &Path, transcript_only: bool) -> Self {
+    pub(crate) fn new(
+        header: String,
+        content: String,
+        cwd: &Path,
+        transcript_only: bool,
+        continued: bool,
+    ) -> Self {
         Self {
             _header: header,
             content,
             cwd: cwd.to_path_buf(),
             transcript_only,
+            continued,
         }
     }
 
@@ -243,14 +251,23 @@ impl ReasoningSummaryCell {
             Some(self.cwd.as_path()),
             &mut lines,
         );
-        let summary_style = Style::default().dim().italic();
+        let summary_style = if self.continued {
+            Style::default().fg(Color::Rgb(255, 128, 0)).italic()
+        } else {
+            Style::default().dim().italic()
+        };
         let summary_lines = lines
             .into_iter()
             .map(|mut line| {
+                line.style = Style::default();
                 line.spans = line
                     .spans
                     .into_iter()
-                    .map(|span| span.patch_style(summary_style))
+                    .map(|span| {
+                        let mut style = span.style.patch(summary_style);
+                        style.bg = Some(Color::Reset);
+                        Span::styled(span.content, style)
+                    })
                     .collect();
                 line
             })
@@ -499,6 +516,7 @@ pub(crate) fn new_user_prompt(
 pub(crate) fn new_reasoning_summary_block(
     full_reasoning_buffer: String,
     cwd: &Path,
+    continued: bool,
 ) -> Box<dyn HistoryCell> {
     let cwd = cwd.to_path_buf();
     let full_reasoning_buffer = full_reasoning_buffer.trim();
@@ -512,6 +530,7 @@ pub(crate) fn new_reasoning_summary_block(
                     full_reasoning_buffer.to_string(),
                     &cwd,
                     /*transcript_only*/ false,
+                    continued,
                 ));
             }
         }
@@ -521,5 +540,6 @@ pub(crate) fn new_reasoning_summary_block(
         full_reasoning_buffer.to_string(),
         &cwd,
         /*transcript_only*/ true,
+        continued,
     ))
 }
diff --git a/codex-rs/tui/src/history_cell/tests.rs b/codex-rs/tui/src/history_cell/tests.rs
index efd599a6c5..aa7d31fa07 100644
--- a/codex-rs/tui/src/history_cell/tests.rs
+++ b/codex-rs/tui/src/history_cell/tests.rs
@@ -238,6 +238,7 @@ fn source_backed_cells_render_raw_source_without_prefix_or_style() {
         "first thought\n\nsecond thought".to_string(),
         &test_cwd(),
         /*transcript_only*/ false,
+        /*continued*/ false,
     );
     let plan = new_proposed_plan(
         "1. Inspect\n\n```sh\ncargo test\n```".to_string(),
@@ -2294,6 +2295,7 @@ fn reasoning_summary_block() {
     let cell = new_reasoning_summary_block(
         "**High level reasoning**\n\nDetailed reasoning goes here.".to_string(),
         &test_cwd(),
+        /*continued*/ false,
     );
 
     let rendered_display = render_lines(&cell.display_lines(/*width*/ 80));
@@ -2325,6 +2327,7 @@ fn reasoning_summary_height_matches_wrapped_rendering_for_url_like_content() {
         summary.to_string(),
         &test_cwd(),
         /*transcript_only*/ false,
+        /*continued*/ false,
     ));
     let width: u16 = 24;
 
@@ -2364,8 +2367,11 @@ fn reasoning_summary_height_matches_wrapped_rendering_for_url_like_content() {
 
 #[test]
 fn reasoning_summary_block_returns_reasoning_cell_when_feature_disabled() {
-    let cell =
-        new_reasoning_summary_block("Detailed reasoning goes here.".to_string(), &test_cwd());
+    let cell = new_reasoning_summary_block(
+        "Detailed reasoning goes here.".to_string(),
+        &test_cwd(),
+        /*continued*/ false,
+    );
 
     let rendered = render_transcript(cell.as_ref());
     assert_eq!(rendered, vec!["■ Detailed reasoning goes here."]);
@@ -2379,6 +2385,7 @@ async fn reasoning_summary_block_respects_config_overrides() {
     let cell = new_reasoning_summary_block(
         "**High level reasoning**\n\nDetailed reasoning goes here.".to_string(),
         &test_cwd(),
+        /*continued*/ false,
     );
 
     let rendered_display = render_lines(&cell.display_lines(/*width*/ 80));
@@ -2397,6 +2404,7 @@ fn reasoning_summary_block_falls_back_when_header_is_missing() {
     let cell = new_reasoning_summary_block(
         "**High level reasoning without closing".to_string(),
         &test_cwd(),
+        /*continued*/ false,
     );
 
     let rendered = render_transcript(cell.as_ref());
@@ -2408,6 +2416,7 @@ fn reasoning_summary_block_falls_back_when_summary_is_missing() {
     let cell = new_reasoning_summary_block(
         "**High level reasoning without closing**".to_string(),
         &test_cwd(),
+        /*continued*/ false,
     );
 
     let rendered = render_transcript(cell.as_ref());
@@ -2416,6 +2425,7 @@ fn reasoning_summary_block_falls_back_when_summary_is_missing() {
     let cell = new_reasoning_summary_block(
         "**High level reasoning without closing**\n\n  ".to_string(),
         &test_cwd(),
+        /*continued*/ false,
     );
 
     let rendered = render_transcript(cell.as_ref());
@@ -2427,6 +2437,7 @@ fn reasoning_summary_block_splits_header_and_summary_when_present() {
     let cell = new_reasoning_summary_block(
         "**High level plan**\n\nWe should fix the bug next.".to_string(),
         &test_cwd(),
+        /*continued*/ false,
     );
 
     let rendered_display = render_lines(&cell.display_lines(/*width*/ 80));
@@ -2447,6 +2458,7 @@ fn reasoning_summary_block_keeps_each_section_header_on_its_own_line() {
     let cell = new_reasoning_summary_block(
         "**First title**\n\nFirst summary.\n\n**Second title**\n\nSecond summary.".to_string(),
         &test_cwd(),
+        /*continued*/ false,
     );
 
     let rendered_transcript = render_transcript(cell.as_ref());
@@ -2464,6 +2476,19 @@ fn reasoning_summary_block_keeps_each_section_header_on_its_own_line() {
     );
 }
 
+#[test]
+fn continued_reasoning_summary_uses_orange_text() {
+    let cell = new_reasoning_summary_block(
+        "Detailed reasoning goes here.".to_string(),
+        &test_cwd(),
+        /*continued*/ true,
+    );
+
+    let lines = cell.transcript_lines(/*width*/ 80);
+    assert_eq!(lines[0].spans[1].style.fg, Some(Color::Rgb(255, 128, 0)));
+    assert_eq!(lines[0].spans[1].style.bg, Some(Color::Reset));
+}
+
 #[test]
 fn deprecation_notice_renders_summary_with_details() {
     let cell = new_deprecation_notice(
@@ -2567,6 +2592,7 @@ fn wrapped_and_prefixed_cells_handle_tiny_widths() {
         "Reasoning summary content for tiny widths.".to_string(),
         &test_cwd(),
         /*transcript_only*/ false,
+        /*continued*/ false,
     );
     let agent_markdown_cell =
         AgentMarkdownCell::new("tiny width agent markdown line\n".to_string(), &test_cwd());
