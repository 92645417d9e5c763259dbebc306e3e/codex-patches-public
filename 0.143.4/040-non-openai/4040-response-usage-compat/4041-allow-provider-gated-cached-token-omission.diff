diff --git a/codex-rs/codex-api/src/endpoint/responses.rs b/codex-rs/codex-api/src/endpoint/responses.rs
index 804f0027ff..e804be5b1c 100644
--- a/codex-rs/codex-api/src/endpoint/responses.rs
+++ b/codex-rs/codex-api/src/endpoint/responses.rs
@@ -8,6 +8,7 @@ use crate::requests::Compression;
 use crate::requests::headers::build_session_headers;
 use crate::requests::headers::insert_header;
 use crate::requests::headers::subagent_header;
+use crate::sse::responses::ResponseParseCompatibility;
 use crate::sse::spawn_response_stream;
 use crate::telemetry::SseTelemetry;
 use codex_client::EncodedJsonBody;
@@ -36,6 +37,7 @@ pub struct ResponsesOptions {
     pub extra_headers: HeaderMap,
     pub compression: Compression,
     pub turn_state: Option<Arc<OnceLock<String>>>,
+    pub response_parse_compatibility: ResponseParseCompatibility,
 }
 
 impl<T: HttpTransport> ResponsesClient<T> {
@@ -79,6 +81,7 @@ impl<T: HttpTransport> ResponsesClient<T> {
             extra_headers,
             compression,
             turn_state,
+            response_parse_compatibility,
         } = options;
 
         let body = EncodedJsonBody::encode(&request)
@@ -93,8 +96,14 @@ impl<T: HttpTransport> ResponsesClient<T> {
             insert_header(&mut headers, "x-openai-subagent", &subagent);
         }
 
-        self.stream_encoded(body, headers, compression, turn_state)
-            .await
+        self.stream_encoded(
+            body,
+            headers,
+            compression,
+            turn_state,
+            response_parse_compatibility,
+        )
+        .await
     }
 
     fn path() -> &'static str {
@@ -121,8 +130,14 @@ impl<T: HttpTransport> ResponsesClient<T> {
     ) -> Result<ResponseStream, ApiError> {
         let body = EncodedJsonBody::encode(&body)
             .map_err(|e| ApiError::Stream(format!("failed to encode responses request: {e}")))?;
-        self.stream_encoded(body, extra_headers, compression, turn_state)
-            .await
+        self.stream_encoded(
+            body,
+            extra_headers,
+            compression,
+            turn_state,
+            ResponseParseCompatibility::default(),
+        )
+        .await
     }
 
     async fn stream_encoded(
@@ -131,6 +146,7 @@ impl<T: HttpTransport> ResponsesClient<T> {
         extra_headers: HeaderMap,
         compression: Compression,
         turn_state: Option<Arc<OnceLock<String>>>,
+        response_parse_compatibility: ResponseParseCompatibility,
     ) -> Result<ResponseStream, ApiError> {
         let request_compression = match compression {
             Compression::None => RequestCompression::None,
@@ -159,6 +175,7 @@ impl<T: HttpTransport> ResponsesClient<T> {
             self.session.provider().stream_idle_timeout,
             self.sse_telemetry.clone(),
             turn_state,
+            response_parse_compatibility,
         ))
     }
 }
diff --git a/codex-rs/codex-api/src/sse/responses.rs b/codex-rs/codex-api/src/sse/responses.rs
index 723dbe64e8..4445aace54 100644
--- a/codex-rs/codex-api/src/sse/responses.rs
+++ b/codex-rs/codex-api/src/sse/responses.rs
@@ -31,11 +31,17 @@ const OPENAI_MODEL_HEADER: &str = "openai-model";
 const REQUEST_ID_HEADER: &str = "x-request-id";
 const TRUSTED_ACCESS_FOR_CYBER_VERIFICATION: &str = "trusted_access_for_cyber";
 
+#[derive(Debug, Clone, Copy, Default)]
+pub struct ResponseParseCompatibility {
+    pub cached_tokens_optional: bool,
+}
+
 pub fn spawn_response_stream(
     stream_response: StreamResponse,
     idle_timeout: Duration,
     telemetry: Option<Arc<dyn SseTelemetry>>,
     turn_state: Option<Arc<OnceLock<String>>>,
+    compatibility: ResponseParseCompatibility,
 ) -> ResponseStream {
     let rate_limit_snapshots = parse_all_rate_limits(&stream_response.headers);
     let models_etag = stream_response
@@ -89,6 +95,7 @@ pub fn spawn_response_stream(
             idle_timeout,
             telemetry,
             safety_buffering_treatment,
+            compatibility,
         )
         .await;
     });
@@ -128,27 +135,40 @@ struct ResponseCompletedUsage {
     total_tokens: i64,
 }
 
-impl From<ResponseCompletedUsage> for TokenUsage {
-    fn from(val: ResponseCompletedUsage) -> Self {
-        TokenUsage {
-            input_tokens: val.input_tokens,
-            cached_input_tokens: val
-                .input_tokens_details
-                .map(|d| d.cached_tokens)
-                .unwrap_or(0),
-            output_tokens: val.output_tokens,
-            reasoning_output_tokens: val
+impl ResponseCompletedUsage {
+    fn into_token_usage(
+        self,
+        compatibility: ResponseParseCompatibility,
+    ) -> Result<TokenUsage, String> {
+        let cached_input_tokens = match self.input_tokens_details {
+            Some(details) => match details.cached_tokens {
+                Some(tokens) => tokens,
+                None if compatibility.cached_tokens_optional => 0,
+                None => {
+                    return Err(
+                        "missing response.completed usage.input_tokens_details.cached_tokens"
+                            .to_string(),
+                    );
+                }
+            },
+            None => 0,
+        };
+        Ok(TokenUsage {
+            input_tokens: self.input_tokens,
+            cached_input_tokens,
+            output_tokens: self.output_tokens,
+            reasoning_output_tokens: self
                 .output_tokens_details
                 .map(|d| d.reasoning_tokens)
                 .unwrap_or(0),
-            total_tokens: val.total_tokens,
-        }
+            total_tokens: self.total_tokens,
+        })
     }
 }
 
 #[derive(Debug, Deserialize)]
 struct ResponseCompletedInputTokensDetails {
-    cached_tokens: i64,
+    cached_tokens: Option<i64>,
 }
 
 #[derive(Debug, Deserialize)]
@@ -325,6 +345,13 @@ impl ResponsesEventError {
 
 pub fn process_responses_event(
     event: ResponsesStreamEvent,
+) -> std::result::Result<Option<ResponseEvent>, ResponsesEventError> {
+    process_responses_event_with_compatibility(event, ResponseParseCompatibility::default())
+}
+
+fn process_responses_event_with_compatibility(
+    event: ResponsesStreamEvent,
+    compatibility: ResponseParseCompatibility,
 ) -> std::result::Result<Option<ResponseEvent>, ResponsesEventError> {
     match event.kind.as_str() {
         "response.output_item.done" => {
@@ -436,7 +463,11 @@ pub fn process_responses_event(
                     Ok(resp) => {
                         return Ok(Some(ResponseEvent::Completed {
                             response_id: resp.id,
-                            token_usage: resp.usage.map(Into::into),
+                            token_usage: resp
+                                .usage
+                                .map(|usage| usage.into_token_usage(compatibility))
+                                .transpose()
+                                .map_err(|err| ResponsesEventError::Api(ApiError::Stream(err)))?,
                             response_performance: None,
                             end_turn: resp.end_turn,
                         }));
@@ -492,6 +523,7 @@ pub async fn process_sse(
         idle_timeout,
         telemetry,
         SafetyBufferingTreatment::default(),
+        ResponseParseCompatibility::default(),
     )
     .await;
 }
@@ -502,6 +534,7 @@ async fn process_sse_with_treatment(
     idle_timeout: Duration,
     telemetry: Option<Arc<dyn SseTelemetry>>,
     safety_buffering_treatment: SafetyBufferingTreatment,
+    compatibility: ResponseParseCompatibility,
 ) {
     let mut stream = stream.eventsource();
     let mut response_error: Option<ApiError> = None;
@@ -589,7 +622,7 @@ async fn process_sse_with_treatment(
             return;
         }
 
-        match process_responses_event(event) {
+        match process_responses_event_with_compatibility(event, compatibility) {
             Ok(Some(event)) => {
                 let is_completed = matches!(event, ResponseEvent::Completed { .. });
                 if tx_event.send(Ok(event)).await.is_err() {
@@ -845,6 +878,53 @@ mod tests {
         );
     }
 
+    #[test]
+    fn cached_tokens_missing_is_strict_unless_provider_marks_it_optional() {
+        let event = json!({
+            "type": "response.completed",
+            "response": {
+                "id": "resp1",
+                "usage": {
+                    "input_tokens": 12,
+                    "input_tokens_details": {},
+                    "output_tokens": 8,
+                    "output_tokens_details": {
+                        "reasoning_tokens": 3
+                    },
+                    "total_tokens": 20
+                }
+            }
+        });
+        let strict_event: ResponsesStreamEvent = serde_json::from_value(event.clone()).unwrap();
+        let compatible_event: ResponsesStreamEvent = serde_json::from_value(event).unwrap();
+
+        let strict_error = process_responses_event(strict_event)
+            .unwrap_err()
+            .into_api_error();
+        assert_matches!(
+            strict_error,
+            ApiError::Stream(message)
+                if message == "missing response.completed usage.input_tokens_details.cached_tokens"
+        );
+
+        let compatible = process_responses_event_with_compatibility(
+            compatible_event,
+            ResponseParseCompatibility {
+                cached_tokens_optional: true,
+            },
+        )
+        .unwrap()
+        .unwrap();
+        assert_matches!(
+            compatible,
+            ResponseEvent::Completed {
+                token_usage: Some(token_usage),
+                ..
+            } if token_usage.cached_input_tokens == 0
+                && token_usage.reasoning_output_tokens == 3
+        );
+    }
+
     #[tokio::test]
     async fn error_when_missing_completed() {
         let item1 = json!({
@@ -1227,6 +1307,7 @@ mod tests {
             idle_timeout(),
             /*telemetry*/ None,
             /*turn_state*/ None,
+            ResponseParseCompatibility::default(),
         );
         assert_eq!(stream.upstream_request_id.as_deref(), Some("req-1"));
         let event = stream
@@ -1267,6 +1348,7 @@ mod tests {
             idle_timeout(),
             /*telemetry*/ None,
             /*turn_state*/ None,
+            ResponseParseCompatibility::default(),
         );
         let mut events = Vec::new();
         while let Some(event) = stream.rx_event.recv().await {
diff --git a/codex-rs/codex-api/tests/clients.rs b/codex-rs/codex-api/tests/clients.rs
index 0141fb678a..927b9fc9ae 100644
--- a/codex-rs/codex-api/tests/clients.rs
+++ b/codex-rs/codex-api/tests/clients.rs
@@ -551,6 +551,7 @@ async fn azure_store_sends_ids_and_headers() -> Result<()> {
                 extra_headers,
                 compression: Compression::None,
                 turn_state: None,
+                ..Default::default()
             },
         )
         .await?;
diff --git a/codex-rs/core/src/client.rs b/codex-rs/core/src/client.rs
index 9aae493c0f..f0cb442215 100644
--- a/codex-rs/core/src/client.rs
+++ b/codex-rs/core/src/client.rs
@@ -1283,6 +1283,7 @@ impl ModelClientSession {
             },
             compression,
             turn_state: Some(Arc::clone(&self.turn_state)),
+            response_parse_compatibility: Default::default(),
         }
     }
 
@@ -1578,6 +1579,13 @@ impl ModelClientSession {
                 service_tier.clone(),
                 responses_metadata,
             )?;
+            if let Some(route) = self
+                .client
+                .non_openai_route_for_model(model_info.slug.as_str(), effort.as_ref())
+            {
+                options.response_parse_compatibility.cached_tokens_optional =
+                    route.cached_tokens_optional;
+            }
             let logical_request = request.clone();
             let use_previous_response_id = self
                 .client
