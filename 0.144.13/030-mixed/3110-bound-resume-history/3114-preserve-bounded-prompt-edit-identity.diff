diff --git a/codex-rs/tui/src/app/event_dispatch.rs b/codex-rs/tui/src/app/event_dispatch.rs
index 58ac620b18..2c27f11d3c 100644
--- a/codex-rs/tui/src/app/event_dispatch.rs
+++ b/codex-rs/tui/src/app/event_dispatch.rs
@@ -238,7 +238,7 @@ impl App {
             }
             AppEvent::ForkSessionForPromptEdit {
                 thread_id,
-                nth_user_message,
+                prompt_identity,
                 mut prompt,
             } => {
                 if self.chat_widget.thread_id() != Some(thread_id) {
@@ -258,7 +258,7 @@ impl App {
                 {
                     Ok(thread) => match crate::app_backtrack::backtrack_fork_before_turn_id(
                         &thread.turns,
-                        nth_user_message,
+                        &prompt_identity,
                         &mut prompt,
                     ) {
                         Ok(Some(before_turn_id)) => {
diff --git a/codex-rs/tui/src/app/safety_buffering.rs b/codex-rs/tui/src/app/safety_buffering.rs
index 1463f03f14..2b2b379ca9 100644
--- a/codex-rs/tui/src/app/safety_buffering.rs
+++ b/codex-rs/tui/src/app/safety_buffering.rs
@@ -48,6 +48,7 @@ impl App {
         let input_state = self.chat_widget.capture_thread_input_state();
 
         let AppCommand::UserTurn {
+            client_user_message_id,
             items,
             model: turn_model,
             effort,
@@ -111,6 +112,7 @@ impl App {
                 }),
         );
         let retry_display = ChatWidget::user_message_display_from_inputs(items);
+        let retry_client_user_message_id = client_user_message_id.clone();
 
         self.config = retry_config.clone();
         let started = app_server
@@ -163,7 +165,7 @@ impl App {
             return;
         }
         self.chat_widget
-            .commit_safety_buffered_retry_submission(retry_display);
+            .commit_safety_buffered_retry_submission(retry_display, retry_client_user_message_id);
     }
 
     fn fail_safety_buffered_branch(
diff --git a/codex-rs/tui/src/app/tests.rs b/codex-rs/tui/src/app/tests.rs
index 008393307a..62b4e2aa84 100644
--- a/codex-rs/tui/src/app/tests.rs
+++ b/codex-rs/tui/src/app/tests.rs
@@ -3986,6 +3986,7 @@ async fn render_clear_ui_header_after_long_transcript_for_snapshot() -> String {
             text_elements: Vec::new(),
             local_image_paths: Vec::new(),
             remote_image_urls: Vec::new(),
+            prompt_identity: None,
         }) as Arc<dyn HistoryCell>
     };
     let agent_cell = |text: &str| -> Arc<dyn HistoryCell> {
@@ -5191,6 +5192,9 @@ async fn backtrack_selection_preserves_selected_prompt_and_requests_branch() {
             text_elements,
             local_image_paths,
             remote_image_urls,
+            prompt_identity: Some(crate::history_cell::UserPromptIdentity::ClientId(format!(
+                "client-{text}"
+            ))),
         }) as Arc<dyn HistoryCell>
     };
     let agent_cell = |text: &str| -> Arc<dyn HistoryCell> {
@@ -5305,6 +5309,9 @@ async fn backtrack_selection_preserves_selected_prompt_and_requests_branch() {
     let expected = BacktrackSelection {
         thread_id: base_id,
         nth_user_message: 1,
+        prompt_identity: crate::history_cell::UserPromptIdentity::ClientId(format!(
+            "client-{edited_text}"
+        )),
         prompt: crate::chatwidget::UserMessage {
             text: edited_text,
             local_images: vec![crate::bottom_pane::LocalImageAttachment {
@@ -5326,10 +5333,10 @@ async fn backtrack_selection_preserves_selected_prompt_and_requests_branch() {
         event,
         AppEvent::ForkSessionForPromptEdit {
             thread_id,
-            nth_user_message,
+            prompt_identity,
             prompt,
         } if thread_id == expected.thread_id
-            && nth_user_message == expected.nth_user_message
+            && prompt_identity == expected.prompt_identity
             && prompt == expected.prompt
     );
 
@@ -5405,6 +5412,7 @@ async fn prompt_edit_forks_before_selected_prompt_and_preserves_source() -> Resu
                 collaboration_mode_kind: ModeKind::default(),
             })),
             RolloutItem::EventMsg(EventMsg::UserMessage(UserMessageEvent {
+                client_id: Some(format!("client-{turn_id}")),
                 message: message.to_string(),
                 images,
                 local_images,
@@ -5458,7 +5466,9 @@ async fn prompt_edit_forks_before_selected_prompt_and_preserves_source() -> Resu
         &mut app_server,
         AppEvent::ForkSessionForPromptEdit {
             thread_id: source_thread_id,
-            nth_user_message: 1,
+            prompt_identity: crate::history_cell::UserPromptIdentity::ClientId(
+                "client-turn-2".to_string(),
+            ),
             prompt: prompt.clone(),
         },
     ))
@@ -5554,13 +5564,34 @@ async fn prompt_edit_before_first_prompt_starts_fresh_thread() -> Result<()> {
     .await?;
     while app_event_rx.try_recv().is_ok() {}
     let mut tui = crate::tui::test_support::make_test_tui()?;
+    let source = app_server
+        .thread_read(source_thread_id, /*include_turns*/ true)
+        .await?;
+    let first_prompt_identity = source
+        .turns
+        .iter()
+        .find_map(|turn| {
+            turn.items.iter().find_map(|item| match item {
+                ThreadItem::UserMessage { id, client_id, .. } => {
+                    Some(client_id.clone().map_or_else(
+                        || crate::history_cell::UserPromptIdentity::Persisted {
+                            turn_id: turn.id.clone(),
+                            item_id: id.clone(),
+                        },
+                        crate::history_cell::UserPromptIdentity::ClientId,
+                    ))
+                }
+                _ => None,
+            })
+        })
+        .expect("source thread should contain its first prompt");
 
     let control = Box::pin(app.handle_event(
         &mut tui,
         &mut app_server,
         AppEvent::ForkSessionForPromptEdit {
             thread_id: source_thread_id,
-            nth_user_message: 0,
+            prompt_identity: first_prompt_identity,
             prompt: crate::chatwidget::UserMessage::from("first prompt"),
         },
     ))
@@ -5834,6 +5865,7 @@ async fn retry_discards_only_trailing_provisional_agent_cells() {
             text_elements: Vec::new(),
             local_image_paths: Vec::new(),
             remote_image_urls: Vec::new(),
+            prompt_identity: None,
         }) as Arc<dyn HistoryCell>,
         Arc::new(AgentMessageCell::new(
             vec![Line::from("partial one")],
@@ -6459,6 +6491,7 @@ async fn clear_only_ui_reset_preserves_chat_session_state() {
         text_elements: Vec::new(),
         local_image_paths: Vec::new(),
         remote_image_urls: Vec::new(),
+        prompt_identity: None,
     }) as Arc<dyn HistoryCell>];
     app.overlay = Some(Overlay::new_transcript(
         app.transcript_cells.clone(),
diff --git a/codex-rs/tui/src/app/thread_routing.rs b/codex-rs/tui/src/app/thread_routing.rs
index 10af436c59..9e17aa49a3 100644
--- a/codex-rs/tui/src/app/thread_routing.rs
+++ b/codex-rs/tui/src/app/thread_routing.rs
@@ -605,6 +605,7 @@ impl App {
                 Ok(true)
             }
             AppCommand::UserTurn {
+                client_user_message_id,
                 items,
                 cwd,
                 approval_policy,
@@ -625,7 +626,12 @@ impl App {
                     let mut retried_after_turn_mismatch = false;
                     loop {
                         match app_server
-                            .turn_steer(thread_id, steer_turn_id.clone(), items.to_vec())
+                            .turn_steer(
+                                thread_id,
+                                steer_turn_id.clone(),
+                                client_user_message_id.clone(),
+                                items.to_vec(),
+                            )
                             .await
                         {
                             Ok(_) => return Ok(true),
@@ -698,6 +704,7 @@ impl App {
                     let response = app_server
                         .turn_start(
                             thread_id,
+                            client_user_message_id.clone(),
                             items.to_vec(),
                             cwd.clone(),
                             *approval_policy,
diff --git a/codex-rs/tui/src/app_backtrack.rs b/codex-rs/tui/src/app_backtrack.rs
index f4114a3f90..4aa9bea8f2 100644
--- a/codex-rs/tui/src/app_backtrack.rs
+++ b/codex-rs/tui/src/app_backtrack.rs
@@ -76,6 +76,7 @@ pub(crate) struct BacktrackSelection {
     pub(crate) thread_id: ThreadId,
     /// The selected user message, counted from the most recent session start.
     pub(crate) nth_user_message: usize,
+    pub(crate) prompt_identity: crate::history_cell::UserPromptIdentity,
     pub(crate) prompt: UserMessage,
 }
 
@@ -175,7 +176,7 @@ impl App {
 
         self.app_event_tx.send(AppEvent::ForkSessionForPromptEdit {
             thread_id: selection.thread_id,
-            nth_user_message: selection.nth_user_message,
+            prompt_identity: selection.prompt_identity,
             prompt: selection.prompt,
         });
     }
@@ -452,6 +453,7 @@ impl App {
         Some(BacktrackSelection {
             thread_id: base_id,
             nth_user_message,
+            prompt_identity: selected.prompt_identity.clone()?,
             prompt: UserMessage {
                 text: selected.message.clone(),
                 local_images,
@@ -468,64 +470,41 @@ impl App {
 /// Replay hides review prompts and other display-empty inputs, so the selected ordinal must be
 /// resolved against the same visible projection before restoring its canonical mention bindings.
 ///
-/// A turn can contain multiple user messages when it was steered. Only its initial prompt can be
-/// reopened independently because app-server cannot fork in the middle of a turn.
+/// A turn can contain multiple user messages when it was steered. App-server cannot fork in the
+/// middle of a turn, so selecting a steer branches before its containing turn and reopens the
+/// steer without retaining earlier activity from that turn.
 pub(crate) fn backtrack_fork_before_turn_id(
     turns: &[Turn],
-    nth_user_message: usize,
+    prompt_identity: &crate::history_cell::UserPromptIdentity,
     prompt: &mut UserMessage,
 ) -> Result<Option<String>> {
-    let mut visible_user_messages_seen = 0_usize;
-    let mut review_mode = false;
     for (turn_index, turn) in turns.iter().enumerate() {
-        let hidden_nested_review_turn = turn_index
-            .checked_sub(/*rhs*/ 1)
-            .and_then(|index| turns.get(index))
-            .is_some_and(|previous| is_hidden_nested_review_turn(previous, turn));
-        let mut user_messages_in_turn = 0_usize;
         for item in &turn.items {
-            let content = match item {
-                ThreadItem::EnteredReviewMode { .. } => {
-                    review_mode = true;
-                    continue;
-                }
-                ThreadItem::ExitedReviewMode { .. } => {
-                    review_mode = false;
-                    continue;
-                }
-                ThreadItem::UserMessage { content, .. } => content,
+            let (item_id, client_id, content) = match item {
+                ThreadItem::UserMessage {
+                    id,
+                    client_id,
+                    content,
+                } => (id, client_id.as_deref(), content),
                 _ => continue,
             };
-            let is_steer = user_messages_in_turn > 0;
-            user_messages_in_turn = user_messages_in_turn.saturating_add(/*rhs*/ 1);
-            if review_mode {
-                continue;
-            }
-
-            let display = ChatWidget::user_message_display_from_inputs(content);
-            if hidden_nested_review_turn {
-                continue;
-            }
-            if display.message.trim().is_empty()
-                && display.text_elements.is_empty()
-                && display.local_images.is_empty()
-                && display.remote_image_urls.is_empty()
-            {
-                continue;
-            }
-            if visible_user_messages_seen != nth_user_message {
-                visible_user_messages_seen =
-                    visible_user_messages_seen.saturating_add(/*rhs*/ 1);
+            let matches_identity = match prompt_identity {
+                crate::history_cell::UserPromptIdentity::ClientId(selected) => {
+                    client_id == Some(selected.as_str())
+                }
+                crate::history_cell::UserPromptIdentity::Persisted {
+                    turn_id,
+                    item_id: selected_item_id,
+                } => turn.id == *turn_id && *item_id == *selected_item_id,
+            };
+            if !matches_identity {
                 continue;
             }
-
-            if is_steer {
-                bail!("the selected prompt is a steer and cannot be branched independently");
-            }
             if matches!(turn.status, TurnStatus::InProgress) {
                 bail!("the selected prompt belongs to a turn that is still in progress");
             }
 
+            let display = ChatWidget::user_message_display_from_inputs(content);
             let selected_local_images = prompt.local_images.iter().map(|image| &image.path);
             if prompt.text != display.message
                 || prompt.text_elements != display.text_elements
@@ -694,6 +673,13 @@ mod tests {
         }
     }
 
+    fn persisted_prompt(turn_id: &str, item_id: &str) -> crate::history_cell::UserPromptIdentity {
+        crate::history_cell::UserPromptIdentity::Persisted {
+            turn_id: turn_id.to_string(),
+            item_id: item_id.to_string(),
+        }
+    }
+
     #[test]
     fn backtrack_fork_before_turn_id_resolves_first_and_later_prompts() {
         let turns = vec![
@@ -709,7 +695,7 @@ mod tests {
         assert_eq!(
             backtrack_fork_before_turn_id(
                 &turns,
-                /*nth_user_message*/ 0,
+                &persisted_prompt("turn-1", "user-0"),
                 &mut prompt("turn-1-prompt-0"),
             )
             .expect("first prompt should resolve"),
@@ -718,7 +704,7 @@ mod tests {
         assert_eq!(
             backtrack_fork_before_turn_id(
                 &turns,
-                /*nth_user_message*/ 1,
+                &persisted_prompt("turn-2", "user-0"),
                 &mut prompt("turn-2-prompt-0"),
             )
             .expect("later prompt should resolve"),
@@ -727,23 +713,89 @@ mod tests {
     }
 
     #[test]
-    fn backtrack_fork_before_turn_id_rejects_mid_turn_steers() {
-        let turns = vec![turn(
-            "turn-1",
+    fn backtrack_fork_before_turn_id_reopens_mid_turn_steers() {
+        let turns = vec![
+            turn("turn-0", TurnStatus::Completed, /*user_messages*/ 1),
+            turn("turn-1", TurnStatus::Completed, /*user_messages*/ 2),
+        ];
+
+        assert_eq!(
+            backtrack_fork_before_turn_id(
+                &turns,
+                &persisted_prompt("turn-1", "user-1"),
+                &mut prompt("turn-1-prompt-1"),
+            )
+            .expect("a steer should branch before its containing turn"),
+            Some("turn-1".to_string())
+        );
+    }
+
+    #[test]
+    fn backtrack_fork_uses_identity_across_an_omitted_prefix_and_later_compaction() {
+        let mut omitted_prefix = turn(
+            "turn-omitted",
             TurnStatus::Completed,
             /*user_messages*/ 2,
-        )];
-
-        let error = backtrack_fork_before_turn_id(
-            &turns,
-            /*nth_user_message*/ 1,
-            &mut prompt("turn-1-prompt-1"),
-        )
-        .expect_err("a steer cannot be branched independently");
+        );
+        let mut selected = turn(
+            "turn-selected",
+            TurnStatus::Completed,
+            /*user_messages*/ 1,
+        );
+        let ThreadItem::UserMessage { client_id, .. } = &mut selected.items[0] else {
+            panic!("test turn should contain a user message");
+        };
+        *client_id = Some("selected-client-id".to_string());
+        let mut repeated = turn(
+            "turn-repeated",
+            TurnStatus::Completed,
+            /*user_messages*/ 1,
+        );
+        let ThreadItem::UserMessage { content, .. } = &mut omitted_prefix.items[1] else {
+            panic!("test turn should contain a steer");
+        };
+        *content = vec![UserInput::Text {
+            text: "selected text".to_string(),
+            text_elements: Vec::new(),
+        }];
+        let ThreadItem::UserMessage { content, .. } = &mut repeated.items[0] else {
+            panic!("test turn should contain the repeated prompt");
+        };
+        *content = vec![UserInput::Text {
+            text: "selected text".to_string(),
+            text_elements: Vec::new(),
+        }];
+        let ThreadItem::UserMessage { content, .. } = &mut selected.items[0] else {
+            panic!("test turn should contain the selected prompt");
+        };
+        *content = vec![UserInput::Text {
+            text: "selected text".to_string(),
+            text_elements: Vec::new(),
+        }];
+        let compacted = Turn {
+            id: "turn-compacted".to_string(),
+            items: vec![ThreadItem::ContextCompaction {
+                id: "compaction-after-resume".to_string(),
+            }],
+            items_view: Default::default(),
+            status: TurnStatus::Completed,
+            error: None,
+            started_at: None,
+            completed_at: None,
+            duration_ms: None,
+        };
+        let turns = vec![omitted_prefix, repeated, compacted, selected];
 
         assert_eq!(
-            error.to_string(),
-            "the selected prompt is a steer and cannot be branched independently"
+            backtrack_fork_before_turn_id(
+                &turns,
+                &crate::history_cell::UserPromptIdentity::ClientId(
+                    "selected-client-id".to_string(),
+                ),
+                &mut prompt("selected text"),
+            )
+            .expect("stable identity should survive omitted-prefix and compaction boundaries"),
+            Some("turn-selected".to_string())
         );
     }
 
@@ -758,7 +810,7 @@ mod tests {
         assert_eq!(
             backtrack_fork_before_turn_id(
                 &turns,
-                /*nth_user_message*/ 0,
+                &persisted_prompt("turn-1", "user-0"),
                 &mut prompt("turn-1-prompt-0"),
             )
             .expect_err("in-progress prompt cannot be branched")
@@ -768,7 +820,7 @@ mod tests {
         assert_eq!(
             backtrack_fork_before_turn_id(
                 &turns,
-                /*nth_user_message*/ 1,
+                &persisted_prompt("missing", "missing"),
                 &mut prompt("missing prompt"),
             )
             .expect_err("missing prompt cannot be branched")
@@ -784,7 +836,7 @@ mod tests {
         assert_eq!(
             backtrack_fork_before_turn_id(
                 &completed_turns,
-                /*nth_user_message*/ 0,
+                &persisted_prompt("turn-1", "user-0"),
                 &mut prompt("different prompt"),
             )
             .expect_err("a stale transcript prompt cannot be branched")
@@ -820,7 +872,7 @@ mod tests {
         assert_eq!(
             backtrack_fork_before_turn_id(
                 &turns,
-                /*nth_user_message*/ 1,
+                &persisted_prompt("turn-2", "user-0"),
                 &mut prompt("turn-2-prompt-0"),
             )
             .expect("the visible prompt after review should resolve"),
@@ -889,7 +941,7 @@ mod tests {
         assert_eq!(
             backtrack_fork_before_turn_id(
                 &turns,
-                /*nth_user_message*/ 0,
+                &persisted_prompt("turn-2", "user-0"),
                 &mut prompt("turn-2-prompt-0"),
             )
             .expect("the visible prompt after a nested review should resolve"),
@@ -931,7 +983,7 @@ mod tests {
         assert_eq!(
             backtrack_fork_before_turn_id(
                 &turns,
-                /*nth_user_message*/ 1,
+                &persisted_prompt("turn-2", "selected-prompt"),
                 &mut selected_prompt,
             )
             .expect("the selected prompt should resolve"),
@@ -999,6 +1051,7 @@ mod tests {
             text_elements: Vec::new(),
             local_image_paths: Vec::new(),
             remote_image_urls: Vec::new(),
+            prompt_identity: None,
         }) as Arc<dyn HistoryCell>);
 
         assert!(has_backtrack_target(&cells));
diff --git a/codex-rs/tui/src/app_command.rs b/codex-rs/tui/src/app_command.rs
index 373891f3ee..1783c7e6aa 100644
--- a/codex-rs/tui/src/app_command.rs
+++ b/codex-rs/tui/src/app_command.rs
@@ -31,6 +31,7 @@ pub(crate) enum AppCommand {
     },
     ClearPendingSteers,
     UserTurn {
+        client_user_message_id: String,
         items: Vec<UserInput>,
         cwd: PathBuf,
         approval_policy: AskForApproval,
@@ -120,6 +121,7 @@ impl AppCommand {
 
     #[allow(clippy::too_many_arguments)]
     pub(crate) fn user_turn(
+        client_user_message_id: String,
         items: Vec<UserInput>,
         cwd: PathBuf,
         approval_policy: AskForApproval,
@@ -134,6 +136,7 @@ impl AppCommand {
         run_user_prompt_submit_hook: bool,
     ) -> Self {
         Self::UserTurn {
+            client_user_message_id,
             items,
             cwd,
             approval_policy,
diff --git a/codex-rs/tui/src/app_event.rs b/codex-rs/tui/src/app_event.rs
index 11ef2dcdb0..b18641c8d1 100644
--- a/codex-rs/tui/src/app_event.rs
+++ b/codex-rs/tui/src/app_event.rs
@@ -217,7 +217,7 @@ pub(crate) enum AppEvent {
     /// Branch before a selected prompt and reopen it in the new thread's composer.
     ForkSessionForPromptEdit {
         thread_id: ThreadId,
-        nth_user_message: usize,
+        prompt_identity: crate::history_cell::UserPromptIdentity,
         prompt: UserMessage,
     },
 
diff --git a/codex-rs/tui/src/app_server_session.rs b/codex-rs/tui/src/app_server_session.rs
index c28d82911d..24e675c058 100644
--- a/codex-rs/tui/src/app_server_session.rs
+++ b/codex-rs/tui/src/app_server_session.rs
@@ -831,6 +831,7 @@ impl AppServerSession {
     pub(crate) async fn turn_start(
         &mut self,
         thread_id: ThreadId,
+        client_user_message_id: String,
         items: Vec<UserInput>,
         cwd: PathBuf,
         approval_policy: AskForApproval,
@@ -863,7 +864,7 @@ impl AppServerSession {
                 request_id,
                 params: TurnStartParams {
                     thread_id: thread_id.to_string(),
-                    client_user_message_id: None,
+                    client_user_message_id: Some(client_user_message_id),
                     input: items,
                     responsesapi_client_metadata: None,
                     additional_context,
@@ -918,6 +919,7 @@ impl AppServerSession {
         &mut self,
         thread_id: ThreadId,
         turn_id: String,
+        client_user_message_id: String,
         items: Vec<UserInput>,
     ) -> std::result::Result<TurnSteerResponse, TypedRequestError> {
         let request_id = self.next_request_id();
@@ -926,7 +928,7 @@ impl AppServerSession {
                 request_id,
                 params: TurnSteerParams {
                     thread_id: thread_id.to_string(),
-                    client_user_message_id: None,
+                    client_user_message_id: Some(client_user_message_id),
                     input: items,
                     responsesapi_client_metadata: None,
                     additional_context: None,
diff --git a/codex-rs/tui/src/chatwidget.rs b/codex-rs/tui/src/chatwidget.rs
index 7751ee56f5..a55b894e80 100644
--- a/codex-rs/tui/src/chatwidget.rs
+++ b/codex-rs/tui/src/chatwidget.rs
@@ -1258,7 +1258,12 @@ impl ChatWidget {
         self.request_redraw();
     }
 
-    fn on_committed_user_message(&mut self, items: &[UserInput], from_replay: bool) {
+    fn on_committed_user_message(
+        &mut self,
+        items: &[UserInput],
+        from_replay: bool,
+        prompt_identity: history_cell::UserPromptIdentity,
+    ) {
         let display = Self::user_message_display_from_inputs(items);
         if from_replay {
             if self.review.is_review_mode {
@@ -1273,7 +1278,7 @@ impl ChatWidget {
                     mention_bindings: mention_bindings_from_user_inputs(items, &display.message),
                     pending_pastes: Vec::new(),
                 });
-            self.on_user_message_display(display);
+            self.on_user_message_display_with_identity(display, Some(prompt_identity));
             return;
         }
 
@@ -1288,32 +1293,40 @@ impl ChatWidget {
                 self.refresh_pending_input_preview();
                 let pending_display =
                     user_message_display_for_history(pending.user_message, &pending.history_record);
-                self.on_user_message_display(pending_display);
+                self.on_user_message_display_with_identity(
+                    pending_display,
+                    Some(prompt_identity.clone()),
+                );
             } else if self.last_rendered_user_message_display.as_ref() != Some(&display) {
                 tracing::warn!(
                     "pending steer matched compare key but queue was empty when rendering committed user message"
                 );
-                self.on_user_message_display(display);
+                self.on_user_message_display_with_identity(display, Some(prompt_identity.clone()));
             }
         } else if !self.review.is_review_mode
             && self.last_rendered_user_message_display.as_ref() != Some(&display)
         {
-            self.on_user_message_display(display);
+            self.on_user_message_display_with_identity(display, Some(prompt_identity));
         }
     }
 
-    fn on_user_message_display(&mut self, display: UserMessageDisplay) {
+    fn on_user_message_display_with_identity(
+        &mut self,
+        display: UserMessageDisplay,
+        prompt_identity: Option<history_cell::UserPromptIdentity>,
+    ) {
         self.last_rendered_user_message_display = Some(display.clone());
         if !display.message.trim().is_empty()
             || !display.text_elements.is_empty()
             || !display.local_images.is_empty()
             || !display.remote_image_urls.is_empty()
         {
-            self.add_to_history(history_cell::new_user_prompt(
+            self.add_to_history(history_cell::new_user_prompt_with_identity(
                 display.message,
                 display.text_elements,
                 display.local_images,
                 display.remote_image_urls,
+                prompt_identity,
             ));
         }
 
diff --git a/codex-rs/tui/src/chatwidget/input_submission.rs b/codex-rs/tui/src/chatwidget/input_submission.rs
index 8c2694b694..eba74f519a 100644
--- a/codex-rs/tui/src/chatwidget/input_submission.rs
+++ b/codex-rs/tui/src/chatwidget/input_submission.rs
@@ -365,7 +365,9 @@ impl ChatWidget {
             .filter(|_| self.current_model_supports_personality());
         let service_tier = self.service_tier_update_for_core();
         let active_permission_profile = self.config.permissions.active_permission_profile();
+        let client_user_message_id = uuid::Uuid::now_v7().to_string();
         let op = AppCommand::user_turn(
+            client_user_message_id.clone(),
             items,
             self.config.cwd.to_path_buf(),
             AskForApproval::from(self.config.permissions.approval_policy.value()),
@@ -442,7 +444,12 @@ impl ChatWidget {
             )
         });
         if let Some(display) = display_user_message {
-            self.on_user_message_display(display);
+            self.on_user_message_display_with_identity(
+                display,
+                Some(history_cell::UserPromptIdentity::ClientId(
+                    client_user_message_id,
+                )),
+            );
         }
 
         self.transcript.needs_final_message_separator = false;
diff --git a/codex-rs/tui/src/chatwidget/replay.rs b/codex-rs/tui/src/chatwidget/replay.rs
index 2a16254647..11809d3000 100644
--- a/codex-rs/tui/src/chatwidget/replay.rs
+++ b/codex-rs/tui/src/chatwidget/replay.rs
@@ -86,8 +86,19 @@ impl ChatWidget {
         let from_replay = render_source.is_replay();
         let replay_kind = render_source.replay_kind();
         match item {
-            ThreadItem::UserMessage { content, .. } => {
-                self.on_committed_user_message(&content, from_replay);
+            ThreadItem::UserMessage {
+                id,
+                client_id,
+                content,
+            } => {
+                let prompt_identity = client_id.map_or_else(
+                    || crate::history_cell::UserPromptIdentity::Persisted {
+                        turn_id: turn_id.clone(),
+                        item_id: id,
+                    },
+                    crate::history_cell::UserPromptIdentity::ClientId,
+                );
+                self.on_committed_user_message(&content, from_replay, prompt_identity);
             }
             ThreadItem::AgentMessage {
                 id,
diff --git a/codex-rs/tui/src/chatwidget/safety_buffering.rs b/codex-rs/tui/src/chatwidget/safety_buffering.rs
index be65e5ee58..48c64a62b5 100644
--- a/codex-rs/tui/src/chatwidget/safety_buffering.rs
+++ b/codex-rs/tui/src/chatwidget/safety_buffering.rs
@@ -70,8 +70,17 @@ impl ChatWidget {
         self.input_queue.user_turn_pending_start = true;
     }
 
-    pub(crate) fn commit_safety_buffered_retry_submission(&mut self, display: UserMessageDisplay) {
-        self.on_user_message_display(display);
+    pub(crate) fn commit_safety_buffered_retry_submission(
+        &mut self,
+        display: UserMessageDisplay,
+        client_user_message_id: String,
+    ) {
+        self.on_user_message_display_with_identity(
+            display,
+            Some(crate::history_cell::UserPromptIdentity::ClientId(
+                client_user_message_id,
+            )),
+        );
     }
 
     pub(crate) fn cancel_safety_buffered_retry_submission(&mut self) {
diff --git a/codex-rs/tui/src/chatwidget/tests/composer_submission.rs b/codex-rs/tui/src/chatwidget/tests/composer_submission.rs
index b9f80384d4..6d79f83232 100644
--- a/codex-rs/tui/src/chatwidget/tests/composer_submission.rs
+++ b/codex-rs/tui/src/chatwidget/tests/composer_submission.rs
@@ -1,5 +1,6 @@
 use super::*;
 use crate::app_event::ConnectorsSnapshot;
+use crate::history_cell::UserPromptIdentity;
 use codex_protocol::models::ManagedFileSystemPermissions;
 use codex_protocol::permissions::FileSystemAccessMode;
 use codex_protocol::permissions::FileSystemPath;
@@ -52,10 +53,16 @@ async fn submission_preserves_text_elements_and_local_images() {
         .set_composer_text(text.clone(), text_elements.clone(), local_images.clone());
     chat.handle_key_event(KeyEvent::new(KeyCode::Enter, KeyModifiers::NONE));
 
-    let items = match next_submit_op(&mut op_rx) {
-        Op::UserTurn { items, .. } => items,
+    let (client_user_message_id, items) = match next_submit_op(&mut op_rx) {
+        Op::UserTurn {
+            client_user_message_id,
+            items,
+            ..
+        } => (client_user_message_id, items),
         other => panic!("expected Op::UserTurn, got {other:?}"),
     };
+    uuid::Uuid::parse_str(&client_user_message_id)
+        .expect("TUI submission should carry a stable client message id");
     assert_eq!(items.len(), 2);
     assert_eq!(
         items[0],
@@ -82,17 +89,22 @@ async fn submission_preserves_text_elements_and_local_images() {
                 cell.text_elements.clone(),
                 cell.local_image_paths.clone(),
                 cell.remote_image_urls.clone(),
+                cell.prompt_identity.clone(),
             ));
             break;
         }
     }
 
-    let (stored_message, stored_elements, stored_images, stored_remote_image_urls) =
+    let (stored_message, stored_elements, stored_images, stored_remote_image_urls, stored_identity) =
         user_cell.expect("expected submitted user history cell");
     assert_eq!(stored_message, text);
     assert_eq!(stored_elements, text_elements);
     assert_eq!(stored_images, local_images);
     assert!(stored_remote_image_urls.is_empty());
+    assert_eq!(
+        stored_identity,
+        Some(UserPromptIdentity::ClientId(client_user_message_id))
+    );
 }
 
 #[tokio::test]
diff --git a/codex-rs/tui/src/history_cell/messages.rs b/codex-rs/tui/src/history_cell/messages.rs
index 69dd0d2de5..5ec6ebaffb 100644
--- a/codex-rs/tui/src/history_cell/messages.rs
+++ b/codex-rs/tui/src/history_cell/messages.rs
@@ -9,6 +9,13 @@ pub(crate) struct UserHistoryCell {
     #[allow(dead_code)]
     pub local_image_paths: Vec<PathBuf>,
     pub remote_image_urls: Vec<String>,
+    pub prompt_identity: Option<UserPromptIdentity>,
+}
+
+#[derive(Clone, Debug, Eq, PartialEq)]
+pub(crate) enum UserPromptIdentity {
+    ClientId(String),
+    Persisted { turn_id: String, item_id: String },
 }
 
 /// Remove CSI sequences and control characters, preserving tabs and newlines.
@@ -479,17 +486,35 @@ impl HistoryCell for StreamingAgentTailCell {
         !self.is_first_line
     }
 }
+#[cfg(test)]
 pub(crate) fn new_user_prompt(
     message: String,
     text_elements: Vec<TextElement>,
     local_image_paths: Vec<PathBuf>,
     remote_image_urls: Vec<String>,
+) -> UserHistoryCell {
+    new_user_prompt_with_identity(
+        message,
+        text_elements,
+        local_image_paths,
+        remote_image_urls,
+        None,
+    )
+}
+
+pub(crate) fn new_user_prompt_with_identity(
+    message: String,
+    text_elements: Vec<TextElement>,
+    local_image_paths: Vec<PathBuf>,
+    remote_image_urls: Vec<String>,
+    prompt_identity: Option<UserPromptIdentity>,
 ) -> UserHistoryCell {
     UserHistoryCell {
         message,
         text_elements,
         local_image_paths,
         remote_image_urls,
+        prompt_identity,
     }
 }
 /// Create the reasoning history cell emitted at the end of a reasoning block.
diff --git a/codex-rs/tui/src/history_cell/tests.rs b/codex-rs/tui/src/history_cell/tests.rs
index 3e63c9e5be..17c8818daf 100644
--- a/codex-rs/tui/src/history_cell/tests.rs
+++ b/codex-rs/tui/src/history_cell/tests.rs
@@ -2012,6 +2012,7 @@ fn user_history_cell_wraps_and_prefixes_each_line_snapshot() {
         text_elements: Vec::new(),
         local_image_paths: Vec::new(),
         remote_image_urls: Vec::new(),
+        prompt_identity: None,
     };
 
     // Small width to force wrapping more clearly. Effective wrap width is width-2 due to the ▌ prefix and trailing space.
@@ -2030,6 +2031,7 @@ fn user_history_cell_renders_remote_image_urls() {
         text_elements: Vec::new(),
         local_image_paths: Vec::new(),
         remote_image_urls: vec!["https://example.com/example.png".to_string()],
+        prompt_identity: None,
     };
 
     let rendered = render_lines(&cell.display_lines(/*width*/ 80)).join("\n");
@@ -2046,6 +2048,7 @@ fn user_history_cell_summarizes_inline_data_urls() {
         text_elements: Vec::new(),
         local_image_paths: Vec::new(),
         remote_image_urls: vec!["data:image/png;base64,aGVsbG8=".to_string()],
+        prompt_identity: None,
     };
 
     let rendered = render_lines(&cell.display_lines(/*width*/ 80)).join("\n");
@@ -2064,6 +2067,7 @@ fn user_history_cell_numbers_multiple_remote_images() {
             "https://example.com/one.png".to_string(),
             "https://example.com/two.png".to_string(),
         ],
+        prompt_identity: None,
     };
 
     let rendered = render_lines(&cell.display_lines(/*width*/ 80)).join("\n");
@@ -2083,6 +2087,7 @@ fn user_history_cell_height_matches_rendered_lines_with_remote_images() {
             "https://example.com/one.png".to_string(),
             "https://example.com/two.png".to_string(),
         ],
+        prompt_identity: None,
     };
 
     let width = 80;
@@ -2102,6 +2107,7 @@ fn user_history_cell_trims_trailing_blank_message_lines() {
         text_elements: Vec::new(),
         local_image_paths: Vec::new(),
         remote_image_urls: vec!["https://example.com/one.png".to_string()],
+        prompt_identity: None,
     };
 
     let rendered = render_lines(&cell.display_lines(/*width*/ 80));
@@ -2125,6 +2131,7 @@ fn user_history_cell_trims_trailing_blank_message_lines_with_text_elements() {
         )],
         local_image_paths: Vec::new(),
         remote_image_urls: vec!["https://example.com/one.png".to_string()],
+        prompt_identity: None,
     };
 
     let rendered = render_lines(&cell.display_lines(/*width*/ 80));
@@ -2145,6 +2152,7 @@ fn render_uses_wrapping_for_long_url_like_line() {
         text_elements: Vec::new(),
         local_image_paths: Vec::new(),
         remote_image_urls: Vec::new(),
+        prompt_identity: None,
     });
 
     let width: u16 = 52;
@@ -2588,6 +2596,7 @@ fn wrapped_and_prefixed_cells_handle_tiny_widths() {
         text_elements: Vec::new(),
         local_image_paths: Vec::new(),
         remote_image_urls: Vec::new(),
+        prompt_identity: None,
     };
     let agent_message_cell = AgentMessageCell::new(
         vec!["tiny width agent line".into()],
@@ -2694,6 +2703,7 @@ fn consolidation_walker_replaces_agent_message_cells() {
         text_elements: Vec::new(),
         local_image_paths: Vec::new(),
         remote_image_urls: Vec::new(),
+        prompt_identity: None,
     }) as Arc<dyn HistoryCell>;
     let head = Arc::new(AgentMessageCell::new(
         vec![Line::from("line 1")],
diff --git a/codex-rs/tui/src/thread_transcript.rs b/codex-rs/tui/src/thread_transcript.rs
index 388053486b..705cbf7495 100644
--- a/codex-rs/tui/src/thread_transcript.rs
+++ b/codex-rs/tui/src/thread_transcript.rs
@@ -47,7 +47,11 @@ pub(crate) fn thread_to_transcript_cells(
 ) -> TranscriptCells {
     let cwd = thread.cwd.as_path();
     let mut cells: TranscriptCells = Vec::new();
-    for item in thread.turns.iter().flat_map(|turn| turn.items.iter()) {
+    for (turn, item) in thread
+        .turns
+        .iter()
+        .flat_map(|turn| turn.items.iter().map(move |item| (turn, item)))
+    {
         match item {
             ThreadItem::UserMessage {
                 id,
@@ -68,6 +72,13 @@ pub(crate) fn thread_to_transcript_cells(
                     text_elements: item.text_elements(),
                     local_image_paths: item.local_image_paths(),
                     remote_image_urls: item.image_urls(),
+                    prompt_identity: Some(client_id.clone().map_or_else(
+                        || crate::history_cell::UserPromptIdentity::Persisted {
+                            turn_id: turn.id.clone(),
+                            item_id: id.clone(),
+                        },
+                        crate::history_cell::UserPromptIdentity::ClientId,
+                    )),
                 }));
             }
             ThreadItem::AgentMessage { text, .. } => {
