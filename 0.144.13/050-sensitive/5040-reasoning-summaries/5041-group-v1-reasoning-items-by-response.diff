diff --git a/codex-rs/app-server-protocol/schema/json/ServerNotification.json b/codex-rs/app-server-protocol/schema/json/ServerNotification.json
index 95058e05fd..2961bc0bc4 100644
--- a/codex-rs/app-server-protocol/schema/json/ServerNotification.json
+++ b/codex-rs/app-server-protocol/schema/json/ServerNotification.json
@@ -4314,9 +4314,25 @@
               },
               "type": "array"
             },
+            "continued": {
+              "type": "boolean"
+            },
             "id": {
               "type": "string"
             },
+            "reasoningOutputTokens": {
+              "format": "int64",
+              "type": [
+                "integer",
+                "null"
+              ]
+            },
+            "responseId": {
+              "type": [
+                "string",
+                "null"
+              ]
+            },
             "summary": {
               "default": [],
               "items": {
diff --git a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json
index 14793638ff..fb3cc34d50 100644
--- a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json
+++ b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json
@@ -18451,9 +18451,25 @@
                 },
                 "type": "array"
               },
+              "continued": {
+                "type": "boolean"
+              },
               "id": {
                 "type": "string"
               },
+              "reasoningOutputTokens": {
+                "format": "int64",
+                "type": [
+                  "integer",
+                  "null"
+                ]
+              },
+              "responseId": {
+                "type": [
+                  "string",
+                  "null"
+                ]
+              },
               "summary": {
                 "default": [],
                 "items": {
diff --git a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json
index 2484d93d99..f97ce8be44 100644
--- a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json
+++ b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json
@@ -16214,9 +16214,25 @@
               },
               "type": "array"
             },
+            "continued": {
+              "type": "boolean"
+            },
             "id": {
               "type": "string"
             },
+            "reasoningOutputTokens": {
+              "format": "int64",
+              "type": [
+                "integer",
+                "null"
+              ]
+            },
+            "responseId": {
+              "type": [
+                "string",
+                "null"
+              ]
+            },
             "summary": {
               "default": [],
               "items": {
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ItemCompletedNotification.json b/codex-rs/app-server-protocol/schema/json/v2/ItemCompletedNotification.json
index ca8a944dbf..e941949380 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ItemCompletedNotification.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ItemCompletedNotification.json
@@ -697,9 +697,25 @@
               },
               "type": "array"
             },
+            "continued": {
+              "type": "boolean"
+            },
             "id": {
               "type": "string"
             },
+            "reasoningOutputTokens": {
+              "format": "int64",
+              "type": [
+                "integer",
+                "null"
+              ]
+            },
+            "responseId": {
+              "type": [
+                "string",
+                "null"
+              ]
+            },
             "summary": {
               "default": [],
               "items": {
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ItemStartedNotification.json b/codex-rs/app-server-protocol/schema/json/v2/ItemStartedNotification.json
index b2cec1515a..fdda8a40f7 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ItemStartedNotification.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ItemStartedNotification.json
@@ -697,9 +697,25 @@
               },
               "type": "array"
             },
+            "continued": {
+              "type": "boolean"
+            },
             "id": {
               "type": "string"
             },
+            "reasoningOutputTokens": {
+              "format": "int64",
+              "type": [
+                "integer",
+                "null"
+              ]
+            },
+            "responseId": {
+              "type": [
+                "string",
+                "null"
+              ]
+            },
             "summary": {
               "default": [],
               "items": {
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ReviewStartResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ReviewStartResponse.json
index 67fc02813a..4a09dfed97 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ReviewStartResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ReviewStartResponse.json
@@ -842,9 +842,25 @@
               },
               "type": "array"
             },
+            "continued": {
+              "type": "boolean"
+            },
             "id": {
               "type": "string"
             },
+            "reasoningOutputTokens": {
+              "format": "int64",
+              "type": [
+                "integer",
+                "null"
+              ]
+            },
+            "responseId": {
+              "type": [
+                "string",
+                "null"
+              ]
+            },
             "summary": {
               "default": [],
               "items": {
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadForkResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadForkResponse.json
index 9acffb1b7c..a07beae581 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadForkResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadForkResponse.json
@@ -1370,9 +1370,25 @@
               },
               "type": "array"
             },
+            "continued": {
+              "type": "boolean"
+            },
             "id": {
               "type": "string"
             },
+            "reasoningOutputTokens": {
+              "format": "int64",
+              "type": [
+                "integer",
+                "null"
+              ]
+            },
+            "responseId": {
+              "type": [
+                "string",
+                "null"
+              ]
+            },
             "summary": {
               "default": [],
               "items": {
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadListResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadListResponse.json
index a4fe3997ee..3672a620f6 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadListResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadListResponse.json
@@ -1161,9 +1161,25 @@
               },
               "type": "array"
             },
+            "continued": {
+              "type": "boolean"
+            },
             "id": {
               "type": "string"
             },
+            "reasoningOutputTokens": {
+              "format": "int64",
+              "type": [
+                "integer",
+                "null"
+              ]
+            },
+            "responseId": {
+              "type": [
+                "string",
+                "null"
+              ]
+            },
             "summary": {
               "default": [],
               "items": {
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadMetadataUpdateResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadMetadataUpdateResponse.json
index 3c6e6903a3..c0d33e254d 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadMetadataUpdateResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadMetadataUpdateResponse.json
@@ -1161,9 +1161,25 @@
               },
               "type": "array"
             },
+            "continued": {
+              "type": "boolean"
+            },
             "id": {
               "type": "string"
             },
+            "reasoningOutputTokens": {
+              "format": "int64",
+              "type": [
+                "integer",
+                "null"
+              ]
+            },
+            "responseId": {
+              "type": [
+                "string",
+                "null"
+              ]
+            },
             "summary": {
               "default": [],
               "items": {
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadReadResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadReadResponse.json
index 14e1de4f42..a2f4349a40 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadReadResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadReadResponse.json
@@ -1161,9 +1161,25 @@
               },
               "type": "array"
             },
+            "continued": {
+              "type": "boolean"
+            },
             "id": {
               "type": "string"
             },
+            "reasoningOutputTokens": {
+              "format": "int64",
+              "type": [
+                "integer",
+                "null"
+              ]
+            },
+            "responseId": {
+              "type": [
+                "string",
+                "null"
+              ]
+            },
             "summary": {
               "default": [],
               "items": {
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadResumeResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadResumeResponse.json
index a5c0145829..4893c815be 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadResumeResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadResumeResponse.json
@@ -1370,9 +1370,25 @@
               },
               "type": "array"
             },
+            "continued": {
+              "type": "boolean"
+            },
             "id": {
               "type": "string"
             },
+            "reasoningOutputTokens": {
+              "format": "int64",
+              "type": [
+                "integer",
+                "null"
+              ]
+            },
+            "responseId": {
+              "type": [
+                "string",
+                "null"
+              ]
+            },
             "summary": {
               "default": [],
               "items": {
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadRollbackResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadRollbackResponse.json
index 0570c0910e..bf0da9fea5 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadRollbackResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadRollbackResponse.json
@@ -1161,9 +1161,25 @@
               },
               "type": "array"
             },
+            "continued": {
+              "type": "boolean"
+            },
             "id": {
               "type": "string"
             },
+            "reasoningOutputTokens": {
+              "format": "int64",
+              "type": [
+                "integer",
+                "null"
+              ]
+            },
+            "responseId": {
+              "type": [
+                "string",
+                "null"
+              ]
+            },
             "summary": {
               "default": [],
               "items": {
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadStartResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadStartResponse.json
index 1cd0473650..088a2ed3f6 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadStartResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadStartResponse.json
@@ -1370,9 +1370,25 @@
               },
               "type": "array"
             },
+            "continued": {
+              "type": "boolean"
+            },
             "id": {
               "type": "string"
             },
+            "reasoningOutputTokens": {
+              "format": "int64",
+              "type": [
+                "integer",
+                "null"
+              ]
+            },
+            "responseId": {
+              "type": [
+                "string",
+                "null"
+              ]
+            },
             "summary": {
               "default": [],
               "items": {
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadStartedNotification.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadStartedNotification.json
index d65d4ccde3..ec1d09b242 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadStartedNotification.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadStartedNotification.json
@@ -1161,9 +1161,25 @@
               },
               "type": "array"
             },
+            "continued": {
+              "type": "boolean"
+            },
             "id": {
               "type": "string"
             },
+            "reasoningOutputTokens": {
+              "format": "int64",
+              "type": [
+                "integer",
+                "null"
+              ]
+            },
+            "responseId": {
+              "type": [
+                "string",
+                "null"
+              ]
+            },
             "summary": {
               "default": [],
               "items": {
diff --git a/codex-rs/app-server-protocol/schema/json/v2/ThreadUnarchiveResponse.json b/codex-rs/app-server-protocol/schema/json/v2/ThreadUnarchiveResponse.json
index 66489e28d8..402ad5555a 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/ThreadUnarchiveResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/ThreadUnarchiveResponse.json
@@ -1161,9 +1161,25 @@
               },
               "type": "array"
             },
+            "continued": {
+              "type": "boolean"
+            },
             "id": {
               "type": "string"
             },
+            "reasoningOutputTokens": {
+              "format": "int64",
+              "type": [
+                "integer",
+                "null"
+              ]
+            },
+            "responseId": {
+              "type": [
+                "string",
+                "null"
+              ]
+            },
             "summary": {
               "default": [],
               "items": {
diff --git a/codex-rs/app-server-protocol/schema/json/v2/TurnCompletedNotification.json b/codex-rs/app-server-protocol/schema/json/v2/TurnCompletedNotification.json
index 408a4b7fde..1916c89686 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/TurnCompletedNotification.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/TurnCompletedNotification.json
@@ -842,9 +842,25 @@
               },
               "type": "array"
             },
+            "continued": {
+              "type": "boolean"
+            },
             "id": {
               "type": "string"
             },
+            "reasoningOutputTokens": {
+              "format": "int64",
+              "type": [
+                "integer",
+                "null"
+              ]
+            },
+            "responseId": {
+              "type": [
+                "string",
+                "null"
+              ]
+            },
             "summary": {
               "default": [],
               "items": {
diff --git a/codex-rs/app-server-protocol/schema/json/v2/TurnStartResponse.json b/codex-rs/app-server-protocol/schema/json/v2/TurnStartResponse.json
index 15fe795c33..92fdde2f84 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/TurnStartResponse.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/TurnStartResponse.json
@@ -842,9 +842,25 @@
               },
               "type": "array"
             },
+            "continued": {
+              "type": "boolean"
+            },
             "id": {
               "type": "string"
             },
+            "reasoningOutputTokens": {
+              "format": "int64",
+              "type": [
+                "integer",
+                "null"
+              ]
+            },
+            "responseId": {
+              "type": [
+                "string",
+                "null"
+              ]
+            },
             "summary": {
               "default": [],
               "items": {
diff --git a/codex-rs/app-server-protocol/schema/json/v2/TurnStartedNotification.json b/codex-rs/app-server-protocol/schema/json/v2/TurnStartedNotification.json
index 001e1a0add..50ae8efe9b 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/TurnStartedNotification.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/TurnStartedNotification.json
@@ -842,9 +842,25 @@
               },
               "type": "array"
             },
+            "continued": {
+              "type": "boolean"
+            },
             "id": {
               "type": "string"
             },
+            "reasoningOutputTokens": {
+              "format": "int64",
+              "type": [
+                "integer",
+                "null"
+              ]
+            },
+            "responseId": {
+              "type": [
+                "string",
+                "null"
+              ]
+            },
             "summary": {
               "default": [],
               "items": {
diff --git a/codex-rs/app-server-protocol/schema/typescript/v2/ThreadItem.ts b/codex-rs/app-server-protocol/schema/typescript/v2/ThreadItem.ts
index df1a5dbe34..9cf61d9a59 100644
--- a/codex-rs/app-server-protocol/schema/typescript/v2/ThreadItem.ts
+++ b/codex-rs/app-server-protocol/schema/typescript/v2/ThreadItem.ts
@@ -27,7 +27,7 @@ import type { PatchApplyStatus } from "./PatchApplyStatus";
 import type { SubAgentActivityKind } from "./SubAgentActivityKind";
 import type { UserInput } from "./UserInput";
 
-export type ThreadItem = { "type": "userMessage", id: string, clientId: string | null, content: Array<UserInput>, } | { "type": "hookPrompt", id: string, fragments: Array<HookPromptFragment>, } | { "type": "agentMessage", id: string, text: string, phase: MessagePhase | null, memoryCitation: MemoryCitation | null, } | { "type": "plan", id: string, text: string, } | { "type": "reasoning", id: string, summary: Array<string>, content: Array<string>, } | { "type": "commandExecution", id: string,
+export type ThreadItem = { "type": "userMessage", id: string, clientId: string | null, content: Array<UserInput>, } | { "type": "hookPrompt", id: string, fragments: Array<HookPromptFragment>, } | { "type": "agentMessage", id: string, text: string, phase: MessagePhase | null, memoryCitation: MemoryCitation | null, } | { "type": "plan", id: string, text: string, } | { "type": "reasoning", id: string, summary: Array<string>, content: Array<string>, responseId?: string, reasoningOutputTokens?: bigint, continued?: boolean, } | { "type": "commandExecution", id: string,
 /**
  * The command to be executed.
  */
diff --git a/codex-rs/app-server-protocol/src/protocol/thread_history.rs b/codex-rs/app-server-protocol/src/protocol/thread_history.rs
index 177237c3c3..e69ce49380 100644
--- a/codex-rs/app-server-protocol/src/protocol/thread_history.rs
+++ b/codex-rs/app-server-protocol/src/protocol/thread_history.rs
@@ -540,6 +540,9 @@ impl ThreadHistoryBuilder {
             id,
             summary: vec![payload.text.clone()],
             content: Vec::new(),
+            response_id: None,
+            reasoning_output_tokens: None,
+            continued: false,
         });
     }
 
@@ -580,6 +583,9 @@ impl ThreadHistoryBuilder {
             id,
             summary: Vec::new(),
             content: vec![payload.text.clone()],
+            response_id: None,
+            reasoning_output_tokens: None,
+            continued: false,
         });
     }
 
@@ -1866,6 +1872,9 @@ mod tests {
                 id: "item-3".into(),
                 summary: vec!["thinking".into()],
                 content: vec!["full reasoning".into()],
+                response_id: None,
+                reasoning_output_tokens: None,
+                continued: false,
             }
         );
 
@@ -2575,6 +2584,9 @@ mod tests {
                 id: "item-2".into(),
                 summary: vec!["first summary".into()],
                 content: vec!["first content".into()],
+                response_id: None,
+                reasoning_output_tokens: None,
+                continued: false,
             }
         );
         assert_eq!(
@@ -2583,6 +2595,9 @@ mod tests {
                 id: "item-4".into(),
                 summary: vec!["second summary".into()],
                 content: Vec::new(),
+                response_id: None,
+                reasoning_output_tokens: None,
+                continued: false,
             }
         );
     }
@@ -4554,6 +4569,9 @@ mod tests {
                         id: "item-1".into(),
                         summary: vec!["summary".into()],
                         content: vec!["raw content".into()],
+                        response_id: None,
+                        reasoning_output_tokens: None,
+                        continued: false,
                     },
                 }],
                 changed_turns: Vec::new(),
diff --git a/codex-rs/app-server-protocol/src/protocol/v2/item.rs b/codex-rs/app-server-protocol/src/protocol/v2/item.rs
index 8cfebdf4ca..4600f303af 100644
--- a/codex-rs/app-server-protocol/src/protocol/v2/item.rs
+++ b/codex-rs/app-server-protocol/src/protocol/v2/item.rs
@@ -266,6 +266,14 @@ pub enum ThreadItem {
         summary: Vec<String>,
         #[serde(default)]
         content: Vec<String>,
+        #[serde(default, skip_serializing_if = "Option::is_none")]
+        #[ts(optional)]
+        response_id: Option<String>,
+        #[serde(default, skip_serializing_if = "Option::is_none")]
+        #[ts(optional)]
+        reasoning_output_tokens: Option<i64>,
+        #[serde(default, skip_serializing_if = "std::ops::Not::not")]
+        continued: bool,
     },
     #[serde(rename_all = "camelCase")]
     #[ts(rename_all = "camelCase")]
@@ -854,6 +862,9 @@ impl From<CoreTurnItem> for ThreadItem {
                 id: reasoning.id,
                 summary: reasoning.summary_text,
                 content: reasoning.raw_content,
+                response_id: reasoning.response_id,
+                reasoning_output_tokens: reasoning.reasoning_output_tokens,
+                continued: reasoning.continued,
             },
             CoreTurnItem::CommandExecution(command) => ThreadItem::CommandExecution {
                 id: command.id,
diff --git a/codex-rs/app-server-protocol/src/protocol/v2/tests.rs b/codex-rs/app-server-protocol/src/protocol/v2/tests.rs
index d8cea5ba11..81beb3175c 100644
--- a/codex-rs/app-server-protocol/src/protocol/v2/tests.rs
+++ b/codex-rs/app-server-protocol/src/protocol/v2/tests.rs
@@ -2642,6 +2642,9 @@ fn core_turn_item_into_thread_item_converts_supported_variants() {
         id: "reasoning-1".to_string(),
         summary_text: vec!["line one".to_string(), "line two".to_string()],
         raw_content: vec![],
+        response_id: Some("response-1".to_string()),
+        reasoning_output_tokens: Some(42),
+        continued: true,
     });
 
     assert_eq!(
@@ -2650,6 +2653,9 @@ fn core_turn_item_into_thread_item_converts_supported_variants() {
             id: "reasoning-1".to_string(),
             summary: vec!["line one".to_string(), "line two".to_string()],
             content: vec![],
+            response_id: Some("response-1".to_string()),
+            reasoning_output_tokens: Some(42),
+            continued: true,
         }
     );
 
@@ -2986,6 +2992,33 @@ fn core_turn_item_into_thread_item_converts_supported_variants() {
     );
 }
 
+#[test]
+fn reasoning_item_metadata_defaults_are_wire_compatible() {
+    let value = json!({
+        "type": "reasoning",
+        "id": "reasoning-1",
+        "summary": [],
+        "content": [],
+    });
+
+    let item: ThreadItem = serde_json::from_value(value.clone()).expect("deserialize reasoning");
+    assert_eq!(
+        item,
+        ThreadItem::Reasoning {
+            id: "reasoning-1".to_string(),
+            summary: Vec::new(),
+            content: Vec::new(),
+            response_id: None,
+            reasoning_output_tokens: None,
+            continued: false,
+        }
+    );
+    assert_eq!(
+        serde_json::to_value(item).expect("serialize reasoning"),
+        value
+    );
+}
+
 #[test]
 fn mcp_tool_call_app_context_serializes_connector_id() {
     let item = ThreadItem::McpToolCall {
diff --git a/codex-rs/core/src/event_mapping.rs b/codex-rs/core/src/event_mapping.rs
index 1339eea03b..0ab6a480bc 100644
--- a/codex-rs/core/src/event_mapping.rs
+++ b/codex-rs/core/src/event_mapping.rs
@@ -195,6 +195,9 @@ pub fn parse_turn_item(item: &ResponseItem) -> Option<TurnItem> {
                 id: id.as_deref().unwrap_or_default().to_string(),
                 summary_text,
                 raw_content,
+                response_id: None,
+                reasoning_output_tokens: None,
+                continued: false,
             }))
         }
         ResponseItem::WebSearchCall { id, action, .. } => {
diff --git a/codex-rs/core/src/session/turn.rs b/codex-rs/core/src/session/turn.rs
index 65f2a95b0e..1cbf697ae6 100644
--- a/codex-rs/core/src/session/turn.rs
+++ b/codex-rs/core/src/session/turn.rs
@@ -103,6 +103,7 @@ use codex_protocol::protocol::AgentReasoningSectionBreakEvent;
 use codex_protocol::protocol::CodexErrorInfo;
 use codex_protocol::protocol::ErrorEvent;
 use codex_protocol::protocol::EventMsg;
+use codex_protocol::protocol::MultiAgentVersion;
 use codex_protocol::protocol::PlanDeltaEvent;
 use codex_protocol::protocol::RawResponseCompletedEvent;
 use codex_protocol::protocol::ReasoningContentDeltaEvent;
@@ -130,6 +131,58 @@ use tracing::instrument;
 use tracing::trace;
 use tracing::trace_span;
 use tracing::warn;
+use uuid::Uuid;
+
+struct DeferredV1ReasoningItem {
+    response_item: ResponseItem,
+    previously_streamed_item: Option<TurnItem>,
+}
+
+async fn flush_v1_reasoning_response(
+    ctx: &mut HandleOutputCtx,
+    deferred_items: Vec<DeferredV1ReasoningItem>,
+    response_id: String,
+    reasoning_output_tokens: Option<i64>,
+    continues_after_response: bool,
+) {
+    let item_count = deferred_items.len();
+    for (index, deferred) in deferred_items.into_iter().enumerate() {
+        let mut finalized = finalize_non_tool_response_item(
+            ctx.sess.as_ref(),
+            TurnItemContributorPolicy::Run(ctx.turn_store.as_ref()),
+            &deferred.response_item,
+            ctx.turn_context.mode == ModeKind::Plan,
+        )
+        .await;
+        if let Some(finalized) = finalized.as_mut()
+            && let TurnItem::Reasoning(reasoning) = &mut finalized.turn_item
+        {
+            reasoning.response_id = Some(response_id.clone());
+            reasoning.continued = index + 1 < item_count || continues_after_response;
+            reasoning.reasoning_output_tokens = (index + 1 == item_count)
+                .then_some(reasoning_output_tokens)
+                .flatten();
+        }
+        let finalized_facts = finalized.as_ref().map(|finalized| finalized.facts.clone());
+        if let Some(finalized) = finalized {
+            if deferred.previously_streamed_item.is_none() {
+                ctx.sess
+                    .emit_turn_item_started(&ctx.turn_context, &finalized.turn_item)
+                    .await;
+            }
+            ctx.sess
+                .emit_turn_item_completed(&ctx.turn_context, finalized.turn_item)
+                .await;
+        }
+        record_completed_response_item_with_finalized_facts(
+            ctx.sess.as_ref(),
+            ctx.turn_context.as_ref(),
+            &deferred.response_item,
+            finalized_facts.as_ref(),
+        )
+        .await;
+    }
+}
 
 /// Takes initial turn input and runs a loop where, at each sampling request,
 /// the model replies with either:
@@ -2027,6 +2080,9 @@ async fn try_run_sampling_request(
     let defer_streamed_turn_items_for_contributors =
         !sess.services.extensions.turn_item_contributors().is_empty();
     let mut active_item_is_streaming_to_client = false;
+    let mut deferred_v1_reasoning_items = Vec::new();
+    let mut v1_reasoning_item_started = false;
+    let mut v1_response_group_id = None;
     let receiving_span = trace_span!("receiving_stream");
     let outcome: CodexResult<SamplingRequestResult> = loop {
         let handle_responses = trace_span!(
@@ -2070,7 +2126,11 @@ async fn try_run_sampling_request(
         record_turn_ttft_metric(&turn_context, &event).await;
 
         match event {
-            ResponseEvent::Created => {}
+            ResponseEvent::Created => {
+                if turn_context.multi_agent_version == MultiAgentVersion::V1 {
+                    v1_response_group_id = Some(format!("local-response-{}", Uuid::new_v4()));
+                }
+            }
             ResponseEvent::OutputItemDone(mut item) => {
                 if turn_context.item_ids_enabled() {
                     assign_missing_streamed_response_item_id(&mut item, active_item.as_ref());
@@ -2145,6 +2205,16 @@ async fn try_run_sampling_request(
                     | ResponseItem::Other => false,
                 };
 
+                if turn_context.multi_agent_version == MultiAgentVersion::V1
+                    && matches!(&item, ResponseItem::Reasoning { .. })
+                {
+                    deferred_v1_reasoning_items.push(DeferredV1ReasoningItem {
+                        response_item: item,
+                        previously_streamed_item,
+                    });
+                    continue;
+                }
+
                 let output_result =
                     match handle_output_item_done(&mut ctx, item, previously_streamed_item)
                         .instrument(handle_responses)
@@ -2172,6 +2242,21 @@ async fn try_run_sampling_request(
                 if turn_context.item_ids_enabled() {
                     assign_missing_streamed_response_item_id(&mut item, /*active_item*/ None);
                 }
+                if turn_context.multi_agent_version == MultiAgentVersion::V1
+                    && let ResponseItem::Reasoning { id, .. } = &item
+                {
+                    if v1_reasoning_item_started {
+                        sess.send_event(
+                            &turn_context,
+                            EventMsg::AgentReasoningSectionBreak(AgentReasoningSectionBreakEvent {
+                                item_id: id.as_deref().unwrap_or_default().to_string(),
+                                summary_index: 0,
+                            }),
+                        )
+                        .await;
+                    }
+                    v1_reasoning_item_started = true;
+                }
                 if let ResponseItem::CustomToolCall {
                     call_id,
                     name,
@@ -2315,6 +2400,24 @@ async fn try_run_sampling_request(
                     &mut assistant_message_stream_parsers,
                 )
                 .await;
+                if turn_context.multi_agent_version == MultiAgentVersion::V1 {
+                    flush_v1_reasoning_response(
+                        &mut HandleOutputCtx {
+                            sess: sess.clone(),
+                            turn_context: turn_context.clone(),
+                            turn_store: Arc::clone(&turn_store),
+                            tool_runtime: tool_runtime.clone(),
+                            cancellation_token: cancellation_token.child_token(),
+                        },
+                        std::mem::take(&mut deferred_v1_reasoning_items),
+                        response_id.clone(),
+                        token_usage
+                            .as_ref()
+                            .map(|usage| usage.reasoning_output_tokens),
+                        needs_follow_up || matches!(end_turn, Some(false)),
+                    )
+                    .await;
+                }
                 sess.send_event(
                     &turn_context,
                     EventMsg::RawResponseCompleted(RawResponseCompletedEvent {
@@ -2498,6 +2601,27 @@ async fn try_run_sampling_request(
     )
     .await;
 
+    if turn_context.multi_agent_version == MultiAgentVersion::V1
+        && !deferred_v1_reasoning_items.is_empty()
+    {
+        flush_v1_reasoning_response(
+            &mut HandleOutputCtx {
+                sess: sess.clone(),
+                turn_context: turn_context.clone(),
+                turn_store: Arc::clone(&turn_store),
+                tool_runtime: tool_runtime.clone(),
+                cancellation_token: cancellation_token.child_token(),
+            },
+            std::mem::take(&mut deferred_v1_reasoning_items),
+            v1_response_group_id
+                .take()
+                .unwrap_or_else(|| format!("local-response-{}", Uuid::new_v4())),
+            None,
+            false,
+        )
+        .await;
+    }
+
     let tool_blocking_timing_guard = if in_flight.is_empty() {
         None
     } else {
diff --git a/codex-rs/core/tests/suite/items.rs b/codex-rs/core/tests/suite/items.rs
index 370c1a289c..db058c8774 100644
--- a/codex-rs/core/tests/suite/items.rs
+++ b/codex-rs/core/tests/suite/items.rs
@@ -7,19 +7,27 @@ use codex_protocol::config_types::ModeKind;
 use codex_protocol::config_types::Settings;
 use codex_protocol::items::AgentMessageContent;
 use codex_protocol::items::TurnItem;
+use codex_protocol::models::ContentItem;
+use codex_protocol::models::FunctionCallOutputBody;
 use codex_protocol::models::PermissionProfile;
+use codex_protocol::models::ReasoningItemContent;
 use codex_protocol::models::WebSearchAction;
 use codex_protocol::protocol::AskForApproval;
 use codex_protocol::protocol::EventMsg;
 use codex_protocol::protocol::ItemCompletedEvent;
 use codex_protocol::protocol::ItemStartedEvent;
+use codex_protocol::protocol::MultiAgentVersion;
 use codex_protocol::protocol::Op;
+use codex_protocol::protocol::RolloutItem;
+use codex_protocol::protocol::RolloutLine;
+use codex_protocol::protocol::TokenUsage;
 use codex_protocol::user_input::ByteRange;
 use codex_protocol::user_input::TextElement;
 use codex_protocol::user_input::UserInput;
 use core_test_support::PathBufExt;
 use core_test_support::responses::ev_assistant_message;
 use core_test_support::responses::ev_completed;
+use core_test_support::responses::ev_function_call;
 use core_test_support::responses::ev_message_item_added;
 use core_test_support::responses::ev_output_text_delta;
 use core_test_support::responses::ev_reasoning_item;
@@ -30,6 +38,7 @@ use core_test_support::responses::ev_response_created;
 use core_test_support::responses::ev_web_search_call_added_partial;
 use core_test_support::responses::ev_web_search_call_done;
 use core_test_support::responses::mount_sse_once;
+use core_test_support::responses::mount_sse_sequence;
 use core_test_support::responses::sse;
 use core_test_support::responses::start_mock_server;
 use core_test_support::skip_if_no_network;
@@ -1028,6 +1037,408 @@ async fn reasoning_content_delta_has_item_metadata() -> anyhow::Result<()> {
     Ok(())
 }
 
+async fn completed_reasoning_items_for_multi_agent_version(
+    multi_agent_version: MultiAgentVersion,
+) -> anyhow::Result<Vec<codex_protocol::items::ReasoningItem>> {
+    let server = start_mock_server().await;
+    let TestCodex { codex, .. } = test_codex()
+        .with_model_info_override("gpt-5.4", move |model| {
+            model.multi_agent_version = Some(multi_agent_version);
+        })
+        .build(&server)
+        .await?;
+
+    mount_sse_once(
+        &server,
+        sse(vec![
+            ev_response_created("resp-1"),
+            ev_reasoning_item_added("reasoning-1", &[""]),
+            ev_reasoning_item("reasoning-1", &["first"], &[]),
+            ev_reasoning_item_added("reasoning-2", &[""]),
+            ev_reasoning_item("reasoning-2", &["second"], &[]),
+            serde_json::json!({
+                "type": "response.completed",
+                "response": {
+                    "id": "resp-1",
+                    "usage": {
+                        "input_tokens": 0,
+                        "input_tokens_details": null,
+                        "output_tokens": 37,
+                        "output_tokens_details": { "reasoning_tokens": 37 },
+                        "total_tokens": 37
+                    }
+                }
+            }),
+        ]),
+    )
+    .await;
+
+    codex
+        .submit(Op::UserInput {
+            items: vec![UserInput::Text {
+                text: "reason through it".into(),
+                text_elements: Vec::new(),
+            }],
+            final_output_json_schema: None,
+            responsesapi_client_metadata: None,
+            additional_context: Default::default(),
+            thread_settings: Default::default(),
+        })
+        .await?;
+
+    let mut reasoning_items = Vec::new();
+    loop {
+        match wait_for_event(&codex, |_| true).await {
+            EventMsg::ItemCompleted(ItemCompletedEvent {
+                item: TurnItem::Reasoning(item),
+                ..
+            }) => reasoning_items.push(item),
+            EventMsg::TurnComplete(_) => break,
+            _ => {}
+        }
+    }
+    Ok(reasoning_items)
+}
+
+#[tokio::test(flavor = "multi_thread", worker_threads = 2)]
+async fn v1_reasoning_items_share_response_usage_at_chain_tail() -> anyhow::Result<()> {
+    skip_if_no_network!(Ok(()));
+
+    let items = completed_reasoning_items_for_multi_agent_version(MultiAgentVersion::V1).await?;
+    assert_eq!(items.len(), 2);
+    assert_eq!(items[0].response_id.as_deref(), Some("resp-1"));
+    assert_eq!(items[1].response_id.as_deref(), Some("resp-1"));
+    assert!(items[0].continued);
+    assert!(!items[1].continued);
+    assert_eq!(items[0].reasoning_output_tokens, None);
+    assert_eq!(items[1].reasoning_output_tokens, Some(37));
+    Ok(())
+}
+
+#[tokio::test(flavor = "multi_thread", worker_threads = 2)]
+async fn v1_rollout_preserves_hidden_response_content_and_each_billing_usage() -> anyhow::Result<()>
+{
+    skip_if_no_network!(Ok(()));
+
+    let server = start_mock_server().await;
+    let TestCodex { codex, .. } = test_codex()
+        .with_model_info_override("gpt-5.4", |model| {
+            model.multi_agent_version = Some(MultiAgentVersion::V1);
+        })
+        .build(&server)
+        .await?;
+    let plan_args = serde_json::json!({
+        "plan": [{
+            "step": "canonical tool input hidden by the TUI",
+            "status": "in_progress"
+        }]
+    })
+    .to_string();
+    let completed = |response_id: &str,
+                     input_tokens: i64,
+                     cached_tokens: i64,
+                     output_tokens: i64,
+                     reasoning_tokens: i64| {
+        serde_json::json!({
+            "type": "response.completed",
+            "response": {
+                "id": response_id,
+                "usage": {
+                    "input_tokens": input_tokens,
+                    "input_tokens_details": { "cached_tokens": cached_tokens },
+                    "output_tokens": output_tokens,
+                    "output_tokens_details": { "reasoning_tokens": reasoning_tokens },
+                    "total_tokens": input_tokens + output_tokens
+                }
+            }
+        })
+    };
+    mount_sse_sequence(
+        &server,
+        vec![
+            sse(vec![
+                ev_response_created("resp-tool"),
+                ev_reasoning_item_added("reasoning-hidden", &[""]),
+                ev_reasoning_item(
+                    "reasoning-hidden",
+                    &[],
+                    &["canonical reasoning hidden by the TUI"],
+                ),
+                ev_function_call("plan-hidden", "update_plan", &plan_args),
+                completed("resp-tool", 101, 67, 29, 23),
+            ]),
+            sse(vec![
+                ev_response_created("resp-output"),
+                ev_assistant_message(
+                    "message-hidden",
+                    "canonical ordinary output hidden by the TUI",
+                ),
+                completed("resp-output", 211, 109, 41, 7),
+            ]),
+        ],
+    )
+    .await;
+
+    codex
+        .submit(Op::UserInput {
+            items: vec![UserInput::Text {
+                text: "persist hidden response content and billing".into(),
+                text_elements: Vec::new(),
+            }],
+            final_output_json_schema: None,
+            responsesapi_client_metadata: None,
+            additional_context: Default::default(),
+            thread_settings: Default::default(),
+        })
+        .await?;
+    wait_for_event(&codex, |event| matches!(event, EventMsg::TurnComplete(_))).await;
+    codex.flush_rollout().await?;
+
+    let rollout_path = codex.rollout_path().expect("rollout path");
+    let rollout = std::fs::read_to_string(rollout_path)?;
+    let items = rollout
+        .lines()
+        .map(serde_json::from_str::<RolloutLine>)
+        .collect::<serde_json::Result<Vec<_>>>()?;
+    let hidden_reasoning = items.iter().find_map(|line| match &line.item {
+        RolloutItem::ResponseItem(codex_protocol::models::ResponseItem::Reasoning {
+            content: Some(content),
+            ..
+        }) => content.iter().find_map(|content| match content {
+            ReasoningItemContent::ReasoningText { text } => Some(text.as_str()),
+            _ => None,
+        }),
+        _ => None,
+    });
+    assert_eq!(
+        hidden_reasoning,
+        Some("canonical reasoning hidden by the TUI")
+    );
+    assert!(!rollout.contains("[No reasoning summary]"));
+
+    let tool_input = items.iter().find_map(|line| match &line.item {
+        RolloutItem::ResponseItem(codex_protocol::models::ResponseItem::FunctionCall {
+            arguments,
+            ..
+        }) => Some(arguments.as_str()),
+        _ => None,
+    });
+    assert_eq!(tool_input, Some(plan_args.as_str()));
+    let tool_output = items.iter().find_map(|line| match &line.item {
+        RolloutItem::ResponseItem(codex_protocol::models::ResponseItem::FunctionCallOutput {
+            output,
+            ..
+        }) => match &output.body {
+            FunctionCallOutputBody::Text(text) => Some(text.as_str()),
+            FunctionCallOutputBody::ContentItems(_) => None,
+        },
+        _ => None,
+    });
+    assert_eq!(tool_output, Some("Plan updated"));
+    let ordinary_output = items.iter().find_map(|line| match &line.item {
+        RolloutItem::ResponseItem(codex_protocol::models::ResponseItem::Message {
+            role,
+            content,
+            ..
+        }) if role == "assistant" => content.iter().find_map(|content| match content {
+            ContentItem::OutputText { text } => Some(text.as_str()),
+            _ => None,
+        }),
+        _ => None,
+    });
+    assert_eq!(
+        ordinary_output,
+        Some("canonical ordinary output hidden by the TUI")
+    );
+
+    let usage = items
+        .iter()
+        .filter_map(|line| match &line.item {
+            RolloutItem::EventMsg(EventMsg::TokenCount(event)) => event
+                .info
+                .as_ref()
+                .map(|info| info.last_token_usage.clone()),
+            _ => None,
+        })
+        .collect::<Vec<_>>();
+    assert!(usage.contains(&TokenUsage {
+        input_tokens: 101,
+        cached_input_tokens: 67,
+        output_tokens: 29,
+        reasoning_output_tokens: 23,
+        total_tokens: 130,
+    }));
+    assert!(usage.contains(&TokenUsage {
+        input_tokens: 211,
+        cached_input_tokens: 109,
+        output_tokens: 41,
+        reasoning_output_tokens: 7,
+        total_tokens: 252,
+    }));
+    Ok(())
+}
+
+#[tokio::test(flavor = "multi_thread", worker_threads = 2)]
+async fn v1_reasoning_chain_continues_across_tool_response() -> anyhow::Result<()> {
+    skip_if_no_network!(Ok(()));
+
+    let server = start_mock_server().await;
+    let TestCodex { codex, .. } = test_codex()
+        .with_model_info_override("gpt-5.4", |model| {
+            model.multi_agent_version = Some(MultiAgentVersion::V1);
+        })
+        .build(&server)
+        .await?;
+    let completed = |response_id: &str, reasoning_tokens: i64| {
+        serde_json::json!({
+            "type": "response.completed",
+            "response": {
+                "id": response_id,
+                "usage": {
+                    "input_tokens": 0,
+                    "input_tokens_details": null,
+                    "output_tokens": reasoning_tokens,
+                    "output_tokens_details": { "reasoning_tokens": reasoning_tokens },
+                    "total_tokens": reasoning_tokens
+                }
+            }
+        })
+    };
+    let plan_args = serde_json::json!({
+        "plan": [{"step": "continue", "status": "in_progress"}]
+    })
+    .to_string();
+    mount_sse_sequence(
+        &server,
+        vec![
+            sse(vec![
+                ev_response_created("resp-1"),
+                ev_reasoning_item_added("reasoning-1", &[""]),
+                ev_reasoning_item("reasoning-1", &["first"], &[]),
+                ev_function_call("plan-1", "update_plan", &plan_args),
+                completed("resp-1", 11),
+            ]),
+            sse(vec![
+                ev_response_created("resp-2"),
+                ev_reasoning_item_added("reasoning-2", &[""]),
+                ev_reasoning_item("reasoning-2", &["second"], &[]),
+                ev_assistant_message("message-1", "done"),
+                completed("resp-2", 13),
+            ]),
+        ],
+    )
+    .await;
+
+    codex
+        .submit(Op::UserInput {
+            items: vec![UserInput::Text {
+                text: "reason through a tool".into(),
+                text_elements: Vec::new(),
+            }],
+            final_output_json_schema: None,
+            responsesapi_client_metadata: None,
+            additional_context: Default::default(),
+            thread_settings: Default::default(),
+        })
+        .await?;
+
+    let mut reasoning_items = Vec::new();
+    loop {
+        match wait_for_event(&codex, |_| true).await {
+            EventMsg::ItemCompleted(ItemCompletedEvent {
+                item: TurnItem::Reasoning(item),
+                ..
+            }) => reasoning_items.push(item),
+            EventMsg::TurnComplete(_) => break,
+            _ => {}
+        }
+    }
+    assert_eq!(reasoning_items.len(), 2);
+    assert_eq!(reasoning_items[0].response_id.as_deref(), Some("resp-1"));
+    assert_eq!(reasoning_items[0].reasoning_output_tokens, Some(11));
+    assert!(reasoning_items[0].continued);
+    assert_eq!(reasoning_items[1].response_id.as_deref(), Some("resp-2"));
+    assert_eq!(reasoning_items[1].reasoning_output_tokens, Some(13));
+    assert!(!reasoning_items[1].continued);
+    Ok(())
+}
+
+#[tokio::test(flavor = "multi_thread", worker_threads = 2)]
+async fn v2_reasoning_items_do_not_receive_v1_chain_metadata() -> anyhow::Result<()> {
+    skip_if_no_network!(Ok(()));
+
+    let items = completed_reasoning_items_for_multi_agent_version(MultiAgentVersion::V2).await?;
+    assert_eq!(items.len(), 2);
+    for item in items {
+        assert_eq!(item.response_id, None);
+        assert_eq!(item.reasoning_output_tokens, None);
+        assert!(!item.continued);
+    }
+    Ok(())
+}
+
+#[tokio::test(flavor = "multi_thread", worker_threads = 2)]
+async fn v1_completed_reasoning_survives_stream_close_before_response_completion()
+-> anyhow::Result<()> {
+    skip_if_no_network!(Ok(()));
+
+    let server = start_mock_server().await;
+    mount_sse_once(
+        &server,
+        sse(vec![
+            ev_response_created("resp-incomplete"),
+            ev_reasoning_item_added("reasoning-incomplete", &[""]),
+            ev_reasoning_item("reasoning-incomplete", &["kept"], &[]),
+        ]),
+    )
+    .await;
+    let TestCodex { codex, .. } = test_codex()
+        .with_config(|config| {
+            config.model_provider.stream_max_retries = Some(0);
+        })
+        .with_model_info_override("gpt-5.4", |model| {
+            model.multi_agent_version = Some(MultiAgentVersion::V1);
+        })
+        .build(&server)
+        .await?;
+
+    codex
+        .submit(Op::UserInput {
+            items: vec![UserInput::Text {
+                text: "preserve completed reasoning on stream close".into(),
+                text_elements: Vec::new(),
+            }],
+            final_output_json_schema: None,
+            responsesapi_client_metadata: None,
+            additional_context: Default::default(),
+            thread_settings: Default::default(),
+        })
+        .await?;
+
+    let mut completed = None;
+    loop {
+        match wait_for_event(&codex, |_| true).await {
+            EventMsg::ItemCompleted(ItemCompletedEvent {
+                item: TurnItem::Reasoning(item),
+                ..
+            }) => completed = Some(item),
+            EventMsg::TurnComplete(_) => break,
+            _ => {}
+        }
+    }
+    let completed = completed.expect("completed reasoning item");
+    assert_eq!(completed.summary_text, ["kept"]);
+    assert!(
+        completed
+            .response_id
+            .as_deref()
+            .is_some_and(|id| id.starts_with("local-response-"))
+    );
+    assert_eq!(completed.reasoning_output_tokens, None);
+    assert!(!completed.continued);
+    Ok(())
+}
+
 #[tokio::test(flavor = "multi_thread", worker_threads = 2)]
 async fn sequential_cutoff_renders_done_summaries_for_active_reasoning_item() -> anyhow::Result<()>
 {
diff --git a/codex-rs/protocol/src/items.rs b/codex-rs/protocol/src/items.rs
index 68eab25692..6958573856 100644
--- a/codex-rs/protocol/src/items.rs
+++ b/codex-rs/protocol/src/items.rs
@@ -159,6 +159,14 @@ pub struct ReasoningItem {
     pub summary_text: Vec<String>,
     #[serde(default)]
     pub raw_content: Vec<String>,
+    #[serde(default, skip_serializing_if = "Option::is_none")]
+    #[ts(optional)]
+    pub response_id: Option<String>,
+    #[serde(default, skip_serializing_if = "Option::is_none")]
+    #[ts(optional)]
+    pub reasoning_output_tokens: Option<i64>,
+    #[serde(default)]
+    pub continued: bool,
 }
 
 #[derive(Debug, Clone, Copy, Deserialize, Serialize, TS, JsonSchema, PartialEq, Eq)]
