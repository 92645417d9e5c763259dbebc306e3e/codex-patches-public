diff --git a/codex-rs/tui/src/chatwidget.rs b/codex-rs/tui/src/chatwidget.rs
index 0b3b68069f..5a3a20bd6c 100644
--- a/codex-rs/tui/src/chatwidget.rs
+++ b/codex-rs/tui/src/chatwidget.rs
@@ -490,9 +490,16 @@ use unicode_segmentation::UnicodeSegmentation;
 
 const USER_SHELL_COMMAND_HELP_TITLE: &str = "Prefix a command with ! to run it locally";
 const USER_SHELL_COMMAND_HELP_HINT: &str = "Example: !ls";
-const ASK_FOR_APPROVAL_LABEL: &str = "Ask for approval";
-const APPROVE_FOR_ME_LABEL: &str = "Approve for me";
-const AUTO_REVIEW_DESCRIPTION: &str = "Only ask for actions detected as potentially unsafe.";
+const ASK_FOR_APPROVAL_LABEL: &str = "Ask For Approval";
+const APPROVE_FOR_ME_LABEL: &str = "Approve For Me";
+const FULL_ACCESS_APPROVE_FOR_ME_LABEL: &str = "Full Access + Approved By Agent";
+const YOLO_LABEL: &str = "YOLO";
+const WORKSPACE_USER_DESCRIPTION: &str =
+    "Workspace write sandbox. Ask the user before internet access or edits outside the workspace.";
+const WORKSPACE_AUTO_REVIEW_DESCRIPTION: &str = "Workspace write sandbox. Ask the auto-review agent before internet access or edits outside the workspace.";
+const FULL_ACCESS_AUTO_REVIEW_DESCRIPTION: &str =
+    "Full filesystem and network access. Ask the auto-review agent before privileged actions.";
+const YOLO_DESCRIPTION: &str = "Full filesystem and network access without approval. Dangerous.";
 const DEFAULT_STATUS_LINE_ITEMS: [&str; 3] = [
     "model-with-reasoning",
     "responses-stream-connection",
diff --git a/codex-rs/tui/src/chatwidget/permission_popups.rs b/codex-rs/tui/src/chatwidget/permission_popups.rs
index 1283a9e490..cc15f28f08 100644
--- a/codex-rs/tui/src/chatwidget/permission_popups.rs
+++ b/codex-rs/tui/src/chatwidget/permission_popups.rs
@@ -60,8 +60,11 @@ impl ChatWidget {
             } else {
                 preset.label.to_string()
             };
-            let base_description =
-                Some(preset.description.replace(" (Identical to Agent mode)", ""));
+            let base_description = if preset.id == "auto" {
+                Some(WORKSPACE_USER_DESCRIPTION.to_string())
+            } else {
+                Some(preset.description.replace(" (Identical to Agent mode)", ""))
+            };
             let approval_disabled_reason = match self
                 .config
                 .permissions
@@ -101,7 +104,7 @@ impl ChatWidget {
                 if guardian_approval_enabled {
                     items.push(SelectionItem {
                         name: APPROVE_FOR_ME_LABEL.to_string(),
-                        description: Some(AUTO_REVIEW_DESCRIPTION.to_string()),
+                        description: Some(WORKSPACE_AUTO_REVIEW_DESCRIPTION.to_string()),
                         is_current: current_review_policy == ApprovalsReviewer::AutoReview
                             && Self::preset_matches_current(
                                 current_approval,
@@ -122,6 +125,70 @@ impl ChatWidget {
                         ..Default::default()
                     });
                 }
+            } else if preset.id == "full-access" {
+                items.push(SelectionItem {
+                    name: base_name,
+                    description: base_description,
+                    is_current: current_review_policy == ApprovalsReviewer::User
+                        && Self::preset_matches_current(
+                            current_approval,
+                            &current_permission_profile,
+                            self.config.cwd.as_path(),
+                            &preset,
+                        ),
+                    actions: default_actions,
+                    dismiss_on_select: true,
+                    disabled_reason: default_disabled_reason.clone(),
+                    ..Default::default()
+                });
+                if guardian_approval_enabled {
+                    let approval = AskForApproval::OnRequest;
+                    let selection = PermissionProfileSelection {
+                        profile_id: ":danger-no-sandbox".to_string(),
+                        approval_policy: Some(approval),
+                        approvals_reviewer: Some(ApprovalsReviewer::AutoReview),
+                        display_label: FULL_ACCESS_APPROVE_FOR_ME_LABEL.to_string(),
+                    };
+                    items.push(SelectionItem {
+                        name: FULL_ACCESS_APPROVE_FOR_ME_LABEL.to_string(),
+                        description: Some(FULL_ACCESS_AUTO_REVIEW_DESCRIPTION.to_string()),
+                        is_current: current_review_policy == ApprovalsReviewer::AutoReview
+                            && current_approval == approval
+                            && matches!(current_permission_profile, PermissionProfile::Disabled),
+                        actions: self.permission_mode_actions(
+                            &preset,
+                            FULL_ACCESS_APPROVE_FOR_ME_LABEL.to_string(),
+                            ApprovalsReviewer::AutoReview,
+                            Some(selection),
+                            /*return_to_permissions*/ !include_read_only,
+                        ),
+                        dismiss_on_select: true,
+                        disabled_reason: self
+                            .config
+                            .permissions
+                            .approval_policy
+                            .can_set(&approval.to_core())
+                            .err()
+                            .map(|err| err.to_string())
+                            .or_else(|| guardian_disabled_reason(true)),
+                        ..Default::default()
+                    });
+                }
+                items.push(SelectionItem {
+                    name: YOLO_LABEL.to_string(),
+                    description: Some(YOLO_DESCRIPTION.to_string()),
+                    is_current: false,
+                    actions: self.permission_mode_actions(
+                        &preset,
+                        YOLO_LABEL.to_string(),
+                        ApprovalsReviewer::User,
+                        /*profile_selection*/ None,
+                        /*return_to_permissions*/ !include_read_only,
+                    ),
+                    dismiss_on_select: true,
+                    disabled_reason: default_disabled_reason,
+                    ..Default::default()
+                });
             } else {
                 items.push(SelectionItem {
                     name: base_name,
@@ -304,8 +371,7 @@ impl ChatWidget {
                 Self::permission_profile_selection_actions,
             )
         };
-        let requires_confirmation = approvals_reviewer == ApprovalsReviewer::User
-            && preset.id == "full-access"
+        let requires_confirmation = preset.id == "full-access"
             && !self
                 .config
                 .notices
diff --git a/codex-rs/tui/src/chatwidget/permissions_menu.rs b/codex-rs/tui/src/chatwidget/permissions_menu.rs
index c9cb3b108f..167d505a15 100644
--- a/codex-rs/tui/src/chatwidget/permissions_menu.rs
+++ b/codex-rs/tui/src/chatwidget/permissions_menu.rs
@@ -26,22 +26,18 @@ impl ChatWidget {
             );
             return;
         };
-        let mut items = vec![
-            self.builtin_permission_mode_selection_item(
-                default,
-                ":workspace",
-                default
-                    .description
-                    .replace(" (Identical to Agent mode)", ""),
-                AskForApproval::from(default.approval),
-                ApprovalsReviewer::User,
-            ),
-        ];
+        let mut items = vec![self.builtin_permission_mode_selection_item(
+            default,
+            ":workspace",
+            WORKSPACE_USER_DESCRIPTION.to_string(),
+            AskForApproval::from(default.approval),
+            ApprovalsReviewer::User,
+        )];
         if self.config.features.enabled(Feature::GuardianApproval) {
             items.push(self.builtin_permission_mode_selection_item(
                 default,
                 ":workspace",
-                AUTO_REVIEW_DESCRIPTION.to_string(),
+                WORKSPACE_AUTO_REVIEW_DESCRIPTION.to_string(),
                 AskForApproval::OnRequest,
                 ApprovalsReviewer::AutoReview,
             ));
@@ -53,6 +49,24 @@ impl ChatWidget {
             AskForApproval::from(full_access.approval),
             ApprovalsReviewer::User,
         ));
+        if self.config.features.enabled(Feature::GuardianApproval) {
+            items.push(self.builtin_permission_mode_selection_item(
+                full_access,
+                ":danger-no-sandbox",
+                FULL_ACCESS_AUTO_REVIEW_DESCRIPTION.to_string(),
+                AskForApproval::OnRequest,
+                ApprovalsReviewer::AutoReview,
+            ));
+        }
+        items.push(self.builtin_permission_mode_selection_item_with_label(
+            full_access,
+            ":danger-no-sandbox",
+            YOLO_LABEL,
+            YOLO_DESCRIPTION.to_string(),
+            AskForApproval::Never,
+            ApprovalsReviewer::User,
+            /*is_current_alias*/ false,
+        ));
         items.push(self.builtin_permission_mode_selection_item(
             read_only,
             ":read-only",
@@ -97,8 +111,30 @@ impl ChatWidget {
         let label = match (preset.id, approvals_reviewer) {
             ("auto", ApprovalsReviewer::AutoReview) => APPROVE_FOR_ME_LABEL,
             ("auto", ApprovalsReviewer::User) => ASK_FOR_APPROVAL_LABEL,
+            ("full-access", ApprovalsReviewer::AutoReview) => FULL_ACCESS_APPROVE_FOR_ME_LABEL,
             _ => preset.label,
         };
+        self.builtin_permission_mode_selection_item_with_label(
+            preset,
+            id,
+            label,
+            description,
+            approval_policy,
+            approvals_reviewer,
+            /*is_current_alias*/ true,
+        )
+    }
+
+    fn builtin_permission_mode_selection_item_with_label(
+        &self,
+        preset: &ApprovalPreset,
+        id: &str,
+        label: &str,
+        description: String,
+        approval_policy: AskForApproval,
+        approvals_reviewer: ApprovalsReviewer,
+        is_current_alias: bool,
+    ) -> SelectionItem {
         let active_profile_id = self
             .config
             .permissions
@@ -117,7 +153,8 @@ impl ChatWidget {
         SelectionItem {
             name: label.to_string(),
             description: Some(description),
-            is_current: active_profile_id.as_deref() == Some(id)
+            is_current: is_current_alias
+                && active_profile_id.as_deref() == Some(id)
                 && current_approval == approval_policy
                 && current_reviewer == approvals_reviewer,
             actions: self.permission_mode_actions(
diff --git a/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__approvals_selection_popup.snap b/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__approvals_selection_popup.snap
index cdf838df99..a7fad3b839 100644
--- a/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__approvals_selection_popup.snap
+++ b/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__approvals_selection_popup.snap
@@ -1,15 +1,17 @@
 ---
 source: tui/src/chatwidget/tests/permissions.rs
+assertion_line: 84
 expression: popup
 ---
   Update Model Permissions
 
-› 1. Ask for approval (current)  Codex can read and edit files in the current
-                                 workspace, and run commands. Approval is
-                                 required to access the internet or edit other
-                                 files.
+› 1. Ask For Approval (current)  Workspace write sandbox. Ask the user before
+                                 internet access or edits outside the
+                                 workspace.
   2. Full Access                 Codex can edit files outside this workspace
                                  and access the internet without asking for
                                  approval. Exercise caution when using.
+  3. YOLO                        Full filesystem and network access without
+                                 approval. Dangerous.
 
   Press enter to confirm or esc to go back
diff --git a/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__permissions_selection_history_full_access_to_default.snap b/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__permissions_selection_history_full_access_to_default.snap
index 5172dd10c2..85dc41627a 100644
--- a/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__permissions_selection_history_full_access_to_default.snap
+++ b/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__permissions_selection_history_full_access_to_default.snap
@@ -1,5 +1,6 @@
 ---
 source: tui/src/chatwidget/tests/permissions.rs
+assertion_line: 808
 expression: "lines_to_single_string(&cells[0])"
 ---
-■ Permissions updated to Ask for approval
+■ Permissions updated to Ask For Approval
diff --git a/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__profile_permissions_selection_popup.snap b/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__profile_permissions_selection_popup.snap
index 5ad5481590..192c19b538 100644
--- a/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__profile_permissions_selection_popup.snap
+++ b/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__profile_permissions_selection_popup.snap
@@ -1,20 +1,27 @@
 ---
 source: tui/src/chatwidget/tests/permissions.rs
+assertion_line: 101
 expression: "render_bottom_popup(&chat, 80)"
 ---
   Update Model Permissions
 
-› 1. Ask for approval (current)  Codex can read and edit files in the current
-                                 workspace, and run commands. Approval is
-                                 required to access the internet or edit other
-                                 files.
-  2. Approve for me              Only ask for actions detected as potentially
-                                 unsafe.
-  3. Full Access                 Codex can edit files outside this workspace
-                                 and access the internet without asking for
-                                 approval. Exercise caution when using.
-  4. Read Only                   Codex can read files in the current
-                                 workspace. Approval is required to edit files
-                                 or access the internet.
+› 1. Ask For Approval (current)       Workspace write sandbox. Ask the user
+                                      before internet access or edits outside
+                                      the workspace.
+  2. Approve For Me                   Workspace write sandbox. Ask the auto-
+                                      review agent before internet access or
+                                      edits outside the workspace.
+  3. Full Access                      Codex can edit files outside this
+                                      workspace and access the internet
+                                      without asking for approval. Exercise
+                                      caution when using.
+  4. Full Access + Approved By Agent  Full filesystem and network access. Ask
+                                      the auto-review agent before privileged
+                                      actions.
+  5. YOLO                             Full filesystem and network access
+                                      without approval. Dangerous.
+  6. Read Only                        Codex can read files in the current
+                                      workspace. Approval is required to edit
+                                      files or access the internet.
 
   Press enter to confirm or esc to go back
diff --git a/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__profile_permissions_selection_popup_with_custom_profiles.snap b/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__profile_permissions_selection_popup_with_custom_profiles.snap
index dac63d4e32..5f5bfc4d44 100644
--- a/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__profile_permissions_selection_popup_with_custom_profiles.snap
+++ b/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__profile_permissions_selection_popup_with_custom_profiles.snap
@@ -1,21 +1,30 @@
 ---
 source: tui/src/chatwidget/tests/permissions.rs
+assertion_line: 131
 expression: "render_bottom_popup(&chat, 80)"
 ---
   Update Model Permissions
 
-  1. Ask for approval       Codex can read and edit files in the current
-                            workspace, and run commands. Approval is required
-                            to access the internet or edit other files.
-  2. Approve for me         Only ask for actions detected as potentially
-                            unsafe.
-  3. Full Access            Codex can edit files outside this workspace and
-                            access the internet without asking for approval.
-                            Exercise caution when using.
-  4. Read Only              Codex can read files in the current workspace.
-                            Approval is required to edit files or access the
-                            internet.
-› 5. locked-down (current)  Inspect and patch only approved workspace files.
-  6. web-enabled            Workspace profile with network access.
+  1. Ask For Approval                 Workspace write sandbox. Ask the user
+                                      before internet access or edits outside
+                                      the workspace.
+  2. Approve For Me                   Workspace write sandbox. Ask the auto-
+                                      review agent before internet access or
+                                      edits outside the workspace.
+  3. Full Access                      Codex can edit files outside this
+                                      workspace and access the internet
+                                      without asking for approval. Exercise
+                                      caution when using.
+  4. Full Access + Approved By Agent  Full filesystem and network access. Ask
+                                      the auto-review agent before privileged
+                                      actions.
+  5. YOLO                             Full filesystem and network access
+                                      without approval. Dangerous.
+  6. Read Only                        Codex can read files in the current
+                                      workspace. Approval is required to edit
+                                      files or access the internet.
+› 7. locked-down (current)            Inspect and patch only approved
+                                      workspace files.
+  8. web-enabled                      Workspace profile with network access.
 
   Press enter to confirm or esc to go back
diff --git a/codex-rs/tui/src/chatwidget/tests/permissions.rs b/codex-rs/tui/src/chatwidget/tests/permissions.rs
index 8272021e5c..31c62d0008 100644
--- a/codex-rs/tui/src/chatwidget/tests/permissions.rs
+++ b/codex-rs/tui/src/chatwidget/tests/permissions.rs
@@ -162,7 +162,7 @@ async fn profile_permissions_selection_emits_named_profile_event_only() {
             approval_policy: Some(AskForApproval::OnRequest),
             approvals_reviewer: Some(ApprovalsReviewer::User),
             display_label,
-        }) if profile_id == ":workspace" && display_label == "Ask for approval"
+        }) if profile_id == ":workspace" && display_label == "Ask For Approval"
     ));
 }
 
@@ -227,7 +227,7 @@ async fn profile_permissions_selection_emits_auto_review_mode_event() {
             approval_policy: Some(AskForApproval::OnRequest),
             approvals_reviewer: Some(ApprovalsReviewer::AutoReview),
             display_label,
-        }) if profile_id == ":workspace" && display_label == "Approve for me"
+        }) if profile_id == ":workspace" && display_label == "Approve For Me"
     ));
 }
 
@@ -275,7 +275,7 @@ async fn approvals_selection_popup_snapshot_windows_degraded_sandbox() {
 
     let popup = render_bottom_popup(&chat, /*width*/ 80);
     assert!(
-        popup.contains("Ask for approval (non-admin sandbox)"),
+        popup.contains("Ask For Approval (non-admin sandbox)"),
         "expected degraded sandbox label in approvals popup: {popup}"
     );
     assert!(
@@ -304,7 +304,7 @@ async fn preset_matching_accepts_workspace_write_with_extra_roots() {
             cwd.as_path(),
             &preset
         ),
-        "WorkspaceWrite with extra roots should still match the Ask for approval preset"
+        "WorkspaceWrite with extra roots should still match the Ask For Approval preset"
     );
     assert!(
         !ChatWidget::preset_matches_current(
@@ -790,7 +790,7 @@ async fn permissions_selection_history_snapshot_full_access_to_default() {
     chat.open_permissions_popup();
     let popup = render_bottom_popup(&chat, /*width*/ 120);
     chat.handle_key_event(KeyEvent::from(KeyCode::Up));
-    if popup.contains("Approve for me") {
+    if popup.contains("Approve For Me") {
         chat.handle_key_event(KeyEvent::from(KeyCode::Up));
     }
     chat.handle_key_event(KeyEvent::from(KeyCode::Enter));
@@ -860,11 +860,36 @@ async fn permissions_selection_hides_auto_review_when_feature_disabled() {
     let popup = render_bottom_popup(&chat, /*width*/ 120);
 
     assert!(
-        !popup.contains("Approve for me"),
-        "expected Approve for me to stay hidden until the feature is enabled: {popup}"
+        !popup.contains("Approve For Me"),
+        "expected Approve For Me to stay hidden until the feature is enabled: {popup}"
     );
 }
 
+#[tokio::test]
+async fn permissions_selection_shows_full_access_reviewer_profiles() {
+    let (mut chat, _rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+    #[cfg(target_os = "windows")]
+    {
+        chat.config.notices.hide_world_writable_warning = Some(true);
+        chat.set_windows_sandbox_mode(Some(WindowsSandboxModeToml::Unelevated));
+    }
+    chat.config.notices.hide_full_access_warning = Some(true);
+    chat.set_feature_enabled(Feature::GuardianApproval, /*enabled*/ true);
+
+    chat.open_permissions_popup();
+    let popup = render_bottom_popup(&chat, /*width*/ 140);
+
+    for label in [
+        "Ask For Approval",
+        "Approve For Me",
+        "Full Access",
+        "Full Access + Approved By Agent",
+        "YOLO",
+    ] {
+        assert!(popup.contains(label), "expected {label} in popup: {popup}");
+    }
+}
+
 #[tokio::test]
 async fn permissions_selection_hides_auto_review_when_feature_disabled_even_if_auto_review_is_active()
  {
@@ -891,8 +916,8 @@ async fn permissions_selection_hides_auto_review_when_feature_disabled_even_if_a
     let popup = render_bottom_popup(&chat, /*width*/ 120);
 
     assert!(
-        !popup.contains("Approve for me"),
-        "expected Approve for me to stay hidden when the feature is disabled: {popup}"
+        !popup.contains("Approve For Me"),
+        "expected Approve For Me to stay hidden when the feature is disabled: {popup}"
     );
 }
 
@@ -937,8 +962,8 @@ async fn permissions_selection_marks_auto_review_current_after_session_configure
     let popup = render_bottom_popup(&chat, /*width*/ 120);
 
     assert!(
-        popup.contains("Approve for me (current)"),
-        "expected Approve for me to be current after SessionConfigured sync: {popup}"
+        popup.contains("Approve For Me (current)"),
+        "expected Approve For Me to be current after SessionConfigured sync: {popup}"
     );
 }
 
@@ -987,8 +1012,8 @@ async fn permissions_selection_marks_auto_review_current_with_custom_workspace_w
     let popup = render_bottom_popup(&chat, /*width*/ 120);
 
     assert!(
-        popup.contains("Approve for me (current)"),
-        "expected Approve for me to be current even with custom workspace-write details: {popup}"
+        popup.contains("Approve For Me (current)"),
+        "expected Approve For Me to be current even with custom workspace-write details: {popup}"
     );
 }
 
@@ -1022,7 +1047,7 @@ async fn permissions_selection_can_disable_auto_review() {
             event,
             AppEvent::UpdateApprovalsReviewer(ApprovalsReviewer::User)
         )),
-        "expected selecting Ask for approval from Approve for me to switch back to manual approval review: {events:?}"
+        "expected selecting Ask For Approval from Approve For Me to switch back to manual approval review: {events:?}"
     );
     assert!(
         !events
@@ -1067,8 +1092,8 @@ async fn permissions_selection_sends_approvals_reviewer_in_override_turn_context
     assert!(
         popup
             .lines()
-            .any(|line| line.contains("Approve for me") && line.contains('›')),
-        "expected one Down from Ask for approval to select Approve for me: {popup}"
+            .any(|line| line.contains("Approve For Me") && line.contains('›')),
+        "expected one Down from Ask For Approval to select Approve For Me: {popup}"
     );
     chat.handle_key_event(KeyEvent::from(KeyCode::Enter));
 
