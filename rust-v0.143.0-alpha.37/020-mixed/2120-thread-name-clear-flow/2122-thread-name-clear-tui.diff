diff --git a/codex-rs/tui/src/bottom_pane/custom_prompt_view.rs b/codex-rs/tui/src/bottom_pane/custom_prompt_view.rs
index bb81af7478..726d834cfa 100644
--- a/codex-rs/tui/src/bottom_pane/custom_prompt_view.rs
+++ b/codex-rs/tui/src/bottom_pane/custom_prompt_view.rs
@@ -31,8 +31,10 @@ pub(crate) type PromptSubmitted = Box<dyn Fn(String) + Send + Sync>;
 /// Minimal multi-line text input view to collect custom review instructions.
 pub(crate) struct CustomPromptView {
     title: String,
+    title_hint: Option<String>,
     placeholder: String,
     context_label: Option<String>,
+    allow_empty_submit: bool,
     on_submit: PromptSubmitted,
 
     // UI state
@@ -49,6 +51,46 @@ impl CustomPromptView {
         initial_text: String,
         context_label: Option<String>,
         on_submit: PromptSubmitted,
+    ) -> Self {
+        Self::new_with_options(
+            title,
+            /*title_hint*/ None,
+            placeholder,
+            initial_text,
+            context_label,
+            /*allow_empty_submit*/ false,
+            on_submit,
+        )
+    }
+
+    pub(crate) fn new_with_title_hint_and_empty_submit(
+        title: String,
+        title_hint: Option<String>,
+        placeholder: String,
+        initial_text: String,
+        context_label: Option<String>,
+        allow_empty_submit: bool,
+        on_submit: PromptSubmitted,
+    ) -> Self {
+        Self::new_with_options(
+            title,
+            title_hint,
+            placeholder,
+            initial_text,
+            context_label,
+            allow_empty_submit,
+            on_submit,
+        )
+    }
+
+    fn new_with_options(
+        title: String,
+        title_hint: Option<String>,
+        placeholder: String,
+        initial_text: String,
+        context_label: Option<String>,
+        allow_empty_submit: bool,
+        on_submit: PromptSubmitted,
     ) -> Self {
         let mut textarea = TextArea::new();
         if !initial_text.is_empty() {
@@ -58,8 +100,10 @@ impl CustomPromptView {
 
         Self {
             title,
+            title_hint,
             placeholder,
             context_label,
+            allow_empty_submit,
             on_submit,
             textarea,
             textarea_state: RefCell::new(TextAreaState::default()),
@@ -87,7 +131,7 @@ impl CustomPromptView {
                 }
                 if modifiers == KeyModifiers::NONE {
                     let text = self.textarea.text().trim().to_string();
-                    if !text.is_empty() {
+                    if self.allow_empty_submit || !text.is_empty() {
                         (self.on_submit)(text);
                         self.completion = Some(ViewCompletion::Accepted);
                     }
@@ -177,7 +221,11 @@ impl Renderable for CustomPromptView {
             width: area.width,
             height: 1,
         };
-        let title_spans: Vec<Span<'static>> = vec![gutter(), self.title.clone().bold()];
+        let mut title_spans: Vec<Span<'static>> = vec![gutter(), self.title.clone().bold()];
+        if let Some(title_hint) = &self.title_hint {
+            title_spans.push(" ".into());
+            title_spans.push(title_hint.clone().yellow());
+        }
         Paragraph::new(Line::from(title_spans)).render(title_area, buf);
 
         // Optional context line
diff --git a/codex-rs/tui/src/chatwidget/interaction.rs b/codex-rs/tui/src/chatwidget/interaction.rs
index aa55492a0b..0eebfb1ea3 100644
--- a/codex-rs/tui/src/chatwidget/interaction.rs
+++ b/codex-rs/tui/src/chatwidget/interaction.rs
@@ -332,12 +332,21 @@ impl ChatWidget {
         } else {
             "Name thread"
         };
-        let view = CustomPromptView::new(
+        let title_hint = existing_name
+            .is_some()
+            .then(|| "clear to remove name".to_string());
+        let view = CustomPromptView::new_with_title_hint_and_empty_submit(
             title.to_string(),
+            title_hint,
             "Type a name and press Enter".to_string(),
             /*initial_text*/ existing_name.unwrap_or_default().to_string(),
             /*context_label*/ None,
+            /*allow_empty_submit*/ existing_name.is_some(),
             Box::new(move |name: String| {
+                if name.trim().is_empty() {
+                    tx.set_thread_name(None);
+                    return;
+                }
                 let Some(name) = normalize_thread_name(&name) else {
                     tx.send(AppEvent::InsertHistoryCell(Box::new(
                         history_cell::new_error_event("Thread name cannot be empty.".to_string()),
diff --git a/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__slash_rename_prefilled_prompt.snap b/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__slash_rename_prefilled_prompt.snap
index f1eb0d223b..d26821cda3 100644
--- a/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__slash_rename_prefilled_prompt.snap
+++ b/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__slash_rename_prefilled_prompt.snap
@@ -1,9 +1,8 @@
 ---
 source: tui/src/chatwidget/tests/slash_commands.rs
-assertion_line: 135
 expression: popup
 ---
-▌ Rename thread
+▌ Rename thread clear to remove name
 ▌
 ▌ Current project title
 
diff --git a/codex-rs/tui/src/chatwidget/tests/slash_commands.rs b/codex-rs/tui/src/chatwidget/tests/slash_commands.rs
index fbf022fe57..077e86d343 100644
--- a/codex-rs/tui/src/chatwidget/tests/slash_commands.rs
+++ b/codex-rs/tui/src/chatwidget/tests/slash_commands.rs
@@ -1201,6 +1201,24 @@ async fn slash_rename_without_existing_thread_name_starts_empty() {
     assert_matches!(rx.try_recv(), Err(TryRecvError::Empty));
 }
 
+#[tokio::test]
+async fn slash_rename_existing_thread_name_can_be_cleared() {
+    let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+    chat.thread_name = Some("Current project title".to_string());
+
+    chat.dispatch_command(SlashCommand::Rename);
+
+    for _ in 0.."Current project title".len() {
+        chat.handle_key_event(KeyEvent::new(KeyCode::Backspace, KeyModifiers::NONE));
+    }
+    chat.handle_key_event(KeyEvent::new(KeyCode::Enter, KeyModifiers::NONE));
+
+    assert_matches!(
+        rx.try_recv(),
+        Ok(AppEvent::CodexOp(Op::SetThreadName { name })) if name.is_none()
+    );
+}
+
 #[tokio::test]
 async fn usage_error_slash_command_is_available_from_local_recall() {
     let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(Some("gpt-5.3-codex")).await;
