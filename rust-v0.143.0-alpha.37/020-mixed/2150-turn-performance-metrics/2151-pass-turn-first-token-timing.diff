diff --git a/codex-rs/analytics/src/analytics_client_tests.rs b/codex-rs/analytics/src/analytics_client_tests.rs
index bf4434fa0c..83e42c944e 100644
--- a/codex-rs/analytics/src/analytics_client_tests.rs
+++ b/codex-rs/analytics/src/analytics_client_tests.rs
@@ -400,6 +400,7 @@ fn sample_turn_completed_notification(
             completed_at: Some(456),
             duration_ms: Some(1234),
         },
+        time_to_first_token_ms: None,
     })
 }
 
diff --git a/codex-rs/app-server-client/src/lib.rs b/codex-rs/app-server-client/src/lib.rs
index 08cbaf8bba..3838dc509a 100644
--- a/codex-rs/app-server-client/src/lib.rs
+++ b/codex-rs/app-server-client/src/lib.rs
@@ -961,6 +961,7 @@ mod tests {
     use futures::SinkExt;
     use futures::StreamExt;
     use pretty_assertions::assert_eq;
+    use std::future::Future;
     use std::ops::Deref;
     use std::path::Path;
     use tempfile::TempDir;
@@ -1018,6 +1019,26 @@ mod tests {
         }
     }
 
+    fn run_current_thread_test_with_stack<F>(name: &str, future: F)
+    where
+        F: Future<Output = ()> + Send + 'static,
+    {
+        let handle = std::thread::Builder::new()
+            .name(name.to_string())
+            .stack_size(32 * 1024 * 1024)
+            .spawn(move || {
+                tokio::runtime::Builder::new_current_thread()
+                    .enable_all()
+                    .build()
+                    .expect("test runtime should build")
+                    .block_on(future);
+            })
+            .expect("test thread should spawn");
+        handle
+            .join()
+            .unwrap_or_else(|_| panic!("{name} thread panicked"));
+    }
+
     async fn start_test_client_with_capacity(
         session_source: SessionSource,
         channel_capacity: usize,
@@ -1226,6 +1247,7 @@ mod tests {
                 completed_at: Some(0),
                 duration_ms: Some(1),
             },
+            time_to_first_token_ms: None,
         })
     }
 
@@ -1257,106 +1279,125 @@ mod tests {
         );
     }
 
-    #[tokio::test]
-    async fn typed_request_roundtrip_works() {
-        let client = start_test_client(SessionSource::Exec).await;
-        let _response: ConfigRequirementsReadResponse = client
-            .request_typed(ClientRequest::ConfigRequirementsRead {
-                request_id: RequestId::Integer(1),
-                params: None,
-            })
-            .await
-            .expect("typed request should succeed");
-        client.shutdown().await.expect("shutdown should complete");
-    }
-
-    #[tokio::test]
-    async fn typed_request_reports_json_rpc_errors() {
-        let client = start_test_client(SessionSource::Exec).await;
-        let err = client
-            .request_typed::<ConfigRequirementsReadResponse>(ClientRequest::ThreadRead {
-                request_id: RequestId::Integer(99),
-                params: codex_app_server_protocol::ThreadReadParams {
-                    thread_id: "missing-thread".to_string(),
-                    include_turns: false,
-                    history_window: None,
-                },
-            })
-            .await
-            .expect_err("missing thread should return a JSON-RPC error");
-        assert!(
-            err.to_string().starts_with("thread/read failed:"),
-            "expected method-qualified JSON-RPC failure message"
-        );
-        client.shutdown().await.expect("shutdown should complete");
-    }
-
-    #[tokio::test]
-    async fn caller_provided_session_source_is_applied() {
-        for (session_source, expected_source) in [
-            (SessionSource::Exec, ApiSessionSource::Exec),
-            (SessionSource::Cli, ApiSessionSource::Cli),
-        ] {
-            let client = start_test_client(session_source).await;
-            let parsed: ThreadStartResponse = client
-                .request_typed(ClientRequest::ThreadStart {
-                    request_id: RequestId::Integer(2),
-                    params: ThreadStartParams {
-                        ephemeral: Some(true),
-                        ..ThreadStartParams::default()
-                    },
+    #[test]
+    fn typed_request_roundtrip_works() {
+        run_current_thread_test_with_stack("typed_request_roundtrip_works", async {
+            let client = start_test_client(SessionSource::Exec).await;
+            let _response: ConfigRequirementsReadResponse = client
+                .request_typed(ClientRequest::ConfigRequirementsRead {
+                    request_id: RequestId::Integer(1),
+                    params: None,
                 })
                 .await
-                .expect("thread/start should succeed");
-            assert_eq!(parsed.thread.source, expected_source);
+                .expect("typed request should succeed");
             client.shutdown().await.expect("shutdown should complete");
-        }
+        });
     }
 
-    #[tokio::test]
-    async fn threads_started_via_app_server_are_visible_through_typed_requests() {
-        let client = start_test_client(SessionSource::Cli).await;
-
-        let response: ThreadStartResponse = client
-            .request_typed(ClientRequest::ThreadStart {
-                request_id: RequestId::Integer(3),
-                params: ThreadStartParams {
-                    ephemeral: Some(true),
-                    ..ThreadStartParams::default()
-                },
-            })
-            .await
-            .expect("thread/start should succeed");
-        let read = client
-            .request_typed::<codex_app_server_protocol::ThreadReadResponse>(
-                ClientRequest::ThreadRead {
-                    request_id: RequestId::Integer(4),
+    #[test]
+    fn typed_request_reports_json_rpc_errors() {
+        run_current_thread_test_with_stack("typed_request_reports_json_rpc_errors", async {
+            let client = start_test_client(SessionSource::Exec).await;
+            let err = client
+                .request_typed::<ConfigRequirementsReadResponse>(ClientRequest::ThreadRead {
+                    request_id: RequestId::Integer(99),
                     params: codex_app_server_protocol::ThreadReadParams {
-                        thread_id: response.thread.id.clone(),
+                        thread_id: "missing-thread".to_string(),
                         include_turns: false,
                         history_window: None,
                     },
-                },
-            )
-            .await
-            .expect("thread/read should return the newly started thread");
-        assert_eq!(read.thread.id, response.thread.id);
+                })
+                .await
+                .expect_err("missing thread should return a JSON-RPC error");
+            assert!(
+                err.to_string().starts_with("thread/read failed:"),
+                "expected method-qualified JSON-RPC failure message"
+            );
+            client.shutdown().await.expect("shutdown should complete");
+        });
+    }
 
-        client.shutdown().await.expect("shutdown should complete");
+    #[test]
+    fn caller_provided_session_source_is_applied() {
+        run_current_thread_test_with_stack("caller_provided_session_source_is_applied", async {
+            for (session_source, expected_source) in [
+                (SessionSource::Exec, ApiSessionSource::Exec),
+                (SessionSource::Cli, ApiSessionSource::Cli),
+            ] {
+                let client = start_test_client(session_source).await;
+                let parsed: ThreadStartResponse = client
+                    .request_typed(ClientRequest::ThreadStart {
+                        request_id: RequestId::Integer(2),
+                        params: ThreadStartParams {
+                            ephemeral: Some(true),
+                            ..ThreadStartParams::default()
+                        },
+                    })
+                    .await
+                    .expect("thread/start should succeed");
+                assert_eq!(parsed.thread.source, expected_source);
+                client.shutdown().await.expect("shutdown should complete");
+            }
+        });
     }
 
-    #[tokio::test]
-    async fn tiny_channel_capacity_still_supports_request_roundtrip() {
-        let client =
-            start_test_client_with_capacity(SessionSource::Exec, /*channel_capacity*/ 1).await;
-        let _response: ConfigRequirementsReadResponse = client
-            .request_typed(ClientRequest::ConfigRequirementsRead {
-                request_id: RequestId::Integer(1),
-                params: None,
-            })
-            .await
-            .expect("typed request should succeed");
-        client.shutdown().await.expect("shutdown should complete");
+    #[test]
+    fn threads_started_via_app_server_are_visible_through_typed_requests() {
+        run_current_thread_test_with_stack(
+            "threads_started_via_app_server_are_visible_through_typed_requests",
+            async {
+                let client = start_test_client(SessionSource::Cli).await;
+
+                let response: ThreadStartResponse = client
+                    .request_typed(ClientRequest::ThreadStart {
+                        request_id: RequestId::Integer(3),
+                        params: ThreadStartParams {
+                            ephemeral: Some(true),
+                            ..ThreadStartParams::default()
+                        },
+                    })
+                    .await
+                    .expect("thread/start should succeed");
+                let read = client
+                    .request_typed::<codex_app_server_protocol::ThreadReadResponse>(
+                        ClientRequest::ThreadRead {
+                            request_id: RequestId::Integer(4),
+                            params: codex_app_server_protocol::ThreadReadParams {
+                                thread_id: response.thread.id.clone(),
+                                include_turns: false,
+                                history_window: None,
+                            },
+                        },
+                    )
+                    .await
+                    .expect("thread/read should return the newly started thread");
+                assert_eq!(read.thread.id, response.thread.id);
+
+                client.shutdown().await.expect("shutdown should complete");
+            },
+        );
+    }
+
+    #[test]
+    fn tiny_channel_capacity_still_supports_request_roundtrip() {
+        run_current_thread_test_with_stack(
+            "tiny_channel_capacity_still_supports_request_roundtrip",
+            async {
+                let client = start_test_client_with_capacity(
+                    SessionSource::Exec,
+                    /*channel_capacity*/ 1,
+                )
+                .await;
+                let _response: ConfigRequirementsReadResponse = client
+                    .request_typed(ClientRequest::ConfigRequirementsRead {
+                        request_id: RequestId::Integer(1),
+                        params: None,
+                    })
+                    .await
+                    .expect("typed request should succeed");
+                client.shutdown().await.expect("shutdown should complete");
+            },
+        );
     }
 
     #[tokio::test]
@@ -2148,6 +2189,7 @@ mod tests {
                             completed_at: Some(0),
                             duration_ms: None,
                         },
+                        time_to_first_token_ms: None,
                     }
                 )
             )
diff --git a/codex-rs/app-server-protocol/schema/typescript/v2/TurnCompletedNotification.ts b/codex-rs/app-server-protocol/schema/typescript/v2/TurnCompletedNotification.ts
index e1b151bfa7..fa94896d13 100644
--- a/codex-rs/app-server-protocol/schema/typescript/v2/TurnCompletedNotification.ts
+++ b/codex-rs/app-server-protocol/schema/typescript/v2/TurnCompletedNotification.ts
@@ -3,4 +3,8 @@
 // This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
 import type { Turn } from "./Turn";
 
-export type TurnCompletedNotification = { threadId: string, turn: Turn, };
+export type TurnCompletedNotification = { threadId: string, turn: Turn,
+/**
+ * Duration between turn start and the first meaningful model output in milliseconds, if known.
+ */
+timeToFirstTokenMs: number | null, };
diff --git a/codex-rs/app-server-protocol/src/protocol/v2/turn.rs b/codex-rs/app-server-protocol/src/protocol/v2/turn.rs
index 0bf9152338..234971856c 100644
--- a/codex-rs/app-server-protocol/src/protocol/v2/turn.rs
+++ b/codex-rs/app-server-protocol/src/protocol/v2/turn.rs
@@ -422,6 +422,9 @@ pub struct Usage {
 pub struct TurnCompletedNotification {
     pub thread_id: String,
     pub turn: Turn,
+    /// Duration between turn start and the first meaningful model output in milliseconds, if known.
+    #[ts(type = "number | null")]
+    pub time_to_first_token_ms: Option<i64>,
 }
 
 #[derive(Serialize, Deserialize, Debug, Clone, PartialEq, JsonSchema, TS)]
diff --git a/codex-rs/app-server/src/bespoke_event_handling.rs b/codex-rs/app-server/src/bespoke_event_handling.rs
index 6ba9307650..6571b17a86 100644
--- a/codex-rs/app-server/src/bespoke_event_handling.rs
+++ b/codex-rs/app-server/src/bespoke_event_handling.rs
@@ -1357,6 +1357,7 @@ struct TurnCompletionMetadata {
     started_at: Option<i64>,
     completed_at: Option<i64>,
     duration_ms: Option<i64>,
+    time_to_first_token_ms: Option<i64>,
 }
 
 async fn emit_turn_completed_with_status(
@@ -1377,6 +1378,7 @@ async fn emit_turn_completed_with_status(
             completed_at: turn_completion_metadata.completed_at,
             duration_ms: turn_completion_metadata.duration_ms,
         },
+        time_to_first_token_ms: turn_completion_metadata.time_to_first_token_ms,
     };
     outgoing
         .send_server_notification(ServerNotification::TurnCompleted(notification))
@@ -1573,6 +1575,7 @@ async fn handle_turn_complete(
             started_at: turn_summary.started_at,
             completed_at: turn_complete_event.completed_at,
             duration_ms: turn_complete_event.duration_ms,
+            time_to_first_token_ms: turn_complete_event.time_to_first_token_ms,
         },
         outgoing,
     )
@@ -1597,6 +1600,7 @@ async fn handle_turn_interrupted(
             started_at: turn_summary.started_at,
             completed_at: turn_aborted_event.completed_at,
             duration_ms: turn_aborted_event.duration_ms,
+            time_to_first_token_ms: None,
         },
         outgoing,
     )
diff --git a/codex-rs/app-server/src/in_process.rs b/codex-rs/app-server/src/in_process.rs
index 085544bd4f..e11837796f 100644
--- a/codex-rs/app-server/src/in_process.rs
+++ b/codex-rs/app-server/src/in_process.rs
@@ -908,6 +908,7 @@ mod tests {
                     completed_at: Some(0),
                     duration_ms: None,
                 },
+                time_to_first_token_ms: None,
             })
         ));
         assert!(server_notification_requires_delivery(
diff --git a/codex-rs/core/src/turn_timing.rs b/codex-rs/core/src/turn_timing.rs
index 04f7aeb69b..bb42d27505 100644
--- a/codex-rs/core/src/turn_timing.rs
+++ b/codex-rs/core/src/turn_timing.rs
@@ -332,9 +332,7 @@ fn response_event_records_turn_ttft(event: &ResponseEvent) -> bool {
         ResponseEvent::OutputItemDone(item) | ResponseEvent::OutputItemAdded(item) => {
             response_item_records_turn_ttft(item)
         }
-        ResponseEvent::OutputTextDelta(_)
-        | ResponseEvent::ReasoningSummaryDelta { .. }
-        | ResponseEvent::ReasoningContentDelta { .. } => true,
+        ResponseEvent::OutputTextDelta(delta) => !delta.is_empty(),
         ResponseEvent::Created
         | ResponseEvent::ServerModel(_)
         | ResponseEvent::ModelVerifications(_)
@@ -343,6 +341,8 @@ fn response_event_records_turn_ttft(event: &ResponseEvent) -> bool {
         | ResponseEvent::ServerReasoningIncluded(_)
         | ResponseEvent::ToolCallInputDelta { .. }
         | ResponseEvent::Completed { .. }
+        | ResponseEvent::ReasoningSummaryDelta { .. }
+        | ResponseEvent::ReasoningContentDelta { .. }
         | ResponseEvent::ReasoningSummaryPartAdded { .. }
         | ResponseEvent::RateLimits(_)
         | ResponseEvent::ModelsEtag(_) => false,
@@ -354,22 +354,7 @@ fn response_item_records_turn_ttft(item: &ResponseItem) -> bool {
         ResponseItem::Message { .. } => {
             raw_assistant_output_text_from_item(item).is_some_and(|text| !text.is_empty())
         }
-        ResponseItem::Reasoning {
-            summary, content, ..
-        } => {
-            summary.iter().any(|entry| match entry {
-                codex_protocol::models::ReasoningItemReasoningSummary::SummaryText { text } => {
-                    !text.is_empty()
-                }
-            }) || content.as_ref().is_some_and(|entries| {
-                entries.iter().any(|entry| match entry {
-                    codex_protocol::models::ReasoningItemContent::ReasoningText { text }
-                    | codex_protocol::models::ReasoningItemContent::Text { text } => {
-                        !text.is_empty()
-                    }
-                })
-            })
-        }
+        ResponseItem::Reasoning { .. } => false,
         ResponseItem::AgentMessage { .. } => false,
         ResponseItem::LocalShellCall { .. }
         | ResponseItem::FunctionCall { .. }
@@ -378,7 +363,7 @@ fn response_item_records_turn_ttft(item: &ResponseItem) -> bool {
         | ResponseItem::WebSearchCall { .. }
         | ResponseItem::ImageGenerationCall { .. }
         | ResponseItem::Compaction { .. }
-        | ResponseItem::ContextCompaction { .. } => true,
+        | ResponseItem::ContextCompaction { .. } => false,
         ResponseItem::CompactionTrigger { .. } => false,
         ResponseItem::AdditionalTools { .. }
         | ResponseItem::FunctionCallOutput { .. }
diff --git a/codex-rs/core/src/turn_timing_tests.rs b/codex-rs/core/src/turn_timing_tests.rs
index fcb34b4742..6b88437809 100644
--- a/codex-rs/core/src/turn_timing_tests.rs
+++ b/codex-rs/core/src/turn_timing_tests.rs
@@ -3,6 +3,8 @@ use codex_protocol::items::AgentMessageItem;
 use codex_protocol::items::TurnItem;
 use codex_protocol::models::ContentItem;
 use codex_protocol::models::FunctionCallOutputPayload;
+use codex_protocol::models::ReasoningItemContent;
+use codex_protocol::models::ReasoningItemReasoningSummary;
 use codex_protocol::models::ResponseItem;
 use pretty_assertions::assert_eq;
 use std::time::Duration;
@@ -13,6 +15,7 @@ use std::time::UNIX_EPOCH;
 use super::TurnProfilePhase;
 use super::TurnProfileState;
 use super::TurnTimingState;
+use super::response_event_records_turn_ttft;
 use super::response_item_records_turn_ttft;
 use crate::ResponseEvent;
 
@@ -104,28 +107,29 @@ async fn turn_timing_state_records_turn_started_epoch_millis() {
 }
 
 #[test]
-fn response_item_records_turn_ttft_for_first_output_signals() {
-    assert!(response_item_records_turn_ttft(
-        &ResponseItem::FunctionCall {
-            id: None,
-            name: "shell".to_string(),
-            namespace: None,
-            arguments: "{}".to_string(),
-            call_id: "call-1".to_string(),
-            internal_chat_message_metadata_passthrough: None,
+fn response_event_records_turn_ttft_for_visible_assistant_text() {
+    assert!(response_event_records_turn_ttft(
+        &ResponseEvent::OutputTextDelta("hi".to_string())
+    ));
+    assert!(!response_event_records_turn_ttft(
+        &ResponseEvent::OutputTextDelta(String::new())
+    ));
+    assert!(!response_event_records_turn_ttft(
+        &ResponseEvent::ReasoningSummaryDelta {
+            delta: "thinking".to_string(),
+            summary_index: 0,
         }
     ));
-    assert!(response_item_records_turn_ttft(
-        &ResponseItem::CustomToolCall {
-            id: None,
-            status: None,
-            call_id: "call-2".to_string(),
-            name: "custom".to_string(),
-            namespace: None,
-            input: "echo hi".to_string(),
-            internal_chat_message_metadata_passthrough: None,
+    assert!(!response_event_records_turn_ttft(
+        &ResponseEvent::ReasoningContentDelta {
+            delta: "raw thinking".to_string(),
+            content_index: 0,
         }
     ));
+}
+
+#[test]
+fn response_item_records_turn_ttft_for_visible_assistant_text() {
     assert!(response_item_records_turn_ttft(&ResponseItem::Message {
         id: None,
         role: "assistant".to_string(),
@@ -138,7 +142,7 @@ fn response_item_records_turn_ttft_for_first_output_signals() {
 }
 
 #[test]
-fn response_item_records_turn_ttft_ignores_empty_non_output_items() {
+fn response_item_records_turn_ttft_ignores_non_visible_text_items() {
     assert!(!response_item_records_turn_ttft(&ResponseItem::Message {
         id: None,
         role: "assistant".to_string(),
@@ -156,6 +160,27 @@ fn response_item_records_turn_ttft_ignores_empty_non_output_items() {
             internal_chat_message_metadata_passthrough: None,
         }
     ));
+    assert!(!response_item_records_turn_ttft(
+        &ResponseItem::FunctionCall {
+            id: None,
+            name: "shell".to_string(),
+            namespace: None,
+            arguments: "{}".to_string(),
+            call_id: "call-1".to_string(),
+            internal_chat_message_metadata_passthrough: None,
+        }
+    ));
+    assert!(!response_item_records_turn_ttft(&ResponseItem::Reasoning {
+        id: Some("reasoning-1".to_string()),
+        summary: vec![ReasoningItemReasoningSummary::SummaryText {
+            text: "summary".to_string(),
+        }],
+        content: Some(vec![ReasoningItemContent::ReasoningText {
+            text: "raw thinking".to_string(),
+        }]),
+        encrypted_content: None,
+        internal_chat_message_metadata_passthrough: None,
+    }));
 }
 
 #[test]
diff --git a/codex-rs/exec/src/event_processor_with_human_output_tests.rs b/codex-rs/exec/src/event_processor_with_human_output_tests.rs
index 44a2371a1c..3d6e13ca61 100644
--- a/codex-rs/exec/src/event_processor_with_human_output_tests.rs
+++ b/codex-rs/exec/src/event_processor_with_human_output_tests.rs
@@ -325,6 +325,7 @@ fn turn_completed_recovers_final_message_from_turn_items() {
                 completed_at: Some(0),
                 duration_ms: None,
             },
+            time_to_first_token_ms: None,
         },
     ));
 
@@ -373,6 +374,7 @@ fn turn_completed_overwrites_stale_final_message_from_turn_items() {
                 completed_at: Some(0),
                 duration_ms: None,
             },
+            time_to_first_token_ms: None,
         },
     ));
 
@@ -417,6 +419,7 @@ fn turn_completed_preserves_streamed_final_message_when_turn_items_are_empty() {
                 completed_at: Some(0),
                 duration_ms: None,
             },
+            time_to_first_token_ms: None,
         },
     ));
 
@@ -461,6 +464,7 @@ fn turn_failed_clears_stale_final_message() {
                 completed_at: Some(0),
                 duration_ms: None,
             },
+            time_to_first_token_ms: None,
         },
     ));
 
@@ -506,6 +510,7 @@ fn turn_interrupted_clears_stale_final_message() {
                 completed_at: Some(0),
                 duration_ms: None,
             },
+            time_to_first_token_ms: None,
         },
     ));
 
diff --git a/codex-rs/exec/src/event_processor_with_jsonl_output_tests.rs b/codex-rs/exec/src/event_processor_with_jsonl_output_tests.rs
index 83cd6d3c6b..25ed62c46a 100644
--- a/codex-rs/exec/src/event_processor_with_jsonl_output_tests.rs
+++ b/codex-rs/exec/src/event_processor_with_jsonl_output_tests.rs
@@ -45,6 +45,7 @@ fn failed_turn_does_not_overwrite_output_last_message_file() {
                 completed_at: Some(0),
                 duration_ms: None,
             },
+            time_to_first_token_ms: None,
         },
     ));
 
diff --git a/codex-rs/exec/src/lib_tests.rs b/codex-rs/exec/src/lib_tests.rs
index 97ded50525..75a0ea1f54 100644
--- a/codex-rs/exec/src/lib_tests.rs
+++ b/codex-rs/exec/src/lib_tests.rs
@@ -414,6 +414,7 @@ fn should_backfill_turn_completed_items_skips_ephemeral_threads() {
                 completed_at: None,
                 duration_ms: None,
             },
+            time_to_first_token_ms: None,
         });
 
     assert!(!should_backfill_turn_completed_items(
diff --git a/codex-rs/exec/tests/event_processor_with_json_output.rs b/codex-rs/exec/tests/event_processor_with_json_output.rs
index 6ed00ec476..1ac1322b6d 100644
--- a/codex-rs/exec/tests/event_processor_with_json_output.rs
+++ b/codex-rs/exec/tests/event_processor_with_json_output.rs
@@ -1127,6 +1127,7 @@ fn plan_update_emits_started_then_updated_then_completed() {
                 completed_at: None,
                 duration_ms: None,
             },
+            time_to_first_token_ms: None,
         },
     ));
     assert_eq!(
@@ -1187,6 +1188,7 @@ fn plan_update_after_completion_starts_new_todo_list_with_new_id() {
                 completed_at: None,
                 duration_ms: None,
             },
+            time_to_first_token_ms: None,
         },
     ));
 
@@ -1270,6 +1272,7 @@ fn token_usage_update_is_emitted_on_turn_completion() {
                 completed_at: None,
                 duration_ms: None,
             },
+            time_to_first_token_ms: None,
         },
     ));
     assert_eq!(
@@ -1310,6 +1313,7 @@ fn turn_completion_recovers_final_message_from_turn_items() {
                 completed_at: None,
                 duration_ms: None,
             },
+            time_to_first_token_ms: None,
         },
     ));
 
@@ -1389,6 +1393,7 @@ fn turn_completion_reconciles_started_items_from_turn_items() {
                 completed_at: None,
                 duration_ms: None,
             },
+            time_to_first_token_ms: None,
         },
     ));
 
@@ -1451,6 +1456,7 @@ fn turn_completion_overwrites_stale_final_message_from_turn_items() {
                 completed_at: None,
                 duration_ms: None,
             },
+            time_to_first_token_ms: None,
         },
     ));
 
@@ -1496,6 +1502,7 @@ fn turn_completion_preserves_streamed_final_message_when_turn_items_are_empty()
                 completed_at: None,
                 duration_ms: None,
             },
+            time_to_first_token_ms: None,
         },
     ));
 
@@ -1549,6 +1556,7 @@ fn failed_turn_clears_stale_final_message() {
                 completed_at: None,
                 duration_ms: None,
             },
+            time_to_first_token_ms: None,
         },
     ));
 
@@ -1576,6 +1584,7 @@ fn turn_completion_falls_back_to_final_plan_text() {
                 completed_at: None,
                 duration_ms: None,
             },
+            time_to_first_token_ms: None,
         },
     ));
 
@@ -1629,6 +1638,7 @@ fn turn_failure_prefers_structured_error_message() {
                 completed_at: None,
                 duration_ms: None,
             },
+            time_to_first_token_ms: None,
         },
     ));
     assert_eq!(
diff --git a/codex-rs/tui/src/app/pending_interactive_replay.rs b/codex-rs/tui/src/app/pending_interactive_replay.rs
index d204105356..57c31a9313 100644
--- a/codex-rs/tui/src/app/pending_interactive_replay.rs
+++ b/codex-rs/tui/src/app/pending_interactive_replay.rs
@@ -677,6 +677,7 @@ mod tests {
                 completed_at: Some(0),
                 duration_ms: Some(1),
             },
+            time_to_first_token_ms: None,
         })
     }
 
diff --git a/codex-rs/tui/src/app/tests.rs b/codex-rs/tui/src/app/tests.rs
index 228b53fe06..e505febe59 100644
--- a/codex-rs/tui/src/app/tests.rs
+++ b/codex-rs/tui/src/app/tests.rs
@@ -4708,6 +4708,7 @@ fn turn_completed_notification(
             duration_ms: Some(1),
             ..test_turn(turn_id, status, Vec::new())
         },
+        time_to_first_token_ms: None,
     })
 }
 
diff --git a/codex-rs/tui/src/app/thread_events.rs b/codex-rs/tui/src/app/thread_events.rs
index b2186f207e..e310ca00d8 100644
--- a/codex-rs/tui/src/app/thread_events.rs
+++ b/codex-rs/tui/src/app/thread_events.rs
@@ -422,6 +422,7 @@ mod tests {
                 duration_ms: Some(1),
                 ..test_turn(turn_id, status, Vec::new())
             },
+            time_to_first_token_ms: None,
         })
     }
 
diff --git a/codex-rs/tui/src/chatwidget/replay.rs b/codex-rs/tui/src/chatwidget/replay.rs
index f155eb1f68..b53ef616e8 100644
--- a/codex-rs/tui/src/chatwidget/replay.rs
+++ b/codex-rs/tui/src/chatwidget/replay.rs
@@ -48,6 +48,7 @@ impl ChatWidget {
                             completed_at,
                             duration_ms,
                         },
+                        time_to_first_token_ms: None,
                     },
                     Some(replay_kind),
                 );
diff --git a/codex-rs/tui/src/chatwidget/tests/app_server.rs b/codex-rs/tui/src/chatwidget/tests/app_server.rs
index 759322bc43..5e69346419 100644
--- a/codex-rs/tui/src/chatwidget/tests/app_server.rs
+++ b/codex-rs/tui/src/chatwidget/tests/app_server.rs
@@ -571,6 +571,7 @@ async fn live_app_server_turn_completed_clears_working_status_after_answer_item(
                 completed_at: Some(0),
                 duration_ms: None,
             },
+            time_to_first_token_ms: None,
         }),
         /*replay_kind*/ None,
     );
@@ -1002,6 +1003,7 @@ async fn live_app_server_failed_turn_does_not_duplicate_error_history() {
                 completed_at: Some(0),
                 duration_ms: None,
             },
+            time_to_first_token_ms: None,
         }),
         /*replay_kind*/ None,
     );
diff --git a/codex-rs/tui/src/chatwidget/tests/helpers.rs b/codex-rs/tui/src/chatwidget/tests/helpers.rs
index 3cc43e352c..e508c77735 100644
--- a/codex-rs/tui/src/chatwidget/tests/helpers.rs
+++ b/codex-rs/tui/src/chatwidget/tests/helpers.rs
@@ -1029,6 +1029,7 @@ pub(super) fn handle_turn_completed(
                 duration_ms,
                 /*error*/ None,
             ),
+            time_to_first_token_ms: None,
         }),
         /*replay_kind*/ None,
     );
@@ -1044,6 +1045,7 @@ pub(super) fn handle_turn_interrupted(chat: &mut ChatWidget, turn_id: &str) {
                 /*duration_ms*/ None,
                 /*error*/ None,
             ),
+            time_to_first_token_ms: None,
         }),
         /*replay_kind*/ None,
     );
diff --git a/codex-rs/tui/src/chatwidget/tests/plan_mode.rs b/codex-rs/tui/src/chatwidget/tests/plan_mode.rs
index 57cc42f863..e794ee4953 100644
--- a/codex-rs/tui/src/chatwidget/tests/plan_mode.rs
+++ b/codex-rs/tui/src/chatwidget/tests/plan_mode.rs
@@ -1207,6 +1207,7 @@ async fn submit_user_message_queues_while_compaction_turn_is_running() {
                 completed_at: Some(0),
                 duration_ms: None,
             },
+            time_to_first_token_ms: None,
         }),
         /*replay_kind*/ None,
     );
diff --git a/codex-rs/tui/src/chatwidget/tests/review_mode.rs b/codex-rs/tui/src/chatwidget/tests/review_mode.rs
index f188d9e76b..a8303d9585 100644
--- a/codex-rs/tui/src/chatwidget/tests/review_mode.rs
+++ b/codex-rs/tui/src/chatwidget/tests/review_mode.rs
@@ -1284,6 +1284,7 @@ async fn interrupted_turn_after_goal_budget_limited_uses_budget_message_snapshot
                     completed_at: None,
                     duration_ms: None,
                 },
+                time_to_first_token_ms: None,
             },
         ),
         /*replay_kind*/ None,
