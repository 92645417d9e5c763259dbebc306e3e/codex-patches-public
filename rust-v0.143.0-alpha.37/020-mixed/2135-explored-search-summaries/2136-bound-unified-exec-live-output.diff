diff --git a/codex-rs/core/src/tools/handlers/shell_spec.rs b/codex-rs/core/src/tools/handlers/shell_spec.rs
index 5fba2cc87a..846088685b 100644
--- a/codex-rs/core/src/tools/handlers/shell_spec.rs
+++ b/codex-rs/core/src/tools/handlers/shell_spec.rs
@@ -5,8 +5,6 @@ use serde_json::Value;
 use serde_json::json;
 use std::collections::BTreeMap;
 
-const OUTPUT_FLOOD_GUIDANCE: &str = "When output may be large, cap it before it reaches the tool with shell-side limits like `sed -n '1,200p' file`, `cmd | head -n 200`, or `cmd | head -c 12000`.";
-
 #[derive(Debug, Clone, Copy, PartialEq, Eq)]
 pub struct CommandToolOptions {
     pub allow_login_shell: bool,
@@ -91,13 +89,12 @@ pub(crate) fn create_exec_command_tool_with_environment_id(
         name: "exec_command".to_string(),
         description: if cfg!(windows) {
             format!(
-                "Runs a command in a PTY, returning output or a session ID for ongoing interaction.\n\n{OUTPUT_FLOOD_GUIDANCE}\n\n{}",
+                "Runs a command in a PTY, returning output or a session ID for ongoing interaction.\n\n{}",
                 windows_shell_guidance()
             )
         } else {
-            format!(
-                "Runs a command in a PTY, returning output or a session ID for ongoing interaction.\n\n{OUTPUT_FLOOD_GUIDANCE}"
-            )
+            "Runs a command in a PTY, returning output or a session ID for ongoing interaction."
+                .to_string()
         },
         strict: false,
         defer_loading: None,
@@ -203,12 +200,10 @@ Examples of valid command strings:
 
 {}"#,
             windows_shell_guidance()
-        ) + "\n\n"
-            + OUTPUT_FLOOD_GUIDANCE
+        )
     } else {
         r#"Runs a shell command and returns its output.
-- Always set the `workdir` param when using the shell_command function. Do not use `cd` unless absolutely necessary.
-- When output may be large, cap it before it reaches the tool with shell-side limits like `sed -n '1,200p' file`, `cmd | head -n 200`, or `cmd | head -c 12000`."#
+- Always set the `workdir` param when using the shell_command function. Do not use `cd` unless absolutely necessary."#
             .to_string()
     };
 
diff --git a/codex-rs/core/src/tools/handlers/shell_spec_tests.rs b/codex-rs/core/src/tools/handlers/shell_spec_tests.rs
index 35c5bb72d4..df854e1289 100644
--- a/codex-rs/core/src/tools/handlers/shell_spec_tests.rs
+++ b/codex-rs/core/src/tools/handlers/shell_spec_tests.rs
@@ -22,13 +22,12 @@ fn exec_command_tool_matches_expected_spec() {
 
     let description = if cfg!(windows) {
         format!(
-            "Runs a command in a PTY, returning output or a session ID for ongoing interaction.\n\n{OUTPUT_FLOOD_GUIDANCE}{}",
+            "Runs a command in a PTY, returning output or a session ID for ongoing interaction.{}",
             windows_shell_guidance_description()
         )
     } else {
-        format!(
-            "Runs a command in a PTY, returning output or a session ID for ongoing interaction.\n\n{OUTPUT_FLOOD_GUIDANCE}"
-        )
+        "Runs a command in a PTY, returning output or a session ID for ongoing interaction."
+            .to_string()
     };
 
     let mut properties = BTreeMap::from([
@@ -218,14 +217,12 @@ Examples of valid command strings:
 - ps aux | grep python: "Get-Process | Where-Object { $_.ProcessName -like '*python*' }"
 - setting an env var: "$env:FOO='bar'; echo $env:FOO"
 - running an inline Python script: "@'\\nprint('Hello, world!')\\n'@ | python -"
-
-When output may be large, cap it before it reaches the tool with shell-side limits like `sed -n '1,200p' file`, `cmd | head -n 200`, or `cmd | head -c 12000`."#
+"#
             .to_string()
             + &windows_shell_guidance_description()
     } else {
         r#"Runs a shell command and returns its output.
-- Always set the `workdir` param when using the shell_command function. Do not use `cd` unless absolutely necessary.
-- When output may be large, cap it before it reaches the tool with shell-side limits like `sed -n '1,200p' file`, `cmd | head -n 200`, or `cmd | head -c 12000`."#
+- Always set the `workdir` param when using the shell_command function. Do not use `cd` unless absolutely necessary."#
             .to_string()
     };
 
diff --git a/codex-rs/core/src/unified_exec/async_watcher.rs b/codex-rs/core/src/unified_exec/async_watcher.rs
index 38010754e9..8a018d86f3 100644
--- a/codex-rs/core/src/unified_exec/async_watcher.rs
+++ b/codex-rs/core/src/unified_exec/async_watcher.rs
@@ -8,7 +8,6 @@ use tokio::time::Sleep;
 
 use super::UnifiedExecContext;
 use super::process::UnifiedExecProcess;
-use crate::exec::MAX_EXEC_OUTPUT_DELTAS_PER_CALL;
 use crate::session::session::Session;
 use crate::session::turn_context::TurnContext;
 use crate::tools::events::ToolEmitter;
@@ -33,6 +32,35 @@ pub(crate) const TRAILING_OUTPUT_GRACE: Duration = Duration::from_millis(100);
 /// downstream event consumers (especially app-server JSON-RPC) don't have to
 /// process arbitrarily large delta payloads.
 const UNIFIED_EXEC_OUTPUT_DELTA_MAX_BYTES: usize = 8192;
+const UNIFIED_EXEC_LIVE_OUTPUT_MAX_BYTES: usize = 64 * 1024;
+const UNIFIED_EXEC_LIVE_OUTPUT_CAP_NOTICE: &[u8] =
+    b"\n[output capped in live view; command continues]\n";
+
+#[derive(Debug, Default)]
+struct LiveOutputLimiter {
+    emitted_bytes: usize,
+    notice_emitted: bool,
+}
+
+impl LiveOutputLimiter {
+    fn next_delta(&mut self, prefix: &[u8]) -> Option<Vec<u8>> {
+        if self.notice_emitted {
+            return None;
+        }
+
+        let remaining = UNIFIED_EXEC_LIVE_OUTPUT_MAX_BYTES.saturating_sub(self.emitted_bytes);
+        if prefix.len() < remaining {
+            self.emitted_bytes = self.emitted_bytes.saturating_add(prefix.len());
+            return Some(prefix.to_vec());
+        }
+
+        self.notice_emitted = true;
+        let mut capped = Vec::with_capacity(remaining + UNIFIED_EXEC_LIVE_OUTPUT_CAP_NOTICE.len());
+        capped.extend_from_slice(&prefix[..remaining]);
+        capped.extend_from_slice(UNIFIED_EXEC_LIVE_OUTPUT_CAP_NOTICE);
+        Some(capped)
+    }
+}
 
 /// Spawn a background task that continuously reads from the PTY, appends to the
 /// shared transcript, and emits ExecCommandOutputDelta events on UTF‑8
@@ -54,7 +82,7 @@ pub(crate) fn start_streaming_output(
         use tokio::sync::broadcast::error::RecvError;
 
         let mut pending = Vec::<u8>::new();
-        let mut emitted_deltas: usize = 0;
+        let mut live_output_limiter = LiveOutputLimiter::default();
 
         let mut grace_sleep: Option<Pin<Box<Sleep>>> = None;
 
@@ -92,7 +120,7 @@ pub(crate) fn start_streaming_output(
                         &call_id,
                         &session_ref,
                         &turn_ref,
-                        &mut emitted_deltas,
+                        &mut live_output_limiter,
                         chunk,
                     ).await;
                 }
@@ -162,7 +190,7 @@ async fn process_chunk(
     call_id: &str,
     session_ref: &Arc<Session>,
     turn_ref: &Arc<TurnContext>,
-    emitted_deltas: &mut usize,
+    live_output_limiter: &mut LiveOutputLimiter,
     chunk: Vec<u8>,
 ) {
     pending.extend_from_slice(&chunk);
@@ -172,19 +200,18 @@ async fn process_chunk(
             guard.push_chunk(prefix.to_vec());
         }
 
-        if *emitted_deltas >= MAX_EXEC_OUTPUT_DELTAS_PER_CALL {
+        let Some(delta) = live_output_limiter.next_delta(&prefix) else {
             continue;
-        }
+        };
 
         let event = ExecCommandOutputDeltaEvent {
             call_id: call_id.to_string(),
             stream: ExecOutputStream::Stdout,
-            chunk: prefix,
+            chunk: delta,
         };
         session_ref
             .send_event(turn_ref.as_ref(), EventMsg::ExecCommandOutputDelta(event))
             .await;
-        *emitted_deltas += 1;
     }
 }
 
diff --git a/codex-rs/core/src/unified_exec/async_watcher_tests.rs b/codex-rs/core/src/unified_exec/async_watcher_tests.rs
index 07d1bec60f..0b0407a175 100644
--- a/codex-rs/core/src/unified_exec/async_watcher_tests.rs
+++ b/codex-rs/core/src/unified_exec/async_watcher_tests.rs
@@ -1,3 +1,6 @@
+use super::LiveOutputLimiter;
+use super::UNIFIED_EXEC_LIVE_OUTPUT_CAP_NOTICE;
+use super::UNIFIED_EXEC_LIVE_OUTPUT_MAX_BYTES;
 use super::split_valid_utf8_prefix_with_max;
 
 use pretty_assertions::assert_eq;
@@ -37,3 +40,18 @@ fn split_valid_utf8_prefix_makes_progress_on_invalid_utf8() {
     assert_eq!(first, vec![0xff]);
     assert_eq!(buf, b"ab".to_vec());
 }
+
+#[test]
+fn live_output_limiter_emits_cap_notice_once() {
+    let mut limiter = LiveOutputLimiter::default();
+    let first = vec![b'a'; UNIFIED_EXEC_LIVE_OUTPUT_MAX_BYTES - 3];
+    assert_eq!(limiter.next_delta(&first), Some(first));
+
+    let capped = limiter
+        .next_delta(b"abcdef")
+        .expect("expected cap notice delta");
+    let mut expected = b"abc".to_vec();
+    expected.extend_from_slice(UNIFIED_EXEC_LIVE_OUTPUT_CAP_NOTICE);
+    assert_eq!(capped, expected);
+    assert_eq!(limiter.next_delta(b"later"), None);
+}
