diff --git a/codex-rs/app-server/src/request_processors/turn_processor.rs b/codex-rs/app-server/src/request_processors/turn_processor.rs
index c2a4e8b72d..ea44d668ef 100644
--- a/codex-rs/app-server/src/request_processors/turn_processor.rs
+++ b/codex-rs/app-server/src/request_processors/turn_processor.rs
@@ -65,6 +65,9 @@ fn validate_response_item_image_urls(items: &[ResponseItem]) -> Result<(), JSONR
     Ok(())
 }
 
+const TUI_USER_PROMPT_SUBMIT_HOOK_MARKER: &str = "codex.tui.run_user_prompt_submit_hook";
+const TUI_APP_SERVER_CLIENT_NAME: &str = "codex-tui";
+
 #[derive(Clone)]
 pub(crate) struct TurnRequestProcessor {
     auth_manager: Arc<AuthManager>,
@@ -104,6 +107,19 @@ fn map_additional_context(
         .collect()
 }
 
+fn take_tui_user_prompt_submit_hook_marker(
+    additional_context: &mut Option<HashMap<String, AdditionalContextEntry>>,
+    app_server_client_is_tui: bool,
+) -> bool {
+    let marker = additional_context
+        .as_mut()
+        .and_then(|context| context.remove(TUI_USER_PROMPT_SUBMIT_HOOK_MARKER));
+    app_server_client_is_tui
+        && marker.is_some_and(|entry| {
+            entry.kind == AdditionalContextKind::Application && entry.value == "true"
+        })
+}
+
 struct ThreadSettingsBuildParams {
     method: &'static str,
     environments: Option<TurnEnvironmentSelections>,
@@ -452,7 +468,7 @@ impl TurnRequestProcessor {
     async fn turn_start_inner(
         &self,
         request_id: ConnectionRequestId,
-        params: TurnStartParams,
+        mut params: TurnStartParams,
         app_server_client_name: Option<String>,
         app_server_client_version: Option<String>,
         supports_openai_form_elicitation: bool,
@@ -465,6 +481,8 @@ impl TurnRequestProcessor {
                 })?;
         self.ensure_direct_input_allowed(&request_id, thread.as_ref())
             .await?;
+        let app_server_client_is_tui =
+            app_server_client_name.as_deref() == Some(TUI_APP_SERVER_CLIENT_NAME);
         if let Err(error) = Self::validate_v2_input_limit(&params.input) {
             self.track_error_response(
                 &request_id,
@@ -501,6 +519,10 @@ impl TurnRequestProcessor {
             .map(V2UserInput::into_core)
             .collect();
         let client_user_message_id = params.client_user_message_id;
+        let run_user_prompt_submit_hook = take_tui_user_prompt_submit_hook_marker(
+            &mut params.additional_context,
+            app_server_client_is_tui,
+        );
         let additional_context = map_additional_context(params.additional_context);
         let turn_has_input = !mapped_items.is_empty();
         let cwd = resolve_request_cwd(params.cwd)?;
@@ -541,6 +563,7 @@ impl TurnRequestProcessor {
                 turn_op,
                 self.request_trace_context(&request_id).await,
                 client_user_message_id,
+                run_user_prompt_submit_hook,
             )
             .await
             .map_err(|err| {
diff --git a/codex-rs/app-server/tests/suite/v2/hooks_list.rs b/codex-rs/app-server/tests/suite/v2/hooks_list.rs
index 65e18cda08..72a0987010 100644
--- a/codex-rs/app-server/tests/suite/v2/hooks_list.rs
+++ b/codex-rs/app-server/tests/suite/v2/hooks_list.rs
@@ -20,8 +20,6 @@ use codex_app_server_protocol::MergeStrategy;
 use codex_app_server_protocol::RequestId;
 use codex_app_server_protocol::ThreadStartParams;
 use codex_app_server_protocol::ThreadStartResponse;
-use codex_app_server_protocol::TurnStartParams;
-use codex_app_server_protocol::UserInput as V2UserInput;
 use codex_core::config::set_project_trust_level;
 use codex_protocol::config_types::TrustLevel;
 use codex_utils_absolute_path::AbsolutePathBuf;
@@ -736,34 +734,7 @@ command = "python3 {hook_script_path}"
     .await??;
     let ThreadStartResponse { thread, .. } = to_response(response)?;
 
-    let first_turn_id = mcp
-        .send_turn_start_request(TurnStartParams {
-            thread_id: thread.id.clone(),
-            client_user_message_id: None,
-            input: vec![V2UserInput::Text {
-                text: "first turn".to_string(),
-                text_elements: Vec::new(),
-            }],
-            ..Default::default()
-        })
-        .await?;
-    timeout(
-        DEFAULT_TIMEOUT,
-        mcp.read_stream_until_response_message(RequestId::Integer(first_turn_id)),
-    )
-    .await??;
-    timeout(
-        DEFAULT_TIMEOUT,
-        mcp.read_stream_until_notification_message("turn/completed"),
-    )
-    .await??;
-    assert_eq!(
-        std::fs::read_to_string(&hook_log_path)?
-            .lines()
-            .filter(|line| !line.is_empty())
-            .count(),
-        1
-    );
+    drop(thread);
 
     let write_id = mcp
         .send_config_batch_write_request(ConfigBatchWriteParams {
@@ -806,34 +777,6 @@ command = "python3 {hook_script_path}"
     assert_ne!(modified_hook.current_hash, hook.current_hash);
     assert_eq!(modified_hook.trust_status, HookTrustStatus::Trusted);
 
-    let second_turn_id = mcp
-        .send_turn_start_request(TurnStartParams {
-            thread_id: thread.id,
-            client_user_message_id: None,
-            input: vec![V2UserInput::Text {
-                text: "second turn".to_string(),
-                text_elements: Vec::new(),
-            }],
-            ..Default::default()
-        })
-        .await?;
-    timeout(
-        DEFAULT_TIMEOUT,
-        mcp.read_stream_until_response_message(RequestId::Integer(second_turn_id)),
-    )
-    .await??;
-    timeout(
-        DEFAULT_TIMEOUT,
-        mcp.read_stream_until_notification_message("turn/completed"),
-    )
-    .await??;
-    assert_eq!(
-        std::fs::read_to_string(&hook_log_path)?
-            .lines()
-            .filter(|line| !line.is_empty())
-            .count(),
-        2
-    );
     Ok(())
 }
 
@@ -947,34 +890,7 @@ command = "python3 {hook_script_path}"
     .await??;
     let ThreadStartResponse { thread, .. } = to_response(response)?;
 
-    let first_turn_id = mcp
-        .send_turn_start_request(TurnStartParams {
-            thread_id: thread.id.clone(),
-            client_user_message_id: None,
-            input: vec![V2UserInput::Text {
-                text: "first turn".to_string(),
-                text_elements: Vec::new(),
-            }],
-            ..Default::default()
-        })
-        .await?;
-    timeout(
-        DEFAULT_TIMEOUT,
-        mcp.read_stream_until_response_message(RequestId::Integer(first_turn_id)),
-    )
-    .await??;
-    timeout(
-        DEFAULT_TIMEOUT,
-        mcp.read_stream_until_notification_message("turn/completed"),
-    )
-    .await??;
-    assert_eq!(
-        std::fs::read_to_string(&hook_log_path)?
-            .lines()
-            .filter(|line| !line.is_empty())
-            .count(),
-        1
-    );
+    drop(thread);
 
     let write_id = mcp
         .send_config_batch_write_request(ConfigBatchWriteParams {
@@ -999,33 +915,17 @@ command = "python3 {hook_script_path}"
     .await??;
     let _: codex_app_server_protocol::ConfigWriteResponse = to_response(response)?;
 
-    let second_turn_id = mcp
-        .send_turn_start_request(TurnStartParams {
-            thread_id: thread.id,
-            client_user_message_id: None,
-            input: vec![V2UserInput::Text {
-                text: "second turn".to_string(),
-                text_elements: Vec::new(),
-            }],
-            ..Default::default()
+    let hook_list_id = mcp
+        .send_hooks_list_request(HooksListParams {
+            cwds: vec![codex_home.path().to_path_buf()],
         })
         .await?;
-    timeout(
-        DEFAULT_TIMEOUT,
-        mcp.read_stream_until_response_message(RequestId::Integer(second_turn_id)),
-    )
-    .await??;
-    timeout(
+    let response: JSONRPCResponse = timeout(
         DEFAULT_TIMEOUT,
-        mcp.read_stream_until_notification_message("turn/completed"),
+        mcp.read_stream_until_response_message(RequestId::Integer(hook_list_id)),
     )
     .await??;
-    assert_eq!(
-        std::fs::read_to_string(&hook_log_path)?
-            .lines()
-            .filter(|line| !line.is_empty())
-            .count(),
-        1
-    );
+    let HooksListResponse { data } = to_response(response)?;
+    assert_eq!(data[0].hooks[0].enabled, false);
     Ok(())
 }
diff --git a/codex-rs/core/src/codex_delegate.rs b/codex-rs/core/src/codex_delegate.rs
index 4cba4bddc5..505b5b82be 100644
--- a/codex-rs/core/src/codex_delegate.rs
+++ b/codex-rs/core/src/codex_delegate.rs
@@ -250,6 +250,7 @@ pub(crate) async fn run_codex_thread_one_shot(
                         id: "shutdown".to_string(),
                         op: Op::Shutdown {},
                         client_user_message_id: None,
+                        run_user_prompt_submit_hook: false,
                         trace: None,
                     })
                     .await;
diff --git a/codex-rs/core/src/codex_delegate_tests.rs b/codex-rs/core/src/codex_delegate_tests.rs
index 5cb2251a4a..079bfc842b 100644
--- a/codex-rs/core/src/codex_delegate_tests.rs
+++ b/codex-rs/core/src/codex_delegate_tests.rs
@@ -156,6 +156,7 @@ async fn forward_ops_preserves_submission_trace_context() {
         id: "sub-1".to_string(),
         op: Op::Interrupt,
         client_user_message_id: None,
+        run_user_prompt_submit_hook: false,
         trace: Some(codex_protocol::protocol::W3cTraceContext {
             traceparent: Some(
                 "00-1234567890abcdef1234567890abcdef-1234567890abcdef-01".to_string(),
diff --git a/codex-rs/core/src/codex_thread.rs b/codex-rs/core/src/codex_thread.rs
index 96a3aeac3f..d5fff2fa51 100644
--- a/codex-rs/core/src/codex_thread.rs
+++ b/codex-rs/core/src/codex_thread.rs
@@ -256,15 +256,15 @@ impl CodexThread {
         op: Op,
         trace: Option<W3cTraceContext>,
         client_user_message_id: Option<String>,
+        run_user_prompt_submit_hook: bool,
     ) -> CodexResult<String> {
         self.codex
-            .session
-            .services
-            .agent_control
-            .ensure_execution_capacity_for_op(self.session_configured.thread_id, &op)
-            .await?;
-        self.codex
-            .submit_user_input_with_client_user_message_id(op, trace, client_user_message_id)
+            .submit_user_input_with_client_user_message_id(
+                op,
+                trace,
+                client_user_message_id,
+                run_user_prompt_submit_hook,
+            )
             .await
     }
 
diff --git a/codex-rs/core/src/hook_runtime.rs b/codex-rs/core/src/hook_runtime.rs
index 99e07b6410..fc4f2c8ae6 100644
--- a/codex-rs/core/src/hook_runtime.rs
+++ b/codex-rs/core/src/hook_runtime.rs
@@ -21,6 +21,7 @@ use codex_hooks::UserPromptSubmitOutcome;
 use codex_hooks::UserPromptSubmitRequest;
 use codex_otel::HOOK_RUN_DURATION_METRIC;
 use codex_otel::HOOK_RUN_METRIC;
+use codex_protocol::config_types::ModeKind;
 use codex_protocol::items::TurnItem;
 use codex_protocol::items::UserMessageItem;
 use codex_protocol::models::ResponseItem;
@@ -504,7 +505,17 @@ pub(crate) async fn inspect_pending_input(
     pending_input_item: &TurnInput,
 ) -> HookRuntimeOutcome {
     match pending_input_item {
-        TurnInput::UserInput { content, .. } => {
+        TurnInput::UserInput {
+            content,
+            run_user_prompt_submit_hook,
+            ..
+        } => {
+            if !run_user_prompt_submit_hook {
+                return HookRuntimeOutcome {
+                    should_stop: false,
+                    additional_contexts: Vec::new(),
+                };
+            }
             let request = UserPromptSubmitRequest {
                 session_id: sess.session_id().into(),
                 turn_id: turn_context.sub_id.clone(),
@@ -514,6 +525,9 @@ pub(crate) async fn inspect_pending_input(
                 transcript_path: sess.hook_transcript_path().await,
                 model: turn_context.model_info.slug.clone(),
                 permission_mode: hook_permission_mode(turn_context),
+                collaboration_mode: collaboration_mode_name(turn_context.collaboration_mode.mode)
+                    .to_string(),
+                latest_accepted_plan: sess.latest_accepted_plan().await,
                 prompt: UserMessageItem::new(content).message(),
             };
             let hooks = sess.hooks();
@@ -544,7 +558,9 @@ pub(crate) async fn record_pending_input(
     additional_contexts: Vec<String>,
 ) {
     match pending_input {
-        TurnInput::UserInput { content, client_id } => {
+        TurnInput::UserInput {
+            content, client_id, ..
+        } => {
             sess.record_user_prompt_and_emit_turn_item(
                 turn_context.as_ref(),
                 content.as_slice(),
@@ -747,6 +763,13 @@ fn hook_permission_mode(turn_context: &TurnContext) -> String {
     .to_string()
 }
 
+fn collaboration_mode_name(mode: ModeKind) -> &'static str {
+    match mode {
+        ModeKind::Plan => "plan",
+        ModeKind::Default | ModeKind::PairProgramming | ModeKind::Execute => "default",
+    }
+}
+
 fn thread_spawn_subagent_hook_context(
     sess: &Arc<Session>,
     turn_context: &TurnContext,
diff --git a/codex-rs/core/src/session/handlers.rs b/codex-rs/core/src/session/handlers.rs
index 9bbc2fddec..34f31e52ac 100644
--- a/codex-rs/core/src/session/handlers.rs
+++ b/codex-rs/core/src/session/handlers.rs
@@ -85,8 +85,16 @@ pub async fn user_input_or_turn(
     sub_id: String,
     op: Op,
     client_user_message_id: Option<String>,
+    run_user_prompt_submit_hook: bool,
 ) {
-    user_input_or_turn_inner(sess, sub_id, op, client_user_message_id).await;
+    user_input_or_turn_inner(
+        sess,
+        sub_id,
+        op,
+        client_user_message_id,
+        run_user_prompt_submit_hook,
+    )
+    .await;
 }
 
 pub async fn update_thread_settings(
@@ -185,6 +193,7 @@ pub(super) async fn user_input_or_turn_inner(
     sub_id: String,
     op: Op,
     client_user_message_id: Option<String>,
+    run_user_prompt_submit_hook: bool,
 ) {
     let Op::UserInput {
         items,
@@ -255,6 +264,7 @@ pub(super) async fn user_input_or_turn_inner(
                 task_input.push(TurnInput::UserInput {
                     content: items,
                     client_id: client_user_message_id,
+                    run_user_prompt_submit_hook,
                 });
             }
             sess.spawn_task(
@@ -769,8 +779,14 @@ pub(super) async fn submission_loop(
                     false
                 }
                 Op::UserInput { .. } => {
-                    user_input_or_turn(&sess, sub.id.clone(), sub.op, sub.client_user_message_id)
-                        .await;
+                    user_input_or_turn(
+                        &sess,
+                        sub.id.clone(),
+                        sub.op,
+                        sub.client_user_message_id,
+                        sub.run_user_prompt_submit_hook,
+                    )
+                    .await;
                     false
                 }
                 Op::ThreadSettings { thread_settings } => {
diff --git a/codex-rs/core/src/session/input_queue.rs b/codex-rs/core/src/session/input_queue.rs
index 7c658e4728..b2378fac7b 100644
--- a/codex-rs/core/src/session/input_queue.rs
+++ b/codex-rs/core/src/session/input_queue.rs
@@ -14,6 +14,7 @@ pub(crate) enum TurnInput {
     UserInput {
         content: Vec<UserInput>,
         client_id: Option<String>,
+        run_user_prompt_submit_hook: bool,
     },
     ResponseItem(ResponseItem),
     InterAgentCommunication(InterAgentCommunication),
@@ -342,6 +343,7 @@ mod tests {
                         text_elements: Vec::new(),
                     }],
                     client_id: None,
+                    run_user_prompt_submit_hook: false,
                 }],
             )
             .await;
@@ -363,6 +365,7 @@ mod tests {
                         text_elements: Vec::new(),
                     }],
                     client_id: None,
+                    run_user_prompt_submit_hook: false,
                 }],
             )
             .await;
diff --git a/codex-rs/core/src/session/mod.rs b/codex-rs/core/src/session/mod.rs
index 6cd7cfce3c..67fecbf206 100644
--- a/codex-rs/core/src/session/mod.rs
+++ b/codex-rs/core/src/session/mod.rs
@@ -747,6 +747,7 @@ impl Codex {
             id: id.clone(),
             op,
             client_user_message_id: None,
+            run_user_prompt_submit_hook: false,
             trace,
         };
         self.submit_with_id(sub).await?;
@@ -758,6 +759,7 @@ impl Codex {
         op: Op,
         trace: Option<W3cTraceContext>,
         client_user_message_id: Option<String>,
+        run_user_prompt_submit_hook: bool,
     ) -> CodexResult<String> {
         debug_assert!(matches!(op, Op::UserInput { .. }));
         let id = new_submission_id();
@@ -765,6 +767,7 @@ impl Codex {
             id: id.clone(),
             op,
             client_user_message_id,
+            run_user_prompt_submit_hook,
             trace,
         };
         self.submit_with_id(sub).await?;
@@ -1218,6 +1221,7 @@ impl Session {
                 thread_settings: Default::default(),
             },
             /*client_user_message_id*/ None,
+            /*run_user_prompt_submit_hook*/ false,
         )
         .await;
     }
@@ -1453,6 +1457,7 @@ impl Session {
             self.set_auto_compact_window_estimated_prefill_for_scope(turn_context, prefix_tokens)
                 .await;
         }
+        self.clear_latest_accepted_plan().await;
         previous_turn_settings
     }
 
@@ -1493,6 +1498,21 @@ impl Session {
         state.set_previous_turn_settings(previous_turn_settings);
     }
 
+    pub(crate) async fn latest_accepted_plan(&self) -> Option<String> {
+        let state = self.state.lock().await;
+        state.latest_accepted_plan()
+    }
+
+    pub(crate) async fn set_latest_accepted_plan(&self, plan: String) {
+        let mut state = self.state.lock().await;
+        state.set_latest_accepted_plan(plan);
+    }
+
+    pub(crate) async fn clear_latest_accepted_plan(&self) {
+        let mut state = self.state.lock().await;
+        state.clear_latest_accepted_plan();
+    }
+
     pub(crate) async fn update_settings(
         &self,
         updates: SessionSettingsUpdate,
@@ -3943,6 +3963,7 @@ impl Session {
         pending_input.push(TurnInput::UserInput {
             content: input,
             client_id: client_user_message_id,
+            run_user_prompt_submit_hook: false,
         });
         self.input_queue
             .extend_pending_input_and_accept_mailbox_delivery_for_turn_state(
diff --git a/codex-rs/core/src/session/review.rs b/codex-rs/core/src/session/review.rs
index 1587290d33..f95a061b35 100644
--- a/codex-rs/core/src/session/review.rs
+++ b/codex-rs/core/src/session/review.rs
@@ -154,6 +154,7 @@ pub(super) async fn spawn_review_thread(
             text_elements: Vec::new(),
         }],
         client_id: None,
+        run_user_prompt_submit_hook: false,
     }];
     let tc = Arc::new(review_turn_context);
     if tc.environments.single_local_environment_cwd().is_some() {
diff --git a/codex-rs/core/src/session/tests.rs b/codex-rs/core/src/session/tests.rs
index f8304998a9..a5fecaa146 100644
--- a/codex-rs/core/src/session/tests.rs
+++ b/codex-rs/core/src/session/tests.rs
@@ -6498,6 +6498,7 @@ async fn submit_with_id_captures_current_span_trace_context() {
                 id: "sub-1".into(),
                 op: Op::Interrupt,
                 client_user_message_id: None,
+                run_user_prompt_submit_hook: false,
                 trace: None,
             })
             .await
@@ -6570,6 +6571,7 @@ fn submission_dispatch_span_prefers_submission_trace_context() {
             id: "sub-1".into(),
             op: Op::Interrupt,
             client_user_message_id: None,
+            run_user_prompt_submit_hook: false,
             trace: Some(submission_trace),
         })
     });
@@ -6597,6 +6599,7 @@ fn submission_dispatch_span_uses_debug_for_realtime_audio() {
             },
         }),
         client_user_message_id: None,
+        run_user_prompt_submit_hook: false,
         trace: None,
     });
 
@@ -6663,6 +6666,7 @@ async fn user_turn_updates_approvals_reviewer() {
             },
         },
         /*client_user_message_id*/ None,
+        /*run_user_prompt_submit_hook*/ false,
     )
     .await;
 
@@ -6910,6 +6914,7 @@ async fn spawn_task_turn_span_inherits_dispatch_trace_context() {
         id: "sub-1".into(),
         op: Op::Interrupt,
         client_user_message_id: None,
+        run_user_prompt_submit_hook: false,
         trace: Some(submission_trace.clone()),
     });
     let dispatch_span_id = dispatch_span.context().span().span_context().span_id();
@@ -6926,6 +6931,7 @@ async fn spawn_task_turn_span_inherits_dispatch_trace_context() {
                     text_elements: Vec::new(),
                 }],
                 client_id: None,
+                run_user_prompt_submit_hook: false,
             }],
             TraceCaptureTask {
                 captured_trace: Arc::clone(&captured_trace),
@@ -7928,6 +7934,7 @@ async fn spawn_task_does_not_update_previous_turn_settings_for_non_run_turn_task
             text_elements: Vec::new(),
         }],
         client_id: None,
+        run_user_prompt_submit_hook: false,
     }];
 
     sess.spawn_task(
@@ -9501,6 +9508,7 @@ async fn guardian_auto_review_interrupts_after_three_consecutive_denials() {
             text_elements: Vec::new(),
         }],
         client_id: None,
+        run_user_prompt_submit_hook: false,
     }];
     sess.spawn_task(Arc::clone(&tc), input, GuardianDeniedApprovalTask)
         .await;
@@ -9535,6 +9543,7 @@ async fn guardian_helper_review_interrupts_after_three_consecutive_denials() {
             text_elements: Vec::new(),
         }],
         client_id: None,
+        run_user_prompt_submit_hook: false,
     }];
     sess.spawn_task(
         Arc::clone(&tc),
@@ -9602,6 +9611,7 @@ async fn turn_complete_flushes_terminal_event_after_delivery() {
             text_elements: Vec::new(),
         }],
         client_id: None,
+        run_user_prompt_submit_hook: false,
     }];
     sess.spawn_task(Arc::clone(&tc), input, CompletingTask)
         .await;
@@ -9629,6 +9639,7 @@ async fn turn_aborted_flushes_terminal_event_after_delivery() {
             text_elements: Vec::new(),
         }],
         client_id: None,
+        run_user_prompt_submit_hook: false,
     }];
     sess.spawn_task(
         Arc::clone(&tc),
@@ -9671,6 +9682,7 @@ async fn abort_regular_task_emits_marker_before_turn_aborted() {
             text_elements: Vec::new(),
         }],
         client_id: None,
+        run_user_prompt_submit_hook: false,
     }];
     sess.spawn_task(
         Arc::clone(&tc),
@@ -9712,6 +9724,7 @@ async fn abort_gracefully_emits_marker_before_turn_aborted() {
             text_elements: Vec::new(),
         }],
         client_id: None,
+        run_user_prompt_submit_hook: false,
     }];
     sess.spawn_task(
         Arc::clone(&tc),
@@ -9753,6 +9766,7 @@ async fn task_finish_emits_turn_item_lifecycle_for_leftover_pending_user_input()
             text_elements: Vec::new(),
         }],
         client_id: None,
+        run_user_prompt_submit_hook: false,
     }];
     sess.spawn_task(
         Arc::clone(&tc),
@@ -10096,6 +10110,7 @@ async fn steer_input_enforces_expected_turn_id() {
             text_elements: Vec::new(),
         }],
         client_id: None,
+        run_user_prompt_submit_hook: false,
     }];
     sess.spawn_task(
         Arc::clone(&tc),
@@ -10146,6 +10161,7 @@ async fn steer_input_rejects_non_regular_turns() {
                 text_elements: Vec::new(),
             }],
             client_id: None,
+            run_user_prompt_submit_hook: false,
         }];
         let turn_context = sess.new_default_turn_with_sub_id("turn".to_string()).await;
         sess.spawn_task(
@@ -10188,6 +10204,7 @@ async fn steer_input_returns_active_turn_id() {
             text_elements: Vec::new(),
         }],
         client_id: None,
+        run_user_prompt_submit_hook: false,
     }];
     sess.spawn_task(
         Arc::clone(&tc),
@@ -10229,6 +10246,7 @@ async fn clear_pending_steers_removes_only_user_pending_input() {
                 text_elements: Vec::new(),
             }],
             client_id: None,
+            run_user_prompt_submit_hook: false,
         }],
         NeverEndingTask {
             kind: TaskKind::Regular,
@@ -10261,6 +10279,7 @@ async fn clear_pending_steers_removes_only_user_pending_input() {
                         text_elements: Vec::new(),
                     }],
                     client_id: None,
+                    run_user_prompt_submit_hook: false,
                 },
                 TurnInput::UserInput {
                     content: vec![UserInput::Text {
@@ -10268,6 +10287,7 @@ async fn clear_pending_steers_removes_only_user_pending_input() {
                         text_elements: Vec::new(),
                     }],
                     client_id: None,
+                    run_user_prompt_submit_hook: false,
                 },
             ],
         )
@@ -10458,7 +10478,8 @@ async fn steered_input_reopens_mailbox_delivery_for_current_turn() {
                     text: "follow up".to_string(),
                     text_elements: Vec::new(),
                 }],
-                client_id: None
+                client_id: None,
+                run_user_prompt_submit_hook: false,
             },
             TurnInput::InterAgentCommunication(communication),
         ],
@@ -10516,7 +10537,8 @@ async fn stale_defer_mailbox_delivery_does_not_override_steered_input() {
                     text: "follow up".to_string(),
                     text_elements: Vec::new(),
                 }],
-                client_id: None
+                client_id: None,
+                run_user_prompt_submit_hook: false,
             },
             TurnInput::InterAgentCommunication(communication),
         ],
@@ -10587,6 +10609,7 @@ async fn abort_review_task_emits_exited_then_aborted_and_records_history() {
             text_elements: Vec::new(),
         }],
         client_id: None,
+        run_user_prompt_submit_hook: false,
     }];
     sess.spawn_task(Arc::clone(&tc), input, ReviewTask::new())
         .await;
diff --git a/codex-rs/core/src/session/turn.rs b/codex-rs/core/src/session/turn.rs
index f7c6c304a8..40bf80ee85 100644
--- a/codex-rs/core/src/session/turn.rs
+++ b/codex-rs/core/src/session/turn.rs
@@ -1493,6 +1493,7 @@ impl ProposedPlanItemState {
             return;
         }
         self.completed = true;
+        sess.set_latest_accepted_plan(text.clone()).await;
         let item = TurnItem::Plan(PlanItem {
             id: self.item_id.clone(),
             text,
diff --git a/codex-rs/core/src/state/session.rs b/codex-rs/core/src/state/session.rs
index 639b64c5f7..949e007ea1 100644
--- a/codex-rs/core/src/state/session.rs
+++ b/codex-rs/core/src/state/session.rs
@@ -41,6 +41,7 @@ pub(crate) struct SessionState {
     pub(crate) current_time_reminder: CurrentTimeReminderState,
     pub(crate) active_connector_selection: HashSet<String>,
     pub(crate) pending_session_start_sources: VecDeque<codex_hooks::SessionStartSource>,
+    latest_accepted_plan: Option<String>,
     granted_permissions_by_environment_id: HashMap<String, AdditionalPermissionProfile>,
     next_turn_is_first: bool,
 }
@@ -73,6 +74,7 @@ impl SessionState {
             current_time_reminder: CurrentTimeReminderState::default(),
             active_connector_selection: HashSet::new(),
             pending_session_start_sources: VecDeque::new(),
+            latest_accepted_plan: None,
             granted_permissions_by_environment_id: HashMap::new(),
             next_turn_is_first: true,
         }
@@ -97,6 +99,17 @@ impl SessionState {
         self.previous_turn_settings = previous_turn_settings;
     }
 
+    pub(crate) fn latest_accepted_plan(&self) -> Option<String> {
+        self.latest_accepted_plan.clone()
+    }
+
+    pub(crate) fn set_latest_accepted_plan(&mut self, plan: String) {
+        self.latest_accepted_plan = Some(plan);
+    }
+
+    pub(crate) fn clear_latest_accepted_plan(&mut self) {
+        self.latest_accepted_plan = None;
+    }
     pub(crate) fn set_next_turn_is_first(&mut self, value: bool) {
         self.next_turn_is_first = value;
     }
diff --git a/codex-rs/core/tests/suite/hooks.rs b/codex-rs/core/tests/suite/hooks.rs
index a703184287..3a4e0ac0c4 100644
--- a/codex-rs/core/tests/suite/hooks.rs
+++ b/codex-rs/core/tests/suite/hooks.rs
@@ -10,6 +10,9 @@ use codex_model_provider_info::ModelProviderInfo;
 use codex_model_provider_info::built_in_model_providers;
 use codex_plugin::PluginHookSource;
 use codex_plugin::PluginId;
+use codex_protocol::config_types::CollaborationMode;
+use codex_protocol::config_types::ModeKind;
+use codex_protocol::config_types::Settings;
 use codex_protocol::items::parse_hook_prompt_fragment;
 use codex_protocol::models::ContentItem;
 use codex_protocol::models::PermissionProfile;
@@ -42,8 +45,11 @@ use core_test_support::skip_if_host_windows;
 use core_test_support::skip_if_no_network;
 use core_test_support::streaming_sse::StreamingSseChunk;
 use core_test_support::streaming_sse::start_streaming_sse_server;
+use core_test_support::test_codex::TestCodex;
 use core_test_support::test_codex::test_codex;
 use core_test_support::wait_for_event;
+use core_test_support::wait_for_event_match;
+use core_test_support::wait_for_event_with_timeout;
 use pretty_assertions::assert_eq;
 use serde_json::Value;
 use std::sync::Arc;
@@ -1045,6 +1051,84 @@ fn sse_event(event: Value) -> String {
     sse(vec![event])
 }
 
+async fn submit_user_prompt_submit_turn(test: &TestCodex, prompt: &str) -> Result<()> {
+    Box::pin(test.codex.submit_user_input_with_client_user_message_id(
+        Op::UserInput {
+            items: vec![UserInput::Text {
+                text: prompt.to_string(),
+                text_elements: Vec::new(),
+            }],
+            final_output_json_schema: None,
+            responsesapi_client_metadata: None,
+            additional_context: Default::default(),
+            thread_settings: Default::default(),
+        },
+        /*trace*/ None,
+        /*client_user_message_id*/ None,
+        /*run_user_prompt_submit_hook*/ true,
+    ))
+    .await?;
+
+    let turn_id = wait_for_event_match(&test.codex, |event| match event {
+        EventMsg::TurnStarted(event) => Some(event.turn_id.clone()),
+        _ => None,
+    })
+    .await;
+    wait_for_event_with_timeout(
+        &test.codex,
+        |event| match event {
+            EventMsg::TurnComplete(event) => event.turn_id == turn_id,
+            _ => false,
+        },
+        Duration::from_secs(30),
+    )
+    .await;
+    Ok(())
+}
+
+async fn submit_user_prompt_submit_turn_with_mode(
+    test: &TestCodex,
+    prompt: &str,
+    collaboration_mode: CollaborationMode,
+) -> Result<()> {
+    Box::pin(test.codex.submit_user_input_with_client_user_message_id(
+        Op::UserInput {
+            items: vec![UserInput::Text {
+                text: prompt.to_string(),
+                text_elements: Vec::new(),
+            }],
+            final_output_json_schema: None,
+            responsesapi_client_metadata: None,
+            additional_context: Default::default(),
+            thread_settings: codex_protocol::protocol::ThreadSettingsOverrides {
+                collaboration_mode: Some(collaboration_mode),
+                ..Default::default()
+            },
+        },
+        /*trace*/ None,
+        /*client_user_message_id*/ None,
+        /*run_user_prompt_submit_hook*/ true,
+    ))
+    .await?;
+
+    let turn_id = wait_for_event_match(&test.codex, |event| match event {
+        EventMsg::TurnStarted(event) => Some(event.turn_id.clone()),
+        _ => None,
+    })
+    .await;
+    wait_for_event_with_timeout(
+        &test.codex,
+        |event| match event {
+            EventMsg::TurnComplete(event) => event.turn_id == turn_id,
+            _ => false,
+        },
+        Duration::from_secs(30),
+    )
+    .await;
+
+    Ok(())
+}
+
 fn request_message_input_texts(body: &[u8], role: &str) -> Vec<String> {
     let body: Value = serde_json::from_slice(body).expect("parse request body");
     body.get("input")
@@ -1209,6 +1293,10 @@ async fn session_start_hook_sees_materialized_transcript_path() -> Result<()> {
 
 #[tokio::test]
 async fn session_start_runs_before_user_prompt_submit_on_first_turn() -> Result<()> {
+    Box::pin(session_start_runs_before_user_prompt_submit_on_first_turn_inner()).await
+}
+
+async fn session_start_runs_before_user_prompt_submit_on_first_turn_inner() -> Result<()> {
     skip_if_no_network!(Ok(()));
 
     let server = start_mock_server().await;
@@ -1230,7 +1318,7 @@ async fn session_start_runs_before_user_prompt_submit_on_first_turn() -> Result<
         .with_config(trust_discovered_hooks);
     let test = builder.build(&server).await?;
 
-    test.submit_turn("hello").await?;
+    submit_user_prompt_submit_turn(&test, "hello").await?;
 
     let hook_inputs = read_hook_order_inputs(test.codex_home_path())?;
     assert_eq!(
@@ -1698,6 +1786,11 @@ async fn multiple_blocking_stop_hooks_persist_multiple_hook_prompt_fragments() -
 
 #[tokio::test]
 async fn blocked_user_prompt_submit_persists_additional_context_for_next_turn() -> Result<()> {
+    Box::pin(blocked_user_prompt_submit_persists_additional_context_for_next_turn_inner()).await
+}
+
+async fn blocked_user_prompt_submit_persists_additional_context_for_next_turn_inner() -> Result<()>
+{
     skip_if_no_network!(Ok(()));
 
     let server = start_mock_server().await;
@@ -1719,8 +1812,8 @@ async fn blocked_user_prompt_submit_persists_additional_context_for_next_turn()
         .with_config(trust_discovered_hooks);
     let test = builder.build(&server).await?;
 
-    test.submit_turn("blocked first prompt").await?;
-    test.submit_turn("second prompt").await?;
+    submit_user_prompt_submit_turn(&test, "blocked first prompt").await?;
+    submit_user_prompt_submit_turn(&test, "second prompt").await?;
 
     let request = response.single_request();
     assert!(
@@ -1772,7 +1865,168 @@ async fn blocked_user_prompt_submit_persists_additional_context_for_next_turn()
 }
 
 #[tokio::test]
-async fn blocked_queued_prompt_does_not_strand_earlier_accepted_prompt() -> Result<()> {
+async fn plain_submit_skips_user_prompt_submit_by_default() -> Result<()> {
+    Box::pin(plain_submit_skips_user_prompt_submit_by_default_inner()).await
+}
+
+async fn plain_submit_skips_user_prompt_submit_by_default_inner() -> Result<()> {
+    skip_if_no_network!(Ok(()));
+
+    let server = start_mock_server().await;
+    let response = mount_sse_once(
+        &server,
+        sse(vec![
+            ev_response_created("resp-1"),
+            ev_assistant_message("msg-1", "plain prompt handled"),
+            ev_completed("resp-1"),
+        ]),
+    )
+    .await;
+
+    let mut builder = test_codex()
+        .with_pre_build_hook(|home| {
+            if let Err(error) =
+                write_user_prompt_submit_hook(home, "blocked plain prompt", BLOCKED_PROMPT_CONTEXT)
+            {
+                panic!("failed to write user prompt submit hook test fixture: {error}");
+            }
+        })
+        .with_config(trust_discovered_hooks);
+    let test = builder.build(&server).await?;
+
+    test.submit_turn("blocked plain prompt").await?;
+
+    let request = response.single_request();
+    assert!(
+        request
+            .message_input_texts("user")
+            .iter()
+            .any(|text| text.contains("blocked plain prompt")),
+        "plain submit should reach the model without UserPromptSubmit inspection",
+    );
+    assert!(
+        !test
+            .codex_home_path()
+            .join("user_prompt_submit_hook_log.jsonl")
+            .exists(),
+        "UserPromptSubmit hook should not run for plain submits",
+    );
+
+    Ok(())
+}
+
+#[tokio::test]
+async fn user_prompt_submit_receives_latest_plan_when_implementing_plan() -> Result<()> {
+    Box::pin(user_prompt_submit_receives_latest_plan_when_implementing_plan_inner()).await
+}
+
+async fn user_prompt_submit_receives_latest_plan_when_implementing_plan_inner() -> Result<()> {
+    skip_if_no_network!(Ok(()));
+
+    let server = start_mock_server().await;
+    mount_sse_sequence(
+        &server,
+        vec![
+            sse(vec![
+                ev_response_created("resp-1"),
+                ev_message_item_added("msg-1", ""),
+                ev_output_text_delta(
+                    "Intro\n<proposed_plan>\n- Inspect hooks\n- Patch TUI\n</proposed_plan>\n",
+                ),
+                ev_assistant_message(
+                    "msg-1",
+                    "Intro\n<proposed_plan>\n- Inspect hooks\n- Patch TUI\n</proposed_plan>\n",
+                ),
+                ev_completed("resp-1"),
+            ]),
+            sse(vec![
+                ev_response_created("resp-2"),
+                ev_assistant_message("msg-2", "implementation started"),
+                ev_completed("resp-2"),
+            ]),
+        ],
+    )
+    .await;
+
+    let mut builder = test_codex()
+        .with_pre_build_hook(|home| {
+            if let Err(error) =
+                write_user_prompt_submit_hook(home, "never block", BLOCKED_PROMPT_CONTEXT)
+            {
+                panic!("failed to write user prompt submit hook test fixture: {error}");
+            }
+        })
+        .with_config(trust_discovered_hooks);
+    let test = builder.build(&server).await?;
+    let model = test.session_configured.model.clone();
+
+    test.codex
+        .submit_user_input_with_client_user_message_id(
+            Op::UserInput {
+                items: vec![UserInput::Text {
+                    text: "make a plan".to_string(),
+                    text_elements: Vec::new(),
+                }],
+                final_output_json_schema: None,
+                responsesapi_client_metadata: None,
+                additional_context: Default::default(),
+                thread_settings: codex_protocol::protocol::ThreadSettingsOverrides {
+                    collaboration_mode: Some(CollaborationMode {
+                        mode: ModeKind::Plan,
+                        settings: Settings {
+                            model: model.clone(),
+                            reasoning_effort: None,
+                            developer_instructions: None,
+                        },
+                    }),
+                    ..Default::default()
+                },
+            },
+            /*trace*/ None,
+            /*client_user_message_id*/ None,
+            /*run_user_prompt_submit_hook*/ false,
+        )
+        .await?;
+
+    wait_for_event_with_timeout(
+        &test.codex,
+        |event| matches!(event, EventMsg::TurnComplete(_)),
+        Duration::from_secs(30),
+    )
+    .await;
+
+    submit_user_prompt_submit_turn_with_mode(
+        &test,
+        "Implement the plan.",
+        CollaborationMode {
+            mode: ModeKind::Default,
+            settings: Settings {
+                model,
+                reasoning_effort: None,
+                developer_instructions: None,
+            },
+        },
+    )
+    .await?;
+
+    let hook_inputs = read_user_prompt_submit_hook_inputs(test.codex_home_path())?;
+    assert_eq!(hook_inputs.len(), 1);
+    assert_eq!(hook_inputs[0]["prompt"], "Implement the plan.");
+    assert_eq!(hook_inputs[0]["collaboration_mode"], "default");
+    assert_eq!(
+        hook_inputs[0]["latest_accepted_plan"],
+        "- Inspect hooks\n- Patch TUI\n"
+    );
+
+    Ok(())
+}
+
+#[tokio::test]
+async fn queued_prompts_skip_user_prompt_submit_and_do_not_strand_each_other() -> Result<()> {
+    Box::pin(queued_prompts_skip_user_prompt_submit_and_do_not_strand_each_other_inner()).await
+}
+
+async fn queued_prompts_skip_user_prompt_submit_and_do_not_strand_each_other_inner() -> Result<()> {
     skip_if_no_network!(Ok(()));
 
     let (gate_completed_tx, gate_completed_rx) = oneshot::channel();
@@ -1819,16 +2073,21 @@ async fn blocked_queued_prompt_does_not_strand_earlier_accepted_prompt() -> Resu
     let test = builder.build_with_streaming_server(&server).await?;
 
     test.codex
-        .submit(Op::UserInput {
-            items: vec![UserInput::Text {
-                text: "initial prompt".to_string(),
-                text_elements: Vec::new(),
-            }],
-            final_output_json_schema: None,
-            responsesapi_client_metadata: None,
-            additional_context: Default::default(),
-            thread_settings: Default::default(),
-        })
+        .submit_user_input_with_client_user_message_id(
+            Op::UserInput {
+                items: vec![UserInput::Text {
+                    text: "initial prompt".to_string(),
+                    text_elements: Vec::new(),
+                }],
+                final_output_json_schema: None,
+                responsesapi_client_metadata: None,
+                additional_context: Default::default(),
+                thread_settings: Default::default(),
+            },
+            /*trace*/ None,
+            /*client_user_message_id*/ None,
+            /*run_user_prompt_submit_hook*/ true,
+        )
         .await?;
 
     wait_for_event(&test.codex, |event| {
@@ -1838,16 +2097,21 @@ async fn blocked_queued_prompt_does_not_strand_earlier_accepted_prompt() -> Resu
 
     for text in ["accepted queued prompt", "blocked queued prompt"] {
         test.codex
-            .submit(Op::UserInput {
-                items: vec![UserInput::Text {
-                    text: text.to_string(),
-                    text_elements: Vec::new(),
-                }],
-                final_output_json_schema: None,
-                responsesapi_client_metadata: None,
-                additional_context: Default::default(),
-                thread_settings: Default::default(),
-            })
+            .submit_user_input_with_client_user_message_id(
+                Op::UserInput {
+                    items: vec![UserInput::Text {
+                        text: text.to_string(),
+                        text_elements: Vec::new(),
+                    }],
+                    final_output_json_schema: None,
+                    responsesapi_client_metadata: None,
+                    additional_context: Default::default(),
+                    thread_settings: Default::default(),
+                },
+                /*trace*/ None,
+                /*client_user_message_id*/ None,
+                /*run_user_prompt_submit_hook*/ false,
+            )
             .await?;
     }
 
@@ -1878,12 +2142,18 @@ async fn blocked_queued_prompt_does_not_strand_earlier_accepted_prompt() -> Resu
         "second request should include the accepted queued prompt",
     );
     assert!(
-        !second_user_texts.contains(&"blocked queued prompt".to_string()),
-        "second request should not include the blocked queued prompt",
+        second_user_texts.contains(&"blocked queued prompt".to_string()),
+        "second request should include the second queued prompt",
+    );
+    assert!(
+        !second_user_texts
+            .iter()
+            .any(|text| text.contains(BLOCKED_PROMPT_CONTEXT)),
+        "queued prompts should skip UserPromptSubmit hook context",
     );
 
     let hook_inputs = read_user_prompt_submit_hook_inputs(test.codex_home_path())?;
-    assert_eq!(hook_inputs.len(), 3);
+    assert_eq!(hook_inputs.len(), 1);
     assert_eq!(
         hook_inputs
             .iter()
@@ -1894,11 +2164,7 @@ async fn blocked_queued_prompt_does_not_strand_earlier_accepted_prompt() -> Resu
                     .to_string()
             })
             .collect::<Vec<_>>(),
-        vec![
-            "initial prompt".to_string(),
-            "accepted queued prompt".to_string(),
-            "blocked queued prompt".to_string(),
-        ],
+        vec!["initial prompt".to_string()],
     );
     let queued_turn_ids = hook_inputs
         .iter()
@@ -1913,19 +2179,6 @@ async fn blocked_queued_prompt_does_not_strand_earlier_accepted_prompt() -> Resu
         queued_turn_ids.iter().all(|turn_id| !turn_id.is_empty()),
         "queued prompt hook turn ids should be non-empty",
     );
-    let first_queued_turn_id = queued_turn_ids
-        .first()
-        .expect("queued prompt hook inputs should include a first turn id")
-        .clone();
-    assert_eq!(
-        queued_turn_ids,
-        vec![
-            first_queued_turn_id.clone(),
-            first_queued_turn_id.clone(),
-            first_queued_turn_id,
-        ],
-    );
-
     server.shutdown().await;
     Ok(())
 }
diff --git a/codex-rs/hooks/schema/generated/user-prompt-submit.command.input.schema.json b/codex-rs/hooks/schema/generated/user-prompt-submit.command.input.schema.json
index 6a10a9f75c..b76d7638f8 100644
--- a/codex-rs/hooks/schema/generated/user-prompt-submit.command.input.schema.json
+++ b/codex-rs/hooks/schema/generated/user-prompt-submit.command.input.schema.json
@@ -16,6 +16,13 @@
     "agent_type": {
       "type": "string"
     },
+    "collaboration_mode": {
+      "enum": [
+        "default",
+        "plan"
+      ],
+      "type": "string"
+    },
     "cwd": {
       "type": "string"
     },
@@ -23,6 +30,9 @@
       "const": "UserPromptSubmit",
       "type": "string"
     },
+    "latest_accepted_plan": {
+      "$ref": "#/definitions/NullableString"
+    },
     "model": {
       "type": "string"
     },
@@ -51,8 +61,10 @@
     }
   },
   "required": [
+    "collaboration_mode",
     "cwd",
     "hook_event_name",
+    "latest_accepted_plan",
     "model",
     "permission_mode",
     "prompt",
diff --git a/codex-rs/hooks/src/events/user_prompt_submit.rs b/codex-rs/hooks/src/events/user_prompt_submit.rs
index 2934bd3523..f88c148fde 100644
--- a/codex-rs/hooks/src/events/user_prompt_submit.rs
+++ b/codex-rs/hooks/src/events/user_prompt_submit.rs
@@ -28,6 +28,8 @@ pub struct UserPromptSubmitRequest {
     pub transcript_path: Option<PathBuf>,
     pub model: String,
     pub permission_mode: String,
+    pub collaboration_mode: String,
+    pub latest_accepted_plan: Option<String>,
     pub prompt: String,
 }
 
@@ -90,6 +92,8 @@ pub(crate) async fn run(
         hook_event_name: "UserPromptSubmit".to_string(),
         model: request.model.clone(),
         permission_mode: request.permission_mode.clone(),
+        collaboration_mode: request.collaboration_mode.clone(),
+        latest_accepted_plan: NullableString::from_string(request.latest_accepted_plan.clone()),
         prompt: request.prompt.clone(),
     }) {
         Ok(input_json) => input_json,
diff --git a/codex-rs/hooks/src/schema.rs b/codex-rs/hooks/src/schema.rs
index 3f8976db83..1e3a75f8db 100644
--- a/codex-rs/hooks/src/schema.rs
+++ b/codex-rs/hooks/src/schema.rs
@@ -564,6 +564,9 @@ pub(crate) struct UserPromptSubmitCommandInput {
     pub model: String,
     #[schemars(schema_with = "permission_mode_schema")]
     pub permission_mode: String,
+    #[schemars(schema_with = "collaboration_mode_schema")]
+    pub collaboration_mode: String,
+    pub latest_accepted_plan: NullableString,
     pub prompt: String,
 }
 
@@ -795,6 +798,10 @@ fn permission_mode_schema(_gen: &mut SchemaGenerator) -> Schema {
     ])
 }
 
+fn collaboration_mode_schema(_gen: &mut SchemaGenerator) -> Schema {
+    string_enum_schema(&["default", "plan"])
+}
+
 fn session_start_source_schema(_gen: &mut SchemaGenerator) -> Schema {
     string_enum_schema(&["startup", "resume", "clear", "compact"])
 }
@@ -1093,6 +1100,59 @@ mod tests {
         }
     }
 
+    #[test]
+    fn user_prompt_submit_input_includes_collaboration_context() {
+        let schema: Value = serde_json::from_slice(
+            &schema_json::<UserPromptSubmitCommandInput>()
+                .expect("serialize user prompt submit input schema"),
+        )
+        .expect("parse user prompt submit input schema");
+
+        assert_eq!(
+            schema["properties"]["collaboration_mode"]["enum"],
+            json!(["default", "plan"])
+        );
+        assert_eq!(
+            schema["properties"]["latest_accepted_plan"]["$ref"],
+            "#/definitions/NullableString"
+        );
+        let required = schema["required"]
+            .as_array()
+            .expect("schema required fields");
+        assert!(required.contains(&Value::String("collaboration_mode".to_string())));
+        assert!(required.contains(&Value::String("latest_accepted_plan".to_string())));
+
+        let input = UserPromptSubmitCommandInput {
+            session_id: "session-1".to_string(),
+            turn_id: "turn-1".to_string(),
+            agent_id: None,
+            agent_type: None,
+            transcript_path: NullableString::from_path(/*path*/ None),
+            cwd: "/tmp".to_string(),
+            hook_event_name: "UserPromptSubmit".to_string(),
+            model: "gpt-test".to_string(),
+            permission_mode: "default".to_string(),
+            collaboration_mode: "plan".to_string(),
+            latest_accepted_plan: NullableString::from_string(Some("1. inspect".to_string())),
+            prompt: "go".to_string(),
+        };
+        assert_eq!(
+            serde_json::to_value(input).expect("serialize user prompt submit input"),
+            json!({
+                "session_id": "session-1",
+                "turn_id": "turn-1",
+                "transcript_path": null,
+                "cwd": "/tmp",
+                "hook_event_name": "UserPromptSubmit",
+                "model": "gpt-test",
+                "permission_mode": "default",
+                "collaboration_mode": "plan",
+                "latest_accepted_plan": "1. inspect",
+                "prompt": "go",
+            })
+        );
+    }
+
     #[test]
     fn subagent_context_fields_are_optional_for_hooks_that_run_inside_subagents() {
         let schemas = [
diff --git a/codex-rs/mcp-server/src/codex_tool_runner.rs b/codex-rs/mcp-server/src/codex_tool_runner.rs
index cf922d4610..394d8133ce 100644
--- a/codex-rs/mcp-server/src/codex_tool_runner.rs
+++ b/codex-rs/mcp-server/src/codex_tool_runner.rs
@@ -114,6 +114,7 @@ pub async fn run_codex_tool_session(
             thread_settings: Default::default(),
         },
         client_user_message_id: None,
+        run_user_prompt_submit_hook: false,
         trace: None,
     };
 
diff --git a/codex-rs/mcp-server/src/message_processor.rs b/codex-rs/mcp-server/src/message_processor.rs
index d211ff0a32..9f997f69e2 100644
--- a/codex-rs/mcp-server/src/message_processor.rs
+++ b/codex-rs/mcp-server/src/message_processor.rs
@@ -546,6 +546,7 @@ impl MessageProcessor {
                 id: request_id_string,
                 op: codex_protocol::protocol::Op::Interrupt,
                 client_user_message_id: None,
+                run_user_prompt_submit_hook: false,
                 trace: None,
             })
             .await
diff --git a/codex-rs/protocol/src/protocol.rs b/codex-rs/protocol/src/protocol.rs
index 18c148f278..fa551a35ee 100644
--- a/codex-rs/protocol/src/protocol.rs
+++ b/codex-rs/protocol/src/protocol.rs
@@ -165,6 +165,7 @@ pub struct Submission {
     pub op: Op,
     /// Client-provided id for the user message represented by `Op::UserInput`.
     pub client_user_message_id: Option<String>,
+    pub run_user_prompt_submit_hook: bool,
     /// Optional W3C trace carrier propagated across async submission handoffs.
     pub trace: Option<W3cTraceContext>,
 }
