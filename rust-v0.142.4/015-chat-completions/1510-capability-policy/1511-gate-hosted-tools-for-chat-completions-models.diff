diff --git a/codex-rs/core/src/tools/spec_plan.rs b/codex-rs/core/src/tools/spec_plan.rs
index 51cd436503..23b192a00f 100644
--- a/codex-rs/core/src/tools/spec_plan.rs
+++ b/codex-rs/core/src/tools/spec_plan.rs
@@ -304,6 +304,7 @@ fn hosted_model_tool_specs(context: &CoreToolPlanContext<'_>) -> Vec<ToolSpec> {
     // `Some(Cached/Live/Disabled)` are the options for mode when standalone search is unavailable
     // and the provider supports hosted search. `None` prevents emitting a hosted search tool.
     let web_search_mode = (!standalone_web_search_available
+        && search_tool_enabled(turn_context)
         && turn_context.provider.capabilities().web_search)
         .then_some(turn_context.config.web_search_mode.value());
     let web_search_config = web_search_mode
@@ -616,7 +617,8 @@ fn add_tool_sources(context: &CoreToolPlanContext<'_>, planned_tools: &mut Plann
 }
 
 fn standalone_web_search_enabled(turn_context: &TurnContext) -> bool {
-    namespace_tools_enabled(turn_context)
+    search_tool_enabled(turn_context)
+        && namespace_tools_enabled(turn_context)
         && (turn_context.model_info.use_responses_lite
             || turn_context
                 .config
diff --git a/codex-rs/core/src/tools/spec_plan_tests.rs b/codex-rs/core/src/tools/spec_plan_tests.rs
index 5d70fb8f17..02e3a5b7ab 100644
--- a/codex-rs/core/src/tools/spec_plan_tests.rs
+++ b/codex-rs/core/src/tools/spec_plan_tests.rs
@@ -1363,6 +1363,7 @@ async fn code_mode_only_can_expose_namespaced_multi_agent_v2_as_normal_tools() {
                 Feature::MultiAgentV2,
             ],
         );
+        turn.model_info.supports_search_tool = true;
         update_config(turn, |config| {
             config.multi_agent_v2.non_code_mode_only = true;
             config.multi_agent_v2.tool_namespace = Some("agents".to_string());
@@ -1433,6 +1434,7 @@ async fn hosted_tools_follow_provider_auth_model_and_config_gates() {
     extension_flag_without_imagegen_tool.assert_visible_lacks(&["image_gen"]);
 
     let live_web_search = probe(|turn| {
+        turn.model_info.supports_search_tool = true;
         set_web_search_mode(turn, WebSearchMode::Live);
         turn.model_info.web_search_tool_type = WebSearchToolType::TextAndImage;
     })
@@ -1451,6 +1453,7 @@ async fn hosted_tools_follow_provider_auth_model_and_config_gates() {
 
     let code_mode_only = probe(|turn| {
         use_chatgpt_auth(turn);
+        turn.model_info.supports_search_tool = true;
         set_features(turn, &[Feature::CodeModeOnly, Feature::MultiAgentV2]);
         set_web_search_mode(turn, WebSearchMode::Live);
         turn.model_info.input_modalities = vec![InputModality::Image];
@@ -1477,6 +1480,7 @@ async fn hosted_tools_follow_provider_auth_model_and_config_gates() {
     );
 
     let standalone_web_search_without_web_run = probe(|turn| {
+        turn.model_info.supports_search_tool = true;
         set_feature(turn, Feature::StandaloneWebSearch, /*enabled*/ true);
         set_web_search_mode(turn, WebSearchMode::Live);
     })
@@ -1485,6 +1489,7 @@ async fn hosted_tools_follow_provider_auth_model_and_config_gates() {
 
     let standalone_web_search = probe_with(
         |turn| {
+            turn.model_info.supports_search_tool = true;
             set_feature(turn, Feature::StandaloneWebSearch, /*enabled*/ true);
             set_web_search_mode(turn, WebSearchMode::Live);
         },
@@ -1496,7 +1501,22 @@ async fn hosted_tools_follow_provider_auth_model_and_config_gates() {
     .await;
     standalone_web_search.assert_visible_lacks(&["web_search"]);
 
+    let search_unsupported_model = probe_with(
+        |turn| {
+            turn.model_info.supports_search_tool = false;
+            set_feature(turn, Feature::StandaloneWebSearch, /*enabled*/ true);
+            set_web_search_mode(turn, WebSearchMode::Live);
+        },
+        ToolPlanInputs {
+            extension_tool_executors: vec![Arc::new(WebRunExtensionTool)],
+            ..Default::default()
+        },
+    )
+    .await;
+    search_unsupported_model.assert_visible_lacks(&["web_search", "web"]);
+
     let unsupported_provider = probe(|turn| {
+        turn.model_info.supports_search_tool = true;
         set_web_search_mode(turn, WebSearchMode::Live);
         use_bedrock_provider(turn);
     })
diff --git a/codex-rs/core/tests/suite/model_runtime_selectors.rs b/codex-rs/core/tests/suite/model_runtime_selectors.rs
index d893e389d3..fdf0321759 100644
--- a/codex-rs/core/tests/suite/model_runtime_selectors.rs
+++ b/codex-rs/core/tests/suite/model_runtime_selectors.rs
@@ -189,6 +189,7 @@ async fn remote_tool_mode_selector_overrides_feature_flags() -> Result<()> {
     let mut code_mode_only_model = remote_model("test-tool-mode-code-mode-only");
     code_mode_only_model.tool_mode = Some(ToolMode::CodeModeOnly);
     code_mode_only_model.input_modalities = vec![InputModality::Text, InputModality::Image];
+    code_mode_only_model.supports_search_tool = true;
     let code_mode_only_body = response_body_for_remote_model(code_mode_only_model, |_| {}).await?;
     assert_eq!(
         tool_names(&code_mode_only_body),
