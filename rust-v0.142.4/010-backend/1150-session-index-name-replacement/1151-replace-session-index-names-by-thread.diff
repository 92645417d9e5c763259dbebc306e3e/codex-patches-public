diff --git a/codex-rs/rollout/src/metadata_tests.rs b/codex-rs/rollout/src/metadata_tests.rs
index 2068e78797..f3b161c0f7 100644
--- a/codex-rs/rollout/src/metadata_tests.rs
+++ b/codex-rs/rollout/src/metadata_tests.rs
@@ -259,6 +259,7 @@ async fn backfill_sessions_uses_last_rollout_timestamp_without_changing_mtime()
     let rollout_path =
         sessions_dir.join(format!("rollout-2026-01-27T12-34-56-{thread_uuid}.jsonl"));
     let session_meta = SessionMeta {
+        session_id: thread_id.into(),
         id: thread_id,
         forked_from_id: None,
         parent_thread_id: None,
@@ -290,6 +291,9 @@ async fn backfill_sessions_uses_last_rollout_timestamp_without_changing_mtime()
             item: RolloutItem::Compacted(CompactedItem {
                 message: "compacted".to_string(),
                 replacement_history: None,
+                window_number: None,
+                first_window_id: None,
+                previous_window_id: None,
                 window_id: None,
             }),
         },
diff --git a/codex-rs/rollout/src/session_index.rs b/codex-rs/rollout/src/session_index.rs
index 96ff9d825e..1d5b323781 100644
--- a/codex-rs/rollout/src/session_index.rs
+++ b/codex-rs/rollout/src/session_index.rs
@@ -5,7 +5,6 @@ use std::io::ErrorKind;
 use std::io::Read;
 use std::io::Seek;
 use std::io::SeekFrom;
-use std::io::Write;
 use std::path::Path;
 use std::path::PathBuf;
 use std::sync::LazyLock;
@@ -28,8 +27,7 @@ pub struct SessionIndexEntry {
     pub updated_at: String,
 }
 
-/// Append a thread name update to the session index.
-/// Name updates are append-only; the most recent entry wins when resolving names or ids.
+/// Replace a thread name in the session index.
 pub async fn append_thread_name(
     codex_home: &Path,
     thread_id: ThreadId,
@@ -46,28 +44,34 @@ pub async fn append_thread_name(
         thread_name: name.to_string(),
         updated_at,
     };
-    append_session_index_entry(codex_home, &entry).await
+    replace_thread_name_entry(codex_home, &entry)
 }
 
-/// Append a raw session index entry to `session_index.jsonl`.
-/// Consumers scan from the end to find the newest match.
-pub async fn append_session_index_entry(
-    codex_home: &Path,
-    entry: &SessionIndexEntry,
-) -> std::io::Result<()> {
+fn replace_thread_name_entry(codex_home: &Path, entry: &SessionIndexEntry) -> std::io::Result<()> {
     let _guard = SESSION_INDEX_LOCK
         .lock()
         .map_err(|err| std::io::Error::other(err.to_string()))?;
     let path = session_index_path(codex_home);
-    let mut file = std::fs::OpenOptions::new()
-        .create(true)
-        .append(true)
-        .open(&path)?;
-    let mut line = serde_json::to_string(entry).map_err(std::io::Error::other)?;
-    line.push('\n');
-    file.write_all(line.as_bytes())?;
-    file.flush()?;
-    Ok(())
+    let contents = match std::fs::read_to_string(&path) {
+        Ok(contents) => contents,
+        Err(err) if err.kind() == ErrorKind::NotFound => String::new(),
+        Err(err) => return Err(err),
+    };
+    let mut rewritten = String::with_capacity(contents.len());
+    for line in contents.lines() {
+        let should_remove = serde_json::from_str::<SessionIndexEntry>(line.trim())
+            .is_ok_and(|existing| existing.id == entry.id);
+        if !should_remove {
+            rewritten.push_str(line);
+            rewritten.push('\n');
+        }
+    }
+    rewritten.push_str(&serde_json::to_string(entry).map_err(std::io::Error::other)?);
+    rewritten.push('\n');
+
+    let temp_path = path.with_extension("jsonl.tmp");
+    std::fs::write(&temp_path, rewritten)?;
+    std::fs::rename(temp_path, path)
 }
 
 /// Remove all recorded names for a thread from the session index.
diff --git a/codex-rs/rollout/src/session_index_tests.rs b/codex-rs/rollout/src/session_index_tests.rs
index 33e4ae4a59..b7a4e67a62 100644
--- a/codex-rs/rollout/src/session_index_tests.rs
+++ b/codex-rs/rollout/src/session_index_tests.rs
@@ -231,6 +231,26 @@ fn scan_index_returns_none_when_entry_missing() -> std::io::Result<()> {
     Ok(())
 }
 
+#[tokio::test]
+async fn append_thread_name_replaces_existing_name_for_thread() -> std::io::Result<()> {
+    let temp = TempDir::new()?;
+    let thread_id = ThreadId::new();
+    let other_id = ThreadId::new();
+
+    append_thread_name(temp.path(), thread_id, "old").await?;
+    append_thread_name(temp.path(), other_id, "other").await?;
+    append_thread_name(temp.path(), thread_id, "new").await?;
+
+    let contents = std::fs::read_to_string(session_index_path(temp.path()))?;
+    assert!(contents.contains("\"thread_name\":\"new\""));
+    assert!(contents.contains("\"thread_name\":\"other\""));
+    assert!(!contents.contains("\"thread_name\":\"old\""));
+
+    let found = find_thread_name_by_id(temp.path(), &thread_id).await?;
+    assert_eq!(found, Some("new".to_string()));
+    Ok(())
+}
+
 #[tokio::test]
 async fn find_thread_names_by_ids_prefers_latest_entry() -> std::io::Result<()> {
     let temp = TempDir::new()?;
